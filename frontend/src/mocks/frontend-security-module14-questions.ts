// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 14 — OAuth & Identity).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m14-221",
    number: "SEC-M14-221",
    title: "OAuth roles",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**OAuth roles** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**OAuth roles** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse OAuth roles in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use OAuth roles in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-222",
    number: "SEC-M14-222",
    title: "Authorization Code Flow",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**Authorization Code Flow** is a frontend security topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before implementing it.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Authorization Code Flow** is a frontend security topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before implementing it.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse Authorization Code Flow as part of a layered security model with clear ownership and an auditable enforcement point.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Authorization Code Flow as part of a layered security model with clear ownership and an auditable enforcement point.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-223",
    number: "SEC-M14-223",
    title: "PKCE",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**PKCE** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**PKCE** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse PKCE in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use PKCE in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-224",
    number: "SEC-M14-224",
    title: "OIDC",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**OIDC** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**OIDC** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse OIDC in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use OIDC in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-225",
    number: "SEC-M14-225",
    title: "ID token",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**ID token** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**ID token** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse ID token in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use ID token in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-226",
    number: "SEC-M14-226",
    title: "Access token",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**Access token** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Access token** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Access token in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Access token in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-227",
    number: "SEC-M14-227",
    title: "Refresh token",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**Refresh token** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Refresh token** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Refresh token in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Refresh token in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-228",
    number: "SEC-M14-228",
    title: "State parameter",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**State parameter** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**State parameter** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse State parameter in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use State parameter in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-229",
    number: "SEC-M14-229",
    title: "Nonce",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**Nonce** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Nonce** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Nonce in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Nonce in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-230",
    number: "SEC-M14-230",
    title: "Redirect URI validation",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**Redirect URI validation** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Redirect URI validation** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Redirect URI validation in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Redirect URI validation in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-231",
    number: "SEC-M14-231",
    title: "Passkeys",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**Passkeys** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Passkeys** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Passkeys in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Passkeys in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-232",
    number: "SEC-M14-232",
    title: "WebAuthn",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**WebAuthn** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**WebAuthn** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse WebAuthn in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use WebAuthn in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-233",
    number: "SEC-M14-233",
    title: "FIDO2",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**FIDO2** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**FIDO2** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse FIDO2 in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use FIDO2 in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-234",
    number: "SEC-M14-234",
    title: "MFA",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**MFA** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**MFA** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse MFA in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use MFA in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m14-235",
    number: "SEC-M14-235",
    title: "Enterprise identity federation",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "OAuth & Identity",
    expectedAnswer: "**Enterprise identity federation** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Enterprise identity federation** is an identity and authentication topic. Explain the actors, protocol message flow, credential/token lifecycle, redirect validation, and which decisions must stay on the trusted identity or API server.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction validSession(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = validSession(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Enterprise identity federation in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Enterprise identity federation in a controlled identity flow with explicit expiry, state/nonce validation, redirect validation, and secure token/session handling.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE14_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));