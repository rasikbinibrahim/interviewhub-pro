// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 1 — Testing Fundamentals).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m1-1",
    number: "TESTQA-M1-1",
    title: "Software Testing and Quality",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Software Testing and Quality** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Software Testing and Quality** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-2",
    number: "TESTQA-M1-2",
    title: "Testing Pyramid",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Testing Pyramid** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Testing Pyramid** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-3",
    number: "TESTQA-M1-3",
    title: "Testing Trophy",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Testing Trophy** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Testing Trophy** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-4",
    number: "TESTQA-M1-4",
    title: "Manual vs Automated Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Manual vs Automated Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Manual vs Automated Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-5",
    number: "TESTQA-M1-5",
    title: "Unit Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Unit Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Unit Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-6",
    number: "TESTQA-M1-6",
    title: "Integration Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Integration Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Integration Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-7",
    number: "TESTQA-M1-7",
    title: "End-to-End Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**End-to-End Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**End-to-End Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-8",
    number: "TESTQA-M1-8",
    title: "Functional and Non-functional Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Functional and Non-functional Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Functional and Non-functional Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-9",
    number: "TESTQA-M1-9",
    title: "Smoke, Sanity and Regression Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Smoke, Sanity and Regression Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Smoke, Sanity and Regression Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-10",
    number: "TESTQA-M1-10",
    title: "Black-box and White-box Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Black-box and White-box Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Black-box and White-box Testing** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-11",
    number: "TESTQA-M1-11",
    title: "Test Coverage",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Test Coverage** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Test Coverage** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-12",
    number: "TESTQA-M1-12",
    title: "Test Strategy and Test Planning",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Test Strategy and Test Planning** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Test Strategy and Test Planning** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-13",
    number: "TESTQA-M1-13",
    title: "Test Isolation and Determinism",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Test Isolation and Determinism** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Test Isolation and Determinism** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-14",
    number: "TESTQA-M1-14",
    title: "Test Data and Fixtures",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Test Data and Fixtures** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Test Data and Fixtures** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-15",
    number: "TESTQA-M1-15",
    title: "Quality Engineering Best Practices",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Quality Engineering Best Practices** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Quality Engineering Best Practices** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
  {
    id: "testqa-m1-16",
    number: "TESTQA-M1-16",
    title: "Testing Fundamentals — Module Review",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Testing Fundamentals",
    expectedAnswer: "**Testing Fundamentals — Module Review** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Testing Fundamentals — Module Review** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Testing Fundamentals"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE1_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));