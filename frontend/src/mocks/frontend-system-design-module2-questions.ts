// Derived from frontend/src/document/Part_14_Frontend_System_Design_Master_Handbook.md (Module 2 — Frontend Architecture).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sysdesign-m2-21",
    number: "SYSDESIGN-M2-21",
    title: "How do you design a scalable React application architecture?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How do you design a scalable React application architecture?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-22",
    number: "SYSDESIGN-M2-22",
    title: "When would you choose feature-based architecture?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **When would you choose feature-based architecture?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-23",
    number: "SYSDESIGN-M2-23",
    title: "When would you choose layered architecture?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **When would you choose layered architecture?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-24",
    number: "SYSDESIGN-M2-24",
    title: "When is a monolith frontend appropriate?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **When is a monolith frontend appropriate?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-25",
    number: "SYSDESIGN-M2-25",
    title: "When should you consider micro frontends?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **When should you consider micro frontends?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-26",
    number: "SYSDESIGN-M2-26",
    title: "What are the trade-offs of Module Federation?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **What are the trade-offs of Module Federation?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-27",
    number: "SYSDESIGN-M2-27",
    title: "How should shared dependencies be managed?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How should shared dependencies be managed?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-28",
    number: "SYSDESIGN-M2-28",
    title: "How should shared UI libraries be structured?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How should shared UI libraries be structured?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-29",
    number: "SYSDESIGN-M2-29",
    title: "How do you design a BFF boundary for frontend applications?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How do you design a BFF boundary for frontend applications?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-30",
    number: "SYSDESIGN-M2-30",
    title: "How do you separate UI, domain, data and infrastructure concerns?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How do you separate UI, domain, data and infrastructure concerns?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-31",
    number: "SYSDESIGN-M2-31",
    title: "How do you apply Clean Architecture to a frontend?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How do you apply Clean Architecture to a frontend?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-32",
    number: "SYSDESIGN-M2-32",
    title: "How does Hexagonal Architecture map to frontend systems?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How does Hexagonal Architecture map to frontend systems?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-33",
    number: "SYSDESIGN-M2-33",
    title: "What is Vertical Slice Architecture?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **What is Vertical Slice Architecture?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-34",
    number: "SYSDESIGN-M2-34",
    title: "What is Domain-Driven Frontend?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **What is Domain-Driven Frontend?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-35",
    number: "SYSDESIGN-M2-35",
    title: "How do you organize cross-cutting concerns?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How do you organize cross-cutting concerns?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-36",
    number: "SYSDESIGN-M2-36",
    title: "How do you prevent dependency cycles?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How do you prevent dependency cycles?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-37",
    number: "SYSDESIGN-M2-37",
    title: "How do you evolve architecture without a rewrite?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How do you evolve architecture without a rewrite?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-38",
    number: "SYSDESIGN-M2-38",
    title: "How do you define architecture ownership across teams?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How do you define architecture ownership across teams?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-39",
    number: "SYSDESIGN-M2-39",
    title: "How do you design for independent team deployment?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How do you design for independent team deployment?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---"],
  },
  {
    id: "sysdesign-m2-40",
    number: "SYSDESIGN-M2-40",
    title: "How do you evaluate architecture trade-offs in an enterprise?",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Airbnb","Stripe","Atlassian","Uber"],
    category: "Frontend Architecture",
    expectedAnswer: "Design a production-grade frontend architecture for **How do you evaluate architecture trade-offs in an enterprise?**. The goal is to satisfy business needs while keeping the system scalable, secure, accessible, observable and maintainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useMemo, useState } from \"react\";\n\ntype Item = {\n  id: string;\n  label: string;\n};\n\ntype Props = {\n  items: readonly Item[];\n};\n\nexport function ExampleFeature({ items }: Props) {\n  const [selectedId, setSelectedId] = useState<string | null>(null);\n\n  const selected = useMemo(\n    () => items.find((item) => item.id === selectedId) ?? null,\n    [items, selectedId],\n  );\n\n  return (\n    <section aria-label=\"Example feature\">\n      <ul>\n        {items.map((item) => (\n          <li key={item.id}>\n            <button\n              type=\"button\"\n              aria-pressed={selectedId === item.id}\n              onClick={() => setSelectedId(item.id)}\n            >\n              {item.label}\n            </button>\n          </li>\n        ))}\n      </ul>\n\n      <p aria-live=\"polite\">\n        {selected ? `Selected: ${selected.label}` : \"Nothing selected\"}\n      </p>\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    productionExample: "```ts\n// Architecture-level configuration should be environment driven.\n// Never place production secrets in client-side source.\nexport const appConfig = {\n  apiBaseUrl: import.meta.env.VITE_API_BASE_URL,\n  appVersion: import.meta.env.VITE_APP_VERSION,\n} as const;\n```",
    bestPractices: ["Users can access the core capability through a responsive frontend.","UI state, server state and navigation state have explicit ownership.","Async operations expose loading, success, empty and failure states.","The system supports recovery from expected failures."],
    tradeOffs: "| Decision | Benefit | Cost / Risk |\n|---|---|---|\n| SSR | Fast content delivery/SEO | Server complexity |\n| CSR | Simple client model | Initial UX/SEO trade-offs |\n| Micro frontend | Team independence | Runtime/build complexity |\n| Monolith | Simpler operations | Larger coordination boundary |\n| Global state | Shared access | Coupling/re-render risk |\n| Local state | Encapsulation | Duplication if shared incorrectly |\n| CDN caching | Lower latency/origin load | Invalidation complexity |",
    commonMistakes: ["Assuming compile-time guarantees hold at runtime without validation."],
    followUpQuestions: ["What would you change at 10× traffic?","What breaks first and how would you detect it?","What would you cache and how would you invalidate it?","What state belongs on the server versus the client?","How would you make this accessible?","How would you secure the system?","How would you migrate a legacy implementation?","How would you test the architecture?","How would you roll out a risky change?","What would you monitor in production?"],
    relatedTopics: ["React Server Components","Server Actions","Edge Runtime","Islands Architecture","Partial Hydration","Module Federation","Micro Frontends","Nx","Turborepo","Design Tokens","Offline-first","CRDT","Event Sourcing","CQRS","BroadcastChannel","Service Workers","Web Workers","IndexedDB","Feature Flags","BFF","GraphQL Federation","Multi-region","White-label","Multi-tenant","AI-assisted Frontend Architecture","---","# Module 3 – Component Design","**Questions 41–60**"],
  },
];

export const MOCK_FRONTEND_SYSTEM_DESIGN_MODULE2_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'System Design',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
