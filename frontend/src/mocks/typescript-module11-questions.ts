// Updated TypeScript interview question bank.
// Source metadata preserved; repeated generic examples normalized into topic-specific coding examples.
import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "ts-m11-176",
    number: "TS-M11-176",
    title: "ES modules",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Modules",
    expectedAnswer: "ES modules use explicit import/export boundaries. TypeScript type-checks those boundaries and the configured toolchain emits or bundles JavaScript modules.",
    deepExplanation: "Step 1 — Understand the topic.\nES modules use explicit import/export boundaries. TypeScript type-checks those boundaries and the configured toolchain emits or bundles JavaScript modules.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nexport type User = {\n  id: string;\n};\n\nexport function getUserId(\n  user: User,\n): string {\n  return user.id;\n}\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nimport {\n  getUserId,\n} from \"./user\";\n\nconsole.log(\n  getUserId({ id: \"u1\" }),\n);\n```\n\nStep 5 — Example result:\n```text\nu1\n```\n\nStep 6 — Complexity / trade-off.\nKeep module boundaries explicit and avoid hidden global dependencies.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **ES modules**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **ES modules**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m11-177",
    number: "TS-M11-177",
    title: "Named exports",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Modules",
    expectedAnswer: "Named exports expose specific symbols by name and make import dependencies explicit.",
    deepExplanation: "Step 1 — Understand the topic.\nNamed exports expose specific symbols by name and make import dependencies explicit.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nexport function add(\n  left: number,\n  right: number,\n): number {\n  return left + right;\n}\n\nexport const version = 1;\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nimport {\n  add,\n  version,\n} from \"./math\";\n\nconsole.log(add(2, 3), version);\n```\n\nStep 5 — Example result:\n```text\n5 1\n```\n\nStep 6 — Complexity / trade-off.\nPrefer named exports when a module exposes multiple stable concepts.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Named exports**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Named exports**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m11-178",
    number: "TS-M11-178",
    title: "Default exports",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Modules",
    expectedAnswer: "Default exports expose one primary module value and can be imported with any local name, which can reduce naming consistency.",
    deepExplanation: "Step 1 — Understand the topic.\nDefault exports expose one primary module value and can be imported with any local name, which can reduce naming consistency.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nexport default function add(\n  left: number,\n  right: number,\n): number {\n  return left + right;\n}\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nimport add from \"./add\";\n\nconsole.log(add(2, 3));\n```\n\nStep 5 — Example result:\n```text\n5\n```\n\nStep 6 — Complexity / trade-off.\nUse default exports when the module clearly has one primary value; use named exports when stable symbol names matter.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Default exports**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Default exports**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m11-179",
    number: "TS-M11-179",
    title: "Type-only imports",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Modules",
    expectedAnswer: "Type-only imports should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nType-only imports should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Type-only imports**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Type-only imports**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m11-180",
    number: "TS-M11-180",
    title: "Type-only exports",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Modules",
    expectedAnswer: "Type-only exports should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nType-only exports should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Type-only exports**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Type-only exports**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m11-181",
    number: "TS-M11-181",
    title: "Module resolution",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Modules",
    expectedAnswer: "Module resolution should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nModule resolution should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Module resolution**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Module resolution**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m11-182",
    number: "TS-M11-182",
    title: "Path aliases",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Modules",
    expectedAnswer: "Path aliases should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nPath aliases should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Path aliases**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Path aliases**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m11-183",
    number: "TS-M11-183",
    title: "Barrel files",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Modules",
    expectedAnswer: "Re-exports expose symbols from another module, while barrel files can create concise imports but may increase coupling or affect tree-shaking.",
    deepExplanation: "Step 1 — Understand the topic.\nRe-exports expose symbols from another module, while barrel files can create concise imports but may increase coupling or affect tree-shaking.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nexport { add } from \"./math\";\nexport type { User } from \"./user\";\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nimport {\n  add,\n} from \"./index\";\n```\n\nStep 5 — Example result:\n```text\nshared export boundary\n```\n\nStep 6 — Complexity / trade-off.\nKeep barrels intentional and avoid circular dependency chains.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Barrel files**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Barrel files**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m11-184",
    number: "TS-M11-184",
    title: "Circular dependencies",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Modules",
    expectedAnswer: "Circular dependencies occur when modules depend on each other directly or indirectly and can produce initialization/order problems.",
    deepExplanation: "Step 1 — Understand the topic.\nCircular dependencies occur when modules depend on each other directly or indirectly and can produce initialization/order problems.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\n// a.ts -> b.ts\n// b.ts -> a.ts\n// Better: move shared contract to c.ts.\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\n// shared.ts\nexport type User = {\n  id: string;\n};\n\n// user.ts imports from shared.ts\n// order.ts imports from shared.ts\n```\n\nStep 5 — Example result:\n```text\nacyclic shared boundary\n```\n\nStep 6 — Complexity / trade-off.\nBreak cycles with dependency inversion, shared contracts, or feature boundaries.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Circular dependencies**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Circular dependencies**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m11-185",
    number: "TS-M11-185",
    title: "Module architecture",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Modules",
    expectedAnswer: "Module architecture should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nModule architecture should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Module architecture**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Module architecture**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos", "# Module 12 – Error Handling", "**Questions 186–195**"],
  },
];

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};

export const MOCK_TYPESCRIPT_MODULE11_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'TypeScript',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
