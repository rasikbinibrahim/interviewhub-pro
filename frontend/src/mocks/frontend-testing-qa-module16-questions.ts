// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 16 — Mobile & Cross-browser Testing).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m16-1",
    number: "TESTQA-M16-1",
    title: "Browser Compatibility",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Browser Compatibility** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Browser Compatibility** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nRun the supported browser matrix with consistent data and isolate engine-specific failures.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Run the supported browser matrix with consistent data and isolate engine-specific failures.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-2",
    number: "TESTQA-M16-2",
    title: "Chrome",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Chrome** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Chrome** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nRun the supported browser matrix with consistent data and isolate engine-specific failures.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Run the supported browser matrix with consistent data and isolate engine-specific failures.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-3",
    number: "TESTQA-M16-3",
    title: "Firefox",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Firefox** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Firefox** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nRun the supported browser matrix with consistent data and isolate engine-specific failures.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Run the supported browser matrix with consistent data and isolate engine-specific failures.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-4",
    number: "TESTQA-M16-4",
    title: "Safari",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Safari** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Safari** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nRun the supported browser matrix with consistent data and isolate engine-specific failures.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Run the supported browser matrix with consistent data and isolate engine-specific failures.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-5",
    number: "TESTQA-M16-5",
    title: "Edge",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Edge** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Edge** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nRun the supported browser matrix with consistent data and isolate engine-specific failures.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Run the supported browser matrix with consistent data and isolate engine-specific failures.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-6",
    number: "TESTQA-M16-6",
    title: "Mobile Browsers",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Mobile Browsers** should cover mobile viewport, touch, performance, network variation, and platform-specific browser behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Mobile Browsers** should cover mobile viewport, touch, performance, network variation, and platform-specific browser behavior.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nCombine browser compatibility with mobile viewport, touch, performance, and network variation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Combine browser compatibility with mobile viewport, touch, performance, and network variation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-7",
    number: "TESTQA-M16-7",
    title: "Responsive Layouts",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Responsive Layouts** should verify important breakpoints, layout transitions, content wrapping, overflow, and touch targets using representative viewport sizes.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Responsive Layouts** should verify important breakpoints, layout transitions, content wrapping, overflow, and touch targets using representative viewport sizes.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nTest representative breakpoints and content extremes rather than every possible pixel width.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test representative breakpoints and content extremes rather than every possible pixel width.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-8",
    number: "TESTQA-M16-8",
    title: "Device Emulation",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Device Emulation** should reproduce representative viewport/device conditions rather than relying on one desktop browser.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Device Emulation** should reproduce representative viewport/device conditions rather than relying on one desktop browser.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nUse representative device/viewport combinations rather than arbitrary emulation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use representative device/viewport combinations rather than arbitrary emulation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-9",
    number: "TESTQA-M16-9",
    title: "Touch Events",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Touch Events** should verify touch targets, gestures, scrolling, cancellation, and pointer/touch interoperability.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Touch Events** should verify touch targets, gestures, scrolling, cancellation, and pointer/touch interoperability.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nVerify gestures, target size, scrolling, pointer/touch interactions, and cancellation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Verify gestures, target size, scrolling, pointer/touch interactions, and cancellation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-10",
    number: "TESTQA-M16-10",
    title: "Orientation Changes",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Orientation Changes** should verify layout, state preservation, and interaction behavior across portrait and landscape transitions.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Orientation Changes** should verify layout, state preservation, and interaction behavior across portrait and landscape transitions.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nVerify portrait/landscape layout and state preservation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Verify portrait/landscape layout and state preservation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-11",
    number: "TESTQA-M16-11",
    title: "Progressive Web Apps",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Progressive Web Apps** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Progressive Web Apps** should verify the same critical behavior against the supported browser engine/version matrix while isolating browser-specific failures.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nRun the supported browser matrix with consistent data and isolate engine-specific failures.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Run the supported browser matrix with consistent data and isolate engine-specific failures.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-12",
    number: "TESTQA-M16-12",
    title: "Polyfills",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Polyfills** should verify feature support detection and only load compatibility code where required.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Polyfills** should verify feature support detection and only load compatibility code where required.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nVerify feature detection and only load compatibility code where needed.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Verify feature detection and only load compatibility code where needed.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-13",
    number: "TESTQA-M16-13",
    title: "Viewport and DPR Testing",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Viewport and DPR Testing** should verify responsive layout under representative viewport dimensions, DPR, zoom, and text-size conditions.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Viewport and DPR Testing** should verify responsive layout under representative viewport dimensions, DPR, zoom, and text-size conditions.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nTest viewport, DPR, zoom, text-size, and responsive layout assumptions.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test viewport, DPR, zoom, text-size, and responsive layout assumptions.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-14",
    number: "TESTQA-M16-14",
    title: "Cross-browser Automation",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Cross-browser Automation** should use stable automation selectors, deterministic environments, and explicit browser/device coverage.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Cross-browser Automation** should use stable automation selectors, deterministic environments, and explicit browser/device coverage.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nUse stable selectors, deterministic data, and explicit device/browser coverage.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use stable selectors, deterministic data, and explicit device/browser coverage.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-15",
    number: "TESTQA-M16-15",
    title: "Enterprise Compatibility Matrix",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Enterprise Compatibility Matrix** should define ownership, supported environments, baseline governance, CI integration, retention, and failure triage.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Enterprise Compatibility Matrix** should define ownership, supported environments, baseline governance, CI integration, retention, and failure triage.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Check {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(checks: readonly Check[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst checks = [\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse the smallest deterministic test that provides meaningful confidence.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use the smallest deterministic test that provides meaningful confidence.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
  {
    id: "testqa-m16-16",
    number: "TESTQA-M16-16",
    title: "Mobile & Cross-browser Testing — Module Review",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Mobile & Cross-browser Testing",
    expectedAnswer: "**Mobile & Cross-browser Testing — Module Review** should cover mobile viewport, touch, performance, network variation, and platform-specific browser behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Mobile & Cross-browser Testing — Module Review** should cover mobile viewport, touch, performance, network variation, and platform-specific browser behavior.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Environment {\n  name: string;\n  supported: boolean;\n}\n\nfunction supportedEnvironments(\n  environments: readonly Environment[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < environments.length; i += 1) {\n    if (environments[i].supported) {\n      result.push(environments[i].name);\n    }\n  }\n\n  return result;\n}\n\nconst supported = supportedEnvironments([\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst environments = [\n  { name: \"Chrome\", supported: true },\n  { name: \"Safari\", supported: true },\n];\n\nconst supported = environments\n  .filter((env) => env.supported)\n  .map((env) => env.name);\n```\n\nStep 5 — Production example:\nCombine browser compatibility with mobile viewport, touch, performance, and network variation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Combine browser compatibility with mobile viewport, touch, performance, and network variation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Mobile & Cross-browser Testing"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE16_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));