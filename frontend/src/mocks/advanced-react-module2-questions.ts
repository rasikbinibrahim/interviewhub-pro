// UPDATED: Module 2 — React Fiber Architecture
// Question-specific senior-level answers, explanations, examples, trade-offs,
// mistakes, follow-ups, and related topics. Original question metadata is preserved.
// Answers are aligned to the actual title of each question instead of reusing generic templates.

// Derived from frontend/src/document/Part_9_Advanced_React_Master_Handbook.md (Module 2 — React Fiber Architecture).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "reactadv-m2-26",
    number: "REACTADV-M2-26",
    title: "Fiber architecture",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "Fiber is React's internal tree representation and unit of work. Each Fiber describes work for a component or host node and carries information React needs for reconciliation, scheduling, context, refs, effects, and relationships in the tree. Fiber is an implementation detail, not a public application API.",
    deepExplanation: "Fiber is React's internal tree representation and unit of work. Each Fiber describes work for a component or host node and carries information React needs for reconciliation, scheduling, context, refs, effects, and relationships in the tree. Fiber is an implementation detail, not a public application API.\n\n```tsx\nfunction Dashboard({ userId }: { userId: string }) {\n  return (\n    <main>\n      <h1>Dashboard</h1>\n      <Profile userId={userId} />\n    </main>\n  );\n}\n```\nDuring rendering React creates or updates Fiber nodes for the component tree. The reconciler determines what changed, while the commit phase applies the required host mutations.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "```tsx\nfunction Dashboard({ userId }: { userId: string }) {\n  return (\n    <main>\n      <h1>Dashboard</h1>\n      <Profile userId={userId} />\n    </main>\n  );\n}\n```\nDuring rendering React creates or updates Fiber nodes for the component tree. The reconciler determines what changed, while the commit phase applies the required host mutations.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Enables interruptible work, reconciliation, prioritization, and scalable rendering. Trade-offs: Fiber adds internal complexity and memory overhead; application code should not couple to its implementation details.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Fiber node structure", "Reconciliation", "Scheduler integration", "Render phase", "Commit phase", "Lanes", "Concurrent rendering", "Profiling"],
  },
  {
    id: "reactadv-m2-27",
    number: "REACTADV-M2-27",
    title: "Fiber node structure",
    difficulty: "Hard",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "A Fiber node stores React's internal bookkeeping for one unit in the rendered tree. Important concepts include the component/type identity, key, props, state, parent/child/sibling relationships, alternate work-in-progress node, lanes, and effect-related flags. Exact internal fields can change between React releases.",
    deepExplanation: "A Fiber node stores React's internal bookkeeping for one unit in the rendered tree. Important concepts include the component/type identity, key, props, state, parent/child/sibling relationships, alternate work-in-progress node, lanes, and effect-related flags. Exact internal fields can change between React releases.\n\n```text\nParent Fiber\n├── Child Fiber A\n│   └── Grandchild Fiber\n└── Child Fiber B\n\nalternate ↔ work-in-progress/current counterpart\n```\nThe important interview point is the relationships and lifecycle, not memorizing private field names.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "```text\nParent Fiber\n├── Child Fiber A\n│   └── Grandchild Fiber\n└── Child Fiber B\n\nalternate ↔ work-in-progress/current counterpart\n```\nThe important interview point is the relationships and lifecycle, not memorizing private field names.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Rich bookkeeping lets React schedule and reconcile work efficiently. Trade-offs: The structure is complex and version-dependent, so internals are unsuitable as a stable application contract.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Fiber architecture", "Alternate fibers", "Current and work-in-progress trees", "Keys", "Reconciliation"],
  },
  {
    id: "reactadv-m2-28",
    number: "REACTADV-M2-28",
    title: "Alternate fibers",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "React commonly maintains a current Fiber tree and a work-in-progress counterpart. The alternate relationship lets React prepare the next tree without immediately replacing the currently committed UI. After a successful commit, the prepared tree becomes current.",
    deepExplanation: "React commonly maintains a current Fiber tree and a work-in-progress counterpart. The alternate relationship lets React prepare the next tree without immediately replacing the currently committed UI. After a successful commit, the prepared tree becomes current.\n\n```text\nCurrent tree\n   ↕ alternate\nWork-in-progress tree\n\nUser sees committed current UI while React prepares the next version.\n```\nThis is an internal model that supports interruption, reuse, and safe commit boundaries.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "```text\nCurrent tree\n   ↕ alternate\nWork-in-progress tree\n\nUser sees committed current UI while React prepares the next version.\n```\nThis is an internal model that supports interruption, reuse, and safe commit boundaries.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Separates preparation from committed UI and supports concurrent scheduling. Trade-offs: Maintaining alternate structures consumes memory and increases implementation complexity.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Current tree", "Work-in-progress tree", "Commit phase", "Concurrent rendering", "Fiber node structure"],
  },
  {
    id: "reactadv-m2-29",
    number: "REACTADV-M2-29",
    title: "Current and work-in-progress trees",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "The current tree represents the UI that has been committed. The work-in-progress tree represents React's next version while it performs reconciliation. React can prepare work incrementally and then commit the finished result, which is a key part of concurrent rendering.",
    deepExplanation: "The current tree represents the UI that has been committed. The work-in-progress tree represents React's next version while it performs reconciliation. React can prepare work incrementally and then commit the finished result, which is a key part of concurrent rendering.\n\n```tsx\nconst [query, setQuery] = useState(\"\");\n\nreturn (\n  <>\n    <input value={query} onChange={e => setQuery(e.target.value)} />\n    <Results query={query} />\n  </>\n);\n```\nAfter `setQuery`, React can prepare updated Fibers in the work-in-progress tree before committing the final host changes.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "```tsx\nconst [query, setQuery] = useState(\"\");\n\nreturn (\n  <>\n    <input value={query} onChange={e => setQuery(e.target.value)} />\n    <Results query={query} />\n  </>\n);\n```\nAfter `setQuery`, React can prepare updated Fibers in the work-in-progress tree before committing the final host changes.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Allows React to prepare, pause, abandon, or retry rendering before committing. Trade-offs: More complex scheduling semantics mean developers must avoid render-time side effects and stale assumptions.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Alternate fibers", "Render phase", "Commit phase", "Concurrent rendering", "Transitions"],
  },
  {
    id: "reactadv-m2-30",
    number: "REACTADV-M2-30",
    title: "Fiber reconciliation",
    difficulty: "Easy",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "Fiber reconciliation compares the previous and next element trees and determines which component instances can be preserved, updated, moved, inserted, or removed. Type and key are central to identity. Fiber provides the data structure through which this work is performed incrementally.",
    deepExplanation: "Fiber reconciliation compares the previous and next element trees and determines which component instances can be preserved, updated, moved, inserted, or removed. Type and key are central to identity. Fiber provides the data structure through which this work is performed incrementally.\n\ntsx\nconst next = items.map(item => (\n  <Row key={item.id} item={item} />\n));\n```\nStable `item.id` keys let React match rows across renders. Changing keys can cause stateful children to be recreated.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nconst next = items.map(item => (\n  <Row key={item.id} item={item} />\n));\n```\nStable `item.id` keys let React match rows across renders. Changing keys can cause stateful children to be recreated.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Efficiently reuses existing component/host work where identity matches. Trade-offs: Incorrect identity keys can cause state loss, stale UI, or unnecessary work.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Reconciliation", "Keys", "Fiber identity", "Bailouts", "Commit phase"],
  },
  {
    id: "reactadv-m2-31",
    number: "REACTADV-M2-31",
    title: "Render phase internals",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "The render phase is React's calculation phase. React traverses relevant Fibers, calls components, processes updates, reconciles children, and builds the next Fiber state. It should be pure because React may start, pause, restart, or discard render work in concurrent environments.",
    deepExplanation: "The render phase is React's calculation phase. React traverses relevant Fibers, calls components, processes updates, reconciles children, and builds the next Fiber state. It should be pure because React may start, pause, restart, or discard render work in concurrent environments.\n\ntsx\nfunction Price({ amount }: { amount: number }) {\n  const formatted = new Intl.NumberFormat(\"en-IN\", {\n    style: \"currency\",\n    currency: \"INR\",\n  }).format(amount);\n\n  return <span>{formatted}</span>;\n}\n```\nFormatting is render-time computation; it should not mutate external systems.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nfunction Price({ amount }: { amount: number }) {\n  const formatted = new Intl.NumberFormat(\"en-IN\", {\n    style: \"currency\",\n    currency: \"INR\",\n  }).format(amount);\n\n  return <span>{formatted}</span>;\n}\n```\nFormatting is render-time computation; it should not mutate external systems.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Pure calculation makes rendering interruptible and predictable. Trade-offs: Render code may execute more often than expected, so expensive or side-effectful work can become costly or incorrect.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Render phase", "Commit phase", "Purity", "Concurrent rendering", "Memoization"],
  },
  {
    id: "reactadv-m2-32",
    number: "REACTADV-M2-32",
    title: "Commit phase internals",
    difficulty: "Hard",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "The commit phase applies the result of reconciliation to the host environment. React performs required DOM mutations and runs the appropriate lifecycle/effect-related work around the commit. Unlike render work, the commit must produce the externally visible update for that finished render.",
    deepExplanation: "The commit phase applies the result of reconciliation to the host environment. React performs required DOM mutations and runs the appropriate lifecycle/effect-related work around the commit. Unlike render work, the commit must produce the externally visible update for that finished render.\n\ntsx\nfunction Panel({ open }: { open: boolean }) {\n  return <section hidden={!open}>Panel content</section>;\n}\n```\nReact may calculate this change during render, then apply the corresponding host update during commit.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nfunction Panel({ open }: { open: boolean }) {\n  return <section hidden={!open}>Panel content</section>;\n}\n```\nReact may calculate this change during render, then apply the corresponding host update during commit.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Provides a controlled boundary for visible mutations and side effects. Trade-offs: Large commit work can block the main thread even when rendering itself was schedulable.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Commit phase", "Render phase", "useLayoutEffect", "Effects", "Browser paint"],
  },
  {
    id: "reactadv-m2-33",
    number: "REACTADV-M2-33",
    title: "Scheduler integration",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "React scheduling decides when rendering work should be performed and which work should receive priority. Modern React exposes scheduling behavior mainly through public concepts such as transitions, deferred values, Suspense, and event priority rather than requiring applications to call internal scheduler APIs.",
    deepExplanation: "React scheduling decides when rendering work should be performed and which work should receive priority. Modern React exposes scheduling behavior mainly through public concepts such as transitions, deferred values, Suspense, and event priority rather than requiring applications to call internal scheduler APIs.\n\ntsx\nconst [isPending, startTransition] = useTransition();\n\nfunction selectTab(tab: string) {\n  startTransition(() => {\n    setTab(tab);\n  });\n}\n```\nThe transition tells React that this update is non-urgent relative to interactions such as typing or clicking.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nconst [isPending, startTransition] = useTransition();\n\nfunction selectTab(tab: string) {\n  startTransition(() => {\n    setTab(tab);\n  });\n}\n```\nThe transition tells React that this update is non-urgent relative to interactions such as typing or clicking.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Lets React prioritize user interactions over expensive non-urgent rendering. Trade-offs: Deferred work can appear later, and developers must design loading/pending states correctly.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Scheduler", "Lanes", "Transitions", "Concurrent rendering", "Suspense", "Event priority"],
  },
  {
    id: "reactadv-m2-34",
    number: "REACTADV-M2-34",
    title: "Lane priorities",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "Lanes are an internal mechanism React uses to represent priority and grouping of pending work. Different updates can be assigned different lanes so React can choose which work to process first and combine compatible work. Lane bitmasks are implementation details, not an application-level API.",
    deepExplanation: "Lanes are an internal mechanism React uses to represent priority and grouping of pending work. Different updates can be assigned different lanes so React can choose which work to process first and combine compatible work. Lane bitmasks are implementation details, not an application-level API.\n\ntsx\nconst [isPending, startTransition] = useTransition();\n\nstartTransition(() => {\n  setSearchResults(expensiveResults);\n});\n```\nThe important application-level concept is that this update is non-urgent; React internally represents and schedules it using its priority model.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nconst [isPending, startTransition] = useTransition();\n\nstartTransition(() => {\n  setSearchResults(expensiveResults);\n});\n```\nThe important application-level concept is that this update is non-urgent; React internally represents and schedules it using its priority model.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Fine-grained internal scheduling and batching. Trade-offs: Priority bookkeeping is complex and can change across React versions.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Scheduler", "Transitions", "Update queues", "Concurrent rendering", "Priority"],
  },
  {
    id: "reactadv-m2-35",
    number: "REACTADV-M2-35",
    title: "Update queues",
    difficulty: "Easy",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "React state updates are represented internally so they can be processed according to the current render and priority. For hooks and class components, React can accumulate pending updates, process them in order, and derive the next state. Functional updates are especially important when multiple updates depend on previous state.",
    deepExplanation: "React state updates are represented internally so they can be processed according to the current render and priority. For hooks and class components, React can accumulate pending updates, process them in order, and derive the next state. Functional updates are especially important when multiple updates depend on previous state.\n\ntsx\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  function incrementThreeTimes() {\n    setCount(c => c + 1);\n    setCount(c => c + 1);\n    setCount(c => c + 1);\n  }\n\n  return <button onClick={incrementThreeTimes}>{count}</button>;\n}\n```\nThe functional form composes updates against the latest state during processing.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  function incrementThreeTimes() {\n    setCount(c => c + 1);\n    setCount(c => c + 1);\n    setCount(c => c + 1);\n  }\n\n  return <button onClick={incrementThreeTimes}>{count}</button>;\n}\n```\nThe functional form composes updates against the latest state during processing.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Supports batching, priority-aware processing, and predictable state transitions. Trade-offs: Scheduling means state updates are not equivalent to immediate variable assignment.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["State scheduling", "Batching", "Functional updates", "useReducer", "Lanes"],
  },
  {
    id: "reactadv-m2-36",
    number: "REACTADV-M2-36",
    title: "State update scheduling",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "Calling a state setter schedules an update; it does not mutate the current render's state variable. React may batch compatible updates, assign priority, render a new tree, and commit it later. Code should therefore describe the desired next state rather than assume synchronous mutation.",
    deepExplanation: "Calling a state setter schedules an update; it does not mutate the current render's state variable. React may batch compatible updates, assign priority, render a new tree, and commit it later. Code should therefore describe the desired next state rather than assume synchronous mutation.\n\ntsx\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  const handleClick = () => {\n    setCount(c => c + 1);\n    console.log(count); // still the value from this render\n  };\n\n  return <button onClick={handleClick}>{count}</button>;\n}\n```\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  const handleClick = () => {\n    setCount(c => c + 1);\n    console.log(count); // still the value from this render\n  };\n\n  return <button onClick={handleClick}>{count}</button>;\n}\n```",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Batching and scheduling reduce unnecessary rendering and improve responsiveness. Trade-offs: Developers must reason in render snapshots rather than mutable synchronous state.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Update queues", "Batching", "State snapshots", "Transitions", "Functional updates"],
  },
  {
    id: "reactadv-m2-37",
    number: "REACTADV-M2-37",
    title: "Fiber traversal",
    difficulty: "Hard",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "Fiber traversal is the internal process of visiting relevant nodes during rendering and commit-related work. The Fiber tree's child, sibling, and parent/return relationships allow React to move through the tree without depending on recursive JavaScript call stacks for all work, which supports incremental processing.",
    deepExplanation: "Fiber traversal is the internal process of visiting relevant nodes during rendering and commit-related work. The Fiber tree's child, sibling, and parent/return relationships allow React to move through the tree without depending on recursive JavaScript call stacks for all work, which supports incremental processing.\n\ntext\nParent\n  ├─ child A\n  │   └─ child A1\n  └─ child B\n\nTraversal can move down to child, across to sibling, and back to the parent.\n```\nThis relationship model is internal and should not be manipulated by application code.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "text\nParent\n  ├─ child A\n  │   └─ child A1\n  └─ child B\n\nTraversal can move down to child, across to sibling, and back to the parent.\n```\nThis relationship model is internal and should not be manipulated by application code.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Supports incremental traversal and scheduling of rendering work. Trade-offs: Internal traversal logic is complex and version-dependent.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Fiber structure", "Bailouts", "Reconciliation", "Scheduler", "Render phase"],
  },
  {
    id: "reactadv-m2-38",
    number: "REACTADV-M2-38",
    title: "Bailouts",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "A bailout means React can avoid unnecessary work for a subtree when it determines that relevant inputs have not changed or the subtree can otherwise be reused. Memoized components, stable props, unchanged state, and reconciliation decisions can contribute to avoiding work. A bailout is an optimization, not a correctness requirement.",
    deepExplanation: "A bailout means React can avoid unnecessary work for a subtree when it determines that relevant inputs have not changed or the subtree can otherwise be reused. Memoized components, stable props, unchanged state, and reconciliation decisions can contribute to avoiding work. A bailout is an optimization, not a correctness requirement.\n\ntsx\nconst Row = memo(function Row({ label }: { label: string }) {\n  return <li>{label}</li>;\n});\n\nfunction List({ labels, selected }: { labels: string[]; selected: string }) {\n  return labels.map(label => <Row key={label} label={label} />);\n}\n```\nIf a row's props remain referentially/effectively unchanged, `memo` can allow React to skip rendering that child.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nconst Row = memo(function Row({ label }: { label: string }) {\n  return <li>{label}</li>;\n});\n\nfunction List({ labels, selected }: { labels: string[]; selected: string }) {\n  return labels.map(label => <Row key={label} label={label} />);\n}\n```\nIf a row's props remain referentially/effectively unchanged, `memo` can allow React to skip rendering that child.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Reduces unnecessary render work. Trade-offs: Comparisons and memoization bookkeeping have costs and can make code harder to understand.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["React.memo", "useMemo", "useCallback", "Reconciliation", "Performance"],
  },
  {
    id: "reactadv-m2-39",
    number: "REACTADV-M2-39",
    title: "Subtree flags",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "React internally records flags describing work that needs to be committed. Subtree-level metadata lets React know whether descendants contain relevant work so commit processing can avoid scanning unrelated parts of the tree. The exact flag names and representation are private implementation details.",
    deepExplanation: "React internally records flags describing work that needs to be committed. Subtree-level metadata lets React know whether descendants contain relevant work so commit processing can avoid scanning unrelated parts of the tree. The exact flag names and representation are private implementation details.\n\ntsx\nfunction Profile({ user }: { user: { name: string } }) {\n  return <section aria-label=\"Profile\">{user.name}</section>;\n}\n```\nIf a render produces no relevant host/effect changes for a subtree, React can avoid unnecessary commit work. Application code should reason from observable behavior rather than inspecting flags.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nfunction Profile({ user }: { user: { name: string } }) {\n  return <section aria-label=\"Profile\">{user.name}</section>;\n}\n```\nIf a render produces no relevant host/effect changes for a subtree, React can avoid unnecessary commit work. Application code should reason from observable behavior rather than inspecting flags.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Helps React efficiently identify where commit work exists. Trade-offs: Additional bookkeeping increases internal complexity and is subject to change.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Commit phase", "Effect flags", "Fiber traversal", "Bailouts", "Profiling"],
  },
  {
    id: "reactadv-m2-40",
    number: "REACTADV-M2-40",
    title: "Effect lists and commit work",
    difficulty: "Easy",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "React tracks which Fibers require commit-related work so it can apply mutations and run the appropriate effects without treating the entire tree as changed. Modern React's exact internal effect-list implementation can change, so the stable concept is selective commit work driven by reconciliation metadata.",
    deepExplanation: "React tracks which Fibers require commit-related work so it can apply mutations and run the appropriate effects without treating the entire tree as changed. Modern React's exact internal effect-list implementation can change, so the stable concept is selective commit work driven by reconciliation metadata.\n\ntsx\nuseEffect(() => {\n  const subscription = subscribe(userId);\n\n  return () => subscription.unsubscribe();\n}, [userId]);\n```\nThe component declares synchronization; React coordinates its lifecycle around commits. The application should not depend on a private effect-list structure.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nuseEffect(() => {\n  const subscription = subscribe(userId);\n\n  return () => subscription.unsubscribe();\n}, [userId]);\n```\nThe component declares synchronization; React coordinates its lifecycle around commits. The application should not depend on a private effect-list structure.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Allows selective processing of mutation and effect work. Trade-offs: Effect scheduling is subtle and incorrect effects can cause loops, stale data, or resource leaks.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Effects", "Commit phase", "useEffect", "useLayoutEffect", "Cleanup"],
  },
  {
    id: "reactadv-m2-41",
    number: "REACTADV-M2-41",
    title: "Concurrent rendering internals",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "Concurrent rendering lets React prepare rendering work in a way that can be interrupted, restarted, deprioritized, or abandoned before commit. It is primarily a scheduling/rendering capability; it does not mean JavaScript components execute simultaneously on multiple threads or that two DOM versions are simultaneously interactive.",
    deepExplanation: "Concurrent rendering lets React prepare rendering work in a way that can be interrupted, restarted, deprioritized, or abandoned before commit. It is primarily a scheduling/rendering capability; it does not mean JavaScript components execute simultaneously on multiple threads or that two DOM versions are simultaneously interactive.\n\ntsx\nconst [isPending, startTransition] = useTransition();\n\nfunction updateFilter(value: string) {\n  setInput(value); // urgent\n  startTransition(() => {\n    setFilter(value); // non-urgent\n  });\n}\n```\nReact can prioritize the input response while preparing the more expensive filter update.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nconst [isPending, startTransition] = useTransition();\n\nfunction updateFilter(value: string) {\n  setInput(value); // urgent\n  startTransition(() => {\n    setFilter(value); // non-urgent\n  });\n}\n```\nReact can prioritize the input response while preparing the more expensive filter update.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Better responsiveness by allowing urgent work to take precedence. Trade-offs: More complex timing semantics and the need to handle interrupted work and pending UI correctly.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Concurrent React", "Transitions", "Scheduler", "Lanes", "Suspense"],
  },
  {
    id: "reactadv-m2-42",
    number: "REACTADV-M2-42",
    title: "Suspense boundaries in Fiber",
    difficulty: "Hard",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "Suspense lets a component tree declare a fallback while React waits for supported asynchronous rendering work, such as lazy-loaded code or framework-integrated data. The boundary gives React a unit for coordinating loading UI and reveal behavior.",
    deepExplanation: "Suspense lets a component tree declare a fallback while React waits for supported asynchronous rendering work, such as lazy-loaded code or framework-integrated data. The boundary gives React a unit for coordinating loading UI and reveal behavior.\n\ntsx\nconst Reports = lazy(() => import(\"./Reports\"));\n\nfunction App() {\n  return (\n    <Suspense fallback={<p role=\"status\">Loading reports…</p>}>\n      <Reports />\n    </Suspense>\n  );\n}\n```\nThe boundary controls what the user sees while the suspended subtree is not ready.\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nconst Reports = lazy(() => import(\"./Reports\"));\n\nfunction App() {\n  return (\n    <Suspense fallback={<p role=\"status\">Loading reports…</p>}>\n      <Reports />\n    </Suspense>\n  );\n}\n```\nThe boundary controls what the user sees while the suspended subtree is not ready.",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Declarative loading states and coordination with concurrent rendering. Trade-offs: Boundary placement affects perceived performance and UX; incorrect granularity can cause excessive fallback replacement or poor loading states.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Suspense", "lazy", "Transitions", "Concurrent rendering", "Streaming SSR"],
  },
  {
    id: "reactadv-m2-43",
    number: "REACTADV-M2-43",
    title: "Error propagation",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "React error propagation should be handled according to the failure source. Error boundaries catch rendering-related errors in their descendant tree, while event-handler exceptions and many asynchronous failures require explicit handling. A production boundary should provide recovery UI and report sanitized diagnostics.",
    deepExplanation: "React error propagation should be handled according to the failure source. Error boundaries catch rendering-related errors in their descendant tree, while event-handler exceptions and many asynchronous failures require explicit handling. A production boundary should provide recovery UI and report sanitized diagnostics.\n\ntsx\nclass AppBoundary extends Component<\n  { children: ReactNode },\n  { failed: boolean }\n> {\n  state = { failed: false };\n\n  static getDerivedStateFromError() {\n    return { failed: true };\n  }\n\n  componentDidCatch(error: Error, info: ErrorInfo) {\n    reportError({ message: error.message, componentStack: info.componentStack });\n  }\n\n  render() {\n    return this.state.failed\n      ? <button onClick={() => location.reload()}>Retry</button>\n      : this.props.children;\n  }\n}\n```\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "tsx\nclass AppBoundary extends Component<\n  { children: ReactNode },\n  { failed: boolean }\n> {\n  state = { failed: false };\n\n  static getDerivedStateFromError() {\n    return { failed: true };\n  }\n\n  componentDidCatch(error: Error, info: ErrorInfo) {\n    reportError({ message: error.message, componentStack: info.componentStack });\n  }\n\n  render() {\n    return this.state.failed\n      ? <button onClick={() => location.reload()}>Retry</button>\n      : this.props.children;\n  }\n}\n```",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Limits the blast radius of render failures and gives users a recovery path. Trade-offs: Error boundaries do not catch every error source and recovery behavior must be designed.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Error boundaries", "Suspense", "Async errors", "Observability", "Recovery UX"],
  },
  {
    id: "reactadv-m2-44",
    number: "REACTADV-M2-44",
    title: "DevTools Fiber inspection",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "React DevTools provides supported ways to inspect component trees, props/state, renders, and profiling information. Although DevTools internally understands Fiber, application developers should use its public inspection and profiling UI rather than relying on private Fiber objects.",
    deepExplanation: "React DevTools provides supported ways to inspect component trees, props/state, renders, and profiling information. Although DevTools internally understands Fiber, application developers should use its public inspection and profiling UI rather than relying on private Fiber objects.\n\ntext\nPerformance investigation:\n1. Open React DevTools Profiler.\n2. Record the slow interaction.\n3. Identify expensive commits/components.\n4. Inspect why components rendered.\n5. Change one architectural variable.\n6. Re-profile and compare.\n```\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "text\nPerformance investigation:\n1. Open React DevTools Profiler.\n2. Record the slow interaction.\n3. Identify expensive commits/components.\n4. Inspect why components rendered.\n5. Change one architectural variable.\n6. Re-profile and compare.\n```",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Gives practical visibility into React rendering behavior without coupling production code to internals. Trade-offs: Profiling itself has overhead and React DevTools cannot explain every browser or backend bottleneck.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["React DevTools", "Profiler", "Fiber", "Performance", "Why did this render?"],
  },
  {
    id: "reactadv-m2-45",
    number: "REACTADV-M2-45",
    title: "Fiber architecture interview scenario",
    difficulty: "Easy",
    experienceLevel: "3–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Fiber Architecture",
    expectedAnswer: "For a senior interview, explain Fiber as the foundation that lets React represent rendering work as units that can be reconciled, prioritized, interrupted, and committed. Then connect the internals to an observable production problem rather than reciting private fields.",
    deepExplanation: "For a senior interview, explain Fiber as the foundation that lets React represent rendering work as units that can be reconciled, prioritized, interrupted, and committed. Then connect the internals to an observable production problem rather than reciting private fields.\n\ntext\nScenario: A dashboard has a 5,000-row table. Typing in a search box becomes laggy.\n\nA strong investigation:\n- Profile the interaction.\n- Identify whether filtering, rendering, commit, layout, or network is expensive.\n- Keep the input update urgent.\n- Consider `useDeferredValue`/`startTransition` for non-urgent derived UI.\n- Virtualize the table if too many rows are rendered.\n- Memoize rows only when stable props and profiling justify it.\n```\n\nWhy it matters: This topic is important because it explains how React turns component updates into schedulable, reconcilable, and commit-ready work in production applications.",
    productionExample: "text\nScenario: A dashboard has a 5,000-row table. Typing in a search box becomes laggy.\n\nA strong investigation:\n- Profile the interaction.\n- Identify whether filtering, rendering, commit, layout, or network is expensive.\n- Keep the input update urgent.\n- Consider `useDeferredValue`/`startTransition` for non-urgent derived UI.\n- Virtualize the table if too many rows are rendered.\n- Memoize rows only when stable props and profiling justify it.\n```",
    bestPractices: ["Keep components focused and composable.", "Keep render logic pure.", "Keep state close to its owner.", "Use stable keys for dynamic collections.", "Prefer explicit data boundaries.", "Add telemetry at important production failure boundaries."],
    tradeOffs: "Advantages: Connects React internals to real performance engineering. Trade-offs: A correct Fiber explanation is not enough; the final solution may involve application architecture, data fetching, virtualization, or browser optimization.",
    commonMistakes: ["Treating Fiber fields as stable public APIs.", "Assuming a render always equals a DOM mutation.", "Ignoring cleanup and cancellation.", "Optimizing without profiling.", "Interview trap: Confusing render phase and commit phase.", "Interview trap: Saying concurrent rendering means concurrent DOM mutations.", "Interview trap: Treating `useMemo` as a semantic guarantee.", "Interview trap: Assuming Context is automatically a global-state replacement."],
    followUpQuestions: ["What is the alternative and why did you reject it?", "What happens during render versus commit?", "What is the memory cost?", "How would React DevTools expose the problem?", "How would you test the behavior?", "What would change at 10× application scale?", "What metrics would you monitor in production?"],
    relatedTopics: ["Fiber", "Profiling", "Virtualization", "Transitions", "useDeferredValue", "Performance"],
  },
];

export const MOCK_ADVANCED_REACT_MODULE2_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Advanced React',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));