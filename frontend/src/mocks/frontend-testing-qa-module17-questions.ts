// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 17 — Enterprise QA Practices).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m17-1",
    number: "TESTQA-M17-1",
    title: "Automation Framework Architecture",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Automation Framework Architecture** should define clear framework boundaries, fixtures, reporting, environment control, and reusable test abstractions without hiding important behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Automation Framework Architecture** should define clear framework boundaries, fixtures, reporting, environment control, and reusable test abstractions without hiding important behavior.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nCreate a reusable framework layer without hiding the behavior under test.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Create a reusable framework layer without hiding the behavior under test.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-2",
    number: "TESTQA-M17-2",
    title: "Test Folder Structure",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Test Folder Structure** should mirror ownership and test layers so the suite remains discoverable as the repository grows.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Test Folder Structure** should mirror ownership and test layers so the suite remains discoverable as the repository grows.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nOrganize test code by ownership and test level so it stays discoverable.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Organize test code by ownership and test level so it stays discoverable.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-3",
    number: "TESTQA-M17-3",
    title: "Reusable Utilities",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Reusable Utilities** should keep shared utilities small, stable, typed, and behavior-focused.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Reusable Utilities** should keep shared utilities small, stable, typed, and behavior-focused.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nKeep shared test helpers small, stable, typed, and behavior-focused.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep shared test helpers small, stable, typed, and behavior-focused.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-4",
    number: "TESTQA-M17-4",
    title: "Shared Mocks",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Shared Mocks** should centralize only stable mocks and keep scenario-specific behavior close to the test.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Shared Mocks** should centralize only stable mocks and keep scenario-specific behavior close to the test.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nCentralize stable mocks while keeping scenario-specific variations local.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Centralize stable mocks while keeping scenario-specific variations local.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-5",
    number: "TESTQA-M17-5",
    title: "Mock Servers",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Mock Servers** should simulate realistic network behavior at the API boundary, including success, failure, latency, and malformed responses.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Mock Servers** should simulate realistic network behavior at the API boundary, including success, failure, latency, and malformed responses.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nSimulate success, failure, latency, and malformed responses at the network boundary.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Simulate success, failure, latency, and malformed responses at the network boundary.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-6",
    number: "TESTQA-M17-6",
    title: "Feature Flags",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Feature Flags** should test both flag states and ensure disabled paths, rollout rules, and cleanup behavior remain valid.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Feature Flags** should test both flag states and ensure disabled paths, rollout rules, and cleanup behavior remain valid.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nTest both flag states, targeting rules, and cleanup.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test both flag states, targeting rules, and cleanup.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-7",
    number: "TESTQA-M17-7",
    title: "Environment Management",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Environment Management** should separate environment configuration from test logic and validate required configuration early.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Environment Management** should separate environment configuration from test logic and validate required configuration early.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nValidate required configuration early and keep environment data out of test logic.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Validate required configuration early and keep environment data out of test logic.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-8",
    number: "TESTQA-M17-8",
    title: "Bug Lifecycle",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Bug Lifecycle** should connect reproduction, evidence, severity, ownership, remediation, and verification.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Bug Lifecycle** should connect reproduction, evidence, severity, ownership, remediation, and verification.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nConnect reproducible evidence to severity, owner, fix, and verification.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Connect reproducible evidence to severity, owner, fix, and verification.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-9",
    number: "TESTQA-M17-9",
    title: "Agile QA",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Agile QA** should move quality activities earlier while preserving risk-based depth for critical flows.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Agile QA** should move quality activities earlier while preserving risk-based depth for critical flows.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nMove quality checks earlier while preserving depth for high-risk flows.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Move quality checks earlier while preserving depth for high-risk flows.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-10",
    number: "TESTQA-M17-10",
    title: "Shift-left Testing",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Shift-left Testing** should detect defects earlier through static checks, unit tests, component tests, contract checks, and fast feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Shift-left Testing** should detect defects earlier through static checks, unit tests, component tests, contract checks, and fast feedback.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nUse fast static/unit/component/contract checks before expensive end-to-end execution.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use fast static/unit/component/contract checks before expensive end-to-end execution.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-11",
    number: "TESTQA-M17-11",
    title: "Risk-based Testing",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Risk-based Testing** should prioritize testing by user impact, likelihood, change risk, and failure cost.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Risk-based Testing** should prioritize testing by user impact, likelihood, change risk, and failure cost.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nPrioritize tests by impact, likelihood, change risk, and failure cost.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Prioritize tests by impact, likelihood, change risk, and failure cost.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-12",
    number: "TESTQA-M17-12",
    title: "Release Validation",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Release Validation** should validate the release candidate with the smallest set of high-value checks that protect critical paths.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Release Validation** should validate the release candidate with the smallest set of high-value checks that protect critical paths.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Job {\n  name: string;\n  passed: boolean;\n}\n\nfunction releaseReady(\n  jobs: readonly Job[],\n): boolean {\n  for (let i = 0; i < jobs.length; i += 1) {\n    if (!jobs[i].passed) return false;\n  }\n\n  return jobs.length > 0;\n}\n\nconst ready = releaseReady([\n  { name: \"unit\", passed: true },\n  { name: \"integration\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst jobs = [\n  { name: \"unit\", passed: true },\n  { name: \"integration\", passed: true },\n];\n\nconst ready =\n  jobs.length > 0 &&\n  jobs.every((job) => job.passed);\n```\n\nStep 5 — Production example:\nValidate critical paths and release-specific risks before promotion.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Validate critical paths and release-specific risks before promotion.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-13",
    number: "TESTQA-M17-13",
    title: "Quality Metrics",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Quality Metrics** should track useful indicators such as defect escape, flake rate, cycle time, pass rate, and signal quality.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Quality Metrics** should track useful indicators such as defect escape, flake rate, cycle time, pass rate, and signal quality.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nTrack defect escape, flake rate, cycle time, pass rate, and signal quality.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Track defect escape, flake rate, cycle time, pass rate, and signal quality.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-14",
    number: "TESTQA-M17-14",
    title: "Test Data Governance",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Test Data Governance** should govern generation, privacy, ownership, refresh, masking, and cleanup of test data.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Test Data Governance** should govern generation, privacy, ownership, refresh, masking, and cleanup of test data.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface TestUnit {\n  name: string;\n  enabled: boolean;\n}\n\nfunction enabledUnits(\n  units: readonly TestUnit[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < units.length; i += 1) {\n    if (units[i].enabled) result.push(units[i].name);\n  }\n\n  return result;\n}\n\nconst units = enabledUnits([\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst units = [\n  { name: \"unit\", enabled: true },\n  { name: \"integration\", enabled: true },\n];\n\nconst enabled = units\n  .filter((unit) => unit.enabled)\n  .map((unit) => unit.name);\n```\n\nStep 5 — Production example:\nGovern data creation, masking, ownership, retention, and cleanup.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Govern data creation, masking, ownership, retention, and cleanup.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-15",
    number: "TESTQA-M17-15",
    title: "Enterprise QA Operating Model",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Enterprise QA Operating Model** should define ownership, supported environments, baseline governance, CI integration, retention, and failure triage.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Enterprise QA Operating Model** should define ownership, supported environments, baseline governance, CI integration, retention, and failure triage.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Check {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(checks: readonly Check[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst checks = [\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse the smallest deterministic test that provides meaningful confidence.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use the smallest deterministic test that provides meaningful confidence.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
  {
    id: "testqa-m17-16",
    number: "TESTQA-M17-16",
    title: "Enterprise QA Practices — Module Review",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise QA Practices",
    expectedAnswer: "**Enterprise QA Practices — Module Review** should define ownership, supported environments, baseline governance, CI integration, retention, and failure triage.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Enterprise QA Practices — Module Review** should define ownership, supported environments, baseline governance, CI integration, retention, and failure triage.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Check {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(checks: readonly Check[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst checks = [\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse the smallest deterministic test that provides meaningful confidence.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use the smallest deterministic test that provides meaningful confidence.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Enterprise QA Practices"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE17_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));