// Derived from frontend/src/document/Part_13_Frontend_Machine_Coding_Master_Handbook.md (Module 17 — Real-time Applications).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "machinecoding-m17-331",
    number: "MACHINECODING-M17-331",
    title: "WebSocket basics",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **WebSocket basics** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-332",
    number: "MACHINECODING-M17-332",
    title: "Connection state",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Connection state** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-333",
    number: "MACHINECODING-M17-333",
    title: "Reconnect with backoff",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Reconnect with backoff** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-334",
    number: "MACHINECODING-M17-334",
    title: "Heartbeat",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Heartbeat** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-335",
    number: "MACHINECODING-M17-335",
    title: "Presence indicator",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Presence indicator** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-336",
    number: "MACHINECODING-M17-336",
    title: "Live notifications",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Live notifications** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-337",
    number: "MACHINECODING-M17-337",
    title: "Live dashboard",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Live dashboard** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-338",
    number: "MACHINECODING-M17-338",
    title: "Server-sent events",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Server-sent events** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-339",
    number: "MACHINECODING-M17-339",
    title: "BroadcastChannel",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **BroadcastChannel** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-340",
    number: "MACHINECODING-M17-340",
    title: "Cross-tab synchronization",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Cross-tab synchronization** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-341",
    number: "MACHINECODING-M17-341",
    title: "Real-time optimistic updates",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Real-time optimistic updates** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-342",
    number: "MACHINECODING-M17-342",
    title: "Conflict handling",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Conflict handling** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-343",
    number: "MACHINECODING-M17-343",
    title: "Event ordering",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Event ordering** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-344",
    number: "MACHINECODING-M17-344",
    title: "Duplicate events",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Duplicate events** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-345",
    number: "MACHINECODING-M17-345",
    title: "Idempotency",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Idempotency** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-346",
    number: "MACHINECODING-M17-346",
    title: "Offline queue",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Offline queue** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-347",
    number: "MACHINECODING-M17-347",
    title: "Real-time error handling",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Real-time error handling** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-348",
    number: "MACHINECODING-M17-348",
    title: "Real-time testing",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Real-time testing** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-349",
    number: "MACHINECODING-M17-349",
    title: "Real-time performance",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Real-time performance** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\nexport function ExampleForm() {\n  const [name, setName] = useState(\"\");\n\n  function submit(event: React.FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    if (!name.trim()) return;\n    console.log({ name });\n  }\n\n  return (\n    <form onSubmit={submit} noValidate>\n      <label htmlFor=\"name\">Name</label>\n      <input\n        id=\"name\"\n        name=\"name\"\n        value={name}\n        onChange={(e) => setName(e.target.value)}\n        required\n      />\n      <button type=\"submit\">Save</button>\n    </form>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m17-350",
    number: "MACHINECODING-M17-350",
    title: "Enterprise real-time architecture",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-time Applications",
    expectedAnswer: "Build a production-quality **Enterprise real-time architecture** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---","# Module 18 – Advanced UI Components","**Questions 351–370**"],
  },
];

export const MOCK_FRONTEND_MACHINE_CODING_MODULE17_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
