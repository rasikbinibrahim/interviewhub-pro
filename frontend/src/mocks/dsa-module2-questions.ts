// Derived from frontend/src/document/DSA_Master_Handbook_Senior_Frontend_Engineers.md (Module 2 — Arrays).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Definition, JavaScript/TypeScript implementation, Time/Space Complexity,
// Trade-offs, Common Mistakes, Interview Tricks, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Apple",
  "Netflix",
  "Uber",
  "Airbnb",
  "Atlassian",
  "Adobe",
  "LinkedIn",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "dsa-m2-11",
    number: "DSA-M2-11",
    title: "Array traversal and mutation",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Arrays",
    expectedAnswer: "Array traversal means visiting elements in sequence; mutation means changing array contents in place. In interviews, first clarify whether mutation is allowed, then choose a single pass, two pointers, or another pattern based on the required operation.",
    deepExplanation: "Step 1 — Understand:\nFor an array of n items, a normal left-to-right traversal visits each element once → O(n).\n\nStep 2 — Manual/core example:\n```ts\nfunction doublePositiveValues(nums: number[]): void {\n  for (let i = 0; i < nums.length; i++) {\n    if (nums[i]! > 0) {\n      nums[i] = nums[i]! * 2;\n    }\n  }\n}\n```\nThis mutates the input in place and uses O(1) auxiliary space.\n\nStep 3 — Built-in example:\n```ts\nfunction doublePositiveValuesUsingBuiltIns(nums: readonly number[]): number[] {\n  return nums.map((value) => (value > 0 ? value * 2 : value));\n}\n```\nThis avoids mutation but allocates a new array.\n\nStep 4 — Interview takeaway:\nChoose mutation when the contract allows in-place work and memory matters. Choose copying/map when immutability is more important.\n\nExample:\n[1,-2,3] → [2,-2,6].",
    productionExample: "Frontend example: normalizing a list of API records can be done in one pass. When using React state, prefer producing a new array rather than mutating state directly; for algorithm rounds, an in-place version may be required.",
    bestPractices: ["State whether the input may be mutated.", "Use one pass when each item needs to be visited once.", "Track the write/read index explicitly for in-place transformations.", "Avoid repeated shift() or splice() inside large loops unless their cost is acceptable.", "Test empty arrays, one element, negatives, and duplicates.", "State time and auxiliary-space complexity."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| In-place mutation | O(1) auxiliary space | Changes input |\n| New array | Immutable and easy to reason about | O(n) extra memory |\n| map/filter | Concise | Allocations and callback overhead |",
    commonMistakes: ["Mutating input when callers expect immutability.", "Using shift() repeatedly in a large array.", "Wrong loop boundary.", "Skipping the first or last element.", "Forgetting empty-array handling."],
    followUpQuestions: ["When should you mutate an array in place?", "Why can shift() be expensive?", "How would you reverse an array manually?", "How would you remove duplicates in place?", "How would you partition an array around a pivot?", "What is the auxiliary space of map()?", "How do two pointers help?", "How would this change for a readonly array?", "How would you benchmark two approaches?", "What changes for a streaming input?"],
    relatedTopics: ["Arrays", "Two Pointers", "In-place Algorithms", "Immutability"],
  },
  {
    id: "dsa-m2-12",
    number: "DSA-M2-12",
    title: "Two Sum",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Arrays",
    expectedAnswer: "Two Sum asks for two indices whose values add to a target. The common optimized approach uses a hash map: for each value x, look for target - x among values already seen.",
    deepExplanation: "Step 1 — Brute force:\nCheck every pair → O(n²).\n\nStep 2 — Manual/core optimized solution:\n```ts\nfunction twoSum(nums: readonly number[], target: number): [number, number] | null {\n  const seen: Record<string, number> = Object.create(null);\n\n  for (let i = 0; i < nums.length; i++) {\n    const need = target - nums[i]!;\n    const previousIndex = seen[String(need)];\n\n    if (previousIndex !== undefined) {\n      return [previousIndex, i];\n    }\n\n    seen[String(nums[i]!)] = i;\n  }\n\n  return null;\n}\n```\n\nStep 3 — Built-in helper solution:\n```ts\nfunction twoSumUsingMap(nums: readonly number[], target: number): [number, number] | null {\n  const seen = new Map<number, number>();\n\n  for (let i = 0; i < nums.length; i++) {\n    const need = target - nums[i]!;\n    const previousIndex = seen.get(need);\n\n    if (previousIndex !== undefined) {\n      return [previousIndex, i];\n    }\n\n    seen.set(nums[i]!, i);\n  }\n\n  return null;\n}\n```\n\nStep 4 — Example:\n[2,7,11,15], target=9\ni=0 → need=7 → not seen → store 2\ni=1 → need=2 → seen at 0\nanswer = [0,1].\n\nTime O(n) average; auxiliary space O(n).",
    productionExample: "Frontend example: a lookup map is useful when matching selected records by ID or finding complementary values without rescanning a collection.",
    bestPractices: ["Check the complement before inserting the current value.", "Store the index, not just the value, when indices are required.", "State the average-case assumption for hash lookup.", "Preserve the original array unless mutation is explicitly allowed.", "Test duplicate values such as [3,3]."],
    tradeOffs: "| Method | Time | Extra Space |\n|---|---:|---:|\n| Nested loops | O(n²) | O(1) |\n| Hash map | O(n) average | O(n) |\n| Sort + two pointers | O(n log n) | Depends on whether indices must be preserved |",
    commonMistakes: ["Inserting before checking the complement and accidentally reusing the same element.", "Returning values when the question asks for indices.", "Assuming hash lookup is worst-case O(1).", "Ignoring duplicate numbers."],
    followUpQuestions: ["What if the array is already sorted?", "How do you return all pairs?", "What if indices are not required?", "Can you solve it in O(1) extra space?", "How would you handle duplicate pairs?", "Why must we check before insert?", "What is the brute-force solution?", "How would this work as a streaming problem?", "What if the array is extremely large?", "How would you test it?"],
    relatedTopics: ["Arrays", "Hashing", "Two Pointers"],
  },
  {
    id: "dsa-m2-13",
    number: "DSA-M2-13",
    title: "Maximum subarray",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Arrays",
    expectedAnswer: "The maximum subarray problem asks for the contiguous subarray with the largest sum. Kadane's algorithm keeps the best sum ending at the current position and decides whether to extend the previous subarray or start a new one.",
    deepExplanation: "Step 1 — Brute force:\nEnumerate all subarrays → O(n²) or O(n³), depending on how the sum is calculated.\n\nStep 2 — Manual/core Kadane:\n```ts\nfunction maxSubarraySum(nums: readonly number[]): number {\n  if (nums.length === 0) return 0;\n\n  let current = nums[0]!;\n  let best = nums[0]!;\n\n  for (let i = 1; i < nums.length; i++) {\n    current = Math.max(nums[i]!, current + nums[i]!);\n    best = Math.max(best, current);\n  }\n\n  return best;\n}\n```\n\nStep 3 — Built-in-friendly variation with reduce:\n```ts\nfunction maxSubarraySumUsingBuiltIns(nums: readonly number[]): number {\n  if (nums.length === 0) return 0;\n\n  let best = nums[0]!;\n  let current = nums[0]!;\n\n  nums.slice(1).forEach((value) => {\n    current = Math.max(value, current + value);\n    best = Math.max(best, current);\n  });\n\n  return best;\n}\n```\n\nStep 4 — Example:\n[-2,1,-3,4,-1,2,1,-5,4]\nBest subarray = [4,-1,2,1]\nSum = 6.\n\nTime O(n), auxiliary space O(1).",
    productionExample: "Frontend example: scoring consecutive events or finding the best contiguous gain/loss period can use the same running-best state.",
    bestPractices: ["Initialize from the first element so all-negative arrays work.", "Keep current sum and global best separate.", "State the contiguous requirement explicitly.", "Use O(1) state when only the maximum sum is needed."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| Kadane | O(n), O(1) extra space | Requires understanding the invariant |\n| Prefix sums | Useful for broader range queries | More state |\n| Brute force | Easy to derive | Too slow at scale |",
    commonMistakes: ["Initializing best to 0 and failing all-negative arrays.", "Confusing subarray with subsequence.", "Resetting current sum incorrectly.", "Returning the wrong state."],
    followUpQuestions: ["How do you return the actual subarray?", "How do you handle all-negative input?", "Can you find the maximum circular subarray?", "How is Kadane related to DP?", "What invariant does current represent?", "Can prefix sums solve it?", "What is the brute-force baseline?", "How would you stream the calculation?", "What changes for 2D matrices?", "How would you test edge cases?"],
    relatedTopics: ["Arrays", "Dynamic Programming", "Prefix Sum"],
  },
  {
    id: "dsa-m2-14",
    number: "DSA-M2-14",
    title: "Prefix sums",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Arrays",
    expectedAnswer: "A prefix sum stores cumulative totals so a contiguous range sum can be answered in O(1) after O(n) preprocessing. For inclusive range [l,r], rangeSum = prefix[r+1] - prefix[l].",
    deepExplanation: "Step 1 — Build prefix:\n```ts\nfunction buildPrefixSum(nums: readonly number[]): number[] {\n  const prefix = new Array<number>(nums.length + 1).fill(0);\n\n  for (let i = 0; i < nums.length; i++) {\n    prefix[i + 1] = prefix[i]! + nums[i]!;\n  }\n\n  return prefix;\n}\n```\n\nStep 2 — Query manually:\n```ts\nfunction rangeSum(prefix: readonly number[], left: number, right: number): number {\n  return prefix[right + 1]! - prefix[left]!;\n}\n```\n\nStep 3 — Example:\nnums=[2,4,1,5]\nprefix=[0,2,6,7,12]\nsum(1,3)=prefix[4]-prefix[1]=12-2=10.\n\nStep 4 — Built-in-friendly version:\n```ts\nfunction buildPrefixSumUsingBuiltIns(nums: readonly number[]): number[] {\n  return nums.reduce<number[]>((prefix, value) => {\n    const last = prefix[prefix.length - 1] ?? 0;\n    prefix.push(last + value);\n    return prefix;\n  }, [0]);\n}\n```\n\nPreprocessing O(n), each query O(1), extra space O(n).",
    productionExample: "Frontend example: dashboard filters that repeatedly ask for range totals can benefit from precomputed prefix values when the underlying array does not change often.",
    bestPractices: ["Use a prefix array of length n+1 to simplify boundaries.", "Define whether ranges are inclusive.", "Consider rebuilding when the underlying data changes.", "Use prefix sums when many range queries are expected."],
    tradeOffs: "| Approach | Query | Extra Space |\n|---|---:|---:|\n| Direct sum | O(n) | O(1) |\n| Prefix sum | O(1) after preprocessing | O(n) |\n| Fenwick tree | O(log n) updates/queries | O(n) |",
    commonMistakes: ["Off-by-one in prefix index.", "Forgetting the leading zero.", "Using prefix sums when the data changes constantly.", "Confusing prefix values with range sums."],
    followUpQuestions: ["How do you handle updates?", "What is a Fenwick tree?", "What about 2D prefix sums?", "Can prefix sums solve subarray sum equals k?", "Why is prefix length n+1 convenient?", "What if numbers are negative?", "What if queries arrive online?", "How much preprocessing is worthwhile?", "How would you validate the formula?", "What are alternatives for dynamic updates?"],
    relatedTopics: ["Arrays", "Prefix Sum", "Fenwick Tree", "Range Queries"],
  },
  {
    id: "dsa-m2-15",
    number: "DSA-M2-15",
    title: "Merge sorted arrays",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Arrays",
    expectedAnswer: "To merge two sorted arrays, compare the current elements from each array and repeatedly take the smaller one. This is the merge step used by merge sort and runs in O(n+m).",
    deepExplanation: "Step 1 — Manual/core:\n```ts\nfunction mergeSortedArrays(\n  a: readonly number[],\n  b: readonly number[],\n): number[] {\n  const result: number[] = [];\n  let i = 0;\n  let j = 0;\n\n  while (i < a.length && j < b.length) {\n    if (a[i]! <= b[j]!) {\n      result.push(a[i]!);\n      i++;\n    } else {\n      result.push(b[j]!);\n      j++;\n    }\n  }\n\n  while (i < a.length) result.push(a[i++]!);\n  while (j < b.length) result.push(b[j++]!);\n\n  return result;\n}\n```\n\nStep 2 — Built-in:\n```ts\nfunction mergeSortedArraysUsingBuiltIns(a: readonly number[], b: readonly number[]): number[] {\n  return [...a, ...b].sort((x, y) => x - y);\n}\n```\n\nStep 3 — Example:\na=[1,3,7], b=[2,4,8]\nTake 1,2,3,4,7,8 → [1,2,3,4,7,8].\n\nStep 4 — Complexity:\nManual merge = O(n+m) time.\nBuilt-in concat + sort = O((n+m) log(n+m)) time.\n",
    productionExample: "Frontend example: merging two already-sorted paginated result streams can avoid a full re-sort when their ordering is guaranteed.",
    bestPractices: ["Exploit the fact that both inputs are already sorted.", "Use two indices rather than repeatedly removing from the front.", "Preserve duplicates unless the problem says otherwise.", "Append remaining elements after one array is exhausted."],
    tradeOffs: "| Method | Time | Space |\n|---|---:|---:|\n| Two pointers | O(n+m) | O(n+m) result |\n| Concat + sort | O((n+m) log(n+m)) | O(n+m) plus sort overhead |",
    commonMistakes: ["Sorting again unnecessarily.", "Forgetting remaining elements.", "Using shift() in a loop.", "Dropping duplicates by mistake."],
    followUpQuestions: ["How is this used in merge sort?", "Can you merge in place?", "How do you merge k sorted arrays?", "What changes for linked lists?", "Can you do it lazily as an iterator?", "Why is shift() a bad choice here?", "How would you prove O(n+m)?", "How would you handle very large inputs?", "What if one array is much smaller?", "How would you test duplicates?"],
    relatedTopics: ["Arrays", "Two Pointers", "Merge Sort"],
  },
  {
    id: "dsa-m2-16",
    number: "DSA-M2-16",
    title: "Rotate array",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Arrays",
    expectedAnswer: "Rotating an array by k moves elements from one end to the other. For right rotation, reduce k modulo n, then use the reversal method: reverse all, reverse the first k, and reverse the rest.",
    deepExplanation: "Step 1 — Normalize k:\nk = k % n.\n\nStep 2 — Manual in-place reversal:\n```ts\nfunction rotateRight(nums: number[], k: number): void {\n  const n = nums.length;\n  if (n === 0) return;\n\n  k %= n;\n\n  function reverse(left: number, right: number): void {\n    while (left < right) {\n      const temp = nums[left]!;\n      nums[left] = nums[right]!;\n      nums[right] = temp;\n      left++;\n      right--;\n    }\n  }\n\n  reverse(0, n - 1);\n  reverse(0, k - 1);\n  reverse(k, n - 1);\n}\n```\n\nStep 3 — Example:\n[1,2,3,4,5,6,7], k=3\nreverse all → [7,6,5,4,3,2,1]\nreverse first 3 → [5,6,7,4,3,2,1]\nreverse rest → [5,6,7,1,2,3,4].\n\nStep 4 — Built-in version:\n```ts\nfunction rotateRightUsingBuiltIns(nums: readonly number[], k: number): number[] {\n  if (nums.length === 0) return [];\n  const shift = k % nums.length;\n  return nums.slice(-shift).concat(nums.slice(0, nums.length - shift));\n}\n```\n\nCore = O(n) time, O(1) auxiliary space.",
    productionExample: "Frontend example: rotating a carousel or circular buffer can use normalized indices rather than physically moving every item when only the view position changes.",
    bestPractices: ["Normalize k with k % n.", "Handle empty input.", "For in-place requirements, use reversal.", "Clarify whether rotation is left or right.", "Avoid repeated unshift/splice operations in large arrays."],
    tradeOffs: "| Method | Benefit | Cost |\n|---|---|---|\n| Reversal | O(1) auxiliary space, O(n) time | In-place mutation |\n| Slice/concat | Easy to read, immutable | O(n) extra memory |",
    commonMistakes: ["Forgetting k > n.", "Wrong direction.", "Reversing the wrong segments.", "Empty-array divide-by-zero logic."],
    followUpQuestions: ["How do you rotate left?", "Can you rotate by one position repeatedly?", "How do you do it with O(1) extra space?", "What if the array is immutable?", "What if k is negative?", "How would you rotate a matrix?", "How is rotation different from shifting?", "How would you test k=0 and k=n?", "What is the juggling algorithm?", "Which method is clearer in production?"],
    relatedTopics: ["Arrays", "Two Pointers", "In-place Algorithms"],
  },
  {
    id: "dsa-m2-17",
    number: "DSA-M2-17",
    title: "Move zeroes",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Arrays",
    expectedAnswer: "Move all zero values to the end while preserving the relative order of non-zero elements. The standard O(n) solution uses a write pointer to place non-zero values first.",
    deepExplanation: "Step 1 — Manual/core:\n```ts\nfunction moveZeroes(nums: number[]): void {\n  let write = 0;\n\n  for (let read = 0; read < nums.length; read++) {\n    if (nums[read] !== 0) {\n      const temp = nums[write]!;\n      nums[write] = nums[read]!;\n      nums[read] = temp;\n      write++;\n    }\n  }\n}\n```\n\nStep 2 — Example:\n[0,1,0,3,12]\nread=0 → skip\nread=1 → place 1 at write=0\nread=0 → skip\nread=3 → place 3 at write=1\nread=12 → place 12 at write=2\nresult=[1,3,12,0,0].\n\nStep 3 — Built-in-friendly version:\n```ts\nfunction moveZeroesUsingBuiltIns(nums: readonly number[]): number[] {\n  return nums.filter((value) => value !== 0)\n    .concat(nums.filter((value) => value === 0));\n}\n```\n\nStep 4 — Complexity:\nManual in-place version O(n) time, O(1) extra space.\nBuilt-in version O(n) extra space.",
    productionExample: "Frontend example: compacting a buffer of optional values can use a write pointer to preserve the original order without creating another large collection.",
    bestPractices: ["Preserve non-zero order.", "Use a write pointer.", "Clarify whether mutation is required.", "Avoid sorting because it can destroy relative order and changes the problem."],
    tradeOffs: "| Method | Benefit | Cost |\n|---|---|---|\n| Two pointers | O(n), O(1) extra | Mutates input |\n| filter + concat | Simple and immutable | O(n) extra memory |",
    commonMistakes: ["Using sort().", "Not preserving non-zero order.", "Using splice repeatedly.", "Forgetting arrays containing only zeros or no zeros."],
    followUpQuestions: ["Can you solve it without swapping?", "How would you move negative numbers instead?", "What if zero order mattered too?", "How would you do this on a linked list?", "Can you make it stable for another special value?", "What is the write-pointer invariant?", "Why is sorting wrong?", "How would you test all-zero input?", "What if memory is constrained?", "How does this relate to partitioning?"],
    relatedTopics: ["Arrays", "Two Pointers", "In-place Algorithms", "Stable Partition"],
  },
  {
    id: "dsa-m2-18",
    number: "DSA-M2-18",
    title: "Intervals",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Arrays",
    expectedAnswer: "Interval problems usually become simpler after sorting by start time. A common greedy pattern is to maintain the current merged interval and extend it while the next interval overlaps.",
    deepExplanation: "Step 1 — Manual/core merge:\n```ts\nfunction mergeIntervals(intervals: readonly [number, number][]): [number, number][] {\n  if (intervals.length === 0) return [];\n\n  const sorted = intervals.map(([start, end]) => [start, end] as [number, number]);\n\n  // Manual insertion sort by start, avoiding sort().\n  for (let i = 1; i < sorted.length; i++) {\n    const current = sorted[i]!;\n    let j = i - 1;\n\n    while (j >= 0 && sorted[j]![0] > current[0]) {\n      sorted[j + 1] = sorted[j]!;\n      j--;\n    }\n\n    sorted[j + 1] = current;\n  }\n\n  const result: [number, number][] = [sorted[0]!];\n\n  for (let i = 1; i < sorted.length; i++) {\n    const current = sorted[i]!;\n    const last = result[result.length - 1]!;\n\n    if (current[0] <= last[1]) {\n      if (current[1] > last[1]) last[1] = current[1];\n    } else {\n      result.push([current[0], current[1]]);\n    }\n  }\n\n  return result;\n}\n```\n\nStep 2 — Built-in version:\n```ts\nfunction mergeIntervalsUsingBuiltIns(intervals: readonly [number, number][]): [number, number][] {\n  const sorted = [...intervals].sort((a, b) => a[0] - b[0]);\n  const result: [number, number][] = [];\n\n  for (const current of sorted) {\n    const last = result[result.length - 1];\n\n    if (!last || current[0] > last[1]) {\n      result.push([current[0], current[1]]);\n    } else if (current[1] > last[1]) {\n      last[1] = current[1];\n    }\n  }\n\n  return result;\n}\n```\n\nStep 3 — Example:\n[[1,3],[2,6],[8,10],[15,18]]\n→ [[1,6],[8,10],[15,18]].\n\nStep 4 — Complexity:\nSorting dominates → O(n log n) time; output storage is O(n).",
    productionExample: "Frontend example: merging availability windows, booking intervals, or maintenance periods is a common interval operation.",
    bestPractices: ["Sort by start time first.", "State whether touching intervals should merge.", "Keep only the current output interval state.", "Avoid mutating the caller's input unless allowed."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| Sort + scan | Simple, general | O(n log n) |\n| Pre-sorted intervals | O(n) merge | Requires sorted input |",
    commonMistakes: ["Sorting by end instead of start for merge problems.", "Wrong overlap condition.", "Mutating input unexpectedly.", "Forgetting touching intervals if the problem treats them as overlapping."],
    followUpQuestions: ["How do you insert an interval?", "How do you detect overlap without merging?", "How do you find meeting rooms?", "What if intervals are already sorted?", "How do you handle open/closed endpoints?", "How do you merge k sources?", "Can a sweep line solve it?", "What changes for weighted intervals?", "How do you test nested intervals?", "How would you process streaming intervals?"],
    relatedTopics: ["Arrays", "Sorting", "Greedy", "Sweep Line"],
  },
  {
    id: "dsa-m2-19",
    number: "DSA-M2-19",
    title: "Matrix traversal",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Arrays",
    expectedAnswer: "Matrix traversal means visiting every cell in a structured order such as row-major, column-major, spiral, or boundary traversal. The main risk is incorrect row/column boundaries.",
    deepExplanation: "Step 1 — Manual row-major traversal:\n```ts\nfunction flattenMatrix(matrix: readonly (readonly number[])[]): number[] {\n  const result: number[] = [];\n\n  for (let row = 0; row < matrix.length; row++) {\n    for (let col = 0; col < matrix[row]!.length; col++) {\n      result.push(matrix[row]![col]!);\n    }\n  }\n\n  return result;\n}\n```\n\nStep 2 — Built-in version:\n```ts\nfunction flattenMatrixUsingBuiltIns(matrix: readonly (readonly number[])[]): number[] {\n  return matrix.flatMap((row) => [...row]);\n}\n```\n\nStep 3 — Example:\n[[1,2,3],[4,5,6]]\nrow-major traversal → [1,2,3,4,5,6].\n\nStep 4 — For more complex problems:\n- Spiral → maintain top/bottom/left/right boundaries.\n- Grid DFS/BFS → use four directions.\n- Diagonal traversal → use row/column rules.\n\nTime is O(rows * cols) for visiting every cell.",
    productionExample: "Frontend example: image data, spreadsheet cells, seating grids, and board games are naturally represented as matrices. Clear row/column boundaries reduce indexing bugs.",
    bestPractices: ["Store rows and columns explicitly.", "Check empty matrices before reading matrix[0].", "Use row/column names consistently.", "For grid movement, centralize the four directions.", "Test 1x1, 1xn, nx1, and rectangular matrices."],
    tradeOffs: "| Traversal | Benefit | Common use |\n|---|---|---|\n| Row-major | Simple | Tables / matrices |\n| Spiral | Boundary-based | Layered output |\n| DFS/BFS | Connectivity | Grids / islands |\n| Diagonal | Structured indexing | Matrix problems |",
    commonMistakes: ["Assuming the matrix is square.", "Reading matrix[0] when empty.", "Mixing row and column indices.", "Incorrect boundary shrinking.", "Forgetting rectangular matrices."],
    followUpQuestions: ["How do you traverse in spiral order?", "How do you rotate a matrix?", "How do you search a sorted matrix?", "How do you find islands?", "How does BFS work on a grid?", "What are four-direction vs eight-direction moves?", "How would you transpose a matrix?", "What is the complexity of row-major traversal?", "How do you handle jagged arrays?", "How would you test a 1xN matrix?"],
    relatedTopics: ["Arrays", "Matrices", "DFS", "BFS", "Grid Problems"],
  },
  {
    id: "dsa-m2-20",
    number: "DSA-M2-20",
    title: "Array performance in JavaScript",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Arrays",
    expectedAnswer: "JavaScript arrays are optimized dynamic collections, but algorithmic costs still matter: indexed access and push/pop are typically efficient, while front insertion/removal and repeated copying can be expensive. Performance also depends on allocation, garbage collection, and data shape.",
    deepExplanation: "Step 1 — Avoid repeated front removal:\n```ts\nfunction consumeQueue(values: readonly number[]): number[] {\n  const queue = [...values];\n  let front = 0;\n  const result: number[] = [];\n\n  while (front < queue.length) {\n    result.push(queue[front++]!);\n  }\n\n  return result;\n}\n```\nUsing shift() repeatedly can force element movement and is a poor default for large queues.\n\nStep 2 — Avoid unnecessary copying:\n```ts\nfunction sumArray(nums: readonly number[]): number {\n  let sum = 0;\n  for (let i = 0; i < nums.length; i++) {\n    sum += nums[i]!;\n  }\n  return sum;\n}\n```\n\nStep 3 — Built-in example:\n```ts\nfunction sumArrayUsingBuiltIns(nums: readonly number[]): number {\n  return nums.reduce((sum, value) => sum + value, 0);\n}\n```\n\nStep 4 — Practical guidance:\nUse simple arrays first. Optimize after profiling or when the algorithm's complexity clearly demands it. Repeated copies, nested scans, and unnecessary allocations usually matter more than micro-optimizing a single loop.",
    productionExample: "Frontend example: large lists can trigger allocation and garbage-collection pressure if every render repeatedly clones or transforms the entire array. Memoization, stable references, virtualization, and appropriate indexing can reduce unnecessary work.",
    bestPractices: ["Know the algorithmic complexity before micro-optimizing.", "Avoid repeated shift/unshift for queue-like workloads.", "Avoid unnecessary spread/slice copies on hot paths.", "Use Map/Set when lookup dominates repeated array scans.", "Profile real workloads before making engine-specific assumptions."],
    tradeOffs: "| Choice | Benefit | Cost |\n|---|---|---|\n| Simple array loop | Low overhead, predictable | More verbose |\n| map/filter/reduce | Readable and expressive | Callback/allocation overhead may matter in hot paths |\n| Map/Set | Fast average lookup | Memory |\n| Copy-on-write | Easier state management | Allocations |",
    commonMistakes: ["Assuming every array method has the same complexity.", "Using shift() as a queue primitive on huge arrays.", "Cloning arrays unnecessarily.", "Optimizing before measuring.", "Ignoring memory/GC costs."],
    followUpQuestions: ["Why is shift() potentially expensive?", "What is amortized push()?", "When should you use Map instead of find()?", "How do array copies affect GC?", "What are typed arrays?", "How would you benchmark two implementations?", "What is the cost of spread syntax?", "How do React state updates change the trade-off?", "How would you process millions of records?", "When is a linked/deque structure better?"],
    relatedTopics: ["Arrays", "JavaScript Performance", "Memory Management", "Map", "Set"],
  },
];

export const MOCK_DSA_MODULE2_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'DSA',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));