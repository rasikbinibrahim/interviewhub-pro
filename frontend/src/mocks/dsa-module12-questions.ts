// Derived from frontend/src/document/DSA_Master_Handbook_Senior_Frontend_Engineers.md (Module 12 — Binary Search).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Definition, JavaScript/TypeScript implementation, Time/Space Complexity,
// Trade-offs, Common Mistakes, Interview Tricks, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Apple",
  "Netflix",
  "Uber",
  "Airbnb",
  "Atlassian",
  "Adobe",
  "LinkedIn",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "dsa-m12-111",
    number: "DSA-M12-111",
    title: "Classic binary search",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Binary Search",
    expectedAnswer: "Binary search finds a target in a sorted array by repeatedly halving the search interval. The key invariant is that if the target exists, it remains inside [left, right].",
    deepExplanation: "Step 1 — Keep a closed interval.\n```ts\nfunction binarySearch(nums: readonly number[], target: number): number {\n  let left = 0;\n  let right = nums.length - 1;\n\n  while (left <= right) {\n    const middle = left + Math.floor((right - left) / 2);\n    const value = nums[middle]!;\n\n    if (value === target) return middle;\n\n    if (value < target) left = middle + 1;\n    else right = middle - 1;\n  }\n\n  return -1;\n}\n```\n\nStep 2 — WITH BUILT-IN:\n```ts\nfunction binarySearchUsingIndexOf(nums: readonly number[], target: number): number {\n  return nums.indexOf(target);\n}\n```\n\nStep 3 — Example:\n[1,3,5,7,9], target 7\nmiddle=5 → search right\nmiddle=7 → found at index 3.\n\nStep 4 — Complexity:\nO(log n) time, O(1) auxiliary space.\nBuilt-in indexOf is O(n), so it is not a binary-search implementation.",
    productionExample: "Use binary search when the data is already sorted or the search predicate is monotonic. In production, avoid copying/sorting merely to make one lookup logarithmic unless repeated lookups justify it.",
    bestPractices: ["State the sorted/monotonic invariant.", "Choose closed or half-open boundaries and stay consistent.", "Prove why one side can be discarded.", "Handle empty and one-element inputs.", "State preprocessing cost separately from lookup cost.", "State sorted-input requirement.", "Define inclusive/exclusive boundaries.", "Use a safe midpoint formula.", "Return a clear not-found value.", "Do not sort inside the lookup unless that cost is intended."],
    tradeOffs: "Off-by-one boundaries.\nUsing binary search on unsorted data.\nInfinite loops from not moving left/right.\nConfusing index with value.",
    commonMistakes: ["Off-by-one loop boundaries.", "Using binary search without sorted/monotonic data.", "Failing to move left/right.", "Mixing [l,r] with [l,r) semantics.", "Claiming O(log n) for the entire workflow when sorting/copying is required.", "Why is it O(log n)?", "Why use left + floor((right-left)/2)?", "Recursive version?", "First/last occurrence?", "What if duplicates exist?", "When is a hash set better?", "What if insertion order changes?", "How do you prove the invariant?", "How do you search strings?", "What if the array is empty?"],
    followUpQuestions: ["What is the invariant?", "Why is it O(log n)?", "What boundary convention do you use?", "How do duplicates change the solution?", "How do you handle empty input?", "Recursive vs iterative?", "Can a Set be better?", "How do you prove correctness?", "What is the preprocessing cost?", "How would you test edge cases?"],
    relatedTopics: ["Binary Search", "Lower Bound", "Upper Bound", "Sorted Arrays", "Complexity"],
  },
  {
    id: "dsa-m12-112",
    number: "DSA-M12-112",
    title: "Lower bound",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Binary Search",
    expectedAnswer: "Lower bound returns the first index whose value is greater than or equal to the target. It is the standard way to find an insertion position that preserves sorted order.",
    deepExplanation: "Step 1 — Find the first index with nums[index] >= target.\n```ts\nfunction lowerBound(nums: readonly number[], target: number): number {\n  let left = 0;\n  let right = nums.length;\n\n  while (left < right) {\n    const middle = left + Math.floor((right - left) / 2);\n\n    if (nums[middle]! < target) left = middle + 1;\n    else right = middle;\n  }\n\n  return left;\n}\n```\n\nStep 2 — WITH BUILT-IN-friendly linear alternative:\n```ts\nfunction lowerBoundLinear(nums: readonly number[], target: number): number {\n  for (let i = 0; i < nums.length; i++) {\n    if (nums[i]! >= target) return i;\n  }\n  return nums.length;\n}\n```\n\nStep 3 — Example:\n[1,2,2,4,6], target 2 → index 1.\ntarget 3 → index 3.\ntarget 9 → index 5.\n\nStep 4 — Complexity:\nBinary-search version O(log n), linear version O(n).",
    productionExample: "Lower bound is useful for insertion points, frequency ranges, interval boundaries, and ordered data structures.",
    bestPractices: ["State the sorted/monotonic invariant.", "Choose closed or half-open boundaries and stay consistent.", "Prove why one side can be discarded.", "Handle empty and one-element inputs.", "State preprocessing cost separately from lookup cost.", "Use half-open [left,right) boundaries.", "Return nums.length when no value is large enough.", "Keep the predicate consistent: value < target moves left."],
    tradeOffs: "Using <= instead of < and skipping duplicates.\nReturning -1 when insertion at the end is valid.\nMixing closed and half-open intervals.",
    commonMistakes: ["Off-by-one loop boundaries.", "Using binary search without sorted/monotonic data.", "Failing to move left/right.", "Mixing [l,r] with [l,r) semantics.", "Claiming O(log n) for the entire workflow when sorting/copying is required.", "How do you find first occurrence?", "How does lowerBound help count values?", "Upper bound?", "How do you insert at the position?", "What if descending order?", "Can you use it on strings?", "How does it work with duplicates?", "What is the loop invariant?", "How do you find range [L,R]?", "What if target is smaller than every value?"],
    followUpQuestions: ["What is the invariant?", "Why is it O(log n)?", "What boundary convention do you use?", "How do duplicates change the solution?", "How do you handle empty input?", "Recursive vs iterative?", "Can a Set be better?", "How do you prove correctness?", "What is the preprocessing cost?", "How would you test edge cases?"],
    relatedTopics: ["Binary Search", "Insertion Position", "Upper Bound", "Ordered Arrays"],
  },
  {
    id: "dsa-m12-113",
    number: "DSA-M12-113",
    title: "Upper bound",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Binary Search",
    expectedAnswer: "Upper bound returns the first index whose value is strictly greater than the target. Together with lower bound, it gives the range of equal values as [lowerBound(target), upperBound(target)).",
    deepExplanation: "Step 1 — Find first value > target.\n```ts\nfunction upperBound(nums: readonly number[], target: number): number {\n  let left = 0;\n  let right = nums.length;\n\n  while (left < right) {\n    const middle = left + Math.floor((right - left) / 2);\n\n    if (nums[middle]! <= target) left = middle + 1;\n    else right = middle;\n  }\n\n  return left;\n}\n```\n\nStep 2 — WITH BUILT-IN-friendly linear alternative:\n```ts\nfunction upperBoundLinear(nums: readonly number[], target: number): number {\n  for (let i = 0; i < nums.length; i++) {\n    if (nums[i]! > target) return i;\n  }\n  return nums.length;\n}\n```\n\nStep 3 — Example:\n[1,2,2,2,4], target 2 → upper bound index 4.\nCount of 2 = upperBound(2) - lowerBound(2) = 3.\n\nStep 4 — Complexity:\nO(log n) binary search, O(n) linear scan.",
    productionExample: "Use upper bound for duplicate counts, range ends, and sorted insertion after existing equal values.",
    bestPractices: ["State the sorted/monotonic invariant.", "Choose closed or half-open boundaries and stay consistent.", "Prove why one side can be discarded.", "Handle empty and one-element inputs.", "State preprocessing cost separately from lookup cost.", "Remember upper bound is strictly greater.", "Use half-open intervals.", "Pair it with lower bound for frequency/range queries."],
    tradeOffs: "Returning the last equal index instead of the first greater index.\nUsing < instead of <= in the predicate.\nOff-by-one at the end.",
    commonMistakes: ["Off-by-one loop boundaries.", "Using binary search without sorted/monotonic data.", "Failing to move left/right.", "Mixing [l,r] with [l,r) semantics.", "Claiming O(log n) for the entire workflow when sorting/copying is required.", "How do lower/upper bound differ?", "How do you count duplicates?", "How do you find first greater?", "How do you find last <= target?", "Descending arrays?", "Range query?", "How does this help percentile/rank queries?", "How do you prove termination?", "What if all values equal target?", "What if target is absent?"],
    followUpQuestions: ["What is the invariant?", "Why is it O(log n)?", "What boundary convention do you use?", "How do duplicates change the solution?", "How do you handle empty input?", "Recursive vs iterative?", "Can a Set be better?", "How do you prove correctness?", "What is the preprocessing cost?", "How would you test edge cases?"],
    relatedTopics: ["Binary Search", "Lower Bound", "Range Queries", "Duplicate Counts"],
  },
  {
    id: "dsa-m12-114",
    number: "DSA-M12-114",
    title: "Search insert position",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Binary Search",
    expectedAnswer: "Search insert position is exactly a lower-bound query: return the first index where the target could be inserted while preserving sorted order.",
    deepExplanation: "Step 1 — Reuse the lower-bound invariant.\n```ts\nfunction searchInsert(nums: readonly number[], target: number): number {\n  let left = 0;\n  let right = nums.length;\n\n  while (left < right) {\n    const middle = left + Math.floor((right - left) / 2);\n\n    if (nums[middle]! < target) left = middle + 1;\n    else right = middle;\n  }\n\n  return left;\n}\n```\n\nStep 2 — WITH BUILT-IN:\n```ts\nfunction searchInsertLinear(nums: readonly number[], target: number): number {\n  const index = nums.indexOf(target);\n  if (index !== -1) return index;\n\n  for (let i = 0; i < nums.length; i++) {\n    if (nums[i]! > target) return i;\n  }\n\n  return nums.length;\n}\n```\n\nStep 3 — Example:\n[1,3,5,6], target 2 → 1.\ntarget 5 → 2.\ntarget 7 → 4.\n\nStep 4 — Complexity:\nO(log n) time, O(1) space.",
    productionExample: "Useful when insertion position must be known without actually inserting. For real insertion into an array, finding the position is O(log n) but shifting elements is O(n).",
    bestPractices: ["State the sorted/monotonic invariant.", "Choose closed or half-open boundaries and stay consistent.", "Prove why one side can be discarded.", "Handle empty and one-element inputs.", "State preprocessing cost separately from lookup cost.", "Return n when inserting after all elements.", "Separate lookup cost from insertion cost.", "Prefer lower-bound semantics for duplicates."],
    tradeOffs: "Using indexOf plus length scan unnecessarily.\nClaiming total insertion is O(log n) for arrays.\nWrong duplicate policy.",
    commonMistakes: ["Off-by-one loop boundaries.", "Using binary search without sorted/monotonic data.", "Failing to move left/right.", "Mixing [l,r] with [l,r) semantics.", "Claiming O(log n) for the entire workflow when sorting/copying is required.", "Why is this lower bound?", "What is insertion complexity?", "How do you handle duplicates?", "How do you insert immutably?", "What changes for linked lists?", "Can Map solve it?", "What if array is descending?", "How do you return first equal index?", "How do you test boundaries?", "What is the invariant?"],
    followUpQuestions: ["What is the invariant?", "Why is it O(log n)?", "What boundary convention do you use?", "How do duplicates change the solution?", "How do you handle empty input?", "Recursive vs iterative?", "Can a Set be better?", "How do you prove correctness?", "What is the preprocessing cost?", "How would you test edge cases?"],
    relatedTopics: ["Lower Bound", "Binary Search", "Arrays", "Insertion"],
  },
  {
    id: "dsa-m12-115",
    number: "DSA-M12-115",
    title: "Rotated sorted array",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Binary Search",
    expectedAnswer: "In a rotated sorted array with distinct values, one side of the midpoint is always sorted. Compare the target with that sorted side to decide which half can be discarded.",
    deepExplanation: "Step 1 — Compare sorted half.\n```ts\nfunction searchRotated(nums: readonly number[], target: number): number {\n  let left = 0;\n  let right = nums.length - 1;\n\n  while (left <= right) {\n    const middle = left + Math.floor((right - left) / 2);\n\n    if (nums[middle] === target) return middle;\n\n    if (nums[left]! <= nums[middle]!) {\n      if (nums[left]! <= target && target < nums[middle]!) {\n        right = middle - 1;\n      } else {\n        left = middle + 1;\n      }\n    } else {\n      if (nums[middle]! < target && target <= nums[right]!) {\n        left = middle + 1;\n      } else {\n        right = middle - 1;\n      }\n    }\n  }\n\n  return -1;\n}\n```\n\nStep 2 — WITH BUILT-IN:\n```ts\nfunction searchRotatedLinear(nums: readonly number[], target: number): number {\n  return nums.indexOf(target);\n}\n```\n\nStep 3 — Example:\n[4,5,6,7,0,1,2], target 0 → index 4.\n\nStep 4 — Complexity:\nDistinct-value version: O(log n) time, O(1) space.\nWith many duplicates, worst-case can degrade to O(n).",
    productionExample: "Useful for cyclic buffers, rotated datasets, and interview search problems where the sorted structure is preserved except for one rotation.",
    bestPractices: ["State the sorted/monotonic invariant.", "Choose closed or half-open boundaries and stay consistent.", "Prove why one side can be discarded.", "Handle empty and one-element inputs.", "State preprocessing cost separately from lookup cost.", "State whether duplicates are allowed.", "Identify which half is sorted.", "Use inclusive comparisons carefully.", "Do not assume ordinary binary search works unchanged."],
    tradeOffs: "Using the wrong half condition.\nForgetting boundaries.\nClaiming O(log n) with heavy duplicates without qualification.",
    commonMistakes: ["Off-by-one loop boundaries.", "Using binary search without sorted/monotonic data.", "Failing to move left/right.", "Mixing [l,r] with [l,r) semantics.", "Claiming O(log n) for the entire workflow when sorting/copying is required.", "How do duplicates change the algorithm?", "How do you find the minimum?", "How many rotations?", "Can you recover the rotation point?", "What if target is absent?", "How do you prove one half is sorted?", "What if array length is 1?", "How do you handle all equal values?", "How would you solve linearly?", "What is the worst case with duplicates?"],
    followUpQuestions: ["What is the invariant?", "Why is it O(log n)?", "What boundary convention do you use?", "How do duplicates change the solution?", "How do you handle empty input?", "Recursive vs iterative?", "Can a Set be better?", "How do you prove correctness?", "What is the preprocessing cost?", "How would you test edge cases?"],
    relatedTopics: ["Binary Search", "Rotated Array", "Sorted Arrays", "Minimum in Rotated Array"],
  },
  {
    id: "dsa-m12-116",
    number: "DSA-M12-116",
    title: "Peak element",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Binary Search",
    expectedAnswer: "A peak is an index whose value is not smaller than its relevant neighbors. Binary search works by comparing nums[mid] with nums[mid+1]: if the slope rises, a peak exists to the right; otherwise one exists at mid or to the left.",
    deepExplanation: "Step 1 — Use the slope.\n```ts\nfunction findPeakElement(nums: readonly number[]): number {\n  if (nums.length === 0) return -1;\n\n  let left = 0;\n  let right = nums.length - 1;\n\n  while (left < right) {\n    const middle = left + Math.floor((right - left) / 2);\n\n    if (nums[middle]! < nums[middle + 1]!) left = middle + 1;\n    else right = middle;\n  }\n\n  return left;\n}\n```\n\nStep 2 — WITH BUILT-IN-friendly linear scan:\n```ts\nfunction findPeakLinear(nums: readonly number[]): number {\n  for (let i = 0; i < nums.length; i++) {\n    const left = i === 0 ? -Infinity : nums[i - 1]!;\n    const right = i === nums.length - 1 ? -Infinity : nums[i + 1]!;\n    if (nums[i]! >= left && nums[i]! >= right) return i;\n  }\n  return -1;\n}\n```\n\nStep 3 — Example:\n[1,2,1,3,5,6,4] → index 1 or 5 is a valid peak; binary search may return 5.\n\nStep 4 — Complexity:\nBinary search O(log n), linear scan O(n).",
    productionExample: "Useful when any valid peak is acceptable. The key is the existence proof: every rising slope leads toward some peak.",
    bestPractices: ["State the sorted/monotonic invariant.", "Choose closed or half-open boundaries and stay consistent.", "Prove why one side can be discarded.", "Handle empty and one-element inputs.", "State preprocessing cost separately from lookup cost.", "Clarify peak definition at boundaries.", "Compare mid with mid+1 only.", "Accept any valid peak unless the problem specifies the first/largest."],
    tradeOffs: "Trying to maximize the value when only a peak index is required.\nReading mid-1 unnecessarily and creating boundary bugs.\nExpecting a unique answer.",
    commonMistakes: ["Off-by-one loop boundaries.", "Using binary search without sorted/monotonic data.", "Failing to move left/right.", "Mixing [l,r] with [l,r) semantics.", "Claiming O(log n) for the entire workflow when sorting/copying is required.", "Why does the slope guarantee a peak?", "What if values contain duplicates?", "How do you find the maximum?", "Can this work in 2D?", "Why is the answer guaranteed?", "What if n=1?", "What is the linear alternative?", "How do you handle boundary peaks?", "What if array is strictly increasing?", "How do you return peak value instead of index?"],
    followUpQuestions: ["What is the invariant?", "Why is it O(log n)?", "What boundary convention do you use?", "How do duplicates change the solution?", "How do you handle empty input?", "Recursive vs iterative?", "Can a Set be better?", "How do you prove correctness?", "What is the preprocessing cost?", "How would you test edge cases?"],
    relatedTopics: ["Binary Search", "Peak Finding", "Divide and Conquer"],
  },
  {
    id: "dsa-m12-117",
    number: "DSA-M12-117",
    title: "Binary search on answer",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Binary Search",
    expectedAnswer: "Binary search on answer works when the answer lies in a numeric range and a feasibility predicate is monotonic: once a capacity/speed/value becomes feasible, all larger values are also feasible, or the reverse.",
    deepExplanation: "Step 1 — Example: minimum ship capacity to deliver packages within D days.\n```ts\nfunction minShipCapacity(\n  weights: readonly number[],\n  days: number,\n): number {\n  let low = 0;\n  let high = 0;\n\n  for (const weight of weights) {\n    low = Math.max(low, weight);\n    high += weight;\n  }\n\n  const canShip = (capacity: number): boolean => {\n    let usedDays = 1;\n    let load = 0;\n\n    for (const weight of weights) {\n      if (load + weight > capacity) {\n        usedDays++;\n        load = 0;\n      }\n      load += weight;\n    }\n\n    return usedDays <= days;\n  };\n\n  while (low < high) {\n    const middle = low + Math.floor((high - low) / 2);\n\n    if (canShip(middle)) high = middle;\n    else low = middle + 1;\n  }\n\n  return low;\n}\n```\n\nStep 2 — WITH BUILT-IN-friendly brute-force baseline:\n```ts\nfunction minShipCapacityLinear(\n  weights: readonly number[],\n  days: number,\n): number {\n  let low = Math.max(...weights);\n  const high = weights.reduce((sum, value) => sum + value, 0);\n\n  for (let capacity = low; capacity <= high; capacity++) {\n    let usedDays = 1;\n    let load = 0;\n\n    for (const weight of weights) {\n      if (load + weight > capacity) {\n        usedDays++;\n        load = 0;\n      }\n      load += weight;\n    }\n\n    if (usedDays <= days) return capacity;\n  }\n\n  return high;\n}\n```\n\nStep 3 — Example:\nweights [1,2,3,1,1], days=4 → minimum capacity = 3.\n\nStep 4 — Complexity:\nBinary search O(n log S), where S = high-low answer range.\nFeasibility check is O(n).",
    productionExample: "Use this pattern only when the feasibility predicate is monotonic. Common cases include capacity, speed, time, minimum maximum load, and allocation limits.",
    bestPractices: ["State the sorted/monotonic invariant.", "Choose closed or half-open boundaries and stay consistent.", "Prove why one side can be discarded.", "Handle empty and one-element inputs.", "State preprocessing cost separately from lookup cost.", "Define low/high from valid answer bounds.", "Prove monotonicity before coding.", "Keep feasibility O(n) or similarly efficient.", "Return the first feasible value for minimization."],
    tradeOffs: "Binary searching unsound answer ranges.\nUsing a non-monotonic predicate.\nOverflow in sums in other languages.\nConfusing maximum feasible with minimum feasible.",
    commonMistakes: ["Off-by-one loop boundaries.", "Using binary search without sorted/monotonic data.", "Failing to move left/right.", "Mixing [l,r] with [l,r) semantics.", "Claiming O(log n) for the entire workflow when sorting/copying is required.", "How do you prove the predicate is monotonic?", "What other problems use this pattern?", "How do you maximize an answer instead?", "How do you choose low/high?", "Why is feasibility O(n)?", "What if days is 1?", "How do you handle zero weights?", "What is answer-space complexity?", "Can two-dimensional answer search work?", "How do you test boundary capacities?"],
    followUpQuestions: ["What is the invariant?", "Why is it O(log n)?", "What boundary convention do you use?", "How do duplicates change the solution?", "How do you handle empty input?", "Recursive vs iterative?", "Can a Set be better?", "How do you prove correctness?", "What is the preprocessing cost?", "How would you test edge cases?"],
    relatedTopics: ["Binary Search", "Monotonic Predicate", "Capacity Allocation", "Optimization"],
  },
  {
    id: "dsa-m12-118",
    number: "DSA-M12-118",
    title: "Search in 2D matrix",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Binary Search",
    expectedAnswer: "For a matrix sorted left-to-right in each row and top-to-bottom across rows, you can treat it as a flattened sorted array and binary-search indices. This applies to the specific fully globally ordered matrix invariant.",
    deepExplanation: "Step 1 — Flattened index mapping.\n```ts\nfunction searchMatrix(\n  matrix: readonly (readonly number[])[],\n  target: number,\n): boolean {\n  const rows = matrix.length;\n  if (rows === 0) return false;\n\n  const cols = matrix[0]!.length;\n  if (cols === 0) return false;\n\n  let left = 0;\n  let right = rows * cols - 1;\n\n  while (left <= right) {\n    const middle = left + Math.floor((right - left) / 2);\n    const row = Math.floor(middle / cols);\n    const col = middle % cols;\n    const value = matrix[row]![col]!;\n\n    if (value === target) return true;\n    if (value < target) left = middle + 1;\n    else right = middle - 1;\n  }\n\n  return false;\n}\n```\n\nStep 2 — WITH BUILT-IN-friendly linear baseline:\n```ts\nfunction searchMatrixLinear(\n  matrix: readonly (readonly number[])[],\n  target: number,\n): boolean {\n  return matrix.some((row) => row.includes(target));\n}\n```\n\nStep 3 — Example:\n[[1,3,5],[7,9,11]], target 9 → true.\n\nStep 4 — Complexity:\nBinary search O(log(R·C)).\nLinear baseline O(R·C).",
    productionExample: "Use flattened binary search only when rows are sorted and every row's first value is greater than the previous row's last value. For a weaker matrix invariant, use another technique.",
    bestPractices: ["State the sorted/monotonic invariant.", "Choose closed or half-open boundaries and stay consistent.", "Prove why one side can be discarded.", "Handle empty and one-element inputs.", "State preprocessing cost separately from lookup cost.", "State the exact matrix invariant.", "Check empty matrix/rows.", "Map flattened index to row/column carefully."],
    tradeOffs: "Using flattened search on a matrix that is only row-sorted.\nDivision/modulo errors.\nIgnoring empty rows.",
    commonMistakes: ["Off-by-one loop boundaries.", "Using binary search without sorted/monotonic data.", "Failing to move left/right.", "Mixing [l,r] with [l,r) semantics.", "Claiming O(log n) for the entire workflow when sorting/copying is required.", "What if only rows are sorted?", "What is the staircase search?", "How do you find coordinates?", "What if rows have different lengths?", "Why is flattening valid here?", "How do you return the index?", "What is complexity?", "Can you search a descending matrix?", "How do duplicates behave?", "How do you test 1x1?"],
    followUpQuestions: ["What is the invariant?", "Why is it O(log n)?", "What boundary convention do you use?", "How do duplicates change the solution?", "How do you handle empty input?", "Recursive vs iterative?", "Can a Set be better?", "How do you prove correctness?", "What is the preprocessing cost?", "How would you test edge cases?"],
    relatedTopics: ["Binary Search", "Matrix Search", "2D Arrays", "Staircase Search"],
  },
  {
    id: "dsa-m12-119",
    number: "DSA-M12-119",
    title: "Capacity allocation problems",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Binary Search",
    expectedAnswer: "Capacity-allocation problems often ask for the minimum capacity, maximum allowed workload, or smallest rate satisfying a deadline. They become binary-search-on-answer problems when feasibility is monotonic.",
    deepExplanation: "Step 1 — Split an array into at most k contiguous groups while minimizing the largest group sum.\n```ts\nfunction splitArrayMinLargestSum(\n  nums: readonly number[],\n  k: number,\n): number {\n  if (nums.length === 0 || k <= 0) return 0;\n\n  let low = 0;\n  let high = 0;\n\n  for (const value of nums) {\n    low = Math.max(low, value);\n    high += value;\n  }\n\n  const canSplit = (limit: number): boolean => {\n    let groups = 1;\n    let current = 0;\n\n    for (const value of nums) {\n      if (current + value > limit) {\n        groups++;\n        current = 0;\n      }\n      current += value;\n    }\n\n    return groups <= k;\n  };\n\n  while (low < high) {\n    const middle = low + Math.floor((high - low) / 2);\n\n    if (canSplit(middle)) high = middle;\n    else low = middle + 1;\n  }\n\n  return low;\n}\n```\n\nStep 2 — WITH BUILT-IN brute-force baseline:\n```ts\nfunction splitArrayLinear(\n  nums: readonly number[],\n  k: number,\n): number {\n  if (nums.length === 0 || k <= 0) return 0;\n\n  let low = Math.max(...nums);\n  const high = nums.reduce((sum, value) => sum + value, 0);\n\n  for (let limit = low; limit <= high; limit++) {\n    let groups = 1;\n    let current = 0;\n\n    for (const value of nums) {\n      if (current + value > limit) {\n        groups++;\n        current = 0;\n      }\n      current += value;\n    }\n\n    if (groups <= k) return limit;\n  }\n\n  return high;\n}\n```\n\nStep 3 — Example:\n[7,2,5,10,8], k=2\nbest split is [7,2,5] and [10,8], answer 18.\n\nStep 4 — Complexity:\nBinary-search solution O(n log S), where S is the answer range.",
    productionExample: "Production frontend examples include batch sizing, upload chunk capacity, worker allocation, rate/capacity planning, and minimizing the maximum load across groups.",
    bestPractices: ["State the sorted/monotonic invariant.", "Choose closed or half-open boundaries and stay consistent.", "Prove why one side can be discarded.", "Handle empty and one-element inputs.", "State preprocessing cost separately from lookup cost.", "Separate answer bounds from feasibility.", "Use maximum element as the minimum capacity for non-negative values.", "Use a monotonic group-count predicate."],
    tradeOffs: "Setting low to 0 when individual values already exceed 0.\nAllowing more groups than k.\nApplying the pattern to data with negative values without rechecking the proof.",
    commonMistakes: ["Off-by-one loop boundaries.", "Using binary search without sorted/monotonic data.", "Failing to move left/right.", "Mixing [l,r] with [l,r) semantics.", "Claiming O(log n) for the entire workflow when sorting/copying is required.", "How do you maximize minimum capacity?", "Why is group count monotonic?", "How do you handle k>=n?", "What if values are zero?", "What if negatives are present?", "How do you reconstruct the partition?", "What is S?", "What other allocation problems fit this pattern?", "How do you test k=1?", "How do you test k=n?"],
    followUpQuestions: ["What is the invariant?", "Why is it O(log n)?", "What boundary convention do you use?", "How do duplicates change the solution?", "How do you handle empty input?", "Recursive vs iterative?", "Can a Set be better?", "How do you prove correctness?", "What is the preprocessing cost?", "How would you test edge cases?"],
    relatedTopics: ["Binary Search", "Binary Search on Answer", "Partitioning", "Optimization"],
  },
  {
    id: "dsa-m12-120",
    number: "DSA-M12-120",
    title: "Binary search performance",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Binary Search",
    expectedAnswer: "Binary search is fast only when its preconditions are satisfied and the implementation preserves a shrinking interval. In frontend systems, algorithmic O(log n) can still lose to preprocessing or data movement costs if the data is not already sorted.",
    deepExplanation: "Step 1 — Compare repeated lookups.\n```ts\nfunction binarySearchMany(\n  nums: readonly number[],\n  targets: readonly number[],\n): boolean[] {\n  return targets.map((target) => binarySearch(nums, target));\n}\n\nfunction binarySearch(\n  nums: readonly number[],\n  target: number,\n): boolean {\n  let left = 0;\n  let right = nums.length - 1;\n\n  while (left <= right) {\n    const middle = left + Math.floor((right - left) / 2);\n\n    if (nums[middle] === target) return true;\n    if (nums[middle]! < target) left = middle + 1;\n    else right = middle - 1;\n  }\n\n  return false;\n}\n```\n\nStep 2 — WITH BUILT-IN alternative:\n```ts\nfunction linearSearchMany(\n  nums: readonly number[],\n  targets: readonly number[],\n): boolean[] {\n  return targets.map((target) => nums.includes(target));\n}\n```\n\nStep 3 — Performance reasoning:\nIf data is already sorted and you have many lookups:\nbinary search ≈ O(q log n).\nIf you must sort first:\nsorting + searches ≈ O(n log n + q log n).\n\nA Set can be better for exact membership:\nbuild O(n) average + q·O(1) average lookup, but it uses extra memory and loses sorted-order queries.\n\nStep 4 — Practical rule:\nChoose the whole-workflow cost, not just the inner lookup complexity.",
    productionExample: "Binary search is excellent for repeated ordered queries, but a hash-based Set may be better for pure membership checks and a single lookup may not justify sorting.",
    bestPractices: ["State the sorted/monotonic invariant.", "Choose closed or half-open boundaries and stay consistent.", "Prove why one side can be discarded.", "Handle empty and one-element inputs.", "State preprocessing cost separately from lookup cost.", "Analyze preprocessing cost.", "Measure the dominant workload.", "Keep the input sorted if ordered queries recur.", "Distinguish exact membership from range/rank queries."],
    tradeOffs: "Saying O(log n) without mentioning sorting cost.\nUsing binary search for one lookup on unsorted data.\nIgnoring memory cost of Set.\nBenchmarking only the inner loop.",
    commonMistakes: ["Off-by-one loop boundaries.", "Using binary search without sorted/monotonic data.", "Failing to move left/right.", "Mixing [l,r] with [l,r) semantics.", "Claiming O(log n) for the entire workflow when sorting/copying is required.", "Binary search vs Set?", "When is sorting worth it?", "How do cache locality and arrays matter?", "How do you benchmark fairly?", "What about typed arrays?", "What if data changes frequently?", "How do lower/upper bounds support ranges?", "When is interpolation search relevant?", "How do duplicates affect query cost?", "How do you optimize many queries?"],
    followUpQuestions: ["What is the invariant?", "Why is it O(log n)?", "What boundary convention do you use?", "How do duplicates change the solution?", "How do you handle empty input?", "Recursive vs iterative?", "Can a Set be better?", "How do you prove correctness?", "What is the preprocessing cost?", "How would you test edge cases?"],
    relatedTopics: ["Binary Search", "Sets", "Lower Bound", "Performance", "Preprocessing"],
  },
];

export const MOCK_DSA_MODULE12_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'DSA',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));