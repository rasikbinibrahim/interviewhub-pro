// Updated TypeScript interview question bank.
// Source metadata preserved; repeated generic examples normalized into topic-specific coding examples.
import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "ts-m14-216",
    number: "TS-M14-216",
    title: "Promise types",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Async TypeScript",
    expectedAnswer: "An async function always returns a Promise of its resolved value, so return types should normally describe Promise<T> explicitly at important API boundaries.",
    deepExplanation: "Step 1 — Understand the topic.\nAn async function always returns a Promise of its resolved value, so return types should normally describe Promise<T> explicitly at important API boundaries.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nasync function loadUser():\n  Promise<{\n    id: string;\n  }> {\n  return {\n    id: \"u1\",\n  };\n}\n\nvoid loadUser().then(\n  user => console.log(user.id),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst loadUser =\n  async (): Promise<User> =>\n    api.getUser();\n```\n\nStep 5 — Example result:\n```text\nu1\n```\n\nStep 6 — Complexity / trade-off.\nAnnotate exported async functions when their result contract is part of a public API.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Promise types**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Promise types**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m14-217",
    number: "TS-M14-217",
    title: "Async return types",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Async TypeScript",
    expectedAnswer: "An async function always returns a Promise of its resolved value, so return types should normally describe Promise<T> explicitly at important API boundaries.",
    deepExplanation: "Step 1 — Understand the topic.\nAn async function always returns a Promise of its resolved value, so return types should normally describe Promise<T> explicitly at important API boundaries.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nasync function loadUser():\n  Promise<{\n    id: string;\n  }> {\n  return {\n    id: \"u1\",\n  };\n}\n\nvoid loadUser().then(\n  user => console.log(user.id),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst loadUser =\n  async (): Promise<User> =>\n    api.getUser();\n```\n\nStep 5 — Example result:\n```text\nu1\n```\n\nStep 6 — Complexity / trade-off.\nAnnotate exported async functions when their result contract is part of a public API.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Async return types**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Async return types**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m14-218",
    number: "TS-M14-218",
    title: "Awaited",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Async TypeScript",
    expectedAnswer: "Awaited should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nAwaited should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Awaited**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Awaited**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m14-219",
    number: "TS-M14-219",
    title: "Typed fetch",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Async TypeScript",
    expectedAnswer: "Typed fetch should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nTyped fetch should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Typed fetch**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Typed fetch**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m14-220",
    number: "TS-M14-220",
    title: "API response generics",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Async TypeScript",
    expectedAnswer: "Generics preserve relationships between input and output types without sacrificing reuse. Prefer constraints that express the minimum required capability.",
    deepExplanation: "Step 1 — Understand the topic.\nAPI response generics should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **API response generics**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **API response generics**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m14-221",
    number: "TS-M14-221",
    title: "Async iterators",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Async TypeScript",
    expectedAnswer: "Async iterators should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nAsync iterators should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Async iterators**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Async iterators**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m14-222",
    number: "TS-M14-222",
    title: "Async generators",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Async TypeScript",
    expectedAnswer: "Async generators yield values over time and can model streamed or incremental asynchronous data.",
    deepExplanation: "Step 1 — Understand the topic.\nAsync generators yield values over time and can model streamed or incremental asynchronous data.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nasync function* values(): AsyncGenerator<number> {\n  yield 1;\n  yield 2;\n}\n\nvoid (async () => {\n  const result: number[] = [];\n\n  for await (\n    const value of values()\n  ) {\n    result.push(value);\n  }\n\n  console.log(result);\n})();\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nfor await (\n  const chunk of stream\n) {\n  renderChunk(chunk);\n}\n```\n\nStep 5 — Example result:\n```text\n[1, 2]\n```\n\nStep 6 — Complexity / trade-off.\nDesign cancellation, ordering, and partial-failure behavior for production streams.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Async generators**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Async generators**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m14-223",
    number: "TS-M14-223",
    title: "AbortController typing",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Async TypeScript",
    expectedAnswer: "AbortController provides a standard cancellation signal for fetch and other APIs that support AbortSignal.",
    deepExplanation: "Step 1 — Understand the topic.\nAbortController provides a standard cancellation signal for fetch and other APIs that support AbortSignal.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Controller = {\n  aborted: boolean;\n};\n\nconst controller: Controller = {\n  aborted: false,\n};\n\nfunction abort(\n  current: Controller,\n): void {\n  current.aborted = true;\n}\n\nabort(controller);\nconsole.log(controller.aborted);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst controller =\n  new AbortController();\n\nfetch(url, {\n  signal: controller.signal,\n});\n\ncontroller.abort();\n```\n\nStep 5 — Example result:\n```text\ntrue\n```\n\nStep 6 — Complexity / trade-off.\nTreat cancellation as a normal control path and prevent stale work from updating state.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **AbortController typing**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **AbortController typing**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m14-224",
    number: "TS-M14-224",
    title: "Concurrency typing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Async TypeScript",
    expectedAnswer: "Concurrency typing should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nConcurrency typing should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Concurrency typing**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Concurrency typing**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m14-225",
    number: "TS-M14-225",
    title: "Async error modeling",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Async TypeScript",
    expectedAnswer: "Async error modeling should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nAsync error modeling should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Async error modeling**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Async error modeling**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos", "# Module 15 – Configuration", "**Questions 226–235**"],
  },
];

export const TYPESCRIPT_QUESTION_SEEDS = QUESTION_SEEDS;
