// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 15 — Security Testing).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m15-1",
    number: "TESTQA-M15-1",
    title: "OWASP Testing",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**OWASP Testing** should map the risk to a concrete attack path, test the control at the trusted boundary, and avoid relying on UI-only checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**OWASP Testing** should map the risk to a concrete attack path, test the control at the trusted boundary, and avoid relying on UI-only checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nMap each risk to a concrete attack path and verify the trusted control.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Map each risk to a concrete attack path and verify the trusted control.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-2",
    number: "TESTQA-M15-2",
    title: "XSS Testing",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**XSS Testing** should exercise untrusted input through the relevant rendering sink and verify it is encoded/sanitized and not executed.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**XSS Testing** should exercise untrusted input through the relevant rendering sink and verify it is encoded/sanitized and not executed.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nExercise malicious inputs through relevant sinks and verify they remain inert.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Exercise malicious inputs through relevant sinks and verify they remain inert.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-3",
    number: "TESTQA-M15-3",
    title: "CSRF Testing",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**CSRF Testing** should exercise state-changing requests with and without the required browser/server defense and verify rejection of forged requests.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**CSRF Testing** should exercise state-changing requests with and without the required browser/server defense and verify rejection of forged requests.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nTest protected and forged state-changing requests and verify rejection of the latter.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test protected and forged state-changing requests and verify rejection of the latter.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-4",
    number: "TESTQA-M15-4",
    title: "JWT Testing",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**JWT Testing** should verify signature, expiry, issuer/audience expectations, invalid/altered tokens, and safe failure behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**JWT Testing** should verify signature, expiry, issuer/audience expectations, invalid/altered tokens, and safe failure behavior.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nTest valid, expired, altered, wrong-audience/issuer, and missing-token cases.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test valid, expired, altered, wrong-audience/issuer, and missing-token cases.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-5",
    number: "TESTQA-M15-5",
    title: "Cookie Security",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**Cookie Security** should validate Secure, HttpOnly, SameSite, path/domain, expiry, and session lifecycle behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Cookie Security** should validate Secure, HttpOnly, SameSite, path/domain, expiry, and session lifecycle behavior.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nVerify secure cookie attributes and session lifecycle.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Verify secure cookie attributes and session lifecycle.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-6",
    number: "TESTQA-M15-6",
    title: "Security Headers",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**Security Headers** should verify expected response headers and ensure the policy remains restrictive without breaking required behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Security Headers** should verify expected response headers and ensure the policy remains restrictive without breaking required behavior.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nVerify required browser security headers and expected policy behavior.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Verify required browser security headers and expected policy behavior.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-7",
    number: "TESTQA-M15-7",
    title: "Authentication Testing",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**Authentication Testing** should cover valid, invalid, expired, locked, logout, refresh, and recovery paths without exposing credentials in test artifacts.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Authentication Testing** should cover valid, invalid, expired, locked, logout, refresh, and recovery paths without exposing credentials in test artifacts.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nCover valid, invalid, expired, logout, refresh, and recovery flows without leaking credentials.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Cover valid, invalid, expired, logout, refresh, and recovery flows without leaking credentials.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-8",
    number: "TESTQA-M15-8",
    title: "Authorization Testing",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**Authorization Testing** should verify permissions at the trusted API/service boundary for allowed and denied roles/resources.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Authorization Testing** should verify permissions at the trusted API/service boundary for allowed and denied roles/resources.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nTest both allowed and denied resources/roles at the trusted service boundary.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test both allowed and denied resources/roles at the trusted service boundary.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-9",
    number: "TESTQA-M15-9",
    title: "Dependency Scanning",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**Dependency Scanning** should verify package inventory, known-vulnerability policy, lockfile integrity, and update/remediation workflow.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Dependency Scanning** should verify package inventory, known-vulnerability policy, lockfile integrity, and update/remediation workflow.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nScan dependency inventory and verify update/remediation policy.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Scan dependency inventory and verify update/remediation policy.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-10",
    number: "TESTQA-M15-10",
    title: "SAST",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**SAST** should identify code-pattern weaknesses early and route actionable findings to owners with a clear severity policy.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**SAST** should identify code-pattern weaknesses early and route actionable findings to owners with a clear severity policy.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nRun code scanning early and turn high-signal findings into owned work.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Run code scanning early and turn high-signal findings into owned work.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-11",
    number: "TESTQA-M15-11",
    title: "DAST",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**DAST** should exercise a deployed application from an attacker perspective while protecting test environments and data.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**DAST** should exercise a deployed application from an attacker perspective while protecting test environments and data.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nRun against deployed test environments with safe data and attack-aware cleanup.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Run against deployed test environments with safe data and attack-aware cleanup.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-12",
    number: "TESTQA-M15-12",
    title: "CSP Validation",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**CSP Validation** should validate the intended CSP directives, reporting, and absence of unsafe bypasses.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**CSP Validation** should validate the intended CSP directives, reporting, and absence of unsafe bypasses.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nValidate directives, reports, rollout, and absence of unsafe bypasses.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Validate directives, reports, rollout, and absence of unsafe bypasses.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-13",
    number: "TESTQA-M15-13",
    title: "Input Validation",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**Input Validation** should cover valid, malformed, boundary, encoded, oversized, and malicious inputs at the correct validation boundary.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Input Validation** should cover valid, malformed, boundary, encoded, oversized, and malicious inputs at the correct validation boundary.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nExercise malformed, boundary, encoded, oversized, and malicious inputs.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Exercise malformed, boundary, encoded, oversized, and malicious inputs.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-14",
    number: "TESTQA-M15-14",
    title: "Secure File Upload Testing",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**Secure File Upload Testing** should verify type/size validation, filename handling, storage isolation, access control, and rejection of malicious or malformed files.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Secure File Upload Testing** should verify type/size validation, filename handling, storage isolation, access control, and rejection of malicious or malformed files.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Finding {\n  name: string;\n  blocked: boolean;\n}\n\nfunction allBlocked(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (!findings[i].blocked) return false;\n  }\n\n  return true;\n}\n\nconst safe = allBlocked([\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst findings = [\n  { name: \"xss-payload\", blocked: true },\n  { name: \"forged-request\", blocked: true },\n];\n\nconst safe = findings.every(\n  (finding) => finding.blocked,\n);\n```\n\nStep 5 — Production example:\nTest type/size validation, storage isolation, access control, and malicious content rejection.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test type/size validation, storage isolation, access control, and malicious content rejection.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-15",
    number: "TESTQA-M15-15",
    title: "Enterprise Security QA",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**Enterprise Security QA** should define ownership, supported environments, baseline governance, CI integration, retention, and failure triage.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Enterprise Security QA** should define ownership, supported environments, baseline governance, CI integration, retention, and failure triage.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Check {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(checks: readonly Check[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst checks = [\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse the smallest deterministic test that provides meaningful confidence.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use the smallest deterministic test that provides meaningful confidence.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
  {
    id: "testqa-m15-16",
    number: "TESTQA-M15-16",
    title: "Security Testing — Module Review",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Security Testing",
    expectedAnswer: "**Security Testing — Module Review** is a module-level revision topic. Be ready to explain the purpose, setup, deterministic strategy, failure diagnosis, and trade-offs.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Security Testing — Module Review** is a module-level revision topic. Be ready to explain the purpose, setup, deterministic strategy, failure diagnosis, and trade-offs.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Check {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(checks: readonly Check[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst checks = [\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse the smallest deterministic test that provides meaningful confidence.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use the smallest deterministic test that provides meaningful confidence.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Security Testing"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE15_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));