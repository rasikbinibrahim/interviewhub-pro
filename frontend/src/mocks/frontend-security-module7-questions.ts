// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 7 — Content Security Policy).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m7-106",
    number: "SEC-M7-106",
    title: "CSP fundamentals",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**CSP fundamentals** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CSP fundamentals** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse CSP fundamentals as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use CSP fundamentals as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-107",
    number: "SEC-M7-107",
    title: "default-src",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**default-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**default-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse default-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use default-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-108",
    number: "SEC-M7-108",
    title: "script-src",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**script-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**script-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse script-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use script-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-109",
    number: "SEC-M7-109",
    title: "style-src",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**style-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**style-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse style-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use style-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-110",
    number: "SEC-M7-110",
    title: "img-src",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**img-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**img-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse img-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use img-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-111",
    number: "SEC-M7-111",
    title: "connect-src",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**connect-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**connect-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse connect-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use connect-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-112",
    number: "SEC-M7-112",
    title: "frame-ancestors",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**frame-ancestors** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**frame-ancestors** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse frame-ancestors as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use frame-ancestors as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-113",
    number: "SEC-M7-113",
    title: "object-src",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**object-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**object-src** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse object-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use object-src as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-114",
    number: "SEC-M7-114",
    title: "nonce",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**nonce** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**nonce** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse nonce as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use nonce as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-115",
    number: "SEC-M7-115",
    title: "hash",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**hash** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**hash** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse hash as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use hash as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-116",
    number: "SEC-M7-116",
    title: "strict-dynamic",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**strict-dynamic** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**strict-dynamic** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse strict-dynamic as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use strict-dynamic as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-117",
    number: "SEC-M7-117",
    title: "CSP reporting",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**CSP reporting** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CSP reporting** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse CSP reporting as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use CSP reporting as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-118",
    number: "SEC-M7-118",
    title: "Report-Only",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**Report-Only** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Report-Only** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse Report-Only as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Report-Only as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-119",
    number: "SEC-M7-119",
    title: "Trusted Types",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**Trusted Types** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Trusted Types** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Trusted Types at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Trusted Types at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m7-120",
    number: "SEC-M7-120",
    title: "CSP Level 3",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Content Security Policy",
    expectedAnswer: "**CSP Level 3** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CSP Level 3** is a Content Security Policy topic. Explain which resource or execution source the directive controls, the secure default, and how rollout/reporting should be handled without creating an accidental compatibility bypass.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Policy {\n  directive: string;\n  values: string[];\n}\n\nfunction directiveValue(\n  policy: Policy,\n): string {\n  let value = policy.directive;\n\n  for (let i = 0; i < policy.values.length; i += 1) {\n    value += ` ${policy.values[i]}`;\n  }\n\n  return value;\n}\n\nconst policy = directiveValue({\n  directive: \"default-src\",\n  values: [\"'self'\"],\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst directives = new Map([\n  [\"default-src\", [\"'self'\"]],\n  [\"object-src\", [\"'none'\"]],\n]);\n\nconst policy = [...directives.entries()]\n  .map(([name, values]) =>\n    `${name} ${values.join(\" \")}`,\n  )\n  .join(\"; \");\n```\n\nStep 5 — Production example:\nUse CSP Level 3 as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use CSP Level 3 as a defense-in-depth browser policy. Roll out with Report-Only first when practical, then enforce a narrow policy and monitor violations.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE7_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));