// Updated TypeScript interview question bank.
// Source metadata preserved; repeated generic examples normalized into topic-specific TypeScript coding examples.
import type { MockTechnicalQuestion } from '@/mocks/questions';
import type { TechnicalQuestionExample } from '@/shared/types/question';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  example?: TechnicalQuestionExample;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "ts-m3-41",
    number: "TS-M3-41",
    title: "var let const with TypeScript",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "let/const are block-scoped; var is function-scoped. TypeScript adds static types but does not change these JavaScript scoping semantics.",
    deepExplanation: "Step 1 — Understand the topic.\nlet/const are block-scoped; var is function-scoped. TypeScript adds static types but does not change these JavaScript scoping semantics.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction example() {\n  let count = 0;\n\n  if (true) {\n    const next = 1;\n    count += next;\n  }\n\n  return count;\n}\n\nconsole.log(example());\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst result = (() => {\n  let count = 0;\n  const next = 1;\n  count += next;\n  return count;\n})();\n```\n\nStep 5 — Example result:\n```text\n1\n```\n\nStep 6 — Complexity / trade-off.\nPrefer const by default, let when reassignment is required, and avoid var in modern TypeScript.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **var let const with TypeScript**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **var let const with TypeScript**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-42",
    number: "TS-M3-42",
    title: "Function parameter types",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "Parameter annotations define the compile-time contract for function inputs.",
    deepExplanation: "Step 1 — Understand the topic.\nParameter annotations define the compile-time contract for function inputs.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction add(\n  left: number,\n  right: number,\n): number {\n  return left + right;\n}\n\nconsole.log(add(2, 3));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst add = (\n  left: number,\n  right: number,\n) => left + right;\n```\n\nStep 5 — Example result:\n```text\n5\n```\n\nStep 6 — Complexity / trade-off.\nAnnotate public APIs and callback contracts clearly.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Function parameter types**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Function parameter types**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-43",
    number: "TS-M3-43",
    title: "Return types",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "Return annotations document and constrain a function's output; inference is fine when the return is obvious and local.",
    deepExplanation: "Step 1 — Understand the topic.\nReturn annotations document and constrain a function's output; inference is fine when the return is obvious and local.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction formatUser(\n  name: string,\n): string {\n  return name.trim();\n}\n\nconsole.log(formatUser(\" Ada \"));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst formatUser = (\n  name: string,\n) => name.trim();\n```\n\nStep 5 — Example result:\n```text\nAda\n```\n\nStep 6 — Complexity / trade-off.\nExplicit return types are especially useful for exported/public functions.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Return types**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Return types**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-44",
    number: "TS-M3-44",
    title: "Optional parameters",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "Optional parameters use ? and become T | undefined inside the function.",
    deepExplanation: "Step 1 — Understand the topic.\nOptional parameters use ? and become T | undefined inside the function.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction greet(\n  name?: string,\n): string {\n  return name ?? \"Guest\";\n}\n\nconsole.log(greet());\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nfunction greet(\n  name = \"Guest\",\n): string {\n  return name;\n}\n```\n\nStep 5 — Example result:\n```text\nGuest\n```\n\nStep 6 — Complexity / trade-off.\nUse optional parameters when omission is meaningful; otherwise prefer required parameters.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Optional parameters**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Optional parameters**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-45",
    number: "TS-M3-45",
    title: "Default parameters",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "Default parameters supply a value when the argument is undefined.",
    deepExplanation: "Step 1 — Understand the topic.\nDefault parameters supply a value when the argument is undefined.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction limit(\n  value: number | undefined,\n  fallback: number,\n): number {\n  return value === undefined\n    ? fallback\n    : value;\n}\n\nconsole.log(limit(undefined, 10));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nfunction limit(\n  value = 10,\n): number {\n  return value;\n}\n```\n\nStep 5 — Example result:\n```text\n10\n```\n\nStep 6 — Complexity / trade-off.\nRemember defaults trigger for undefined, not for every falsy value.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Default parameters**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Default parameters**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-46",
    number: "TS-M3-46",
    title: "Rest parameters",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "Rest parameters collect remaining arguments into a typed array.",
    deepExplanation: "Step 1 — Understand the topic.\nRest parameters collect remaining arguments into a typed array.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction sum(\n  ...values: number[]\n): number {\n  let total = 0;\n\n  for (const value of values) {\n    total += value;\n  }\n\n  return total;\n}\n\nconsole.log(sum(1,2,3));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst total = [1,2,3].reduce(\n  (sum, value) => sum + value,\n  0,\n);\n```\n\nStep 5 — Example result:\n```text\n6\n```\n\nStep 6 — Complexity / trade-off.\nUse rest parameters for variable-arity APIs and type the element array.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Rest parameters**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Rest parameters**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-47",
    number: "TS-M3-47",
    title: "Function overloads",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "Overloads expose multiple call signatures while one implementation handles the runtime behavior.",
    deepExplanation: "Step 1 — Understand the topic.\nOverloads expose multiple call signatures while one implementation handles the runtime behavior.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction format(\n  value: string,\n): string;\nfunction format(\n  value: number,\n): string;\nfunction format(\n  value: string | number,\n): string {\n  return String(value);\n}\n\nconsole.log(format(42));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nfunction format(\n  value: string | number,\n): string {\n  return String(value);\n}\n```\n\nStep 5 — Example result:\n```text\n42\n```\n\nStep 6 — Complexity / trade-off.\nOverloads are useful when return types depend on input shape; avoid overloads when a union is sufficient.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Function overloads**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Function overloads**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-48",
    number: "TS-M3-48",
    title: "Arrow functions",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "Arrow functions provide concise function syntax and lexical this behavior.",
    deepExplanation: "Step 1 — Understand the topic.\nArrow functions provide concise function syntax and lexical this behavior.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nconst add = (\n  left: number,\n  right: number,\n): number => left + right;\n\nconsole.log(add(2,3));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst values = [1,2,3];\nconst doubled = values.map(\n  value => value * 2,\n);\n\nconsole.log(doubled);\n```\n\nStep 5 — Example result:\n```text\n[2, 4, 6]\n```\n\nStep 6 — Complexity / trade-off.\nUse arrows for callbacks and closures; use named functions when intent/stack traces benefit.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Arrow functions**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Arrow functions**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-49",
    number: "TS-M3-49",
    title: "Callback types",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "A callback type describes a function accepted by another function.",
    deepExplanation: "Step 1 — Understand the topic.\nA callback type describes a function accepted by another function.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Mapper = (\n  value: number,\n) => string;\n\nfunction mapOne(\n  value: number,\n  mapper: Mapper,\n): string {\n  return mapper(value);\n}\n\nconsole.log(\n  mapOne(42, String),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst result = [1,2,3].map(\n  value => String(value),\n);\n\nconsole.log(result);\n```\n\nStep 5 — Example result:\n```text\n42\n```\n\nStep 6 — Complexity / trade-off.\nName reusable callback contracts when they appear across modules.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Callback types**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Callback types**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-50",
    number: "TS-M3-50",
    title: "Higher-order functions",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "A higher-order function accepts or returns functions, enabling reusable behavior composition.",
    deepExplanation: "Step 1 — Understand the topic.\nA higher-order function accepts or returns functions, enabling reusable behavior composition.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction withLogging(\n  operation: () => number,\n): () => number {\n  return () => {\n    const result = operation();\n    console.log(result);\n    return result;\n  };\n}\n\nwithLogging(() => 42)();\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst withLogging = (\n  operation: () => number,\n) => () => {\n  const result = operation();\n  console.log(result);\n  return result;\n};\n```\n\nStep 5 — Example result:\n```text\n42\n```\n\nStep 6 — Complexity / trade-off.\nKeep higher-order abstractions simple enough that the call site remains readable.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Higher-order functions**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Higher-order functions**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-51",
    number: "TS-M3-51",
    title: "Generic functions",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "Generics preserve relationships between input and output types without sacrificing reuse. Prefer constraints that express the minimum required capability.",
    deepExplanation: "Step 1 — Understand the topic.\nGeneric functions preserve the relationship between input and output types without hard-coding one concrete type.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction first<T>(\n  values: T[],\n): T | undefined {\n  return values.length > 0\n    ? values[0]\n    : undefined;\n}\n\nconsole.log(first([\"Ada\",\"Grace\"]));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst first =\n  <T,>(values: T[]) => values[0];\n```\n\nStep 5 — Example result:\n```text\nAda\n```\n\nStep 6 — Complexity / trade-off.\nUse generic constraints when the implementation requires specific capabilities.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Generic functions**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Generic functions**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-52",
    number: "TS-M3-52",
    title: "Function type aliases",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "Function type aliases is a TypeScript concept that should be understood in terms of compile-time behavior, runtime JavaScript, practical typing, and the trade-offs of using it in a production codebase.",
    deepExplanation: "Step 1 — Understand the topic.\nFunction type aliases is a TypeScript concept that should be understood in terms of compile-time behavior, runtime JavaScript, practical typing, and the trade-offs of using it in a production codebase.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: number;\n};\n\nfunction update(value: Value): Value {\n  return {\n    value: value.value + 1,\n  };\n}\n\nconsole.log(update({ value: 0 }));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = {\n  value: 0,\n};\n\nconst next = {\n  ...value,\n  value: value.value + 1,\n};\n\nconsole.log(next);\n```\n\nStep 5 — Example result:\n```text\n{ value: 1 }\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest type design that preserves useful safety without adding unnecessary type-level complexity.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Function type aliases**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Function type aliases**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-53",
    number: "TS-M3-53",
    title: "this parameter",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "A TypeScript this parameter gives a function an explicit compile-time type for its this context; it is removed from emitted JavaScript.",
    deepExplanation: "Step 1 — Understand the topic.\nA TypeScript this parameter gives a function an explicit compile-time type for its this context; it is removed from emitted JavaScript.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Formatter = {\n  prefix: string;\n};\n\nfunction format(\n  this: Formatter,\n  value: string,\n): string {\n  return `${this.prefix}${value}`;\n}\n\nconst formatter: Formatter = {\n  prefix: \"#\",\n};\n\nconsole.log(\n  format.call(\n    formatter,\n    \"42\",\n  ),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst formatter = {\n  prefix: \"#\",\n  format(value: string) {\n    return `${this.prefix}${value}`;\n  },\n};\n\nconsole.log(\n  formatter.format(\"42\"),\n);\n```\n\nStep 5 — Example result:\n```text\n#42\n```\n\nStep 6 — Complexity / trade-off.\nPrefer object methods/closures when they are simpler than explicit this binding.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **this parameter**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **this parameter**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-54",
    number: "TS-M3-54",
    title: "Type predicates",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "A type predicate such as value is string tells the compiler that a boolean function has established a narrower type.",
    deepExplanation: "Step 1 — Understand the topic.\nA type predicate such as value is string tells the compiler that a boolean function has established a narrower type.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction isString(\n  value: unknown,\n): value is string {\n  return typeof value === \"string\";\n}\n\nconst value: unknown = \"Ada\";\n\nif (isString(value)) {\n  console.log(value.toUpperCase());\n}\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value: unknown = \"Ada\";\n\nif (typeof value === \"string\") {\n  console.log(value.toUpperCase());\n}\n```\n\nStep 5 — Example result:\n```text\nADA\n```\n\nStep 6 — Complexity / trade-off.\nUse predicates when the same narrowing rule is reused across modules.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Type predicates**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Type predicates**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m3-55",
    number: "TS-M3-55",
    title: "Function best practices",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Variables & Functions",
    expectedAnswer: "Function best practices is a TypeScript concept that should be understood in terms of compile-time behavior, runtime JavaScript, practical typing, and the trade-offs of using it in a production codebase.",
    deepExplanation: "Step 1 — Understand the topic.\nFunction best practices is a TypeScript concept that should be understood in terms of compile-time behavior, runtime JavaScript, practical typing, and the trade-offs of using it in a production codebase.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: number;\n};\n\nfunction update(value: Value): Value {\n  return {\n    value: value.value + 1,\n  };\n}\n\nconsole.log(update({ value: 0 }));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = {\n  value: 0,\n};\n\nconst next = {\n  ...value,\n  value: value.value + 1,\n};\n\nconsole.log(next);\n```\n\nStep 5 — Example result:\n```text\n{ value: 1 }\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest type design that preserves useful safety without adding unnecessary type-level complexity.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Function best practices**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Function best practices**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos", "# Module 4 – Interfaces", "**Questions 56–75**"],
  },
];

export const TYPESCRIPT_QUESTION_SEEDS = QUESTION_SEEDS;