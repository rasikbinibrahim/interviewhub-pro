// Derived from frontend/src/document/Part_6_Browser_Internals_Web_APIs_Master_Handbook.md (Module 12 — WebSockets & SSE).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question, Execution/Flow, Advanced Production Example, Performance/Security Notes,
// Best Practices, Advantages/Disadvantages, Common Mistakes/Interview Traps, Senior
// Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "jsbrowser-m12-186",
    number: "JSBROWSER-M12-186",
    title: "WebSocket protocol",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "WebSockets & SSE",
    expectedAnswer: "WebSocket provides a persistent, full-duplex communication channel between browser and server after an HTTP-based opening handshake upgrades the connection. Once established, the browser exposes open, message, error and close events and can send/receive frames without repeated HTTP request setup.",
    deepExplanation: "js\nconst socket = new WebSocket(\"wss://api.example.com/realtime\");\n\nsocket.addEventListener(\"open\", () => {\n  socket.send(JSON.stringify({ type: \"subscribe\", channel: \"orders\" }));\n});\n\nsocket.addEventListener(\"message\", event => {\n  const message = JSON.parse(event.data);\n  updateOrderFeed(message);\n});\n\nsocket.addEventListener(\"close\", event => {\n  console.log(\"closed\", event.code, event.reason);\n});\n```",
    productionExample: "A trading or operations dashboard uses one authenticated WebSocket connection to receive live order/status events. The application keeps connection state separate from domain state and renders only meaningful updates.",
    bestPractices: ["Use `wss://` in production.","Define a typed message protocol.","Track connection state explicitly.","Validate incoming payloads.","Clean up sockets on feature teardown.","Measure message rate and client processing time."],
    tradeOffs: "WebSockets give low-latency bidirectional communication, but long-lived connections add lifecycle, reconnect, scaling, memory and observability complexity. They are unnecessary when simple request/response or one-way streaming is sufficient.",
    commonMistakes: ["Calling WebSocket a request/response API.","Assuming a successful TCP/WebSocket connection means application authentication is complete.","Trusting every received message.","Opening one socket per component."],
    followUpQuestions: ["WebSocket vs SSE?","How does the opening handshake work?","How would you authenticate a WebSocket connection?","What happens when the tab becomes hidden?"],
    relatedTopics: ["WebSocket","HTTP upgrade","Full-duplex","Realtime","wss","Connection state"],
  },
  {
    id: "jsbrowser-m12-187",
    number: "JSBROWSER-M12-187",
    title: "WebSocket lifecycle",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "WebSockets & SSE",
    expectedAnswer: "The practical WebSocket lifecycle is: construct → connecting → open → message traffic → closing/closed, with error events possible at any stage. Production code should make connection ownership explicit, remove listeners, cancel reconnect timers and reconcile in-flight state when the connection closes.",
    deepExplanation: "ts\ntype SocketState = \"idle\" | \"connecting\" | \"open\" | \"closing\" | \"closed\";\n\nfunction attachSocket(socket: WebSocket) {\n  socket.onopen = () => setState(\"open\");\n  socket.onclose = () => {\n    setState(\"closed\");\n    scheduleReconnect();\n  };\n  socket.onerror = handleSocketError;\n}\n```",
    productionExample: "A React application owns a socket through a feature-level service, not individual row components. Route changes dispose the connection and reconnect logic is centralized so duplicate sockets cannot accumulate.",
    bestPractices: ["Use one clear owner.","Represent lifecycle state explicitly.","Cleanup event handlers and timers.","Avoid reconnecting after intentional logout/unmount.","Use telemetry for connection duration and close codes."],
    tradeOffs: "The lifecycle is simple conceptually but race conditions appear when reconnect, logout and component unmount happen together. Centralized ownership reduces those races.",
    commonMistakes: ["Creating sockets in render.","Scheduling reconnect after an intentional close.","Leaving multiple listeners attached across reconnects.","Treating `error` as the complete lifecycle signal without handling `close`."],
    followUpQuestions: ["How do you prevent duplicate sockets?","What is the difference between `error` and `close`?","How should logout interact with reconnect logic?"],
    relatedTopics: ["WebSocket lifecycle","CloseEvent","ErrorEvent","Cleanup","Reconnect"],
  },
  {
    id: "jsbrowser-m12-188",
    number: "JSBROWSER-M12-188",
    title: "WebSocket reconnect",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "WebSockets & SSE",
    expectedAnswer: "WebSocket reconnect is an application-level strategy for restoring a connection after an unexpected close. A robust strategy uses bounded exponential backoff with jitter, stops after intentional shutdown, avoids connection storms and resynchronizes missed state after reconnect.",
    deepExplanation: "js\nlet attempt = 0;\n\nfunction nextDelay() {\n  const base = Math.min(30_000, 500 * 2 ** attempt);\n  attempt += 1;\n  return base * (0.8 + Math.random() * 0.4);\n}\n\nfunction reconnect() {\n  setTimeout(connect, nextDelay());\n}\n```",
    productionExample: "A collaboration app reconnects after laptop sleep/network changes, then sends a resume token or fetches a fresh snapshot before applying live events so the client does not silently miss state changes.",
    bestPractices: ["Use exponential backoff and jitter.","Reset the attempt counter after a stable connection.","Do not reconnect after intentional shutdown.","Reconcile missed events after reconnect.","Respect server retry/maintenance signals."],
    tradeOffs: "Reconnect improves resilience but can amplify an outage if every client retries at once. Backoff/jitter adds delay but protects the service.",
    commonMistakes: ["Immediate infinite reconnect loops.","No jitter.","Assuming reconnection restores missed events automatically.","Reconnecting after logout."],
    followUpQuestions: ["How do you avoid a thundering herd?","How do you recover missed events?","When should reconnect stop?"],
    relatedTopics: ["Reconnect","Backoff","Jitter","Resynchronization","Connection recovery"],
  },
  {
    id: "jsbrowser-m12-189",
    number: "JSBROWSER-M12-189",
    title: "WebSocket heartbeat",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "WebSockets & SSE",
    expectedAnswer: "A WebSocket heartbeat is an application-level liveness mechanism used to detect dead connections or intermediaries that otherwise appear open. Browsers expose WebSocket events but application/server heartbeat design depends on the protocol and infrastructure; native WebSocket ping/pong control is typically server/library-side rather than a browser `ping()` API.",
    deepExplanation: "js\nconst heartbeat = setInterval(() => {\n  if (socket.readyState === WebSocket.OPEN) {\n    socket.send(JSON.stringify({ type: \"heartbeat\" }));\n  }\n}, 30_000);\n\n// Clear heartbeat when socket ownership ends.\nclearInterval(heartbeat);\n```",
    productionExample: "A realtime dashboard sends a lightweight application heartbeat and expects an acknowledgment within a configured window. Missing acknowledgments trigger controlled reconnect while visible status shows 'reconnecting'.",
    bestPractices: ["Keep heartbeat payloads tiny.","Pause/adjust behavior for hidden tabs when appropriate.","Clear timers on close/unmount.","Use server-side idle/timeout policy consistently.","Do not treat heartbeat success as full application health."],
    tradeOffs: "Heartbeats detect stale connections but add traffic and battery usage. Too-short intervals waste resources; too-long intervals delay failure detection.",
    commonMistakes: ["Using a huge JSON payload as heartbeat.","Forgetting timer cleanup.","Assuming heartbeat proves the server processed business messages.","Using browser `WebSocket.ping()` as if it were a public API."],
    followUpQuestions: ["Ping/pong vs application heartbeat?","How should heartbeat intervals be chosen?","How do hidden tabs affect timers?"],
    relatedTopics: ["Heartbeat","Ping/Pong","Liveness","Timers","WebSocket"],
  },
  {
    id: "jsbrowser-m12-190",
    number: "JSBROWSER-M12-190",
    title: "WebSocket backpressure",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "WebSockets & SSE",
    expectedAnswer: "Backpressure occurs when messages arrive faster than the browser/application can process or render them. Native WebSocket APIs expose `bufferedAmount` for client-side outbound buffering, but incoming-message pressure is largely an application architecture problem. Production systems often batch, drop obsolete updates, coalesce events or move heavy processing off the main thread.",
    deepExplanation: "js\nfunction sendIfSafe(socket: WebSocket, payload: string) {\n  if (socket.bufferedAmount > 1_000_000) {\n    return; // application-specific backpressure policy\n  }\n\n  socket.send(payload);\n}\n```",
    productionExample: "A market-data UI receives thousands of price updates per second. Instead of rendering every event, it stores the latest value per symbol and commits one visual update per animation frame.",
    bestPractices: ["Measure message rate and processing time.","Coalesce superseded events.","Separate data ingestion from rendering.","Use workers for suitable CPU-heavy processing.","Bound queues and buffers.","Use `bufferedAmount` for outbound pressure."],
    tradeOffs: "Backpressure improves stability but can reduce fidelity by dropping/coalescing data. The correct policy depends on whether every event is business-critical.",
    commonMistakes: ["Rendering every incoming message.","Allowing unbounded arrays of pending events.","Using `bufferedAmount` as a measure of incoming pressure.","Dropping events that must never be lost."],
    followUpQuestions: ["How do you design backpressure for live price data?","What data can be safely coalesced?","When would you use a worker?"],
    relatedTopics: ["Backpressure","bufferedAmount","Coalescing","Workers","Real-time rendering"],
  },
  {
    id: "jsbrowser-m12-191",
    number: "JSBROWSER-M12-191",
    title: "Server-Sent Events",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "WebSockets & SSE",
    expectedAnswer: "Server-Sent Events (SSE) provides a one-way server-to-browser event stream over HTTP using the `text/event-stream` format. The browser's EventSource API maintains the connection and dispatches named/message events; it is a strong fit when the server pushes updates and the browser does not need bidirectional messaging.",
    deepExplanation: "js\nconst source = new EventSource(\"/api/events\");\n\nsource.onmessage = event => {\n  const data = JSON.parse(event.data);\n  updateFeed(data);\n};\n\nsource.onerror = () => {\n  showConnectionState(\"reconnecting\");\n};\n```",
    productionExample: "A notification or operations dashboard uses SSE for one-way live updates, while ordinary fetch handles commands/mutations. This avoids building a bidirectional protocol when the browser only receives events.",
    bestPractices: ["Use SSE when traffic is naturally server-to-client.","Handle connection errors and lifecycle cleanup.","Parse event payloads defensively.","Use meaningful event types/IDs.","Keep each event small."],
    tradeOffs: "SSE is simpler than WebSocket for one-way streaming and has browser-managed reconnection semantics, but it is not bidirectional and has HTTP/proxy/infrastructure constraints.",
    commonMistakes: ["Using SSE when the client must constantly send realtime messages.","Assuming SSE is a generic WebSocket replacement.","Ignoring proxy buffering/timeouts.","Sending huge event payloads."],
    followUpQuestions: ["SSE vs WebSocket?","How does EventSource reconnect?","What does the `text/event-stream` format look like?"],
    relatedTopics: ["SSE","EventSource","text/event-stream","Realtime"],
  },
  {
    id: "jsbrowser-m12-192",
    number: "JSBROWSER-M12-192",
    title: "SSE lifecycle",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "WebSockets & SSE",
    expectedAnswer: "An EventSource lifecycle is created → CONNECTING → OPEN → message/error events → CLOSED. The browser manages much of the reconnect behavior, while application code still owns event listeners, cleanup, authentication strategy and UI connection state.",
    deepExplanation: "js\nconst source = new EventSource(\"/events\");\n\nsource.onopen = () => setStatus(\"open\");\nsource.onmessage = handleMessage;\nsource.onerror = () => setStatus(\"connecting\");\n\n// Later:\nsource.close();\n```",
    productionExample: "A React feature creates EventSource once for a route-level realtime feed and calls `close()` during cleanup. The UI distinguishes connecting, open and disconnected states.",
    bestPractices: ["Own one EventSource per required stream.","Call `close()` on teardown.","Handle error/reconnect state.","Use event IDs when resuming matters.","Keep parsing and rendering lightweight."],
    tradeOffs: "EventSource simplifies reconnecting one-way streams but leaves application-level state reconciliation and resource cleanup to the application.",
    commonMistakes: ["Creating EventSource on every render.","Never calling close().","Assuming error means permanent failure.","Ignoring stale/replayed events after reconnect."],
    followUpQuestions: ["What state does EventSource expose?","When does onerror fire?","How does EventSource reconnect?"],
    relatedTopics: ["EventSource","SSE lifecycle","Connection state","Cleanup"],
  },
  {
    id: "jsbrowser-m12-193",
    number: "JSBROWSER-M12-193",
    title: "SSE reconnection",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "WebSockets & SSE",
    expectedAnswer: "SSE reconnection is largely built into the EventSource model. The browser attempts to reconnect after connection loss and can use the event stream's retry information. The server can send event IDs so the client can communicate where it last received data, allowing recovery from missed events when the backend supports replay.",
    deepExplanation: "text\nServer:\nid: 1042\nevent: order.updated\ndata: {...}\n\nClient reconnect:\nLast-Event-ID: 1042\n```\n\nThe server can use the last received event ID to replay events that occurred after the disconnect.",
    productionExample: "A production notification stream assigns monotonically increasing event IDs and stores a short replay window. After reconnect, the server resumes from Last-Event-ID; otherwise the client requests a fresh snapshot.",
    bestPractices: ["Use event IDs for resumable streams.","Define a replay window.","Handle duplicate/replayed events idempotently.","Clean up EventSource instances.","Expose reconnect state to users."],
    tradeOffs: "Replay support increases server/storage complexity but gives much stronger correctness. Without replay, SSE reconnection can recover the connection but still lose events.",
    commonMistakes: ["Assuming reconnect guarantees zero missed events.","Not using IDs for important event streams.","Appending duplicates after replay.","Creating custom reconnect loops that fight EventSource behavior."],
    followUpQuestions: ["How does Last-Event-ID work?","How would you guarantee exactly-once business effects?","When should a client refresh from a full snapshot?"],
    relatedTopics: ["SSE reconnect","Last-Event-ID","Event IDs","Replay","Idempotency"],
  },
  {
    id: "jsbrowser-m12-194",
    number: "JSBROWSER-M12-194",
    title: "WebSocket vs SSE",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "WebSockets & SSE",
    expectedAnswer: "WebSocket is a bidirectional message channel, while SSE is a server-to-browser event stream over HTTP. Choose WebSocket when the client and server both need realtime messages or low-latency bidirectional interaction; choose SSE when server-to-client streaming is the dominant requirement and HTTP semantics/proxies are a better fit.",
    deepExplanation: "text\nWebSocket:\nClient ⇄ Server\n\nSSE:\nClient ← Server\n```\n\nExamples:\n- Collaborative cursor/control traffic → WebSocket\n- Deployment/job/status feed → SSE",
    productionExample: "A monitoring dashboard uses SSE for status events and ordinary fetch for mutations. A collaborative editor uses WebSocket because edits, presence and acknowledgments flow in both directions.",
    bestPractices: ["Choose based on communication direction and protocol needs.","Consider proxy/load-balancer behavior.","Design reconnect/resync explicitly.","Measure connection counts and message rates.","Keep payloads typed/versioned."],
    tradeOffs: "WebSocket offers more flexibility but more protocol/lifecycle complexity. SSE is simpler for one-way streaming but cannot natively send client messages on the same stream.",
    commonMistakes: ["Choosing WebSocket for every realtime feature.","Choosing SSE for bidirectional collaboration.","Ignoring infrastructure/proxy limits.","Comparing only raw latency without operational fit."],
    followUpQuestions: ["How do you choose between them?","How does SSE scale behind proxies?","Can SSE support authentication?"],
    relatedTopics: ["WebSocket","SSE","EventSource","Realtime architecture"],
  },
  {
    id: "jsbrowser-m12-195",
    number: "JSBROWSER-M12-195",
    title: "Real-time frontend architecture",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "WebSockets & SSE",
    expectedAnswer: "A production realtime frontend separates transport from state and rendering. A connection layer handles protocol/lifecycle/reconnect, a normalization layer applies events to domain state, and the UI subscribes to efficient state slices. The architecture must define ordering, deduplication, resync, backpressure, visibility and failure behavior.",
    deepExplanation: "text\nWebSocket/SSE\n      ↓\nTransport adapter\n      ↓\nEvent validation\n      ↓\nDomain state / cache\n      ↓\nSelective UI subscriptions\n      ↓\nRendering\n```\n\nThe transport should never directly mutate arbitrary UI components.",
    productionExample: "A large SaaS console uses a shared realtime service per tenant/session, normalizes events into a client cache, coalesces high-frequency updates and refreshes a snapshot after reconnect gaps.",
    bestPractices: ["Separate transport from UI.","Define event schemas/versioning.","Make event application idempotent.","Use selective subscriptions.","Plan reconnect/resync.","Instrument latency from server event to paint."],
    tradeOffs: "This architecture scales better than per-component sockets but adds coordination and state-management complexity. Centralized transport can also become a bottleneck if ownership is poorly designed.",
    commonMistakes: ["Opening a socket from every component.","Directly mutating UI from socket callbacks.","No event versioning.","No recovery strategy after missed messages."],
    followUpQuestions: ["How do you avoid duplicate realtime connections?","How would you handle event ordering?","How do you measure end-to-end realtime latency?"],
    relatedTopics: ["Realtime architecture","WebSocket","SSE","Event ordering","State normalization","Observability"],
  },

];

export const MOCK_BROWSER_INTERNALS_MODULE12_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Browser/Web APIs',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));