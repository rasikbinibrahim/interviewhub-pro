// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 18 — Real-world Testing Scenarios).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m18-1",
    number: "TESTQA-M18-1",
    title: "Login",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Login** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Login** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-2",
    number: "TESTQA-M18-2",
    title: "Shopping Cart",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Shopping Cart** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Shopping Cart** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-3",
    number: "TESTQA-M18-3",
    title: "Checkout",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Checkout** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Checkout** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-4",
    number: "TESTQA-M18-4",
    title: "Search",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Search** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Search** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-5",
    number: "TESTQA-M18-5",
    title: "Dashboard",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Dashboard** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Dashboard** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-6",
    number: "TESTQA-M18-6",
    title: "Infinite Scroll",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Infinite Scroll** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Infinite Scroll** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-7",
    number: "TESTQA-M18-7",
    title: "Calendar",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Calendar** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Calendar** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-8",
    number: "TESTQA-M18-8",
    title: "Chat",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Chat** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Chat** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-9",
    number: "TESTQA-M18-9",
    title: "Notifications",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Notifications** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Notifications** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-10",
    number: "TESTQA-M18-10",
    title: "Banking Application",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Banking Application** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Banking Application** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-11",
    number: "TESTQA-M18-11",
    title: "Healthcare Portal",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Healthcare Portal** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Healthcare Portal** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-12",
    number: "TESTQA-M18-12",
    title: "CRM",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**CRM** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**CRM** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-13",
    number: "TESTQA-M18-13",
    title: "ERP",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**ERP** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**ERP** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-14",
    number: "TESTQA-M18-14",
    title: "Kanban Board",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Kanban Board** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Kanban Board** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-15",
    number: "TESTQA-M18-15",
    title: "Admin Dashboard",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Admin Dashboard** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Admin Dashboard** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-16",
    number: "TESTQA-M18-16",
    title: "Offline Recovery",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Offline Recovery** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Offline Recovery** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-17",
    number: "TESTQA-M18-17",
    title: "Production Bug Reproduction",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Production Bug Reproduction** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Production Bug Reproduction** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
  {
    id: "testqa-m18-18",
    number: "TESTQA-M18-18",
    title: "Real-world Testing Scenarios — Module Review",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Real-world Testing Scenarios",
    expectedAnswer: "**Real-world Testing Scenarios — Module Review** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Real-world Testing Scenarios — Module Review** is best tested as a realistic user workflow with business rules, failures, accessibility, data integrity, and recovery explicitly covered.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Step {\n  name: string;\n  completed: boolean;\n}\n\nfunction workflowReady(\n  steps: readonly Step[],\n): boolean {\n  for (let i = 0; i < steps.length; i += 1) {\n    if (!steps[i].completed) return false;\n  }\n\n  return steps.length > 0;\n}\n\nconst ready = workflowReady([\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst steps = [\n  { name: \"login\", completed: true },\n  { name: \"checkout\", completed: true },\n];\n\nconst ready =\n  steps.length > 0 &&\n  steps.every((step) => step.completed);\n```\n\nStep 5 — Production example:\nTest the realistic user workflow with business rules, errors, security, accessibility, and recovery.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test the realistic user workflow with business rules, errors, security, accessibility, and recovery.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Real-world Testing Scenarios"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE18_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));