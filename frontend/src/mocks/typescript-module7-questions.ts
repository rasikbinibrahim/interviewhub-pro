// Updated TypeScript interview question bank.
// Source metadata preserved; repeated generic examples normalized into topic-specific TypeScript coding examples.
import type { MockTechnicalQuestion } from '@/mocks/questions';
import type { TechnicalQuestionExample } from '@/shared/types/question';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  example?: TechnicalQuestionExample;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "ts-m7-111",
    number: "TS-M7-111",
    title: "Classes in TypeScript",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "A TypeScript class combines runtime JavaScript behavior with compile-time type checking for fields, methods, constructors and interfaces.",
    deepExplanation: "Step 1 — Understand the topic.\nA TypeScript class combines runtime JavaScript behavior with compile-time type checking for fields, methods, constructors and interfaces.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nclass User {\n  constructor(\n    public name: string,\n  ) {}\n\n  greet(): string {\n    return `Hello ${this.name}`;\n  }\n}\n\nconst user =\n  new User(\"Ada\");\n\nconsole.log(user.greet());\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nclass User {\n  constructor(\n    public name: string,\n  ) {}\n\n  greet() {\n    return `Hello ${this.name}`;\n  }\n}\n```\n\nStep 5 — Example result:\n```text\nHello Ada\n```\n\nStep 6 — Complexity / trade-off.\nUse classes when object identity/behavior and lifecycle make them a good runtime abstraction.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Classes in TypeScript**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Classes in TypeScript**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-112",
    number: "TS-M7-112",
    title: "Access modifiers",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "public, private and protected control TypeScript access at compile time; JavaScript #private fields provide true runtime privacy.",
    deepExplanation: "Step 1 — Understand the topic.\npublic, private and protected control TypeScript access at compile time; JavaScript #private fields provide true runtime privacy.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nclass Account {\n  private balance = 0;\n\n  deposit(\n    amount: number,\n  ): void {\n    this.balance += amount;\n  }\n\n  getBalance(): number {\n    return this.balance;\n  }\n}\n\nconst account = new Account();\naccount.deposit(100);\n\nconsole.log(\n  account.getBalance(),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nclass Account {\n  #balance = 0;\n\n  deposit(\n    amount: number,\n  ) {\n    this.#balance += amount;\n  }\n}\n```\n\nStep 5 — Example result:\n```text\n100\n```\n\nStep 6 — Complexity / trade-off.\nUse #private when runtime encapsulation matters; TS private is primarily a compile-time restriction.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Access modifiers**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Access modifiers**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-113",
    number: "TS-M7-113",
    title: "public private protected",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "public, private and protected control TypeScript access at compile time; JavaScript #private fields provide true runtime privacy.",
    deepExplanation: "Step 1 — Understand the topic.\npublic, private and protected control TypeScript access at compile time; JavaScript #private fields provide true runtime privacy.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nclass Account {\n  private balance = 0;\n\n  deposit(\n    amount: number,\n  ): void {\n    this.balance += amount;\n  }\n\n  getBalance(): number {\n    return this.balance;\n  }\n}\n\nconst account = new Account();\naccount.deposit(100);\n\nconsole.log(\n  account.getBalance(),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nclass Account {\n  #balance = 0;\n\n  deposit(\n    amount: number,\n  ) {\n    this.#balance += amount;\n  }\n}\n```\n\nStep 5 — Example result:\n```text\n100\n```\n\nStep 6 — Complexity / trade-off.\nUse #private when runtime encapsulation matters; TS private is primarily a compile-time restriction.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **public private protected**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **public private protected**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-114",
    number: "TS-M7-114",
    title: "readonly properties",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "A readonly property cannot be reassigned through the class type after initialization.",
    deepExplanation: "Step 1 — Understand the topic.\nA readonly property cannot be reassigned through the class type after initialization.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nclass User {\n  readonly id: string;\n\n  constructor(id: string) {\n    this.id = id;\n  }\n}\n\nconst user =\n  new User(\"u1\");\n\nconsole.log(user.id);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nclass User {\n  constructor(\n    public readonly id: string,\n  ) {}\n}\n```\n\nStep 5 — Example result:\n```text\nu1\n```\n\nStep 6 — Complexity / trade-off.\nReadonly does not deep-freeze nested objects.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **readonly properties**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **readonly properties**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-115",
    number: "TS-M7-115",
    title: "Parameter properties",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "Parameter properties let constructor parameters declare and initialize class fields in one concise syntax.",
    deepExplanation: "Step 1 — Understand the topic.\nParameter properties let constructor parameters declare and initialize class fields in one concise syntax.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nclass User {\n  constructor(\n    public readonly id: string,\n  ) {}\n}\n\nconsole.log(\n  new User(\"u1\").id,\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nclass User {\n  constructor(\n    public id: string,\n  ) {}\n}\n```\n\nStep 5 — Example result:\n```text\nu1\n```\n\nStep 6 — Complexity / trade-off.\nUse them when the concise constructor contract remains readable.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Parameter properties**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Parameter properties**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-116",
    number: "TS-M7-116",
    title: "Constructors",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "Constructors is a TypeScript concept that should be understood in terms of compile-time behavior, runtime JavaScript, practical typing, and the trade-offs of using it in a production codebase.",
    deepExplanation: "Step 1 — Understand the topic.\nConstructors is a TypeScript concept that should be understood in terms of compile-time behavior, runtime JavaScript, practical typing, and the trade-offs of using it in a production codebase.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: number;\n};\n\nfunction update(value: Value): Value {\n  return {\n    value: value.value + 1,\n  };\n}\n\nconsole.log(update({ value: 0 }));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = {\n  value: 0,\n};\n\nconst next = {\n  ...value,\n  value: value.value + 1,\n};\n\nconsole.log(next);\n```\n\nStep 5 — Example result:\n```text\n{ value: 1 }\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest type design that preserves useful safety without adding unnecessary type-level complexity.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Constructors**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Constructors**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-117",
    number: "TS-M7-117",
    title: "Abstract classes",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "Abstract classes define reusable base behavior and require subclasses to implement abstract members.",
    deepExplanation: "Step 1 — Understand the topic.\nAbstract classes define reusable base behavior and require subclasses to implement abstract members.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nabstract class Payment {\n  abstract pay(\n    amount: number,\n  ): string;\n\n  receipt(\n    amount: number,\n  ): string {\n    return `Paid ${amount}`;\n  }\n}\n\nclass CardPayment extends Payment {\n  pay(amount: number): string {\n    return this.receipt(amount);\n  }\n}\n\nconsole.log(\n  new CardPayment().pay(100),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nabstract class Payment {\n  abstract pay(\n    amount: number,\n  ): string;\n}\n```\n\nStep 5 — Example result:\n```text\nPaid 100\n```\n\nStep 6 — Complexity / trade-off.\nPrefer composition/interfaces when inheritance is not a strong domain relationship.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Abstract classes**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Abstract classes**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-118",
    number: "TS-M7-118",
    title: "Implements",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "implements checks that a class satisfies an interface contract without changing the class's runtime inheritance.",
    deepExplanation: "Step 1 — Understand the topic.\nimplements checks that a class satisfies an interface contract without changing the class's runtime inheritance.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface Logger {\n  log(\n    message: string,\n  ): void;\n}\n\nclass ConsoleLogger\n  implements Logger {\n  log(\n    message: string,\n  ): void {\n    console.log(message);\n  }\n}\n\nnew ConsoleLogger().log(\"ok\");\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nclass ConsoleLogger\n  implements Logger {\n  log(message: string) {\n    console.log(message);\n  }\n}\n```\n\nStep 5 — Example result:\n```text\nok\n```\n\nStep 6 — Complexity / trade-off.\nUse implements to enforce ports/contracts at compile time.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Implements**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Implements**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-119",
    number: "TS-M7-119",
    title: "Extends",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "extends creates class inheritance; a subclass can override compatible methods while preserving the base contract.",
    deepExplanation: "Step 1 — Understand the topic.\nextends creates class inheritance; a subclass can override compatible methods while preserving the base contract.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nclass Animal {\n  speak(): string {\n    return \"sound\";\n  }\n}\n\nclass Dog extends Animal {\n  override speak(): string {\n    return \"woof\";\n  }\n}\n\nconsole.log(\n  new Dog().speak(),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nclass Dog extends Animal {\n  override speak() {\n    return \"woof\";\n  }\n}\n```\n\nStep 5 — Example result:\n```text\nwoof\n```\n\nStep 6 — Complexity / trade-off.\nPrefer shallow inheritance and explicit override when subclassing.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Extends**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Extends**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-120",
    number: "TS-M7-120",
    title: "Method overriding",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "extends creates class inheritance; a subclass can override compatible methods while preserving the base contract.",
    deepExplanation: "Step 1 — Understand the topic.\nextends creates class inheritance; a subclass can override compatible methods while preserving the base contract.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nclass Animal {\n  speak(): string {\n    return \"sound\";\n  }\n}\n\nclass Dog extends Animal {\n  override speak(): string {\n    return \"woof\";\n  }\n}\n\nconsole.log(\n  new Dog().speak(),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nclass Dog extends Animal {\n  override speak() {\n    return \"woof\";\n  }\n}\n```\n\nStep 5 — Example result:\n```text\nwoof\n```\n\nStep 6 — Complexity / trade-off.\nPrefer shallow inheritance and explicit override when subclassing.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Method overriding**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Method overriding**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-121",
    number: "TS-M7-121",
    title: "Static members",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "Static members belong to the class itself rather than an instance.",
    deepExplanation: "Step 1 — Understand the topic.\nStatic members belong to the class itself rather than an instance.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nclass MathBox {\n  static double(\n    value: number,\n  ): number {\n    return value * 2;\n  }\n}\n\nconsole.log(\n  MathBox.double(21),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst double = (\n  value: number,\n) => value * 2;\n\nconsole.log(double(21));\n```\n\nStep 5 — Example result:\n```text\n42\n```\n\nStep 6 — Complexity / trade-off.\nUse static members for class-level behavior, not as a substitute for modules everywhere.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Static members**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Static members**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-122",
    number: "TS-M7-122",
    title: "Getters and setters",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "Getters expose computed/read access and setters centralize controlled assignment logic.",
    deepExplanation: "Step 1 — Understand the topic.\nGetters expose computed/read access and setters centralize controlled assignment logic.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nclass User {\n  constructor(\n    private _name: string,\n  ) {}\n\n  get name(): string {\n    return this._name;\n  }\n\n  set name(value: string) {\n    this._name = value.trim();\n  }\n}\n\nconst user =\n  new User(\"Ada\");\n\nuser.name = \" Grace \";\n\nconsole.log(user.name);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst user = {\n  name: \"Ada\",\n};\n\nObject.defineProperty(\n  user,\n  \"displayName\",\n  {\n    get() {\n      return user.name.trim();\n    },\n  },\n);\n```\n\nStep 5 — Example result:\n```text\nGrace\n```\n\nStep 6 — Complexity / trade-off.\nAvoid getters/setters when a plain method/property makes the API clearer.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Getters and setters**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Getters and setters**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-123",
    number: "TS-M7-123",
    title: "Generic classes",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "Generics preserve relationships between input and output types without sacrificing reuse. Prefer constraints that express the minimum required capability.",
    deepExplanation: "Step 1 — Understand the topic.\nGeneric classes carry a type parameter across instance state and methods.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nclass Box<T> {\n  constructor(\n    public value: T,\n  ) {}\n\n  get(): T {\n    return this.value;\n  }\n}\n\nconsole.log(\n  new Box<number>(42).get(),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst box =\n  new Box(\"Ada\");\n\nconsole.log(box.get());\n```\n\nStep 5 — Example result:\n```text\n42\n```\n\nStep 6 — Complexity / trade-off.\nUse generics to preserve type relationships, not merely to add abstraction.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Generic classes**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Generic classes**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-124",
    number: "TS-M7-124",
    title: "OOP design",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "OOP design is a TypeScript concept that should be understood in terms of compile-time behavior, runtime JavaScript, practical typing, and the trade-offs of using it in a production codebase.",
    deepExplanation: "Step 1 — Understand the topic.\nOOP design is a TypeScript concept that should be understood in terms of compile-time behavior, runtime JavaScript, practical typing, and the trade-offs of using it in a production codebase.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: number;\n};\n\nfunction update(value: Value): Value {\n  return {\n    value: value.value + 1,\n  };\n}\n\nconsole.log(update({ value: 0 }));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = {\n  value: 0,\n};\n\nconst next = {\n  ...value,\n  value: value.value + 1,\n};\n\nconsole.log(next);\n```\n\nStep 5 — Example result:\n```text\n{ value: 1 }\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest type design that preserves useful safety without adding unnecessary type-level complexity.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **OOP design**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **OOP design**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m7-125",
    number: "TS-M7-125",
    title: "Class best practices",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Classes & OOP",
    expectedAnswer: "Class best practices is a TypeScript concept that should be understood in terms of compile-time behavior, runtime JavaScript, practical typing, and the trade-offs of using it in a production codebase.",
    deepExplanation: "Step 1 — Understand the topic.\nClass best practices is a TypeScript concept that should be understood in terms of compile-time behavior, runtime JavaScript, practical typing, and the trade-offs of using it in a production codebase.\n\nStep 2 — Easy method.\nRead the type from left to right, identify what is checked at compile time, then trace one small runtime example.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: number;\n};\n\nfunction update(value: Value): Value {\n  return {\n    value: value.value + 1,\n  };\n}\n\nconsole.log(update({ value: 0 }));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = {\n  value: 0,\n};\n\nconst next = {\n  ...value,\n  value: value.value + 1,\n};\n\nconsole.log(next);\n```\n\nStep 5 — Example result:\n```text\n{ value: 1 }\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest type design that preserves useful safety without adding unnecessary type-level complexity.\n\nStep 7 — Edge cases.\nCheck null/undefined, unions, widening/narrowing, inference loss, runtime boundaries, incorrect assertions, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, when the idiomatic/built-in approach is preferable, and the maintainability trade-off.",
    productionExample: "Production example for **Class best practices**: keep the type contract close to the owning API/module, then validate the corresponding runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Class best practices**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos", "# Module 8 – Enums & Namespaces", "**Questions 126–135**"],
  },
];

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};

export const MOCK_TYPESCRIPT_MODULE7_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'TypeScript',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
