// Derived from frontend/src/document/Part_9_Advanced_React_Master_Handbook.md (Module 15 — React Patterns).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "reactadv-m15-251",
    number: "REACTADV-M15-251",
    title: "Composition over inheritance",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "Composition is preferred in React because behavior and structure can be combined through children, slots, render functions, hooks and context without creating rigid class hierarchies. Components should receive the capabilities or content they need instead of inheriting implementation details from a base component.",
    deepExplanation: "```tsx\ntype PanelProps = {\n  title: string;\n  actions?: React.ReactNode;\n  children: React.ReactNode;\n};\n\nfunction Panel({ title, actions, children }: PanelProps) {\n  return (\n    <section>\n      <header>\n        <h2>{title}</h2>\n        {actions}\n      </header>\n      {children}\n    </section>\n  );\n}\n\nfunction BillingPanel() {\n  return (\n    <Panel\n      title=\"Billing\"\n      actions={<button>Download invoice</button>}\n    >\n      <InvoiceList />\n    </Panel>\n  );\n}\n```\n\nThe reusable component owns layout and accessibility; the caller composes the content and behavior. This avoids a deep inheritance tree with dozens of boolean flags.",
    productionExample: "A design system uses composable layout, dialog and table primitives. Product screens supply content, actions and domain-specific behavior through slots/children rather than extending base UI classes.",
    bestPractices: ["Prefer composition and explicit dependencies.", "Use slots/children for structural variation.", "Use hooks for shared behavior rather than base classes.", "Keep reusable components narrowly scoped.", "Expose extension points intentionally instead of adding flag combinations indefinitely."],
    tradeOffs: "Composition improves flexibility and local reasoning, but too many composition layers can make data flow harder to follow. Inheritance can be appropriate for non-React domain models, but it is rarely the best React component reuse mechanism.",
    commonMistakes: ["Creating a `BaseForm` with dozens of conditional props.", "Assuming inheritance is required for code reuse.", "Hiding behavior inside deeply nested wrapper components.", "Using composition without documenting ownership of callbacks/state."],
    followUpQuestions: ["When is inheritance actually useful?", "Composition versus render props?", "How would you design an extensible Dialog component?", "How do you prevent composition APIs from becoming too generic?"],
    relatedTopics: ["Composition", "Children", "Slots", "Hooks", "Headless components", "Design systems"],
  },
  {
    id: "reactadv-m15-252",
    number: "REACTADV-M15-252",
    title: "Compound components",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "Compound components expose a coordinated family of components that share implicit state and semantics through context or a shared parent. This pattern gives consumers flexible markup while the parent controls the interaction model.",
    deepExplanation: "```tsx\nconst TabsContext = createContext<{\n  value: string;\n  setValue: (value: string) => void;\n} | null>(null);\n\nfunction Tabs({ children }: { children: React.ReactNode }) {\n  const [value, setValue] = useState(\"overview\");\n\n  return (\n    <TabsContext.Provider value={{ value, setValue }}>\n      <div role=\"tablist\">{children}</div>\n    </TabsContext.Provider>\n  );\n}\n\nfunction Tab({\n  value,\n  children,\n}: {\n  value: string;\n  children: React.ReactNode;\n}) {\n  const context = useContext(TabsContext);\n  if (!context) throw new Error(\"Tab must be inside Tabs\");\n\n  return (\n    <button\n      role=\"tab\"\n      aria-selected={context.value === value}\n      onClick={() => context.setValue(value)}\n    >\n      {children}\n    </button>\n  );\n}\n```\n\n`Tabs`, `Tab` and `TabPanel` form one controlled API while the consumer decides the composition order.",
    productionExample: "A design system provides `Dialog`, `Dialog.Trigger`, `Dialog.Content` and `Dialog.Close` so product teams can compose accessible dialogs without passing open state through every layer.",
    bestPractices: ["Define a clear parent/child contract.", "Fail fast when a compound child is outside its parent.", "Keep context values narrow.", "Preserve accessibility relationships.", "Document which children are required/optional."],
    tradeOffs: "Benefits: expressive APIs and flexible composition. Trade-offs: implicit context dependencies can be harder to debug; invalid nesting can become a runtime error; complex compound APIs may be over-engineered.",
    commonMistakes: ["Making compound children depend on undocumented context.", "Using one giant context value for all child behavior.", "Ignoring keyboard/accessibility semantics.", "Creating too many compound subcomponents for simple markup."],
    followUpQuestions: ["Compound components versus slots?", "How do you type compound component APIs?", "How would you preserve accessibility in Tabs?", "How do you prevent invalid nesting?"],
    relatedTopics: ["Compound components", "Context", "Tabs", "Dialog", "Headless UI", "Accessibility"],
  },
  {
    id: "reactadv-m15-253",
    number: "REACTADV-M15-253",
    title: "Render props",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "The render-props pattern passes a function as a prop so a reusable component can own behavior while the caller controls what UI is rendered. It was a major React reuse pattern before hooks; today hooks often provide a simpler alternative, but render props remain useful for explicit data/control boundaries.",
    deepExplanation: "```tsx\ntype MouseProps = {\n  children: (position: { x: number; y: number }) => React.ReactNode;\n};\n\nfunction MouseTracker({ children }: MouseProps) {\n  const [position, setPosition] = useState({ x: 0, y: 0 });\n\n  return (\n    <div\n      onMouseMove={event =>\n        setPosition({ x: event.clientX, y: event.clientY })\n      }\n    >\n      {children(position)}\n    </div>\n  );\n}\n\nfunction App() {\n  return (\n    <MouseTracker>\n      {position => <p>{position.x}, {position.y}</p>}\n    </MouseTracker>\n  );\n}\n```\n\nThe reusable component owns event handling; the caller owns the rendered presentation.",
    productionExample: "A charting primitive exposes render-prop state such as tooltip position and dimensions while allowing different product screens to render their own tooltip UI.",
    bestPractices: ["Keep the render function contract small.", "Avoid recreating expensive render functions when unnecessary.", "Prefer hooks when they make composition simpler.", "Document which values are guaranteed.", "Keep side effects outside the render callback."],
    tradeOffs: "Render props provide explicit control and work well for reusable behavior, but nested functions can make JSX deeply nested. Hooks usually provide flatter composition for new code.",
    commonMistakes: ["Using render props for every reusable component.", "Putting side effects in the render callback.", "Passing a huge object that couples caller and provider.", "Ignoring hook alternatives."],
    followUpQuestions: ["Render props versus custom hooks?", "When are render props still appropriate?", "How do render props affect memoization?"],
    relatedTopics: ["Render props", "Custom hooks", "Composition", "Function-as-child", "Memoization"],
  },
  {
    id: "reactadv-m15-254",
    number: "REACTADV-M15-254",
    title: "Higher-order components",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "A higher-order component (HOC) is a function that takes a component and returns an enhanced component. HOCs are still useful for cross-cutting concerns in legacy or library code, but hooks, composition and context often provide clearer alternatives in modern React.",
    deepExplanation: "```tsx\nfunction withLoading<P extends object>(\n  Component: React.ComponentType<P>,\n) {\n  return function WithLoading(\n    props: P & { loading: boolean },\n  ) {\n    if (props.loading) return <p>Loading…</p>;\n\n    const { loading: _loading, ...rest } = props;\n    return <Component {...(rest as P)} />;\n  };\n}\n\nconst UserCardWithLoading = withLoading(UserCard);\n```\n\nA HOC creates a new component boundary and can alter prop contracts, display names and refs. Those effects should be deliberate.",
    productionExample: "A legacy class-component application uses HOCs for analytics/error instrumentation while new feature code prefers hooks and composition. The platform team avoids introducing new HOC layers unless they provide a clear library-level benefit.",
    bestPractices: ["Use HOCs for genuine cross-cutting enhancement.", "Preserve display names and refs when required.", "Avoid mutating the wrapped component.", "Keep injected props clearly typed.", "Prefer hooks for new application-level shared behavior."],
    tradeOffs: "HOCs enable reusable enhancement but add wrapper layers and can make debugging/typing harder. Hooks usually reduce wrapper nesting but cannot directly replace every library-level component transformation.",
    commonMistakes: ["Mutating the input component.", "Overwriting user props with injected props unexpectedly.", "Creating HOCs inside render.", "Stacking many HOCs with unclear ownership."],
    followUpQuestions: ["HOC versus custom hook?", "How do you forward refs through a HOC?", "How do you preserve static properties?"],
    relatedTopics: ["HOC", "Hooks", "forwardRef", "Composition", "Legacy React"],
  },
  {
    id: "reactadv-m15-255",
    number: "REACTADV-M15-255",
    title: "Headless components",
    difficulty: "Easy",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "A headless component encapsulates interaction, state and accessibility behavior without enforcing visual markup or styling. It gives product teams design freedom while a shared primitive owns the hard behavioral logic.",
    deepExplanation: "```tsx\ntype MenuProps = {\n  children: (api: {\n    open: boolean;\n    toggle: () => void;\n  }) => React.ReactNode;\n};\n\nfunction HeadlessMenu({ children }: MenuProps) {\n  const [open, setOpen] = useState(false);\n\n  return children({\n    open,\n    toggle: () => setOpen(value => !value),\n  });\n}\n```\n\nA production headless primitive would also own keyboard navigation, focus management, ARIA relationships and dismissal behavior.",
    productionExample: "A design system provides a headless Combobox behavior layer while different products render it as a desktop dropdown, mobile sheet or custom visual picker.",
    bestPractices: ["Treat accessibility behavior as part of the primitive.", "Keep styling out of the headless layer.", "Expose a small stable behavioral API.", "Support controlled and uncontrolled usage only when justified.", "Test keyboard, pointer and screen-reader behavior."],
    tradeOffs: "Benefits: highly reusable behavior across brands/design systems. Trade-offs: consumers must implement visual and sometimes structural details correctly; the API can become complex when exposing every possible behavior.",
    commonMistakes: ["Calling a component headless while still hard-coding visual markup.", "Exposing low-level internal state instead of meaningful behavior.", "Leaving focus/keyboard behavior to every consumer."],
    followUpQuestions: ["Headless component versus compound component?", "How would you build a headless Combobox?", "Which accessibility behavior belongs in the primitive?"],
    relatedTopics: ["Headless UI", "Compound components", "Accessibility", "Render props", "Design systems"],
  },
  {
    id: "reactadv-m15-256",
    number: "REACTADV-M15-256",
    title: "Provider pattern",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "The provider pattern supplies a dependency or state value to a subtree, commonly through React Context. Providers are useful for cross-cutting concerns such as theme, authentication metadata, localization and feature configuration, but high-frequency state should not automatically be placed in a root provider.",
    deepExplanation: "```tsx\nconst ThemeContext = createContext<\"light\" | \"dark\">(\"light\");\n\nfunction ThemeProvider({ children }: { children: React.ReactNode }) {\n  const [theme, setTheme] = useState<\"light\" | \"dark\">(\"light\");\n\n  return (\n    <ThemeContext.Provider value={theme}>\n      <button onClick={() => setTheme(value =>\n        value === \"light\" ? \"dark\" : \"light\"\n      )}>\n        Toggle\n      </button>\n      {children}\n    </ThemeContext.Provider>\n  );\n}\n```\n\nThe provider establishes a dependency scope. Consumers read the nearest value.",
    productionExample: "An enterprise app composes `ConfigProvider`, `AuthProvider`, `ThemeProvider` and `LocaleProvider`, with domain-specific providers mounted closer to features that need them.",
    bestPractices: ["Keep provider values small and intentional.", "Split contexts by update frequency/domain.", "Fail fast when a required provider is missing.", "Document provider ordering dependencies.", "Keep high-frequency local state out of broad providers."],
    tradeOffs: "Providers simplify dependency propagation but can produce wide update scopes and deeply nested trees. External stores are often better for fine-grained high-frequency shared state.",
    commonMistakes: ["Creating one giant AppContext.", "Assuming context is a security boundary.", "Ignoring provider value identity.", "Using providers for simple parent-child props."],
    followUpQuestions: ["When should you split a provider?", "Provider versus dependency injection container?", "How do you optimize context updates?"],
    relatedTopics: ["Context", "Provider", "Dependency injection", "State architecture", "External stores"],
  },
  {
    id: "reactadv-m15-257",
    number: "REACTADV-M15-257",
    title: "State reducer pattern",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "The state reducer pattern lets a reusable component expose state transitions through a reducer that consumers can override or extend. It is useful when a component has a meaningful internal state machine but advanced consumers need to customize specific transitions without rewriting the component.",
    deepExplanation: "```tsx\ntype State = {\n  open: boolean;\n};\n\ntype Action =\n  | { type: \"OPEN\" }\n  | { type: \"CLOSE\" };\n\ntype Reducer = (state: State, action: Action) => State;\n\nfunction useMenuState(\n  reducer: Reducer = (state, action) => {\n    if (action.type === \"OPEN\") return { open: true };\n    if (action.type === \"CLOSE\") return { open: false };\n    return state;\n  },\n) {\n  return useReducer(reducer, { open: false });\n}\n```\n\nA library can expose controlled reducer hooks so consumers can add domain-specific behavior while retaining the component's default transitions.",
    productionExample: "A dropdown library lets a product override the default `OPEN` transition to log an analytics event or block opening when permissions are missing, without forking the component.",
    bestPractices: ["Keep default transitions correct and complete.", "Document supported action types.", "Avoid exposing internal implementation actions unnecessarily.", "Keep reducer functions pure.", "Use the pattern only when consumer-level state customization is valuable."],
    tradeOffs: "This pattern provides powerful extensibility but can expose too much internal behavior and make the public API harder to maintain. Controlled props or explicit callbacks may be simpler for smaller components.",
    commonMistakes: ["Using the reducer as a generic extension point for every concern.", "Mutating state in reducers.", "Changing action contracts frequently.", "Forcing every consumer to understand internal actions."],
    followUpQuestions: ["Reducer pattern versus controlled props?", "How would you preserve backwards compatibility?", "When is a state machine better?"],
    relatedTopics: ["Reducers", "State machines", "Controlled components", "Extensible APIs"],
  },
  {
    id: "reactadv-m15-258",
    number: "REACTADV-M15-258",
    title: "Props getter pattern",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "A props getter is a function that returns a set of props to spread onto a consumer's element, centralizing behavior and accessibility wiring while allowing the consumer to choose markup. It is useful for reusable interaction primitives, though modern headless hooks can often provide a cleaner typed API.",
    deepExplanation: "tsx\nfunction useToggle() {\n  const [open, setOpen] = useState(false);\n\n  function getButtonProps() {\n    return {\n      type: \"button\" as const,\n      \"aria-expanded\": open,\n      onClick: () => setOpen(value => !value),\n    };\n  }\n\n  return {\n    open,\n    getButtonProps,\n  };\n}\n\nfunction Menu() {\n  const { open, getButtonProps } = useToggle();\n\n  return (\n    <>\n      <button {...getButtonProps()}>Menu</button>\n      {open && <div>Content</div>}\n    </>\n  );\n}\n```",
    productionExample: "A headless disclosure primitive provides `getTriggerProps()` so every consumer gets consistent keyboard/ARIA behavior while the product controls the visual button markup.",
    bestPractices: ["Document which props are safe to override.", "Merge event handlers predictably.", "Preserve required accessibility props.", "Keep getter output stable when identity matters.", "Prefer explicit return objects for strong typing."],
    tradeOffs: "Props getters are flexible and easy to integrate with arbitrary markup, but spread-based APIs can make prop precedence and event-handler composition difficult to reason about.",
    commonMistakes: ["Letting consumer props accidentally overwrite required ARIA behavior.", "Replacing rather than composing event handlers.", "Returning a giant object of unrelated props."],
    followUpQuestions: ["Props getter versus render props?", "How do you merge event handlers safely?", "How do you type polymorphic props getters?"],
    relatedTopics: ["Props getters", "Headless components", "ARIA", "Custom hooks", "Polymorphism"],
  },
  {
    id: "reactadv-m15-259",
    number: "REACTADV-M15-259",
    title: "Controlled and uncontrolled APIs",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "A reusable component can support controlled, uncontrolled or hybrid usage. Controlled mode gives the parent ownership of state; uncontrolled mode lets the component manage internal state with an initial value. The API must avoid switching modes during a component's lifetime and should define callback semantics clearly.",
    deepExplanation: "tsx\ntype ToggleProps = {\n  open?: boolean;\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n};\n\nfunction Toggle({\n  open: controlledOpen,\n  defaultOpen = false,\n  onOpenChange,\n}: ToggleProps) {\n  const [internalOpen, setInternalOpen] = useState(defaultOpen);\n  const isControlled = controlledOpen !== undefined;\n  const open = isControlled ? controlledOpen : internalOpen;\n\n  function setOpen(next: boolean) {\n    if (!isControlled) setInternalOpen(next);\n    onOpenChange?.(next);\n  }\n\n  return (\n    <button\n      aria-expanded={open}\n      onClick={() => setOpen(!open)}\n    >\n      Toggle\n    </button>\n  );\n}\n```",
    productionExample: "A design-system Dialog supports `defaultOpen` for simple usage and `open/onOpenChange` for applications that coordinate dialogs with routing or global workflow state.",
    bestPractices: ["Detect controlled mode from prop presence, not value truthiness.", "Never switch between controlled and uncontrolled modes.", "Fire change callbacks consistently.", "Document who owns state in each mode.", "Use stable default values."],
    tradeOffs: "Hybrid APIs maximize reuse but increase implementation and documentation complexity. A single controlled-only API is simpler for highly orchestrated components.",
    commonMistakes: ["Using `open || internalOpen` and creating ambiguous ownership.", "Switching modes dynamically.", "Not calling onChange in controlled mode.", "Using `defaultOpen` as if it updates after mount."],
    followUpQuestions: ["How do you detect mode switching?", "What does defaultValue mean?", "When should a component support both modes?"],
    relatedTopics: ["Controlled components", "Uncontrolled components", "Design systems", "State ownership"],
  },
  {
    id: "reactadv-m15-260",
    number: "REACTADV-M15-260",
    title: "Polymorphic components",
    difficulty: "Easy",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "A polymorphic component lets consumers choose the rendered element or component through an API such as `as` or `asChild`, while preserving shared styling/behavior. The TypeScript contract must preserve valid props for the chosen element and prevent invalid combinations.",
    deepExplanation: "tsx\ntype ButtonProps = {\n  as?: \"button\" | \"a\";\n  children: React.ReactNode;\n} & React.ButtonHTMLAttributes<HTMLButtonElement>\n  & React.AnchorHTMLAttributes<HTMLAnchorElement>;\n\nfunction Button({\n  as = \"button\",\n  children,\n  ...props\n}: ButtonProps) {\n  const Component = as;\n  return <Component {...props}>{children}</Component>;\n}\n\nfunction Example() {\n  return (\n    <Button as=\"a\" href=\"/billing\">\n      Billing\n    </Button>\n  );\n}\n```\n\nA production-quality implementation should use discriminated/polymorphic TypeScript types so anchor-only props are not accidentally accepted on a button.",
    productionExample: "A design system's Button can render as a native button for actions or an anchor for navigation while preserving consistent visual tokens and accessibility semantics.",
    bestPractices: ["Preserve native semantics of the rendered element.", "Type element-specific props carefully.", "Do not render a link as a button just to reuse styles.", "Test keyboard behavior and accessibility.", "Keep the polymorphic API small."],
    tradeOffs: "Polymorphism reduces duplicated visual primitives but makes TypeScript and accessibility contracts more complex. Many applications should prefer explicit `Button` and `Link` variants when semantics differ.",
    commonMistakes: ["Allowing arbitrary strings without type safety.", "Rendering `<a>` for an action with no href.", "Passing button-only props to anchors.", "Overengineering a polymorphic API for a small component."],
    followUpQuestions: ["How would you type a polymorphic component correctly?", "`as` versus `asChild`?", "When should you use separate components instead?"],
    relatedTopics: ["Polymorphism", "TypeScript generics", "Accessibility", "Design systems", "Slot pattern"],
  },
  {
    id: "reactadv-m15-261",
    number: "REACTADV-M15-261",
    title: "Slot patterns",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "A slot pattern lets a parent component define named or positional insertion points for consumer content. React's children and explicit slot props are the basic mechanism; some UI libraries implement a slot utility that merges props into a child. The key is separating layout ownership from content ownership.",
    deepExplanation: "tsx\ntype CardProps = {\n  header?: React.ReactNode;\n  footer?: React.ReactNode;\n  children: React.ReactNode;\n};\n\nfunction Card({ header, footer, children }: CardProps) {\n  return (\n    <article>\n      {header && <header>{header}</header>}\n      <section>{children}</section>\n      {footer && <footer>{footer}</footer>}\n    </article>\n  );\n}\n\n<Card\n  header={<h2>Invoice</h2>}\n  footer={<button>Download</button>}\n>\n  <InvoiceDetails />\n</Card>\n```",
    productionExample: "A design system's Panel supports `header`, `actions`, `footer` and main content slots so teams can compose different screens without creating dozens of panel variants.",
    bestPractices: ["Use explicit slot names for stable semantic regions.", "Keep slots semantically clear.", "Avoid one slot prop per tiny visual detail.", "Document which slots accept interactive content.", "Preserve heading, landmark and focus semantics."],
    tradeOffs: "Slots provide flexible composition with a simple mental model, but a growing number of slot props can turn into an implicit template language. Compound components can be better for interactive shared state.",
    commonMistakes: ["Using slots to pass unrelated state and callbacks everywhere.", "Creating dozens of optional slots.", "Breaking semantic structure when custom slot content is supplied."],
    followUpQuestions: ["Slots versus compound components?", "Children versus named slots?", "How do you type slot APIs?"],
    relatedTopics: ["Slots", "Children", "Composition", "Compound components", "Design systems"],
  },
  {
    id: "reactadv-m15-262",
    number: "REACTADV-M15-262",
    title: "Container and presentational pattern",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "Container/presentational separation places data fetching, state orchestration and event wiring in one layer and rendering concerns in another. It can improve testability and reuse, but modern hooks and colocated components often make a strict two-component split unnecessary.",
    deepExplanation: "tsx\nfunction UserListContainer() {\n  const users = useUsers();\n\n  return (\n    <UserList\n      users={users.data ?? []}\n      loading={users.isLoading}\n    />\n  );\n}\n\nfunction UserList({\n  users,\n  loading,\n}: {\n  users: User[];\n  loading: boolean;\n}) {\n  if (loading) return <p>Loading…</p>;\n\n  return (\n    <ul>\n      {users.map(user => <li key={user.id}>{user.name}</li>)}\n    </ul>\n  );\n}\n```",
    productionExample: "A data-heavy dashboard keeps route/data orchestration separate from a reusable presentational table. The table can be tested with static props while the container owns query/loading/error behavior.",
    bestPractices: ["Use the split when it improves reuse or testing.", "Avoid artificial container wrappers.", "Keep data fetching close to the route/domain when that is clearer.", "Pass simple typed view models to presentation components."],
    tradeOffs: "The pattern improves separation but can create unnecessary files and prop plumbing. Hooks often let a single feature component achieve similar separation with less indirection.",
    commonMistakes: ["Creating containers for every component.", "Moving simple local state into a container just for the pattern.", "Passing many low-level props through multiple wrappers."],
    followUpQuestions: ["Is this pattern still useful with hooks?", "How does it compare with custom hooks?", "When should presentation remain a pure component?"],
    relatedTopics: ["Custom hooks", "Separation of concerns", "Presentational components", "Data fetching"],
  },
  {
    id: "reactadv-m15-263",
    number: "REACTADV-M15-263",
    title: "Feature flag pattern",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "Feature flags separate deployment from release by deciding at runtime or build time which behavior is active. A robust React implementation treats flags as configuration, keeps sensitive authorization out of client-only checks, supports targeted rollout and includes cleanup/expiry so stale flags do not become permanent branches.",
    deepExplanation: "tsx\ntype Flags = {\n  newCheckout: boolean;\n};\n\nconst FeatureFlagContext = createContext<Flags | null>(null);\n\nfunction NewCheckoutGate() {\n  const flags = useContext(FeatureFlagContext);\n  if (!flags?.newCheckout) return <LegacyCheckout />;\n\n  return <NewCheckout />;\n}\n```",
    productionExample: "A payments team rolls a new checkout to 5% of users, monitors conversion and error rates, then increases rollout. The flag system records owner, creation date, expiry date and targeting rules so old flags can be removed.",
    bestPractices: ["Treat flags as configuration, not authorization.", "Record owner and expiry metadata.", "Support deterministic targeting when required.", "Monitor both flag variants.", "Test enabled and disabled paths.", "Remove temporary flags after rollout."],
    tradeOffs: "Flags enable gradual rollout and rollback without redeploying code, but increase behavioral combinations and technical debt. Permanent flags should have a clear reason.",
    commonMistakes: ["Using a client-side flag to enforce security.", "Leaving dead flags for years.", "Testing only the default variant.", "Creating deeply nested flag conditions."],
    followUpQuestions: ["Feature flag versus permission?", "How do you test flag combinations?", "How do you clean up stale flags?", "How would you roll out to 1% of users?"],
    relatedTopics: ["Feature flags", "Progressive delivery", "A/B testing", "Configuration", "Rollouts"],
  },
  {
    id: "reactadv-m15-264",
    number: "REACTADV-M15-264",
    title: "Adapter pattern",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "An adapter converts one interface into another interface expected by a consumer. In React applications it is commonly useful when integrating third-party UI controls, API clients or legacy modules into a standard internal contract.",
    deepExplanation: "tsx\ntype AppSelectProps = {\n  value: string;\n  onChange: (value: string) => void;\n};\n\nfunction ThirdPartySelectAdapter({\n  value,\n  onChange,\n}: AppSelectProps) {\n  return (\n    <ThirdPartySelect\n      selectedValue={value}\n      onValueChanged={(option: { id: string }) =>\n        onChange(option.id)\n      }\n    />\n  );\n}\n```\n\nThe rest of the application depends on `AppSelectProps`, not the third-party event shape.",
    productionExample: "A design system wraps two different date-picker vendors behind one `DateField` contract. Product code remains stable while the platform team can replace the underlying vendor.",
    bestPractices: ["Keep adapter contracts domain-oriented.", "Translate events and values explicitly.", "Hide vendor-specific types from most application code.", "Add integration tests around the adapter.", "Do not leak third-party behavior unnecessarily."],
    tradeOffs: "Adapters isolate vendor churn but add a layer that must evolve with both sides. Over-adapting stable libraries can create unnecessary maintenance.",
    commonMistakes: ["Creating a generic adapter for every component without benefit.", "Leaking vendor APIs through the adapter.", "Not testing edge cases such as null/undefined values."],
    followUpQuestions: ["Adapter versus facade?", "How would you migrate vendors safely?", "Where should vendor-specific logic live?"],
    relatedTopics: ["Adapter", "Facade", "Dependency inversion", "Design systems", "Third-party integration"],
  },
  {
    id: "reactadv-m15-265",
    number: "REACTADV-M15-265",
    title: "Facade pattern",
    difficulty: "Easy",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "A facade provides a simplified interface over a more complex subsystem. In React applications, a facade can hide authentication, query/cache, analytics or design-system complexity from feature components while exposing only the operations the feature needs.",
    deepExplanation: "tsx\ntype UserSessionFacade = {\n  user: User | null;\n  signOut: () => Promise<void>;\n  can: (permission: string) => boolean;\n};\n\nfunction createSessionFacade(): UserSessionFacade {\n  return {\n    user: sessionStore.user,\n    signOut: () => sessionService.signOut(),\n    can: permission => permissionService.has(permission),\n  };\n}\n```",
    productionExample: "Feature teams use `useSession()` rather than calling identity, permission and token services separately. The facade owns the integration while keeping components focused on product behavior.",
    bestPractices: ["Keep the facade small.", "Expose business-relevant operations.", "Hide unstable implementation details.", "Avoid turning the facade into a god service.", "Test facade behavior at the boundary."],
    tradeOffs: "Facades reduce coupling and simplify consumers, but can become over-broad and hide important behavior if they grow without discipline.",
    commonMistakes: ["Creating a facade that exposes the entire application service layer.", "Hiding errors instead of normalizing them.", "Making the facade responsible for unrelated domains."],
    followUpQuestions: ["Facade versus adapter?", "When does a facade become a god object?", "How would you version a shared facade?"],
    relatedTopics: ["Facade", "Adapter", "Service layer", "Dependency inversion"],
  },
  {
    id: "reactadv-m15-266",
    number: "REACTADV-M15-266",
    title: "Dependency injection in React",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "Dependency injection supplies services or dependencies from outside the component that consumes them. React can use props, Context or provider patterns to inject dependencies such as API clients, clocks, feature services or repositories, improving testability and reducing hard-coded global imports.",
    deepExplanation: "tsx\ntype Dependencies = {\n  userRepository: UserRepository;\n};\n\nconst DependenciesContext =\n  createContext<Dependencies | null>(null);\n\nfunction useDependencies() {\n  const value = useContext(DependenciesContext);\n  if (!value) throw new Error(\"Missing DependenciesProvider\");\n  return value;\n}\n\nfunction UserProfile({ id }: { id: string }) {\n  const { userRepository } = useDependencies();\n  // Feature code depends on the interface, not a concrete HTTP client.\n  return <UserView userId={id} repository={userRepository} />;\n}\n```",
    productionExample: "Tests provide an in-memory repository while production provides the HTTP-backed repository. This makes feature tests deterministic and isolates network behavior from domain rendering.",
    bestPractices: ["Inject stable interfaces rather than concrete infrastructure.", "Keep dependency contracts small.", "Use Context when the dependency scope matches the subtree.", "Avoid creating a service locator that hides every dependency.", "Make test implementations behaviorally equivalent enough for the use case."],
    tradeOffs: "DI improves substitution and testing but can add provider configuration and indirection. Direct imports are simpler for stable pure utilities.",
    commonMistakes: ["Injecting every helper function.", "Hiding all dependencies behind one giant context.", "Using DI to avoid clear module boundaries.", "Providing different behavior in tests that masks production bugs."],
    followUpQuestions: ["Props versus Context for injection?", "How would you inject a clock or API client?", "What is service locator anti-pattern?"],
    relatedTopics: ["Dependency injection", "Context", "Providers", "Repository pattern", "Testing"],
  },
  {
    id: "reactadv-m15-267",
    number: "REACTADV-M15-267",
    title: "Reusable component APIs",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "A reusable React API should define clear responsibilities, ownership, accessible semantics, controlled/uncontrolled behavior, extension points and compatibility guarantees. The best API exposes stable concepts rather than leaking implementation details.",
    deepExplanation: "tsx\ntype DialogProps = {\n  title: string;\n  children: React.ReactNode;\n  open?: boolean;\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n};\n\nfunction Dialog(props: DialogProps) {\n  // Implementation omitted; API defines ownership and extension points.\n  return <section>{props.children}</section>;\n}\n```",
    productionExample: "A design system version-controls shared component APIs and provides migration notes when props change. Product teams depend on stable semantics such as `open/onOpenChange`, while DOM internals can evolve without breaking consumers.",
    bestPractices: ["Optimize for semantic clarity.", "Keep APIs small.", "Document controlled/uncontrolled ownership.", "Preserve accessibility contracts.", "Use TypeScript to prevent invalid prop combinations.", "Treat breaking changes as migration work."],
    tradeOffs: "A powerful reusable API reduces duplication but becomes expensive to change. Overly generic components often become harder to use than a small domain-specific component.",
    commonMistakes: ["Adding props for every product edge case.", "Exposing internal state names.", "Changing controlled behavior without a migration path.", "Using `any` to hide API typing problems."],
    followUpQuestions: ["How do you version a component API?", "When should a component stop being generic?", "How do you deprecate props safely?"],
    relatedTopics: ["Component API design", "TypeScript", "SemVer", "Design systems", "Accessibility"],
  },
  {
    id: "reactadv-m15-268",
    number: "REACTADV-M15-268",
    title: "Design system patterns",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "A React design system should standardize primitives, accessibility, interaction behavior and composition while allowing product-specific composition. Patterns such as compound components, headless behavior, slots, polymorphism and controlled/uncontrolled APIs should be used selectively rather than combined into every component.",
    deepExplanation: "tsx\nfunction Button({\n  variant = \"primary\",\n  children,\n  ...props\n}: React.ButtonHTMLAttributes<HTMLButtonElement> & {\n  variant?: \"primary\" | \"secondary\";\n}) {\n  return (\n    <button data-variant={variant} {...props}>\n      {children}\n    </button>\n  );\n}\n``` \n\nA mature design system keeps primitive behavior predictable and lets higher-level product components compose those primitives instead of creating one mega-component.",
    productionExample: "An enterprise design system provides Buttons, Inputs, Dialogs, Comboboxes and Form primitives with consistent accessibility and tokens. Product teams compose them into workflows without copying keyboard/focus logic.",
    bestPractices: ["Standardize accessibility and interaction first.", "Keep primitives composable.", "Provide strong TypeScript contracts.", "Document controlled/uncontrolled modes.", "Test primitives across browsers and assistive technologies.", "Keep domain-specific business logic outside the base design system."],
    tradeOffs: "A design system creates consistency and reduces repeated bugs but requires governance, versioning and migration investment. Over-abstraction slows product teams.",
    commonMistakes: ["Turning every component into a universal component.", "Mixing business rules into design primitives.", "Breaking accessibility while adding customization hooks.", "Ignoring backward compatibility."],
    followUpQuestions: ["Headless versus styled components?", "How would you govern a 100-component system?", "How do you version design-system packages?"],
    relatedTopics: ["Design systems", "Headless components", "Compound components", "Accessibility", "Component APIs"],
  },
  {
    id: "reactadv-m15-269",
    number: "REACTADV-M15-269",
    title: "Enterprise pattern trade-offs",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "At enterprise scale, React patterns should be chosen by concrete requirements such as state ownership, reuse, accessibility, performance, team boundaries and migration cost. No pattern is universally best; a senior engineer should explain why a simpler alternative is insufficient and how the chosen pattern will be removed or evolved later.",
    deepExplanation: "```ts\ntype Decision = {\n  problem: string;\n  pattern: string;\n  reason: string;\n  cost: string;\n};\n\nconst decision: Decision = {\n  problem: \"Shared accessible menu behavior\",\n  pattern: \"Headless compound component\",\n  reason: \"Flexible markup with centralized behavior\",\n  cost: \"Context and API complexity\",\n};\n``` \n\nA useful architecture decision records the problem, selected pattern, alternatives, measurable success criteria and exit/migration plan.",
    productionExample: "A platform team chooses a headless compound Dialog instead of a configurable mega-component because multiple product brands need different markup while sharing keyboard/focus behavior. The decision is documented and validated through accessibility and adoption metrics.",
    bestPractices: ["Choose patterns from requirements, not fashion.", "Record alternatives and trade-offs.", "Measure performance and adoption.", "Keep migration paths explicit.", "Prefer the simplest pattern that satisfies the problem.", "Review patterns periodically as React capabilities evolve."],
    tradeOffs: "Enterprise patterns reduce duplication and standardize quality, but every abstraction adds API, documentation and migration costs. Excessive pattern layering can reduce local clarity.",
    commonMistakes: ["Using a pattern because it is interview-popular.", "Combining HOC + render props + context + slots without a concrete need.", "Ignoring team learning cost.", "Never deleting obsolete abstractions."],
    followUpQuestions: ["How would you choose between five patterns for the same problem?", "What signals tell you to remove an abstraction?", "How do React 19 APIs change older patterns?"],
    relatedTopics: ["Architecture decision records", "Design systems", "Composition", "Abstraction cost", "React 19"],
  },
  {
    id: "reactadv-m15-270",
    number: "REACTADV-M15-270",
    title: "Pattern selection interview",
    difficulty: "Easy",
    experienceLevel: "8–12 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React Patterns",
    expectedAnswer: "For a pattern-selection interview, start with the concrete problem, constraints and desired ownership. Compare the simplest viable option with alternatives, state the trade-offs, explain failure modes and show how you would validate the choice in production. Pattern names are secondary to reasoning.",
    deepExplanation: "text\nScenario: Build an enterprise Dialog that must support:\n- controlled and uncontrolled use\n- custom visual markup\n- keyboard/focus accessibility\n- analytics\n- nested product content\n\nA strong decision might be:\n1. Headless compound component for structure/behavior.\n2. Context for local dialog coordination.\n3. Controlled/uncontrolled API for state ownership.\n4. Slots/children for custom visual content.\n5. Central accessibility behavior.\n6. Avoid an HOC because it adds a wrapper layer without solving the core problem.\n```",
    productionExample: "An interview answer should connect the pattern to measurable outcomes: API ergonomics, accessibility defects, rerender cost, bundle impact, adoption and migration cost. The design should include testing and a rollback or replacement strategy.",
    bestPractices: ["Start from requirements.", "Compare at least one simpler alternative.", "Explain ownership and boundaries.", "Discuss accessibility and performance.", "Include testing and migration strategy.", "Use measurable success criteria."],
    tradeOffs: "The strongest senior answer is not the most sophisticated pattern; it is the smallest abstraction that solves the problem while preserving future options.",
    commonMistakes: ["Listing patterns without explaining selection criteria.", "Choosing a pattern because it is modern.", "Ignoring accessibility and operational costs.", "Not discussing the alternative."],
    followUpQuestions: ["What simpler solution would you choose first?", "What would make you change the decision?", "How would you prove the pattern works at scale?", "How would you migrate away from it?"],
    relatedTopics: ["Pattern selection", "System design", "Trade-offs", "Architecture decisions", "Senior interviews"],
  },

];

export const MOCK_ADVANCED_REACT_MODULE15_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Advanced React',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));