// Derived from frontend/src/document/DevOps_Cloud_CICD_Frontend_Infrastructure_Master_Handbook.md (Module 3 — Docker).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "devops-m3-1",
    number: "DEVOPS-M3-1",
    title: "Containers",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "A Docker container is an isolated process created from an image, using OS-level isolation features such as namespaces and cgroups while sharing the host kernel. Containers package application code and runtime dependencies without the overhead of a full guest operating system.",
    deepExplanation: "```bash\ndocker run --rm -p 8080:80 nginx:alpine\ndocker ps\ndocker logs <container>\ndocker exec -it <container> sh\n```\n\nA container has its own filesystem view, process tree, network namespace and resource limits, but it does not contain a separate kernel. The image is the packaged filesystem and metadata; the running container is the process instantiated from that image.",
    productionExample: "A frontend service is built into an immutable image, run with a non-root user, constrained with CPU/memory limits and exposed only through the intended application port. The same image digest is promoted across environments.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Containers improve portability and deployment consistency, but still require host/kernel management, image maintenance, networking, monitoring and security controls.",
    commonMistakes: ["Thinking containers are full virtual machines.","Running everything as root.","Using mutable latest images in production.","Storing state inside ephemeral container layers."],
    followUpQuestions: ["Container vs VM?","How do namespaces and cgroups help?","How do you persist state?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-2",
    number: "DEVOPS-M3-2",
    title: "Images",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "A Docker image is an immutable, layered filesystem plus metadata used to create containers. Images are usually built from a Dockerfile and identified by tags and content digests.",
    deepExplanation: "```bash\ndocker build -t registry.example.com/frontend:1.4.0 .\ndocker images\ndocker inspect registry.example.com/frontend:1.4.0\ndocker push registry.example.com/frontend:1.4.0\n```\n\nA production deployment should record the image digest, not rely only on a mutable tag. Layers can be reused between builds and pulls, reducing transfer/build work when cache inputs stay unchanged.",
    productionExample: "CI builds `frontend:git-9f31c2`, scans it, pushes it to a private registry and records the immutable digest. Kubernetes/ECS then deploys that digest so a tag cannot silently point to different content.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Images make releases reproducible and portable, but large or stale images increase storage, transfer and attack surface.",
    commonMistakes: ["Using `latest` as a release identifier.","Embedding secrets in image layers.","Copying the entire repository into the build context.","Ignoring base-image CVEs."],
    followUpQuestions: ["Tag vs digest?","How are layers reused?","How do you inspect image contents?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-3",
    number: "DEVOPS-M3-3",
    title: "Layers",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "Docker images are built from filesystem layers. Each Dockerfile instruction can contribute a layer or build-cache boundary, while container writes happen in a writable layer above the image's read-only layers.",
    deepExplanation: "```dockerfile\nFROM node:22-alpine\nWORKDIR /app\n\nCOPY package*.json ./\nRUN npm ci\n\nCOPY . .\nRUN npm run build\n```\n\nChanging only application source should allow the dependency-install layer to remain cached. Ordering high-stability instructions before frequently changing instructions improves build performance.",
    productionExample: "A monorepo frontend image moves dependency installation before source-copy steps, so normal application edits reuse the expensive `npm ci` layer. CI also uses BuildKit cache exports to share layers between runners.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Layer reuse speeds builds and transfers, but poor layer ordering or large early layers can destroy cache efficiency.",
    commonMistakes: ["Copying all source before dependency manifests.","Putting secrets in an early layer.","Assuming deleting a file in a later layer removes it from image history."],
    followUpQuestions: ["What invalidates a Docker layer cache?","Why doesn't `rm secret` remove a secret from history?","How do multi-stage builds reduce layers?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-4",
    number: "DEVOPS-M3-4",
    title: "Registries",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "A container registry stores and distributes images and related metadata. It can be public or private and commonly integrates authentication, vulnerability scanning, retention policies, replication and artifact provenance.",
    deepExplanation: "```bash\ndocker login registry.example.com\ndocker tag app:local registry.example.com/team/app:1.4.0\ndocker push registry.example.com/team/app:1.4.0\ndocker pull registry.example.com/team/app:1.4.0\n```\n\nA registry should be treated as part of the software supply chain. CI credentials need least privilege, production should pull from trusted repositories, and release systems should record immutable image digests.",
    productionExample: "A private registry separates development, staging and production repositories or permissions. CI can push images; production deployment identities can pull but not overwrite previously released tags.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Registries centralize distribution and governance but add storage, network, availability and credential-management dependencies.",
    commonMistakes: ["Using one shared admin credential.","Allowing arbitrary overwrites of release tags.","Ignoring image retention costs.","Pulling untrusted images into production."],
    followUpQuestions: ["How do image digests work?","How would you secure registry access?","What is an image retention strategy?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-5",
    number: "DEVOPS-M3-5",
    title: "Dockerfile",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "A Dockerfile is a declarative build recipe describing the base image, filesystem, environment, command and other image configuration. Docker builds it into an image using Docker's build engine.",
    deepExplanation: "```dockerfile\nFROM node:22-alpine AS build\nWORKDIR /app\n\nCOPY package*.json ./\nRUN npm ci\n\nCOPY . .\nRUN npm run build\n\nFROM nginx:alpine\nCOPY --from=build /app/dist /usr/share/nginx/html\nEXPOSE 80\n\nUSER nginx\n```\n\nThe file separates build-time tooling from the small runtime image and makes the build reproducible when the inputs are pinned appropriately.",
    productionExample: "A React/Vite application uses a Node build stage and an Nginx runtime stage. The production image contains only static assets and the HTTP server, not npm, source code or build dependencies.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Dockerfiles make builds reproducible but can become complex and cache-sensitive. Over-optimization can hurt readability and maintainability.",
    commonMistakes: ["Running development servers in production images.","Installing compilers/runtime tools in the final image unnecessarily.","Using `latest` base images without a strategy.","Using secrets in `ARG`/`ENV`."],
    followUpQuestions: ["CMD vs ENTRYPOINT?","COPY vs ADD?","How do you optimize a Dockerfile?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-6",
    number: "DEVOPS-M3-6",
    title: "Volumes",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "Docker volumes provide persistent storage managed outside a container's writable layer. They allow data to survive container replacement and can be mounted into one or more containers depending on the use case.",
    deepExplanation: "```bash\ndocker volume create db-data\ndocker run -d   --name postgres   -v db-data:/var/lib/postgresql/data   postgres:16\ndocker volume ls\n```\n\nA volume is different from a bind mount: volumes are managed by Docker, while bind mounts directly expose a host path. For production databases, managed cloud storage or database services are often preferable to local Docker volumes.",
    productionExample: "A local development stack uses a named volume for PostgreSQL so restarting containers does not delete the database. In production, the database is a managed service with durable storage and backups.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Volumes preserve state but introduce lifecycle, backup and portability concerns. Bind mounts are convenient for development but couple containers to host paths.",
    commonMistakes: ["Assuming container deletion deletes all volume data.","Using host bind mounts for production state without a backup design.","Mounting sensitive host directories unnecessarily."],
    followUpQuestions: ["Volume vs bind mount?","How do you back up a Docker volume?","What happens when a volume is removed?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-7",
    number: "DEVOPS-M3-7",
    title: "Networking",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "Docker networking connects containers and controls how they reach each other and external networks. Common drivers include bridge networks for local isolation, host networking for special cases, and overlay networking in orchestrated environments.",
    deepExplanation: "```bash\ndocker network create app-net\n\ndocker run -d --name api --network app-net api:1.4\ndocker run -d --name web --network app-net web:1.4\n```\n\nWithin a user-defined bridge network, containers can normally reach one another using service/container names rather than hard-coded IP addresses. Publish ports only when traffic must enter from the host or outside network.",
    productionExample: "A Compose stack puts the frontend, API and database on a private application network. Only the reverse proxy publishes ports publicly; the database port is not exposed to the host.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Network isolation improves security and service discovery, but adds DNS, proxy, port and troubleshooting complexity.",
    commonMistakes: ["Publishing every container port.","Hard-coding container IP addresses.","Assuming network isolation replaces authentication/authorization."],
    followUpQuestions: ["EXPOSE vs publish `-p`?","Bridge vs host network?","How do containers discover each other?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-8",
    number: "DEVOPS-M3-8",
    title: "Compose",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "Docker Compose defines and runs multi-container application environments using a declarative YAML model. It is especially useful for local development, integration testing and small controlled environments.",
    deepExplanation: "```yaml\nservices:\n  web:\n    build: .\n    ports:\n      - \"3000:80\"\n    depends_on:\n      - api\n\n  api:\n    build: ./api\n    environment:\n      DATABASE_URL: postgresql://postgres:postgres@db:5432/app\n    depends_on:\n      - db\n\n  db:\n    image: postgres:16\n    volumes:\n      - db-data:/var/lib/postgresql/data\n\nvolumes:\n  db-data:\n```\n\nCompose creates a project network so services can communicate through service names such as `db` and `api`.",
    productionExample: "Developers use the same Compose file to reproduce a frontend/API/database environment locally and in integration tests, while production runs on the organization's orchestrator/platform rather than assuming Compose is the entire production control plane.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Compose is easy to understand and productive, but it does not replace a full production orchestrator for complex availability, scaling and rollout requirements.",
    commonMistakes: ["Assuming `depends_on` means the dependency is ready to serve traffic.","Committing production secrets into Compose files.","Treating local networking exactly like Kubernetes/cloud networking."],
    followUpQuestions: ["Compose vs Kubernetes?","How do you wait for service readiness?","How do Compose networks work?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-9",
    number: "DEVOPS-M3-9",
    title: "Build Cache",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "Docker build cache reuses previously computed build steps when the instruction and relevant inputs are unchanged. Good Dockerfile ordering and BuildKit cache mounts can drastically reduce repeated build time.",
    deepExplanation: "```dockerfile\n# syntax=docker/dockerfile:1\nFROM node:22-alpine\nWORKDIR /app\n\nCOPY package*.json ./\nRUN --mount=type=cache,target=/root/.npm npm ci\n\nCOPY . .\nRUN npm run build\n```\n\nThe dependency install can reuse both Docker layers and package-manager cache. CI can additionally import/export build cache so ephemeral runners do not start completely cold.",
    productionExample: "A CI pipeline exports BuildKit cache to a registry-backed cache. Most frontend builds reuse dependency and tooling layers, cutting build time without changing the final immutable image.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Caching improves build speed and reduces network work, but consumes storage and can introduce stale-cache troubleshooting. Builds must remain reproducible even when caches are empty.",
    commonMistakes: ["Assuming cache is mandatory for correctness.","Using overly broad cache keys.","Putting `COPY . .` before dependency installation.","Caching secrets or private credentials."],
    followUpQuestions: ["What invalidates a Docker layer?","Layer cache vs npm cache?","How do you use cache in CI?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-10",
    number: "DEVOPS-M3-10",
    title: "BuildKit",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "BuildKit is Docker's modern build engine providing features such as improved parallelism, efficient caching, secrets/SSH mounts and advanced build outputs. It is designed to make image builds faster, more reproducible and more capable.",
    deepExplanation: "```bash\nDOCKER_BUILDKIT=1 docker buildx build   --platform linux/amd64,linux/arm64   -t registry.example.com/web:1.4.0   --push .\n```\n\nBuildKit supports ephemeral mounts such as:\n\n```dockerfile\nRUN --mount=type=secret,id=npmrc     npm ci\n```\n\nThe secret is available to the build step without being committed into the resulting image layer.",
    productionExample: "A platform team uses `docker buildx build` to publish multi-architecture frontend/edge images and uses secret mounts for private package installation without embedding credentials into image layers.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "BuildKit unlocks better caching and multi-platform workflows but adds concepts such as builders, cache exporters and secret mounts that teams must understand.",
    commonMistakes: ["Passing secrets through Dockerfile `ARG`.","Assuming cache behavior is identical across all builders.","Publishing multi-platform images without testing each architecture."],
    followUpQuestions: ["BuildKit vs legacy builder?","How do secret mounts work?","What is buildx?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-11",
    number: "DEVOPS-M3-11",
    title: "Multi-stage Builds",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "Multi-stage Docker builds use multiple `FROM` stages so build tools and source are kept in intermediate stages while the final image copies only the runtime artifacts it needs. This reduces final image size and attack surface.",
    deepExplanation: "```dockerfile\nFROM node:22-alpine AS build\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\n\nFROM nginx:alpine AS runtime\nCOPY --from=build /app/dist /usr/share/nginx/html\nEXPOSE 80\n```\n\nThe final image does not contain Node.js, npm, source files or build caches unless explicitly copied.",
    productionExample: "A static React app uses a Node build stage and Nginx runtime stage. The release image is significantly smaller than the full Node image and contains only the files required to serve the application.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Multi-stage builds reduce image size and exposure, but they can make debugging more complex because build tools are absent from the final image.",
    commonMistakes: ["Copying the whole build context into the runtime stage.","Keeping package managers or compilers in production.","Debugging the runtime image as if it were the build image."],
    followUpQuestions: ["Why multi-stage for frontend apps?","How do you debug a tiny runtime image?","Could distroless be useful?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-12",
    number: "DEVOPS-M3-12",
    title: "Optimization",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "Docker optimization means reducing image size, build time, transfer time, runtime overhead and attack surface while preserving reproducibility and operability. The biggest wins usually come from multi-stage builds, small contexts, correct cache ordering and minimal runtime dependencies.",
    deepExplanation: "Optimization checklist:\n\n```text\n.dockerignore\n    ↓\nstable Dockerfile ordering\n    ↓\nBuildKit cache\n    ↓\nmulti-stage build\n    ↓\nminimal runtime image\n    ↓\nimmutable digest\n```\n\nInspect the result:\n\n```bash\ndocker image ls\ndocker history <image>\ndocker inspect <image>\n```\n\nOptimization should be measured against build duration, pull time, startup time, memory footprint and security findings.",
    productionExample: "A frontend image drops from 1.1GB to a small Nginx runtime image by removing Node/build tools, excluding `.git` and local dependencies from the build context, and using multi-stage builds.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Smaller images usually improve transfer and attack surface, but ultra-minimal images can make debugging harder and may remove useful diagnostics.",
    commonMistakes: ["Optimizing only image size.","Using obscure minimal images without operational familiarity.","Sacrificing reproducibility for cache tricks."],
    followUpQuestions: ["Image size vs startup time?","How do you profile build duration?","What is the right base-image size?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-13",
    number: "DEVOPS-M3-13",
    title: "Security",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "Docker security covers the host, daemon, image supply chain, container runtime, network, secrets, Linux capabilities, filesystem access and deployment identity. Containers are not a security boundary by themselves.",
    deepExplanation: "Example hardened runtime:\n\n```dockerfile\nFROM nginx:alpine\nCOPY dist /usr/share/nginx/html\nUSER 101\n```\n\nRuntime controls may include:\n\n```bash\ndocker run   --read-only   --cap-drop=ALL   --security-opt=no-new-privileges   myapp:1.4\n```\n\nUse secret managers or BuildKit secret mounts for sensitive credentials rather than storing secrets in images.",
    productionExample: "Production images are scanned, signed/attested where required, run as non-root, use read-only filesystems where possible, drop unnecessary capabilities and run with least-privilege cloud identities.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Hardening improves containment but can break applications that expect writable filesystems, privileged operations or root access. Controls should be tested before production rollout.",
    commonMistakes: ["Assuming a container is isolated enough without host security.","Using privileged containers casually.","Embedding credentials in Dockerfile layers.","Ignoring base-image and package vulnerabilities."],
    followUpQuestions: ["Container vs VM security?","How do capabilities work?","How do you manage container secrets?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-14",
    number: "DEVOPS-M3-14",
    title: "Scanning",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "Container scanning analyzes images and related artifacts for known vulnerabilities, insecure packages, configuration issues and sometimes malware or provenance concerns. Scanning should occur early in CI and again before production where risk warrants.",
    deepExplanation: "Example pipeline idea:\n\n```bash\ndocker build -t app:$GIT_SHA .\ndocker scout cves app:$GIT_SHA\n# or another approved image/SCA scanner\n```\n\nA mature policy distinguishes severity, exploitability, reachability and whether the vulnerable package exists in the final runtime image. The correct action may be rebuild, upgrade, mitigate, accept with documentation or block the release.",
    productionExample: "A release pipeline scans the final frontend/edge image, blocks high-risk exploitable vulnerabilities, records the scan result with the image digest and alerts when a newly disclosed CVE affects an already-deployed image.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Scanning improves visibility but can create noise and pipeline friction if every finding blocks delivery. Risk-based policies are more useful than raw vulnerability counts.",
    commonMistakes: ["Scanning only the source repository and not the image.","Blocking everything regardless of exploitability.","Ignoring newly disclosed issues after deployment.","Treating scanner output as a complete security assessment."],
    followUpQuestions: ["SCA vs image scanning?","What should block a release?","How do you handle false positives?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-15",
    number: "DEVOPS-M3-15",
    title: "Debugging",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "Docker debugging means isolating whether a failure comes from the image build, container process, filesystem, environment, network, dependency, runtime permissions or host/orchestrator. Use inspection commands to collect evidence before modifying the image.",
    deepExplanation: "A practical workflow:\n\n```bash\ndocker ps -a\ndocker logs <container>\ndocker inspect <container>\ndocker exec -it <container> sh\ndocker stats <container>\ndocker network inspect <network>\n```\n\nThen inspect the image:\n\n```bash\ndocker history <image>\ndocker image inspect <image>\n```\n\nFor startup failures, confirm the command, working directory, environment, file permissions, architecture and listening address. For frontend containers, remember that `localhost` inside a container refers to that container, not another service.",
    productionExample: "A container starts successfully but the frontend cannot reach the API. `docker logs` shows the web server is healthy; network inspection reveals the frontend is using `localhost:3000` instead of the service name `api:3000`. The fix is configuration, not rebuilding Docker.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Deep debugging can require shell access that minimal images do not provide. Adding debug tools to production images increases attack surface, so use temporary debug containers or sidecars where possible.",
    commonMistakes: ["Rebuilding repeatedly without inspecting logs.","Using `localhost` incorrectly between containers.","Assuming the host filesystem is the same as the container filesystem.","Adding troubleshooting packages permanently to production images."],
    followUpQuestions: ["How do you debug a crash-looping container?","How do you inspect a container without a shell?","Why does localhost behave differently in containers?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },
  {
    id: "devops-m3-16",
    number: "DEVOPS-M3-16",
    title: "Production Deployment",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Docker",
    expectedAnswer: "Docker production deployment means promoting a tested immutable image into a controlled runtime with secure configuration, health checks, resource limits, observability and a rollback strategy. The container image should be the same artifact tested before production.",
    deepExplanation: "Example release flow:\n\n```text\ngit commit\n  -> CI tests/security\n  -> build image\n  -> scan\n  -> push digest\n  -> staging\n  -> smoke/health checks\n  -> canary/blue-green\n  -> production\n  -> monitor\n  -> rollback if unhealthy\n```\n\nA deployment should reference an immutable image digest:\n\n```text\nregistry.example.com/frontend@sha256:<digest>\n```\n\nRuntime configuration and secrets are injected by the platform rather than baked into the image.",
    productionExample: "A React frontend is compiled into an Nginx image, scanned, pushed to a private registry and deployed by the platform using the image digest. A canary receives a small share of traffic; error rate and health signals determine promotion or rollback.",
    bestPractices: ["Pin release inputs and prefer immutable image digests.","Keep runtime images minimal and non-root where possible.","Automate build, test, scan and deployment verification.","Separate secrets from image content.","Monitor runtime health, resource usage and rollout status."],
    tradeOffs: "Immutable deployment improves reproducibility but requires good image registry, observability and rollout tooling. Canary/blue-green adds infrastructure and operational complexity.",
    commonMistakes: ["Rebuilding separately for production.","Using mutable tags.","No health checks.","No rollback path.","Storing production secrets in images."],
    followUpQuestions: ["Docker deployment vs Kubernetes deployment?","How do canary releases work?","What happens if the image registry is unavailable?"],
    relatedTopics: ["Docker","Containers","Images","Dockerfile","Networking","CI/CD","Security"],
  },

];

export const MOCK_DEVOPS_MODULE3_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'DevOps/Cloud',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));