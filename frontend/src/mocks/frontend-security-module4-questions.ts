// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 4 — Cross-Site Scripting).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m4-56",
    number: "SEC-M4-56",
    title: "Reflected XSS",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**Reflected XSS** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Reflected XSS** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Reflected XSS at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Reflected XSS at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-57",
    number: "SEC-M4-57",
    title: "Stored XSS",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**Stored XSS** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Stored XSS** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Stored XSS at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Stored XSS at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-58",
    number: "SEC-M4-58",
    title: "DOM XSS",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**DOM XSS** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**DOM XSS** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse DOM XSS at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use DOM XSS at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-59",
    number: "SEC-M4-59",
    title: "dangerouslySetInnerHTML",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**dangerouslySetInnerHTML** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**dangerouslySetInnerHTML** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse dangerouslySetInnerHTML at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use dangerouslySetInnerHTML at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-60",
    number: "SEC-M4-60",
    title: "Output encoding",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**Output encoding** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Output encoding** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Output encoding at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Output encoding at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-61",
    number: "SEC-M4-61",
    title: "Input validation",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**Input validation** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Input validation** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse Input validation as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Input validation as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-62",
    number: "SEC-M4-62",
    title: "Sanitization",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**Sanitization** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Sanitization** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Sanitization at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Sanitization at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-63",
    number: "SEC-M4-63",
    title: "DOMPurify",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**DOMPurify** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**DOMPurify** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse DOMPurify as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use DOMPurify as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-64",
    number: "SEC-M4-64",
    title: "Trusted Types",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**Trusted Types** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Trusted Types** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Trusted Types at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Trusted Types at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-65",
    number: "SEC-M4-65",
    title: "CSP for XSS",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**CSP for XSS** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CSP for XSS** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse CSP for XSS at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use CSP for XSS at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-66",
    number: "SEC-M4-66",
    title: "Safe DOM APIs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**Safe DOM APIs** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Safe DOM APIs** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Safe DOM APIs at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Safe DOM APIs at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-67",
    number: "SEC-M4-67",
    title: "URL injection",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**URL injection** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**URL injection** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse URL injection at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use URL injection at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-68",
    number: "SEC-M4-68",
    title: "HTML injection",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**HTML injection** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**HTML injection** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse HTML injection at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use HTML injection at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-69",
    number: "SEC-M4-69",
    title: "JavaScript URL hazards",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**JavaScript URL hazards** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**JavaScript URL hazards** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse JavaScript URL hazards at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use JavaScript URL hazards at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-70",
    number: "SEC-M4-70",
    title: "Template injection awareness",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**Template injection awareness** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Template injection awareness** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Template injection awareness at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Template injection awareness at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-71",
    number: "SEC-M4-71",
    title: "XSS in React",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**XSS in React** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**XSS in React** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse XSS in React at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use XSS in React at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-72",
    number: "SEC-M4-72",
    title: "XSS in third-party content",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**XSS in third-party content** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**XSS in third-party content** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse XSS in third-party content at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use XSS in third-party content at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-73",
    number: "SEC-M4-73",
    title: "XSS testing",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**XSS testing** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**XSS testing** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse XSS testing at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use XSS testing at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-74",
    number: "SEC-M4-74",
    title: "XSS incident response",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**XSS incident response** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**XSS incident response** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse XSS incident response at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use XSS incident response at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m4-75",
    number: "SEC-M4-75",
    title: "XSS defense architecture",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cross-Site Scripting",
    expectedAnswer: "**XSS defense architecture** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**XSS defense architecture** is an XSS-defense topic. The key is controlling how untrusted input crosses into HTML, DOM, URL, or script-sensitive contexts, while keeping authorization and trust decisions on trusted boundaries.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isSafeText(value: string): boolean {\n  return !value.includes(\"<script\");\n}\n\nconst allowed = isSafeText(\"hello\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst input = \"hello\";\n\nconst element = document.createElement(\"p\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse XSS defense architecture at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use XSS defense architecture at the rendering/data boundary. Treat user content as untrusted by default, avoid unsafe HTML sinks, and add security tests for dangerous inputs.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE4_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));