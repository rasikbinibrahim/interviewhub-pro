// Derived from frontend/src/document/Part_9_Advanced_React_Master_Handbook.md (Module 12 — Advanced Routing).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "reactadv-m12-201",
    number: "REACTADV-M12-201",
    title: "Nested routing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "Nested routing models a route hierarchy so child URLs, layouts and data can be composed inside a parent route. It is useful when several screens share a shell such as navigation, breadcrumbs or permissions while the child route changes independently.",
    deepExplanation: "```tsx\n// React Router-style example\nconst router = createBrowserRouter([\n  {\n    path: \"/projects\",\n    element: <ProjectsLayout />,\n    children: [\n      { index: true, element: <ProjectList /> },\n      { path: \":projectId\", element: <ProjectDetails /> },\n      { path: \":projectId/settings\", element: <ProjectSettings /> },\n    ],\n  },\n]);\n\nfunction ProjectsLayout() {\n  return (\n    <section>\n      <ProjectSidebar />\n      <Outlet />\n    </section>\n  );\n}\n```\n\nThe parent owns the shared layout while `Outlet` renders the matched child route. A production router may use a framework-specific API, but the architectural idea is the same.",
    productionExample: "A SaaS application has `/projects`, `/projects/:id`, and `/projects/:id/settings`. The sidebar, project navigation and permission checks remain mounted while only the nested content changes, preserving local layout state and reducing repeated page structure.",
    bestPractices: ["Align route nesting with real UI/layout ownership.", "Keep shared layouts above the routes that use them.", "Use stable route IDs and clear URL semantics.", "Avoid deeply nesting routes only to organize files.", "Test direct navigation and refresh for every nested path."],
    tradeOffs: "Nested routing reduces duplication and preserves shared layouts, but deep hierarchies can become hard to reason about and can couple URL structure to UI structure.",
    commonMistakes: ["Duplicating the same layout in every child route.", "Creating deeply nested URLs without product value.", "Assuming nested routes automatically share all data.", "Forgetting to handle direct access to child URLs."],
    followUpQuestions: ["How does route nesting affect layouts?", "How would you share data between parent and child routes?", "When should a route be flattened?", "How do you test deep links?"],
    relatedTopics: ["Nested routes", "Outlet", "Layouts", "Dynamic segments", "Route loaders", "Route hierarchy"],
  },
  {
    id: "reactadv-m12-202",
    number: "REACTADV-M12-202",
    title: "Data routers",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "A data router integrates navigation with route-level data loading, mutations and error handling rather than treating routing as only URL-to-component mapping. This lets the router coordinate loading states, redirects, aborts and route-level errors with navigation.",
    deepExplanation: "```tsx\nconst router = createBrowserRouter([\n  {\n    path: \"/users/:userId\",\n    loader: ({ params, request }) =>\n      fetchUser(params.userId!, request.signal),\n    element: <UserPage />,\n    errorElement: <RouteError />,\n  },\n]);\n```\n\nThe loader runs as part of navigation. The router can cancel obsolete work using the request signal and expose loader state through its APIs.",
    productionExample: "An enterprise dashboard loads account permissions before entering `/billing`. The data router starts route data work during navigation, cancels obsolete requests when the user navigates elsewhere, and renders a route-specific error UI if the load fails.",
    bestPractices: ["Use route loaders for data that is coupled to route entry.", "Pass and honor the navigation AbortSignal.", "Keep server authorization in the data layer.", "Avoid loading unrelated data in every parent route.", "Define route-level loading and failure UX."],
    tradeOffs: "Data routers reduce duplicated loading/navigation orchestration, but route modules can become tightly coupled to data dependencies and router-specific APIs.",
    commonMistakes: ["Treating loaders as a general state store.", "Ignoring cancellation.", "Loading everything at the root route.", "Duplicating the same request in loader and component without a clear reason."],
    followUpQuestions: ["Data router versus useEffect fetching?", "How does loader cancellation work?", "Where should authorization happen?", "How do loader errors reach the UI?"],
    relatedTopics: ["Data routers", "Loaders", "Actions", "AbortController", "Route errors", "Navigation state"],
  },
  {
    id: "reactadv-m12-203",
    number: "REACTADV-M12-203",
    title: "Route loaders",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "A route loader fetches or prepares data required by a route before or during navigation. It is most useful when the data is a prerequisite for rendering the route and should participate in navigation lifecycle, cancellation and route-level error handling.",
    deepExplanation: "```tsx\nconst routes = [\n  {\n    path: \"/orders/:orderId\",\n    loader: async ({ params, request }) => {\n      const response = await fetch(\n        `/api/orders/${params.orderId}`,\n        { signal: request.signal },\n      );\n\n      if (!response.ok) {\n        throw new Response(\"Order not found\", { status: response.status });\n      }\n\n      return response.json();\n    },\n    element: <OrderPage />,\n  },\n];\n```\n\nA route can then read the loader result through the router's supported data API.",
    productionExample: "An order-detail route should not render a misleading empty page while its primary order record is loading. The loader establishes the route's data dependency and provides a route-level failure path.",
    bestPractices: ["Use loaders for route prerequisites, not every small component query.", "Respect abort signals.", "Validate and authorize fetched data.", "Keep unrelated secondary data lazy or nested.", "Handle 404/401/403/5xx separately."],
    tradeOffs: "Route loaders improve navigation/data coordination but can increase route coupling. Component-level queries remain useful for independent or highly reusable data.",
    commonMistakes: ["Using a loader for a small UI-only toggle.", "Ignoring request cancellation.", "Returning unvalidated API data.", "Treating a route loader as a security boundary."],
    followUpQuestions: ["Loader versus TanStack Query?", "How do you cache loader data?", "How do you handle a 404?", "How do you cancel a loader?"],
    relatedTopics: ["Route loaders", "Data routers", "Caching", "AbortSignal", "Route errors"],
  },
  {
    id: "reactadv-m12-204",
    number: "REACTADV-M12-204",
    title: "Route actions",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "Route actions model mutations associated with a route or form submission. They let routing/data frameworks coordinate mutation state, navigation, revalidation and errors. The important senior concern is mutation ownership and cache consistency, not just where the function is declared.",
    deepExplanation: "```tsx\nconst routes = [\n  {\n    path: \"/projects/:projectId\",\n    action: async ({ params, request }) => {\n      const formData = await request.formData();\n      const name = String(formData.get(\"name\") ?? \"\").trim();\n\n      if (!name) {\n        return { ok: false, message: \"Name is required\" };\n      }\n\n      await updateProject(params.projectId!, { name });\n      return { ok: true };\n    },\n    element: <ProjectSettings />,\n  },\n];\n```\n\nThe router/framework can coordinate the form submission with navigation and revalidation according to its data APIs.",
    productionExample: "A project settings screen submits an update through the route action. The action validates input on the server, performs the mutation, and the route revalidates data so the page displays authoritative project state.",
    bestPractices: ["Validate and authorize mutations on the server.", "Use route actions for mutations tightly coupled to a route.", "Define idempotency for retryable writes.", "Revalidate affected route data after success.", "Return structured expected errors."],
    tradeOffs: "Route actions centralize navigation-aware mutations and revalidation, but they add router-specific semantics. Independent domain mutations may still be better represented by a dedicated API/service layer.",
    commonMistakes: ["Putting unrelated background mutations into route actions.", "Assuming action invocation is secure without server authorization.", "Forgetting revalidation after mutation.", "Retrying non-idempotent actions blindly."],
    followUpQuestions: ["Route action versus API client?", "How do actions trigger revalidation?", "How do you handle optimistic UI?", "When should a mutation live outside the route layer?"],
    relatedTopics: ["Route actions", "Form actions", "Mutations", "Revalidation", "Data routers"],
  },
  {
    id: "reactadv-m12-205",
    number: "REACTADV-M12-205",
    title: "Protected routes",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "A protected route prevents unauthenticated users from accessing a UI region, but frontend route protection is only a UX/navigation mechanism. Real authorization must happen on the server or trusted backend for every protected resource and mutation.",
    deepExplanation: "tsx\nfunction ProtectedRoute({\n  authenticated,\n  children,\n}: {\n  authenticated: boolean;\n  children: React.ReactNode;\n}) {\n  if (!authenticated) {\n    return <Navigate to=\"/login\" replace />;\n  }\n\n  return children;\n}\n```\n\nThe browser can redirect unauthenticated users, but the API must still reject unauthorized requests even if someone manually calls the endpoint.",
    productionExample: "An enterprise admin portal redirects users without a session to login and remembers the intended destination. The backend independently checks session and role permissions for every admin API.",
    bestPractices: ["Treat route guards as UX, not the security boundary.", "Handle session loading before deciding access.", "Preserve safe return URLs.", "Check authorization on the server.", "Avoid leaking protected data before the guard completes."],
    tradeOffs: "Client route guards improve navigation UX but cannot secure the data themselves. Server authorization is stronger but may require additional latency and route/data coordination.",
    commonMistakes: ["Hiding a route and assuming it is secure.", "Checking roles only in React.", "Rendering sensitive data before the auth check finishes.", "Trusting arbitrary redirect URLs."],
    followUpQuestions: ["Authentication versus authorization?", "How do you prevent open redirects?", "What happens while the session is loading?", "Where should role checks live?"],
    relatedTopics: ["Authentication", "Authorization", "Route guards", "Sessions", "Access control"],
  },
  {
    id: "reactadv-m12-206",
    number: "REACTADV-M12-206",
    title: "Authorization routing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "Authorization routing maps user permissions to available routes and actions. The frontend can use roles/permissions to shape navigation and prevent confusing access attempts, but the backend must enforce the same policy for actual resources and mutations.",
    deepExplanation: "tsx\ntype Permission =\n  | \"billing.read\"\n  | \"billing.write\"\n  | \"users.manage\";\n\nfunction Can({\n  permission,\n  permissions,\n  children,\n}: {\n  permission: Permission;\n  permissions: Set<Permission>;\n  children: React.ReactNode;\n}) {\n  return permissions.has(permission) ? children : null;\n}\n\nfunction BillingRoute({ permissions }: { permissions: Set<Permission> }) {\n  return (\n    <Can permission=\"billing.read\" permissions={permissions}>\n      <BillingPage />\n    </Can>\n  );\n}\n```\n\nThis is a UI authorization check. The backend must still enforce `billing.read` or `billing.write` on the actual API/resource.",
    productionExample: "A multi-tenant SaaS app hides billing and user-management routes from users without the required permissions and shows a 403 page for direct navigation. The API verifies tenant membership and permission on every request.",
    bestPractices: ["Model permissions explicitly rather than scattering role strings.", "Separate navigation visibility from real authorization.", "Validate tenant scope server-side.", "Use least-privilege permissions.", "Test both visible and direct unauthorized URLs."],
    tradeOffs: "Permission-driven routing gives users a coherent UI, but authorization logic can become complex in multi-tenant systems. Central policy services improve consistency but introduce dependency and latency.",
    commonMistakes: ["Using only role names such as `admin` everywhere.", "Checking permissions only during navigation.", "Forgetting tenant/resource-level authorization.", "Embedding a large policy matrix in every component."],
    followUpQuestions: ["RBAC versus permission-based access?", "How do you handle tenant-level permissions?", "How do you keep frontend and backend policies aligned?", "What happens when permissions change during a session?"],
    relatedTopics: ["RBAC", "ABAC", "Permissions", "Multi-tenancy", "Authorization", "Route guards"],
  },
  {
    id: "reactadv-m12-207",
    number: "REACTADV-M12-207",
    title: "Dynamic segments",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "Dynamic route segments represent variable identifiers in the URL, such as `/users/:userId` or `/products/[slug]`. They are useful when URL identity maps directly to a domain resource. A production implementation must validate the parameter, handle not-found and authorization cases, and avoid assuming every path parameter is trustworthy.",
    deepExplanation: "tsx\nconst routes = [\n  {\n    path: \"/users/:userId\",\n    element: <UserPage />,\n  },\n];\n\nfunction UserPage() {\n  const { userId } = useParams<{ userId: string }>();\n\n  if (!userId) {\n    return <NotFound />;\n  }\n\n  return <UserDetails id={userId} />;\n}\n```\n\nThe parameter identifies a resource; the server still validates that the current user can access that resource.",
    productionExample: "A knowledge-base app uses `/articles/:slug`. The router extracts the slug, the server looks up the article, and a route-level 404 is rendered if the article does not exist or is not published.",
    bestPractices: ["Validate and normalize dynamic parameters.", "Use meaningful stable identifiers in URLs.", "Handle not-found explicitly.", "Enforce authorization server-side.", "Avoid leaking sensitive IDs unnecessarily."],
    tradeOffs: "Readable dynamic URLs improve navigation and deep linking. IDs can expose internal structure if chosen poorly; slugs and opaque IDs may be preferable depending on privacy and SEO requirements.",
    commonMistakes: ["Trusting the URL parameter for authorization.", "Assuming the parameter is always present or valid.", "Using unstable client-generated IDs in canonical URLs.", "Ignoring URL encoding/decoding issues."],
    followUpQuestions: ["How do you validate route params?", "Slug versus numeric ID?", "How would you handle a missing resource?", "How do route params affect cache keys?"],
    relatedTopics: ["Dynamic segments", "URL params", "Slugs", "Resource identity", "404 handling"],
  },
  {
    id: "reactadv-m12-208",
    number: "REACTADV-M12-208",
    title: "Search parameters",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "Search parameters represent optional, shareable route state such as filters, sort order, pagination or search terms. They are useful when state should survive refresh, be bookmarkable and participate in navigation history. Sensitive or purely ephemeral state should not automatically be placed in the URL.",
    deepExplanation: "tsx\nfunction ProductFilters() {\n  const [searchParams, setSearchParams] = useSearchParams();\n\n  const sort = searchParams.get(\"sort\") ?? \"popular\";\n  const category = searchParams.get(\"category\") ?? \"all\";\n\n  function updateCategory(value: string) {\n    const next = new URLSearchParams(searchParams);\n    next.set(\"category\", value);\n    setSearchParams(next);\n  }\n\n  return (\n    <select value={category} onChange={e => updateCategory(e.target.value)}>\n      <option value=\"all\">All</option>\n      <option value=\"books\">Books</option>\n    </select>\n  );\n}\n```\n\nThe URL becomes the source of truth for shareable filter state.",
    productionExample: "A marketplace product list stores `?category=books&sort=price&page=2` in the URL. Users can refresh, share the link, use browser back/forward and return to the same filter state.",
    bestPractices: ["Use URL state for shareable/navigation-relevant state.", "Serialize values deterministically.", "Keep URLs readable and bounded.", "Validate incoming parameters.", "Do not place secrets or sensitive data in query strings.", "Decide whether changes should push or replace history."],
    tradeOffs: "URL state improves shareability and navigation behavior, but URLs can become noisy and every change can affect history and data fetching. Local state is simpler for ephemeral UI.",
    commonMistakes: ["Putting passwords or tokens in query parameters.", "Creating a new history entry for every keystroke.", "Parsing all values as trusted typed data.", "Duplicating the same state in both URL and local state without synchronization rules."],
    followUpQuestions: ["When should URL state replace local state?", "Push versus replace?", "How do you debounce URL updates?", "How do you validate complex query parameters?"],
    relatedTopics: ["Search params", "URL state", "History", "Filters", "Deep linking"],
  },
  {
    id: "reactadv-m12-209",
    number: "REACTADV-M12-209",
    title: "Lazy routes",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "Lazy routes defer loading route code until navigation requires it. In practice this is route-level code splitting using dynamic imports and Suspense/router-native lazy APIs. The goal is to reduce initial JavaScript while preserving acceptable first-navigation latency.",
    deepExplanation: "tsx\nimport { lazy, Suspense } from \"react\";\n\nconst AdminPage = lazy(() => import(\"./AdminPage\"));\n\nfunction AdminRoute() {\n  return (\n    <Suspense fallback={<p role=\"status\">Loading admin…</p>}>\n      <AdminPage />\n    </Suspense>\n  );\n}\n```\n\nThe route is downloaded when needed rather than being included in the initial bundle.",
    productionExample: "A SaaS product keeps reports, billing and admin screens in separate chunks. The initial dashboard remains small, while likely next routes are optionally prefetched after the current route becomes idle.",
    bestPractices: ["Split at meaningful route boundaries.", "Keep lazy imports at module scope.", "Provide route-level loading and error states.", "Measure chunk size and first-navigation latency.", "Prefetch only high-probability next routes."],
    tradeOffs: "Lazy loading improves initial startup but adds navigation latency and chunk-failure scenarios. Over-splitting can create many small requests and complicated caching.",
    commonMistakes: ["Lazy-loading every tiny component.", "Declaring lazy components inside render.", "Using Suspense without an error boundary for failed chunks.", "Ignoring shared dependencies between chunks."],
    followUpQuestions: ["When is route-level splitting better than component-level splitting?", "How do you recover from a failed chunk?", "How would you prefetch likely next routes?"],
    relatedTopics: ["Code splitting", "React.lazy", "Suspense", "Route chunks", "Prefetching"],
  },
  {
    id: "reactadv-m12-210",
    number: "REACTADV-M12-210",
    title: "Route-level Suspense",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "A route-level Suspense boundary defines the loading UX for a route or route section when supported lazy or asynchronous rendering work is not ready. Boundary placement determines how much of the UI is replaced by the fallback, so it should align with meaningful navigation units.",
    deepExplanation: "tsx\n<Routes>\n  <Route\n    path=\"/reports\"\n    element={\n      <Suspense fallback={<ReportsSkeleton />}>\n        <ReportsPage />\n      </Suspense>\n    }\n  />\n</Routes>\n```\n\nA route fallback should preserve the surrounding application shell where possible while communicating that the requested content is loading.",
    productionExample: "When navigating from the dashboard to reports, the global navigation remains interactive while the reports content shows a skeleton. This is preferable to blanking the entire application during the route chunk/data load.",
    bestPractices: ["Keep stable chrome outside the route Suspense boundary.", "Design fallbacks around the route's real geometry.", "Pair loading boundaries with error boundaries.", "Avoid a single app-wide spinner for every route.", "Test slow, fast and failed navigation."],
    tradeOffs: "Route-level boundaries improve perceived performance and isolation, but too many boundaries can produce fragmented loading transitions; one giant boundary can hide useful existing UI.",
    commonMistakes: ["Replacing the entire app shell with a spinner.", "Using a fallback unrelated to the final layout.", "Ignoring accessibility of loading states.", "Assuming Suspense handles all route errors."],
    followUpQuestions: ["Where should Suspense sit relative to the layout?", "How does it interact with route loaders?", "Why also use an error boundary?"],
    relatedTopics: ["Suspense", "Route loading", "Skeleton UI", "Error boundaries", "Lazy routes"],
  },
  {
    id: "reactadv-m12-211",
    number: "REACTADV-M12-211",
    title: "Error routes",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "Error routes provide route-scoped recovery UI when navigation, loading, rendering or action processing fails. A good routing system distinguishes not-found, unauthorized/forbidden and unexpected server/client failures and provides an appropriate recovery path.",
    deepExplanation: "tsx\nconst router = createBrowserRouter([\n  {\n    path: \"/orders/:id\",\n    loader: loadOrder,\n    element: <OrderPage />,\n    errorElement: <OrderError />,\n  },\n]);\n\nfunction OrderError() {\n  return (\n    <section role=\"alert\">\n      <h1>Unable to load this order</h1>\n      <Link to=\"/orders\">Back to orders</Link>\n    </section>\n  );\n}\n```\n\nThe exact error API differs by router, but the principle is to keep failure handling close to the route that can recover from it.",
    productionExample: "A broken reports API shows a route-specific error with retry/back navigation while the application shell and navigation remain available. A 404 gets a resource-specific not-found page rather than a generic crash screen.",
    bestPractices: ["Separate 404, 401/403 and unexpected failures.", "Make fallback UI accessible.", "Provide retry or navigation actions.", "Log unexpected failures centrally.", "Do not expose backend stack traces or sensitive details."],
    tradeOffs: "Route-scoped errors provide localized recovery. The trade-off is additional error-state design and router-specific semantics.",
    commonMistakes: ["Using one generic error page for every status.", "Treating authorization failure as not-found without a product reason.", "Leaving users trapped on an error route.", "Logging sensitive server responses directly to telemetry."],
    followUpQuestions: ["Route error versus ErrorBoundary?", "How do you handle 404?", "How do you recover from a failed loader?", "What should the root error page do?"],
    relatedTopics: ["Route errors", "Error boundaries", "404", "403", "Recovery UI", "Observability"],
  },
  {
    id: "reactadv-m12-212",
    number: "REACTADV-M12-212",
    title: "Navigation state",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "Navigation state describes the lifecycle of a route transition: idle, loading/pending, submitting, redirecting or completed, depending on the router. Exposing this state lets the UI show progress, disable duplicate actions and coordinate pending navigation without inventing ad-hoc flags.",
    deepExplanation: "tsx\nfunction GlobalProgress() {\n  const navigation = useNavigation();\n\n  const pending = navigation.state !== \"idle\";\n\n  return (\n    <div aria-live=\"polite\">\n      {pending ? \"Navigating…\" : \"Ready\"}\n    </div>\n  );\n}\n```\n\nThe exact API is router-specific, but the architecture should derive pending UI from the router rather than maintaining a separate `isNavigating` flag.",
    productionExample: "A large application shows a thin top progress indicator during navigation while keeping the current page visible. Route-level skeletons handle the new page's content loading separately.",
    bestPractices: ["Use router-provided navigation state.", "Keep pending indicators lightweight.", "Do not disable unrelated interactions unnecessarily.", "Coordinate navigation state with Suspense and loaders.", "Test fast navigation so indicators do not flicker."],
    tradeOffs: "Centralized navigation state prevents duplicated flags and inconsistent UX. Overly prominent global progress UI can make fast navigation feel slower or distract users.",
    commonMistakes: ["Maintaining manual `isNavigating` state everywhere.", "Blocking the entire app during every navigation.", "Showing a spinner for sub-second transitions.", "Confusing navigation pending with a specific API mutation."],
    followUpQuestions: ["How do you avoid loading-indicator flicker?", "Navigation state versus request state?", "How does navigation state interact with transitions?"],
    relatedTopics: ["Navigation state", "Pending UI", "Loaders", "Transitions", "Suspense"],
  },
  {
    id: "reactadv-m12-213",
    number: "REACTADV-M12-213",
    title: "Prefetching routes",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "Route prefetching starts downloading likely-needed route code or data before the user explicitly navigates. It can reduce next-navigation latency when intent is predictable, but it consumes bandwidth and cache space, so it should be selective.",
    deepExplanation: "tsx\nconst loadReports = () => import(\"./ReportsPage\");\n\nfunction ReportsLink() {\n  return (\n    <a\n      href=\"/reports\"\n      onPointerEnter={() => {\n        void loadReports();\n      }}\n    >\n      Reports\n    </a>\n  );\n}\n```\n\nThis is a simple client-side intent prefetch. Routers/frameworks often provide better prefetch APIs that should be preferred when available.",
    productionExample: "A B2B dashboard prefetches the reports route when the user hovers/focuses the Reports navigation item, but does not prefetch every admin route. On constrained networks, the app reduces or disables speculative prefetching.",
    bestPractices: ["Prefetch high-probability navigation targets.", "Respect network/device constraints when appropriate.", "Prefer framework/router-native prefetch APIs.", "Measure hit rate and bandwidth waste.", "Avoid duplicate prefetch requests."],
    tradeOffs: "Prefetching can make navigation feel instant, but wrong predictions waste bandwidth and compete with critical resources.",
    commonMistakes: ["Prefetching every route.", "Prefetching huge chunks on slow networks.", "Confusing preload and prefetch semantics.", "Ignoring whether the prefetched chunk is actually reused."],
    followUpQuestions: ["Prefetch versus preload?", "What should trigger prefetching?", "How do you measure speculative bandwidth waste?", "How does code-splitting strategy affect prefetching?"],
    relatedTopics: ["Prefetching", "Preload", "Code splitting", "Dynamic import", "Resource hints"],
  },
  {
    id: "reactadv-m12-214",
    number: "REACTADV-M12-214",
    title: "Scroll restoration",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "Scroll restoration preserves or restores the user's scroll position across navigation. It should distinguish browser back/forward behavior from a new navigation where scrolling to the top is normally expected. Long pages, nested scroll containers and asynchronous content make restoration more complex.",
    deepExplanation: "tsx\nfunction useScrollToTop(pathname: string) {\n  useEffect(() => {\n    window.scrollTo({ top: 0, left: 0 });\n  }, [pathname]);\n}\n```\n\nA production router should preferably use its built-in scroll restoration when it supports history-aware behavior. Manual effects are appropriate only when the router does not provide the required semantics.",
    productionExample: "In a marketplace app, clicking into a product starts at the top of the product page, but pressing browser Back restores the previous search-result scroll position. The router stores history-associated positions rather than using a blanket scroll-to-top on every navigation.",
    bestPractices: ["Prefer router-native scroll restoration.", "Differentiate PUSH/REPLACE from POP/back-forward navigation.", "Account for nested scroll containers.", "Avoid restoring before the page has enough content to match the previous layout.", "Test refresh, back, forward and lazy-loaded content."],
    tradeOffs: "Native/router restoration is simpler and more accurate. Manual restoration provides control but is easy to break with async content, browser history and nested scrolling.",
    commonMistakes: ["Always calling scrollTo(0,0) after every route change.", "Ignoring nested scroll containers.", "Restoring before content dimensions are stable.", "Breaking browser back-button expectations."],
    followUpQuestions: ["How should Back behave differently from a new navigation?", "How do you restore scroll in a virtualized list?", "What happens when content height changes after restoration?"],
    relatedTopics: ["Scroll restoration", "History", "Browser navigation", "Nested scrolling", "Virtualization"],
  },
  {
    id: "reactadv-m12-215",
    number: "REACTADV-M12-215",
    title: "Enterprise routing architecture",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced Routing",
    expectedAnswer: "Enterprise routing architecture should define route hierarchy, layouts, data loading, mutations, authorization, code splitting, loading/error states, navigation state, URL state and observability as one coherent system. The router should be a platform boundary, not a collection of ad-hoc guards and effects.",
    deepExplanation: "tsx\nconst router = createBrowserRouter([\n  {\n    path: \"/\",\n    element: <AppShell />,\n    errorElement: <RootError />,\n    children: [\n      {\n        path: \"projects\",\n        element: <ProjectsLayout />,\n        loader: loadProjects,\n        children: [\n          {\n            index: true,\n            element: <ProjectList />,\n          },\n          {\n            path: \":projectId\",\n            lazy: () => import(\"./project-route\"),\n          },\n        ],\n      },\n    ],\n  },\n]);\n```\n\nThe exact router API varies, but the enterprise concerns remain: a stable route tree, explicit data ownership, protected operations, scoped loading/error UI and measurable performance.",
    productionExample: "A 50-team SaaS platform standardizes route conventions: domain-based nested routes, route-level loaders for prerequisites, actions for route-owned mutations, server-side authorization, lazy route chunks, scoped Suspense/error boundaries, URL-based filter state and centralized navigation telemetry.",
    bestPractices: ["Define route conventions across teams.", "Keep security decisions server-side.", "Use nested layouts deliberately.", "Standardize loading/error/recovery behavior.", "Use lazy routes for meaningful feature boundaries.", "Keep URL state shareable and validated.", "Instrument navigation latency, route errors and chunk failures.", "Document migration and versioning rules for router APIs."],
    tradeOffs: "A standardized routing architecture reduces duplicated navigation/data/security patterns, but central rules can become rigid and router-specific. Enterprise teams need conventions with documented exceptions.",
    commonMistakes: ["Putting all business logic in route components.", "Creating one global auth guard and assuming it secures every API.", "Using nested routes only for folder organization.", "Ignoring deep-link, refresh and error behavior.", "Allowing every team to invent route-loading and error patterns."],
    followUpQuestions: ["How would you design routing for 500 screens?", "How do loaders, actions and server APIs divide responsibility?", "How would you migrate from an older router?", "What routing metrics matter in production?", "How do you handle multi-tenant authorization?"],
    relatedTopics: ["Routing architecture", "Nested routing", "Data routers", "Authorization", "Lazy routes", "Suspense", "Error routes", "Navigation state", "Observability"],
  },

];

export const MOCK_ADVANCED_REACT_MODULE12_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Advanced React',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));