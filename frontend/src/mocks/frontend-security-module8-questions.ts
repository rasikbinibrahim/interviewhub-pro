// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 8 — Cookies & Browser Storage).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m8-121",
    number: "SEC-M8-121",
    title: "Cookies",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Cookies** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Cookies** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface StoredValue {\n  key: string;\n  value: string;\n}\n\nfunction findValue(\n  entries: readonly StoredValue[],\n  key: string,\n): string | null {\n  for (let i = 0; i < entries.length; i += 1) {\n    if (entries[i].key === key) {\n      return entries[i].value;\n    }\n  }\n\n  return null;\n}\n\nconst value = findValue(\n  [{ key: \"theme\", value: \"dark\" }],\n  \"theme\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst storage = new Map<string, string>();\n\nstorage.set(\"theme\", \"dark\");\n\nconst value =\n  storage.get(\"theme\") ?? null;\n```\n\nStep 5 — Production example:\nUse Cookies only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Cookies only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-122",
    number: "SEC-M8-122",
    title: "HttpOnly",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**HttpOnly** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**HttpOnly** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse HttpOnly as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use HttpOnly as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-123",
    number: "SEC-M8-123",
    title: "Secure",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Secure** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Secure** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse Secure as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Secure as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-124",
    number: "SEC-M8-124",
    title: "SameSite",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**SameSite** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**SameSite** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse SameSite as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use SameSite as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-125",
    number: "SEC-M8-125",
    title: "Domain",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Domain** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Domain** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse Domain as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Domain as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-126",
    number: "SEC-M8-126",
    title: "Path",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Path** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Path** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse Path as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Path as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-127",
    number: "SEC-M8-127",
    title: "Expires",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Expires** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Expires** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse Expires as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Expires as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-128",
    number: "SEC-M8-128",
    title: "Max-Age",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Max-Age** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Max-Age** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse Max-Age as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Max-Age as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-129",
    number: "SEC-M8-129",
    title: "Cookie prefixes",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Cookie prefixes** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Cookie prefixes** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface StoredValue {\n  key: string;\n  value: string;\n}\n\nfunction findValue(\n  entries: readonly StoredValue[],\n  key: string,\n): string | null {\n  for (let i = 0; i < entries.length; i += 1) {\n    if (entries[i].key === key) {\n      return entries[i].value;\n    }\n  }\n\n  return null;\n}\n\nconst value = findValue(\n  [{ key: \"theme\", value: \"dark\" }],\n  \"theme\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst storage = new Map<string, string>();\n\nstorage.set(\"theme\", \"dark\");\n\nconst value =\n  storage.get(\"theme\") ?? null;\n```\n\nStep 5 — Production example:\nUse Cookie prefixes only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Cookie prefixes only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-130",
    number: "SEC-M8-130",
    title: "CHIPS",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**CHIPS** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CHIPS** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse CHIPS as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use CHIPS as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-131",
    number: "SEC-M8-131",
    title: "Partitioned cookies",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Partitioned cookies** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Partitioned cookies** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface StoredValue {\n  key: string;\n  value: string;\n}\n\nfunction findValue(\n  entries: readonly StoredValue[],\n  key: string,\n): string | null {\n  for (let i = 0; i < entries.length; i += 1) {\n    if (entries[i].key === key) {\n      return entries[i].value;\n    }\n  }\n\n  return null;\n}\n\nconst value = findValue(\n  [{ key: \"theme\", value: \"dark\" }],\n  \"theme\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst storage = new Map<string, string>();\n\nstorage.set(\"theme\", \"dark\");\n\nconst value =\n  storage.get(\"theme\") ?? null;\n```\n\nStep 5 — Production example:\nUse Partitioned cookies only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Partitioned cookies only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-132",
    number: "SEC-M8-132",
    title: "SameParty overview",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**SameParty overview** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**SameParty overview** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse SameParty overview as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use SameParty overview as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-133",
    number: "SEC-M8-133",
    title: "localStorage",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**localStorage** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**localStorage** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface StoredValue {\n  key: string;\n  value: string;\n}\n\nfunction findValue(\n  entries: readonly StoredValue[],\n  key: string,\n): string | null {\n  for (let i = 0; i < entries.length; i += 1) {\n    if (entries[i].key === key) {\n      return entries[i].value;\n    }\n  }\n\n  return null;\n}\n\nconst value = findValue(\n  [{ key: \"theme\", value: \"dark\" }],\n  \"theme\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst storage = new Map<string, string>();\n\nstorage.set(\"theme\", \"dark\");\n\nconst value =\n  storage.get(\"theme\") ?? null;\n```\n\nStep 5 — Production example:\nUse localStorage only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use localStorage only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-134",
    number: "SEC-M8-134",
    title: "sessionStorage",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**sessionStorage** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**sessionStorage** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface StoredValue {\n  key: string;\n  value: string;\n}\n\nfunction findValue(\n  entries: readonly StoredValue[],\n  key: string,\n): string | null {\n  for (let i = 0; i < entries.length; i += 1) {\n    if (entries[i].key === key) {\n      return entries[i].value;\n    }\n  }\n\n  return null;\n}\n\nconst value = findValue(\n  [{ key: \"theme\", value: \"dark\" }],\n  \"theme\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst storage = new Map<string, string>();\n\nstorage.set(\"theme\", \"dark\");\n\nconst value =\n  storage.get(\"theme\") ?? null;\n```\n\nStep 5 — Production example:\nUse sessionStorage only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use sessionStorage only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-135",
    number: "SEC-M8-135",
    title: "IndexedDB",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**IndexedDB** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**IndexedDB** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface StoredValue {\n  key: string;\n  value: string;\n}\n\nfunction findValue(\n  entries: readonly StoredValue[],\n  key: string,\n): string | null {\n  for (let i = 0; i < entries.length; i += 1) {\n    if (entries[i].key === key) {\n      return entries[i].value;\n    }\n  }\n\n  return null;\n}\n\nconst value = findValue(\n  [{ key: \"theme\", value: \"dark\" }],\n  \"theme\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst storage = new Map<string, string>();\n\nstorage.set(\"theme\", \"dark\");\n\nconst value =\n  storage.get(\"theme\") ?? null;\n```\n\nStep 5 — Production example:\nUse IndexedDB only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use IndexedDB only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-136",
    number: "SEC-M8-136",
    title: "Storage partitioning",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Storage partitioning** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Storage partitioning** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface StoredValue {\n  key: string;\n  value: string;\n}\n\nfunction findValue(\n  entries: readonly StoredValue[],\n  key: string,\n): string | null {\n  for (let i = 0; i < entries.length; i += 1) {\n    if (entries[i].key === key) {\n      return entries[i].value;\n    }\n  }\n\n  return null;\n}\n\nconst value = findValue(\n  [{ key: \"theme\", value: \"dark\" }],\n  \"theme\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst storage = new Map<string, string>();\n\nstorage.set(\"theme\", \"dark\");\n\nconst value =\n  storage.get(\"theme\") ?? null;\n```\n\nStep 5 — Production example:\nUse Storage partitioning only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Storage partitioning only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-137",
    number: "SEC-M8-137",
    title: "Secure storage strategy",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Secure storage strategy** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Secure storage strategy** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface StoredValue {\n  key: string;\n  value: string;\n}\n\nfunction findValue(\n  entries: readonly StoredValue[],\n  key: string,\n): string | null {\n  for (let i = 0; i < entries.length; i += 1) {\n    if (entries[i].key === key) {\n      return entries[i].value;\n    }\n  }\n\n  return null;\n}\n\nconst value = findValue(\n  [{ key: \"theme\", value: \"dark\" }],\n  \"theme\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst storage = new Map<string, string>();\n\nstorage.set(\"theme\", \"dark\");\n\nconst value =\n  storage.get(\"theme\") ?? null;\n```\n\nStep 5 — Production example:\nUse Secure storage strategy only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Secure storage strategy only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-138",
    number: "SEC-M8-138",
    title: "Token storage trade-offs",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Token storage trade-offs** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Token storage trade-offs** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface StoredValue {\n  key: string;\n  value: string;\n}\n\nfunction findValue(\n  entries: readonly StoredValue[],\n  key: string,\n): string | null {\n  for (let i = 0; i < entries.length; i += 1) {\n    if (entries[i].key === key) {\n      return entries[i].value;\n    }\n  }\n\n  return null;\n}\n\nconst value = findValue(\n  [{ key: \"theme\", value: \"dark\" }],\n  \"theme\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst storage = new Map<string, string>();\n\nstorage.set(\"theme\", \"dark\");\n\nconst value =\n  storage.get(\"theme\") ?? null;\n```\n\nStep 5 — Production example:\nUse Token storage trade-offs only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Token storage trade-offs only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-139",
    number: "SEC-M8-139",
    title: "Browser privacy controls",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Browser privacy controls** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Browser privacy controls** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface StoredValue {\n  key: string;\n  value: string;\n}\n\nfunction findValue(\n  entries: readonly StoredValue[],\n  key: string,\n): string | null {\n  for (let i = 0; i < entries.length; i += 1) {\n    if (entries[i].key === key) {\n      return entries[i].value;\n    }\n  }\n\n  return null;\n}\n\nconst value = findValue(\n  [{ key: \"theme\", value: \"dark\" }],\n  \"theme\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst storage = new Map<string, string>();\n\nstorage.set(\"theme\", \"dark\");\n\nconst value =\n  storage.get(\"theme\") ?? null;\n```\n\nStep 5 — Production example:\nUse Browser privacy controls only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Browser privacy controls only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m8-140",
    number: "SEC-M8-140",
    title: "Enterprise storage architecture",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Cookies & Browser Storage",
    expectedAnswer: "**Enterprise storage architecture** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Enterprise storage architecture** is a browser storage/security topic. Explain who can read the data, the XSS/CSRF/privacy implications, lifecycle and partitioning behavior, and what belongs in client storage versus a server-managed session.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface StoredValue {\n  key: string;\n  value: string;\n}\n\nfunction findValue(\n  entries: readonly StoredValue[],\n  key: string,\n): string | null {\n  for (let i = 0; i < entries.length; i += 1) {\n    if (entries[i].key === key) {\n      return entries[i].value;\n    }\n  }\n\n  return null;\n}\n\nconst value = findValue(\n  [{ key: \"theme\", value: \"dark\" }],\n  \"theme\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst storage = new Map<string, string>();\n\nstorage.set(\"theme\", \"dark\");\n\nconst value =\n  storage.get(\"theme\") ?? null;\n```\n\nStep 5 — Production example:\nUse Enterprise storage architecture only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Enterprise storage architecture only for data whose confidentiality and lifecycle are appropriate for that storage mechanism. Keep sensitive credentials out of script-readable storage when a safer server-managed alternative exists.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE8_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));