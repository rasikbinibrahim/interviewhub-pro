// Derived from frontend/src/document/Part_11_Frontend_Performance_Optimization_Master_Handbook.md (Module 8 — CSS Performance).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "perf-m8-136",
    number: "PERF-M8-136",
    title: "CSS parsing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**CSS parsing** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CSS parsing** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse CSS parsing at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use CSS parsing at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-137",
    number: "PERF-M8-137",
    title: "CSSOM",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**CSSOM** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CSSOM** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface Timing {\n  start: number;\n  end: number;\n}\n\nfunction duration(\n  timing: Timing,\n): number {\n  return timing.end - timing.start;\n}\n\nconst ms = duration({\n  start: 100,\n  end: 145,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst start = performance.now();\nconst end = performance.now();\n\nconst ms = end - start;\n```\n\nStep 5 — Production example:\nUse browser DevTools traces to locate the CSSOM bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use browser DevTools traces to locate the CSSOM bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-138",
    number: "PERF-M8-138",
    title: "Selector performance",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**Selector performance** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Selector performance** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Selector performance at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Selector performance at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-139",
    number: "PERF-M8-139",
    title: "Style recalculation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**Style recalculation** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Style recalculation** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Style recalculation at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Style recalculation at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-140",
    number: "PERF-M8-140",
    title: "Layout cost",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**Layout cost** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Layout cost** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface Timing {\n  start: number;\n  end: number;\n}\n\nfunction duration(\n  timing: Timing,\n): number {\n  return timing.end - timing.start;\n}\n\nconst ms = duration({\n  start: 100,\n  end: 145,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst start = performance.now();\nconst end = performance.now();\n\nconst ms = end - start;\n```\n\nStep 5 — Production example:\nUse browser DevTools traces to locate the Layout cost bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use browser DevTools traces to locate the Layout cost bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-141",
    number: "PERF-M8-141",
    title: "Paint cost",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**Paint cost** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Paint cost** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface Timing {\n  start: number;\n  end: number;\n}\n\nfunction duration(\n  timing: Timing,\n): number {\n  return timing.end - timing.start;\n}\n\nconst ms = duration({\n  start: 100,\n  end: 145,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst start = performance.now();\nconst end = performance.now();\n\nconst ms = end - start;\n```\n\nStep 5 — Production example:\nUse browser DevTools traces to locate the Paint cost bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use browser DevTools traces to locate the Paint cost bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-142",
    number: "PERF-M8-142",
    title: "Composite-only properties",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**Composite-only properties** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Composite-only properties** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface Timing {\n  start: number;\n  end: number;\n}\n\nfunction duration(\n  timing: Timing,\n): number {\n  return timing.end - timing.start;\n}\n\nconst ms = duration({\n  start: 100,\n  end: 145,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst start = performance.now();\nconst end = performance.now();\n\nconst ms = end - start;\n```\n\nStep 5 — Production example:\nUse browser DevTools traces to locate the Composite-only properties bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use browser DevTools traces to locate the Composite-only properties bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-143",
    number: "PERF-M8-143",
    title: "Transform",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**Transform** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Transform** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Transform at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Transform at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-144",
    number: "PERF-M8-144",
    title: "Opacity",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**Opacity** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Opacity** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Opacity at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Opacity at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-145",
    number: "PERF-M8-145",
    title: "CSS containment",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**CSS containment** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CSS containment** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface Timing {\n  start: number;\n  end: number;\n}\n\nfunction duration(\n  timing: Timing,\n): number {\n  return timing.end - timing.start;\n}\n\nconst ms = duration({\n  start: 100,\n  end: 145,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst start = performance.now();\nconst end = performance.now();\n\nconst ms = end - start;\n```\n\nStep 5 — Production example:\nUse browser DevTools traces to locate the CSS containment bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use browser DevTools traces to locate the CSS containment bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-146",
    number: "PERF-M8-146",
    title: "content-visibility",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**content-visibility** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**content-visibility** concerns the browser rendering pipeline. Explain what stage it belongs to, what causes work at that stage, how it can block or delay pixels, and how to measure the bottleneck before changing code.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface Timing {\n  start: number;\n  end: number;\n}\n\nfunction duration(\n  timing: Timing,\n): number {\n  return timing.end - timing.start;\n}\n\nconst ms = duration({\n  start: 100,\n  end: 145,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst start = performance.now();\nconst end = performance.now();\n\nconst ms = end - start;\n```\n\nStep 5 — Production example:\nUse browser DevTools traces to locate the content-visibility bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use browser DevTools traces to locate the content-visibility bottleneck, then reduce the amount or frequency of work instead of blindly changing rendering code.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-147",
    number: "PERF-M8-147",
    title: "Critical CSS",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**Critical CSS** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Critical CSS** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Critical CSS at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Critical CSS at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-148",
    number: "PERF-M8-148",
    title: "Unused CSS",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**Unused CSS** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Unused CSS** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Unused CSS at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Unused CSS at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-149",
    number: "PERF-M8-149",
    title: "CSS splitting",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**CSS splitting** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CSS splitting** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse CSS splitting at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use CSS splitting at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m8-150",
    number: "PERF-M8-150",
    title: "Font loading",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSS Performance",
    expectedAnswer: "**Font loading** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Font loading** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Font loading at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Font loading at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---","# Module 9 – Bundle Optimization","**Questions 151–170**"],
  },
];

export const MOCK_FRONTEND_PERFORMANCE_MODULE8_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Performance',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));