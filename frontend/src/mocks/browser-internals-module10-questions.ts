// Derived from frontend/src/document/Part_6_Browser_Internals_Web_APIs_Master_Handbook.md (Module 10 — Service Workers).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question, Execution/Flow, Advanced Production Example, Performance/Security Notes,
// Best Practices, Advantages/Disadvantages, Common Mistakes/Interview Traps, Senior
// Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "jsbrowser-m10-156",
    number: "JSBROWSER-M10-156",
    title: "Service Worker overview",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "A Service Worker is a worker script associated with an origin and scope that can handle lifecycle events and intercept network requests for controlled clients. It has no direct DOM access; communication with pages uses messaging and other worker-compatible APIs. It is commonly used for offline behavior, caching, background work and push-related features.",
    deepExplanation: "```js\n// page\nif (\"serviceWorker\" in navigator) {\n  const registration =\n    await navigator.serviceWorker.register(\"/sw.js\", {\n      scope: \"/\",\n    });\n\n  console.log(registration.scope);\n}\n```\n\n```js\n// sw.js\nself.addEventListener(\"fetch\", event => {\n  event.respondWith(fetch(event.request));\n});\n```",
    productionExample: "A production PWA registers one versioned worker, caches only appropriate resources, exposes update state to the UI and provides observability for install/activation/fetch failures.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Service Workers enable offline/caching and background capabilities but introduce lifecycle, cache invalidation, debugging and deployment complexity.",
    commonMistakes: ["Thinking a Service Worker can access the DOM.","Treating registration as immediate control of the current page.","Caching everything automatically.","Ignoring update/lifecycle behavior."],
    followUpQuestions: ["How is a Service Worker different from a Web Worker?","What does scope control?","When does a page become controlled?"],
    relatedTopics: ["Service Worker","Lifecycle","Scope","Cache API","FetchEvent","PWA"],
  },
  {
    id: "jsbrowser-m10-157",
    number: "JSBROWSER-M10-157",
    title: "Registration",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "Registration installs a Service Worker script for an origin and defines its scope and registration metadata. The browser may fetch the script, compare it with the existing worker, and create or update a registration. Registration does not mean the current page is immediately controlled by the newly installed worker.",
    deepExplanation: "js\nconst registration =\n  await navigator.serviceWorker.register(\"/sw.js\", {\n    scope: \"/app/\",\n  });\n\nconsole.log(registration.scope);\nregistration.addEventListener(\"updatefound\", () => {\n  console.log(\"New worker found\");\n});\n```",
    productionExample: "A large application registers the worker once from its application shell, listens for `updatefound`, surfaces update status and handles registration failures without blocking normal application startup.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Registration is relatively lightweight but adds lifecycle and scope rules. A narrow scope reduces accidental interception while limiting where the worker can control pages.",
    commonMistakes: ["Registering on every render.","Assuming successful registration means immediate control.","Using an overly broad scope without reason.","Ignoring registration errors."],
    followUpQuestions: ["How is scope calculated?","What happens when the script changes?","How do updatefound and installing relate?"],
    relatedTopics: ["Registration","Scope","ServiceWorkerRegistration","updatefound"],
  },
  {
    id: "jsbrowser-m10-158",
    number: "JSBROWSER-M10-158",
    title: "Installation lifecycle",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "During installation the browser creates the new worker and dispatches its install event. This is the normal place to prepare immutable resources, such as versioned application-shell assets. Calling `event.waitUntil()` extends the lifetime of the install step until the supplied promise settles; a rejection causes installation to fail.",
    deepExplanation: "js\nself.addEventListener(\"install\", event => {\n  event.waitUntil(\n    caches.open(\"app-shell-v2\").then(cache =>\n      cache.addAll([\n        \"/\",\n        \"/app.css\",\n        \"/app.js\",\n      ])\n    )\n  );\n});\n```",
    productionExample: "A production worker precaches only the minimum shell required for offline startup. Large optional assets are cached later so installation is not blocked by unnecessary downloads.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Install-time precaching gives deterministic offline assets but can delay activation if too much work is required or a resource is unavailable.",
    commonMistakes: ["Doing unbounded network work in install.","Forgetting waitUntil.","Failing the entire worker install because an optional asset is unavailable.","Caching user-specific data in a static shell cache."],
    followUpQuestions: ["What happens if install rejects?","Why is waitUntil important?","What should belong in an app-shell cache?"],
    relatedTopics: ["Install event","waitUntil","Precache","Cache API","Service Worker lifecycle"],
  },
  {
    id: "jsbrowser-m10-159",
    number: "JSBROWSER-M10-159",
    title: "Activation lifecycle",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "After successful installation, a Service Worker enters the activation phase. The activate event is used for tasks such as deleting obsolete caches and preparing the new version. `event.waitUntil()` can keep activation alive until cleanup/initialization completes. A newly activated worker may still need `clients.claim()` if immediate control is desired and the architecture allows it.",
    deepExplanation: "js\nself.addEventListener(\"activate\", event => {\n  event.waitUntil(\n    caches.keys().then(names =>\n      Promise.all(\n        names\n          .filter(name => name.startsWith(\"app-\") &&\n                          name !== \"app-v3\")\n          .map(name => caches.delete(name))\n      )\n    )\n  );\n});\n```",
    productionExample: "A deployment activates a new worker and removes old cache versions during activation. The team monitors activation failures because a broken cleanup step can delay the new worker.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Activation cleanup is essential for cache hygiene but can make updates fail if the cleanup code is incorrect. Immediate claim can reduce update delay but changes control timing.",
    commonMistakes: ["Deleting every cache in activate.","Assuming activate instantly controls all tabs.","Calling clients.claim() without understanding existing page behavior.","Leaving old caches indefinitely."],
    followUpQuestions: ["Install vs activate?","What does clients.claim() do?","Why can old pages still use an older worker?"],
    relatedTopics: ["Activate event","clients.claim","Cache cleanup","Lifecycle"],
  },
  {
    id: "jsbrowser-m10-160",
    number: "JSBROWSER-M10-160",
    title: "Cache during install",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "Caching during install means storing resources needed for the worker's initial offline strategy before the worker becomes active. The installation promise should cover required cache population, and cache keys should be versioned so future workers can coexist safely during updates.",
    deepExplanation: "js\nconst SHELL = \"shell-v3\";\n\nself.addEventListener(\"install\", event => {\n  event.waitUntil(\n    caches.open(SHELL).then(cache =>\n      cache.addAll([\n        \"/\",\n        \"/index.html\",\n        \"/styles.css\",\n        \"/main.js\",\n      ])\n    )\n  );\n});\n```",
    productionExample: "A PWA caches only the minimal shell at install, while images and non-critical API data use runtime caching after startup. This keeps installation fast and reduces update failures.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Precache improves offline startup but increases installation bandwidth and can make a deployment fail if a required asset is unavailable.",
    commonMistakes: ["Precache every API response.","Caching mutable user data as if it were an immutable asset.","No cache versioning.","Ignoring failed cache population."],
    followUpQuestions: ["Precache vs runtime cache?","How should hashed assets affect cache strategy?","What happens when addAll fails?"],
    relatedTopics: ["Precache","Install","Cache API","Asset hashing"],
  },
  {
    id: "jsbrowser-m10-161",
    number: "JSBROWSER-M10-161",
    title: "Cache invalidation",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "Cache invalidation is the process of replacing or removing obsolete cached resources so clients do not use incompatible content. A common Service Worker pattern is versioned cache names plus cleanup during activation, while immutable hashed assets can use long-lived caching.",
    deepExplanation: "js\nconst CACHE = \"app-v4\";\n\nself.addEventListener(\"activate\", event => {\n  event.waitUntil(\n    caches.keys().then(keys =>\n      Promise.all(\n        keys\n          .filter(key => key.startsWith(\"app-\") &&\n                         key !== CACHE)\n          .map(key => caches.delete(key))\n      )\n    )\n  );\n});\n```",
    productionExample: "A production release uses content-hashed JS/CSS files and versioned shell caches. Old caches are deleted during activation only after the new worker is ready.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Long-lived immutable caching gives excellent performance, but mutable resources require careful freshness rules. Aggressive cache deletion can hurt offline continuity and rollback safety.",
    commonMistakes: ["Using one cache forever.","Deleting all caches on every fetch.","Assuming cache invalidation is instant across every open client.","Ignoring backward compatibility between old pages and new workers."],
    followUpQuestions: ["Cache versioning vs content hashing?","How do you support safe rollback?","What is the danger of caching HTML too aggressively?"],
    relatedTopics: ["Cache invalidation","Cache versioning","Content hashing","Rollback"],
  },
  {
    id: "jsbrowser-m10-162",
    number: "JSBROWSER-M10-162",
    title: "Fetch interception",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "A Service Worker can receive `fetch` events for requests made by controlled clients within its scope. It can choose to respond from Cache API, forward to the network, combine strategies or synthesize a response. `event.respondWith()` must be called synchronously during the event handling phase.",
    deepExplanation: "js\nself.addEventListener(\"fetch\", event => {\n  if (new URL(event.request.url).origin !== self.location.origin) {\n    return;\n  }\n\n  event.respondWith(\n    caches.match(event.request).then(cached =>\n      cached || fetch(event.request)\n    )\n  );\n});\n```",
    productionExample: "A production PWA applies cache-first to versioned static assets and network-first to dynamic read data. Navigation requests have a separate fallback policy.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Interception provides powerful offline/control behavior but can break networking globally if the handler is wrong. Debugging becomes harder because the Service Worker sits between app and network.",
    commonMistakes: ["Calling respondWith asynchronously after the event is gone.","Caching POST or personalized responses without a design.","Intercepting every request with one giant strategy.","Returning opaque/incorrect responses without understanding their behavior."],
    followUpQuestions: ["Which requests can a Service Worker intercept?","How does respondWith affect the network?","How would you bypass cache for APIs?"],
    relatedTopics: ["FetchEvent","respondWith","Cache API","Network strategies"],
  },
  {
    id: "jsbrowser-m10-163",
    number: "JSBROWSER-M10-163",
    title: "Offline-first strategy",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "Offline-first means the application is designed to continue useful work when network access is unavailable. A robust design separates disposable cached resources from authoritative data, persists pending mutations, exposes synchronization state and defines conflict/retry semantics.",
    deepExplanation: "text\nUI action\n  ↓\nLocal state / IndexedDB\n  ↓\nPending mutation queue\n  ↓\nService Worker / sync path\n  ↓\nServer\n  ↓\nReconcile local state\n```",
    productionExample: "A field-service application lets technicians edit jobs in poor connectivity. Notes and queued changes are stored locally, sync state is visible, retries use idempotency and the server resolves authoritative conflicts.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Offline-first improves resilience but creates distributed consistency and storage complexity. It is often unnecessary for simple read-only marketing sites.",
    commonMistakes: ["Using navigator.onLine as the only truth.","Retrying non-idempotent writes blindly.","No conflict policy.","Treating cached data as permanently valid."],
    followUpQuestions: ["How do you resolve conflicts?","IndexedDB vs Cache API?","What happens when a user logs out with pending mutations?"],
    relatedTopics: ["Offline-first","IndexedDB","Background Sync","Conflict resolution","Idempotency"],
  },
  {
    id: "jsbrowser-m10-164",
    number: "JSBROWSER-M10-164",
    title: "Cache-first strategy",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "Cache-first serves a matching cached response first and falls back to the network when the cache misses. It is well suited to immutable static assets or resources where fast local availability is more valuable than immediate freshness.",
    deepExplanation: "js\nself.addEventListener(\"fetch\", event => {\n  if (event.request.destination === \"script\") {\n    event.respondWith(\n      caches.match(event.request).then(cached =>\n        cached || fetch(event.request)\n      )\n    );\n  }\n});\n```",
    productionExample: "A PWA caches content-hashed JS/CSS bundles cache-first. Because filenames change when content changes, stale asset risk is minimized.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Cache-first gives very low latency and offline capability, but freshness is weaker unless versioning/invalidation is strong.",
    commonMistakes: ["Using cache-first for live account data.","Caching HTML forever without an update plan.","No offline miss handling."],
    followUpQuestions: ["Cache-first vs stale-while-revalidate?","When is cache-first unsafe?","How do hashed assets make cache-first effective?"],
    relatedTopics: ["Cache-first","Immutable assets","Caching","PWA"],
  },
  {
    id: "jsbrowser-m10-165",
    number: "JSBROWSER-M10-165",
    title: "Network-first strategy",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "Network-first attempts the network before falling back to a cached response. It suits frequently changing data where freshness matters, while still providing resilience when the network fails.",
    deepExplanation: "js\nevent.respondWith(\n  fetch(event.request)\n    .then(response => {\n      const copy = response.clone();\n      caches.open(\"api-v1\")\n        .then(cache => cache.put(event.request, copy));\n      return response;\n    })\n    .catch(() => caches.match(event.request))\n);\n```",
    productionExample: "A news/PWA feed uses network-first for articles. A fresh response updates the cache; if the network is unavailable, the last cached version is shown with an explicit offline state.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Network-first prioritizes freshness but can feel slow offline or on poor networks. A cache fallback improves resilience but may show stale data.",
    commonMistakes: ["Using network-first with no timeout/abort strategy.","Caching personalized responses broadly.","Treating cached fallback as current truth."],
    followUpQuestions: ["Network-first vs stale-while-revalidate?","How do you avoid slow offline startup?","How do you handle private API responses?"],
    relatedTopics: ["Network-first","Caching","Offline fallback","Fetch"],
  },
  {
    id: "jsbrowser-m10-166",
    number: "JSBROWSER-M10-166",
    title: "Stale-while-revalidate",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "Stale-while-revalidate returns cached content immediately when available and refreshes it from the network in the background. It is a good fit for data where slightly stale content is acceptable and fast response matters.",
    deepExplanation: "js\nconst cached = await caches.match(request);\n\nconst update = fetch(request).then(response => {\n  const copy = response.clone();\n  return caches.open(\"runtime-v1\").then(cache =>\n    cache.put(request, copy)\n  );\n});\n\nif (cached) {\n  event.waitUntil(update);\n  event.respondWith(cached);\n} else {\n  event.respondWith(update);\n}\n```",
    productionExample: "A documentation portal serves cached article content instantly, then revalidates quietly. A visible refresh indicator appears only when new data materially changes the page.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Users get fast responses, but the first response can be stale and cache updates can fail silently unless observed.",
    commonMistakes: ["Using stale data for balances/payment status.","Calling every cached resource SWR without freshness requirements.","No monitoring of refresh failures."],
    followUpQuestions: ["SWR vs HTTP Cache-Control stale-while-revalidate?","How would you surface stale state to users?","Which resources should never be stale?"],
    relatedTopics: ["Stale-while-revalidate","Cache API","Freshness","Offline"],
  },
  {
    id: "jsbrowser-m10-167",
    number: "JSBROWSER-M10-167",
    title: "Background Sync",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "Background Sync lets a Service Worker ask the browser to retry certain work when connectivity becomes available, where supported. It is useful for queued, retryable operations rather than guaranteed immediate execution. The application must persist the work and make retries safe.",
    deepExplanation: "js\nself.addEventListener(\"sync\", event => {\n  if (event.tag === \"sync-notes\") {\n    event.waitUntil(flushPendingNotes());\n  }\n});\n```\n\nThe pending queue itself should be durable, typically in IndexedDB; the sync event is only the trigger to attempt work.",
    productionExample: "A notes application stores unsent mutations in IndexedDB and registers a sync tag. When connectivity permits, the worker replays idempotent operations and removes them only after server confirmation.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Background Sync can improve reliability without foreground polling, but browser support/conditions vary and execution is controlled by the browser.",
    commonMistakes: ["Assuming sync always runs immediately.","Keeping the queue only in memory.","Retrying payments without idempotency.","Deleting queued work before server success."],
    followUpQuestions: ["One-off Background Sync vs periodic background sync?","How do you make replay idempotent?","What if the browser never grants sync time?"],
    relatedTopics: ["Background Sync","IndexedDB","Offline","Retries","Idempotency"],
  },
  {
    id: "jsbrowser-m10-168",
    number: "JSBROWSER-M10-168",
    title: "Push notifications",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "Web Push lets a service worker receive push events even when the associated page is not open, subject to browser/platform policies and permission. The browser delivers a push event to the worker, which can show a notification or update stored state.",
    deepExplanation: "js\nself.addEventListener(\"push\", event => {\n  const data = event.data?.json() ?? {};\n\n  event.waitUntil(\n    self.registration.showNotification(\n      data.title ?? \"Update\",\n      { body: data.body ?? \"\" }\n    )\n  );\n});\n```",
    productionExample: "A collaboration app requests notification permission only after the user opts in, stores a push subscription securely, and handles revocation/invalid subscriptions on the server.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Push can re-engage users but creates privacy, permission, battery and notification-fatigue concerns. It also depends on browser/platform infrastructure.",
    commonMistakes: ["Sending notification permission prompts immediately on load.","Putting sensitive information into notifications.","Ignoring subscription expiration or user opt-out."],
    followUpQuestions: ["Push API vs Notification API?","How do subscriptions work?","How do notification clicks route the user?"],
    relatedTopics: ["Push API","Notifications","Service Worker","Permissions"],
  },
  {
    id: "jsbrowser-m10-169",
    number: "JSBROWSER-M10-169",
    title: "Service Worker security",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "Service Worker security depends on secure context requirements, origin and scope restrictions, careful caching, message validation and server-side authorization. A Service Worker has powerful network/storage capabilities within its scope, so a compromised or malicious script can have broad impact on controlled content.",
    deepExplanation: "js\nself.addEventListener(\"message\", event => {\n  if (!event.source) return;\n\n  const data = event.data;\n  if (!data || typeof data.type !== \"string\") return;\n\n  // Validate allowed message types before acting.\n});\n```",
    productionExample: "A company protects worker deployment through HTTPS, strict CSP/security review, controlled build pipelines and careful message/caching rules. Sensitive API responses are never cached by broad runtime rules.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Service Workers provide powerful control but increase blast radius: a bad worker can break network behavior for many routes until users receive a corrected version.",
    commonMistakes: ["Treating the Service Worker as harmless frontend code.","Caching authenticated responses accidentally.","Trusting postMessage data.","Ignoring CSP/deployment integrity."],
    followUpQuestions: ["How can a worker compromise an origin's UX?","How do you safely roll back a bad worker?","What should never be cached?"],
    relatedTopics: ["Service Worker security","HTTPS","CSP","Origin","Caching","postMessage"],
  },
  {
    id: "jsbrowser-m10-170",
    number: "JSBROWSER-M10-170",
    title: "Service Worker performance",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "Service Workers",
    expectedAnswer: "Service Worker performance depends on startup cost, script execution, cache lookup, network fallback, cloning/serialization, storage access and the amount of work done for each fetch event. The worker is not a free network proxy; heavy logic can add latency or battery/storage cost.",
    deepExplanation: "js\nself.addEventListener(\"fetch\", event => {\n  const url = new URL(event.request.url);\n\n  if (url.pathname.startsWith(\"/assets/\")) {\n    event.respondWith(\n      caches.match(event.request).then(\n        cached => cached || fetch(event.request)\n      )\n    );\n  }\n});\n```\n\nKeeping routing logic narrow avoids executing expensive logic for every request.",
    productionExample: "A production PWA measures fetch-event overhead and only intercepts routes that benefit from caching. Static assets use cache-first, dynamic APIs use a separate strategy and expensive parsing stays out of the fetch handler.",
    bestPractices: ["Treat the Service Worker as application infrastructure, not ordinary UI code.","Version caches and define explicit update/rollback behavior.","Keep fetch interception narrowly scoped.","Use waitUntil/respondWith correctly for lifecycle/event work.","Test offline, update, failure and recovery scenarios."],
    tradeOffs: "Service Worker caching can dramatically improve repeat navigation but adds script execution/storage overhead and debugging complexity.",
    commonMistakes: ["Intercepting every request with expensive code.","Cloning huge responses unnecessarily.","Doing CPU-heavy JSON processing in every fetch event.","Ignoring worker update costs."],
    followUpQuestions: ["How do you measure Service Worker overhead?","When should you bypass the worker?","How does response cloning affect memory?"],
    relatedTopics: ["Service Worker","FetchEvent","Cache API","Performance","Response.clone"],
  },

];

export const MOCK_BROWSER_INTERNALS_MODULE10_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Browser/Web APIs',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));