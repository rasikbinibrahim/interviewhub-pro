// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 2 — JavaScript Testing).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m2-1",
    number: "TESTQA-M2-1",
    title: "Pure Functions",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Pure Functions** is best tested as a deterministic input/output contract with no dependency on time, DOM, network, or shared mutable state.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Pure Functions** is best tested as a deterministic input/output contract with no dependency on time, DOM, network, or shared mutable state.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\nfunction normalizeEmail(value: string): string {\n  let result = \"\";\n  for (let i = 0; i < value.length; i += 1) {\n    result += value[i].toLowerCase();\n  }\n  return result.trim();\n}\n\nconst actual = normalizeEmail(\"  User@Example.com  \");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst actual = \"  User@Example.com  \".trim().toLowerCase();\n```\n\nStep 5 — Production example:\nUse pure-function tests for deterministic domain transformations, validation, parsing, sorting, and calculations.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use pure-function tests for deterministic domain transformations, validation, parsing, sorting, and calculations.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-2",
    number: "TESTQA-M2-2",
    title: "Side Effects",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Side Effects** should be tested around the observable side effect and its cleanup, without asserting directly on hook internals.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Side Effects** should be tested around the observable side effect and its cleanup, without asserting directly on hook internals.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest the observable side effect and cleanup through the consuming component; avoid testing implementation internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test the observable side effect and cleanup through the consuming component; avoid testing implementation internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-3",
    number: "TESTQA-M2-3",
    title: "Mock Functions",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Mock Functions** should replace a controlled dependency boundary so the test can verify interaction or failure behavior deterministically.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Mock Functions** should replace a controlled dependency boundary so the test can verify interaction or failure behavior deterministically.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CallRecord { name: string; count: number; }\n\nfunction recordCall(records: readonly CallRecord[], name: string): CallRecord[] {\n  const next: CallRecord[] = [];\n  let found = false;\n\n  for (let i = 0; i < records.length; i += 1) {\n    const record = records[i];\n\n    if (record.name === name) {\n      next.push({ name, count: record.count + 1 });\n      found = true;\n    } else {\n      next.push(record);\n    }\n  }\n\n  if (!found) next.push({ name, count: 1 });\n  return next;\n}\n\nconst calls = recordCall([], \"fetchUser\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst calls = new Map<string, number>();\n\ncalls.set(\"fetchUser\", (calls.get(\"fetchUser\") ?? 0) + 1);\n\nconst count = calls.get(\"fetchUser\") ?? 0;\n```\n\nStep 5 — Production example:\nUse a mock at a stable dependency boundary when deterministic interaction or failure behavior is required.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use a mock at a stable dependency boundary when deterministic interaction or failure behavior is required.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-4",
    number: "TESTQA-M2-4",
    title: "Spy Functions",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Spy Functions** is useful when the real implementation should run but the test also needs evidence about a call, argument, or interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Spy Functions** is useful when the real implementation should run but the test also needs evidence about a call, argument, or interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CallRecord { name: string; count: number; }\n\nfunction recordCall(records: readonly CallRecord[], name: string): CallRecord[] {\n  const next: CallRecord[] = [];\n  let found = false;\n\n  for (let i = 0; i < records.length; i += 1) {\n    const record = records[i];\n\n    if (record.name === name) {\n      next.push({ name, count: record.count + 1 });\n      found = true;\n    } else {\n      next.push(record);\n    }\n  }\n\n  if (!found) next.push({ name, count: 1 });\n  return next;\n}\n\nconst calls = recordCall([], \"fetchUser\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst calls = new Map<string, number>();\n\ncalls.set(\"fetchUser\", (calls.get(\"fetchUser\") ?? 0) + 1);\n\nconst count = calls.get(\"fetchUser\") ?? 0;\n```\n\nStep 5 — Production example:\nUse a spy when the real implementation should execute and the test also needs interaction evidence.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use a spy when the real implementation should execute and the test also needs interaction evidence.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-5",
    number: "TESTQA-M2-5",
    title: "Stubs and Fakes",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Stubs and Fakes** provides controlled dependency behavior so failures and external conditions can be reproduced reliably.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Stubs and Fakes** provides controlled dependency behavior so failures and external conditions can be reproduced reliably.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CallRecord { name: string; count: number; }\n\nfunction recordCall(records: readonly CallRecord[], name: string): CallRecord[] {\n  const next: CallRecord[] = [];\n  let found = false;\n\n  for (let i = 0; i < records.length; i += 1) {\n    const record = records[i];\n\n    if (record.name === name) {\n      next.push({ name, count: record.count + 1 });\n      found = true;\n    } else {\n      next.push(record);\n    }\n  }\n\n  if (!found) next.push({ name, count: 1 });\n  return next;\n}\n\nconst calls = recordCall([], \"fetchUser\");\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst calls = new Map<string, number>();\n\ncalls.set(\"fetchUser\", (calls.get(\"fetchUser\") ?? 0) + 1);\n\nconst count = calls.get(\"fetchUser\") ?? 0;\n```\n\nStep 5 — Production example:\nUse a stub/fake to control external behavior such as network, time, storage, or dependency failure.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use a stub/fake to control external behavior such as network, time, storage, or dependency failure.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-6",
    number: "TESTQA-M2-6",
    title: "Fake Timers",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Fake Timers** replaces real time with deterministic control so delayed behavior can be tested without real waiting.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Fake Timers** replaces real time with deterministic control so delayed behavior can be tested without real waiting.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TimerState { elapsed: number; running: boolean; }\n\nfunction tick(state: TimerState): TimerState {\n  if (!state.running) return state;\n\n  return { elapsed: state.elapsed + 1, running: true };\n}\n\nconst next = tick({ elapsed: 0, running: true });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst timer = { elapsed: 0, running: true };\n\nconst next = timer.running\n  ? { ...timer, elapsed: timer.elapsed + 1 }\n  : timer;\n```\n\nStep 5 — Production example:\nUse controlled timers for debounce, polling, expiration, and scheduled UI behavior, and restore timer state afterward.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use controlled timers for debounce, polling, expiration, and scheduled UI behavior, and restore timer state afterward.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-7",
    number: "TESTQA-M2-7",
    title: "Async Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Async Testing** should await observable completion and cover loading, success, error, and cleanup states without arbitrary delays.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Async Testing** should await observable completion and cover loading, success, error, and cleanup states without arbitrary delays.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ntype Result<T> =\n  | { ok: true; value: T }\n  | { ok: false; message: string };\n\nfunction normalizeError(error: unknown): Result<never> {\n  if (error instanceof Error) {\n    return { ok: false, message: error.message };\n  }\n\n  return { ok: false, message: \"Unknown error\" };\n}\n\nconst result = normalizeError(new Error(\"request failed\"));\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst error = new Error(\"request failed\");\n\nconst result = {\n  ok: false as const,\n  message: error.message,\n};\n```\n\nStep 5 — Production example:\nAwait observable completion and cover loading, success, error, and cleanup paths without arbitrary waits.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Await observable completion and cover loading, success, error, and cleanup paths without arbitrary waits.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-8",
    number: "TESTQA-M2-8",
    title: "Callback Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Callback Testing** should verify callback invocation, ordering, arguments, and completion with an explicit signal.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Callback Testing** should verify callback invocation, ordering, arguments, and completion with an explicit signal.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ntype Result<T> =\n  | { ok: true; value: T }\n  | { ok: false; message: string };\n\nfunction normalizeError(error: unknown): Result<never> {\n  if (error instanceof Error) {\n    return { ok: false, message: error.message };\n  }\n\n  return { ok: false, message: \"Unknown error\" };\n}\n\nconst result = normalizeError(new Error(\"request failed\"));\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst error = new Error(\"request failed\");\n\nconst result = {\n  ok: false as const,\n  message: error.message,\n};\n```\n\nStep 5 — Production example:\nAssert callback arguments, ordering, and completion explicitly.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Assert callback arguments, ordering, and completion explicitly.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-9",
    number: "TESTQA-M2-9",
    title: "Promise Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Promise Testing** should cover fulfilled and rejected promises and await the assertion path so tests cannot finish early.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Promise Testing** should cover fulfilled and rejected promises and await the assertion path so tests cannot finish early.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ntype Result<T> =\n  | { ok: true; value: T }\n  | { ok: false; message: string };\n\nfunction normalizeError(error: unknown): Result<never> {\n  if (error instanceof Error) {\n    return { ok: false, message: error.message };\n  }\n\n  return { ok: false, message: \"Unknown error\" };\n}\n\nconst result = normalizeError(new Error(\"request failed\"));\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst error = new Error(\"request failed\");\n\nconst result = {\n  ok: false as const,\n  message: error.message,\n};\n```\n\nStep 5 — Production example:\nTest both fulfillment and rejection and always await the assertion path.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test both fulfillment and rejection and always await the assertion path.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-10",
    number: "TESTQA-M2-10",
    title: "Exception Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Exception Testing** should assert the public error contract rather than incidental stack or implementation details.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Exception Testing** should assert the public error contract rather than incidental stack or implementation details.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ntype Result<T> =\n  | { ok: true; value: T }\n  | { ok: false; message: string };\n\nfunction normalizeError(error: unknown): Result<never> {\n  if (error instanceof Error) {\n    return { ok: false, message: error.message };\n  }\n\n  return { ok: false, message: \"Unknown error\" };\n}\n\nconst result = normalizeError(new Error(\"request failed\"));\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst error = new Error(\"request failed\");\n\nconst result = {\n  ok: false as const,\n  message: error.message,\n};\n```\n\nStep 5 — Production example:\nAssert the public error contract rather than incidental stack details.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Assert the public error contract rather than incidental stack details.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-11",
    number: "TESTQA-M2-11",
    title: "Parameterized Tests",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Parameterized Tests** should express one behavior over a representative input matrix while keeping every case readable.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Parameterized Tests** should express one behavior over a representative input matrix while keeping every case readable.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface Case { input: number; expected: number; }\n\nfunction double(value: number): number {\n  return value * 2;\n}\n\nconst cases: readonly Case[] = [\n  { input: 0, expected: 0 },\n  { input: 2, expected: 4 },\n];\n\nconst passed: boolean[] = [];\n\nfor (let i = 0; i < cases.length; i += 1) {\n  passed.push(double(cases[i].input) === cases[i].expected);\n}\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst cases = [[0, 0], [2, 4]] as const;\n\nconst passed = cases.map(\n  ([input, expected]) => input * 2 === expected,\n);\n```\n\nStep 5 — Production example:\nUse a readable case matrix for related inputs and make each case independently diagnosable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use a readable case matrix for related inputs and make each case independently diagnosable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-12",
    number: "TESTQA-M2-12",
    title: "Boundary and Edge-case Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Boundary and Edge-case Testing** should cover empty, minimum, maximum, duplicate, invalid, boundary, and recovery conditions.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Boundary and Edge-case Testing** should cover empty, minimum, maximum, duplicate, invalid, boundary, and recovery conditions.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nKeep high-value boundary tests close to the behavior they protect.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep high-value boundary tests close to the behavior they protect.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-13",
    number: "TESTQA-M2-13",
    title: "Performance Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Performance Testing** should define a measurable budget and use stable data; noisy timing work belongs in a dedicated benchmark layer when needed.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Performance Testing** should define a measurable budget and use stable data; noisy timing work belongs in a dedicated benchmark layer when needed.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse a dedicated benchmark/performance layer for noisy timing work rather than slowing normal unit tests.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use a dedicated benchmark/performance layer for noisy timing work rather than slowing normal unit tests.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-14",
    number: "TESTQA-M2-14",
    title: "Coverage and Debugging",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**Coverage and Debugging** should use coverage as a map of untested branches and risky paths, not as a quality target by itself.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Coverage and Debugging** should use coverage as a map of untested branches and risky paths, not as a quality target by itself.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse coverage to find untested branches and risky paths, not as the only quality metric.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use coverage to find untested branches and risky paths, not as the only quality metric.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-15",
    number: "TESTQA-M2-15",
    title: "JavaScript Testing Architecture",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**JavaScript Testing Architecture** should define test ownership, fixtures, mocking policy, environments, reporting, and CI responsibilities.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**JavaScript Testing Architecture** should define test ownership, fixtures, mocking policy, environments, reporting, and CI responsibilities.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nDefine fixtures, mocking, environments, cleanup, reports, and CI rules at repository level.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Define fixtures, mocking, environments, cleanup, reports, and CI rules at repository level.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
  {
    id: "testqa-m2-16",
    number: "TESTQA-M2-16",
    title: "JavaScript Testing — Module Review",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "JavaScript Testing",
    expectedAnswer: "**JavaScript Testing — Module Review** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**JavaScript Testing — Module Review** should be tested by defining the observable contract, the risk, the appropriate test level, and the failure evidence. Testing quality comes from meaningful behavior coverage and reliable feedback.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface TestCase { name: string; enabled: boolean; }\n\nfunction enabledTests(tests: readonly TestCase[]): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < tests.length; i += 1) {\n    if (tests[i].enabled) result.push(tests[i].name);\n  }\n\n  return result;\n}\n\nconst selected = enabledTests([\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst tests = [\n  { name: \"happy-path\", enabled: true },\n  { name: \"edge-case\", enabled: true },\n];\n\nconst selected = tests\n  .filter((test) => test.enabled)\n  .map((test) => test.name);\n```\n\nStep 5 — Production example:\nUse the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use the lowest-cost test level that protects the highest-risk behavior and keeps the feedback loop reliable.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["JavaScript Testing"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE2_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));