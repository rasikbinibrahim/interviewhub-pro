// Derived from frontend/src/document/DevOps_Cloud_CICD_Frontend_Infrastructure_Master_Handbook.md (Module 4 — Kubernetes).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "devops-m4-1",
    number: "DEVOPS-M4-1",
    title: "Pods",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Pods is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Pods solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-2",
    number: "DEVOPS-M4-2",
    title: "ReplicaSets",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "ReplicaSets is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does ReplicaSets solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-3",
    number: "DEVOPS-M4-3",
    title: "Deployments",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Deployments is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Deployments solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-4",
    number: "DEVOPS-M4-4",
    title: "DaemonSets",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "DaemonSets is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does DaemonSets solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-5",
    number: "DEVOPS-M4-5",
    title: "StatefulSets",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "StatefulSets is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does StatefulSets solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-6",
    number: "DEVOPS-M4-6",
    title: "Namespaces",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Namespaces is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Namespaces solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-7",
    number: "DEVOPS-M4-7",
    title: "Services",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Services is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Services solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-8",
    number: "DEVOPS-M4-8",
    title: "Ingress",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Ingress is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Ingress solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-9",
    number: "DEVOPS-M4-9",
    title: "ConfigMaps",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "ConfigMaps is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does ConfigMaps solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-10",
    number: "DEVOPS-M4-10",
    title: "Secrets",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Secrets is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Secrets solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-11",
    number: "DEVOPS-M4-11",
    title: "Volumes",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Volumes is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Volumes solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-12",
    number: "DEVOPS-M4-12",
    title: "Autoscaling",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Autoscaling is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Autoscaling solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-13",
    number: "DEVOPS-M4-13",
    title: "Health Checks",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Health Checks is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Health Checks solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-14",
    number: "DEVOPS-M4-14",
    title: "Liveness",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Liveness is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Liveness solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-15",
    number: "DEVOPS-M4-15",
    title: "Readiness",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Readiness is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Readiness solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-16",
    number: "DEVOPS-M4-16",
    title: "Jobs",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Jobs is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Jobs solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-17",
    number: "DEVOPS-M4-17",
    title: "CronJobs",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "CronJobs is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does CronJobs solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-18",
    number: "DEVOPS-M4-18",
    title: "Networking",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Networking is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Networking solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-19",
    number: "DEVOPS-M4-19",
    title: "Service Mesh Overview",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Service Mesh Overview is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Service Mesh Overview solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
  {
    id: "devops-m4-20",
    number: "DEVOPS-M4-20",
    title: "Production Architecture",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Production Architecture is a mechanism or practice used to make software delivery, infrastructure, operations, security or reliability more predictable and scalable.",
    deepExplanation: "Example:\n\n```ts\nexport interface RuntimeConfig {\n  apiBaseUrl: string;\n  environment: \"development\" | \"staging\" | \"production\";\n}\n\nexport function validateConfig(input: Partial<RuntimeConfig>): RuntimeConfig {\n  if (!input.apiBaseUrl) throw new Error(\"apiBaseUrl is required\");\n\n  const environment = input.environment ?? \"development\";\n  if (![\"development\", \"staging\", \"production\"].includes(environment)) {\n    throw new Error(\"Invalid environment\");\n  }\n\n  return { apiBaseUrl: input.apiBaseUrl, environment };\n}\n```\n\nAdditional example:\n\n```ts\nexport function getRuntimeConfig(config) {\n  if (!config || typeof config !== \"object\") {\n    throw new TypeError(\"Invalid runtime configuration\");\n  }\n\n  return Object.freeze({\n    apiBaseUrl: String(config.apiBaseUrl ?? \"\"),\n    environment: String(config.environment ?? \"development\"),\n  });\n}\n```\n\nWhy it exists: It addresses recurring production problems such as manual work, configuration drift, slow feedback, deployment risk, inconsistent environments, outages or uncontrolled cost.\n\nInternal working: A change moves through automated validation, artifact creation, controlled promotion and runtime verification.",
    productionExample: "Use versioned immutable artifacts, automated verification, environment-specific configuration, health validation and an explicit rollback path.",
    bestPractices: ["Automate repeatable work.","Version infrastructure.","Prefer immutable artifacts.","Make deployments observable.","Use least privilege.","Separate runtime and build-time configuration.","Test rollback.","Keep pipelines fast.","Track cost.","Document runbooks."],
    tradeOffs: "Advantages: Repeatability; Automation; Faster feedback; Safer releases; Better observability; Scalable operations. Disadvantages: Initial complexity; Platform maintenance; Learning curve; Potential cloud cost; Configuration mistakes can propagate quickly.",
    commonMistakes: ["Treating DevOps as only CI/CD","Delaying security until production","Using mutable `latest` artifacts","Ignoring CDN invalidation","Not testing recovery","Ignoring operational ownership"],
    followUpQuestions: ["What problem does Production Architecture solve?","How does it work internally?","When should you use it?","When should you avoid it?","What are the failure modes?","How does it affect cost?","How do you secure it?","How do you monitor it?","How do you recover from failure?","How does it affect frontend delivery?"],
    relatedTopics: ["Kubernetes"],
  },
];

export const MOCK_DEVOPS_MODULE4_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'DevOps/Cloud',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
