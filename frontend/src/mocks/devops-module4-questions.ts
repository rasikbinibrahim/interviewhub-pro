// Derived from frontend/src/document/DevOps_Cloud_CICD_Frontend_Infrastructure_Master_Handbook.md (Module 4 — Kubernetes).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "devops-m4-1",
    number: "DEVOPS-M4-1",
    title: "Pods",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A Pod is Kubernetes' smallest deployable unit and groups one or more containers that share a network namespace, IP address, ports and optionally volumes. Containers in the same Pod are co-located and should be tightly coupled rather than treated as independent replicas.",
    deepExplanation: "```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: frontend\nspec:\n  containers:\n    - name: web\n      image: registry.example.com/frontend@sha256:...\n      ports:\n        - containerPort: 80\n```\n\nPods are normally created and managed by higher-level controllers such as Deployments or StatefulSets rather than manually. A Pod is ephemeral: if it is replaced, its identity/IP can change, which is why Services provide stable discovery.",
    productionExample: "A frontend Deployment manages multiple Pods running the same image. Each Pod has readiness/liveness probes and resource requests so the scheduler and Service can make safe placement/routing decisions.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Pods provide a simple shared execution boundary but are ephemeral and not a scaling abstraction by themselves. Extra sidecars also increase resource usage and operational complexity.",
    commonMistakes: ["Treating a Pod as a VM.","Putting unrelated services in one Pod.","Using Pod IPs as stable service endpoints.","Expecting Pod identity to persist across replacement."],
    followUpQuestions: ["Pod vs container?","Why use a Deployment instead of a Pod?","How do containers communicate inside a Pod?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-2",
    number: "DEVOPS-M4-2",
    title: "ReplicaSets",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A ReplicaSet maintains a specified number of Pods matching a label selector. It creates replacement Pods when matching Pods disappear, providing replica-level availability. Deployments normally manage ReplicaSets rather than teams creating ReplicaSets directly for application rollouts.",
    deepExplanation: "```yaml\napiVersion: apps/v1\nkind: ReplicaSet\nmetadata:\n  name: frontend-rs\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: frontend\n  template:\n    metadata:\n      labels:\n        app: frontend\n    spec:\n      containers:\n        - name: web\n          image: nginx:1.27\n```\n\nWhen a Pod is deleted, the ReplicaSet controller reconciles the desired replica count and creates a replacement.",
    productionExample: "A Deployment creates a new ReplicaSet during an image rollout while keeping the previous ReplicaSet available for rollback. Engineers usually interact with the Deployment, not the ReplicaSet directly.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "ReplicaSets give reliable replica reconciliation, but they intentionally do not provide the full rollout/history functionality of Deployments.",
    commonMistakes: ["Editing Deployment-managed ReplicaSets manually.","Using overlapping selectors.","Assuming a ReplicaSet guarantees application readiness rather than only Pod count."],
    followUpQuestions: ["ReplicaSet vs Deployment?","How does reconciliation work?","What happens during a rollout?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-3",
    number: "DEVOPS-M4-3",
    title: "Deployments",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A Deployment manages ReplicaSets and provides declarative rollout, scaling and rollback for stateless workloads. It updates the desired Pod template and Kubernetes gradually reconciles the new version.",
    deepExplanation: "```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: frontend\nspec:\n  replicas: 3\n  strategy:\n    type: RollingUpdate\n  selector:\n    matchLabels:\n      app: frontend\n  template:\n    metadata:\n      labels:\n        app: frontend\n    spec:\n      containers:\n        - name: web\n          image: registry.example.com/frontend:2026.08.20\n          readinessProbe:\n            httpGet:\n              path: /health\n              port: 80\n```\n\nUseful commands:\n\n```bash\nkubectl apply -f deployment.yaml\nkubectl rollout status deployment/frontend\nkubectl rollout history deployment/frontend\nkubectl rollout undo deployment/frontend\n```",
    productionExample: "A frontend Deployment rolls out a new image using a rolling update. Readiness probes prevent traffic from reaching Pods before they can serve requests, and rollout history provides a rollback path.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Rolling updates reduce downtime but can temporarily run multiple versions and consume extra capacity. Poor readiness probes can make a rollout look healthy when it is not.",
    commonMistakes: ["No readiness probe.","Using `latest` image tags.","Changing Pods manually instead of Deployment spec.","Not planning rollback capacity."],
    followUpQuestions: ["Deployment vs StatefulSet?","How does rolling update work?","How do maxSurge and maxUnavailable affect rollout?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-4",
    number: "DEVOPS-M4-4",
    title: "DaemonSets",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A DaemonSet ensures that a Pod runs on each eligible node, or on a subset selected by node labels/affinity. It is suitable for node-level agents such as logging, monitoring or networking components.",
    deepExplanation: "```yaml\napiVersion: apps/v1\nkind: DaemonSet\nmetadata:\n  name: node-exporter\nspec:\n  selector:\n    matchLabels:\n      app: node-exporter\n  template:\n    metadata:\n      labels:\n        app: node-exporter\n    spec:\n      containers:\n        - name: exporter\n          image: prom/node-exporter:v1.8.2\n          ports:\n            - containerPort: 9100\n```\n\nWhen a new eligible node joins, the DaemonSet controller schedules the Pod there automatically.",
    productionExample: "A platform runs a monitoring agent as a DaemonSet so every worker node exposes node-level metrics without manually creating one Pod per node.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "DaemonSets provide consistent node coverage but scale with node count and can consume significant cluster-wide resources.",
    commonMistakes: ["Using DaemonSet for ordinary application replicas.","Forgetting special system nodes may need tolerations.","Ignoring node resource impact."],
    followUpQuestions: ["DaemonSet vs Deployment?","How do taints/tolerations affect it?","How does a DaemonSet react to new nodes?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-5",
    number: "DEVOPS-M4-5",
    title: "StatefulSets",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A StatefulSet manages stateful workloads that need stable network identities, predictable Pod ordering and persistent storage associations. Unlike Deployments, StatefulSet Pod identities are ordinal and stable across rescheduling, subject to the storage and cluster setup.",
    deepExplanation: "```yaml\napiVersion: apps/v1\nkind: StatefulSet\nmetadata:\n  name: redis\nspec:\n  serviceName: redis\n  replicas: 3\n  selector:\n    matchLabels:\n      app: redis\n  template:\n    metadata:\n      labels:\n        app: redis\n    spec:\n      containers:\n        - name: redis\n          image: redis:7.2\n          volumeMounts:\n            - name: data\n              mountPath: /data\n  volumeClaimTemplates:\n    - metadata:\n        name: data\n      spec:\n        accessModes: [\"ReadWriteOnce\"]\n        resources:\n          requests:\n            storage: 10Gi\n```",
    productionExample: "A stateful database cluster uses StatefulSet when its topology and persistent identity fit Kubernetes semantics. For many production databases, a managed database service may still be simpler and safer.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "StatefulSets provide stable identity and storage but make operations harder than stateless Deployments. Data durability, backup and failover remain application/database concerns.",
    commonMistakes: ["Using StatefulSet just because an app writes files.","Assuming Kubernetes alone makes databases highly available.","Skipping backups."],
    followUpQuestions: ["StatefulSet vs Deployment?","How do volumeClaimTemplates work?","What happens when a StatefulSet Pod is recreated?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-6",
    number: "DEVOPS-M4-6",
    title: "Namespaces",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A Namespace scopes namespaced Kubernetes resources and provides an organizational boundary for teams, environments or applications. Namespaces can be combined with RBAC, quotas and network policies, but they are not a complete security boundary by themselves.",
    deepExplanation: "```yaml\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: frontend-prod\n---\napiVersion: v1\nkind: ResourceQuota\nmetadata:\n  name: compute-quota\n  namespace: frontend-prod\nspec:\n  hard:\n    requests.cpu: \"4\"\n    requests.memory: 8Gi\n```\n\nResources are then created explicitly in the namespace:\n\n```bash\nkubectl -n frontend-prod get pods\n```",
    productionExample: "An enterprise cluster isolates staging and production into separate namespaces, with per-namespace RBAC, quotas and network policies. Stronger isolation between tenants may still require separate clusters/accounts.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Namespaces are lightweight and useful for organization, but they share the cluster control plane and kernel, so they are weaker isolation than separate clusters or accounts.",
    commonMistakes: ["Treating namespace as a complete security boundary.","Forgetting default namespaces in commands.","No quotas, allowing one team to exhaust shared resources."],
    followUpQuestions: ["Namespace vs cluster?","How do quotas work?","How does RBAC interact with namespaces?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-7",
    number: "DEVOPS-M4-7",
    title: "Services",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A Kubernetes Service provides a stable virtual endpoint and selects Pods by labels. It decouples clients from ephemeral Pod IPs and can expose applications internally or externally depending on the Service type.",
    deepExplanation: "```yaml\napiVersion: v1\nkind: Service\nmetadata:\n  name: frontend\nspec:\n  selector:\n    app: frontend\n  ports:\n    - port: 80\n      targetPort: 80\n  type: ClusterIP\n```\n\nClients can call:\n\n```text\nhttp://frontend:80\n```\n\nThe Service resolves to a stable virtual IP/DNS name while the EndpointSlice tracks matching ready endpoints.",
    productionExample: "An API Service selects frontend/API Pods through labels. Pods can be recreated or rescheduled without changing the DNS endpoint used by other services.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Services simplify service discovery and traffic distribution, but introduce another networking layer that must be understood when debugging DNS, kube-proxy/eBPF or ingress paths.",
    commonMistakes: ["Using Pod IPs directly.","Selector labels that match the wrong Pods.","Assuming Service means external internet exposure."],
    followUpQuestions: ["ClusterIP vs NodePort vs LoadBalancer?","How are endpoints selected?","What happens when a Pod is not ready?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-8",
    number: "DEVOPS-M4-8",
    title: "Ingress",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Ingress is a Kubernetes API for describing HTTP/HTTPS routing rules from outside the cluster to Services. It requires an Ingress controller or equivalent implementation to actually provision/load-balance traffic.",
    deepExplanation: "```yaml\napiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: frontend\nspec:\n  ingressClassName: nginx\n  rules:\n    - host: app.example.com\n      http:\n        paths:\n          - path: /\n            pathType: Prefix\n            backend:\n              service:\n                name: frontend\n                port:\n                  number: 80\n```",
    productionExample: "A cluster uses an ingress controller to terminate TLS and route `/api` to the API Service and `/` to the frontend Service under the same host.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Ingress centralizes edge routing but couples behavior to the chosen controller. Advanced routing may be better modeled by Gateway API or a managed load balancer.",
    commonMistakes: ["Assuming an Ingress object routes traffic without a controller.","Storing secrets or TLS keys carelessly.","Ignoring controller-specific annotations."],
    followUpQuestions: ["Ingress vs Service?","Ingress vs Gateway API?","Where does TLS terminate?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-9",
    number: "DEVOPS-M4-9",
    title: "ConfigMaps",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A ConfigMap stores non-sensitive configuration data for Pods. Values can be exposed as environment variables, command arguments or mounted files. It is not a secure secret store.",
    deepExplanation: "```yaml\napiVersion: v1\nkind: ConfigMap\nmetadata:\n  name: frontend-config\ndata:\n  API_BASE_URL: \"https://api.example.com\"\n  LOG_LEVEL: \"info\"\n```\n\nReference it in a Deployment:\n\n```yaml\nenvFrom:\n  - configMapRef:\n      name: frontend-config\n```",
    productionExample: "Environment-specific API endpoints and feature settings are stored in ConfigMaps while credentials are kept in Secrets or an external secret manager. Changes are versioned through deployment tooling.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "ConfigMaps separate configuration from images and enable operational changes, but they can cause rollout/propagation complexity and do not provide secrecy.",
    commonMistakes: ["Putting API keys/passwords in ConfigMaps.","Assuming environment changes always restart Pods automatically.","Creating unbounded configuration blobs."],
    followUpQuestions: ["ConfigMap vs Secret?","How do mounted ConfigMaps update?","How would you roll out a configuration change safely?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-10",
    number: "DEVOPS-M4-10",
    title: "Secrets",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Kubernetes Secret stores sensitive data such as tokens, passwords or certificates in Kubernetes objects, but base64 encoding is not encryption. Secret security depends on API-server encryption at rest, RBAC, transport security, admission policies and potentially an external secret manager.",
    deepExplanation: "```yaml\napiVersion: v1\nkind: Secret\nmetadata:\n  name: api-credentials\ntype: Opaque\nstringData:\n  API_TOKEN: \"<managed-secret-value>\"\n```\n\nConsume it:\n\n```yaml\nenv:\n  - name: API_TOKEN\n    valueFrom:\n      secretKeyRef:\n        name: api-credentials\n        key: API_TOKEN\n```\n\nFor higher-security environments, use an external secret manager and a controller such as External Secrets rather than committing secret manifests to Git.",
    productionExample: "Production credentials are held in a managed secret store and synchronized to Kubernetes. Workload ServiceAccounts receive only the permissions they require, and secret access is audited.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Kubernetes Secrets integrate cleanly with workloads but still require careful cluster security. External secret systems improve governance but add dependencies/controllers.",
    commonMistakes: ["Assuming base64 means encrypted.","Checking real secret values into Git.","Giving broad read access to Secrets."],
    followUpQuestions: ["Secret vs ConfigMap?","How are Secrets encrypted at rest?","How would you rotate a secret without downtime?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-11",
    number: "DEVOPS-M4-11",
    title: "Volumes",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Kubernetes Volumes provide storage to containers and define how data lives relative to Pod/container lifecycle. PersistentVolume (PV), PersistentVolumeClaim (PVC) and StorageClass provide durable storage abstractions independent of a single container filesystem.",
    deepExplanation: "```yaml\napiVersion: v1\nkind: PersistentVolumeClaim\nmetadata:\n  name: uploads\nspec:\n  accessModes:\n    - ReadWriteOnce\n  resources:\n    requests:\n      storage: 20Gi\n```\n\nA Pod mounts it:\n\n```yaml\nvolumeMounts:\n  - name: uploads\n    mountPath: /data\nvolumes:\n  - name: uploads\n    persistentVolumeClaim:\n      claimName: uploads\n```",
    productionExample: "A stateful service requests storage through a PVC backed by a cloud StorageClass. Kubernetes binds it to a suitable PV and the workload mounts it consistently across restarts.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Persistent storage gives durability but introduces storage costs, backup/restore complexity and scheduling constraints.",
    commonMistakes: ["Treating ephemeral volumes as durable storage.","Ignoring storage class behavior.","Deleting PVCs casually.","Assuming ReadWriteOnce means any Pod/node can mount simultaneously."],
    followUpQuestions: ["PV vs PVC vs StorageClass?","What happens when a Pod moves nodes?","How do snapshots/backups work?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-12",
    number: "DEVOPS-M4-12",
    title: "Autoscaling",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Kubernetes autoscaling adjusts resources or replica counts based on observed metrics and desired targets. Horizontal Pod Autoscaler changes Pod replica count; Vertical Pod Autoscaler can recommend or adjust resource requests; Cluster Autoscaler changes node capacity where supported.",
    deepExplanation: "```yaml\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: frontend\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: frontend\n  minReplicas: 3\n  maxReplicas: 10\n  metrics:\n    - type: Resource\n      resource:\n        name: cpu\n        target:\n          type: Utilization\n          averageUtilization: 70\n```\n\nThe HPA depends on metrics being available and on resource requests being defined sensibly.",
    productionExample: "During peak traffic, the frontend Deployment scales from 3 to 8 replicas based on resource/load metrics while the cluster autoscaler adds nodes if existing capacity is insufficient.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Autoscaling improves elasticity and cost efficiency but can be slow, oscillatory or expensive if thresholds and resource requests are poorly chosen.",
    commonMistakes: ["Autoscaling without enough node capacity.","Using CPU for an app whose bottleneck is queue depth or latency.","No cooldown/behavior tuning.","Forgetting cold-start time."],
    followUpQuestions: ["HPA vs VPA vs Cluster Autoscaler?","What metrics should you scale on?","How do you prevent scaling thrash?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-13",
    number: "DEVOPS-M4-13",
    title: "Health Checks",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Kubernetes health checks are probes that let the control plane determine whether a container is running, ready to receive traffic or needs startup grace. The main probe types are liveness, readiness and startup probes.",
    deepExplanation: "```yaml\nlivenessProbe:\n  httpGet:\n    path: /health/live\n    port: 8080\nreadinessProbe:\n  httpGet:\n    path: /health/ready\n    port: 8080\nstartupProbe:\n  httpGet:\n    path: /health/startup\n    port: 8080\n  failureThreshold: 30\n  periodSeconds: 10\n```\n\nUse different endpoints when the semantics differ. A readiness failure should normally stop traffic; a liveness failure can restart the container; a startup probe can delay liveness checks during slow initialization.",
    productionExample: "A Node API exposes lightweight live/ready endpoints. During startup it may need a database migration or cache warm-up, so a startup probe prevents premature restarts while readiness keeps traffic away until dependencies are usable.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Good probes improve availability and rollout safety. Bad probes can create restart loops or remove healthy capacity during a transient dependency problem.",
    commonMistakes: ["Using one endpoint for every probe.","Making liveness depend on external databases.","No startup probe for long initialization."],
    followUpQuestions: ["Liveness vs readiness?","When do you need startup probes?","How can a bad probe create an outage?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-14",
    number: "DEVOPS-M4-14",
    title: "Liveness",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A liveness probe answers whether a container should continue running. If a liveness probe repeatedly fails according to the configured thresholds, the kubelet may restart the container.",
    deepExplanation: "```yaml\nlivenessProbe:\n  httpGet:\n    path: /health/live\n    port: 8080\n  initialDelaySeconds: 10\n  periodSeconds: 10\n  failureThreshold: 3\n```\n\nThe endpoint should indicate a state where restarting the process is a reasonable recovery action. It should not fail merely because a downstream service is temporarily unavailable.",
    productionExample: "An API detects an internal deadlock and returns unhealthy on its liveness endpoint. Kubernetes restarts the stuck container while readiness prevents new traffic from reaching unhealthy instances.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Liveness provides automatic recovery but can amplify incidents if the probe fails during a shared dependency outage.",
    commonMistakes: ["Checking database connectivity in liveness.","Too-aggressive intervals and thresholds.","Ignoring restart loops."],
    followUpQuestions: ["When should liveness fail?","Why not check all dependencies?","How do you detect CrashLoopBackOff causes?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-15",
    number: "DEVOPS-M4-15",
    title: "Readiness",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A readiness probe determines whether a Pod is ready to receive traffic. When readiness fails, the Pod is normally removed from ready Service endpoints without necessarily restarting the container.",
    deepExplanation: "```yaml\nreadinessProbe:\n  httpGet:\n    path: /health/ready\n    port: 8080\n  periodSeconds: 5\n  failureThreshold: 3\n```\n\nReadiness is useful during startup, deployments, dependency initialization and temporary overload states. It should describe whether serving traffic now is safe.",
    productionExample: "During a rolling deployment, new frontend/API Pods become ready only after the application is listening and required local state is initialized. Service traffic then shifts to the ready endpoints.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Readiness improves routing safety but too-sensitive checks can reduce available capacity and trigger cascading load on remaining Pods.",
    commonMistakes: ["Making readiness identical to liveness.","Failing readiness on every optional downstream dependency.","Ignoring the impact of slow readiness on rollout time."],
    followUpQuestions: ["Readiness vs liveness?","How does Service use readiness?","What happens when all Pods become unready?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-16",
    number: "DEVOPS-M4-16",
    title: "Jobs",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A Kubernetes Job creates Pods that run to completion rather than serving continuously. It tracks successful completion count and retries failed Pods according to its policy.",
    deepExplanation: "```yaml\napiVersion: batch/v1\nkind: Job\nmetadata:\n  name: db-migration\nspec:\n  backoffLimit: 3\n  template:\n    spec:\n      restartPolicy: Never\n      containers:\n        - name: migrate\n          image: registry.example.com/api:1.4.0\n          command: [\"./migrate\"]\n```\n\nJobs are useful for migrations, one-off batch processing or controlled maintenance tasks. Idempotency matters because a failed Job may retry.",
    productionExample: "A release pipeline runs a database migration Job before promoting the application. The job is versioned with the same application artifact and its success becomes a deployment gate.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Jobs give declarative batch execution but retries can duplicate effects if the workload is not idempotent.",
    commonMistakes: ["Non-idempotent jobs with automatic retries.","No timeout/cleanup strategy.","Treating a Job like a Deployment."],
    followUpQuestions: ["Job vs CronJob?","How do you make migrations idempotent?","How do failed Jobs retry?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-17",
    number: "DEVOPS-M4-17",
    title: "CronJobs",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A CronJob creates Jobs on a defined schedule. It is useful for periodic maintenance, reporting, cleanup or batch tasks where Kubernetes should manage scheduling and job lifecycle.",
    deepExplanation: "```yaml\napiVersion: batch/v1\nkind: CronJob\nmetadata:\n  name: nightly-cleanup\nspec:\n  schedule: \"0 2 * * *\"\n  concurrencyPolicy: Forbid\n  successfulJobsHistoryLimit: 3\n  failedJobsHistoryLimit: 3\n  jobTemplate:\n    spec:\n      template:\n        spec:\n          restartPolicy: Never\n          containers:\n            - name: cleanup\n              image: registry.example.com/tools:1.4.0\n              command: [\"./cleanup\"]\n```\n\n`concurrencyPolicy`, history limits and retry behavior are important for production correctness.",
    productionExample: "A nightly data cleanup runs as a CronJob with `Forbid` concurrency so a slow previous run does not overlap a new one. Logs and metrics are collected for each Job.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "CronJobs simplify periodic scheduling but are not a distributed transaction system. Cluster outages, controller delays and long-running jobs need explicit operational handling.",
    commonMistakes: ["Allowing overlapping jobs accidentally.","No cleanup/history limits.","Assuming exactly-once execution."],
    followUpQuestions: ["CronJob concurrency policies?","What happens if a schedule is missed?","How do you make scheduled work idempotent?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-18",
    number: "DEVOPS-M4-18",
    title: "Networking",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "Kubernetes networking provides Pod-to-Pod communication, Service discovery and external connectivity through a cluster network implementation (CNI). Pods receive routable IPs within the cluster, while Services provide stable virtual access to changing Pod endpoints.",
    deepExplanation: "Typical flow:\n\n```text\nClient\n  -> Ingress / LoadBalancer\n  -> Service\n  -> ready Pod\n```\n\nInside the cluster:\n\n```text\napi.default.svc.cluster.local\n```\n\ncan resolve through cluster DNS. NetworkPolicies can further restrict which Pods are allowed to communicate.\n\nTroubleshooting often uses:\n\n```bash\nkubectl get svc,endpointslice\nkubectl get pods -o wide\nkubectl exec <pod> -- nslookup api\n```",
    productionExample: "A frontend Service routes requests to API Pods selected by labels. A NetworkPolicy permits frontend-to-api traffic while denying unrelated namespace traffic, and ingress handles external TLS.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Kubernetes networking is powerful but can be complex because traffic may traverse CNI, kube-proxy/eBPF, Services, ingress and cloud load balancers.",
    commonMistakes: ["Hard-coding Pod IPs.","Assuming namespace boundaries block traffic automatically.","Ignoring DNS and EndpointSlice state."],
    followUpQuestions: ["How does Service discovery work?","What is CNI?","How do NetworkPolicies affect traffic?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-19",
    number: "DEVOPS-M4-19",
    title: "Service Mesh Overview",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A service mesh is an infrastructure layer for managing service-to-service traffic with features such as mTLS, retries, timeouts, traffic policy and telemetry. It commonly uses a data plane plus a control plane, often implemented using sidecars or newer ambient approaches depending on the platform.",
    deepExplanation: "Conceptual flow:\n\n```text\nService A\n   |\nproxy/data plane\n   |\nproxy/data plane\n   |\nService B\n        ^\n control plane config\n```\n\nThe mesh can provide consistent traffic policy and identity without every application implementing the same networking logic. It also adds CPU, memory and operational complexity and should be adopted only when the capabilities justify it.",
    productionExample: "A large microservices platform uses a mesh to standardize mTLS and request telemetry across many teams while keeping application code focused on business logic. The platform team owns mesh upgrades and policy.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "A mesh centralizes traffic capabilities but adds another distributed control plane/data plane and can make debugging more complex.",
    commonMistakes: ["Assuming a mesh automatically fixes bad application protocols.","Ignoring retry amplification.","Deploying a mesh before understanding operational overhead."],
    followUpQuestions: ["Service mesh vs API gateway?","What is mTLS?","How can retries make an outage worse?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },
  {
    id: "devops-m4-20",
    number: "DEVOPS-M4-20",
    title: "Production Architecture",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Kubernetes",
    expectedAnswer: "A production Kubernetes architecture combines workload controllers, Services/Ingress, namespaces/RBAC, configuration and secret management, persistent storage where required, resource management, health probes, autoscaling, observability, security controls and a resilient rollout strategy.",
    deepExplanation: "Representative architecture:\n\n```text\nInternet\n  -> Cloud Load Balancer / Ingress\n  -> Service\n  -> Deployment Pods\n       |-> ConfigMap\n       |-> Secret\n       |-> probes\n       |-> resources\n  -> Observability\n  -> Autoscaling\n\nStateful dependencies\n  -> managed database / durable storage\n```\n\nA production design should also account for multi-zone scheduling, PodDisruptionBudgets, NetworkPolicies, backup/restore, image provenance and cluster upgrade strategy.",
    productionExample: "A frontend/API platform runs stateless Deployments across multiple nodes/zones, exposes them through Ingress/Services, uses autoscaling and readiness probes, stores secrets via an external secret manager and keeps the primary database managed outside the cluster where practical.",
    bestPractices: ["Use declarative manifests and version-control all important changes.","Set requests/limits, probes and rollout policies deliberately.","Use least-privilege RBAC and secure secret handling.","Prefer immutable image digests and observable deployments.","Design explicitly for failure, rollback, scaling and recovery."],
    tradeOffs: "Kubernetes provides powerful primitives but increases operational responsibility. Production quality depends as much on cluster/security/observability design as on YAML syntax.",
    commonMistakes: ["Treating Kubernetes as automatic high availability.","No resource limits or probes.","Running databases in-cluster without backup/recovery expertise.","No upgrade/rollback plan."],
    followUpQuestions: ["How would you design for multi-zone failure?","What belongs inside vs outside Kubernetes?","How do you secure a production cluster?"],
    relatedTopics: ["Kubernetes","Pods","Deployments","Services","Networking","Security","Observability","Autoscaling"],
  },

];

export const MOCK_DEVOPS_MODULE4_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'DevOps/Cloud',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));