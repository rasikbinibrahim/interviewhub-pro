// Derived from frontend/src/document/DSA_Master_Handbook_Senior_Frontend_Engineers.md (Module 18 — Trie, Segment Tree & Advanced Structures).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Definition, JavaScript/TypeScript implementation, Time/Space Complexity,
// Trade-offs, Common Mistakes, Interview Tricks, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Apple",
  "Netflix",
  "Uber",
  "Airbnb",
  "Atlassian",
  "Adobe",
  "LinkedIn",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "dsa-m18-171",
    number: "DSA-M18-171",
    title: "Trie fundamentals",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Trie, Segment Tree & Advanced Structures",
    expectedAnswer: "A Trie stores characters along shared prefixes. Exact and prefix operations are O(L), where L is the query length.",
    deepExplanation: "Step 1 — Understand the operation and invariant.\n\n```ts\nclass TrieNode {\n  children = new Map<string, TrieNode>();\n  isWord = false;\n}\n\nclass Trie {\n  private readonly root = new TrieNode();\n\n  insert(word: string): void {\n    let node = this.root;\n    for (const char of word) {\n      if (!node.children.has(char)) node.children.set(char, new TrieNode());\n      node = node.children.get(char)!;\n    }\n    node.isWord = true;\n  }\n\n  search(word: string): boolean {\n    let node = this.root;\n    for (const char of word) {\n      const next = node.children.get(char);\n      if (!next) return false;\n      node = next;\n    }\n    return node.isWord;\n  }\n\n  startsWith(prefix: string): boolean {\n    let node = this.root;\n    for (const char of prefix) {\n      const next = node.children.get(char);\n      if (!next) return false;\n      node = next;\n    }\n    return true;\n  }\n}\n```\n```ts\nfunction searchUsingArray(words: readonly string[], word: string): boolean {\n  return words.includes(word);\n}\n```\n\nExample takeaway: choose the data structure from the required operations and constraints.",
    productionExample: "Use this structure when its operation guarantees justify the added memory and implementation complexity.",
    bestPractices: ["Define the required operation before choosing the structure.", "State the invariant and complexity before coding.", "Test empty, one-item, and boundary cases.", "Prefer the simplest structure that satisfies the workload.", "Keep helper types self-contained and indexing explicit."],
    tradeOffs: "Choose advanced structures only when their query/update guarantees provide a meaningful benefit over Array, Set, Map, or sorted data.",
    commonMistakes: ["Choosing an advanced structure without a matching workload.", "Ignoring memory overhead.", "Mixing exact and probabilistic guarantees.", "Forgetting boundary cases.", "Leaving helper types undeclared."],
    followUpQuestions: ["Why is this structure appropriate?", "What is the invariant?", "What are the time and space complexities?", "What simpler alternative exists?", "When should you not use it?", "How do you test edge cases?", "How does scale change the choice?", "How do you reduce memory?", "How would you support updates?", "How would you adapt it for production?"],
    relatedTopics: ["Trie", "Segment Tree", "Fenwick Tree", "Union Find", "LRU Cache", "Bloom Filter", "Advanced Data Structures"],
  },
  {
    id: "dsa-m18-172",
    number: "DSA-M18-172",
    title: "Autocomplete with Trie",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Trie, Segment Tree & Advanced Structures",
    expectedAnswer: "Autocomplete walks to the prefix node, then explores its subtree. Prefix lookup is O(P), plus returned-result traversal.",
    deepExplanation: "Step 1 — Understand the operation and invariant.\n\n```ts\nclass SuggestNode {\n  children = new Map<string, SuggestNode>();\n  isWord = false;\n}\n\nclass AutocompleteTrie {\n  private readonly root = new SuggestNode();\n\n  insert(word: string): void {\n    let node = this.root;\n    for (const char of word) {\n      if (!node.children.has(char)) node.children.set(char, new SuggestNode());\n      node = node.children.get(char)!;\n    }\n    node.isWord = true;\n  }\n\n  suggest(prefix: string, limit = 10): string[] {\n    let node = this.root;\n    for (const char of prefix) {\n      const next = node.children.get(char);\n      if (!next) return [];\n      node = next;\n    }\n\n    const result: string[] = [];\n    const visit = (current: SuggestNode, word: string): void => {\n      if (result.length >= limit) return;\n      if (current.isWord) result.push(word);\n      for (const [char, child] of current.children) visit(child, word + char);\n    };\n\n    visit(node, prefix);\n    return result.slice(0, limit);\n  }\n}\n```\n```ts\nfunction suggestUsingFilter(words: readonly string[], prefix: string, limit = 10): string[] {\n  return words.filter((word) => word.startsWith(prefix)).slice(0, limit);\n}\n```\n\nExample takeaway: choose the data structure from the required operations and constraints.",
    productionExample: "Use this structure when its operation guarantees justify the added memory and implementation complexity.",
    bestPractices: ["Define the required operation before choosing the structure.", "State the invariant and complexity before coding.", "Test empty, one-item, and boundary cases.", "Prefer the simplest structure that satisfies the workload.", "Keep helper types self-contained and indexing explicit."],
    tradeOffs: "Choose advanced structures only when their query/update guarantees provide a meaningful benefit over Array, Set, Map, or sorted data.",
    commonMistakes: ["Choosing an advanced structure without a matching workload.", "Ignoring memory overhead.", "Mixing exact and probabilistic guarantees.", "Forgetting boundary cases.", "Leaving helper types undeclared."],
    followUpQuestions: ["Why is this structure appropriate?", "What is the invariant?", "What are the time and space complexities?", "What simpler alternative exists?", "When should you not use it?", "How do you test edge cases?", "How does scale change the choice?", "How do you reduce memory?", "How would you support updates?", "How would you adapt it for production?"],
    relatedTopics: ["Trie", "Segment Tree", "Fenwick Tree", "Union Find", "LRU Cache", "Bloom Filter", "Advanced Data Structures"],
  },
  {
    id: "dsa-m18-173",
    number: "DSA-M18-173",
    title: "Prefix search",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Trie, Segment Tree & Advanced Structures",
    expectedAnswer: "Prefix search asks whether any stored key starts with a prefix. Trie gives O(P) navigation; a scan is simpler for small data.",
    deepExplanation: "Step 1 — Understand the operation and invariant.\n\n```ts\nclass PrefixNode {\n  children = new Map<string, PrefixNode>();\n}\n\nfunction buildPrefixTrie(words: readonly string[]): PrefixNode {\n  const root = new PrefixNode();\n  for (const word of words) {\n    let node = root;\n    for (const char of word) {\n      if (!node.children.has(char)) node.children.set(char, new PrefixNode());\n      node = node.children.get(char)!;\n    }\n  }\n  return root;\n}\n\nfunction hasPrefix(root: PrefixNode, prefix: string): boolean {\n  let node = root;\n  for (const char of prefix) {\n    const next = node.children.get(char);\n    if (!next) return false;\n    node = next;\n  }\n  return true;\n}\n```\n```ts\nfunction hasPrefixUsingSome(words: readonly string[], prefix: string): boolean {\n  return words.some((word) => word.startsWith(prefix));\n}\n```\n\nExample takeaway: choose the data structure from the required operations and constraints.",
    productionExample: "Use this structure when its operation guarantees justify the added memory and implementation complexity.",
    bestPractices: ["Define the required operation before choosing the structure.", "State the invariant and complexity before coding.", "Test empty, one-item, and boundary cases.", "Prefer the simplest structure that satisfies the workload.", "Keep helper types self-contained and indexing explicit."],
    tradeOffs: "Choose advanced structures only when their query/update guarantees provide a meaningful benefit over Array, Set, Map, or sorted data.",
    commonMistakes: ["Choosing an advanced structure without a matching workload.", "Ignoring memory overhead.", "Mixing exact and probabilistic guarantees.", "Forgetting boundary cases.", "Leaving helper types undeclared."],
    followUpQuestions: ["Why is this structure appropriate?", "What is the invariant?", "What are the time and space complexities?", "What simpler alternative exists?", "When should you not use it?", "How do you test edge cases?", "How does scale change the choice?", "How do you reduce memory?", "How would you support updates?", "How would you adapt it for production?"],
    relatedTopics: ["Trie", "Segment Tree", "Fenwick Tree", "Union Find", "LRU Cache", "Bloom Filter", "Advanced Data Structures"],
  },
  {
    id: "dsa-m18-174",
    number: "DSA-M18-174",
    title: "Segment tree fundamentals",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Trie, Segment Tree & Advanced Structures",
    expectedAnswer: "A segment tree stores interval aggregates and supports point updates and range queries in O(log n).",
    deepExplanation: "Step 1 — Understand the operation and invariant.\n\n```ts\nclass SegmentTree {\n  private readonly tree: number[];\n  private readonly n: number;\n\n  constructor(values: readonly number[]) {\n    this.n = values.length;\n    this.tree = new Array<number>(Math.max(1, 4 * this.n)).fill(0);\n    if (this.n > 0) this.build(values, 1, 0, this.n - 1);\n  }\n\n  private build(a: readonly number[], node: number, left: number, right: number): void {\n    if (left === right) {\n      this.tree[node] = a[left]!;\n      return;\n    }\n    const mid = left + Math.floor((right - left) / 2);\n    this.build(a, node * 2, left, mid);\n    this.build(a, node * 2 + 1, mid + 1, right);\n    this.tree[node] = this.tree[node * 2]! + this.tree[node * 2 + 1]!;\n  }\n\n  query(ql: number, qr: number): number {\n    return this.queryNode(1, 0, this.n - 1, ql, qr);\n  }\n\n  private queryNode(node: number, left: number, right: number, ql: number, qr: number): number {\n    if (qr < left || right < ql) return 0;\n    if (ql <= left && right <= qr) return this.tree[node]!;\n    const mid = left + Math.floor((right - left) / 2);\n    return this.queryNode(node * 2, left, mid, ql, qr) +\n      this.queryNode(node * 2 + 1, mid + 1, right, ql, qr);\n  }\n}\n```\n```ts\nfunction rangeSumUsingReduce(values: readonly number[], left: number, right: number): number {\n  return values.slice(left, right + 1).reduce((sum, value) => sum + value, 0);\n}\n```\n\nExample takeaway: choose the data structure from the required operations and constraints.",
    productionExample: "Use this structure when its operation guarantees justify the added memory and implementation complexity.",
    bestPractices: ["Define the required operation before choosing the structure.", "State the invariant and complexity before coding.", "Test empty, one-item, and boundary cases.", "Prefer the simplest structure that satisfies the workload.", "Keep helper types self-contained and indexing explicit."],
    tradeOffs: "Choose advanced structures only when their query/update guarantees provide a meaningful benefit over Array, Set, Map, or sorted data.",
    commonMistakes: ["Choosing an advanced structure without a matching workload.", "Ignoring memory overhead.", "Mixing exact and probabilistic guarantees.", "Forgetting boundary cases.", "Leaving helper types undeclared."],
    followUpQuestions: ["Why is this structure appropriate?", "What is the invariant?", "What are the time and space complexities?", "What simpler alternative exists?", "When should you not use it?", "How do you test edge cases?", "How does scale change the choice?", "How do you reduce memory?", "How would you support updates?", "How would you adapt it for production?"],
    relatedTopics: ["Trie", "Segment Tree", "Fenwick Tree", "Union Find", "LRU Cache", "Bloom Filter", "Advanced Data Structures"],
  },
  {
    id: "dsa-m18-175",
    number: "DSA-M18-175",
    title: "Fenwick tree",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Trie, Segment Tree & Advanced Structures",
    expectedAnswer: "A Fenwick tree supports prefix sums and point updates in O(log n) with O(n) memory.",
    deepExplanation: "Step 1 — Understand the operation and invariant.\n\n```ts\nclass FenwickTree {\n  private readonly tree: number[];\n\n  constructor(size: number) {\n    this.tree = new Array<number>(size + 1).fill(0);\n  }\n\n  update(index: number, delta: number): void {\n    for (let i = index + 1; i < this.tree.length; i += i & -i) {\n      this.tree[i] = this.tree[i]! + delta;\n    }\n  }\n\n  prefixSum(index: number): number {\n    let sum = 0;\n    for (let i = index + 1; i > 0; i -= i & -i) sum += this.tree[i]!;\n    return sum;\n  }\n\n  rangeSum(left: number, right: number): number {\n    return this.prefixSum(right) - (left === 0 ? 0 : this.prefixSum(left - 1));\n  }\n}\n```\n```ts\nfunction rangeSumWithPrefix(values: readonly number[], left: number, right: number): number {\n  const prefix = new Array<number>(values.length + 1).fill(0);\n  for (let i = 0; i < values.length; i++) prefix[i + 1] = prefix[i]! + values[i]!;\n  return prefix[right + 1]! - prefix[left]!;\n}\n```\n\nExample takeaway: choose the data structure from the required operations and constraints.",
    productionExample: "Use this structure when its operation guarantees justify the added memory and implementation complexity.",
    bestPractices: ["Define the required operation before choosing the structure.", "State the invariant and complexity before coding.", "Test empty, one-item, and boundary cases.", "Prefer the simplest structure that satisfies the workload.", "Keep helper types self-contained and indexing explicit."],
    tradeOffs: "Choose advanced structures only when their query/update guarantees provide a meaningful benefit over Array, Set, Map, or sorted data.",
    commonMistakes: ["Choosing an advanced structure without a matching workload.", "Ignoring memory overhead.", "Mixing exact and probabilistic guarantees.", "Forgetting boundary cases.", "Leaving helper types undeclared."],
    followUpQuestions: ["Why is this structure appropriate?", "What is the invariant?", "What are the time and space complexities?", "What simpler alternative exists?", "When should you not use it?", "How do you test edge cases?", "How does scale change the choice?", "How do you reduce memory?", "How would you support updates?", "How would you adapt it for production?"],
    relatedTopics: ["Trie", "Segment Tree", "Fenwick Tree", "Union Find", "LRU Cache", "Bloom Filter", "Advanced Data Structures"],
  },
  {
    id: "dsa-m18-176",
    number: "DSA-M18-176",
    title: "Union Find",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Trie, Segment Tree & Advanced Structures",
    expectedAnswer: "Union Find maintains connectivity components with find and union. Path compression plus union by size gives near-constant amortized cost.",
    deepExplanation: "Step 1 — Understand the operation and invariant.\n\n```ts\nclass UnionFind {\n  private readonly parent: number[];\n  private readonly size: number[];\n\n  constructor(count: number) {\n    this.parent = new Array<number>(count);\n    this.size = new Array<number>(count).fill(1);\n    for (let i = 0; i < count; i++) this.parent[i] = i;\n  }\n\n  find(x: number): number {\n    let root = x;\n    while (root !== this.parent[root]) root = this.parent[root]!;\n    while (x !== root) {\n      const next = this.parent[x]!;\n      this.parent[x] = root;\n      x = next;\n    }\n    return root;\n  }\n\n  union(a: number, b: number): boolean {\n    let ra = this.find(a);\n    let rb = this.find(b);\n    if (ra === rb) return false;\n    if (this.size[ra]! < this.size[rb]!) [ra, rb] = [rb, ra];\n    this.parent[rb] = ra;\n    this.size[ra]! += this.size[rb]!;\n    return true;\n  }\n\n  connected(a: number, b: number): boolean {\n    return this.find(a) === this.find(b);\n  }\n}\n```\n```ts\nfunction sameGroup(groups: ReadonlyMap<number, number>, a: number, b: number): boolean {\n  return groups.get(a) === groups.get(b);\n}\n```\n\nExample takeaway: choose the data structure from the required operations and constraints.",
    productionExample: "Use this structure when its operation guarantees justify the added memory and implementation complexity.",
    bestPractices: ["Define the required operation before choosing the structure.", "State the invariant and complexity before coding.", "Test empty, one-item, and boundary cases.", "Prefer the simplest structure that satisfies the workload.", "Keep helper types self-contained and indexing explicit."],
    tradeOffs: "Choose advanced structures only when their query/update guarantees provide a meaningful benefit over Array, Set, Map, or sorted data.",
    commonMistakes: ["Choosing an advanced structure without a matching workload.", "Ignoring memory overhead.", "Mixing exact and probabilistic guarantees.", "Forgetting boundary cases.", "Leaving helper types undeclared."],
    followUpQuestions: ["Why is this structure appropriate?", "What is the invariant?", "What are the time and space complexities?", "What simpler alternative exists?", "When should you not use it?", "How do you test edge cases?", "How does scale change the choice?", "How do you reduce memory?", "How would you support updates?", "How would you adapt it for production?"],
    relatedTopics: ["Trie", "Segment Tree", "Fenwick Tree", "Union Find", "LRU Cache", "Bloom Filter", "Advanced Data Structures"],
  },
  {
    id: "dsa-m18-177",
    number: "DSA-M18-177",
    title: "Disjoint sets",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Trie, Segment Tree & Advanced Structures",
    expectedAnswer: "Disjoint sets are the components managed by Union Find. They are ideal when connectivity only grows through unions.",
    deepExplanation: "Step 1 — Understand the operation and invariant.\n\n```ts\nclass DisjointSet {\n  private readonly parent: number[];\n  private readonly rank: number[];\n\n  constructor(size: number) {\n    this.parent = Array.from({ length: size }, (_, i) => i);\n    this.rank = new Array<number>(size).fill(0);\n  }\n\n  find(x: number): number {\n    if (this.parent[x] !== x) this.parent[x] = this.find(this.parent[x]!);\n    return this.parent[x]!;\n  }\n\n  union(a: number, b: number): boolean {\n    let ra = this.find(a);\n    let rb = this.find(b);\n    if (ra === rb) return false;\n    if (this.rank[ra]! < this.rank[rb]!) [ra, rb] = [rb, ra];\n    this.parent[rb] = ra;\n    if (this.rank[ra] === this.rank[rb]) this.rank[ra]!++;\n    return true;\n  }\n}\n```\n```ts\nfunction initialGroups(size: number): Set<number> {\n  return new Set(Array.from({ length: size }, (_, i) => i));\n}\n```\n\nExample takeaway: choose the data structure from the required operations and constraints.",
    productionExample: "Use this structure when its operation guarantees justify the added memory and implementation complexity.",
    bestPractices: ["Define the required operation before choosing the structure.", "State the invariant and complexity before coding.", "Test empty, one-item, and boundary cases.", "Prefer the simplest structure that satisfies the workload.", "Keep helper types self-contained and indexing explicit."],
    tradeOffs: "Choose advanced structures only when their query/update guarantees provide a meaningful benefit over Array, Set, Map, or sorted data.",
    commonMistakes: ["Choosing an advanced structure without a matching workload.", "Ignoring memory overhead.", "Mixing exact and probabilistic guarantees.", "Forgetting boundary cases.", "Leaving helper types undeclared."],
    followUpQuestions: ["Why is this structure appropriate?", "What is the invariant?", "What are the time and space complexities?", "What simpler alternative exists?", "When should you not use it?", "How do you test edge cases?", "How does scale change the choice?", "How do you reduce memory?", "How would you support updates?", "How would you adapt it for production?"],
    relatedTopics: ["Trie", "Segment Tree", "Fenwick Tree", "Union Find", "LRU Cache", "Bloom Filter", "Advanced Data Structures"],
  },
  {
    id: "dsa-m18-178",
    number: "DSA-M18-178",
    title: "LRU cache",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Trie, Segment Tree & Advanced Structures",
    expectedAnswer: "An LRU cache evicts the least recently used item. A Map plus a doubly linked list gives O(1) lookup, promotion, insertion, and eviction.",
    deepExplanation: "Step 1 — Understand the operation and invariant.\n\n```ts\nclass LruNode<K, V> {\n  constructor(\n    public key: K,\n    public value: V,\n    public prev: LruNode<K, V> | null = null,\n    public next: LruNode<K, V> | null = null,\n  ) {}\n}\n\nclass LRUCache<K, V> {\n  private readonly map = new Map<K, LruNode<K, V>>();\n  private readonly head = new LruNode<K, V>(undefined as K, undefined as V);\n  private readonly tail = new LruNode<K, V>(undefined as K, undefined as V);\n\n  constructor(private readonly capacity: number) {\n    this.head.next = this.tail;\n    this.tail.prev = this.head;\n  }\n\n  get(key: K): V | undefined {\n    const node = this.map.get(key);\n    if (!node) return undefined;\n    this.remove(node);\n    this.insertAfterHead(node);\n    return node.value;\n  }\n\n  set(key: K, value: V): void {\n    if (this.capacity <= 0) return;\n    const existing = this.map.get(key);\n\n    if (existing) {\n      existing.value = value;\n      this.remove(existing);\n      this.insertAfterHead(existing);\n      return;\n    }\n\n    const node = new LruNode(key, value);\n    this.map.set(key, node);\n    this.insertAfterHead(node);\n\n    if (this.map.size > this.capacity) {\n      const oldest = this.tail.prev;\n      if (oldest && oldest !== this.head) {\n        this.remove(oldest);\n        this.map.delete(oldest.key);\n      }\n    }\n  }\n\n  private remove(node: LruNode<K, V>): void {\n    node.prev!.next = node.next;\n    node.next!.prev = node.prev;\n  }\n\n  private insertAfterHead(node: LruNode<K, V>): void {\n    node.next = this.head.next;\n    node.prev = this.head;\n    this.head.next!.prev = node;\n    this.head.next = node;\n  }\n}\n```\n```ts\nfunction createMapLru<K, V>(capacity: number) {\n  const cache = new Map<K, V>();\n\n  return {\n    get(key: K): V | undefined {\n      const value = cache.get(key);\n      if (value === undefined) return undefined;\n      cache.delete(key);\n      cache.set(key, value);\n      return value;\n    },\n    set(key: K, value: V): void {\n      cache.delete(key);\n      cache.set(key, value);\n      while (cache.size > capacity) {\n        const oldest = cache.keys().next().value as K | undefined;\n        if (oldest === undefined) break;\n        cache.delete(oldest);\n      }\n    },\n  };\n}\n```\n\nExample takeaway: choose the data structure from the required operations and constraints.",
    productionExample: "Use this structure when its operation guarantees justify the added memory and implementation complexity.",
    bestPractices: ["Define the required operation before choosing the structure.", "State the invariant and complexity before coding.", "Test empty, one-item, and boundary cases.", "Prefer the simplest structure that satisfies the workload.", "Keep helper types self-contained and indexing explicit."],
    tradeOffs: "Choose advanced structures only when their query/update guarantees provide a meaningful benefit over Array, Set, Map, or sorted data.",
    commonMistakes: ["Choosing an advanced structure without a matching workload.", "Ignoring memory overhead.", "Mixing exact and probabilistic guarantees.", "Forgetting boundary cases.", "Leaving helper types undeclared."],
    followUpQuestions: ["Why is this structure appropriate?", "What is the invariant?", "What are the time and space complexities?", "What simpler alternative exists?", "When should you not use it?", "How do you test edge cases?", "How does scale change the choice?", "How do you reduce memory?", "How would you support updates?", "How would you adapt it for production?"],
    relatedTopics: ["Trie", "Segment Tree", "Fenwick Tree", "Union Find", "LRU Cache", "Bloom Filter", "Advanced Data Structures"],
  },
  {
    id: "dsa-m18-179",
    number: "DSA-M18-179",
    title: "Bloom filter concepts",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Trie, Segment Tree & Advanced Structures",
    expectedAnswer: "A Bloom filter is a compact probabilistic membership structure. It may return false positives but should not return false negatives for inserted values.",
    deepExplanation: "Step 1 — Understand the operation and invariant.\n\n```ts\nclass BloomFilter {\n  private readonly bits: Uint8Array;\n\n  constructor(private readonly size: number) {\n    if (size <= 0) throw new Error(\"size must be positive\");\n    this.bits = new Uint8Array(size);\n  }\n\n  private hash(value: string, seed: number): number {\n    let hash = seed;\n    for (let i = 0; i < value.length; i++) {\n      hash = Math.imul(hash ^ value.charCodeAt(i), 16777619);\n    }\n    return (hash >>> 0) % this.size;\n  }\n\n  add(value: string): void {\n    this.bits[this.hash(value, 2166136261)] = 1;\n    this.bits[this.hash(value, 1315423911)] = 1;\n  }\n\n  mightContain(value: string): boolean {\n    return this.bits[this.hash(value, 2166136261)] === 1 &&\n      this.bits[this.hash(value, 1315423911)] === 1;\n  }\n}\n```\n```ts\nfunction exactMembership(values: readonly string[], value: string): boolean {\n  return new Set(values).has(value);\n}\n```\n\nExample takeaway: choose the data structure from the required operations and constraints.",
    productionExample: "Use this structure when its operation guarantees justify the added memory and implementation complexity.",
    bestPractices: ["Define the required operation before choosing the structure.", "State the invariant and complexity before coding.", "Test empty, one-item, and boundary cases.", "Prefer the simplest structure that satisfies the workload.", "Keep helper types self-contained and indexing explicit."],
    tradeOffs: "Choose advanced structures only when their query/update guarantees provide a meaningful benefit over Array, Set, Map, or sorted data.",
    commonMistakes: ["Choosing an advanced structure without a matching workload.", "Ignoring memory overhead.", "Mixing exact and probabilistic guarantees.", "Forgetting boundary cases.", "Leaving helper types undeclared."],
    followUpQuestions: ["Why is this structure appropriate?", "What is the invariant?", "What are the time and space complexities?", "What simpler alternative exists?", "When should you not use it?", "How do you test edge cases?", "How does scale change the choice?", "How do you reduce memory?", "How would you support updates?", "How would you adapt it for production?"],
    relatedTopics: ["Trie", "Segment Tree", "Fenwick Tree", "Union Find", "LRU Cache", "Bloom Filter", "Advanced Data Structures"],
  },
  {
    id: "dsa-m18-180",
    number: "DSA-M18-180",
    title: "Advanced structure selection",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Trie, Segment Tree & Advanced Structures",
    expectedAnswer: "Choose the simplest structure that matches required operations: Trie for prefixes, Segment Tree for dynamic range aggregates, Fenwick Tree for prefix/range sums with point updates, Union Find for connectivity, LRU for recency eviction, and Bloom Filter for approximate membership.",
    deepExplanation: "Step 1 — Understand the operation and invariant.\n\n```ts\ntype StructureChoice =\n  | \"Trie\"\n  | \"SegmentTree\"\n  | \"FenwickTree\"\n  | \"UnionFind\"\n  | \"LRUCache\"\n  | \"BloomFilter\";\n\nfunction chooseStructure(input: {\n  prefixSearch?: boolean;\n  dynamicRange?: boolean;\n  rangeSum?: boolean;\n  connectivity?: boolean;\n  recencyCache?: boolean;\n  approximateMembership?: boolean;\n}): StructureChoice {\n  if (input.prefixSearch) return \"Trie\";\n  if (input.connectivity) return \"UnionFind\";\n  if (input.recencyCache) return \"LRUCache\";\n  if (input.approximateMembership) return \"BloomFilter\";\n  if (input.rangeSum) return \"FenwickTree\";\n  if (input.dynamicRange) return \"SegmentTree\";\n  return \"Trie\";\n}\n```\n```ts\nfunction chooseSimpleStructure(\n  exactLookup: boolean,\n  orderedRange: boolean,\n): \"Set\" | \"Sorted Array\" | \"Array\" {\n  if (exactLookup) return \"Set\";\n  if (orderedRange) return \"Sorted Array\";\n  return \"Array\";\n}\n```\n\nExample takeaway: choose the data structure from the required operations and constraints.",
    productionExample: "Use this structure when its operation guarantees justify the added memory and implementation complexity.",
    bestPractices: ["Define the required operation before choosing the structure.", "State the invariant and complexity before coding.", "Test empty, one-item, and boundary cases.", "Prefer the simplest structure that satisfies the workload.", "Keep helper types self-contained and indexing explicit."],
    tradeOffs: "Choose advanced structures only when their query/update guarantees provide a meaningful benefit over Array, Set, Map, or sorted data.",
    commonMistakes: ["Choosing an advanced structure without a matching workload.", "Ignoring memory overhead.", "Mixing exact and probabilistic guarantees.", "Forgetting boundary cases.", "Leaving helper types undeclared."],
    followUpQuestions: ["Why is this structure appropriate?", "What is the invariant?", "What are the time and space complexities?", "What simpler alternative exists?", "When should you not use it?", "How do you test edge cases?", "How does scale change the choice?", "How do you reduce memory?", "How would you support updates?", "How would you adapt it for production?"],
    relatedTopics: ["Trie", "Segment Tree", "Fenwick Tree", "Union Find", "LRU Cache", "Bloom Filter", "Advanced Data Structures"],
  },
];

export const MOCK_DSA_MODULE18_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'DSA',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));