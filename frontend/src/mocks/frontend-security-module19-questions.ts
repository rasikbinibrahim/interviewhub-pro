// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 19 — Real-world Security Scenarios).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m19-306",
    number: "SEC-M19-306",
    title: "Stored XSS incident",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Stored XSS incident** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// ❌ Vulnerable: untrusted HTML is interpreted by the browser.\n// element.innerHTML = userInput;\n\n// ✅ Secure default: render text as text.\nexport function SafeMessage({ message }: { message: string }) {\n  return <p>{message}</p>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-307",
    number: "SEC-M19-307",
    title: "Broken access control incident",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Broken access control incident** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-308",
    number: "SEC-M19-308",
    title: "JWT leakage",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**JWT leakage** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-309",
    number: "SEC-M19-309",
    title: "Refresh token compromise",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Refresh token compromise** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-310",
    number: "SEC-M19-310",
    title: "CORS misconfiguration",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**CORS misconfiguration** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-311",
    number: "SEC-M19-311",
    title: "CSRF incident",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**CSRF incident** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-312",
    number: "SEC-M19-312",
    title: "CSP regression",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**CSP regression** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Feature-detect Trusted Types before using the API.\nconst policy = globalThis.trustedTypes?.createPolicy(\"app\", {\n  createHTML: (input) => {\n    // Production policy should sanitize with an approved sanitizer.\n    return input;\n  },\n});\n\nconsole.log(Boolean(policy));\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-313",
    number: "SEC-M19-313",
    title: "Clickjacking incident",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Clickjacking incident** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-314",
    number: "SEC-M19-314",
    title: "Dependency confusion incident",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Dependency confusion incident** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-315",
    number: "SEC-M19-315",
    title: "Typosquatting incident",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Typosquatting incident** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-316",
    number: "SEC-M19-316",
    title: "Malicious third-party script",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Malicious third-party script** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-317",
    number: "SEC-M19-317",
    title: "Secret exposure",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Secret exposure** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-318",
    number: "SEC-M19-318",
    title: "Open redirect",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Open redirect** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-319",
    number: "SEC-M19-319",
    title: "Prototype pollution",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Prototype pollution** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-320",
    number: "SEC-M19-320",
    title: "Insecure upload",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Insecure upload** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-321",
    number: "SEC-M19-321",
    title: "Session fixation",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Session fixation** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-322",
    number: "SEC-M19-322",
    title: "Account takeover",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Account takeover** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-323",
    number: "SEC-M19-323",
    title: "Supply-chain compromise",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Supply-chain compromise** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-324",
    number: "SEC-M19-324",
    title: "Production incident response",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Production incident response** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m19-325",
    number: "SEC-M19-325",
    title: "Security architecture review",
    difficulty: "Medium",
    experienceLevel: "12+ Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Real-world Security Scenarios",
    expectedAnswer: "**Security architecture review** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE19_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
