// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 7 — Redux Testing).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m7-1",
    number: "TESTQA-M7-1",
    title: "Reducers",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Reducers** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Reducers** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-2",
    number: "TESTQA-M7-2",
    title: "Actions",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Actions** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Actions** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-3",
    number: "TESTQA-M7-3",
    title: "Slices",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Slices** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Slices** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-4",
    number: "TESTQA-M7-4",
    title: "Selectors",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Selectors** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Selectors** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-5",
    number: "TESTQA-M7-5",
    title: "Async Thunks",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Async Thunks** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Async Thunks** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-6",
    number: "TESTQA-M7-6",
    title: "RTK Query",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**RTK Query** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**RTK Query** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-7",
    number: "TESTQA-M7-7",
    title: "Middleware",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Middleware** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Middleware** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-8",
    number: "TESTQA-M7-8",
    title: "Store Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Store Testing** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Store Testing** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-9",
    number: "TESTQA-M7-9",
    title: "Authentication State",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Authentication State** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Authentication State** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-10",
    number: "TESTQA-M7-10",
    title: "Loading and Error States",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Loading and Error States** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Loading and Error States** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-11",
    number: "TESTQA-M7-11",
    title: "API Mocking",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**API Mocking** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**API Mocking** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-12",
    number: "TESTQA-M7-12",
    title: "Integration Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Integration Testing** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Integration Testing** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-13",
    number: "TESTQA-M7-13",
    title: "State Normalization",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**State Normalization** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**State Normalization** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-14",
    number: "TESTQA-M7-14",
    title: "Redux Test Utilities",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Redux Test Utilities** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Redux Test Utilities** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-15",
    number: "TESTQA-M7-15",
    title: "Enterprise Redux Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Enterprise Redux Testing** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Enterprise Redux Testing** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
  {
    id: "testqa-m7-16",
    number: "TESTQA-M7-16",
    title: "Redux Testing — Module Review",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Redux Testing",
    expectedAnswer: "**Redux Testing — Module Review** should test pure state transitions at the reducer level and store/UI behavior at the integration level.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Redux Testing — Module Review** should test pure state transitions at the reducer level and store/UI behavior at the integration level.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface CounterState { value: number; }\n\ntype Action =\n  | { type: \"increment\" }\n  | { type: \"decrement\" };\n\nfunction reducer(state: CounterState, action: Action): CounterState {\n  if (action.type === \"increment\") return { value: state.value + 1 };\n  return { value: state.value - 1 };\n}\n\nconst next = reducer({ value: 0 }, { type: \"increment\" });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { value: 0 };\n\nconst next = { value: state.value + 1 };\n```\n\nStep 5 — Production example:\nUse reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use reducer tests for pure state logic and integration tests for store, middleware, and UI behavior.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Redux Testing"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE7_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));