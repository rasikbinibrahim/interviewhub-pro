// Derived from frontend/src/document/DSA_Master_Handbook_Senior_Frontend_Engineers.md (Module 1 — DSA Fundamentals).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Definition, JavaScript/TypeScript implementation, Time/Space Complexity,
// Trade-offs, Common Mistakes, Interview Tricks, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  'Google',
  'Meta',
  'Amazon',
  'Microsoft',
  'Apple',
  'Netflix',
  'Uber',
  'Airbnb',
  'Atlassian',
  'Adobe',
  'LinkedIn',
  'Flipkart',
  'Zoho',
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};

const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "dsa-m1-1",
    number: "DSA-M1-1",
    title: "Big-O, Big-Theta and Big-Omega",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DSA Fundamentals",
    expectedAnswer: "Big-O describes an asymptotic upper bound, Big-Theta describes a tight asymptotic bound, and Big-Omega describes a lower bound. In interviews, use them to explain how runtime or memory grows as input size n grows.",
    deepExplanation: "Big-O answers: “How bad can growth get?”\nBig-Theta answers: “What is the tight growth rate?”\nBig-Omega answers: “What lower growth bound does the algorithm have?”\n\nExample:\n```ts\nfunction sumArray(nums: readonly number[]): number {\n  let sum = 0;\n  for (let i = 0; i < nums.length; i++) {\n    sum += nums[i]!;\n  }\n  return sum;\n}\n```\n\nThe loop visits every element once, so the time complexity is Θ(n), which also means O(n) and Ω(n). Extra variables are constant, so auxiliary space is O(1).\n\nEasy interview rule: count how many times the main work scales with n. One loop is usually O(n); two nested loops are usually O(n²); halving the search space is O(log n); sorting is typically O(n log n).\n\nWithout built-in helpers:\n```ts\nfunction containsValue(nums: readonly number[], target: number): boolean {\n  for (let i = 0; i < nums.length; i++) {\n    if (nums[i] === target) return true;\n  }\n  return false;\n}\n```\nBuilt-in-friendly alternative:\n```ts\nfunction containsValueUsingBuiltIns(nums: readonly number[], target: number): boolean {\n  return nums.includes(target);\n}\n```",
    productionExample: "Frontend example: when rendering a list of n records, a single pass is O(n). Re-running a full scan inside another loop can become O(n²), which is why lookup maps, memoization, or precomputed indexes can matter at scale.",
    bestPractices: ["State the input size n before giving complexity.", "Distinguish time complexity from auxiliary space.", "State average-case assumptions for hash tables.", "Ignore constants and lower-order terms in asymptotic notation.", "Explain the bottleneck in plain English before naming O(n), O(log n), etc.", "Use examples to prove the stated complexity."],
    tradeOffs: "| Choice | Benefit | Cost |\n|---|---|---|\n| O(1) extra space | Lower memory pressure | May require more careful in-place logic |\n| Hash lookup | O(1) average lookup | Extra memory |\n| Sorting first | Enables binary search/order-based strategies | O(n log n) preprocessing |",
    commonMistakes: ["Saying Big-O means exact runtime.", "Confusing O(n) with O(1).", "Ignoring nested loops.", "Forgetting recursion stack space.", "Claiming hash lookup is always O(1).", "Including constants such as 2n as a different asymptotic class."],
    followUpQuestions: ["What is the difference between O(n) and Θ(n)?", "Why is binary search O(log n)?", "What is the space complexity of recursive DFS?", "Why is sorting usually O(n log n)?", "What is amortized complexity?", "How do you analyze a loop that halves n each time?", "What changes if input is already sorted?", "How do you compare two algorithms with different time/space trade-offs?", "What is average versus worst-case hashing?", "How would you explain complexity to a non-technical interviewer?"],
    relatedTopics: ["Time Complexity", "Space Complexity", "Binary Search", "Hashing", "Recursion"],
  },
  {
    id: "dsa-m1-2",
    number: "DSA-M1-2",
    title: "Space complexity and auxiliary space",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DSA Fundamentals",
    expectedAnswer: "Space complexity measures how memory usage grows with input size. Auxiliary space means the extra working memory used by the algorithm, excluding the input storage itself.",
    deepExplanation: "Consider:\n```ts\nfunction copyArray(nums: readonly number[]): number[] {\n  const result: number[] = [];\n  for (let i = 0; i < nums.length; i++) {\n    result.push(nums[i]!);\n  }\n  return result;\n}\n```\nThe returned copy uses O(n) additional space.\n\nIn-place version:\n```ts\nfunction reverseInPlace(nums: number[]): void {\n  let left = 0;\n  let right = nums.length - 1;\n  while (left < right) {\n    const temp = nums[left]!;\n    nums[left] = nums[right]!;\n    nums[right] = temp;\n    left++;\n    right--;\n  }\n}\n```\nThis uses O(1) auxiliary space.\n\nRecursion also consumes stack space. A recursion depth of n can use O(n) call-stack space even if no array or map is allocated.",
    productionExample: "Frontend example: repeatedly cloning large arrays/objects can increase garbage collection and memory pressure. Prefer controlled mutation when the API contract allows it, or use structural sharing when immutability is required.",
    bestPractices: ["Separate input memory from auxiliary memory.", "Count arrays, maps, sets, object allocations, and recursion depth.", "Mention whether a helper creates copies.", "Watch for hidden allocations from spread, slice, filter, map, and string operations.", "Prefer O(1) auxiliary space when it materially improves scale without hurting correctness."],
    tradeOffs: "| Strategy | Benefit | Cost |\n|---|---|---|\n| In-place | O(1) extra space | Mutates input |\n| Copying | Easier reasoning / immutability | O(n) memory |\n| Recursion | Clear code | Call-stack usage |\n| Iteration | Predictable stack use | Sometimes more verbose |",
    commonMistakes: ["Calling input size itself auxiliary space.", "Forgetting recursion stack.", "Ignoring temporary arrays created by helpers.", "Assuming Map/Set is free.", "Treating immutable copies as O(1) space."],
    followUpQuestions: ["What is auxiliary space?", "Why can recursion be O(n) space?", "When is copying preferable to mutation?", "Can this O(n) solution be made O(1) extra space?", "What is in-place processing?", "How does garbage collection affect practical memory usage?", "What is the difference between stack and heap memory?", "How would you measure memory in production?", "What hidden allocations can JavaScript helpers create?", "How does space complexity affect large frontend lists?"],
    relatedTopics: ["Space Complexity", "Recursion", "Arrays", "Memory Management", "Immutability"],
  },
  {
    id: "dsa-m1-3",
    number: "DSA-M1-3",
    title: "Arrays and contiguous memory",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DSA Fundamentals",
    expectedAnswer: "Arrays store indexed elements and are especially useful when you need fast random access by position. In algorithm interviews, contiguous or index-addressable access often enables two pointers, sliding windows, prefix sums, and binary search.",
    deepExplanation: "Array access by index is typically O(1). Traversing an array is O(n).\n\nManual example:\n```ts\nfunction maxValue(nums: readonly number[]): number {\n  if (nums.length === 0) throw new Error(\"empty array\");\n  let best = nums[0]!;\n  for (let i = 1; i < nums.length; i++) {\n    if (nums[i]! > best) best = nums[i]!;\n  }\n  return best;\n}\n```\n\nBuilt-in-friendly version:\n```ts\nfunction maxValueUsingBuiltIns(nums: readonly number[]): number {\n  return Math.max(...nums);\n}\n```\n\nCommon array patterns include two pointers, sliding window, prefix sum, binary search, and monotonic stacks. The right pattern depends on the ordering and query requirements.",
    productionExample: "Frontend example: rendering API results is naturally array-based. For frequent ID lookups, convert the data to a Map rather than repeatedly scanning the array with find().",
    bestPractices: ["Know index-based access is O(1).", "Be careful with insert/remove at the front or middle.", "Prefer pointers over repeated copying when possible.", "Use a Map/Set for frequent membership lookups.", "Clarify whether mutation is allowed."],
    tradeOffs: "| Choice | Benefit | Cost |\n|---|---|---|\n| Array | Fast indexed access | Middle/front insertion can be expensive |\n| Set | Fast average membership | No direct index |\n| Map | Fast key lookup | Extra memory |",
    commonMistakes: ["Using shift() repeatedly in large loops.", "Off-by-one errors.", "Mutating arrays unexpectedly.", "Forgetting empty-array handling.", "Using nested scans where a map or pointer technique is enough."],
    followUpQuestions: ["When is an array better than a linked list?", "Why is shift() slower than pop()?", "What is the difference between slice and splice?", "How do two pointers reduce complexity?", "What is a prefix sum?", "When should you convert an array to a Map?", "What is the complexity of push/pop/index access?", "How do typed arrays differ from normal arrays?", "How would you remove duplicates?", "How would you process a streaming array?"],
    relatedTopics: ["Arrays", "Two Pointers", "Sliding Window", "Prefix Sum", "Hashing"],
  },
  {
    id: "dsa-m1-4",
    number: "DSA-M1-4",
    title: "Linked structures and references",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DSA Fundamentals",
    expectedAnswer: "Linked structures connect nodes through references rather than relying on contiguous indexed storage. They are useful when frequent insertion/removal around known nodes matters more than random access.",
    deepExplanation: `A singly linked node can be modeled as:\n\`\`\`ts\ninterface ListNode {\n  value: number;\n  next: ListNode | null;\n}\n\`\`\`\n\nManual traversal:\n\`\`\`ts\ninterface ListNode {
  value: number;
  next: ListNode | null;
}

function lengthOfList(head: ListNode | null): number {\n  let count = 0;\n  let node = head;\n  while (node !== null) {\n    count++;\n    node = node.next;\n  }\n  return count;\n}\n\`\`\`\n\nRandom access is O(n), because you must follow references from the head. Inserting after a known node can be O(1).`,
    productionExample: "Frontend example: linked structures are uncommon as the default UI data type, but they appear in LRU caches, undo/redo chains, and custom schedulers where node relationships matter.",
    bestPractices: ["Keep node ownership clear.", "Handle null/head cases explicitly.", "Use iteration when recursion depth could be large.", "Know that random access is O(n).", "Explain insertion/removal complexity based on whether the node is already known."],
    tradeOffs: "| Choice | Benefit | Cost |\n|---|---|---|\n| Linked list | O(1) insertion/removal at known node | O(n) access/search |\n| Array | O(1) indexed access | Middle/front updates can be costly |\n| Doubly linked list | O(1) removal with previous/next references | More memory per node |",
    commonMistakes: ["Losing the next reference during mutation.", "Forgetting head/tail updates.", "Assuming linked-list access is O(1).", "Creating cycles accidentally.", "Ignoring null handling."],
    followUpQuestions: ["How do you reverse a linked list?", "How do you detect a cycle?", "When would you use a doubly linked list?", "How does an LRU cache use a linked list?", "What is the fast/slow pointer technique?", "How do you merge two sorted lists?", "How do you find the middle node?", "How do you remove the nth node from the end?", "Why is array indexing faster?", "What are practical frontend uses of linked structures?"],
    relatedTopics: ["Linked Lists", "Two Pointers", "LRU Cache", "References"],
  },
  {
    id: "dsa-m1-5",
    number: "DSA-M1-5",
    title: "Stack and queue fundamentals",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DSA Fundamentals",
    expectedAnswer: "A stack is LIFO (last in, first out); a queue is FIFO (first in, first out). They are foundational for recursion, parsing, BFS, scheduling, and monotonic-stack problems.",
    deepExplanation: "Stack without a built-in helper method:\n```ts\nfunction reverseStringManual(value: string): string {\n  const stack: string[] = [];\n  for (let i = 0; i < value.length; i++) stack.push(value[i]!);\n\n  let result = \"\";\n  while (stack.length > 0) result += stack.pop()!;\n  return result;\n}\n```\n\nQueue with an index instead of shift():\n```ts\nfunction processQueue(values: readonly number[]): number[] {\n  const queue = [...values];\n  let front = 0;\n  const result: number[] = [];\n  while (front < queue.length) {\n    result.push(queue[front++]!);\n  }\n  return result;\n}\n```\n\nUsing shift() repeatedly can cause unnecessary array movement, so a front pointer is often preferable in interview code.",
    productionExample: "Frontend example: a queue can model pending network tasks; a stack models browser history or undo steps. BFS uses a queue, while DFS can use recursion or an explicit stack.",
    bestPractices: ["Use push/pop for stack behavior.", "Use a front pointer for array-backed queues.", "State LIFO vs FIFO clearly.", "Avoid repeated shift() in large queues.", "Define whether empty operations return undefined, null, or throw."],
    tradeOffs: "| Structure | Best for | Typical core operations |\n|---|---|---|\n| Stack | Undo, parsing, DFS | push/pop |\n| Queue | Scheduling, BFS | enqueue/dequeue |\n| Deque | Both ends | front/back operations |",
    commonMistakes: ["Using FIFO logic where LIFO is required.", "Using shift() in a large BFS.", "Forgetting empty-stack cases.", "Mixing queue order with priority-queue behavior."],
    followUpQuestions: ["How do stacks support recursion?", "Why is BFS naturally queue-based?", "How do you implement a queue without shift()?", "What is a deque?", "How does a monotonic stack work?", "How would you implement Min Stack?", "What is the difference between BFS and DFS?", "Where are stacks used in JavaScript execution?", "How do you test queue boundaries?", "When should you use a heap instead of a queue?"],
    relatedTopics: ["Stack", "Queue", "BFS", "DFS", "Deque", "Heap"],
  },
  {
    id: "dsa-m1-6",
    number: "DSA-M1-6",
    title: "Recursion fundamentals",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DSA Fundamentals",
    expectedAnswer: "Recursion solves a problem by reducing it to smaller instances of the same problem until a base case is reached. A correct recursive solution needs a base case, a smaller recursive call, and progress toward termination.",
    deepExplanation: "Example:\n```ts\nfunction factorial(n: number): number {\n  if (n <= 1) return 1;       // base case\n  return n * factorial(n - 1); // smaller problem\n}\n```\n\nWithout built-in helpers:\n```ts\nfunction sumToN(n: number): number {\n  if (n <= 0) return 0;\n  return n + sumToN(n - 1);\n}\n```\n\nIterative equivalent:\n```ts\nfunction sumToNIterative(n: number): number {\n  let sum = 0;\n  for (let i = 1; i <= n; i++) sum += i;\n  return sum;\n}\n```\n\nRecursion is especially natural for trees, divide-and-conquer, backtracking, and DFS. Its main practical risk in JavaScript is deep call-stack usage.",
    productionExample: "Frontend example: recursive tree rendering and menu traversal are natural use cases. For very deep user-generated trees, iterative traversal may be safer to avoid call-stack limits.",
    bestPractices: ["Write the base case first.", "Make each recursive call smaller.", "State the recursion invariant.", "Estimate maximum depth.", "Use iteration when input depth can be very large.", "Avoid unnecessary object/array creation on every call."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| Recursion | Clear for hierarchical problems | Call-stack usage |\n| Iteration | Predictable memory | Sometimes more bookkeeping |\n| Memoization | Avoids repeated work | Extra memory |",
    commonMistakes: ["Missing base case.", "Recursive call does not make progress.", "Recomputing the same subproblem.", "Ignoring recursion depth.", "Mutating shared state incorrectly during backtracking."],
    followUpQuestions: ["What is the base case?", "How do you calculate recursion-stack space?", "What is tail recursion?", "Why can naive Fibonacci be exponential?", "How does memoization help?", "When should recursion be replaced with iteration?", "How does DFS use recursion?", "What is backtracking?", "How do you debug recursive functions?", "How do you prevent shared-state bugs?"],
    relatedTopics: ["Recursion", "DFS", "Backtracking", "Dynamic Programming", "Trees"],
  },
  {
    id: "dsa-m1-7",
    number: "DSA-M1-7",
    title: "Hash tables",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DSA Fundamentals",
    expectedAnswer: "A hash table stores key-value pairs and uses a hash function to choose where a key is stored. In JavaScript, Map is the general-purpose built-in hash-table-like structure.",
    deepExplanation: "Frequency-count example without Map:\n```ts\nfunction countCharactersManual(value: string): Record<string, number> {\n  const count: Record<string, number> = Object.create(null);\n  for (let i = 0; i < value.length; i++) {\n    const ch = value[i]!;\n    count[ch] = (count[ch] ?? 0) + 1;\n  }\n  return count;\n}\n```\n\nBuilt-in version:\n```ts\nfunction countCharactersUsingMap(value: string): Map<string, number> {\n  const count = new Map<string, number>();\n  for (const ch of value) {\n    count.set(ch, (count.get(ch) ?? 0) + 1);\n  }\n  return count;\n}\n```\n\nAverage-case lookup/insert/delete is typically O(1), but worst-case behavior depends on collisions and implementation. Hash tables trade memory for fast membership and frequency queries.",
    productionExample: "Frontend example: normalize API records by ID into a Map so rendering code can retrieve items in average O(1) instead of repeatedly scanning an array.",
    bestPractices: ["State that O(1) is average-case.", "Choose keys with stable equality semantics.", "Use Map when non-string keys or explicit key semantics matter.", "Avoid converting large collections back and forth unnecessarily.", "Consider memory usage for large tables."],
    tradeOffs: "| Structure | Benefit | Cost |\n|---|---|---|\n| Map | Fast average lookup, flexible keys | Extra memory |\n| Object | Simple string-key lookup | Prototype/key semantics |\n| Array | Ordered iteration/index access | O(n) search unless indexed separately |",
    commonMistakes: ["Claiming worst-case O(1).", "Forgetting collisions conceptually.", "Using object keys without considering inherited properties.", "Building a Map when an array scan is enough for tiny input.", "Ignoring memory cost."],
    followUpQuestions: ["What causes collisions?", "How are collisions handled?", "Map vs Object?", "Why is Set useful?", "What is a frequency map?", "How do hashes support Two Sum?", "What is load factor?", "What happens with custom objects as keys?", "What is average vs worst-case lookup?", "How would you serialize a Map?"],
    relatedTopics: ["Hashing", "Map", "Set", "Frequency Counting", "Two Sum"],
  },
  {
    id: "dsa-m1-8",
    number: "DSA-M1-8",
    title: "Trees and graphs overview",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DSA Fundamentals",
    expectedAnswer: "Trees are connected acyclic hierarchical structures; graphs are general networks that may contain cycles and multiple paths. Common traversal choices are DFS and BFS.",
    deepExplanation: "Tree DFS example:\n```ts\ninterface Node {\n  value: number;\n  left: Node | null;\n  right: Node | null;\n}\n\nfunction sumTree(root: Node | null): number {\n  if (root === null) return 0;\n  return root.value + sumTree(root.left) + sumTree(root.right);\n}\n```\n\nGraph BFS without shift():\n```ts\nfunction bfs(start: number, graph: readonly number[][]): number[] {\n  const queue = [start];\n  const visited = new Set<number>([start]);\n  const order: number[] = [];\n  let front = 0;\n\n  while (front < queue.length) {\n    const node = queue[front++]!;\n    order.push(node);\n    for (const next of graph[node] ?? []) {\n      if (!visited.has(next)) {\n        visited.add(next);\n        queue.push(next);\n      }\n    }\n  }\n  return order;\n}\n```\n\nBuilt-in-friendly alternatives can use Map/Set for adjacency and visited tracking.",
    productionExample: "Frontend example: DOM/component trees are naturally hierarchical, while dependency graphs and workflow graphs are general graph problems. Choosing DFS vs BFS depends on whether you need depth-first exploration or shortest-edge-distance layering.",
    bestPractices: ["Identify whether the structure is a tree or graph.", "For graphs, decide whether cycles exist.", "Track visited nodes when cycles are possible.", "Choose BFS for unweighted shortest-path distance.", "Choose DFS for exhaustive traversal/component exploration."],
    tradeOffs: "| Technique | Benefit | Cost |\n|---|---|---|\n| DFS | Natural recursive structure | Stack depth |\n| BFS | Layered traversal / shortest edges | Queue memory |\n| Adjacency list | Memory efficient for sparse graphs | Neighbor lookup requires traversal |",
    commonMistakes: ["Assuming a graph is acyclic.", "Forgetting visited tracking.", "Using DFS when shortest unweighted distance is required.", "Using recursion on extremely deep graphs.", "Confusing node count with edge count."],
    followUpQuestions: ["DFS vs BFS?", "How do you detect a cycle?", "How do you find connected components?", "How do you find shortest path in an unweighted graph?", "What is topological sorting?", "What is a tree's degree?", "How do you serialize a tree?", "Adjacency list vs matrix?", "When is recursion unsafe for DFS?", "How would you test graph traversal?"],
    relatedTopics: ["Trees", "Graphs", "BFS", "DFS", "Shortest Path", "Topological Sort"],
  },
  {
    id: "dsa-m1-9",
    number: "DSA-M1-9",
    title: "Choosing the right data structure",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DSA Fundamentals",
    expectedAnswer: "Choose the data structure by starting from the operations you need most often: indexed access, lookup, insertion, deletion, ordering, uniqueness, priority, or traversal. Optimize for the dominant operation rather than picking the most familiar structure.",
    deepExplanation: "Decision guide:\n- Need index access → Array.\n- Need key lookup → Map.\n- Need uniqueness/membership → Set.\n- Need LIFO → Stack.\n- Need FIFO → Queue.\n- Need min/max priority → Heap.\n- Need shared prefixes → Trie.\n- Need hierarchy → Tree.\n- Need general relationships → Graph.\n\nExample: suppose you repeatedly search users by ID.\nManual array scan:\n```ts\nfunction findUser(users: readonly { id: string }[], id: string) {\n  for (const user of users) {\n    if (user.id === id) return user;\n  }\n  return undefined;\n}\n```\nThis is O(n) per lookup.\n\nMap-backed approach:\n```ts\nfunction indexUsers(users: readonly { id: string }[]): Map<string, { id: string }> {\n  const index = new Map<string, { id: string }>();\n  for (const user of users) index.set(user.id, user);\n  return index;\n}\n```\nThen average lookup is O(1) after O(n) preprocessing.",
    productionExample: "Frontend example: a list may remain an Array for ordered rendering but be paired with a Map by ID for fast updates. One structure does not have to do every job.",
    bestPractices: ["List dominant operations before choosing a structure.", "Separate read-heavy and write-heavy requirements.", "State preprocessing costs.", "Consider memory overhead.", "Use combinations such as Array + Map when appropriate."],
    tradeOffs: "| Requirement | Good default | Trade-off |\n|---|---|---|\n| Ordered sequence | Array | Search is O(n) |\n| Key lookup | Map | Extra memory |\n| Unique values | Set | No index |\n| Priority | Heap | More complex than array sort for dynamic updates |\n| Prefix queries | Trie | More nodes/memory |",
    commonMistakes: ["Choosing data structure by habit.", "Ignoring the dominant operation.", "Ignoring initialization cost.", "Ignoring memory overhead.", "Using a heap where simple sorting is enough for one-time data."],
    followUpQuestions: ["Why Map instead of Array.find()?", "When is Set better than an Array?", "When should you use a heap?", "When does a Trie make sense?", "Array vs linked list?", "Map vs Object?", "What if both ordering and lookup are required?", "How do you evaluate memory overhead?", "What changes for streaming data?", "How do you benchmark a choice?"],
    relatedTopics: ["Arrays", "Map", "Set", "Stack", "Queue", "Heap", "Trie", "Trees", "Graphs"],
  },
  {
    id: "dsa-m1-10",
    number: "DSA-M1-10",
    title: "Pattern recognition for interviews",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DSA Fundamentals",
    expectedAnswer: "Pattern recognition means identifying the reusable structure behind a problem—such as two pointers, sliding window, hash lookup, binary search, stack, heap, DFS/BFS, recursion, or dynamic programming—before writing code.",
    deepExplanation: "A practical interview checklist:\n1. What is the input structure?\n2. Is it sorted or nearly sorted?\n3. Is the answer about a contiguous range?\n4. Do I need fast membership/frequency lookup?\n5. Is there a pair relationship?\n6. Is there a hierarchy or graph?\n7. Is there a monotonic or ordering invariant?\n8. Are there repeated subproblems?\n9. Is the objective a best/min/max/count value?\n10. What is the brute-force baseline?\n\nExample: Longest substring without repeating characters.\nBrute force can inspect many substrings. The key clue is “contiguous substring” + “no duplicates”, which points to Sliding Window + Hash Map.\n\nCore version idea:\n```ts\nfunction lengthOfLongestSubstring(s: string): number {\n  const lastSeen: Record<string, number> = Object.create(null);\n  let left = 0;\n  let best = 0;\n\n  for (let right = 0; right < s.length; right++) {\n    const index = lastSeen[s[right]!];\n    if (index !== undefined && index >= left) left = index + 1;\n    lastSeen[s[right]!] = right;\n    best = Math.max(best, right - left + 1);\n  }\n  return best;\n}\n```\nThe pattern becomes obvious once you translate the words “longest”, “substring”, and “without repeating” into a window invariant.",
    productionExample: "Frontend example: when filtering/searching rapidly in a UI, recognizing a sliding window or memoization opportunity can be more useful than blindly applying nested loops.",
    bestPractices: ["Start with brute force.", "Underline words such as sorted, contiguous, longest, minimum, unique, frequency, prefix, path, and k.", "State the invariant before coding.", "Validate the pattern against edge cases.", "Do not force a pattern when its invariant does not hold."],
    tradeOffs: "| Pattern | Typical use | Common trade-off |\n|---|---|---|\n| Two pointers | Sorted arrays / opposite ends | Requires useful ordering or monotonic movement |\n| Sliding window | Contiguous range | Window invariant must be maintainable |\n| Hash map | Fast lookup/frequency | Extra memory |\n| Binary search | Monotonic decision | Requires ordering/monotonicity |\n| DFS/BFS | Trees/graphs | Visited/stack/queue memory |",
    commonMistakes: ["Jumping to a pattern without stating the invariant.", "Ignoring brute-force baseline.", "Using sliding window when negatives break monotonicity.", "Using binary search without a monotonic condition.", "Forgetting visited state in graph traversal.", "Confusing substring with subsequence."],
    followUpQuestions: ["How do you recognize sliding window?", "How do you recognize two pointers?", "When is binary search applicable?", "How do you spot a hash-map problem?", "How do you spot a monotonic-stack problem?", "When is BFS preferable to DFS?", "How do you recognize DP?", "How do you recognize backtracking?", "How do you turn brute force into an optimized solution?", "How do you communicate the pattern in an interview?"],
    relatedTopics: ["Two Pointers", "Sliding Window", "Hashing", "Binary Search", "Stack", "Heap", "DFS", "BFS", "Dynamic Programming"],
  },
];

export const MOCK_DSA_MODULE1_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'DSA',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));