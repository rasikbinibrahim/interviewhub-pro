// Derived from frontend/src/document/Part_9_Advanced_React_Master_Handbook.md (Module 5 — Error Handling).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "reactadv-m5-81",
    number: "REACTADV-M5-81",
    title: "Error boundaries",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "An error boundary is a React class component that catches errors thrown during rendering, lifecycle methods, and constructors in its descendant tree, then renders fallback UI. It does not catch every kind of failure: event-handler errors, arbitrary async callback errors, server-side errors, or errors thrown by the boundary itself need other handling strategies.",
    deepExplanation: "Use a boundary around an independently recoverable UI region. `getDerivedStateFromError` determines fallback state; `componentDidCatch` is the appropriate place for logging/telemetry.\n\n```tsx\nclass ErrorBoundary extends Component<\n  { children: ReactNode; fallback?: ReactNode },\n  { hasError: boolean }\n> {\n  state = { hasError: false };\n\n  static getDerivedStateFromError() {\n    return { hasError: true };\n  }\n\n  componentDidCatch(error: Error, info: ErrorInfo) {\n    reportError(error, { componentStack: info.componentStack });\n  }\n\n  render() {\n    return this.state.hasError\n      ? this.props.fallback ?? <p role=\"alert\">Something went wrong.</p>\n      : this.props.children;\n  }\n}\n```\n\nWhy it matters: a boundary prevents one render-time failure from taking down an entire intended UI region and gives the product a controlled recovery experience.",
    productionExample: "Production example: wrap route-level or dashboard widgets separately so a broken recommendations widget can show a local fallback while the rest of the page remains usable.",
    bestPractices: ["Place boundaries at meaningful recovery boundaries, not around every component.","Keep fallback UI accessible and actionable.","Log the error with component stack and release/version metadata.","Do not expose stack traces or sensitive request data to users.","Provide a deliberate reset/retry strategy when the underlying problem may be transient."],
    tradeOffs: "Advantages: isolates render-time failures; provides controlled fallback UI; creates a natural telemetry boundary. Trade-offs: a boundary does not catch event-handler/ordinary async errors; too many boundaries complicate recovery behavior; a fallback can hide a systemic defect if telemetry is poor.",
    commonMistakes: ["Saying an error boundary catches all JavaScript errors.","Using `try/catch` around JSX render calls instead of a boundary.","Forgetting that the boundary itself can fail.","Treating fallback rendering as a complete recovery mechanism.","Not testing the boundary with a deliberately throwing child."],
    followUpQuestions: ["Which errors do error boundaries not catch?","Where should telemetry be recorded?","How would you reset a failed boundary?","How would you choose boundary placement?"],
    relatedTopics: ["getDerivedStateFromError","componentDidCatch","Fallback UI","Error isolation","Error telemetry","Recovery"],
  },
  {
    id: "reactadv-m5-82",
    number: "REACTADV-M5-82",
    title: "Render errors",
    difficulty: "Hard",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "Render errors occur when React encounters an exception while rendering a component or one of its descendants. A class error boundary can catch render-phase errors in its descendant tree and replace that subtree with fallback UI.",
    deepExplanation: "A render error is different from an API failure represented by normal component state. If a component throws because it cannot produce valid UI, the nearest error boundary can transition to its fallback. The boundary should remain small enough that recovery is meaningful.\n\n```tsx\nfunction Profile({ user }: { user: User }) {\n  if (!user.profile) {\n    throw new Error(\"Profile data is invalid\");\n  }\n  return <h1>{user.profile.displayName}</h1>;\n}\n\nfunction ProfileSection() {\n  return (\n    <ErrorBoundary fallback={<p role=\"alert\">Profile unavailable.</p>}>\n      <Profile user={user} />\n    </ErrorBoundary>\n  );\n}\n```\n\nThe important interview distinction is that the boundary catches the descendant render failure; it does not turn arbitrary business errors into render errors automatically.",
    productionExample: "Production example: isolate a complex analytics/chart widget so malformed data or a chart-library render exception does not blank the entire dashboard.",
    bestPractices: ["Keep boundary scope aligned with a useful recovery unit.","Use meaningful, non-sensitive error messages in fallback UI.","Attach route/widget identifiers to telemetry.","Test both successful rendering and thrown-render fallback."],
    tradeOffs: "Advantages: clear isolation for render failures; simple user-visible recovery. Trade-offs: the failed subtree is replaced; retry requires an explicit reset/remount strategy; a boundary cannot repair invalid application state by itself.",
    commonMistakes: ["Calling a network 500 response a render error.","Assuming a boundary catches errors thrown in every callback.","Catching errors and silently returning null."],
    followUpQuestions: ["What happens when a child throws during render?","Can React continue rendering siblings?","How do you test a render error?"],
    relatedTopics: ["Render phase","Error boundaries","Component stack","Fallback UI","Testing"],
  },
  {
    id: "reactadv-m5-83",
    number: "REACTADV-M5-83",
    title: "Commit-time errors",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "Errors can also surface around commit-time work such as lifecycle methods and ref-related effects. React error boundaries are designed to handle errors from descendant lifecycle methods as well as render-related failures, but error handling should still distinguish React-managed failures from external side effects.",
    deepExplanation: "The commit phase applies the finished render and runs commit-related work. A production boundary should surround the component subtree whose lifecycle/effect failures can make that subtree unusable.\n\n```tsx\nclass Boundary extends Component<\n  { children: ReactNode },\n  { failed: boolean }\n> {\n  state = { failed: false };\n\n  static getDerivedStateFromError() {\n    return { failed: true };\n  }\n\n  componentDidCatch(error: Error, info: ErrorInfo) {\n    reportError(error, { componentStack: info.componentStack });\n  }\n\n  render() {\n    return this.state.failed\n      ? <p role=\"alert\">This section is unavailable.</p>\n      : this.props.children;\n  }\n}\n```\n\nDo not describe React's internal commit sequence as a public API contract; focus on the observable behavior and supported lifecycle/error-boundary APIs.",
    productionExample: "Production example: a third-party widget whose initialization throws is isolated behind a widget boundary while the surrounding application remains available.",
    bestPractices: ["Keep side effects guarded and cleanup-safe.","Use boundaries for component-tree failures and explicit try/catch for imperative APIs where appropriate.","Log enough context to reproduce the failure without sensitive payloads."],
    tradeOffs: "Advantages: protects a UI region from descendant lifecycle failures. Trade-offs: side effects outside React still require their own handling; commit failures can leave the affected subtree unusable and need recovery.",
    commonMistakes: ["Saying error boundaries only catch render functions.","Confusing render and commit phases.","Relying on private Fiber internals to explain behavior."],
    followUpQuestions: ["Which lifecycle errors are caught?","How do refs/effects differ from render errors?","What should be logged?"],
    relatedTopics: ["Render phase","Commit phase","Effects","Refs","Error boundaries"],
  },
  {
    id: "reactadv-m5-84",
    number: "REACTADV-M5-84",
    title: "Event handler errors",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "Errors thrown directly from event handlers are not caught by React error boundaries. Handle expected failures inside the event flow with try/catch or promise rejection handling, update UI state when recovery is possible, and send unexpected failures to telemetry.",
    deepExplanation: "Event handlers execute in response to user interaction, outside the render operation that error boundaries protect. Therefore the handler owns its operational error path.\n\n```tsx\nfunction SaveButton() {\n  const [error, setError] = useState<string | null>(null);\n\n  async function handleSave() {\n    setError(null);\n    try {\n      await saveProfile();\n    } catch (error) {\n      setError(\"We couldn't save your changes.\");\n      reportError(error);\n    }\n  }\n\n  return (\n    <>\n      <button onClick={handleSave}>Save</button>\n      {error && <p role=\"alert\">{error}</p>}\n    </>\n  );\n}\n```\n\nExpected user failures such as validation or conflict responses should usually become normal UI state rather than generic crash screens.",
    productionExample: "Production example: a payment or save button catches API failures locally, disables duplicate submissions, shows an actionable message, and logs unexpected exceptions.",
    bestPractices: ["Catch promise rejections explicitly.","Separate expected business/API failures from programmer defects.","Prevent duplicate submissions where relevant.","Keep user messages safe and actionable.","Report unexpected failures with correlation/request IDs when available."],
    tradeOffs: "Advantages: gives the operation-specific UI control and clear recovery. Trade-offs: every async interaction needs deliberate failure handling; broad global handlers cannot replace local UX.",
    commonMistakes: ["Expecting an ErrorBoundary to catch onClick exceptions.","Using `catch {}` and silently swallowing errors.","Showing raw server exceptions to users."],
    followUpQuestions: ["How do you handle async handler rejection?","Which failures become form state?","When should an error be rethrown?"],
    relatedTopics: ["Event handlers","Promises","Form errors","Network errors","Telemetry"],
  },
  {
    id: "reactadv-m5-85",
    number: "REACTADV-M5-85",
    title: "Async error handling",
    difficulty: "Easy",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "Async errors from fetches, timers, subscriptions, or other callbacks need explicit ownership. Error boundaries do not automatically catch an exception that occurs later in an arbitrary async callback. Model asynchronous work with loading/success/error states or a data-fetching abstraction and handle cancellation/races.",
    deepExplanation: "A robust async state is explicit and tied to the request lifecycle.\n\n```tsx\ntype State =\n  | { status: \"idle\" }\n  | { status: \"loading\" }\n  | { status: \"success\"; data: User }\n  | { status: \"error\"; message: string };\n\nfunction UserPanel() {\n  const [state, setState] = useState<State>({ status: \"idle\" });\n\n  async function loadUser() {\n    setState({ status: \"loading\" });\n    try {\n      const data = await fetchUser();\n      setState({ status: \"success\", data });\n    } catch (error) {\n      reportError(error);\n      setState({ status: \"error\", message: \"Unable to load user.\" });\n    }\n  }\n\n  // render by state...\n}\n```\n\nFor production data fetching, a query/cache library can centralize retries, cancellation, stale data and error states.",
    productionExample: "Production example: a search screen treats network timeout, cancellation, stale response and server error as separate operational states rather than crashing the component.",
    bestPractices: ["Cancel obsolete requests where possible.","Prevent stale responses from overwriting newer state.","Distinguish cancellation from genuine failure.","Use retries only for appropriate transient failures.","Centralize server-state concerns when the application is large."],
    tradeOffs: "Advantages: explicit and predictable async UX. Trade-offs: manual state machines can become verbose; data libraries add abstraction but reduce duplicated request lifecycle code.",
    commonMistakes: ["Putting async errors into an ErrorBoundary without a supported throwing mechanism.","Ignoring race conditions.","Retrying every error indefinitely.","Updating state after unmounted/obsolete work without cancellation strategy."],
    followUpQuestions: ["How do you cancel fetches?","How do you avoid stale responses?","When would you use React Query/TanStack Query?"],
    relatedTopics: ["AbortController","Race conditions","Server state","Retries","Query caching"],
  },
  {
    id: "reactadv-m5-86",
    number: "REACTADV-M5-86",
    title: "Error boundary placement",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "Boundary placement should follow failure isolation and recovery boundaries. A root boundary is a final safety net, while route-, page-, and widget-level boundaries let unaffected UI continue working and provide more useful recovery actions.",
    deepExplanation: "A useful hierarchy is often:\n\n```tsx\n<RootErrorBoundary>\n  <AppShell>\n    <RouteErrorBoundary>\n      <Dashboard>\n        <WidgetErrorBoundary>\n          <AnalyticsWidget />\n        </WidgetErrorBoundary>\n      </Dashboard>\n    </RouteErrorBoundary>\n  </AppShell>\n</RootErrorBoundary>\n```\n\nThe exact hierarchy depends on product requirements. The smaller the boundary, the more localized the fallback can be; the larger the boundary, the more failures it can isolate with one implementation.",
    productionExample: "Production example: a banking dashboard can keep navigation and account balances visible while isolating a non-critical market-news widget behind its own boundary.",
    bestPractices: ["Use a root boundary as a last-resort safety net.","Add finer boundaries around independently recoverable/high-risk UI.","Avoid wrapping every leaf component.","Align fallback actions with the failed scope.","Document boundary ownership."],
    tradeOffs: "Advantages: graceful degradation and localized recovery. Trade-offs: more boundaries require more UX decisions; an overly broad boundary can hide useful parts of the application.",
    commonMistakes: ["Putting one boundary around the entire app and calling it sufficient.","Creating hundreds of boundaries with no recovery rationale.","Making a widget fallback look like a full-page outage."],
    followUpQuestions: ["Where would you put route boundaries?","How many boundaries are enough?","What should the root fallback do?"],
    relatedTopics: ["Error isolation","Route boundaries","Graceful degradation","Recovery UX"],
  },
  {
    id: "reactadv-m5-87",
    number: "REACTADV-M5-87",
    title: "Fallback UIs",
    difficulty: "Hard",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "Fallback UI is the user-facing state shown after a component subtree cannot render normally. A good fallback is scoped to the failure, accessible, understandable, and actionable; it should not expose stack traces or imply the entire application is broken when only one region failed.",
    deepExplanation: "A reusable fallback can expose a retry action:\n\n```tsx\nfunction ErrorFallback({\n  onRetry,\n}: {\n  onRetry: () => void;\n}) {\n  return (\n    <section role=\"alert\" aria-live=\"assertive\">\n      <h2>We couldn't load this section</h2>\n      <button onClick={onRetry}>Try again</button>\n    </section>\n  );\n}\n```\n\nThe retry action should reset the failed boundary or remount the affected resource; simply re-rendering the same broken state may fail again.",
    productionExample: "Production example: a product-recommendation card shows a compact unavailable message and retry control instead of replacing the entire product page.",
    bestPractices: ["Match fallback size to failure scope.","Use accessible alert/status semantics appropriately.","Provide retry/navigation options when meaningful.","Avoid exposing technical details.","Keep fallback rendering itself simple and reliable."],
    tradeOffs: "Advantages: graceful degradation and better UX. Trade-offs: fallbacks can mask defects if telemetry is absent; poorly designed retries can create request loops.",
    commonMistakes: ["Using a spinner forever after a permanent failure.","Showing raw exception text.","Providing a retry button that does not actually reset the failed state."],
    followUpQuestions: ["How should a fallback be accessible?","How do you reset it?","When should you show a full-page error?"],
    relatedTopics: ["Recovery UI","Accessibility","Retry","Reset","Error boundaries"],
  },
  {
    id: "reactadv-m5-88",
    number: "REACTADV-M5-88",
    title: "Error recovery",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "Recovery means returning a failed UI region to a usable state. The correct recovery action depends on whether the problem is transient, stale state, invalid input, a failed network request, or a programmer defect. Resetting a boundary alone does not guarantee that the underlying problem is fixed.",
    deepExplanation: "A reset can be driven by a changing key or an explicit boundary API.\n\n```tsx\nfunction Screen({ resourceId }: { resourceId: string }) {\n  return (\n    <ErrorBoundary\n      key={resourceId}\n      fallback={<p role=\"alert\">Unable to open this resource.</p>}\n    >\n      <ResourceView id={resourceId} />\n    </ErrorBoundary>\n  );\n}\n```\n\nChanging the key creates a fresh boundary instance when the resource changes. For a retry of the same resource, the recovery mechanism should also reset the failed state and any associated request/cache state as needed.",
    productionExample: "Production example: after a user switches accounts, the account page remounts its route boundary so the previous account's failed component state does not leak into the new account.",
    bestPractices: ["Make recovery deterministic.","Reset related request/cache state when necessary.","Do not automatically retry programmer errors in a tight loop.","Give users an escape route such as navigation.","Record recovery attempts in telemetry when failures are important."],
    tradeOffs: "Advantages: restores usability without a full application reload. Trade-offs: some failures require a page reload or corrected backend/data state; retries can repeat side effects if not designed carefully.",
    commonMistakes: ["Assuming retry always fixes a code defect.","Automatically retrying mutations.","Resetting the UI but keeping corrupted cached state."],
    followUpQuestions: ["How would you implement a retry?","When is a hard reload justified?","How do you prevent retry loops?"],
    relatedTopics: ["Reset keys","Retries","Cache invalidation","Recovery","Idempotency"],
  },
  {
    id: "reactadv-m5-89",
    number: "REACTADV-M5-89",
    title: "Resetting error boundaries",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "An error boundary remains in its error state until its state is changed or the boundary is remounted. Resetting can be implemented with boundary state, a changing `key`, or a dedicated reusable boundary component that exposes a retry mechanism.",
    deepExplanation: "A common reusable pattern is to reset when a recovery key changes:\n\n```tsx\nfunction FeatureBoundary({\n  resetKey,\n  children,\n}: {\n  resetKey: string;\n  children: ReactNode;\n}) {\n  return (\n    <ErrorBoundary key={resetKey}>\n      {children}\n    </ErrorBoundary>\n  );\n}\n```\n\nFor a retry button inside the fallback, the boundary component can keep a `hasError` state and set it back to `false` from an explicit reset callback.",
    productionExample: "Production example: route boundaries reset when the route identity changes; a local retry button remounts only the failed widget rather than reloading the entire application.",
    bestPractices: ["Reset only the smallest useful scope.","Tie reset behavior to a real change in resource/input state when possible.","Ensure the fallback's retry callback cannot create an infinite render/retry loop.","Test repeated failure → reset → failure scenarios."],
    tradeOffs: "Advantages: predictable recovery and scoped remounting. Trade-offs: remounting discards local component state; resetting without fixing the cause can immediately fail again.",
    commonMistakes: ["Thinking `setState` in an unrelated parent automatically resets a boundary.","Using a changing key indiscriminately and losing useful state.","Retrying continuously without user control."],
    followUpQuestions: ["How does a key reset a boundary?","What state is lost during remount?","How would you test retry behavior?"],
    relatedTopics: ["Keys","Remounting","Recovery","State reset","Testing"],
  },
  {
    id: "reactadv-m5-90",
    number: "REACTADV-M5-90",
    title: "Logging errors",
    difficulty: "Easy",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "Production error logging should capture enough context to diagnose a failure while minimizing sensitive data. Record normalized error information, component stack where available, release/version, environment, route and correlation identifiers, then aggregate and alert on meaningful patterns.",
    deepExplanation: "A boundary can send structured telemetry:\n\n```tsx\ncomponentDidCatch(error: Error, info: ErrorInfo) {\n  errorReporter.captureException(error, {\n    tags: {\n      route: window.location.pathname,\n      release: APP_VERSION,\n    },\n    extra: {\n      componentStack: info.componentStack,\n    },\n  });\n}\n```\n\nThe reporting layer should redact secrets and avoid dumping arbitrary application state into telemetry.",
    productionExample: "Production example: an error-monitoring pipeline groups exceptions by release and stack signature, attaches a request/correlation ID, and alerts when a new error spikes after deployment.",
    bestPractices: ["Normalize error objects consistently.","Include release/build identifiers.","Use source maps safely for production debugging.","Redact tokens, passwords, personal data and large payloads.","Sample noisy errors while retaining high-value signals.","Monitor error rate and recovery success, not just event count."],
    tradeOffs: "Advantages: faster diagnosis and release correlation. Trade-offs: telemetry has cost and privacy/security implications; excessive logging creates noise and can hide important incidents.",
    commonMistakes: ["Logging full request bodies or tokens.","Using only `console.error` in production.","Failing to identify the deployed release.","Creating one alert for every individual error event."],
    followUpQuestions: ["What metadata should be logged?","How do you redact PII?","How do you group duplicate errors?"],
    relatedTopics: ["Observability","Structured logging","Privacy","Source maps","Alerting"],
  },
  {
    id: "reactadv-m5-91",
    number: "REACTADV-M5-91",
    title: "Sentry integration patterns",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "An error-monitoring service such as Sentry should sit behind a small application-level reporting abstraction. Capture exceptions at important boundaries, add tags/context such as route and release, configure source maps and sampling, and scrub sensitive data before transmission.",
    deepExplanation: "Keep vendor-specific calls out of most components:\n\n```ts\n// errorReporter.ts\nimport * as Sentry from \"@sentry/react\";\n\nexport function reportError(error: unknown, context?: Record<string, unknown>) {\n  Sentry.withScope((scope) => {\n    scope.setExtras(context ?? {});\n    Sentry.captureException(error);\n  });\n}\n```\n\nA root integration can also provide a React error boundary, but feature code should depend on the application's `reportError` contract so the vendor can be replaced or tested independently.",
    productionExample: "Production example: initialize Sentry once at application startup, upload source maps during CI, attach release/environment tags, and use a scrubber before sending user-related context.",
    bestPractices: ["Initialize monitoring once.","Use releases and source maps for actionable stack traces.","Configure environment-aware sampling.","Scrub sensitive data.","Centralize reporting behind an application abstraction.","Test reporting behavior without making real telemetry calls."],
    tradeOffs: "Advantages: mature aggregation, stack traces, releases, breadcrumbs and alerting. Trade-offs: vendor dependency, telemetry cost, privacy configuration and possible bundle/runtime overhead.",
    commonMistakes: ["Calling the vendor directly from every component.","Sending raw user/session objects.","Forgetting source-map/release configuration.","Treating monitoring as a substitute for correct local error UX."],
    followUpQuestions: ["How would you integrate an error boundary with Sentry?","What would you sample?","How do you prevent PII leakage?"],
    relatedTopics: ["Sentry","Error monitoring","Source maps","Release tracking","Telemetry abstraction"],
  },
  {
    id: "reactadv-m5-92",
    number: "REACTADV-M5-92",
    title: "Route-level error boundaries",
    difficulty: "Hard",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "Route-level error boundaries isolate failures to a navigational region. The exact API depends on the router/framework, but the architecture is consistent: each route or route group should have a fallback appropriate to that page, while a root boundary remains the final safety net.",
    deepExplanation: "With a router that supports route error elements, the conceptual setup is:\n\n```tsx\nconst router = createBrowserRouter([\n  {\n    path: \"/\",\n    element: <AppShell />,\n    errorElement: <RouteError />,\n    children: [\n      { path: \"dashboard\", element: <Dashboard /> },\n      { path: \"settings\", element: <Settings /> },\n    ],\n  },\n]);\n```\n\nIf the router/framework has its own error-boundary mechanism, prefer it for route errors because it can understand navigation state, loader/action failures and route hierarchy.",
    productionExample: "Production example: a broken settings page shows a route-specific recovery screen while the application shell and navigation remain available.",
    bestPractices: ["Use framework/router-native boundaries when available.","Keep route fallback UI navigable.","Provide route-specific recovery actions.","Keep a root-level last-resort boundary.","Test direct navigation, refresh, nested route failure and recovery."],
    tradeOffs: "Advantages: failure isolation maps naturally to navigation and user intent. Trade-offs: behavior varies across routers/frameworks; nested route error semantics require documentation and testing.",
    commonMistakes: ["Using only a root boundary for a large application.","Assuming all router errors are React render errors.","Leaving users trapped on an inaccessible error screen."],
    followUpQuestions: ["How do route boundaries differ from component boundaries?","What should happen on a failed nested route?","How would you preserve navigation?"],
    relatedTopics: ["Routing","Nested routes","Route loaders","Error elements","Recovery UX"],
  },
  {
    id: "reactadv-m5-93",
    number: "REACTADV-M5-93",
    title: "Suspense errors",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "Suspense and error handling solve different states: Suspense handles content that is not ready, while an error boundary handles a thrown error. A robust async UI often places an error boundary around or alongside a Suspense boundary so loading and failure have distinct fallbacks.",
    deepExplanation: "For code-splitting, `lazy` integrates with Suspense for loading, while a boundary handles a rejected module load:\n\n```tsx\n<ErrorBoundary fallback={<p role=\"alert\">Module failed to load.</p>}>\n  <Suspense fallback={<p role=\"status\">Loading…</p>}>\n    <LazyReports />\n  </Suspense>\n</ErrorBoundary>\n```\n\nThis gives two distinct states: not ready yet versus failed. The exact error behavior of data-fetching integrations depends on the framework/library's supported Suspense contract.",
    productionExample: "Production example: a route's lazy JavaScript chunk displays a loading state while downloading and a recoverable error screen if the chunk cannot be loaded.",
    bestPractices: ["Keep loading and error semantics separate.","Use framework-supported Suspense data patterns rather than custom undocumented promise throwing.","Provide retry/navigation for failed lazy resources.","Test loading, success and rejection paths."],
    tradeOffs: "Advantages: declarative separation of loading and failure states. Trade-offs: Suspense data/error semantics vary by framework and library; boundary placement affects which fallback users see.",
    commonMistakes: ["Calling Suspense an error boundary.","Using a spinner as an error state.","Assuming every thrown promise/error behaves identically across data libraries."],
    followUpQuestions: ["How does Suspense differ from an ErrorBoundary?","Where should the boundaries be nested?","How do lazy chunk failures recover?"],
    relatedTopics: ["Suspense","lazy","Error boundaries","Code splitting","Loading states"],
  },
  {
    id: "reactadv-m5-94",
    number: "REACTADV-M5-94",
    title: "Production error taxonomy",
    difficulty: "Medium",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "A production error taxonomy should classify failures by domain and actionability: programmer/render defects, user/input errors, authentication/authorization failures, network/server failures, resource failures, and operational/platform failures. Each class should have an appropriate user experience, retry policy, severity and telemetry strategy.",
    deepExplanation: "A typed taxonomy makes handling explicit:\n\n```ts\ntype AppError =\n  | { kind: \"validation\"; message: string }\n  | { kind: \"unauthorized\"; message: string }\n  | { kind: \"forbidden\"; message: string }\n  | { kind: \"network\"; retryable: boolean; message: string }\n  | { kind: \"server\"; status: number; message: string }\n  | { kind: \"unexpected\"; cause: unknown };\n\nfunction isRetryable(error: AppError) {\n  return error.kind === \"network\" ||\n    (error.kind === \"server\" && error.status >= 500);\n}\n```\n\nThe taxonomy should map technical failures to safe user-facing behavior and observability severity.",
    productionExample: "Production example: validation errors stay beside form fields, 401 triggers session handling, 403 shows permission UX, 5xx may offer retry, and unexpected render failures go to an error boundary and high-severity telemetry.",
    bestPractices: ["Define stable error categories and ownership.","Separate user-safe messages from diagnostic details.","Attach severity and retryability metadata.","Keep HTTP status codes from becoming the entire domain taxonomy.","Document escalation/alerting rules."],
    tradeOffs: "Advantages: consistent UX and operational response. Trade-offs: taxonomy requires maintenance and mapping logic; over-granular categories become difficult to manage.",
    commonMistakes: ["Treating every error as 'something went wrong'.","Retrying validation or authorization failures.","Exposing internal stack traces or backend details.","Using HTTP status alone as the complete frontend taxonomy."],
    followUpQuestions: ["Which errors are retryable?","How do you map API errors to UI?","What becomes a production alert?"],
    relatedTopics: ["Error taxonomy","HTTP errors","Retry policy","Severity","Observability"],
  },
  {
    id: "reactadv-m5-95",
    number: "REACTADV-M5-95",
    title: "Enterprise error architecture",
    difficulty: "Easy",
    experienceLevel: "3–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Stripe","Atlassian","Uber","Airbnb"],
    category: "Error Handling",
    expectedAnswer: "An enterprise React error architecture should use layered handling: local operation errors for forms/actions, data/query error states for server state, route boundaries for navigation failures, component error boundaries for render/lifecycle failures, and a root boundary plus centralized telemetry as the final safety net.",
    deepExplanation: "A scalable conceptual structure is:\n\n```tsx\n<GlobalTelemetry>\n  <RootErrorBoundary>\n    <AuthProvider>\n      <AppRouter>\n        <RouteBoundary>\n          <Suspense fallback={<RouteLoading />}>\n            <Feature />\n          </Suspense>\n        </RouteBoundary>\n      </AppRouter>\n    </AuthProvider>\n  </RootErrorBoundary>\n</GlobalTelemetry>\n```\n\nThe exact order depends on framework and provider dependencies. The key is that each layer owns a distinct class of failure and recovery behavior.",
    productionExample: "Production example: a large SaaS application isolates widgets and routes, handles API failures in query/form layers, reports unexpected UI crashes centrally, and uses release-based dashboards to detect regressions after deployments.",
    bestPractices: ["Define ownership for each error class.","Use the smallest meaningful recovery scope.","Centralize telemetry but keep user-facing handling local.","Standardize redaction, severity, correlation IDs and release metadata.","Test failure paths as first-class product behavior.","Track error rate, affected users, recovery success and release correlation."],
    tradeOffs: "Advantages: clear separation of concerns, graceful degradation and strong observability. Trade-offs: more architecture and testing; boundary interactions can become complex; vendor/framework choices influence implementation.",
    commonMistakes: ["One global try/catch for the whole React application.","One giant error context for every failure type.","Treating telemetry as recovery.","Retrying non-retryable failures.","Ignoring accessibility of fallback screens."],
    followUpQuestions: ["Design an error architecture for a 500-component SaaS app.","How would you migrate from ad-hoc try/catch?","How would you measure error-budget impact?","Where would Sentry/another monitor fit?"],
    relatedTopics: ["Error boundaries","Route errors","Suspense","Server state","Observability","Error budgets","Testing"],
  },

];

export const MOCK_ADVANCED_REACT_MODULE5_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Advanced React',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));