// Derived from frontend/src/document/Frontend_Architecture_Design_Patterns_Enterprise_Development_Master_Handbook.md (Module 14 — Logging & Monitoring).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "arch-m14-1",
    number: "ARCH-M14-1",
    title: "Logging",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Logging & Monitoring",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Logging: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Logging solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Logging & Monitoring"],
  },
  {
    id: "arch-m14-2",
    number: "ARCH-M14-2",
    title: "Analytics",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Logging & Monitoring",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Analytics: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Analytics solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Logging & Monitoring"],
  },
  {
    id: "arch-m14-3",
    number: "ARCH-M14-3",
    title: "Performance Monitoring",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Logging & Monitoring",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Performance Monitoring: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Performance Monitoring solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Logging & Monitoring"],
  },
  {
    id: "arch-m14-4",
    number: "ARCH-M14-4",
    title: "Crash Reporting",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Logging & Monitoring",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Crash Reporting: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Crash Reporting solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Logging & Monitoring"],
  },
  {
    id: "arch-m14-5",
    number: "ARCH-M14-5",
    title: "Sentry",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Logging & Monitoring",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Sentry: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Sentry solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Logging & Monitoring"],
  },
  {
    id: "arch-m14-6",
    number: "ARCH-M14-6",
    title: "Datadog",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Logging & Monitoring",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Datadog: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Datadog solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Logging & Monitoring"],
  },
  {
    id: "arch-m14-7",
    number: "ARCH-M14-7",
    title: "New Relic",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Logging & Monitoring",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// New Relic: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does New Relic solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Logging & Monitoring"],
  },
  {
    id: "arch-m14-8",
    number: "ARCH-M14-8",
    title: "Grafana",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Logging & Monitoring",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Grafana: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Grafana solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Logging & Monitoring"],
  },
  {
    id: "arch-m14-9",
    number: "ARCH-M14-9",
    title: "OpenTelemetry",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Logging & Monitoring",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// OpenTelemetry: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does OpenTelemetry solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Logging & Monitoring"],
  },
  {
    id: "arch-m14-10",
    number: "ARCH-M14-10",
    title: "Alerting",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Logging & Monitoring",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Alerting: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Alerting solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Logging & Monitoring"],
  },
  {
    id: "arch-m14-11",
    number: "ARCH-M14-11",
    title: "Dashboards",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Logging & Monitoring",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Dashboards: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Dashboards solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Logging & Monitoring"],
  },
];

export const MOCK_FRONTEND_ARCHITECTURE_MODULE14_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Architecture',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
