// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 5 — CSRF).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m5-76",
    number: "SEC-M5-76",
    title: "CSRF fundamentals",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**CSRF fundamentals** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-77",
    number: "SEC-M5-77",
    title: "CSRF attack flow",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**CSRF attack flow** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-78",
    number: "SEC-M5-78",
    title: "SameSite cookies",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**SameSite cookies** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Prefer server-managed sessions with hardened cookies.\n// Example response policy:\n// Set-Cookie: session=...; HttpOnly; Secure; SameSite=Lax; Path=/\nconst securityPolicy = {\n  httpOnly: true,\n  secure: true,\n  sameSite: \"Lax\",\n};\nconsole.log(securityPolicy);\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-79",
    number: "SEC-M5-79",
    title: "CSRF tokens",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**CSRF tokens** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-80",
    number: "SEC-M5-80",
    title: "Double-submit cookie",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**Double-submit cookie** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Prefer server-managed sessions with hardened cookies.\n// Example response policy:\n// Set-Cookie: session=...; HttpOnly; Secure; SameSite=Lax; Path=/\nconst securityPolicy = {\n  httpOnly: true,\n  secure: true,\n  sameSite: \"Lax\",\n};\nconsole.log(securityPolicy);\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-81",
    number: "SEC-M5-81",
    title: "Origin validation",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**Origin validation** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-82",
    number: "SEC-M5-82",
    title: "Referer validation",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**Referer validation** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-83",
    number: "SEC-M5-83",
    title: "Fetch Metadata",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**Fetch Metadata** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-84",
    number: "SEC-M5-84",
    title: "CORS vs CSRF",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**CORS vs CSRF** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-85",
    number: "SEC-M5-85",
    title: "Credentialed requests",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**Credentialed requests** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-86",
    number: "SEC-M5-86",
    title: "Login CSRF",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**Login CSRF** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-87",
    number: "SEC-M5-87",
    title: "State-changing requests",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**State-changing requests** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-88",
    number: "SEC-M5-88",
    title: "SPA CSRF defenses",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**SPA CSRF defenses** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-89",
    number: "SEC-M5-89",
    title: "Cookie-based sessions",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**Cookie-based sessions** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Prefer server-managed sessions with hardened cookies.\n// Example response policy:\n// Set-Cookie: session=...; HttpOnly; Secure; SameSite=Lax; Path=/\nconst securityPolicy = {\n  httpOnly: true,\n  secure: true,\n  sameSite: \"Lax\",\n};\nconsole.log(securityPolicy);\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m5-90",
    number: "SEC-M5-90",
    title: "Token-based APIs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CSRF",
    expectedAnswer: "**Token-based APIs** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE5_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
