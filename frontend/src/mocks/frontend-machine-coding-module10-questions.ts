// Derived from frontend/src/document/Part_13_Frontend_Machine_Coding_Master_Handbook.md (Module 10 — Calendar).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "machinecoding-m10-191",
    number: "MACHINECODING-M10-191",
    title: "Monthly calendar",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Monthly calendar** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-192",
    number: "MACHINECODING-M10-192",
    title: "Weekly calendar",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Weekly calendar** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-193",
    number: "MACHINECODING-M10-193",
    title: "Daily calendar",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Daily calendar** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-194",
    number: "MACHINECODING-M10-194",
    title: "Date navigation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Date navigation** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-195",
    number: "MACHINECODING-M10-195",
    title: "Date selection",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Date selection** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-196",
    number: "MACHINECODING-M10-196",
    title: "Range selection",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Range selection** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-197",
    number: "MACHINECODING-M10-197",
    title: "Event rendering",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Event rendering** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-198",
    number: "MACHINECODING-M10-198",
    title: "Event creation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Event creation** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-199",
    number: "MACHINECODING-M10-199",
    title: "Event editing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Event editing** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-200",
    number: "MACHINECODING-M10-200",
    title: "Event deletion",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Event deletion** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-201",
    number: "MACHINECODING-M10-201",
    title: "Drag and drop events",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Drag and drop events** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\ntype Item = { id: string; label: string };\n\nexport function SortableList({ items }: { items: Item[] }) {\n  return (\n    <ul aria-label=\"Sortable items\">\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" aria-label={`Move ${item.label}`}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-202",
    number: "MACHINECODING-M10-202",
    title: "Resize events",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Resize events** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-203",
    number: "MACHINECODING-M10-203",
    title: "Timezone handling",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Timezone handling** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-204",
    number: "MACHINECODING-M10-204",
    title: "Recurring events",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Recurring events** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-205",
    number: "MACHINECODING-M10-205",
    title: "Calendar accessibility",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Calendar accessibility** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-206",
    number: "MACHINECODING-M10-206",
    title: "Calendar responsive design",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Calendar responsive design** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-207",
    number: "MACHINECODING-M10-207",
    title: "Calendar performance",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Calendar performance** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\nexport function ExampleForm() {\n  const [name, setName] = useState(\"\");\n\n  function submit(event: React.FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    if (!name.trim()) return;\n    console.log({ name });\n  }\n\n  return (\n    <form onSubmit={submit} noValidate>\n      <label htmlFor=\"name\">Name</label>\n      <input\n        id=\"name\"\n        name=\"name\"\n        value={name}\n        onChange={(e) => setName(e.target.value)}\n        required\n      />\n      <button type=\"submit\">Save</button>\n    </form>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-208",
    number: "MACHINECODING-M10-208",
    title: "Calendar testing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Calendar testing** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-209",
    number: "MACHINECODING-M10-209",
    title: "Calendar Storybook",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Calendar Storybook** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m10-210",
    number: "MACHINECODING-M10-210",
    title: "Enterprise calendar",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Calendar",
    expectedAnswer: "Build a production-quality **Enterprise calendar** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---","# Module 11 – Infinite Scroll & Virtualization","**Questions 211–230**"],
  },
];

export const MOCK_FRONTEND_MACHINE_CODING_MODULE10_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
