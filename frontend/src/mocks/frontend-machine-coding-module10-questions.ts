// Derived from frontend/src/document/Frontend_Architecture_Design_Patterns_Enterprise_Development_Master_Handbook.md (Module 10 — Event-driven Architecture).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "arch-m10-1",
    number: "ARCH-M10-1",
    title: "Pub/Sub",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Event-driven Architecture",
    expectedAnswer: "Pub/Sub decouples publishers from subscribers by sending events to a topic or channel without requiring direct object references.",
    deepExplanation: "Step 1 — Understand Pub/Sub.\n\nPub/Sub decouples publishers from subscribers by sending events to a topic or channel without requiring direct object references.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Listener<T> = (event: T) => void;\n\nclass PubSub<T> {\n  private listeners: Listener<T>[] = [];\n\n  subscribe(listener: Listener<T>): () => void {\n    this.listeners.push(listener);\n\n    return () => {\n      const next: Listener<T>[] = [];\n\n      for (let i = 0; i < this.listeners.length; i += 1) {\n        if (this.listeners[i] !== listener) {\n          next.push(this.listeners[i]);\n        }\n      }\n\n      this.listeners = next;\n    };\n  }\n\n  publish(event: T): void {\n    for (let i = 0; i < this.listeners.length; i += 1) {\n      this.listeners[i](event);\n    }\n  }\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nclass PubSub<T> {\n  private readonly listeners = new Set<(event: T) => void>();\n\n  subscribe(listener: (event: T) => void): () => void {\n    this.listeners.add(listener);\n    return () => this.listeners.delete(listener);\n  }\n\n  publish(event: T): void {\n    this.listeners.forEach((listener) => listener(event));\n  }\n}\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse it for independent reactions such as notifications, analytics, or UI updates.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use it for independent reactions such as notifications, analytics, or UI updates.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Pub/Sub improves decoupling but requires event naming, lifecycle, and error handling conventions.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Pub/Sub", "Events", "Topics", "Subscriptions"],
  },
  {
    id: "arch-m10-2",
    number: "ARCH-M10-2",
    title: "Observer",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Event-driven Architecture",
    expectedAnswer: "Observer lets one subject notify registered observers when a value changes.",
    deepExplanation: "Step 1 — Understand Observer.\n\nObserver lets one subject notify registered observers when a value changes.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface Observer<T> {\n  update(value: T): void;\n}\n\nclass Observable<T> {\n  private observers: Observer<T>[] = [];\n\n  attach(observer: Observer<T>): void {\n    this.observers.push(observer);\n  }\n\n  notify(value: T): void {\n    for (let i = 0; i < this.observers.length; i += 1) {\n      this.observers[i].update(value);\n    }\n  }\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nclass Observable<T> {\n  private readonly observers = new Set<(value: T) => void>();\n\n  subscribe(observer: (value: T) => void): () => void {\n    this.observers.add(observer);\n    return () => this.observers.delete(observer);\n  }\n\n  notify(value: T): void {\n    this.observers.forEach((observer) => observer(value));\n  }\n}\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse it for local in-process state or event notification where ownership and cleanup are simple.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use it for local in-process state or event notification where ownership and cleanup are simple.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Observers need unsubscribe semantics to avoid leaks and stale listeners.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Observer", "Subscriptions", "Reactive State", "Events"],
  },
  {
    id: "arch-m10-3",
    number: "ARCH-M10-3",
    title: "Event Bus",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Event-driven Architecture",
    expectedAnswer: "An event bus is a centralized in-process routing abstraction for named events and handlers.",
    deepExplanation: "Step 1 — Understand Event Bus.\n\nAn event bus is a centralized in-process routing abstraction for named events and handlers.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface DomainEvent {\n  type: string;\n  payload: unknown;\n}\n\nclass EventBus {\n  private handlers: {\n    type: string;\n    handler: (event: DomainEvent) => void;\n  }[] = [];\n\n  on(\n    type: string,\n    handler: (event: DomainEvent) => void,\n  ): void {\n    this.handlers.push({ type, handler });\n  }\n\n  emit(event: DomainEvent): void {\n    for (let i = 0; i < this.handlers.length; i += 1) {\n      if (this.handlers[i].type === event.type) {\n        this.handlers[i].handler(event);\n      }\n    }\n  }\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nclass EventBus {\n  private readonly handlers = new Map<\n    string,\n    Set<(payload: unknown) => void>\n  >();\n\n  on(\n    type: string,\n    handler: (payload: unknown) => void,\n  ): void {\n    const handlers = this.handlers.get(type) ?? new Set();\n    handlers.add(handler);\n    this.handlers.set(type, handlers);\n  }\n\n  emit(type: string, payload: unknown): void {\n    this.handlers.get(type)?.forEach((handler) => handler(payload));\n  }\n}\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse it when multiple modules need a decoupled event contract and a small routing layer is enough.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use it when multiple modules need a decoupled event contract and a small routing layer is enough.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "A large event bus can become an invisible dependency graph if event ownership is not documented.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Event Bus", "Domain Events", "Decoupling", "Event Routing"],
  },
  {
    id: "arch-m10-4",
    number: "ARCH-M10-4",
    title: "MessageChannel",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Event-driven Architecture",
    expectedAnswer: "MessageChannel creates two linked ports for asynchronous message passing, useful for isolated execution contexts such as workers or controlled browser channels.",
    deepExplanation: "Step 1 — Understand MessageChannel.\n\nMessageChannel creates two linked ports for asynchronous message passing, useful for isolated execution contexts such as workers or controlled browser channels.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface PortLike<T> {\n  postMessage(message: T): void;\n  onMessage(handler: (message: T) => void): void;\n}\n\nfunction connect<T>(\n  left: PortLike<T>,\n  right: PortLike<T>,\n): void {\n  left.onMessage((message) => right.postMessage(message));\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\ntype MessageHandler<T> = (message: T) => void;\n\nclass SimplePort<T> {\n  private readonly handlers = new Set<MessageHandler<T>>();\n\n  onMessage(handler: MessageHandler<T>): () => void {\n    this.handlers.add(handler);\n    return () => this.handlers.delete(handler);\n  }\n\n  postMessage(message: T): void {\n    this.handlers.forEach((handler) => handler(message));\n  }\n}\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse it when two execution contexts need a direct message pipe rather than a global event bus.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use it when two execution contexts need a direct message pipe rather than a global event bus.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "It is a local communication mechanism, not a durable event system.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["MessageChannel", "Web Workers", "Ports", "Messaging"],
  },
  {
    id: "arch-m10-5",
    number: "ARCH-M10-5",
    title: "BroadcastChannel",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Event-driven Architecture",
    expectedAnswer: "BroadcastChannel lets same-origin browsing contexts exchange messages without directly referencing each other.",
    deepExplanation: "Step 1 — Understand BroadcastChannel.\n\nBroadcastChannel lets same-origin browsing contexts exchange messages without directly referencing each other.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface BroadcastEnvelope {\n  source: string;\n  value: string;\n}\n\nfunction shouldAccept(\n  message: BroadcastEnvelope,\n  source: string,\n): boolean {\n  return message.source !== source;\n}\n\nconst accepted = shouldAccept(\n  { source: \"tab-1\", value: \"refresh\" },\n  \"tab-2\",\n);\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst channel = new Map<string, string[]>();\n\nfunction publish(name: string, value: string): void {\n  const values = channel.get(name) ?? [];\n  channel.set(name, [...values, value]);\n}\n\npublish(\"session\", \"logout\");\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse it for tab-to-tab notifications such as logout, cache invalidation, or session refresh signals.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use it for tab-to-tab notifications such as logout, cache invalidation, or session refresh signals.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "It is convenient but messages are ephemeral and scope-limited.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["BroadcastChannel", "Multi-tab", "Session Sync", "Browser APIs"],
  },
  {
    id: "arch-m10-6",
    number: "ARCH-M10-6",
    title: "WebSocket",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Event-driven Architecture",
    expectedAnswer: "WebSocket provides a long-lived bidirectional connection for low-latency interactive communication.",
    deepExplanation: "Step 1 — Understand WebSocket.\n\nWebSocket provides a long-lived bidirectional connection for low-latency interactive communication.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface SocketHandlers {\n  onOpen(): void;\n  onMessage(value: string): void;\n  onClose(): void;\n}\n\nfunction createSocketState(): SocketHandlers {\n  return {\n    onOpen: () => undefined,\n    onMessage: (value) => console.log(value),\n    onClose: () => undefined,\n  };\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst listeners = new Map<\n  \"open\" | \"message\" | \"close\",\n  Array<() => void>\n>();\n\nlisteners.set(\"open\", []);\nlisteners.get(\"open\")?.push(() => undefined);\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse it for chat, collaboration, live dashboards, or other experiences requiring client-to-server and server-to-client messages.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use it for chat, collaboration, live dashboards, or other experiences requiring client-to-server and server-to-client messages.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Persistent connections require reconnect, heartbeat, backpressure, and lifecycle handling.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["WebSocket", "Realtime", "Reconnect", "Backpressure"],
  },
  {
    id: "arch-m10-7",
    number: "ARCH-M10-7",
    title: "SSE",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Event-driven Architecture",
    expectedAnswer: "Server-Sent Events provide a one-way server-to-browser event stream over HTTP.",
    deepExplanation: "Step 1 — Understand SSE.\n\nServer-Sent Events provide a one-way server-to-browser event stream over HTTP.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface ServerEvent {\n  id: string;\n  data: string;\n}\n\nfunction applyEvent(\n  events: readonly ServerEvent[],\n  event: ServerEvent,\n): ServerEvent[] {\n  const next: ServerEvent[] = [];\n\n  for (let i = 0; i < events.length; i += 1) {\n    next.push(events[i]);\n  }\n\n  next.push(event);\n  return next;\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst events = [\n  { id: \"1\", data: \"ready\" },\n];\n\nconst next = [\n  ...events,\n  { id: \"2\", data: \"updated\" },\n];\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse SSE for notifications, progress, logs, or dashboards where the browser primarily receives updates.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use SSE for notifications, progress, logs, or dashboards where the browser primarily receives updates.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "SSE is simpler than WebSocket for one-way streams but is not bidirectional.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["SSE", "EventStream", "Realtime", "Notifications"],
  },
  {
    id: "arch-m10-8",
    number: "ARCH-M10-8",
    title: "Custom Events",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Event-driven Architecture",
    expectedAnswer: "Custom browser events provide a small DOM-level event contract for communication between components or isolated UI modules.",
    deepExplanation: "Step 1 — Understand Custom Events.\n\nCustom browser events provide a small DOM-level event contract for communication between components or isolated UI modules.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface UiEvent {\n  type: string;\n  detail: string;\n}\n\nfunction matches(uiEvent: UiEvent, type: string): boolean {\n  return uiEvent.type === type;\n}\n\nconst uiEvent: UiEvent = {\n  type: \"cart.updated\",\n  detail: \"3\",\n};\n\nconst matched = matches(uiEvent, \"cart.updated\");\n\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst cartEvent = new CustomEvent(\"cart.updated\", {\n  detail: { count: 3 },\n});\n\nconst eventType = cartEvent.type;\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse them sparingly when browser-native event propagation is the desired integration boundary.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use them sparingly when browser-native event propagation is the desired integration boundary.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "They are easy to add but can become implicit contracts that are difficult to discover.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Custom Events", "DOM Events", "Browser APIs", "Communication"],
  },
  {
    id: "arch-m10-9",
    number: "ARCH-M10-9",
    title: "Event Replay",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Event-driven Architecture",
    expectedAnswer: "Event replay reprocesses previously recorded events to rebuild projections or recover a consumer state.",
    deepExplanation: "Step 1 — Understand Event Replay.\n\nEvent replay reprocesses previously recorded events to rebuild projections or recover a consumer state.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface RecordedEvent {\n  sequence: number;\n  type: string;\n  payload: unknown;\n}\n\nfunction eventsAfter(\n  events: readonly RecordedEvent[],\n  sequence: number,\n): RecordedEvent[] {\n  const result: RecordedEvent[] = [];\n\n  for (let i = 0; i < events.length; i += 1) {\n    if (events[i].sequence > sequence) {\n      result.push(events[i]);\n    }\n  }\n\n  return result;\n}\n\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\ninterface RecordedEvent {\n  sequence: number;\n  type: string;\n  payload: unknown;\n}\n\nconst events: RecordedEvent[] = [\n  { sequence: 1, type: \"created\", payload: {} },\n  { sequence: 2, type: \"updated\", payload: {} },\n];\n\nconst pending = events.filter(\n  (record) => record.sequence > 1,\n);\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse it when events are durable and the consumer can deterministically rebuild state from them.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use it when events are durable and the consumer can deterministically rebuild state from them.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Replay requires versioned schemas, idempotency, and careful side-effect handling.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Event Replay", "Event Sourcing", "Idempotency", "Projections"],
  },
  {
    id: "arch-m10-10",
    number: "ARCH-M10-10",
    title: "Monitoring",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Event-driven Architecture",
    expectedAnswer: "Event monitoring tracks throughput, handler latency, error rate, queue/backlog health, and event processing failures.",
    deepExplanation: "Step 1 — Understand Monitoring.\n\nEvent monitoring tracks throughput, handler latency, error rate, queue/backlog health, and event processing failures.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface EventMetric {\n  type: string;\n  durationMs: number;\n}\n\nfunction totalDuration(\n  metrics: readonly EventMetric[],\n): number {\n  let total = 0;\n\n  for (let i = 0; i < metrics.length; i += 1) {\n    total += metrics[i].durationMs;\n  }\n\n  return total;\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst metrics = [\n  { type: \"cart.updated\", durationMs: 12 },\n  { type: \"order.created\", durationMs: 30 },\n];\n\nconst total = metrics.reduce(\n  (sum, metric) => sum + metric.durationMs,\n  0,\n);\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse structured event metrics and correlation IDs so incidents can be traced across producers and consumers.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use structured event metrics and correlation IDs so incidents can be traced across producers and consumers.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Monitoring adds cost but is essential once event flows become asynchronous and distributed.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Monitoring", "Event Metrics", "Tracing", "Event-driven Architecture"],
  },
];

export const MOCK_FRONTEND_MACHINE_CODING_MODULE10_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Architecture',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));