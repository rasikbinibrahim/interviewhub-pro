// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 8 — API Testing).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m8-1",
    number: "TESTQA-M8-1",
    title: "REST API Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"REST API Testing\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"REST API Testing\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-2",
    number: "TESTQA-M8-2",
    title: "GraphQL Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"GraphQL Testing\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"GraphQL Testing\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-3",
    number: "TESTQA-M8-3",
    title: "WebSocket Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"WebSocket Testing\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"WebSocket Testing\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-4",
    number: "TESTQA-M8-4",
    title: "SSE Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"SSE Testing\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"SSE Testing\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-5",
    number: "TESTQA-M8-5",
    title: "Pagination",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Pagination\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Pagination\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-6",
    number: "TESTQA-M8-6",
    title: "Infinite Scroll APIs",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Infinite Scroll APIs\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Infinite Scroll APIs\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-7",
    number: "TESTQA-M8-7",
    title: "Authentication APIs",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Authentication APIs\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Authentication APIs\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-8",
    number: "TESTQA-M8-8",
    title: "Authorization APIs",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Authorization APIs\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Authorization APIs\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-9",
    number: "TESTQA-M8-9",
    title: "Mock Service Worker",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Mock Service Worker\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Mock Service Worker\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-10",
    number: "TESTQA-M8-10",
    title: "Fetch Mocking",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Fetch Mocking\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Fetch Mocking\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-11",
    number: "TESTQA-M8-11",
    title: "Axios Mocking",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Axios Mocking\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Axios Mocking\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-12",
    number: "TESTQA-M8-12",
    title: "Caching",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Caching\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Caching\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-13",
    number: "TESTQA-M8-13",
    title: "Offline Scenarios",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Offline Scenarios\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Offline Scenarios\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-14",
    number: "TESTQA-M8-14",
    title: "Retries and Timeouts",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Retries and Timeouts\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"Retries and Timeouts\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-15",
    number: "TESTQA-M8-15",
    title: "AbortController",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"AbortController\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"AbortController\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-16",
    number: "TESTQA-M8-16",
    title: "API Contract Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Testing this topic means verifying its public behavior under normal, boundary, failure, accessibility, performance, and security conditions.",
    deepExplanation: "Example:\n\n```ts\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"API Contract Testing\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```\n\nWhy it exists: It reduces escaped defects, documents behavior, enables safe refactoring, and creates confidence during continuous delivery.\n\nInternal working: ```text\nArrange → Act → Observe → Assert → Cleanup → Report\n```",
    productionExample: "```tsx\nimport { render, screen } from \"@testing-library/react\";\nimport userEvent from \"@testing-library/user-event\";\nimport { vi, describe, test, expect } from \"vitest\";\nimport { SearchForm } from \"./SearchForm\";\n\ndescribe(\"API Contract Testing\", () => {\n  test(\"submits normalized user input\", async () => {\n    const user = userEvent.setup();\n    const onSubmit = vi.fn();\n    render(<SearchForm label=\"Search\" onSubmit={onSubmit} />);\n    await user.type(screen.getByRole(\"textbox\", { name: \"Search\" }), \"  React  \");\n    await user.click(screen.getByRole(\"button\", { name: \"Search\" }));\n    expect(onSubmit).toHaveBeenCalledWith(\"React\");\n  });\n});\n```",
    bestPractices: ["Test behavior rather than implementation details.","Keep tests deterministic and isolated.","Use realistic interactions.","Make failures diagnostic.","Keep setup explicit.","Match test level to risk.","Treat accessibility as a quality dimension.","Review test code like production code."],
    tradeOffs: "| Strategy | Strength | Cost |\n|---|---|---|\n| Unit | Fast feedback | Limited integration confidence |\n| Integration | Realistic boundary | More setup |\n| E2E | High user-flow confidence | Slower and more environment-sensitive |\n| Mock | Isolation | Risk of drift |\n| Real dependency | High fidelity | Higher cost |",
    commonMistakes: ["Arbitrary sleeps","Shared mutable fixtures","Excessive mocking","Snapshot overuse","Testing private state","Ignoring cleanup","Treating coverage as quality","Running every check in every PR regardless of value"],
    followUpQuestions: ["What behavior should be protected?","Which test level is appropriate and why?","What should be mocked?","How do you make this deterministic?","How do you test failure states?","How do you test accessibility?","How do you diagnose CI-only failures?","How do you measure useful coverage?","How would you scale the suite?","What would you change if test runtime doubled?"],
    relatedTopics: ["API Testing"],
  },
  {
    id: "testqa-m8-17",
    number: "TESTQA-M8-17",
    title: "API Testing — Module Review",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "API Testing",
    expectedAnswer: "Explain **API Testing — Module Review** from first principles through production trade-offs.",
    deepExplanation: "",
    productionExample: "Work through the accompanying code example for \"API Testing — Module Review\" and verify the documented behavior before generalizing to production.",
    bestPractices: ["Prefer the simplest design that solves the real problem."],
    tradeOffs: "Advantages: clearer boundaries and easier reasoning. Disadvantages: added abstraction and operational complexity.",
    commonMistakes: ["Adopting a pattern or tool before understanding the problem it solves."],
    followUpQuestions: ["How would you explain API Testing — Module Review at a senior level?"],
    relatedTopics: ["API Testing"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE8_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
