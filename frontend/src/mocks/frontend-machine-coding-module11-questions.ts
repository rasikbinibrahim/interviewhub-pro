// Derived from frontend/src/document/Frontend_Architecture_Design_Patterns_Enterprise_Development_Master_Handbook.md (Module 11 — Dependency Injection).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "arch-m11-1",
    number: "ARCH-M11-1",
    title: "IoC",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Dependency Injection",
    expectedAnswer: "Inversion of Control means a component does not create or own every dependency; the surrounding composition root supplies those dependencies.",
    deepExplanation: "Step 1 — Understand IoC.\n\nInversion of Control means a component does not create or own every dependency; the surrounding composition root supplies those dependencies.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface Logger {\n  log(message: string): void;\n}\n\ninterface ServiceDeps {\n  logger: Logger;\n}\n\nfunction createService(deps: ServiceDeps) {\n  return {\n    run(): void {\n      deps.logger.log(\"running\");\n    },\n  };\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nfunction createService(\n  logger: (message: string) => void,\n) {\n  return {\n    run(): void {\n      logger(\"running\");\n    },\n  };\n}\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse IoC to keep behavior independent of infrastructure construction details.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use IoC to keep behavior independent of infrastructure construction details.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "IoC improves testability but adds composition work.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["IoC", "Dependency Injection", "Composition Root", "Testability"],
  },
  {
    id: "arch-m11-2",
    number: "ARCH-M11-2",
    title: "Constructor Injection",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Dependency Injection",
    expectedAnswer: "Constructor injection passes required dependencies when an object is created, making required collaborators explicit and easy to test.",
    deepExplanation: "Step 1 — Understand Constructor Injection.\n\nConstructor injection passes required dependencies when an object is created, making required collaborators explicit and easy to test.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface Clock {\n  now(): number;\n}\n\nclass TokenService {\n  constructor(private readonly clock: Clock) {}\n\n  expiresIn(seconds: number): number {\n    return this.clock.now() + seconds;\n  }\n}\n\nconst service = new TokenService({\n  now: () => 1000,\n});\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nclass TokenService {\n  constructor(\n    private readonly now: () => number,\n  ) {}\n\n  expiresIn(seconds: number): number {\n    return this.now() + seconds;\n  }\n}\n\nconst service = new TokenService(() => Date.now());\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse it for mandatory dependencies and stable service contracts.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use it for mandatory dependencies and stable service contracts.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Too many constructor parameters can indicate that the class has too many responsibilities.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Constructor Injection", "DI", "Explicit Dependencies", "Testing"],
  },
  {
    id: "arch-m11-3",
    number: "ARCH-M11-3",
    title: "Factory Injection",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Dependency Injection",
    expectedAnswer: "Factory injection supplies a function or factory that creates a dependency when runtime parameters determine which implementation is needed.",
    deepExplanation: "Step 1 — Understand Factory Injection.\n\nFactory injection supplies a function or factory that creates a dependency when runtime parameters determine which implementation is needed.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface ApiClient {\n  get(path: string): Promise<string>;\n}\n\ninterface ClientFactory {\n  create(baseUrl: string): ApiClient;\n}\n\nfunction useApi(\n  factory: ClientFactory,\n  baseUrl: string,\n): ApiClient {\n  return factory.create(baseUrl);\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nfunction createApi(\n  baseUrl: string,\n): { get(path: string): Promise<string> } {\n  return {\n    async get(path) {\n      return `${baseUrl}${path}`;\n    },\n  };\n}\n\nconst api = createApi(\"/api\");\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse factories when the concrete dependency varies by tenant, environment, or request.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use factories when the concrete dependency varies by tenant, environment, or request.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Factory injection adds indirection but keeps creation rules outside business logic.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Factory Injection", "Factories", "Runtime Selection", "DI"],
  },
  {
    id: "arch-m11-4",
    number: "ARCH-M11-4",
    title: "Context Injection",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Dependency Injection",
    expectedAnswer: "Context injection supplies a small execution context such as tenant, locale, request ID, or environment through a shared boundary.",
    deepExplanation: "Step 1 — Understand Context Injection.\n\nContext injection supplies a small execution context such as tenant, locale, request ID, or environment through a shared boundary.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface AppContext {\n  apiUrl: string;\n  locale: string;\n}\n\nfunction createUserUrl(\n  context: AppContext,\n  id: string,\n): string {\n  return `${context.apiUrl}/users/${id}?locale=${context.locale}`;\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst context = {\n  apiUrl: \"/api\",\n  locale: \"en-IN\",\n};\n\nconst userUrl = `${context.apiUrl}/users/1?locale=${context.locale}`;\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse it for scoped values that many nested operations need.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use it for scoped values that many nested operations need.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Large contexts can hide dependencies and create accidental global state.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Context Injection", "Scoped Context", "DI", "Multi-tenancy"],
  },
  {
    id: "arch-m11-5",
    number: "ARCH-M11-5",
    title: "Hook Injection",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Dependency Injection",
    expectedAnswer: "Hook injection provides a React-specific dependency seam by passing behavior or a hook result rather than importing infrastructure directly inside a component.",
    deepExplanation: "Step 1 — Understand Hook Injection.\n\nHook injection provides a React-specific dependency seam by passing behavior or a hook result rather than importing infrastructure directly inside a component.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface UseAuth {\n  userId(): string | null;\n}\n\nfunction canEdit(\n  auth: UseAuth,\n): boolean {\n  return auth.userId() !== null;\n}\n\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\ninterface UseAuth {\n  userId(): string | null;\n}\n\nfunction createAuthHook(\n  getUserId: () => string | null,\n): UseAuth {\n  return {\n    userId: getUserId,\n  };\n}\n\nconst auth = createAuthHook(() => \"u1\");\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse it when component logic needs testable access to auth, data, navigation, or browser capabilities.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use it when component logic needs testable access to auth, data, navigation, or browser capabilities.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Hooks remain subject to React lifecycle and Rules of Hooks.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Hook Injection", "Custom Hooks", "React", "DI"],
  },
  {
    id: "arch-m11-6",
    number: "ARCH-M11-6",
    title: "Service Locator",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Dependency Injection",
    expectedAnswer: "Service Locator stores dependencies in a registry that consumers resolve at runtime. It reduces parameter plumbing but makes dependencies less explicit.",
    deepExplanation: "Step 1 — Understand Service Locator.\n\nService Locator stores dependencies in a registry that consumers resolve at runtime. It reduces parameter plumbing but makes dependencies less explicit.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface Services {\n  logger?: { log(message: string): void };\n  clock?: { now(): number };\n}\n\nfunction resolveLogger(\n  services: Services,\n): { log(message: string): void } {\n  if (!services.logger) {\n    throw new Error(\"logger not registered\");\n  }\n\n  return services.logger;\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst services = new Map<string, unknown>();\n\nservices.set(\"logger\", {\n  log: (message: string) => console.log(message),\n});\n\nconst logger = services.get(\"logger\") as {\n  log(message: string): void;\n};\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse it carefully at infrastructure or composition boundaries, not as the default application pattern.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use it carefully at infrastructure or composition boundaries, not as the default application pattern.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Hidden dependencies make testing and reasoning harder.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Service Locator", "Registry", "Hidden Dependencies", "Anti-patterns"],
  },
  {
    id: "arch-m11-7",
    number: "ARCH-M11-7",
    title: "Enterprise Examples",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Dependency Injection",
    expectedAnswer: "Enterprise dependency injection should be organized around composition roots, stable interfaces, and environment-specific implementations.",
    deepExplanation: "Step 1 — Understand Enterprise Examples.\n\nEnterprise dependency injection should be organized around composition roots, stable interfaces, and environment-specific implementations.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface AppDependencies {\n  logger(message: string): void;\n  now(): number;\n}\n\nfunction createOrderService(\n  deps: AppDependencies,\n) {\n  return {\n    createOrder(id: string): string {\n      deps.logger(`create:${id}`);\n      return `${id}:${deps.now()}`;\n    },\n  };\n}\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nfunction createOrderService(\n  logger: (message: string) => void,\n  now: () => number,\n) {\n  return {\n    createOrder(id: string): string {\n      logger(`create:${id}`);\n      return `${id}:${now()}`;\n    },\n  };\n}\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse explicit adapters for logging, HTTP, analytics, feature flags, and storage so applications can swap infrastructure safely.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use explicit adapters for logging, HTTP, analytics, feature flags, and storage so applications can swap infrastructure safely.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Enterprise DI can become over-engineered if every trivial function gets an interface.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Enterprise DI", "Composition Root", "Adapters", "Testing"],
  },
  {
    id: "arch-m11-8",
    number: "ARCH-M11-8",
    title: "Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Dependency Injection",
    expectedAnswer: "Dependency injection improves tests by allowing deterministic fakes, stubs, or in-memory implementations to replace network, storage, time, and other side effects.",
    deepExplanation: "Step 1 — Understand Testing.\n\nDependency injection improves tests by allowing deterministic fakes, stubs, or in-memory implementations to replace network, storage, time, and other side effects.\n\nCore idea: keep the boundary explicit, validate inputs, and keep implementation details behind a small contract.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface PaymentGateway {\n  charge(amount: number): Promise<boolean>;\n}\n\nasync function checkout(\n  gateway: PaymentGateway,\n  amount: number,\n): Promise<boolean> {\n  if (amount <= 0) return false;\n  return gateway.charge(amount);\n}\n\nconst fakeGateway: PaymentGateway = {\n  async charge() {\n    return true;\n  },\n};\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nfunction checkout(\n  charge: (amount: number) => Promise<boolean>,\n  amount: number,\n): Promise<boolean> {\n  if (amount <= 0) return Promise.resolve(false);\n  return charge(amount);\n}\n\nconst result = checkout(\n  async () => true,\n  100,\n);\n```\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest interface, implement the simplest working path, and only then add tooling or optimization.\n\nStep 3 — Production reasoning:\nUse dependency seams where behavior crosses a meaningful boundary.\n\nComplexity:\nThe examples use direct lookups, loops, maps, sets, or simple transforms; the practical complexity is driven by the data size and real I/O involved.\n\nStep 4 — Failure/edge cases:\nHandle invalid input, unavailable dependencies, stale state, lifecycle cleanup, version mismatches, and partial failure according to the boundary being modeled.\n\nStep 5 — Senior interview takeaway:\nExplain why the boundary exists, what it owns, what it deliberately does not own, and the simplest alternative you would choose for a smaller system.",
    productionExample: "Use dependency seams where behavior crosses a meaningful boundary.",
    bestPractices: ["Define the ownership and public contract before choosing a tool or pattern.", "Keep boundaries explicit and make dependency direction enforceable.", "Prefer the simplest implementation that satisfies the real use case.", "Add tests for failure paths, lifecycle, compatibility, and migration behavior.", "Measure performance and operational cost instead of assuming the abstraction is free."],
    tradeOffs: "Over-mocking can make tests pass while the real integration is broken.",
    commonMistakes: ["Choosing the tool or pattern because it is fashionable rather than necessary.", "Hiding important behavior behind overly generic abstractions.", "Ignoring failure, cleanup, versioning, or compatibility requirements.", "Creating shared infrastructure before there are real consumers.", "Optimizing for hypothetical scale instead of current constraints."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the dependency or ownership boundary?", "How would you test it?", "What are its failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it in production?", "What trade-offs would you explain in a senior interview?"],
    relatedTopics: ["Testing", "Mocks", "Fakes", "Dependency Injection"],
  },
];

export const MOCK_FRONTEND_MACHINE_CODING_MODULE11_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Architecture',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));