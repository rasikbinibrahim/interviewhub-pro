// Derived from frontend/src/document/Frontend_Architecture_Design_Patterns_Enterprise_Development_Master_Handbook.md (Module 12 — Enterprise React Applications).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "arch-m12-1",
    number: "ARCH-M12-1",
    title: "CRM",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Enterprise React Applications",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// CRM: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does CRM solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Enterprise React Applications"],
  },
  {
    id: "arch-m12-2",
    number: "ARCH-M12-2",
    title: "ERP",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Enterprise React Applications",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// ERP: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does ERP solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Enterprise React Applications"],
  },
  {
    id: "arch-m12-3",
    number: "ARCH-M12-3",
    title: "Banking",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Enterprise React Applications",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Banking: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Banking solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Enterprise React Applications"],
  },
  {
    id: "arch-m12-4",
    number: "ARCH-M12-4",
    title: "Healthcare",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Enterprise React Applications",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Healthcare: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Healthcare solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Enterprise React Applications"],
  },
  {
    id: "arch-m12-5",
    number: "ARCH-M12-5",
    title: "LMS",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Enterprise React Applications",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// LMS: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does LMS solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Enterprise React Applications"],
  },
  {
    id: "arch-m12-6",
    number: "ARCH-M12-6",
    title: "Analytics",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Enterprise React Applications",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Analytics: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Analytics solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Enterprise React Applications"],
  },
  {
    id: "arch-m12-7",
    number: "ARCH-M12-7",
    title: "Admin Dashboard",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Enterprise React Applications",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Admin Dashboard: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Admin Dashboard solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Enterprise React Applications"],
  },
  {
    id: "arch-m12-8",
    number: "ARCH-M12-8",
    title: "Multi-tenant SaaS",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Enterprise React Applications",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Multi-tenant SaaS: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Multi-tenant SaaS solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Enterprise React Applications"],
  },
  {
    id: "arch-m12-9",
    number: "ARCH-M12-9",
    title: "Offline-first Apps",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Enterprise React Applications",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Offline-first Apps: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Offline-first Apps solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Enterprise React Applications"],
  },
  {
    id: "arch-m12-10",
    number: "ARCH-M12-10",
    title: "White-label Products",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Enterprise React Applications",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// White-label Products: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does White-label Products solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Enterprise React Applications"],
  },
];

export const MOCK_FRONTEND_ARCHITECTURE_MODULE12_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Architecture',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
