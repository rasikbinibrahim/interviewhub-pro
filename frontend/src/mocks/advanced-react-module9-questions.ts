// Derived from frontend/src/document/Part_9_Advanced_React_Master_Handbook.md (Module 9 — Server Components).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "reactadv-m9-151",
    number: "REACTADV-M9-151",
    title: "React Server Components",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "React Server Components (RSC) are components whose rendering and server-side data access happen on the server, allowing server-only dependencies and implementation details to stay out of the browser bundle. They can compose with Client Components at explicit boundaries. RSC is a rendering architecture used by React frameworks/runtimes that support it; it is not the same thing as SSR, because Server Components describe component execution and the resulting server-rendered component payload, while SSR is a way of producing HTML.",
    deepExplanation: "```tsx\n// Server Component in an RSC-capable framework\nexport default async function ProductPage({\n  id,\n}: {\n  id: string;\n}) {\n  const product = await getProduct(id);\n\n  return (\n    <main>\n      <h1>{product.name}</h1>\n      <AddToCart productId={product.id} />\n    </main>\n  );\n}\n```\n\n```tsx\n// Client Component\n\"use client\";\n\nexport function AddToCart({ productId }: { productId: string }) {\n  return (\n    <button onClick={() => addToCart(productId)}>\n      Add to cart\n    </button>\n  );\n}\n```",
    productionExample: "For an e-commerce product page, the server component can fetch product data and keep database/API credentials and server-only libraries on the server. Only the interactive AddToCart control becomes client code. The production design still needs authorization, validation, caching and observability.",
    bestPractices: ["Keep database credentials, secrets and server-only SDKs on the server.", "Push the client boundary down to the smallest interactive subtree.", "Pass only supported serializable values across the server/client boundary.", "Use framework-supported caching and revalidation deliberately.", "Keep authorization on the server even when a UI is rendered by an RSC.", "Measure the client JavaScript reduction rather than assuming every RSC conversion improves performance."],
    tradeOffs: "Advantages: smaller client bundles; direct server data access; clear separation between server-only and browser-only code; strong composition. Trade-offs: framework/runtime coupling; serialization boundaries; more complex debugging; caching can affect correctness; interactive behavior still requires Client Components.",
    commonMistakes: ["Calling RSC the same thing as SSR.", "Assuming every component is automatically a Server Component in every React setup.", "Passing arbitrary functions across the boundary.", "Putting secrets in Client Components.", "Assuming RSC eliminates all client JavaScript."],
    followUpQuestions: ["RSC vs SSR?", "RSC vs static rendering?", "Why does a Client Component boundary affect the bundle?", "What can cross an RSC boundary?", "How would you measure an RSC migration?"],
    relatedTopics: ["Server Components", "Client Components", "SSR", "RSC Payload", "Serialization", "Streaming", "Suspense", "Framework integration"],
  },
  {
    id: "reactadv-m9-152",
    number: "REACTADV-M9-152",
    title: "Server versus Client Components",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "Server and Client Components have different execution environments. Server Components can access server resources and do not ship their implementation to the browser. Client Components run in the browser and can use interactive APIs such as state, effects and browser event handlers. The important architectural decision is where the client boundary should be placed, not simply labeling every component as one type.",
    deepExplanation: "```tsx\n// Server Component\nexport default async function Dashboard() {\n  const account = await getAccount();\n\n  return (\n    <section>\n      <h1>{account.name}</h1>\n      <Notifications />\n    </section>\n  );\n}\n```\n\n```tsx\n// Client Component\n\"use client\";\n\nimport { useState } from \"react\";\n\nexport function Notifications() {\n  const [open, setOpen] = useState(false);\n\n  return (\n    <>\n      <button onClick={() => setOpen(value => !value)}>\n        Notifications\n      </button>\n      {open && <div>3 new notifications</div>}\n    </>\n  );\n}\n```",
    productionExample: "A SaaS dashboard keeps the page shell, permissions lookup and account summary on the server, while interactive filters, menus and editors are Client Components. This reduces browser JavaScript without forcing the entire dashboard to become server-only.",
    bestPractices: ["Use Server Components by default where the framework supports them.", "Create Client Components only where browser interactivity or client-only APIs are required.", "Keep client boundaries narrow.", "Avoid importing server-only modules into client code.", "Design data ownership and caching before choosing boundaries."],
    tradeOffs: "Advantages: smaller client surface and direct server access. Trade-offs: boundaries complicate data flow and testing; client components are still necessary for interaction; framework conventions matter.",
    commonMistakes: ["Using `useState` or browser APIs in a Server Component.", "Adding `use client` to an entire route unnecessarily.", "Assuming `use client` means the component only renders in the browser.", "Passing non-supported values through the boundary."],
    followUpQuestions: ["Where should the boundary be placed?", "What happens to imported modules under a client boundary?", "Can a Client Component render a Server Component directly?", "How would you migrate a large SPA?"],
    relatedTopics: ["use client", "Server Components", "Client Components", "Hooks", "Bundle splitting", "SSR"],
  },
  {
    id: "reactadv-m9-153",
    number: "REACTADV-M9-153",
    title: "use client boundary",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "The `\"use client\"` directive marks a module as a Client Component entry boundary in an RSC-capable framework. It tells the framework that the module and its client-side dependency graph are intended to run in the browser. It is not a generic React directive for enabling hooks in every React application.",
    deepExplanation: "```tsx\n\"use client\";\n\nimport { useState } from \"react\";\n\nexport function SearchBox() {\n  const [query, setQuery] = useState(\"\");\n\n  return (\n    <label>\n      Search\n      <input\n        value={query}\n        onChange={event => setQuery(event.target.value)}\n      />\n    </label>\n  );\n}\n```",
    productionExample: "A search box requires browser input state and event handlers, so it is a Client Component. The parent results page can remain a Server Component and pass the minimum supported data/configuration needed by the search UI.",
    bestPractices: ["Place `use client` at the smallest practical boundary.", "Keep heavy server-only dependencies outside the client graph.", "Pass minimal serializable props.", "Do not add the directive merely because a component is reused.", "Review bundle analysis after introducing a client boundary."],
    tradeOffs: "Advantages: explicit browser boundary and predictable client dependency graph. Trade-offs: everything reachable through the client entry can contribute to the client bundle; server-only APIs cannot be used there; props crossing the boundary have constraints.",
    commonMistakes: ["Thinking `use client` is required for every component using React.", "Putting the directive at the root layout/page without need.", "Importing database/secret modules into a Client Component.", "Passing arbitrary functions as props across the RSC boundary."],
    followUpQuestions: ["Does `use client` make a component render only on the client?", "How does the directive affect bundle size?", "What props can cross the boundary?", "How do you find an oversized client boundary?"],
    relatedTopics: ["Client Components", "Server Components", "Serialization", "Bundle splitting", "Hooks"],
  },
  {
    id: "reactadv-m9-154",
    number: "REACTADV-M9-154",
    title: "Serializable props",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "Props crossing an RSC server-to-client boundary must be values React's serialization protocol supports. Typical supported values include primitives, arrays, objects, and certain React/JavaScript values defined by the React Server Components protocol; ordinary application functions and server-only objects should not be treated as transferable browser values. A practical rule is to pass data, identifiers and configuration—not live server objects or callbacks.",
    deepExplanation: "```tsx\n// Server Component\nexport default async function ProductPage() {\n  const product = await getProduct();\n\n  return (\n    <ProductActions\n      productId={product.id}\n      initialQuantity={1}\n    />\n  );\n}\n```\n\n```tsx\n\"use client\";\n\ntype Props = {\n  productId: string;\n  initialQuantity: number;\n};\n\nexport function ProductActions({\n  productId,\n  initialQuantity,\n}: Props) {\n  // Browser-side interaction uses the identifier/data.\n  return <button data-product-id={productId}>Add</button>;\n}\n```",
    productionExample: "Instead of passing a database model containing methods, sockets or internal fields to a Client Component, the server maps it to a small view model such as `{id, name, price}`. This reduces coupling and avoids leaking server-only implementation details.",
    bestPractices: ["Define explicit DTO/view-model types at boundaries.", "Pass IDs rather than live server resources.", "Keep secrets and authorization data out of props.", "Avoid passing huge objects when only a few fields are needed.", "Test the actual framework/RSC serialization behavior for uncommon types."],
    tradeOffs: "Advantages: explicit boundary and reduced coupling. Trade-offs: serialization can require mapping/transformation; large payloads increase transfer cost; complex values may not cross the boundary as expected.",
    commonMistakes: ["Passing database clients or class instances as if they were ordinary JSON.", "Passing a callback from a Server Component to a Client Component without using a supported server-function mechanism.", "Assuming every JavaScript object is serializable.", "Sending entire records when a small DTO is enough."],
    followUpQuestions: ["Why can't ordinary functions cross the boundary?", "What is a DTO in an RSC architecture?", "How would you validate boundary data?", "How does payload size affect performance?"],
    relatedTopics: ["RSC serialization", "DTO", "Server Actions", "Client Components", "RSC Payload"],
  },
  {
    id: "reactadv-m9-155",
    number: "REACTADV-M9-155",
    title: "Server Component rendering",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "A Server Component executes on the server and contributes to the RSC render result rather than behaving like a normal browser component with client-side state. The framework can combine that result with HTML/streaming and Client Component references. Server Component rendering can therefore access server-side data sources while keeping the component implementation out of the browser bundle.",
    deepExplanation: "```tsx\nexport default async function OrdersPage() {\n  const orders = await db.order.findMany({\n    orderBy: { createdAt: \"desc\" },\n    take: 20,\n  });\n\n  return (\n    <ul>\n      {orders.map(order => (\n        <li key={order.id}>\n          {order.id}: {order.total}\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "An authenticated orders page fetches the user's orders on the server and renders the initial list there. A separate Client Component handles filters or pagination controls when browser interaction is required.",
    bestPractices: ["Keep server-only data access in server code.", "Avoid unnecessary client conversion of data-heavy components.", "Use stable keys and explicit loading/error states.", "Understand the framework's caching behavior.", "Measure server latency and RSC/HTML payload size."],
    tradeOffs: "Advantages: direct server data access and reduced client code. Trade-offs: server latency affects initial response; dynamic rendering can increase server work; debugging crosses server/client environments.",
    commonMistakes: ["Assuming Server Components can use browser event handlers.", "Confusing the RSC payload with final HTML.", "Ignoring server latency because the browser bundle is smaller.", "Performing expensive database work on every render without a caching strategy."],
    followUpQuestions: ["RSC payload vs HTML?", "How does streaming change rendering?", "How do you cache server component data?", "How do errors propagate?"],
    relatedTopics: ["RSC Payload", "SSR", "Streaming", "Suspense", "Caching", "Server data fetching"],
  },
  {
    id: "reactadv-m9-156",
    number: "REACTADV-M9-156",
    title: "Streaming Server Components",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "Streaming Server Components allow server-rendered work to be delivered progressively instead of waiting for the entire component tree to finish. Suspense boundaries can define units of work that may stream as their data becomes available. This can improve time to first useful UI while preserving independent loading states.",
    deepExplanation: "```tsx\nimport { Suspense } from \"react\";\n\nexport default function Dashboard() {\n  return (\n    <main>\n      <h1>Dashboard</h1>\n\n      <Suspense fallback={<RevenueSkeleton />}>\n        <Revenue />\n      </Suspense>\n\n      <Suspense fallback={<OrdersSkeleton />}>\n        <RecentOrders />\n      </Suspense>\n    </main>\n  );\n}\n```\n\n```tsx\nasync function Revenue() {\n  const revenue = await getRevenue();\n  return <strong>{revenue}</strong>;\n}\n```",
    productionExample: "A dashboard has a fast account summary and a slow analytics query. The summary can appear while analytics is still pending, with the analytics section streaming into its Suspense boundary when ready.",
    bestPractices: ["Place Suspense boundaries around meaningful independent work.", "Use loading UIs that preserve layout stability.", "Avoid creating hundreds of tiny boundaries without a reason.", "Measure TTFB, LCP, INP and server workload.", "Make slow backend dependencies observable."],
    tradeOffs: "Advantages: progressive rendering and better perceived performance. Trade-offs: more complex loading states; backend latency can still dominate; poor boundary placement can create fragmented UI or waterfalls.",
    commonMistakes: ["Assuming streaming makes slow database queries fast.", "Putting one Suspense boundary around the entire page.", "Using skeletons that cause large layout shifts.", "Ignoring server backpressure and concurrency."],
    followUpQuestions: ["Streaming SSR vs streaming RSC?", "How do Suspense boundaries affect UX?", "How do you avoid waterfalls?", "How would you measure streaming success?"],
    relatedTopics: ["Suspense", "Streaming SSR", "RSC Payload", "Loading UI", "Performance"],
  },
  {
    id: "reactadv-m9-157",
    number: "REACTADV-M9-157",
    title: "Hydration boundaries",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "A hydration boundary is an architectural point where server-rendered UI becomes interactive on the client. In an RSC-capable application, Client Components define much of the interactive client surface, while the framework coordinates hydration. Good boundaries minimize unnecessary client JavaScript and keep the initial client render consistent with the server output.",
    deepExplanation: "```tsx\n// Server Component\nexport default function Page() {\n  return (\n    <>\n      <Header />\n      <InteractiveFilter />\n    </>\n  );\n}\n\n// Client boundary\n\"use client\";\n\nexport function InteractiveFilter() {\n  const [value, setValue] = useState(\"all\");\n\n  return (\n    <select value={value} onChange={e => setValue(e.target.value)}>\n      <option value=\"all\">All</option>\n      <option value=\"open\">Open</option>\n    </select>\n  );\n}\n```",
    productionExample: "A commerce page renders product content on the server but hydrates only the cart controls, filters and account menu. The team verifies that the server and initial client output match and monitors hydration errors and interaction latency.",
    bestPractices: ["Keep interactive boundaries narrow.", "Make the initial client render deterministic.", "Avoid browser-only values during the server/client matching render.", "Use framework-supported lazy/deferred hydration features when available.", "Monitor hydration errors and client JavaScript size."],
    tradeOffs: "Advantages: smaller interactive surface and potentially faster startup. Trade-offs: too many boundaries can increase complexity; client interactivity cannot exist before its code is available; hydration still consumes CPU.",
    commonMistakes: ["Treating every server-rendered element as independently hydrated.", "Reading localStorage during the initial render without a strategy.", "Ignoring hydration mismatch warnings.", "Assuming less JavaScript means zero client CPU cost."],
    followUpQuestions: ["What causes hydration mismatch?", "How do RSC and hydration differ?", "How would you prioritize hydration?", "What metrics show hydration is too expensive?"],
    relatedTopics: ["Hydration", "Client Components", "RSC", "SSR", "Streaming", "Performance"],
  },
  {
    id: "reactadv-m9-158",
    number: "REACTADV-M9-158",
    title: "Server Actions relationship",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "Server Actions are framework-integrated async server functions that can be invoked from Client Components/forms through the React Server Components architecture. React 19 provides the Action model and APIs such as form Actions and useActionState; the exact server transport, authorization model, caching and revalidation behavior is framework-specific. Server Actions are for mutations or server-side operations, not a replacement for every API.",
    deepExplanation: "```tsx\n// Server-side module in a framework supporting Server Actions\n\"use server\";\n\nexport async function updateProfile(formData: FormData) {\n  const session = await requireUser();\n  const name = String(formData.get(\"name\") ?? \"\").trim();\n\n  if (!name) {\n    return { ok: false, message: \"Name is required\" };\n  }\n\n  await db.user.update({\n    where: { id: session.userId },\n    data: { name },\n  });\n\n  return { ok: true, message: \"Saved\" };\n}\n```\n\n```tsx\n\"use client\";\n\nimport { updateProfile } from \"./actions\";\n\nexport function ProfileForm() {\n  return (\n    <form action={updateProfile}>\n      <input name=\"name\" required />\n      <button type=\"submit\">Save</button>\n    </form>\n  );\n}\n```",
    productionExample: "A settings form submits directly to a server mutation. The server checks authentication and authorization, validates the FormData, writes to the database and performs framework-specific cache invalidation/revalidation. The client shows pending and result state through React Action APIs.",
    bestPractices: ["Authenticate and authorize every mutation.", "Treat FormData and action arguments as untrusted input.", "Use idempotency for operations where retries can duplicate side effects.", "Return structured expected-error results.", "Understand framework-specific cache invalidation/revalidation.", "Never expose secrets in client code."],
    tradeOffs: "Advantages: less custom mutation plumbing and strong form integration. Trade-offs: framework coupling; network boundaries remain; security and observability are still application responsibilities; not every React host supports Server Actions.",
    commonMistakes: ["Saying Server Actions and Server Components are the same thing.", "Assuming `use server` automatically makes a function secure.", "Skipping authorization because the action is called from a trusted UI.", "Assuming React core defines the framework's HTTP transport."],
    followUpQuestions: ["Server Action vs REST API?", "How do you secure a Server Action?", "How do you handle retries?", "How does cache revalidation work?", "When would you still use a normal API?"],
    relatedTopics: ["Server Actions", "Actions", "useActionState", "Forms", "Authentication", "Authorization", "Caching"],
  },
  {
    id: "reactadv-m9-159",
    number: "REACTADV-M9-159",
    title: "Caching",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "Caching in an RSC application exists at multiple layers: browser/cache headers, CDN, framework data/request caching, application caches and database caches. React Server Components do not define one universal application cache. A senior engineer must define the source of truth, cache key, TTL/revalidation policy, invalidation strategy and consistency requirements.",
    deepExplanation: "```tsx\nexport default async function ProductPage({\n  id,\n}: {\n  id: string;\n}) {\n  const product = await getProduct(id, {\n    revalidateSeconds: 300,\n  });\n\n  return <h1>{product.name}</h1>;\n}\n```\n\nThe exact caching API is framework-specific. The architectural contract should specify what is cached and when it becomes stale.",
    productionExample: "A product catalog can tolerate five-minute staleness, while inventory cannot. Product metadata is cached by product ID, but inventory is fetched from an authoritative service or invalidated more aggressively. The team monitors hit rate, stale reads and origin load.",
    bestPractices: ["Document cache keys and ownership.", "Define explicit freshness requirements per data type.", "Never cache sensitive user-specific data under a shared key.", "Invalidate or revalidate after mutations where correctness requires it.", "Measure hit rate and stale/error behavior.", "Separate framework cache semantics from business-level caching."],
    tradeOffs: "Advantages: lower origin load and latency; better scalability. Trade-offs: stale data; invalidation complexity; memory/storage cost; cache stampedes; privacy risks from incorrect keying.",
    commonMistakes: ["Treating all data as safe to cache.", "Caching user-specific responses under a public key.", "Using TTL without considering mutation invalidation.", "Assuming React automatically caches every fetch.", "Ignoring cache stampede and cold-start behavior."],
    followUpQuestions: ["What is your cache key?", "How do you invalidate after a mutation?", "What data should never be publicly cached?", "How would you prevent a stampede?", "How do you measure cache effectiveness?"],
    relatedTopics: ["Revalidation", "CDN", "HTTP Cache-Control", "Data fetching", "Server Actions", "Consistency"],
  },
  {
    id: "reactadv-m9-160",
    number: "REACTADV-M9-160",
    title: "Data fetching on the server",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "Server-side data fetching in an RSC-capable application means fetching data in Server Components or server-side functions so credentials and server-only dependencies remain on the server. It can reduce browser waterfalls and expose data close to the rendering boundary. The correct strategy still depends on freshness, personalization, latency, caching and mutation requirements.",
    deepExplanation: "```tsx\nexport default async function OrdersPage() {\n  const session = await requireUser();\n\n  const orders = await getOrders({\n    userId: session.userId,\n    limit: 20,\n  });\n\n  return (\n    <section>\n      <h1>Recent orders</h1>\n      {orders.map(order => (\n        <article key={order.id}>\n          <strong>{order.id}</strong>\n          <span>{order.total}</span>\n        </article>\n      ))}\n    </section>\n  );\n}\n```",
    productionExample: "An account page fetches the authenticated user's profile and orders on the server. The browser receives only the rendered result and the interactive client code it needs. Sensitive service credentials never enter the client bundle.",
    bestPractices: ["Authenticate before user-specific fetches.", "Validate authorization at the data-access layer.", "Avoid request waterfalls by parallelizing independent server fetches where appropriate.", "Use caching only when the data's privacy/freshness requirements allow it.", "Add timeouts, retries and observability around remote services.", "Keep data-access code separate from presentation when it improves testability."],
    tradeOffs: "Advantages: secure server-only credentials; lower client data-fetching complexity; potentially fewer browser waterfalls. Trade-offs: server response time increases with slow dependencies; personalized rendering can reduce cacheability; server load increases.",
    commonMistakes: ["Fetching user-specific data without authorization.", "Serially awaiting independent requests.", "Returning sensitive fields to the client unnecessarily.", "Assuming server fetching automatically means the response is cached."],
    followUpQuestions: ["How do you parallelize server fetches?", "How do you handle a slow API?", "What belongs in a repository/data-access layer?", "How do you cache personalized data safely?"],
    relatedTopics: ["Server Components", "Data fetching", "Caching", "Authentication", "Authorization", "Suspense"],
  },
  {
    id: "reactadv-m9-161",
    number: "REACTADV-M9-161",
    title: "Client bundle reduction",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "RSC can reduce the client bundle because Server Component implementations and their server-only dependencies are not shipped as browser JavaScript. The biggest gains come from keeping large data-heavy or server-only subtrees on the server and limiting Client Component boundaries. Bundle reduction should be measured by transferred JavaScript, parsed/evaluated bytes and interaction startup—not only raw bundle size.",
    deepExplanation: "```tsx\n// Keep this server-side when no browser interactivity is needed.\nexport async function ProductRecommendations() {\n  const products = await getRecommendations();\n\n  return (\n    <ul>\n      {products.map(product => (\n        <li key={product.id}>{product.name}</li>\n      ))}\n    </ul>\n  );\n}\n```\n\nOnly the interactive controls around this list need to become Client Components.",
    productionExample: "A marketplace initially marks an entire product page as client-rendered. The team moves product description, recommendations and review summaries to Server Components while retaining the gallery controls and cart button as Client Components. Bundle analysis confirms the reduction.",
    bestPractices: ["Measure before/after transferred and evaluated JavaScript.", "Keep large server-only libraries out of client dependency graphs.", "Avoid broad `use client` boundaries.", "Lazy-load genuinely optional client features.", "Track Core Web Vitals and interaction performance after bundle changes."],
    tradeOffs: "Advantages: smaller browser JavaScript; less hydration work; direct server access. Trade-offs: server execution increases; boundaries can complicate architecture; some interactions require client code; bundle reduction may not improve every metric if server latency dominates.",
    commonMistakes: ["Assuming every RSC page has zero client JavaScript.", "Putting `use client` on a high-level component that imports many libraries.", "Measuring only gzip size and ignoring parse/evaluation cost.", "Ignoring server response latency."],
    followUpQuestions: ["How do you identify the largest client boundary?", "Which metric matters more: bundle size or execution time?", "How would you prove RSC improved LCP?", "How do you prevent client dependency leakage?"],
    relatedTopics: ["RSC", "use client", "Code splitting", "Hydration", "Bundle analysis", "Core Web Vitals"],
  },
  {
    id: "reactadv-m9-162",
    number: "REACTADV-M9-162",
    title: "RSC limitations",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "RSC has important limits: it requires an RSC-capable runtime/framework; Server Components cannot use browser-only interactive APIs; data and component boundaries have serialization constraints; server rendering introduces server latency and operational complexity; caching and invalidation can become subtle; debugging spans server and browser environments. RSC is an architectural tool, not a universal replacement for client rendering.",
    deepExplanation: "```tsx\n// Server Component: good for data access\nexport async function ProductInfo() {\n  const product = await getProduct();\n  return <ProductDetails product={product} />;\n}\n\n// Client Component: required for interaction\n\"use client\";\n\nexport function ProductDetails({ product }: {\n  product: { id: string; name: string };\n}) {\n  const [expanded, setExpanded] = useState(false);\n\n  return (\n    <button onClick={() => setExpanded(value => !value)}>\n      {expanded ? \"Hide\" : \"Show\"} {product.name}\n    </button>\n  );\n}\n```",
    productionExample: "A real-time collaborative editor should not be converted entirely to Server Components simply to reduce JavaScript. Its core editing interactions, subscriptions and local state require a substantial client runtime. RSC can still be used for surrounding navigation, initial data and non-interactive content.",
    bestPractices: ["Choose RSC based on workload and interaction needs.", "Keep highly interactive surfaces client-side when appropriate.", "Define server/client data contracts.", "Plan caching and invalidation before migration.", "Use progressive adoption rather than rewriting the whole application.", "Keep operational observability for both server and browser."],
    tradeOffs: "Advantages: server-first data access and smaller client surface where applicable. Trade-offs: architectural complexity; server dependency; serialization constraints; caching/invalidation challenges; not ideal for every highly interactive workload.",
    commonMistakes: ["Thinking RSC means no JavaScript.", "Converting a real-time editor entirely to Server Components.", "Ignoring framework support requirements.", "Assuming RSC solves data consistency automatically."],
    followUpQuestions: ["When should you not use RSC?", "How would RSC work with a real-time app?", "What are the biggest migration risks?", "How do caching and personalization interact?"],
    relatedTopics: ["RSC", "Client Components", "Real-time applications", "Caching", "Serialization", "Architecture"],
  },
  {
    id: "reactadv-m9-163",
    number: "REACTADV-M9-163",
    title: "Security considerations",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "RSC changes where code and data execute, but it does not automatically secure an application. Server Components and Server Actions can access sensitive resources, so authorization must be enforced on the server. Client Components must never receive secrets, privileged tokens or data they are not allowed to display. Inputs crossing the boundary must be treated as untrusted.",
    deepExplanation: "```tsx\n// Server-side data access\nexport async function getAdminReport() {\n  const session = await requireUser();\n\n  if (!session.roles.includes(\"admin\")) {\n    throw new Error(\"Forbidden\");\n  }\n\n  return db.auditLog.findMany({\n    take: 100,\n  });\n}\n```\n\nThe Client Component should receive only the report data it is authorized to display, not database credentials or privileged server objects.",
    productionExample: "An admin dashboard checks authorization on the server before querying audit records. The UI may hide the admin navigation for normal users, but that is only UX; the server authorization remains the security boundary.",
    bestPractices: ["Authorize on the server for every sensitive operation.", "Validate all client-provided IDs, FormData and action arguments.", "Return least-privilege view models.", "Keep secrets server-side.", "Use secure session/cookie configuration.", "Apply CSP and standard web security controls.", "Log security-relevant failures without leaking sensitive data."],
    tradeOffs: "Advantages: server execution can keep secrets and privileged SDKs out of the browser. Trade-offs: server code becomes a high-value security boundary; mistakes in authorization or cache keys can expose data; distributed caching adds privacy considerations.",
    commonMistakes: ["Relying on hidden UI controls for authorization.", "Caching personalized data publicly.", "Passing tokens/secrets as component props.", "Trusting a user ID from FormData without authorization.", "Assuming Server Actions are private internal functions."],
    followUpQuestions: ["How do you secure a Server Action?", "What can leak through a cache?", "How do you prevent IDOR?", "How would you audit an RSC application?"],
    relatedTopics: ["Authentication", "Authorization", "Server Actions", "Caching", "CSP", "Data privacy", "Input validation"],
  },
  {
    id: "reactadv-m9-164",
    number: "REACTADV-M9-164",
    title: "Debugging RSC",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "Debugging RSC requires separating server execution problems, RSC boundary/serialization problems, framework routing/caching problems and browser hydration/client interaction problems. A senior workflow reproduces the issue, identifies which environment owns the failing code, inspects the server response and client bundle, then verifies cache and boundary behavior.",
    deepExplanation: "```tsx\n// Server Component\nexport default async function Page() {\n  console.log(\"server:\", new Date().toISOString());\n\n  const data = await getData();\n\n  return (\n    <>\n      <h1>{data.title}</h1>\n      <ClientWidget value={data.value} />\n    </>\n  );\n}\n```\n\n```tsx\n\"use client\";\n\nexport function ClientWidget({ value }: { value: number }) {\n  console.log(\"client:\", value);\n  return <button>{value}</button>;\n}\n```\n\nThe logs demonstrate that the two components execute in different environments; production debugging should additionally use structured server/client telemetry.",
    productionExample: "If a dashboard shows stale data, first determine whether the source is the database, server cache, framework cache, RSC response, browser cache or client state. If the page has a hydration mismatch, compare server output and the first client render before changing unrelated components.",
    bestPractices: ["Log server and client events with request/correlation IDs.", "Inspect network responses and RSC/HTML behavior using framework-supported tooling.", "Check cache keys and revalidation timing.", "Reproduce with production-like SSR and streaming.", "Keep server/client boundaries explicit.", "Add error monitoring for server exceptions, hydration errors and client exceptions."],
    tradeOffs: "Advantages: systematic environment-based debugging reduces guesswork. Trade-offs: RSC issues can span several layers; local development behavior may differ from production caching/streaming; framework internals can be complex.",
    commonMistakes: ["Looking only at browser console logs.", "Assuming stale data is always a React state bug.", "Ignoring framework caching.", "Debugging a hydration mismatch without comparing server and client output.", "Logging secrets while trying to diagnose server code."],
    followUpQuestions: ["How do you debug stale RSC data?", "How do you debug a serialization error?", "How do you trace one request across server and browser?", "What would you inspect in the network panel?"],
    relatedTopics: ["RSC", "Hydration", "Caching", "Observability", "Streaming", "Error handling"],
  },
  {
    id: "reactadv-m9-165",
    number: "REACTADV-M9-165",
    title: "Enterprise RSC architecture",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Server Components",
    expectedAnswer: "Enterprise RSC architecture should establish clear server/client boundaries, data-access ownership, authentication/authorization, caching/revalidation, error handling, observability and performance budgets. RSC should be adopted selectively: server-render data-heavy or server-only features, while interactive controls remain client-side. The architecture must also account for framework constraints, deployment topology and team conventions.",
    deepExplanation: "```tsx\n// Server page\nexport default async function AccountPage() {\n  const user = await requireUser();\n  const account = await getAccount(user.id);\n\n  return (\n    <main>\n      <AccountSummary account={account} />\n      <AccountPreferences userId={user.id} />\n    </main>\n  );\n}\n```\n\n```tsx\n// Interactive boundary\n\"use client\";\n\nexport function AccountPreferences({\n  userId,\n}: {\n  userId: string;\n}) {\n  const [editing, setEditing] = useState(false);\n\n  return (\n    <section>\n      <button onClick={() => setEditing(value => !value)}>\n        {editing ? \"Close\" : \"Edit\"}\n      </button>\n      {editing && <PreferencesForm userId={userId} />}\n    </section>\n  );\n}\n```",
    productionExample: "For a large SaaS platform, teams define a default Server Component policy, a narrow Client Component policy, shared server data-access libraries, typed DTOs, authentication middleware, cache/revalidation conventions, observability standards and performance budgets. Feature teams can adopt RSC without inventing their own security or caching model.",
    bestPractices: ["Create architecture rules for client boundaries.", "Centralize authentication and authorization.", "Define typed server-to-client view models.", "Document cache and invalidation policies.", "Instrument server latency, RSC payloads and browser performance.", "Use error boundaries and structured expected-error handling.", "Roll out incrementally with feature-level metrics.", "Keep framework-specific behavior behind clear application conventions."],
    tradeOffs: "Advantages: scalable separation of server and client concerns; smaller client surface; reusable enterprise conventions. Trade-offs: significant architecture and tooling complexity; framework dependency; migration cost; caching and debugging require mature engineering practices.",
    commonMistakes: ["Making the whole application a Client Component.", "Creating inconsistent security and caching rules across teams.", "Passing large database objects across boundaries.", "Migrating without bundle, latency and error baselines.", "Treating RSC as a replacement for API/domain architecture."],
    followUpQuestions: ["How would you migrate a 1M-line SPA?", "How would you standardize RSC across 50 teams?", "What belongs in a shared platform layer?", "Which metrics define migration success?", "How do you handle framework lock-in?"],
    relatedTopics: ["RSC", "Client Components", "Architecture", "Caching", "Security", "Observability", "Performance", "Migration"],
  },

];

export const MOCK_ADVANCED_REACT_MODULE9_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Advanced React',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));