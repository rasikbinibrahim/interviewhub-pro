// Python + DSA Interview Handbook — Module 23: Production Engineering.
// Hand-authored technical questions covering how a Python/FastAPI +
// PostgreSQL/MongoDB application actually goes from local development to a
// reliable, observable, secure production system — the Twelve-Factor App,
// Linux process/signal handling for graceful shutdown, FastAPI's production
// deployment architecture (Uvicorn/Gunicorn workers, health checks, error
// handling), Docker/CI-CD, testing strategy, rate limiting and idempotency,
// caching failure modes, observability (RED/USE, SLI/SLO/SLA, tracing),
// reliability patterns (retries, timeouts, circuit breakers, bulkheads),
// background processing/message queues, file/object storage, scalability,
// incident response, and disaster recovery — with genuine architecture
// reasoning and production-grade code. Mirrors the MockTechnicalQuestion
// shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  'Google',
  'Meta',
  'Amazon',
  'Microsoft',
  'Netflix',
  'Adobe',
  'Atlassian',
  'Stripe',
  'Uber',
  'Flipkart',
  'Zoho',
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};

const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: 'python-m23-1',
    number: 'PY-M23-1',
    title: 'The Twelve-Factor App, applied concretely to a FastAPI service',
    difficulty: 'Medium',
    experienceLevel: '2–4 Years',
    category: 'Production Fundamentals',
    expectedAnswer:
      'The Twelve-Factor App is a set of principles for building applications that deploy cleanly and scale predictably on modern infrastructure — the ones that matter most for a FastAPI service are: config lives in ENVIRONMENT VARIABLES (never hard-coded or checked into git), the app is STATELESS and treats backing services (PostgreSQL, MongoDB, Redis) as attached resources reachable via a URL, processes are DISPOSABLE (fast startup, graceful shutdown on SIGTERM), and LOGS are treated as an event stream written to stdout, not managed as files by the application itself.',
    deepExplanation:
      'The factors most directly relevant to a FastAPI/Python production service, with concrete implementation:\n\n```text\nI.    Codebase       — one codebase in version control, many deploys (dev/staging/prod all run\n                        the SAME codebase at different revisions, not divergent forks)\nII.   Dependencies   — explicitly declared (pyproject.toml/requirements.txt) and ISOLATED\n                        (virtual env / container) — never rely on system-wide packages being present\nIII.  Config         — stored in environment variables, strictly separated from code\nIV.   Backing services — treated as attached resources via a URL (DATABASE_URL, REDIS_URL) —\n                        swapping a local Postgres for a managed cloud instance requires ZERO code change\nV.    Build/release/run — strictly separated stages: build (create the artifact), release\n                        (combine build + config), run (execute) — never modify code in the running env\nVI.   Processes      — the app is STATELESS; anything that must persist goes in a backing\n                        service (session state in Redis, not in-process memory)\nVII.  Port binding   — the app is self-contained and exports HTTP via port binding\n                        (Uvicorn listening on a port), not relying on a runtime-injected web server\nVIII. Concurrency     — scale via the PROCESS model (more Uvicorn/Gunicorn worker processes),\n                        not by making a single process handle unbounded work\nIX.   Disposability   — fast startup, and GRACEFUL SHUTDOWN on SIGTERM (finish in-flight\n                        requests, close DB connections cleanly) — see the Linux signals question\nX.    Dev/prod parity — keep dev, staging, and prod as similar as possible (same PostgreSQL\n                        version locally via Docker, not SQLite in dev + PostgreSQL in prod)\nXI.   Logs            — treated as an event STREAM written to stdout — the application never\n                        manages log FILES or rotation itself; the execution environment captures\n                        and routes stdout (Docker, Kubernetes, a log aggregator)\nXII.  Admin processes  — one-off tasks (a DB migration, a data backfill script) run as\n                        one-off processes in the SAME environment/codebase, not as ad hoc scripts\n                        with drifted dependencies\n```\n\nFastAPI-specific implementation of the highest-impact factors:\n\n```python\n# Factor III (config) — via pydantic-settings, reading from environment variables\nfrom pydantic_settings import BaseSettings\n\nclass Settings(BaseSettings):\n    database_url: str          # required — app FAILS TO START if missing, by design (fail fast)\n    redis_url: str\n    environment: str = "development"\n\nsettings = Settings()   # reads DATABASE_URL, REDIS_URL, ENVIRONMENT from the process environment\n\n# Factor XI (logs as a stream) — log to stdout, NEVER open a log file directly in application code\nimport logging, sys\nlogging.basicConfig(stream=sys.stdout, level=logging.INFO)\n```\n\nWhy Factor III (config in environment variables) is the single highest-leverage practice: it is what makes the EXACT SAME container image deployable to dev/staging/production unchanged — only the environment variables differ — which is the foundation of reliable, repeatable deployments and is directly what Docker/CI-CD pipelines (covered later in this module) are built around.',
    productionExample:
      'A FastAPI service reads `DATABASE_URL` from the environment and FAILS TO START (rather than silently falling back to a default/localhost value) if it is missing — this "fail fast on missing required config" behavior, combined with never hard-coding a database host, is precisely what let the same Docker image be promoted unchanged from staging to production during a deploy, with only the injected environment variables differing between the two environments.',
    bestPractices: [
      'Read ALL configuration from environment variables via a validated settings class (e.g. `pydantic-settings`) — fail fast at startup if a required value is missing, rather than silently using a wrong default.',
      'Write logs to stdout only — never have application code open, manage, or rotate log FILES directly; let the execution environment (Docker, Kubernetes, a log shipper) handle log routing and retention.',
      'Keep the SAME container image immutable across dev/staging/production, differing only by injected environment variables — never rebuild a different image per environment.',
    ],
    tradeOffs:
      'Strict Twelve-Factor discipline (stateless processes, config only via environment, logs only to stdout) adds a small amount of upfront structure compared to a quick local script, but is precisely what makes horizontal scaling, container orchestration, and reliable CI/CD promotion between environments possible without special-casing — violating these principles (e.g. storing session state in process memory) works fine for a single-instance toy project but breaks immediately the moment you run more than one instance.',
    commonMistakes: [
      'Hard-coding a database connection string or API key directly in source code (or worse, committing a `.env` file with real secrets to git) instead of reading it from the environment at runtime.',
      'Storing mutable application state in process memory (an in-process cache, a session dict) that silently breaks or becomes inconsistent the moment the app runs as more than one instance/worker.',
      'Having application code manage log file rotation/retention itself instead of writing to stdout and letting the execution environment handle log routing — this breaks cleanly in a containerized/orchestrated environment where the filesystem is often ephemeral.',
    ],
    followUpQuestions: [
      'Why does treating logs strictly as an stdout event stream (rather than application-managed files) matter specifically in a containerized deployment?',
      'How does Factor IV ("backing services as attached resources") make it possible to run integration tests against a temporary Docker Postgres instance with zero code changes from production?',
      'What breaks first in a FastAPI app that keeps rate-limit counters in an in-process Python dict once it is scaled to multiple worker processes?',
    ],
    relatedTopics: ['Twelve-Factor App', 'Configuration Management', 'Statelessness', 'Structured Logging', 'Environment Variables'],
  },
  {
    id: 'python-m23-2',
    number: 'PY-M23-2',
    title: 'Linux process signals and graceful shutdown for Uvicorn workers',
    difficulty: 'Medium',
    experienceLevel: '2–4 Years',
    category: 'Linux for Production',
    expectedAnswer:
      'SIGTERM is a polite "please shut down" signal that a process CAN catch and respond to (finish in-flight work, close connections, then exit) — this is what orchestrators (Docker, Kubernetes) send during a normal stop/restart/rolling-deployment. SIGKILL is an unconditional, uncatchable "terminate immediately" signal used only as a last resort when a process fails to exit within a grace period — the difference between these two is the entire foundation of GRACEFUL SHUTDOWN, and a FastAPI service that does not handle SIGTERM properly will drop in-flight requests and potentially corrupt state on every single deploy.',
    deepExplanation:
      '```text\nSIGTERM  — "please terminate" — CATCHABLE, the process can run cleanup code before exiting\nSIGINT   — "interrupt" — what Ctrl+C sends; also catchable, similar intent to SIGTERM\nSIGKILL  — "terminate NOW" — NOT catchable, the OS kills the process immediately, no cleanup possible\n```\n\nThe orchestration flow during a normal deploy/restart, and exactly why the SIGTERM-then-SIGKILL grace period exists:\n\n```text\n1. Orchestrator (Docker/Kubernetes) decides to stop the container (deploy, scale-down, restart)\n2. Orchestrator sends SIGTERM to the process\n3. The process should: stop accepting NEW connections, finish IN-FLIGHT requests, close DB\n   connections cleanly, then exit voluntarily\n4. If the process has not exited within a GRACE PERIOD (commonly ~30s, configurable), the\n   orchestrator sends SIGKILL — an unconditional, immediate kill, no more chances to clean up\n```\n\nUvicorn (and Gunicorn managing Uvicorn workers) already handles the SIGTERM -> stop-accepting-new-connections -> drain-in-flight-requests sequence for HTTP requests automatically — but APPLICATION-LEVEL cleanup (closing a database connection pool, flushing a metrics buffer, releasing a distributed lock) is the developer\'s responsibility, implemented via FastAPI\'s `lifespan` shutdown phase (covered in the next question):\n\n```python\nfrom contextlib import asynccontextmanager\nfrom fastapi import FastAPI\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    app.state.db_engine = create_async_engine(settings.database_url)\n    yield\n    # this cleanup code runs during the SIGTERM-triggered shutdown sequence —\n    # NOT during SIGKILL, which gives the process no chance to run any code at all\n    await app.state.db_engine.dispose()\n\napp = FastAPI(lifespan=lifespan)\n```\n\nWhy the grace period LENGTH matters as a real production tuning decision: too SHORT (e.g. 5 seconds) risks SIGKILL arriving while genuine in-flight requests are still being processed, abruptly dropping client connections mid-response; too LONG (e.g. 5 minutes) slows down every deploy and scale-down operation unnecessarily — the correct value is informed by your SLOWEST realistic request duration (e.g. a long-running report-generation endpoint needs a longer grace period than a service that only ever does sub-second CRUD).\n\nA related, frequently-tested Linux concept: a ZOMBIE process is a child process that has exited but whose exit status has not yet been reaped by its parent (it lingers in the process table as a "defunct" entry) — usually harmless and transient, but a parent process that NEVER reaps children accumulates zombies indefinitely, eventually exhausting the process table; an ORPHAN process is one whose PARENT has exited while the child is still running (it gets re-parented, typically to PID 1) — neither is directly about SIGTERM/SIGKILL, but both come up in the same "process lifecycle" interview territory.',
    productionExample:
      'A production Kubernetes deployment configures a `terminationGracePeriodSeconds` of 45 seconds specifically because the service has one report-export endpoint that can legitimately take up to 30 seconds to complete — without that tuned grace period (the Kubernetes default is 30s), a rolling deployment during a busy period could SIGKILL a worker mid-export, silently truncating a user\'s downloaded report.',
    bestPractices: [
      'Implement FastAPI `lifespan` shutdown cleanup (closing DB engines, flushing buffers) for anything that needs an explicit teardown step — Uvicorn handles the HTTP-connection-draining part of graceful shutdown automatically, but application-level resource cleanup is the developer\'s responsibility.',
      'Tune the orchestrator\'s SIGTERM-to-SIGKILL grace period based on your SLOWEST realistic in-flight request duration, not a generic default — too short risks dropped requests on every deploy.',
      'Never rely on SIGKILL-triggered cleanup — code that "should run on shutdown" placed anywhere except the SIGTERM-triggered path (like `lifespan` shutdown) simply never runs if SIGKILL is what actually terminates the process.',
    ],
    tradeOffs:
      'A longer grace period reduces the risk of abruptly dropping legitimate in-flight requests during a deploy, at the cost of slower rolling deployments and scale-down operations across the whole fleet — the right value is a genuine tradeoff between deploy speed and request-completion safety, tuned to the specific service\'s realistic request-duration distribution.',
    commonMistakes: [
      'Assuming Uvicorn/FastAPI automatically cleans up application-level resources (database connection pools, open file handles) on shutdown without explicit `lifespan` teardown code — Uvicorn handles HTTP connection draining, not arbitrary application resources.',
      'Setting an orchestrator grace period far shorter than the service\'s slowest realistic request duration, causing SIGKILL to abruptly truncate legitimate in-flight work during routine deploys.',
      'Writing cleanup logic that assumes it will always run, without accounting for the fact that SIGKILL gives a process zero opportunity to execute any code at all.',
    ],
    followUpQuestions: [
      'What specifically does Uvicorn do automatically upon receiving SIGTERM, and what is still the application developer\'s responsibility?',
      'How would you determine the correct grace-period value for a specific production service?',
      'What is the difference between a zombie process and an orphan process, and does either relate directly to the SIGTERM/SIGKILL distinction?',
    ],
    relatedTopics: ['Linux Signals', 'Graceful Shutdown', 'SIGTERM', 'SIGKILL', 'Process Lifecycle', 'Uvicorn'],
  },
  {
    id: 'python-m23-3',
    number: 'PY-M23-3',
    title: 'FastAPI production deployment architecture — Uvicorn workers, Gunicorn, and choosing worker count',
    difficulty: 'Hard',
    experienceLevel: '4+ Years',
    category: 'FastAPI Production',
    expectedAnswer:
      'A production FastAPI deployment runs MULTIPLE Uvicorn WORKER PROCESSES (each with its own event loop, using its own CPU core) behind a load balancer/reverse proxy, because a single asyncio event loop is inherently single-process/single-core — async concurrency within one worker handles many simultaneous I/O-bound requests efficiently, but only PROCESS-level parallelism uses multiple CPU cores. The current recommended pattern is Gunicorn as a mature PROCESS MANAGER supervising multiple `uvicorn.workers.UvicornWorker` processes (auto-restart on crash, graceful reloads), or, increasingly common on Kubernetes, running one Uvicorn process per container/pod and letting the orchestrator handle process-level replication instead.',
    deepExplanation:
      '```text\nInternet\n  |\nCDN / WAF                — caches static content, blocks malicious traffic at the edge\n  |\nLoad Balancer             — distributes traffic across multiple FastAPI INSTANCES (pods/VMs)\n  |\nReverse Proxy (often built into the LB, or Nginx) — TLS termination, request buffering\n  |\nFastAPI instance 1        FastAPI instance 2        ... (each instance may itself run\n  |  Uvicorn worker 1        |  Uvicorn worker 1         multiple Uvicorn workers via Gunicorn)\n  |  Uvicorn worker 2        |  Uvicorn worker 2\n  |\nService Layer -> Database (via a connection pool, shared appropriately across workers)\n```\n\nChoosing worker COUNT — the standard formula and its reasoning: `workers = (2 x CPU_cores) + 1` is Gunicorn\'s classic starting-point recommendation for I/O-bound workloads (each worker spends much of its time WAITING on I/O — the database, an external API — so having somewhat more workers than cores lets the OS schedule useful work while some workers are blocked on I/O). For a GENUINELY CPU-bound FastAPI service (heavy in-process computation, not I/O-bound), worker count should track CPU CORE COUNT more closely (`workers ≈ CPU_cores`), since extra workers beyond available cores cannot do more actual computation in parallel and just add memory/context-switching overhead.\n\n```bash\n# Gunicorn supervising multiple Uvicorn workers — the traditional, still-common production pattern\ngunicorn app.main:app \\\n  --workers 5 \\\n  --worker-class uvicorn.workers.UvicornWorker \\\n  --bind 0.0.0.0:8000 \\\n  --timeout 30 \\\n  --graceful-timeout 30\n\n# on Kubernetes: run ONE Uvicorn process per pod, and scale REPLICA COUNT instead —\n# the orchestrator (not Gunicorn) handles process-level parallelism and restart-on-crash\nuvicorn app.main:app --host 0.0.0.0 --port 8000\n# (paired with a Kubernetes Deployment specifying, e.g., replicas: 6)\n```\n\nWhy the Kubernetes-native pattern (one Uvicorn process per pod, scale replica count) has become increasingly common instead of Gunicorn-managed multi-worker: Kubernetes ALREADY provides process supervision (restart a crashed pod), horizontal scaling (more replicas), and rolling deployment orchestration at the ORCHESTRATOR level — running Gunicorn\'s own worker-supervision machinery INSIDE each pod becomes partially redundant with what the orchestrator already does, and can complicate resource-limit reasoning (a pod\'s CPU/memory limit needs to account for N internal workers, not just one process). Teams NOT on an orchestrator with this built-in supervision still benefit from Gunicorn managing multiple Uvicorn workers directly on a VM.\n\nCONNECTION POOL sizing must account for the TOTAL worker count across the whole fleet: if each of 6 instances runs 5 Uvicorn workers, and each worker\'s SQLAlchemy engine has a pool of `pool_size=10, max_overflow=5` connections, that is a THEORETICAL MAXIMUM of `6 x 5 x 15 = 450` simultaneous database connections — a number that must stay comfortably under PostgreSQL\'s `max_connections` (commonly 100 by default) or MongoDB\'s equivalent limit, which is exactly why PgBouncer/connection-pooling middleware (Module 15) becomes close to mandatory once a fleet scales beyond a handful of instances.',
    productionExample:
      'A team migrating from a single VM running Gunicorn with 9 Uvicorn workers to Kubernetes discovered their SQLAlchemy connection pool math no longer worked once they scaled pod REPLICAS instead of in-process worker COUNT — 12 pods x 1 worker each x a pool sized for the old "9 workers on one box" assumption exceeded PostgreSQL\'s connection limit; the fix was both reducing per-pod pool size and introducing PgBouncer in front of PostgreSQL to absorb the larger effective connection count from many small pods.',
    bestPractices: [
      'Start worker count from `(2 x CPU_cores) + 1` for I/O-bound FastAPI workloads (the common case), and closer to `CPU_cores` for genuinely CPU-bound workloads — then tune based on observed CPU/memory utilization under real load.',
      'Calculate TOTAL possible database connections across the entire fleet (instances × workers-per-instance × pool size) and confirm it stays comfortably under the database\'s connection limit — introduce PgBouncer/connection-pooling middleware once the fleet grows large enough that this math gets tight.',
      'On an orchestrator that already provides process supervision and horizontal scaling (Kubernetes), prefer one Uvicorn process per pod and scale replica count, rather than duplicating that supervision inside Gunicorn as well.',
    ],
    tradeOffs:
      'Gunicorn-managed multi-worker deployment gives mature, battle-tested process supervision (auto-restart, graceful reload) on a single host with no orchestrator dependency, at the cost of more complex per-host resource/connection-pool math; the Kubernetes-native one-process-per-pod pattern simplifies per-process reasoning and defers supervision/scaling to the orchestrator, at the cost of depending on that orchestrator already being in place and correctly configured.',
    commonMistakes: [
      'Setting worker count far higher than CPU core count for a CPU-bound workload, adding memory and context-switching overhead with no corresponding increase in actual parallel computation.',
      'Sizing each worker\'s database connection pool without accounting for the TOTAL connection count across the entire fleet, exceeding the database\'s connection limit once scaled to many instances/workers.',
      'Running Gunicorn with multiple internal workers PER POD on Kubernetes without adjusting the pod\'s CPU/memory resource limits accordingly, causing the orchestrator to under-provision resources relative to actual internal concurrency.',
    ],
    followUpQuestions: [
      'Why does the `(2 x cores) + 1` worker-count formula make sense specifically for I/O-bound workloads and not CPU-bound ones?',
      'Walk through the total possible database connection count for a fleet of 8 instances, each running 4 Uvicorn workers with a SQLAlchemy pool_size of 10 and max_overflow of 5 — and explain what you would do if that exceeds your database\'s connection limit.',
      'What specific supervision responsibilities does Kubernetes take over that Gunicorn would otherwise provide on a bare VM deployment?',
    ],
    relatedTopics: ['Uvicorn', 'Gunicorn', 'Worker Processes', 'Connection Pooling', 'Kubernetes', 'Horizontal Scaling'],
  },
  {
    id: 'python-m23-4',
    number: 'PY-M23-4',
    title: 'FastAPI lifespan events and health checks — liveness vs readiness',
    difficulty: 'Medium',
    experienceLevel: '2–4 Years',
    category: 'FastAPI Production',
    expectedAnswer:
      'The modern `lifespan` async context manager replaces the deprecated `@app.on_event("startup"/"shutdown")` decorators, running setup code (opening a DB engine, connecting to Redis) once before the app starts accepting requests and teardown code once after it stops. LIVENESS ("is the process alive at all?") and READINESS ("can this instance currently serve real traffic?") are DIFFERENT questions an orchestrator asks for DIFFERENT reasons — a liveness failure means "kill and restart this process", while a readiness failure means "stop routing NEW traffic here, but do not restart it" (e.g. temporarily overloaded, or its database dependency is briefly unreachable) — conflating the two causes an orchestrator to restart healthy processes unnecessarily.',
    deepExplanation:
      '```python\nfrom contextlib import asynccontextmanager\nfrom fastapi import FastAPI\nfrom sqlalchemy.ext.asyncio import create_async_engine\nimport redis.asyncio as redis\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    # STARTUP — runs once, before the app accepts any requests\n    app.state.db_engine = create_async_engine(settings.database_url, pool_size=10)\n    app.state.redis = redis.from_url(settings.redis_url)\n    yield\n    # SHUTDOWN — runs once, during the SIGTERM-triggered graceful shutdown sequence\n    await app.state.db_engine.dispose()\n    await app.state.redis.aclose()\n\napp = FastAPI(lifespan=lifespan)\n\n\n# --- liveness: cheap, fast, checks ONLY that the process itself is alive and responsive ---\n# NEVER check external dependencies here — a slow database should not cause the orchestrator\n# to conclude the PROCESS is dead and restart it (restarting will not fix a slow database!)\n@app.get("/health/live")\nasync def liveness():\n    return {"status": "alive"}\n\n\n# --- readiness: checks whether this instance can ACTUALLY serve real traffic right now ---\n@app.get("/health/ready")\nasync def readiness():\n    checks = {}\n    try:\n        async with app.state.db_engine.connect() as conn:\n            await conn.execute(text("SELECT 1"))\n        checks["database"] = "ok"\n    except Exception:\n        checks["database"] = "unavailable"\n    try:\n        await app.state.redis.ping()\n        checks["redis"] = "ok"\n    except Exception:\n        checks["redis"] = "unavailable"\n    all_healthy = all(v == "ok" for v in checks.values())\n    status_code = 200 if all_healthy else 503\n    return JSONResponse(status_code=status_code, content={"status": "ready" if all_healthy else "not_ready", "checks": checks})\n```\n\nWhy the distinction matters OPERATIONALLY, precisely: if `/health/ready` (which checks the database) were mistakenly wired up as the LIVENESS probe, a brief database blip would cause Kubernetes to conclude the FastAPI PROCESS itself is unhealthy and RESTART it — but restarting a perfectly healthy process does nothing to fix an unavailable database, and actively makes things worse (now you have fewer running instances, AND a restart storm as the orchestrator keeps trying to "fix" a problem that is not in the process it is restarting). The correct wiring: liveness checks ONLY "is this process\'s own event loop responsive" (no external calls); readiness checks "are this instance\'s DEPENDENCIES currently reachable", and a readiness failure tells the load balancer to simply stop ROUTING traffic to this instance (via the `503` triggering removal from the LB\'s healthy pool) without killing/restarting the process — traffic resumes automatically once readiness recovers.\n\nThe legacy `@app.on_event("startup")`/`@app.on_event("shutdown")` decorators are explicitly deprecated in modern FastAPI — they are still functional for backward compatibility but the `lifespan` context manager is the current recommended pattern, primarily because it correctly scopes startup/shutdown as ONE cohesive block (making it trivial to share state like a database engine handle between the two phases via a single `yield`) rather than two disconnected event handlers.',
    productionExample:
      'A production Kubernetes deployment wires `/health/live` to the `livenessProbe` (restart the pod only if this fails repeatedly) and `/health/ready` to the `readinessProbe` (remove the pod from the Service\'s load-balanced endpoints if this fails, without restarting it) — during a brief PostgreSQL failover (a few seconds of unavailability), every pod\'s readiness probe correctly failed and traffic was routed only to pods that happened to already be excluded... no wait: correctly, traffic was simply NOT routed to any pod during that brief window (all readiness probes failed together), and every pod recovered and rejoined the routing pool automatically the moment the database came back — with zero unnecessary pod restarts, because liveness (checking only the process itself) never flagged a problem.',
    bestPractices: [
      'Keep liveness checks CHEAP and INTERNAL ONLY — never call an external dependency (database, Redis, another service) from a liveness endpoint, since a dependency outage should not cause the orchestrator to restart an otherwise-healthy process.',
      'Use the modern `lifespan` async context manager for startup/shutdown, not the deprecated `@app.on_event` decorators — it correctly scopes setup/teardown as one cohesive block and is the currently recommended FastAPI pattern.',
      'Return a `503` (not `200`) from the readiness endpoint when a dependency is unavailable, so the load balancer/orchestrator actually stops routing new traffic to this instance until it recovers.',
    ],
    tradeOffs:
      'Separating liveness from readiness adds a small amount of endpoint/config complexity (two probes to wire up correctly in the orchestrator, not one) in exchange for preventing a class of self-inflicted incidents — restart storms triggered by a dependency outage that a restart cannot actually fix — that is common and genuinely damaging in production systems that conflate the two checks into one.',
    commonMistakes: [
      'Wiring a database/dependency check into the LIVENESS probe, causing an unrelated dependency outage to trigger unnecessary pod restarts that do nothing to fix the actual problem.',
      'Continuing to use the deprecated `@app.on_event("startup"/"shutdown")` decorators in new code instead of the current recommended `lifespan` async context manager.',
      'Returning `200 OK` from a readiness check even when a critical dependency is unavailable, causing the load balancer to keep routing traffic to an instance that cannot actually serve it correctly.',
    ],
    followUpQuestions: [
      'Walk through exactly what goes wrong if liveness and readiness checks are conflated into a single endpoint that checks the database.',
      'Why does the `lifespan` pattern make it easier to share state (like a database engine handle) between startup and shutdown code compared to the older `@app.on_event` decorators?',
      'How would you design a readiness check for a dependency that is only OCCASIONALLY needed (e.g. a third-party API used by only one endpoint) — should it still block overall readiness?',
    ],
    relatedTopics: ['Lifespan', 'Health Checks', 'Liveness', 'Readiness', 'Kubernetes', 'Graceful Shutdown'],
  },
  {
    id: 'python-m23-5',
    number: 'PY-M23-5',
    title: 'Production error handling — a standard error envelope and never leaking stack traces',
    difficulty: 'Medium',
    experienceLevel: '2–4 Years',
    category: 'FastAPI Production',
    expectedAnswer:
      'A production API needs a CENTRALIZED, CONSISTENT error response shape across every possible failure mode (validation errors, database errors, auth errors, unexpected exceptions) so clients can reliably parse and branch on errors, and must NEVER leak internal details (stack traces, raw exception messages, database schema hints) to the client — those details belong in structured LOGS (correlated by a request ID), not the HTTP response body, since leaking them is both a security risk (information disclosure aiding an attacker) and a poor API contract (internal refactoring should not change the client-visible error shape).',
    deepExplanation:
      '```python\nimport uuid\nimport logging\nfrom fastapi import FastAPI, Request, HTTPException, status\nfrom fastapi.exceptions import RequestValidationError\nfrom fastapi.responses import JSONResponse\nfrom starlette.exceptions import HTTPException as StarletteHTTPException\n\nlogger = logging.getLogger(__name__)\napp = FastAPI()\n\ndef error_envelope(code: str, message: str, request_id: str) -> dict:\n    return {"error": {"code": code, "message": message, "request_id": request_id}}\n\n@app.exception_handler(RequestValidationError)\nasync def validation_exception_handler(request: Request, exc: RequestValidationError):\n    request_id = getattr(request.state, "request_id", str(uuid.uuid4()))\n    return JSONResponse(\n        status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,\n        content=error_envelope("VALIDATION_ERROR", "Request validation failed", request_id),\n    )\n\n@app.exception_handler(StarletteHTTPException)\nasync def http_exception_handler(request: Request, exc: StarletteHTTPException):\n    request_id = getattr(request.state, "request_id", str(uuid.uuid4()))\n    return JSONResponse(\n        status_code=exc.status_code,\n        content=error_envelope(f"HTTP_{exc.status_code}", str(exc.detail), request_id),\n    )\n\n@app.exception_handler(Exception)\nasync def unhandled_exception_handler(request: Request, exc: Exception):\n    request_id = getattr(request.state, "request_id", str(uuid.uuid4()))\n    # log the FULL exception with stack trace INTERNALLY, correlated by request_id\n    logger.exception("Unhandled exception", extra={"request_id": request_id})\n    # return a GENERIC message to the client — never str(exc) or the traceback itself\n    return JSONResponse(\n        status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,\n        content=error_envelope("INTERNAL_ERROR", "An unexpected error occurred", request_id),\n    )\n```\n\nWhy the `request_id` field in the error envelope is not just a nice-to-have: it is the ONLY link between "what the client saw" and "what actually happened internally" — when a client reports "I got an error", asking for their `request_id` lets an engineer immediately find the FULL exception, stack trace, and surrounding log context for that exact request, without needing the client to describe symptoms or reproduce the issue. This is precisely why the request_id must be generated (or extracted from an incoming `X-Request-ID` header) as early as possible in the middleware chain and threaded through every log line for that request.\n\nWhy NEVER return `str(exc)` or a stack trace to the client, stated concretely: a raw exception message can leak internal implementation details (a database column name, a file path, an internal service hostname) that materially help an attacker understand the system\'s internals — and even when NOT security-sensitive, exposing internal exception text as part of the API contract means any internal refactor (renaming a variable, changing an exception type) becomes a BREAKING CHANGE for any client that started depending on that exact error text, which is a maintenance trap worth avoiding entirely by keeping the client-facing message generic and stable.\n\nThe layered exception-handler registration (specific handlers for `RequestValidationError` and `HTTPException`, a CATCH-ALL for `Exception`) ensures EVERY possible failure — including ones no developer anticipated — still produces the same consistent envelope shape and gets logged, rather than occasionally leaking a raw, unhandled Python traceback as the HTTP response body (FastAPI\'s default behavior in debug mode, which must never be enabled in production).',
    productionExample:
      'A production incident where a database connection briefly failed produced dozens of confusing client-side bug reports quoting the RAW exception text ("connection to server at \'10.0.4.12\', port 5432 failed") that had leaked into the API response — after implementing the centralized catch-all handler above, the SAME class of failure instead returned a generic `INTERNAL_ERROR` with a `request_id`, and the internal database hostname/IP never appeared in a client-visible response again, while the full diagnostic detail remained available internally via the correlated log entry.',
    bestPractices: [
      'Register a centralized, catch-all exception handler (`@app.exception_handler(Exception)`) that logs the full exception internally but returns only a generic, stable error message to the client — never rely on FastAPI\'s default unhandled-exception behavior in production.',
      'Include a `request_id` in every error response and thread it through all log lines for that request, so a client-reported error can be immediately correlated with full internal diagnostic detail.',
      'Never disable/omit FastAPI\'s debug-mode protections in production — debug mode can expose full stack traces in HTTP responses, which must never happen outside local development.',
    ],
    tradeOffs:
      'A generic, stable client-facing error message protects against information disclosure and API-contract breakage from internal refactors, at the cost of requiring GOOD internal logging/observability (correlated by request_id) to actually diagnose issues — without that logging discipline, hiding the detailed error from the client just makes debugging harder for the engineering team too, so the two practices (generic client errors + rich correlated internal logs) must be adopted together, not separately.',
    commonMistakes: [
      'Returning raw exception text (`str(exc)`) or a stack trace in the HTTP response body, leaking internal implementation details to clients and turning internal errors into an implicit, fragile part of the API contract.',
      'Omitting a `request_id` from error responses, making it impossible to correlate a client-reported error with the corresponding internal log entry without extensive back-and-forth.',
      'Leaving FastAPI/Starlette in debug mode in a production deployment, which can expose full tracebacks directly in HTTP error responses.',
    ],
    followUpQuestions: [
      'Walk through exactly how a `request_id` generated in middleware gets threaded through to both the error response AND the internal log entry for the same request.',
      'Why is exposing detailed internal exception text a maintenance problem (not just a security one) if clients start depending on it?',
      'How would you extend this error-handling pattern to map specific database exceptions (e.g. a unique constraint violation) to a more specific, still-generic-enough client error code like `DUPLICATE_RESOURCE`?',
    ],
    relatedTopics: ['Error Handling', 'Exception Handlers', 'Request ID', 'Security', 'Structured Logging', 'API Design'],
  },
  {
    id: 'python-m23-6',
    number: 'PY-M23-6',
    title: 'Structured JSON logging and what must never be logged',
    difficulty: 'Medium',
    experienceLevel: '2–4 Years',
    category: 'Observability & Logging',
    expectedAnswer:
      '`print()` statements are not a production logging strategy because they cannot be filtered by severity, cannot be routed/aggregated consistently, carry no structured metadata, and are trivially lost if stdout buffering or redirection is misconfigured — production logging uses a structured LOGGER emitting JSON records (level, timestamp, service name, request/trace/correlation IDs, and the message) so logs are machine-parseable, filterable, and correlatable across a distributed system, with strict discipline around NEVER logging sensitive data (passwords, tokens, full card numbers, other PII) even accidentally.',
    deepExplanation:
      '```python\nimport logging, json, sys\nfrom datetime import datetime, timezone\n\nclass JSONFormatter(logging.Formatter):\n    def format(self, record: logging.LogRecord) -> str:\n        payload = {\n            "timestamp": datetime.now(timezone.utc).isoformat(),\n            "level": record.levelname,\n            "service": "user-api",\n            "message": record.getMessage(),\n            "request_id": getattr(record, "request_id", None),\n            "trace_id": getattr(record, "trace_id", None),\n        }\n        if record.exc_info:\n            payload["exception"] = self.formatException(record.exc_info)\n        return json.dumps(payload)\n\nhandler = logging.StreamHandler(sys.stdout)   # write to STDOUT — never a file (Factor XI)\nhandler.setFormatter(JSONFormatter())\nlogger = logging.getLogger("app")\nlogger.addHandler(handler)\nlogger.setLevel(logging.INFO)\n\n# usage — request_id/trace_id attached via a middleware-populated `extra` dict, threading\n# correlation context through every log line for a given request\nlogger.info("order created", extra={"request_id": req_id, "trace_id": trace_id})\n```\n\nResulting log line, exactly matching the structured shape a log aggregator (e.g. an ELK stack, or a cloud logging service) can index and query on:\n\n```json\n{"timestamp": "2026-08-21T10:15:32.001Z", "level": "INFO", "service": "user-api", "message": "order created", "request_id": "a1b2c3d4", "trace_id": "e5f6a7b8", "method": "POST", "path": "/orders", "status": 201, "duration_ms": 42}\n```\n\nWhy `print()` genuinely fails in production, concretely: it has no LEVEL (cannot distinguish "informational" from "critical" without parsing the message text itself), it is UNSTRUCTURED (a log aggregator must resort to fragile text-pattern matching rather than querying a JSON field), it provides no built-in way to attach REQUEST/TRACE correlation context, and in some deployment configurations (buffered stdout, certain process supervisors) `print()` output can be delayed, reordered relative to actual event timing, or lost entirely if the process crashes before the buffer flushes.\n\nWhat must NEVER be logged, and why each is dangerous even in a supposedly "internal-only" log: passwords and raw auth tokens (a log breach becomes a full account-compromise breach, not just a data leak); credit-card numbers/full PII (regulatory violation — PCI-DSS explicitly prohibits logging full card numbers; GDPR/similar regulations restrict PII retention, and logs often have LONGER retention and WIDER access than the primary database); API keys/secrets (equivalent to leaking the credential itself). The standard mitigation is explicit REDACTION at the logging layer — never relying on developers to remember not to log a sensitive field, but structurally preventing it:\n\n```python\nSENSITIVE_FIELDS = {"password", "access_token", "refresh_token", "credit_card", "ssn"}\n\ndef redact(data: dict) -> dict:\n    return {k: ("***REDACTED***" if k.lower() in SENSITIVE_FIELDS else v) for k, v in data.items()}\n\nlogger.info("user registered", extra={"payload": redact(request_body)})\n```',
    productionExample:
      'A production incident investigation was delayed by nearly an hour because the on-call engineer had to manually grep through unstructured `print()`-based text logs across a dozen containers looking for a specific failing request, with no way to filter by severity or correlate log lines across services — after migrating to structured JSON logging with request_id correlation, the SAME class of investigation became a single query in the log aggregator, filtering on `request_id = "..."` across every service the request touched, cutting investigation time from an hour to under two minutes.',
    bestPractices: [
      'Use a structured JSON logger writing to stdout (never `print()`, never application-managed log files) for all production logging, with consistent fields (timestamp, level, service, request_id, trace_id) on every entry.',
      'Implement REDACTION structurally, at the logging layer itself (a helper function or logging filter applied automatically), rather than relying on every developer remembering not to log a sensitive field manually.',
      'Thread a `request_id` (generated or extracted from an incoming header early in middleware) through every log line for a given request, enabling instant cross-service correlation during incident investigation.',
    ],
    tradeOffs:
      'Structured JSON logging is more verbose to set up initially (a formatter, consistent field conventions, correlation-id middleware) than scattering `print()` statements, but pays for itself immediately the first time a production incident needs to be investigated — the cost is upfront engineering discipline; the payoff is investigation time measured in minutes instead of hours once logs are actually needed under pressure.',
    commonMistakes: [
      'Using `print()` (or unstructured `logging` calls with no consistent format) for anything beyond quick local debugging, losing the ability to filter, correlate, and query logs effectively in production.',
      'Logging a full request/response payload without redacting sensitive fields, accidentally writing passwords, tokens, or PII into logs that often have broader access and longer retention than the primary database.',
      'Failing to thread a correlation/request ID through log lines emitted from different layers (middleware, service, repository) of the same request, making cross-layer investigation of a single failing request needlessly difficult.',
    ],
    followUpQuestions: [
      'Why can a log aggregator query structured JSON logs far more effectively than unstructured text logs, concretely?',
      'How would you design automatic redaction so a NEW sensitive field added to a model in the future is redacted by default, rather than requiring every developer to remember to update a redaction list?',
      'What log RETENTION policy considerations come into play once logs might inadvertently contain PII, even with redaction in place?',
    ],
    relatedTopics: ['Structured Logging', 'JSON Logs', 'Sensitive Data Redaction', 'Correlation ID', 'Observability'],
  },
  {
    id: 'python-m23-7',
    number: 'PY-M23-7',
    title: 'Writing a production-grade Dockerfile for a FastAPI app — multi-stage builds and non-root users',
    difficulty: 'Hard',
    experienceLevel: '4+ Years',
    category: 'Docker',
    expectedAnswer:
      'A production Dockerfile uses a MULTI-STAGE BUILD (a "builder" stage installs dependencies and compiles anything needed; a slim "runtime" stage copies only the finished artifacts, discarding build tools) to minimize final image size and attack surface, runs the application as a NON-ROOT user (limiting the blast radius if the container is ever compromised), and orders instructions to maximize Docker\'s LAYER CACHING (dependency installation happens BEFORE copying application code, so code changes do not invalidate the expensive dependency-install layer).',
    deepExplanation:
      '```dockerfile\n# ---- Stage 1: builder — installs dependencies, including any build-time tools ----\nFROM python:3.12-slim AS builder\n\nWORKDIR /app\n\n# copy ONLY the dependency manifest first — this layer is cached and only invalidated\n# when dependencies actually change, NOT on every application code change\nCOPY pyproject.toml poetry.lock ./\nRUN pip install --no-cache-dir poetry \\\n  && poetry config virtualenvs.in-project true \\\n  && poetry install --no-root --only main   # --only main excludes dev/test dependencies\n\n# ---- Stage 2: runtime — a slim final image with ONLY what is needed to RUN the app ----\nFROM python:3.12-slim AS runtime\n\n# create a dedicated non-root user — never run the application as root\nRUN groupadd -r appuser && useradd -r -g appuser appuser\n\nWORKDIR /app\n\n# copy ONLY the installed virtual environment from the builder stage — no build tools,\n# no poetry, no compiler toolchains end up in the final image\nCOPY --from=builder /app/.venv /app/.venv\nENV PATH="/app/.venv/bin:$PATH"\n\n# copy application code LAST — this is the layer most likely to change on every build,\n# so putting it last means earlier (dependency) layers stay cached across builds\nCOPY --chown=appuser:appuser ./app ./app\n\nUSER appuser   # drop root privileges for the actual running process\n\nHEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \\\n  CMD curl -f http://localhost:8000/health/live || exit 1\n\nEXPOSE 8000\nCMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]\n```\n\nWhy each specific choice matters, precisely: MULTI-STAGE builds discard the ENTIRE builder stage (poetry, compilers, any build-only dependencies) from the final image — a typical result is a final image of tens of MB instead of several hundred MB, which matters for both faster deploys/pulls AND a smaller attack surface (fewer installed packages means fewer potential vulnerabilities to scan/patch). NON-ROOT execution means that even if an attacker achieves code execution inside the container (via an application vulnerability), they do so as an UNPRIVILEGED user, unable to, for example, modify system files or escalate privileges as easily as they could running as root — this is a genuine defense-in-depth layer, not a theoretical concern. LAYER ORDERING (dependencies before application code) exploits Docker\'s build cache: changing ONE line of application code and rebuilding will reuse the cached dependency-installation layer (often the slowest step) rather than reinstalling every dependency from scratch on every single build — a meaningful CI/CD pipeline speed difference at scale.\n\nThe `HEALTHCHECK` instruction lets Docker (and orchestrators that respect it) know whether the CONTAINER itself is healthy, independent of the orchestrator\'s own readiness/liveness probes (Kubernetes typically uses its OWN probes rather than the Dockerfile HEALTHCHECK, but for plain `docker run`/Docker Compose deployments without Kubernetes, this is the primary health-signaling mechanism).\n\nA base-image choice worth explicit reasoning: `python:3.12-slim` (Debian-based, minimal) is a common middle ground between `python:3.12` (full, larger, more pre-installed tooling) and `python:3.12-alpine` (smallest, but Alpine\'s musl libc can cause subtle compatibility issues with some Python C-extension packages that expect glibc) — the "smallest possible image" is not always the right default if it introduces build/compatibility friction that costs more engineering time than the extra megabytes actually save.',
    productionExample:
      'A team\'s CI/CD pipeline build time dropped from ~4 minutes to under 30 seconds for typical application-code-only changes after reordering their Dockerfile to install dependencies BEFORE copying application code — the dependency-installation layer, unchanged by a code-only commit, was now correctly reused from Docker\'s build cache instead of being needlessly reinstalled on every single build.',
    bestPractices: [
      'Use a multi-stage build to keep the final runtime image minimal — discard build tools, compilers, and dev-only dependencies that were only needed to PRODUCE the artifact, not to RUN it.',
      'Always run the application process as a dedicated non-root user in the final image — never leave a production container running as root.',
      'Order Dockerfile instructions to copy dependency manifests and install dependencies BEFORE copying application code, maximizing Docker layer-cache reuse across builds where only code (not dependencies) changed.',
    ],
    tradeOffs:
      'Multi-stage builds and careful layer ordering add a small amount of Dockerfile complexity/verbosity compared to a single naive `FROM python; COPY .; RUN pip install; CMD ...` approach, but pay for themselves quickly through smaller image size (faster deploys, smaller attack surface) and dramatically faster CI/CD build times once dependency-layer caching is actually exploited correctly.',
    commonMistakes: [
      'Running the application as root inside the container, unnecessarily widening the blast radius of any application-level vulnerability that achieves code execution.',
      'Copying application code BEFORE installing dependencies, causing every single code change to invalidate the dependency-installation cache layer and needlessly slow down every build.',
      'Skipping a multi-stage build and shipping build tools/compilers/dev dependencies in the final production image, increasing both image size and the surface area a vulnerability scanner has to evaluate.',
    ],
    followUpQuestions: [
      'Walk through exactly which Docker layers get invalidated (and which stay cached) when only application code changes, given the Dockerfile ordering shown above.',
      'What specific security benefit does running as a non-root user provide if an attacker already achieves code execution inside the container?',
      'When might `python:3.12-slim` be the WRONG base image choice compared to the full `python:3.12` image, despite being smaller?',
    ],
    relatedTopics: ['Docker', 'Multi-Stage Builds', 'Layer Caching', 'Container Security', 'Non-Root Containers', 'Dockerfile'],
  },
  {
    id: 'python-m23-8',
    number: 'PY-M23-8',
    title: 'Docker Compose for a local production-like stack, and container security fundamentals',
    difficulty: 'Medium',
    experienceLevel: '2–4 Years',
    category: 'Docker',
    expectedAnswer:
      'Docker Compose orchestrates multiple containers (FastAPI, PostgreSQL, MongoDB, Redis) as one locally-runnable stack, using named NETWORKS (containers reach each other by service name, not hardcoded IPs), VOLUMES (persisting database data across container restarts), HEALTH CHECKS (so dependent services wait for a database to be genuinely ready, not just "container started"), and environment-variable-based configuration mirroring how the same services would be wired in production — giving developers a close-to-production environment (Twelve-Factor\'s "dev/prod parity") without needing real cloud infrastructure locally.',
    deepExplanation:
      '```yaml\n# docker-compose.yml\nservices:\n  api:\n    build: .\n    ports:\n      - "8000:8000"\n    environment:\n      DATABASE_URL: postgresql+asyncpg://app:app@postgres:5432/app_db   # service NAME as hostname\n      MONGO_URL: mongodb://mongo:27017\n      REDIS_URL: redis://redis:6379/0\n    depends_on:\n      postgres:\n        condition: service_healthy    # wait for postgres to be genuinely READY, not just started\n      mongo:\n        condition: service_healthy\n      redis:\n        condition: service_healthy\n    networks: [backend]\n\n  postgres:\n    image: postgres:16-alpine\n    environment:\n      POSTGRES_USER: app\n      POSTGRES_PASSWORD: app\n      POSTGRES_DB: app_db\n    volumes:\n      - pgdata:/var/lib/postgresql/data   # persists across container restarts/recreations\n    healthcheck:\n      test: ["CMD-SHELL", "pg_isready -U app"]\n      interval: 5s\n      timeout: 3s\n      retries: 5\n    networks: [backend]\n\n  mongo:\n    image: mongo:7\n    volumes:\n      - mongodata:/data/db\n    healthcheck:\n      test: ["CMD", "mongosh", "--eval", "db.adminCommand(\'ping\')"]\n      interval: 5s\n      retries: 5\n    networks: [backend]\n\n  redis:\n    image: redis:7-alpine\n    healthcheck:\n      test: ["CMD", "redis-cli", "ping"]\n      interval: 5s\n      retries: 5\n    networks: [backend]\n\nvolumes:\n  pgdata:\n  mongodata:\n\nnetworks:\n  backend:\n```\n\nWhy `condition: service_healthy` (not just `depends_on: [postgres]` alone) matters concretely: plain `depends_on` only waits for the postgres CONTAINER PROCESS to start, which happens well before PostgreSQL has actually finished its own startup sequence and is ready to accept connections — without the healthcheck-gated condition, the FastAPI container would frequently start, attempt its first database connection, and CRASH on a "connection refused" error before Postgres was truly ready, a classic and confusing local-dev flakiness source.\n\nWhy named VOLUMES (not just leaving data inside the container\'s writable layer) matter: `docker compose down` (without `-v`) removes containers but PRESERVES named volumes, so database data survives a routine "stop and restart the stack" — losing this would mean re-seeding test data on every single local restart, a serious developer-experience problem, not just a production concern.\n\nContainer SECURITY fundamentals worth stating explicitly alongside this compose setup: never bake secrets (real passwords, API keys) into a `docker-compose.yml` committed to git — use a `.env` file (gitignored) or a secrets manager for anything beyond local-only throwaway credentials; use MINIMAL base images (`-alpine`/`-slim` variants) to reduce the vulnerability surface a container-image SCANNER (Trivy, Snyk, or a cloud registry\'s built-in scanning) needs to evaluate; apply explicit RESOURCE LIMITS (`mem_limit`, `cpus` in Compose, or `resources.limits` in Kubernetes) so one misbehaving container cannot starve every other container on the same host of memory/CPU; and use NETWORK ISOLATION (as shown, a dedicated `backend` network) so, for example, a public-facing web tier container cannot reach an internal database directly unless explicitly placed on the same network.',
    productionExample:
      'A new engineer\'s local environment setup went from "install and configure PostgreSQL, MongoDB, and Redis manually, hope the versions match production" (historically a multi-hour, error-prone onboarding step) to a single `docker compose up`, producing a stack running the EXACT SAME PostgreSQL/MongoDB/Redis versions as production, with health-check-gated startup ordering meaning the FastAPI container never crash-loops waiting on a database that has not finished initializing yet.',
    bestPractices: [
      'Gate `depends_on` with `condition: service_healthy` (backed by a real healthcheck per service) rather than relying on plain container-start ordering, which does not guarantee the dependency is actually ready to accept connections.',
      'Use named volumes for any stateful service (PostgreSQL, MongoDB) so data survives routine container restarts/recreations during local development.',
      'Never commit real secrets into `docker-compose.yml` — use a gitignored `.env` file for local development and a proper secrets manager for anything beyond throwaway local credentials.',
    ],
    tradeOffs:
      'A full multi-service Docker Compose stack gives close dev/prod parity and eliminates "works on my machine" environment drift, at the cost of higher local resource usage (running PostgreSQL, MongoDB, Redis, and the app simultaneously) and some added onboarding complexity for developers new to Docker — for a very small project, a simpler setup (e.g. SQLite locally, one real database in production) might be pragmatically acceptable, but that tradeoff should be made deliberately, weighing dev/prod parity against local simplicity.',
    commonMistakes: [
      'Using plain `depends_on` without a `service_healthy` condition, causing the application container to attempt database connections before the database is actually ready and crash-loop on startup.',
      'Committing real secrets directly into a `docker-compose.yml` file tracked in git, even for "just local development" — habits formed locally have a way of leaking into less-careful production configuration.',
      'Omitting named volumes for stateful services, losing all local database data on every `docker compose down` and forcing repeated, tedious re-seeding.',
    ],
    followUpQuestions: [
      'Why is a healthcheck-gated `depends_on` condition necessary even though the database container process itself starts almost immediately?',
      'How would you structure Docker Compose configuration to support both a full local stack AND a lighter "just the app, pointed at a shared dev database" mode for different developer preferences?',
      'What specific container-level resource-limit misconfiguration could let one container starve every other container on the same host?',
    ],
    relatedTopics: ['Docker Compose', 'Container Orchestration', 'Health Checks', 'Volumes', 'Container Security', 'Dev/Prod Parity'],
  },
  {
    id: 'python-m23-9',
    number: 'PY-M23-9',
    title: 'CI/CD pipeline stages and deployment strategies — rolling, blue/green, canary',
    difficulty: 'Hard',
    experienceLevel: '4+ Years',
    category: 'CI/CD',
    expectedAnswer:
      'A production CI/CD pipeline runs a sequence of increasingly expensive, increasingly confidence-building stages (lint -> unit tests -> integration tests -> security/dependency scan -> build image -> push to registry -> deploy to staging -> smoke tests -> deploy to production) that FAIL FAST — cheap checks run first so an obvious problem is caught in seconds, not after a slow build/deploy. The DEPLOYMENT STRATEGY (rolling, blue/green, canary) determines HOW the new version reaches production traffic, trading off deployment speed/cost against blast-radius control if the new version turns out to be broken.',
    deepExplanation:
      '```text\nGit Push\n  |\nLint / Format check    — fastest possible feedback (seconds), catches trivial issues immediately\n  |\nUnit Tests              — fast, no external dependencies, run on every push\n  |\nIntegration Tests       — slower, spins up real (containerized) Postgres/MongoDB, tests actual\n  |                        database interaction — genuinely catches bugs unit tests structurally cannot\nSecurity / Dependency Scan  — checks for known-vulnerable dependencies BEFORE building an image with them\n  |\nBuild Docker Image      — only reached once all cheaper checks already passed\n  |\nPush to Registry\n  |\nDeploy to Staging\n  |\nSmoke Tests              — a handful of critical-path checks against the REAL staging deployment\n  |                        (e.g. "can I log in", "does the health endpoint return 200")\nDeploy to Production     — via whichever strategy is chosen (see below)\n```\n\nDeployment strategies, precisely, with their real tradeoffs:\n\n```text\nRECREATE       — stop ALL old instances, then start ALL new ones.\n                 Simplest, but causes a real DOWNTIME window. Rarely acceptable for a\n                 user-facing production API.\n\nROLLING        — replace old instances with new ones GRADUALLY, a few at a time, while the\n                 rest keep serving traffic. Zero downtime IF the app is stateless and both old\n                 and new versions can coexist briefly (a real requirement — see below).\n                 The DEFAULT strategy on Kubernetes.\n\nBLUE/GREEN     — run a COMPLETE second environment ("green") alongside the current one\n                 ("blue"), fully deploy and verify the new version there, then SWITCH traffic\n                 all at once (via a load balancer/DNS change). Enables an instant, clean\n                 rollback (switch traffic back to blue) but requires 2x infrastructure\n                 capacity during the transition.\n\nCANARY         — route a SMALL percentage of real traffic (e.g. 5%) to the new version,\n                 monitor its error rate/latency closely, and gradually increase the percentage\n                 (5% -> 25% -> 50% -> 100%) only if metrics stay healthy at each step — the\n                 slowest strategy but gives the SMALLEST blast radius if the new version is broken.\n```\n\nWhy ROLLING deployments REQUIRE backward-compatible changes, and this is the single most important, most-missed production engineering constraint: during a rolling deployment, OLD and NEW application code run SIMULTANEOUSLY for a period, both querying the SAME database — if the new version\'s code depends on a database column that does not exist yet (or the old version\'s code breaks against a column the new migration already removed), the deployment window itself causes live errors. This is exactly why database migrations must follow the EXPAND/CONTRACT pattern (Module 17/22): first deploy a migration that ADDS a new column (backward compatible — old code ignores it, new code can use it), THEN deploy the new application code that uses it, and only in a LATER, separate deploy remove the old column once no running code references it anymore — never combine "add column" and "remove old column" in one migration deployed alongside code that assumes the old column is already gone.\n\nCanary rollback CRITERIA should be defined and AUTOMATED before the deployment starts, not decided ad hoc while watching a dashboard under pressure: e.g. "automatically roll back if the new version\'s error rate exceeds 1% OR p95 latency exceeds 500ms, sustained for more than 2 minutes, at any canary percentage stage."',
    productionExample:
      'A team\'s rolling deployment caused a five-minute window of intermittent 500 errors because a single deploy simultaneously shipped a migration that RENAMED a column and application code that assumed the rename was already complete — during the rolling window, OLD instances (still running against the renamed schema) crashed on every request touching that column; the fix, going forward, was strict adherence to expand/contract migrations (add the new column, deploy code that writes to BOTH old and new columns, backfill, deploy code that reads only the new column, THEN drop the old column in a separate, later deploy).',
    bestPractices: [
      'Order CI pipeline stages from cheapest/fastest to most expensive, so an obvious failure is caught in seconds rather than after a slow build/deploy step has already run.',
      'Follow the expand/contract migration pattern for ANY schema change deployed via a rolling strategy, since old and new application code versions run simultaneously against the same database during the rollout window.',
      'Define automated, quantitative rollback criteria (error rate/latency thresholds) for canary deployments BEFORE the deployment begins, rather than making a judgment call under pressure while a canary is already live.',
    ],
    tradeOffs:
      'Rolling deployments are the cheapest and fastest (no duplicated infrastructure) but require every change to be backward-compatible during the rollout window; blue/green gives instant, clean rollback at the cost of 2x infrastructure during the transition; canary gives the smallest blast radius for a bad deploy but is the slowest to reach full rollout and requires solid real-time metrics to make automated go/no-go decisions at each stage — the right strategy depends on how costly a bad deploy would be versus how much deployment speed/infrastructure cost the team is willing to trade for safety.',
    commonMistakes: [
      'Deploying a schema migration and the application code that depends on it in the SAME rolling deployment, causing live errors during the window where old and new code run simultaneously against an incompatible schema state.',
      'Running expensive, slow checks (a full build, an integration test suite) BEFORE cheap, fast ones (lint, unit tests) in a CI pipeline, wasting time on slow feedback for trivial failures.',
      'Making canary rollback decisions ad hoc/manually under pressure instead of defining automated, quantitative rollback criteria before the deployment begins.',
    ],
    followUpQuestions: [
      'Walk through the exact sequence of deploys needed to safely rename a database column using the expand/contract pattern under a rolling deployment strategy.',
      'Why does blue/green deployment require double infrastructure capacity, and when is that cost clearly justified versus not?',
      'How would you design automated canary analysis to compare the new version\'s error rate/latency against the OLD version\'s baseline, rather than against a fixed absolute threshold?',
    ],
    relatedTopics: ['CI/CD', 'Deployment Strategies', 'Rolling Deployment', 'Blue/Green Deployment', 'Canary Deployment', 'Expand/Contract Migrations'],
  },
  {
    id: 'python-m23-10',
    number: 'PY-M23-10',
    title: 'Testing strategy — the test pyramid, and pytest patterns for FastAPI + PostgreSQL/MongoDB',
    difficulty: 'Medium',
    experienceLevel: '2–4 Years',
    category: 'Testing',
    expectedAnswer:
      'The test pyramid prescribes MANY fast, isolated UNIT tests at the base (business logic, no real I/O), FEWER INTEGRATION tests in the middle (real database, testing repository/query correctness), and a SMALL number of END-TO-END/API tests at the top (the full stack, verifying critical user-facing flows) — this shape exists because unit tests are cheap to write and run (milliseconds, easy to pinpoint failures) while integration/E2E tests are expensive (seconds to minutes, harder to diagnose failures, more brittle to unrelated changes), so the bulk of coverage and fast feedback should come from the cheap layer.',
    deepExplanation:
      '```text\n         /\\\n        /E2E\\        — few: full-stack, critical user journeys only (login -> checkout -> ...)\n       /------\\\n      /Integr. \\      — some: real DB, tests repository/query logic and actual persistence\n     /----------\\\n    / Unit tests  \\   — MANY: pure business logic, no real I/O, fast (milliseconds), isolated\n   /----------------\\\n```\n\nA FastAPI unit test (service layer, NO real database — a fake/mock repository injected):\n\n```python\nimport pytest\nfrom app.services.order_service import OrderService\n\nclass FakeOrderRepository:\n    def __init__(self):\n        self.orders = {}\n    async def create(self, order):\n        self.orders[order.id] = order\n        return order\n\n@pytest.mark.asyncio\nasync def test_order_service_rejects_empty_cart():\n    service = OrderService(repository=FakeOrderRepository())\n    with pytest.raises(ValueError, match="cart cannot be empty"):\n        await service.place_order(customer_id="c1", items=[])\n# runs in milliseconds, requires NO database — pure business-logic verification\n```\n\nA PostgreSQL integration test using a per-test TRANSACTION ROLLBACK for isolation (the standard, fast pattern — avoids the cost of recreating the schema between every test):\n\n```python\nimport pytest\nfrom sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker\n\n@pytest.fixture\nasync def db_session():\n    engine = create_async_engine(TEST_DATABASE_URL)\n    async with engine.connect() as conn:\n        trans = await conn.begin()\n        Session = async_sessionmaker(bind=conn, expire_on_commit=False)\n        async with Session() as session:\n            yield session\n        await trans.rollback()   # undo EVERYTHING this test did — the next test starts clean\n    await engine.dispose()\n\nasync def test_create_user_persists_correctly(db_session):\n    repo = UserRepository(db_session)\n    user = await repo.create(email="ada@example.com", name="Ada")\n    fetched = await repo.get_by_id(user.id)\n    assert fetched.email == "ada@example.com"\n```\n\nA MongoDB integration test, using a dedicated TEST DATABASE dropped/recreated per test session (Motor/PyMongo has no equivalent transaction-rollback isolation trick for a simple standalone instance the way PostgreSQL does, so per-COLLECTION cleanup between tests is the common pattern):\n\n```python\n@pytest.fixture\nasync def mongo_db():\n    client = AsyncIOMotorClient(TEST_MONGO_URL)\n    db = client["test_db"]\n    yield db\n    await client.drop_database("test_db")   # clean slate for the NEXT test\n    client.close()\n```\n\nA FastAPI API test using `TestClient` + `dependency_overrides` (swapping the real database dependency for a test one, WITHOUT modifying application code):\n\n```python\nfrom fastapi.testclient import TestClient\nfrom app.main import app\nfrom app.api.dependencies import get_db\n\napp.dependency_overrides[get_db] = lambda: fake_test_session\nclient = TestClient(app)\n\ndef test_create_user_endpoint_returns_201():\n    response = client.post("/users", json={"email": "grace@example.com", "name": "Grace"})\n    assert response.status_code == 201\n    assert "password" not in response.json()   # verify sensitive fields are never returned\n```\n\nWhy the pyramid SHAPE (not just "write tests") is the actual lesson: an inverted pyramid (mostly slow E2E tests, few unit tests) is a common, costly anti-pattern — it means the test SUITE itself becomes slow (minutes instead of seconds) and FLAKY (E2E tests touch more moving parts, more surface for unrelated infrastructure issues to cause false failures), directly slowing down the CI feedback loop this module\'s pipeline design depends on for fast, cheap-first-failure detection.',
    productionExample:
      'A team\'s CI pipeline had grown to take 25 minutes per run because their test suite was almost entirely slow, database-backed integration/E2E tests with very few pure unit tests — restructuring the suite to push business-logic coverage down into fast unit tests (using fake repositories) and reserving integration/E2E tests specifically for verifying actual persistence/query correctness and critical user journeys cut the typical CI run to under 4 minutes, directly speeding up every developer\'s feedback loop.',
    bestPractices: [
      'Push the bulk of test coverage down into fast, isolated unit tests (fake/mock repositories, no real I/O) and reserve integration/E2E tests specifically for verifying real persistence behavior and critical end-to-end user journeys.',
      'Use a per-test transaction-rollback pattern for PostgreSQL integration tests (fast, no schema recreation needed) and a drop-database-per-test-session pattern for MongoDB, matching each database\'s actual isolation capabilities.',
      'Use FastAPI\'s `dependency_overrides` to swap real dependencies (database sessions, external clients) for test doubles in API-level tests, without modifying application code to accommodate testing.',
    ],
    tradeOffs:
      'A pyramid-shaped test suite (many fast unit tests, fewer slow integration/E2E tests) gives fast CI feedback and precise failure localization, at the cost of needing careful architecture (repository pattern, dependency injection) to actually make business logic testable in isolation from real I/O — an inverted pyramid is easier to "just write" without that architectural discipline, but produces a slow, flaky, hard-to-maintain test suite as the codebase grows.',
    commonMistakes: [
      'Writing predominantly slow, database-backed integration/E2E tests for logic that could have been covered by fast, isolated unit tests, needlessly slowing down the CI feedback loop.',
      'Recreating the entire database schema from scratch for every single test instead of using a transaction-rollback (PostgreSQL) or scoped-database (MongoDB) isolation pattern, making the test suite far slower than necessary.',
      'Testing business logic only through full API-level tests, making failures hard to localize (a single business-rule bug can cause failures across many API test cases) instead of pinpointing it via a focused unit test.',
    ],
    followUpQuestions: [
      'Why does the transaction-rollback pattern for PostgreSQL integration tests avoid the cost of recreating the schema between tests, and why does MongoDB typically need a different isolation approach?',
      'How does the repository pattern (Module 12/17/21) specifically enable fast unit testing of business logic without a real database connection?',
      'At what point would you decide a specific piece of logic deserves an integration test rather than relying solely on a unit test with a fake repository?',
    ],
    relatedTopics: ['Testing Strategy', 'Test Pyramid', 'Pytest', 'Integration Testing', 'Dependency Overrides', 'Test Isolation'],
  },
  {
    id: 'python-m23-11',
    number: 'PY-M23-11',
    title: 'Rate limiting architecture — token bucket via Redis, and idempotency keys in production',
    difficulty: 'Hard',
    experienceLevel: '4+ Years',
    category: 'Reliability & Rate Limiting',
    expectedAnswer:
      'Rate limiting protects a service from being overwhelmed (by a misbehaving client, a traffic spike, or an abusive actor) by rejecting requests once a client exceeds an allowed rate — the TOKEN BUCKET algorithm (each client has a bucket that refills at a steady rate and is drained per request, allowing bounded BURSTS up to the bucket size) is the most commonly used production algorithm because it handles legitimate bursty traffic gracefully, unlike a naive FIXED WINDOW counter, which allows up to 2x the intended rate right at a window boundary. A DISTRIBUTED rate limiter (shared across multiple FastAPI instances) requires a shared store — Redis, using atomic operations, is the standard choice.',
    deepExplanation:
      '```text\nFixed window (naive, has a real boundary-burst flaw):\n  count requests in each fixed 60-second window, reject once count > limit\n  FLAW: a client can send `limit` requests at 0:59 and ANOTHER `limit` requests at 1:01 —\n  2x the intended rate within a 2-second span, since it straddles two separate windows\n\nSliding window / Token bucket (the production-grade fix):\n  a "bucket" holds up to N tokens, refilling at a steady rate (e.g. 10 tokens/second);\n  each request consumes 1 token; if the bucket is empty, the request is rejected (or queued)\n  -> naturally smooths bursts and avoids the fixed-window boundary flaw entirely\n```\n\nA Redis-backed token-bucket rate limiter, using an atomic Lua script (essential for correctness under CONCURRENT requests from the same client hitting multiple FastAPI instances simultaneously):\n\n```python\nimport time\nimport redis.asyncio as redis\n\nTOKEN_BUCKET_SCRIPT = """\nlocal key = KEYS[1]\nlocal capacity = tonumber(ARGV[1])\nlocal refill_rate = tonumber(ARGV[2])   -- tokens per second\nlocal now = tonumber(ARGV[3])\n\nlocal bucket = redis.call(\'HMGET\', key, \'tokens\', \'last_refill\')\nlocal tokens = tonumber(bucket[1]) or capacity\nlocal last_refill = tonumber(bucket[2]) or now\n\nlocal elapsed = now - last_refill\ntokens = math.min(capacity, tokens + elapsed * refill_rate)\n\nif tokens < 1 then\n    redis.call(\'HMSET\', key, \'tokens\', tokens, \'last_refill\', now)\n    redis.call(\'EXPIRE\', key, 3600)\n    return 0   -- rejected\nelse\n    tokens = tokens - 1\n    redis.call(\'HMSET\', key, \'tokens\', tokens, \'last_refill\', now)\n    redis.call(\'EXPIRE\', key, 3600)\n    return 1   -- allowed\nend\n"""\n\nasync def is_allowed(redis_client: redis.Redis, client_id: str, capacity: int = 100, refill_rate: float = 10.0) -> bool:\n    result = await redis_client.eval(TOKEN_BUCKET_SCRIPT, 1, f"ratelimit:{client_id}", capacity, refill_rate, time.time())\n    return result == 1\n\n# FastAPI dependency wiring this into protected endpoints\nasync def rate_limit_dependency(request: Request, redis_client: redis.Redis = Depends(get_redis)):\n    client_id = request.client.host   # or, better, an authenticated user/API-key id\n    if not await is_allowed(redis_client, client_id):\n        raise HTTPException(status_code=429, detail="Rate limit exceeded", headers={"Retry-After": "1"})\n```\n\nWhy the LUA SCRIPT (not separate Redis GET/SET calls from Python) matters critically: Redis executes a Lua script ATOMICALLY — no other client\'s request can interleave between reading the current token count and writing the updated one. Doing this as separate `GET` then `SET` calls from Python has a genuine RACE CONDITION under concurrent requests: two simultaneous requests could both read "5 tokens available" before either writes back the decremented value, allowing BOTH through when only one should have been permitted — this is the exact same read-modify-write race condition pattern covered for MongoDB\'s `$inc` in Module 19, applied here to Redis.\n\nIDEMPOTENCY KEYS in production, tying back to Module 9\'s foundational treatment: the store backing idempotency-key lookups can be EITHER Redis (fast, but needs an appropriate TTL so keys eventually expire — commonly 24 hours, matching how long a client might realistically retry) OR a PostgreSQL table (slower per-lookup, but naturally durable and consistent with the SAME transaction as the operation it protects, which matters when the idempotency check and the actual business operation need to be atomic together) — the choice depends on whether the idempotency guarantee needs to survive a Redis outage/restart (favoring PostgreSQL) or whether raw lookup speed matters more (favoring Redis) for that specific endpoint.',
    productionExample:
      'A public API endpoint rate-limited via a naive fixed-window counter was found to allow legitimate-looking traffic spikes of nearly 2x the documented rate limit right at minute boundaries — abusive clients had reverse-engineered this and deliberately timed bursts to straddle window boundaries; migrating to the Redis-backed token-bucket implementation above eliminated the boundary-burst exploit entirely, since the bucket\'s continuous refill has no discrete "boundary" to game.',
    bestPractices: [
      'Use a token-bucket (or sliding-window) algorithm rather than a naive fixed-window counter, to avoid the well-known boundary-burst flaw that allows up to double the intended rate near window edges.',
      'Implement the actual token check-and-decrement as an ATOMIC Redis operation (a Lua script, or Redis\'s native `INCR`/`EXPIRE` combination for simpler cases) — never as separate GET-then-SET calls from application code, which introduces a genuine race condition under concurrent requests.',
      'Choose the idempotency-key storage backend (Redis vs a database table) based on whether the guarantee needs to survive a cache restart/outage and whether it needs to be atomically consistent with the protected operation itself.',
    ],
    tradeOffs:
      'A token-bucket rate limiter correctly smooths bursty traffic and avoids the fixed-window boundary flaw, at the cost of slightly more implementation complexity (tracking both a token count and a last-refill timestamp, and needing an atomic script rather than a single Redis command); a distributed rate limiter shared via Redis correctly enforces limits across an entire multi-instance fleet, at the cost of adding Redis as a hard dependency for every rate-limited request — a Redis outage means rate limiting either fails open (allowing all traffic through, a safety risk) or fails closed (rejecting all traffic, an availability risk), and this failure-mode choice must be made deliberately.',
    commonMistakes: [
      'Implementing rate limiting with a naive fixed-window counter, allowing clients to exploit the window-boundary flaw for effectively double the intended rate.',
      'Implementing the Redis token check-and-decrement as separate, non-atomic GET/SET calls from application code, introducing a race condition that lets more requests through than the configured limit under concurrent load.',
      'Not deciding explicitly (and testing) what happens to rate-limited endpoints when Redis itself is unavailable — silently failing open or closed without a deliberate choice is a real production risk either way.',
    ],
    followUpQuestions: [
      'Walk through exactly how the fixed-window boundary-burst flaw allows nearly double the intended rate limit, with concrete request timestamps.',
      'What would you choose — fail open or fail closed — for rate limiting if Redis becomes unavailable, and how would that choice differ between a public API and an internal admin tool?',
      'How would you extend this rate limiter to support DIFFERENT limits per endpoint or per subscription tier, rather than one global limit per client?',
    ],
    relatedTopics: ['Rate Limiting', 'Token Bucket', 'Redis', 'Idempotency', 'Distributed Systems', 'Race Conditions'],
  },
  {
    id: 'python-m23-12',
    number: 'PY-M23-12',
    title: 'Cache stampede / thundering herd, and mitigation strategies',
    difficulty: 'Hard',
    experienceLevel: '4+ Years',
    category: 'Caching Architecture',
    expectedAnswer:
      'A CACHE STAMPEDE (thundering herd) occurs when a popular cache key expires (or the cache becomes unavailable) and MANY concurrent requests simultaneously experience a cache miss, all rushing to recompute/re-fetch the same expensive underlying data at once — instead of one request regenerating the cache entry while others wait or use a stale value, the underlying database/service is hit with a sudden spike of duplicate, redundant load, which can itself cause a cascading outage. The standard mitigations are: a distributed LOCK so only ONE request regenerates the value while others wait briefly, PROBABILISTIC EARLY EXPIRATION (regenerate slightly before actual expiry, spread across requests, so expiry is never a single sharp moment), and STALE-WHILE-REVALIDATE (serve the stale value immediately while regenerating in the background).',
    deepExplanation:
      '```text\nThe stampede, concretely:\n  a popular product\'s cache entry expires at exactly 10:00:00.000\n  1,000 concurrent requests arrive in the next 50ms, ALL see a cache miss\n  ALL 1,000 requests independently query PostgreSQL for the SAME data simultaneously\n  -> the database, which normally handles this load fine when SPREAD OUT, gets hit with\n     1,000x the expected simultaneous load for this one query, and can fall over\n```\n\nMitigation 1 — distributed lock (only one request regenerates; others wait briefly or serve stale):\n\n```python\nasync def get_product_cached(product_id: str) -> dict:\n    cache_key = f"product:{product_id}"\n    cached = await redis_client.get(cache_key)\n    if cached:\n        return json.loads(cached)\n\n    lock_key = f"lock:{cache_key}"\n    got_lock = await redis_client.set(lock_key, "1", nx=True, ex=5)   # only ONE request wins this\n    if got_lock:\n        try:\n            product = await db.products.find_one({"_id": ObjectId(product_id)})\n            await redis_client.set(cache_key, json.dumps(product, default=str), ex=60)\n            return product\n        finally:\n            await redis_client.delete(lock_key)\n    else:\n        # did NOT win the lock — someone else is already regenerating it; wait briefly and retry\n        await asyncio.sleep(0.05)\n        return await get_product_cached(product_id)   # retry, likely now a cache HIT\n```\n\nMitigation 2 — probabilistic early expiration (spreads regeneration across TIME instead of all requests racing at the exact expiry moment):\n\n```python\nimport random\n\nasync def get_with_early_refresh(cache_key: str, ttl: int, fetch_fn):\n    cached = await redis_client.get(cache_key)\n    if cached:\n        data, set_at = json.loads(cached)["value"], json.loads(cached)["set_at"]\n        age = time.time() - set_at\n        # probabilistically decide to refresh EARLY, before actual expiry — the probability\n        # increases as the entry approaches its real TTL, spreading refreshes across many\n        # DIFFERENT requests over time instead of all colliding at the exact expiry instant\n        if random.random() < (age / ttl) ** 5:\n            asyncio.create_task(refresh_in_background(cache_key, fetch_fn, ttl))\n        return data\n    fresh = await fetch_fn()\n    await redis_client.set(cache_key, json.dumps({"value": fresh, "set_at": time.time()}), ex=ttl)\n    return fresh\n```\n\nWhy this matters more for HOT keys specifically, not every cache entry: a stampede is proportional to how many CONCURRENT requests are hitting the SAME key at the moment of expiry — a rarely-accessed key expiring simply causes one ordinary cache miss with no stampede risk at all; the mitigation effort should be targeted at genuinely HIGH-TRAFFIC keys (a homepage\'s "featured products" list, a viral post\'s comment count) where the concurrent-miss scenario is realistic, not applied uniformly to every cached value in the system.\n\nA related, broader failure mode worth naming explicitly: if REDIS ITSELF becomes entirely unavailable (not just a key expiring, but the whole cache layer down), EVERY cached read suddenly becomes a cache miss simultaneously — this is a stampede at the scale of the ENTIRE cache, not just one key, and is precisely why a well-designed system needs the database to be able to survive a full cache outage without falling over (adequate connection pooling, and ideally a circuit breaker that degrades gracefully rather than the database being taken down by the sudden full-traffic surge).',
    productionExample:
      'A flash-sale event caused a homepage\'s "featured deals" cache entry to expire during a period of extremely high concurrent traffic, triggering a stampede that spiked PostgreSQL CPU to 100% and caused a brief site-wide outage as thousands of simultaneous cache-miss queries overwhelmed the database — after implementing the distributed-lock mitigation shown above, a subsequent similar-traffic event saw only ONE query hit the database on that cache key\'s expiry, with the other thousands of concurrent requests either waiting briefly for the lock holder or transparently retrying against the now-repopulated cache.',
    bestPractices: [
      'Use a distributed lock (or a "single-flight" pattern) so only ONE request regenerates an expired hot cache key, while concurrent requests wait briefly or serve a stale value instead of all hitting the underlying database simultaneously.',
      'Apply stampede mitigation specifically to genuinely HIGH-TRAFFIC cache keys, not uniformly to every cached value — the risk is proportional to concurrent request volume on that specific key at expiry.',
      'Ensure the underlying database/service can survive a FULL cache-layer outage without collapsing — adequate connection pooling and, ideally, a circuit breaker for graceful degradation — since a stampede at the scale of the entire cache is a real, higher-severity variant of this same problem.',
    ],
    tradeOffs:
      'Distributed-lock-based stampede mitigation adds real implementation complexity (lock acquisition/release, retry logic, choosing a sensible lock TTL) and a small amount of added latency for the requests that must wait for the lock holder, in exchange for protecting the underlying database from a traffic spike that could otherwise cause a genuine outage — for LOW-traffic cache keys where a stampede is not a realistic risk, this added complexity is unnecessary overhead.',
    commonMistakes: [
      'Applying no stampede mitigation to a genuinely hot cache key, leaving the system vulnerable to a database-overwhelming spike every time that key expires under high concurrent traffic.',
      'Applying stampede-mitigation complexity (locks, probabilistic early refresh) uniformly to every cached value in the system, including low-traffic keys where the added complexity provides no real benefit.',
      'Assuming the cache layer itself can never fail entirely, and not designing the underlying database to survive a full cache outage (every cached read becoming a simultaneous miss) without falling over.',
    ],
    followUpQuestions: [
      'Walk through exactly what happens to the underlying database when a full Redis outage occurs for a system with no stampede protection, and how a circuit breaker (covered elsewhere in this module) could help.',
      'How does probabilistic early expiration avoid concentrating cache regeneration at the EXACT expiry moment, compared to a simple fixed TTL?',
      'How would you decide which specific cache keys in a real system warrant stampede mitigation versus which do not?',
    ],
    relatedTopics: ['Cache Stampede', 'Thundering Herd', 'Distributed Locks', 'Redis', 'Cache Invalidation', 'Graceful Degradation'],
  },
  {
    id: 'python-m23-13',
    number: 'PY-M23-13',
    title: 'The three pillars of observability, RED/USE methods, and SLI/SLO/SLA with error budgets',
    difficulty: 'Hard',
    experienceLevel: '4+ Years',
    category: 'Observability',
    expectedAnswer:
      'The three pillars — LOGS (discrete, detailed records of individual events), METRICS (aggregated numeric measurements over time, cheap to store and query at scale), and TRACES (the path a single request takes across services/components) — answer different questions and are all needed together: metrics tell you SOMETHING is wrong and roughly where; traces tell you WHERE in a multi-service call chain the problem is; logs tell you EXACTLY what happened for a specific request. RED (Rate, Errors, Duration) is the standard metric set for a SERVICE/API; USE (Utilization, Saturation, Errors) is the standard metric set for a RESOURCE (CPU, database connections, disk) — applying the wrong framework to the wrong thing misses the metrics that actually matter for that layer.',
    deepExplanation:
      '```text\nRED method (for SERVICES — e.g. the FastAPI API layer):\n  Rate      — requests per second\n  Errors    — rate of failed requests (5xx, or business-logic failures)\n  Duration  — request latency distribution (p50, p95, p99 — NEVER just the average, which\n              hides the tail latency that actually determines real user experience)\n\nUSE method (for RESOURCES — e.g. CPU, database connection pool, disk):\n  Utilization — % of time the resource is busy (e.g. CPU at 80% utilization)\n  Saturation  — how much EXTRA work is queued/waiting beyond what the resource can\n                immediately handle (e.g. the run queue length, or connections WAITING\n                for a pool slot) — this is what predicts an impending problem BEFORE\n                utilization alone would show it\n  Errors      — count of resource-level errors (disk I/O errors, OOM kills)\n```\n\nWhy p99 latency (not average) is the metric that actually matters for user experience: if 99% of requests complete in 50ms but 1% take 5 seconds, the AVERAGE looks like ~100ms (deceptively fine), but 1 in 100 real users is having a genuinely bad experience — at any meaningful traffic volume, that 1% represents a large ABSOLUTE number of unhappy users, and averages structurally hide this. p95/p99 percentiles are what most SLOs are actually defined against, precisely because they surface tail-latency problems averages conceal.\n\nSLI / SLO / SLA, precisely, and why the distinction matters:\n\n```text\nSLI (Service Level Indicator) — the ACTUAL MEASUREMENT: "p95 latency was 280ms over the last hour"\nSLO (Service Level Objective)  — the INTERNAL TARGET: "p95 latency should be < 300ms"\nSLA (Service Level Agreement)  — the EXTERNAL, often CONTRACTUAL promise (frequently with\n                                   financial penalties for violation), usually set LOOSER than\n                                   the internal SLO, giving genuine margin for error\n```\n\nERROR BUDGET, the practical mechanism SLOs enable: if the SLO is "99.9% availability over 30 days", that mathematically ALLOWS ~43 minutes of downtime per month — this ALLOWANCE is the "error budget". The genuinely useful, senior-level insight this enables: an error budget explicitly reframes reliability work as a TRADEABLE RESOURCE rather than an unlimited mandate — if the team has consumed 40 of their 43 allowed minutes early in the month, that is an explicit, quantitative signal to SLOW DOWN risky deploys and prioritize stability; if they have consumed almost none of the budget, that is a legitimate, DATA-BACKED signal that the team can afford to ship a riskier feature or move faster, because reliability headroom genuinely exists. This turns "how careful should we be right now" from a vague gut feeling into a concrete number everyone can look at.\n\nDISTRIBUTED TRACING (OpenTelemetry being the current standard, vendor-neutral instrumentation approach) ties a `trace_id` to an ENTIRE request\'s journey across every service/component it touches, with each individual hop recorded as a SPAN (with its own duration, and a `parent_span_id` linking it back into the overall trace tree) — this is specifically what answers "WHERE in this multi-hop request did the slowness actually occur" (the database query? an external API call? application code itself?), a question neither metrics nor logs alone can answer as directly for a request spanning multiple services.',
    productionExample:
      'A team investigating "why do users report occasional slowness even though our average latency dashboard looks fine" discovered the problem only after switching their primary latency dashboard from average to p99 — the average had been masking a genuine, recurring 2-second latency spike affecting roughly 2% of requests, caused by occasional lock contention on a specific database table, invisible in the average but glaringly obvious once p99 was tracked and alerted on.',
    bestPractices: [
      'Track and alert on p95/p99 latency, never average alone — averages structurally hide the tail-latency problems that actually determine real user experience for a meaningful fraction of traffic.',
      'Apply RED metrics to services/APIs and USE metrics to infrastructure resources — using the wrong framework for the wrong layer misses the signals that actually matter for diagnosing problems there.',
      'Define SLOs with explicit error budgets and use budget consumption as a genuine, data-backed input to deploy-risk decisions ("we have burned 90% of this month\'s budget — slow down and prioritize stability") rather than a purely qualitative judgment call.',
    ],
    tradeOffs:
      'Comprehensive observability (structured logs + RED/USE metrics + distributed tracing) requires real upfront instrumentation investment and ongoing infrastructure (a metrics store, a tracing backend, dashboards, alerting rules) — the payoff is dramatically faster incident diagnosis and a genuinely quantitative basis for reliability/velocity tradeoff decisions (via error budgets), which is difficult to overstate the value of once an organization has actually experienced trying to debug a production incident WITHOUT this instrumentation in place.',
    commonMistakes: [
      'Tracking only average latency on dashboards/alerts, missing genuine tail-latency problems that a meaningful fraction of real users are actually experiencing.',
      'Applying RED metrics (rate/errors/duration) to infrastructure RESOURCES, or USE metrics (utilization/saturation/errors) to application-level SERVICES, missing the framework actually suited to that layer.',
      'Defining an SLO with no corresponding error-budget POLICY (what actually happens once the budget is consumed), leaving the SLO as a number nobody actually acts on operationally.',
    ],
    followUpQuestions: [
      'Walk through a concrete scenario where average latency looks perfectly healthy while p99 latency reveals a real, user-impacting problem.',
      'How would you design an error-budget policy that automatically influences deploy-risk decisions (e.g. freezing non-critical deploys) once a threshold is crossed?',
      'How does a distributed trace with a shared `trace_id` across services differ from simply correlating separate services\' logs by a shared `request_id`, and when do you genuinely need the former?',
    ],
    relatedTopics: ['Observability', 'RED Method', 'USE Method', 'SLI/SLO/SLA', 'Error Budget', 'Distributed Tracing', 'OpenTelemetry'],
  },
  {
    id: 'python-m23-14',
    number: 'PY-M23-14',
    title: 'Reliability patterns — retries with backoff/jitter, timeouts, circuit breakers, and bulkheads',
    difficulty: 'Hard',
    experienceLevel: '4+ Years',
    category: 'Reliability Engineering',
    expectedAnswer:
      'Every external call (a database query, another service, a third-party API) needs an explicit TIMEOUT (never wait indefinitely), and only genuinely TRANSIENT failures should be RETRIED, using EXPONENTIAL BACKOFF WITH JITTER (increasing delay between attempts, randomized to prevent synchronized retry storms) — never retried BLINDLY, which risks a "retry storm" that amplifies an already-struggling downstream service\'s load. A CIRCUIT BREAKER goes further: after repeated failures, it stops even ATTEMPTING calls to a failing dependency for a cooldown period, protecting both the caller (fast-failing instead of waiting on doomed calls) and the struggling downstream service (giving it room to recover instead of continuing to be hammered).',
    deepExplanation:
      '```python\nimport asyncio, random\n\nasync def call_with_retry(fn, max_retries: int = 3, base_delay: float = 0.1):\n    for attempt in range(max_retries):\n        try:\n            return await asyncio.wait_for(fn(), timeout=2.0)   # ALWAYS have a timeout — never wait forever\n        except (asyncio.TimeoutError, ConnectionError) as exc:   # only RETRYABLE errors — never\n            if attempt == max_retries - 1:                        # retry a 400 Bad Request, which will\n                raise                                              # fail identically every time\n            # exponential backoff WITH jitter — delay grows each attempt, randomized to prevent\n            # every retrying client from hammering the recovering service at the SAME instant\n            delay = base_delay * (2 ** attempt) + random.uniform(0, base_delay)\n            await asyncio.sleep(delay)\n```\n\nWhy JITTER specifically matters, not just exponential growth alone: without jitter, if 1,000 clients all failed at the SAME moment (e.g. a brief downstream blip) and all retry with the SAME deterministic backoff schedule, they will all retry AGAIN at the exact same synchronized moments — turning "1,000 clients each independently backing off" into "1,000 clients simultaneously hammering the recovering service in synchronized waves", which can prevent it from ever actually recovering. Randomized jitter spreads these retries out in time, avoiding this "retry storm" pattern entirely.\n\nCircuit breaker states and transitions, precisely:\n\n```text\nCLOSED (normal operation)\n  -> calls pass through normally; failures are COUNTED\n  -> if failure rate exceeds a threshold within a window -> transition to OPEN\n\nOPEN (failing fast)\n  -> calls are REJECTED IMMEDIATELY, without even attempting the downstream call\n  -> protects BOTH the caller (no wasted time waiting on a doomed call) AND the\n     struggling downstream service (no continued load while it tries to recover)\n  -> after a COOLDOWN period -> transition to HALF-OPEN\n\nHALF-OPEN (testing recovery)\n  -> allow a SMALL number of test calls through\n  -> if they SUCCEED -> transition back to CLOSED (fully resume normal traffic)\n  -> if they FAIL -> transition back to OPEN (the dependency has not actually recovered yet)\n```\n\n```python\nclass CircuitBreaker:\n    def __init__(self, failure_threshold: int = 5, cooldown_seconds: float = 30):\n        self.failure_count = 0\n        self.state = "closed"\n        self.opened_at: float | None = None\n        self.failure_threshold = failure_threshold\n        self.cooldown_seconds = cooldown_seconds\n\n    async def call(self, fn):\n        if self.state == "open":\n            if time.time() - self.opened_at < self.cooldown_seconds:\n                raise CircuitOpenError("Circuit is open — failing fast")\n            self.state = "half_open"   # cooldown elapsed — allow ONE test call through\n        try:\n            result = await fn()\n            self.failure_count = 0\n            self.state = "closed"      # a successful call in half-open fully closes the circuit\n            return result\n        except Exception:\n            self.failure_count += 1\n            if self.failure_count >= self.failure_threshold:\n                self.state = "open"\n                self.opened_at = time.time()\n            raise\n```\n\nBULKHEADS — isolating failure domains so ONE failing dependency cannot exhaust resources needed by UNRELATED functionality: e.g. using SEPARATE connection pools (or thread/task pools) for calls to a critical payment provider versus a non-critical recommendations service — without this isolation, a slow/hanging recommendations API can exhaust the SAME shared connection pool that payment processing also depends on, taking down a critical function because of a non-critical one\'s failure. This is the same "compartmentalization" concept as a ship\'s bulkheads, which is exactly where the pattern\'s name comes from.\n\nWhy timeouts must exist on EVERY external call, stated as plainly as possible: an external call with NO timeout can hang FOREVER if the downstream service simply never responds (not even an error — total silence) — this single missing timeout can exhaust an entire worker pool one request at a time as more and more requests pile up waiting indefinitely, eventually taking down the ENTIRE service even though the actual failure was in one downstream dependency.',
    productionExample:
      'A payment-processing service that called a third-party fraud-detection API with NO timeout experienced a full production outage when that third-party API silently stopped responding (no error, just an indefinitely hanging connection) — every incoming payment request eventually hung waiting on the fraud check, exhausting the entire Uvicorn worker pool within minutes even though payment processing itself was otherwise perfectly healthy; the permanent fix added both an explicit 2-second timeout AND a circuit breaker around that specific call, so a future similar outage would fail fast after a brief cooldown period instead of cascading into a total service outage.',
    bestPractices: [
      'Set an explicit timeout on EVERY external call (database query, HTTP call to another service, third-party API) — never allow a call to potentially wait indefinitely.',
      'Only retry genuinely TRANSIENT failure types (timeouts, connection errors, 5xx responses) — never retry errors that will fail identically every time (400 Bad Request, 401 Unauthorized, validation errors).',
      'Use exponential backoff WITH randomized jitter for retries, and a circuit breaker for calls to dependencies that can fail for extended periods, to avoid retry storms and to fail fast once a dependency is clearly unhealthy.',
    ],
    tradeOffs:
      'Retries with backoff/jitter improve resilience against brief, transient failures at the cost of added latency for the failing request and some implementation complexity in correctly classifying retryable vs non-retryable errors; a circuit breaker adds even more state/complexity but protects both the caller and the struggling dependency during EXTENDED outages, at the cost of legitimate requests being rejected during the OPEN state even if the dependency might have actually recovered slightly before the cooldown elapses — a deliberate, tunable tradeoff between fast-failing and giving a recovering dependency room to breathe.',
    commonMistakes: [
      'Making an external call with no explicit timeout, risking an entire worker pool being exhausted by requests hanging indefinitely on a single unresponsive dependency.',
      'Retrying EVERY failure type indiscriminately, including non-retryable errors (like a 400 Bad Request) that will fail identically on every retry attempt, wasting time and resources for no benefit.',
      'Implementing retry backoff WITHOUT jitter, causing many concurrently-failing clients to retry in synchronized waves that can prevent a recovering downstream service from ever actually stabilizing.',
    ],
    followUpQuestions: [
      'Walk through exactly how a missing timeout on one external call can cascade into a full-service outage, even when the rest of the application logic is completely healthy.',
      'Why does jitter specifically prevent a "retry storm", and what would go wrong with exponential backoff alone (no jitter) under many simultaneously-failing clients?',
      'How would you design bulkhead isolation so a slow, non-critical dependency cannot exhaust resources shared with a critical one?',
    ],
    relatedTopics: ['Retries', 'Exponential Backoff', 'Jitter', 'Timeouts', 'Circuit Breaker', 'Bulkheads', 'Reliability Engineering'],
  },
  {
    id: 'python-m23-15',
    number: 'PY-M23-15',
    title: 'Background processing — FastAPI BackgroundTasks limitations vs a real task queue',
    difficulty: 'Medium',
    experienceLevel: '2–4 Years',
    category: 'Background Processing',
    expectedAnswer:
      'FastAPI\'s built-in `BackgroundTasks` runs code AFTER the response is sent, within the SAME process — appropriate only for quick, best-effort, non-critical work (a fire-and-forget audit log entry, a quick cache warm) where losing the task on a process crash/restart is an acceptable risk. A REAL task queue (Celery, RQ, or arq, backed by Redis/RabbitMQ as the broker) is required once work needs DURABILITY (survives a process crash/restart), RETRIES (with backoff, on failure), independent SCALING (dedicated worker processes/machines separate from the API), or SCHEDULING (run at a specific time, or periodically) — `BackgroundTasks` provides none of these guarantees.',
    deepExplanation:
      '```text\nFastAPI BackgroundTasks (same process, best-effort):\n  Client request -> FastAPI handler -> response sent to client -> background_tasks code\n  runs AFTER the response, in the SAME Uvicorn worker process\n  -> if the WORKER PROCESS crashes/restarts before the task completes, the task is\n     SILENTLY LOST — there is no persistence, no retry, no record it ever existed\n\nReal task queue (Celery/RQ/arq, separate process(es), durable):\n  Client request -> FastAPI handler -> ENQUEUE a task message to Redis/RabbitMQ (durable,\n  survives the API process restarting) -> response sent to client immediately\n  -> a SEPARATE worker process (or fleet of worker processes, scaled independently of\n     the API) picks up the task from the queue, executes it, and can RETRY on failure\n```\n\n```python\n# FastAPI BackgroundTasks — appropriate ONLY for quick, genuinely best-effort work\nfrom fastapi import BackgroundTasks\n\ndef log_audit_event(user_id: str, action: str):\n    # if this fails or the process crashes mid-execution, the audit log entry is\n    # simply lost — acceptable ONLY if that loss is genuinely tolerable\n    audit_logger.info(f"{user_id} performed {action}")\n\n@app.post("/orders")\nasync def create_order(payload: OrderCreate, background_tasks: BackgroundTasks):\n    order = await order_service.create(payload)\n    background_tasks.add_task(log_audit_event, order.customer_id, "order_created")\n    return order   # response sent BEFORE log_audit_event actually runs\n\n\n# Celery — durable, retryable, independently-scalable background work\nfrom celery import Celery\n\ncelery_app = Celery("tasks", broker=settings.redis_url)\n\n@celery_app.task(bind=True, max_retries=3, default_retry_delay=5)\ndef send_order_confirmation_email(self, order_id: str):\n    try:\n        email_provider.send(order_id)\n    except EmailProviderTimeout as exc:\n        raise self.retry(exc=exc)   # retried with backoff, DURABLY — survives a worker restart\n\n@app.post("/orders")\nasync def create_order(payload: OrderCreate):\n    order = await order_service.create(payload)\n    send_order_confirmation_email.delay(order.id)   # enqueued DURABLY — survives ANY process crash\n    return order\n```\n\nThe decisive diagnostic question for choosing between the two, stated precisely: "if this task is silently LOST entirely (never runs, no record, no retry), is that genuinely acceptable?" — for a best-effort audit log entry duplicated elsewhere anyway, or a non-critical cache warm-up, the answer is often yes, and `BackgroundTasks` is the simpler, sufficient choice. For sending a payment confirmation email, updating a downstream system that the business depends on, or ANY task where "this must actually happen, eventually, even if it fails on the first attempt" — the answer is no, and a real durable task queue is required.\n\nIDEMPOTENCY still matters for queue-based background tasks specifically because message queues typically guarantee AT-LEAST-ONCE delivery (the same task can be delivered/executed more than once, e.g. if a worker crashes after completing the work but before acknowledging the message) — exactly the same idempotency discipline covered for the outbox pattern (Module 22) and rate limiting/payments (this module) applies here: task handlers should be written to be safely re-runnable.',
    productionExample:
      'An order-confirmation-email feature originally used `BackgroundTasks`, and during a routine deploy (which restarts Uvicorn worker processes), a measurable number of confirmation emails for orders placed in the final seconds before the restart were silently never sent — no error, no record, just lost — migrating this specific task to Celery (durable, retryable, surviving the API process restarting independently) eliminated the lost-email class of bug entirely, while a genuinely best-effort "log this page view for analytics" task elsewhere in the same codebase remained appropriately on `BackgroundTasks`, since occasional loss there was an accepted, tolerable tradeoff.',
    bestPractices: [
      'Use FastAPI `BackgroundTasks` only for genuinely best-effort work where silent loss on a process crash/restart is an acceptable, deliberate tradeoff.',
      'Use a real, durable task queue (Celery, RQ, or arq) for any background work that must actually happen — sending emails, updating downstream systems, processing payments asynchronously — where loss is not acceptable.',
      'Write task-queue handlers to be idempotent, since message queues typically guarantee at-least-once (not exactly-once) delivery, meaning a task can occasionally be executed more than once.',
    ],
    tradeOffs:
      '`BackgroundTasks` requires zero additional infrastructure (no broker, no separate worker processes to deploy/monitor) and is trivially simple to use, at the cost of NO durability guarantee whatsoever — appropriate only when that tradeoff is genuinely acceptable; a real task queue adds meaningful infrastructure (a message broker, dedicated worker processes/deployments to operate and monitor) in exchange for durability, retries, and independent scaling — worth the added operational complexity specifically once a task\'s reliable execution actually matters to the business.',
    commonMistakes: [
      'Using `BackgroundTasks` for work that genuinely must happen reliably (sending a critical email, updating a downstream billing system), discovering only in production that tasks are silently lost during routine deploys/restarts.',
      'Introducing a full Celery/message-broker infrastructure for trivial, genuinely best-effort work where the added operational complexity provides no real benefit over the much simpler `BackgroundTasks`.',
      'Writing task-queue handlers that are NOT idempotent, causing duplicate side effects (a duplicate email, a duplicate charge) when a task is redelivered after an at-least-once delivery retry.',
    ],
    followUpQuestions: [
      'Walk through exactly why a FastAPI worker process restart (during a routine rolling deployment) silently loses any in-flight `BackgroundTasks` work.',
      'Why do most message queues guarantee at-least-once (not exactly-once) delivery, and what does that imply for how task handlers must be written?',
      'How would you decide, for a specific new feature, whether its background work belongs on `BackgroundTasks` or a real task queue?',
    ],
    relatedTopics: ['Background Tasks', 'Celery', 'Message Queues', 'Task Durability', 'Idempotency', 'At-Least-Once Delivery'],
  },
  {
    id: 'python-m23-16',
    number: 'PY-M23-16',
    title: 'File upload architecture — why large files should not pass through the database, and secure upload handling',
    difficulty: 'Medium',
    experienceLevel: '2–4 Years',
    category: 'File & Object Storage',
    expectedAnswer:
      'Large files (images, videos, PDFs) should be uploaded directly to OBJECT STORAGE (S3-style), with only the resulting URL/reference stored as METADATA in PostgreSQL/MongoDB — storing binary file content directly in a relational/document database bloats the database\'s working set (hurting cache efficiency for ALL other queries, not just file-related ones), makes routine database backups dramatically larger and slower, and does not scale as well for serving/CDN-caching large binary content as object storage, which is purpose-built for exactly this workload. Secure upload handling requires validating file size, MIME type, and extension server-side (never trusting client-declared values), generating a random filename (never using the client-supplied one directly), and considering malware scanning for user-supplied content.',
    deepExplanation:
      '```text\nWHY NOT store large files directly in the database:\n  - bloats the database\'s WORKING SET — large binary blobs compete with the actual\n    query-relevant data for the database\'s memory cache, hurting performance for\n    EVERY OTHER query, not just file-related ones\n  - makes routine backups dramatically larger and slower (backing up gigabytes of\n    binary blobs alongside your actual transactional data)\n  - object storage (S3-style) is PURPOSE-BUILT for this workload: cheap at scale,\n    natively integrates with a CDN for fast global serving, supports signed URLs\n    for secure time-limited access\n\nThe correct architecture:\n  Client -> FastAPI (validates, generates a signed upload URL OR proxies the upload)\n         -> Object Storage (the actual file bytes)\n         -> FastAPI writes ONLY the resulting URL/key + metadata (filename, size,\n            content-type, uploaded_by, uploaded_at) into PostgreSQL/MongoDB\n```\n\nA production-oriented upload endpoint, with the security validations that matter:\n\n```python\nimport uuid, mimetypes\nfrom fastapi import FastAPI, UploadFile, File, HTTPException\n\nALLOWED_CONTENT_TYPES = {"image/jpeg", "image/png", "image/webp"}\nMAX_FILE_SIZE = 5 * 1024 * 1024   # 5 MB\n\n@app.post("/uploads")\nasync def upload_file(file: UploadFile = File(...)):\n    # NEVER trust the client-declared content_type alone — validate it explicitly\n    if file.content_type not in ALLOWED_CONTENT_TYPES:\n        raise HTTPException(status_code=400, detail="Unsupported file type")\n\n    contents = await file.read()\n    if len(contents) > MAX_FILE_SIZE:\n        raise HTTPException(status_code=413, detail="File too large")\n\n    # NEVER use the client-supplied filename directly — it can contain path-traversal\n    # sequences ("../../etc/passwd") or collide with/overwrite an existing file;\n    # generate a random, safe filename and preserve only a validated extension\n    extension = mimetypes.guess_extension(file.content_type) or ""\n    safe_filename = f"{uuid.uuid4()}{extension}"\n\n    object_key = f"uploads/{safe_filename}"\n    await object_storage_client.put_object(key=object_key, body=contents, content_type=file.content_type)\n\n    # store ONLY metadata + the object storage reference in the database — never the file bytes\n    record = await db.uploads.insert_one({\n        "object_key": object_key,\n        "original_filename": file.filename,   # kept for DISPLAY purposes only, never used as a path\n        "content_type": file.content_type,\n        "size_bytes": len(contents),\n        "uploaded_at": datetime.now(timezone.utc),\n    })\n    return {"id": str(record.inserted_id), "url": object_storage_client.get_signed_url(object_key)}\n```\n\nWhy validating content-type via the ACTUAL file bytes (or at minimum, both the declared MIME type AND the file extension, cross-checked) matters more than trusting the client\'s declared `Content-Type` header alone: a malicious client can set `Content-Type: image/png` on a header while uploading an executable script — for genuinely security-sensitive upload paths, inspecting the actual file MAGIC BYTES (the file\'s real binary signature) rather than trusting any client-supplied metadata is the more robust validation.\n\nSIGNED URLs for secure, time-limited access: rather than making uploaded files PUBLICLY readable forever, object storage supports generating a SIGNED URL — a URL with an embedded, cryptographically-verified expiration and permission scope, valid only for a short window (e.g. 15 minutes) — this is the standard way to let an authenticated user download/view a private file without either (a) proxying the entire file through the FastAPI application (wasteful bandwidth/compute) or (b) making the file permanently, publicly accessible to anyone with the URL.',
    productionExample:
      'A document-management feature that originally stored PDF file bytes directly as MongoDB `Binary` fields saw its database\'s working set balloon and overall query latency across UNRELATED collections degrade noticeably as the document library grew — migrating file storage to S3-compatible object storage (keeping only a URL/key reference in MongoDB) both fixed the working-set bloat and, as a side benefit, enabled CDN-backed fast global file delivery that the database could never have provided directly.',
    bestPractices: [
      'Store large binary files (images, videos, PDFs) in dedicated object storage, never directly as blob columns/fields in PostgreSQL/MongoDB — keep the database storing only metadata and a reference/URL.',
      'Validate file size, content-type, and extension server-side explicitly — never trust a client-declared `Content-Type` header alone, and for security-sensitive upload paths, inspect actual file magic bytes.',
      'Generate a random, safe filename/object key for every upload — never use the client-supplied filename directly, which can contain path-traversal sequences or collide with an existing object.',
    ],
    tradeOffs:
      'Storing files in dedicated object storage adds an additional infrastructure component (and a small amount of added architectural complexity — coordinating a file upload with its metadata record, and handling the case where one succeeds and the other fails) compared to the apparent simplicity of "just store the bytes in the same database", but avoids serious, compounding performance and backup-cost problems as file volume grows — the added complexity is well worth it for any application handling more than trivial file volume.',
    commonMistakes: [
      'Storing file content directly in the primary database, bloating its working set and backup size in a way that degrades performance for entirely unrelated queries as file volume grows.',
      'Trusting a client-supplied filename directly as the storage path/key, opening a path-traversal vulnerability or allowing an upload to silently overwrite an unrelated existing file.',
      'Making all uploaded files permanently, publicly accessible instead of using time-limited signed URLs for private content, unnecessarily widening the exposure of user-supplied files.',
    ],
    followUpQuestions: [
      'Walk through the failure mode where the file successfully uploads to object storage but the metadata write to the database fails (or vice versa) — how would you make this operation consistent?',
      'Why is validating the declared `Content-Type` header alone insufficient for a genuinely security-sensitive upload path, and what additional validation would you add?',
      'How would signed URLs let you serve private user files through a CDN without making them permanently publicly accessible?',
    ],
    relatedTopics: ['Object Storage', 'File Upload Security', 'Signed URLs', 'MIME Validation', 'Path Traversal', 'Database Design'],
  },
  {
    id: 'python-m23-17',
    number: 'PY-M23-17',
    title: 'Incident response process — severity levels, and diagnosing a database connection pool exhaustion incident',
    difficulty: 'Hard',
    experienceLevel: '4+ Years',
    category: 'Incident Response',
    expectedAnswer:
      'A structured incident response process (alert -> acknowledge -> assess/triage -> mitigate -> communicate -> recover -> root cause -> postmortem -> prevention) exists specifically to keep a high-pressure, time-sensitive situation from becoming chaotic — SEVERITY LEVELS (commonly SEV-1 through SEV-4, though organizations vary the exact scheme) exist to calibrate URGENCY and RESPONSE (who gets paged, how fast, what communication is required) proportional to actual customer/business impact, not proportional to how alarming the underlying technical symptom LOOKS.',
    deepExplanation:
      '```text\nAlert          — an automated monitoring system detects an anomaly and pages on-call\n  |\nAcknowledge    — the on-call engineer confirms they are actively responding (stops further\n  |               escalation/paging of others, sets clear ownership)\nAssess/Triage  — determine actual SEVERITY and customer impact — is this affecting all users,\n  |               some users, or an internal-only system? Is data at risk?\nMitigate       — the IMMEDIATE priority: stop the bleeding (rollback a bad deploy, fail over\n  |               to a healthy replica, scale up capacity) — NOT yet finding the root cause\nCommunicate    — update stakeholders/status page at a regular cadence, even with "still\n  |               investigating" — silence during an incident is worse than an honest update\nRecover        — confirm the mitigation actually worked and the system is genuinely stable\n  |               again, not just superficially quiet\nRoot Cause     — ONLY once the immediate fire is out — dig into WHY this actually happened\n  |\nPostmortem     — a written, blameless account: timeline, impact, root cause, action items\n  |\nPrevention     — the action items from the postmortem actually get done, not just written down\n```\n\nA representative severity scheme (organizations define their own specifics, but the calibration principle is universal):\n\n```text\nSEV-1 — critical, widespread customer impact (the whole service is down, or data is being\n         lost/corrupted) — page immediately, all-hands response, executive visibility\nSEV-2 — significant impact (a major feature is broken, or a subset of customers affected)\n         — page on-call promptly, dedicated response, but not necessarily all-hands\nSEV-3 — minor impact (a non-critical feature degraded, a workaround exists) — addressed\n         during business hours, no immediate page\nSEV-4 — cosmetic/negligible impact — tracked as a normal bug, no incident process needed\n```\n\nA worked diagnosis of a REAL, common production incident — database connection pool exhaustion — walking the full process:\n\n**Symptoms**: API requests suddenly returning 500 errors or timing out; error logs showing `TimeoutError: QueuePool limit of size 10 overflow 5 reached, connection timed out`.\n\n**Metrics to check**: active database connections (via `pg_stat_activity` on PostgreSQL, or the equivalent MongoDB `serverStatus().connections`) — is it pinned at the configured max? Application-level connection pool metrics (SQLAlchemy exposes pool checkouts/checkins if instrumented) — are checkouts far exceeding checkins, suggesting connections are being HELD and never released?\n\n**Investigation**: `SELECT pid, state, query, query_start FROM pg_stat_activity WHERE state != \'idle\' ORDER BY query_start;` — looking specifically for connections stuck in a long-running or hung state, or, just as commonly, connections sitting `idle in transaction` (a transaction was opened but never committed/rolled back, HOLDING the connection indefinitely).\n\n**Root cause (a common real one)**: a code path acquires a database session via a dependency but an exception is raised BEFORE the session\'s cleanup (`finally`/context-manager exit) runs correctly — e.g. a bug where a `yield`-based FastAPI dependency\'s cleanup path has an edge case that occasionally does not execute, silently leaking one connection per affected request until the pool is fully exhausted.\n\n**Immediate mitigation**: restart the affected application instances (forcibly releasing all held connections back cleanly) to restore service immediately, while the root cause investigation continues in parallel — NOT waiting to find the root cause before restoring service.\n\n**Permanent fix**: fix the specific code path leaking connections (ensuring session cleanup is genuinely unconditional), add `pool_pre_ping=True` (detects and discards stale/broken connections automatically) and a `pool_timeout` with clear logging when connections are held unusually long, and add a specific ALERT on "active connections approaching pool limit" so this class of issue is caught well before it becomes a full outage next time.',
    productionExample:
      'A production connection-pool-exhaustion incident was traced to a specific admin-panel endpoint that acquired a database session but, on one particular validation-failure branch, raised an exception in a way that bypassed the session\'s normal cleanup path — under low admin-panel traffic this leaked roughly one connection every few hours, invisible for weeks, until cumulative leakage finally exhausted the pool during a period of otherwise-normal traffic, causing a SEV-2 incident that looked at first like unrelated general API slowness until `pg_stat_activity` investigation revealed the actual `idle in transaction` connections accumulating from that one specific code path.',
    bestPractices: [
      'Calibrate severity levels by actual customer/business IMPACT, not by how alarming a technical symptom looks in isolation — a scary-looking error in an internal-only tool is not the same severity as a widespread customer-facing outage.',
      'Prioritize MITIGATION (restoring service) over root-cause investigation during the active incident — find the root cause AFTER the immediate fire is out, not instead of putting it out.',
      'Always run a blameless postmortem with concrete action items after any significant incident, and track those action items to actual completion — a postmortem that produces no completed follow-through prevents nothing.',
    ],
    tradeOffs:
      'A structured, disciplined incident-response process adds a small amount of process overhead (defined roles, communication cadence, formal postmortems) compared to an ad hoc "just fix it" approach, but pays for itself directly in faster, calmer, more effective incident resolution and — critically — in actually PREVENTING recurrence through tracked postmortem action items, versus repeatedly firefighting the same class of incident without ever addressing its root cause.',
    commonMistakes: [
      'Spending the initial incident-response time investigating root cause instead of mitigating customer impact first, prolonging the outage unnecessarily while the "why" investigation is still ongoing.',
      'Treating every alarming-looking error as maximum severity regardless of actual customer impact, causing alert fatigue and diluting the urgency signal for genuinely critical incidents.',
      'Writing a postmortem with action items that are never actually tracked to completion, guaranteeing the same class of incident recurs.',
    ],
    followUpQuestions: [
      'Walk through exactly how `idle in transaction` connections differ from genuinely active, long-running queries in `pg_stat_activity`, and why the distinction matters for diagnosing a pool exhaustion incident.',
      'Why is `pool_pre_ping=True` a useful SQLAlchemy production setting, and what specific failure mode does it protect against?',
      'How would you design an alert to catch connection-pool exhaustion BEFORE it causes a full outage, rather than only after the fact?',
    ],
    relatedTopics: ['Incident Response', 'Severity Levels', 'Postmortems', 'Connection Pool Exhaustion', 'Production Debugging', 'PostgreSQL'],
  },
  {
    id: 'python-m23-18',
    number: 'PY-M23-18',
    title: 'Disaster recovery — backup strategy, RPO/RTO, and DR architecture options',
    difficulty: 'Hard',
    experienceLevel: '4+ Years',
    category: 'Disaster Recovery',
    expectedAnswer:
      'RPO (Recovery Point Objective) answers "how much DATA can we afford to lose?" (measured as a time window — e.g. an RPO of 5 minutes means losing at most the last 5 minutes of writes is acceptable); RTO (Recovery Time Objective) answers "how quickly must the system be BACK UP?" — these two numbers, set deliberately based on actual business cost of data loss versus downtime, directly DICTATE which disaster recovery architecture (backup-and-restore, pilot light, warm standby, hot standby/multi-region) is actually required, since each provides a different RPO/RTO at a very different infrastructure cost.',
    deepExplanation:
      '```text\nStrategy            RPO              RTO             Relative Cost   How it works\n--------            ---              ---             -------------   ------------\nBackup & Restore     Hours (since      Hours            Lowest          periodic backups, restored\n                     last backup)      to a NEW/rebuilt                 to fresh infrastructure only\n                                       environment                       when disaster actually occurs\n\nPilot Light          Minutes (via       Tens of minutes  Low-moderate    minimal, always-running core\n                     continuous log      to scale up                     infrastructure (e.g. a small\n                     shipping)                                            replica DB) kept warm; full\n                                                                          capacity spun up on demand\n\nWarm Standby         Seconds to         Minutes          Moderate-high   a scaled-down but FUNCTIONAL\n                     low minutes                                          full replica environment\n                                                                          running continuously, scaled\n                                                                          up to full capacity on failover\n\nHot Standby /        Near-zero          Near-zero        Highest         a FULL-CAPACITY duplicate\nMulti-Region                             (often automatic                environment running live in\n                                          failover)                       another region, actively\n                                                                          serving traffic (or ready to\n                                                                          instantly), continuously synced\n```\n\nWorked example applying RPO/RTO to a decision, precisely: an internal analytics dashboard used by a handful of employees might reasonably have RPO = 24 hours, RTO = 4 hours — a simple nightly backup-and-restore strategy is entirely adequate, and anything more elaborate would be needless cost. A core PAYMENT PROCESSING system, by contrast, might require RPO = seconds (losing even a few seconds of transaction data is a serious financial/compliance problem) and RTO = minutes (extended downtime directly costs revenue and damages trust) — this combination essentially MANDATES a warm/hot standby architecture with continuous, near-real-time replication; a nightly-backup strategy would be a genuine, unacceptable business risk for this system, no matter how much cheaper it is.\n\nPostgreSQL-specific backup mechanics worth knowing precisely: a LOGICAL backup (`pg_dump`) exports data as portable SQL/archive format — flexible (can restore into a different PostgreSQL version, or even a modified schema) but SLOWER for both backup and restore on large databases; a PHYSICAL backup (base backup + continuous WAL archiving) copies the actual on-disk data files plus a stream of write-ahead-log segments — enables POINT-IN-TIME RECOVERY (restore to any specific moment, not just the last full backup\'s timestamp, by replaying WAL up to that exact point) and is dramatically faster to restore at scale, at the cost of being tied to compatible PostgreSQL versions/architecture. MongoDB\'s equivalent: `mongodump`/`mongorestore` for logical backups; filesystem/volume snapshots (coordinated with WiredTiger checkpoints) for physical backups, with OPLOG-based point-in-time recovery achievable in a replica-set configuration.\n\nThe critical, frequently-skipped practice this entire topic depends on: backups are WORTHLESS until they have been ACTUALLY TESTED via a real restore — a backup process that has silently been producing corrupted or incomplete backups for months, discovered only during an actual disaster when it is too late to fix, is a genuinely common and devastating real-world failure mode; a periodic, scheduled "restore this backup into a scratch environment and verify it actually works" drill is what turns a backup strategy from a false sense of security into a genuine, reliable capability.',
    productionExample:
      'A company\'s disaster-recovery plan looked comprehensive on paper (nightly `pg_dump` backups, stored in a separate region) but had never been tested via an actual restore — when a real database corruption incident occurred, the team discovered the backup restoration script had been silently failing for the past six weeks due to an unrelated permissions change, meaning the most recent GOOD backup was six weeks old, turning what should have been a well-controlled RPO-of-24-hours incident into a much more serious, high-impact data-loss event — the direct, costly lesson being that untested backups provide no real guarantee at all.',
    bestPractices: [
      'Set RPO and RTO deliberately per system, based on the ACTUAL business cost of data loss versus downtime for that specific system — never apply one blanket DR strategy uniformly across systems with genuinely different criticality.',
      'Prefer physical backups with continuous WAL archiving (PostgreSQL) for systems needing tight RPO and fast restore at scale, reserving logical backups (pg_dump) for smaller databases or cases needing cross-version portability.',
      'Regularly and genuinely TEST backup restoration (a scheduled drill restoring into a real scratch environment) — an untested backup provides no actual guarantee and is a common, devastating real-world failure mode.',
    ],
    tradeOffs:
      'Tighter RPO/RTO targets (hot standby, multi-region) provide dramatically better disaster-recovery guarantees at proportionally higher infrastructure and operational cost (continuous replication, duplicated full-capacity infrastructure, cross-region networking complexity) — the right investment level is a genuine business decision weighing the actual cost of data loss/downtime for that specific system against the ongoing cost of the DR infrastructure required to prevent it, not a technical decision made in isolation.',
    commonMistakes: [
      'Setting up a backup strategy and never actually testing restoration, discovering only during a real disaster that backups have been silently broken or incomplete for an extended period.',
      'Applying the SAME disaster-recovery strategy uniformly across systems with very different actual business criticality, either over-investing in DR for a low-stakes internal tool or dangerously under-investing for a critical revenue-generating system.',
      'Choosing a DR architecture based on infrastructure cost alone, without first establishing the actual RPO/RTO requirements that DICTATE which architecture is genuinely sufficient.',
    ],
    followUpQuestions: [
      'Walk through why a physical backup with WAL archiving enables point-in-time recovery in a way a periodic logical `pg_dump` alone cannot.',
      'How would you design and schedule a genuine backup-restoration test drill without disrupting the production system it is protecting?',
      'For a specific system, how would you determine the actual business cost of data loss (informing RPO) versus downtime (informing RTO), rather than picking numbers arbitrarily?',
    ],
    relatedTopics: ['Disaster Recovery', 'RPO', 'RTO', 'Backup Strategy', 'Point-in-Time Recovery', 'High Availability'],
  },
  {
    id: 'python-m23-19',
    number: 'PY-M23-19',
    title: 'Scalability — statelessness, load balancing algorithms, and database scaling recap',
    difficulty: 'Medium',
    experienceLevel: '2–4 Years',
    category: 'Scalability',
    expectedAnswer:
      'A FastAPI service should be STATELESS (no request-specific data held in process memory between requests) specifically because it is the property that makes horizontal scaling trivial — a load balancer can route ANY request to ANY instance interchangeably, and instances can be added/removed/restarted freely without losing anything, since all durable state lives in backing services (PostgreSQL, MongoDB, Redis). The LOAD BALANCING ALGORITHM (round robin, least connections, weighted) determines HOW traffic is distributed across those interchangeable instances, and the right choice depends on whether requests have roughly uniform or wildly varying processing cost.',
    deepExplanation:
      '```text\nLoad balancing algorithms, and when each is the right choice:\n\nRound robin        — requests distributed in strict rotation (instance 1, 2, 3, 1, 2, 3, ...)\n                       Simple, works well when requests have roughly UNIFORM processing cost.\n\nLeast connections    — routes each new request to whichever instance currently has the\n                       FEWEST active connections. Better than round robin when request\n                       processing TIME varies significantly (a slow instance naturally\n                       receives fewer new requests until it catches up).\n\nWeighted routing      — some instances receive proportionally MORE traffic than others\n                       (e.g. a newer, more powerful instance type gets a higher weight) —\n                       also the basis for canary deployments (Module 23\'s CI/CD topic),\n                       where a small weight is assigned to the new version initially.\n\nHealth-check-aware   — ALL of the above should additionally exclude instances currently\n                       failing their readiness/health check (this module\'s FastAPI health\n                       check topic) from receiving ANY new traffic at all.\n```\n\nWhy STATEFUL design breaks horizontal scaling concretely, with a specific example: a naive rate limiter implemented as an in-process Python dict (`{client_id: request_count}`) works perfectly on a single instance, but the MOMENT a second instance is added, each instance tracks its OWN independent count — a client could send `limit` requests to instance A and ANOTHER `limit` requests to instance B, both instances independently believing the client is within its limit, when the ACTUAL combined rate is double the intended limit. This exact failure mode is precisely why this module\'s rate-limiting question mandated a SHARED store (Redis) — the underlying principle generalizes to session data, cache, or any state a scaled-out fleet needs to agree on.\n\nDatabase scaling, tying back directly to Module 22\'s horizontal-scaling treatment, recapped at the production-architecture level: READ REPLICAS scale reads only (the application layer must explicitly route read-only queries to replicas — commonly via a separate "read" database session/engine configured with a replica connection string, distinct from the "write" session pointed at the primary); connection POOLING (per-instance pool size × instance count, staying under the database\'s connection limit, exactly as covered in this module\'s Uvicorn-worker-count question) becomes MORE critical, not less, as the application layer scales horizontally, since MORE stateless application instances means MORE potential simultaneous database connections competing for the same fixed database connection limit.\n\nThe senior-level synthesis this question is really testing: horizontal scalability is not a single technique, it is a PROPERTY that emerges from consistently applying statelessness (this question), proper connection-pool math (the Uvicorn-workers question), shared state stores for anything that must be coordinated across instances (the rate-limiting question), and health-check-aware load balancing (the health-checks question) — all together, as a coherent architecture, not any single trick in isolation.',
    productionExample:
      'A team scaling their FastAPI fleet from 2 to 12 instances during a traffic-growth period discovered their session-affinity-dependent shopping cart feature (storing cart contents in an in-process dict, "temporarily" during initial development) broke intermittently — a user\'s cart would appear to randomly empty whenever the load balancer happened to route a subsequent request to a DIFFERENT instance than the one that held their cart state in memory; migrating cart storage to Redis (a shared, externally-held state store reachable identically from every instance) fixed the bug permanently and, as a direct consequence, finally made the fleet genuinely horizontally scalable without this class of correctness bug.',
    bestPractices: [
      'Keep every FastAPI instance genuinely stateless — any data that must persist between requests or be visible across instances belongs in a backing service (PostgreSQL, MongoDB, Redis), never in process memory.',
      'Choose "least connections" (rather than plain round robin) load balancing when request processing time varies significantly across requests, so a temporarily slow instance is not overloaded with additional new requests.',
      'Recalculate total possible database connections (instance count × per-instance pool size) every time the fleet scales, ensuring it stays comfortably under the database\'s connection limit as instance count grows.',
    ],
    tradeOffs:
      'Strict statelessness requires slightly more upfront architecture discipline (routing anything stateful to a backing service rather than the convenient shortcut of process memory) compared to a quick single-instance prototype, but is the specific property that makes horizontal scaling actually WORK correctly — without it, adding more instances does not just fail to help, it actively introduces subtle, intermittent correctness bugs exactly like the shopping-cart example above.',
    commonMistakes: [
      'Storing any request-coordinating state (rate-limit counters, session data, in-flight operation locks) in process memory, which works fine on one instance and breaks — often subtly and intermittently — the moment a second instance is added.',
      'Using plain round-robin load balancing for a workload with highly variable request processing time, overloading temporarily slow instances with a disproportionate share of new requests.',
      'Scaling application instance count without recalculating the resulting total database connection demand, silently approaching or exceeding the database\'s connection limit as the fleet grows.',
    ],
    followUpQuestions: [
      'Walk through exactly how an in-process rate limiter breaks once a second application instance is added, with concrete request counts.',
      'Why does "least connections" load balancing handle variable request-processing-time workloads better than round robin, mechanically?',
      'How would you route read-only queries to database read replicas at the application layer while keeping writes correctly routed to the primary?',
    ],
    relatedTopics: ['Statelessness', 'Load Balancing', 'Horizontal Scaling', 'Connection Pooling', 'Read Replicas', 'Session State'],
  },
  {
    id: 'python-m23-20',
    number: 'PY-M23-20',
    title: 'Coding: a production-ready FastAPI health/readiness system with dependency checks, timeouts, and a circuit breaker',
    difficulty: 'Hard',
    experienceLevel: '4+ Years',
    category: 'Production Coding',
    expectedAnswer:
      'A realistic "production readiness" coding exercise ties together most of this module\'s core patterns in one worked example: a `lifespan`-managed set of clients (database, Redis, an external API wrapped in a circuit breaker), separate liveness/readiness endpoints with proper timeouts on each dependency check, structured logging with a request-ID, and a centralized error handler — exactly the shape of code a senior engineer is expected to produce when asked to "make this FastAPI service production-ready" in an interview or real onboarding task.',
    deepExplanation:
      '```python\nimport time, uuid, logging, asyncio\nfrom contextlib import asynccontextmanager\nfrom fastapi import FastAPI, Request, HTTPException\nfrom fastapi.responses import JSONResponse\nfrom sqlalchemy.ext.asyncio import create_async_engine\nfrom sqlalchemy import text\nimport redis.asyncio as redis\n\nlogger = logging.getLogger("app")\n\nclass CircuitBreaker:\n    def __init__(self, failure_threshold: int = 5, cooldown: float = 30):\n        self.failures, self.state, self.opened_at = 0, "closed", None\n        self.failure_threshold, self.cooldown = failure_threshold, cooldown\n\n    async def call(self, fn, *, timeout: float):\n        if self.state == "open":\n            if time.time() - self.opened_at < self.cooldown:\n                raise HTTPException(503, "Dependency temporarily unavailable")\n            self.state = "half_open"\n        try:\n            result = await asyncio.wait_for(fn(), timeout=timeout)\n            self.failures, self.state = 0, "closed"\n            return result\n        except Exception:\n            self.failures += 1\n            if self.failures >= self.failure_threshold:\n                self.state, self.opened_at = "open", time.time()\n            raise\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    app.state.db_engine = create_async_engine(settings.database_url, pool_size=10, pool_pre_ping=True)\n    app.state.redis = redis.from_url(settings.redis_url)\n    app.state.pricing_api_breaker = CircuitBreaker()\n    logger.info("Application startup complete")\n    yield\n    await app.state.db_engine.dispose()\n    await app.state.redis.aclose()\n    logger.info("Application shutdown complete")\n\napp = FastAPI(lifespan=lifespan)\n\n@app.middleware("http")\nasync def request_id_middleware(request: Request, call_next):\n    request_id = request.headers.get("X-Request-ID", str(uuid.uuid4()))\n    request.state.request_id = request_id\n    start = time.perf_counter()\n    response = await call_next(request)\n    duration_ms = round((time.perf_counter() - start) * 1000, 2)\n    logger.info("request completed", extra={\n        "request_id": request_id, "method": request.method, "path": request.url.path,\n        "status": response.status_code, "duration_ms": duration_ms,\n    })\n    response.headers["X-Request-ID"] = request_id\n    return response\n\n@app.exception_handler(Exception)\nasync def unhandled_exception_handler(request: Request, exc: Exception):\n    request_id = getattr(request.state, "request_id", "unknown")\n    logger.exception("Unhandled exception", extra={"request_id": request_id})\n    return JSONResponse(status_code=500, content={\n        "error": {"code": "INTERNAL_ERROR", "message": "An unexpected error occurred", "request_id": request_id}\n    })\n\n# LIVENESS — cheap, internal only, NO external dependency calls\n@app.get("/health/live")\nasync def liveness():\n    return {"status": "alive"}\n\n# READINESS — checks actual dependencies, each with its OWN short timeout so one slow\n# dependency cannot make the readiness check itself hang indefinitely\n@app.get("/health/ready")\nasync def readiness():\n    checks = {}\n    try:\n        async with app.state.db_engine.connect() as conn:\n            await asyncio.wait_for(conn.execute(text("SELECT 1")), timeout=2.0)\n        checks["database"] = "ok"\n    except Exception:\n        checks["database"] = "unavailable"\n    try:\n        await asyncio.wait_for(app.state.redis.ping(), timeout=1.0)\n        checks["redis"] = "ok"\n    except Exception:\n        checks["redis"] = "unavailable"\n    healthy = all(v == "ok" for v in checks.values())\n    return JSONResponse(status_code=200 if healthy else 503, content={"status": "ready" if healthy else "not_ready", "checks": checks})\n\n# an endpoint using the circuit breaker around a flaky external dependency\n@app.get("/products/{product_id}/price")\nasync def get_live_price(product_id: str):\n    async def fetch():\n        return await pricing_api_client.get_price(product_id)\n    try:\n        return await app.state.pricing_api_breaker.call(fetch, timeout=2.0)\n    except HTTPException:\n        raise\n    except Exception:\n        raise HTTPException(status_code=502, detail="Pricing service unavailable")\n```\n\nThis single worked example deliberately exercises: `lifespan` for durable resource setup/teardown (this module\'s health-checks question), `pool_pre_ping=True` for stale-connection resilience (this module\'s incident-response question), request-ID-correlated structured logging via middleware (this module\'s logging question), a centralized exception handler returning a stable error envelope (this module\'s error-handling question), properly separated liveness vs readiness with per-dependency TIMEOUTS on the readiness check itself (this module\'s health-checks question, extended so the readiness check cannot itself hang forever on one slow dependency), and a circuit breaker wrapping a flaky external call (this module\'s reliability-patterns question).',
    productionExample:
      'This exact pattern — `lifespan`-managed clients, request-ID middleware, a centralized exception handler, timeout-bounded readiness checks, and circuit-breaker-wrapped external calls — is what a senior engineer is typically asked to add to an intentionally bare-bones FastAPI starter application during a "make this production-ready" take-home or onboarding exercise, precisely because it demonstrates fluency across the full breadth of this module rather than any single isolated technique.',
    bestPractices: [
      'Wrap EACH individual dependency check inside a readiness endpoint with its OWN short timeout, so one slow dependency cannot cause the entire readiness check itself to hang indefinitely.',
      'Thread the SAME `request_id` through middleware-level request logging AND the centralized exception handler, so any error response can be immediately correlated with its full request-lifecycle log context.',
      'Combine a circuit breaker with an explicit per-call timeout (not one or the other) — the timeout bounds an individual call\'s worst-case latency, while the circuit breaker prevents REPEATED slow/failing calls from continuing to consume that same worst-case latency budget indefinitely.',
    ],
    tradeOffs:
      'This level of production hardening (structured logging, correlated error handling, timeout-bounded health checks, circuit breakers) adds real code volume and a few new concepts to reason about compared to a minimal FastAPI app, but is precisely the difference between a service that fails predictably and observably versus one that fails in confusing, hard-to-diagnose, cascading ways under real production conditions — the investment is squarely justified for anything beyond a genuinely throwaway prototype.',
    commonMistakes: [
      'Implementing a readiness check that calls dependencies with NO individual timeout, allowing one slow/hanging dependency to make the readiness probe itself hang indefinitely — which can cause an orchestrator to treat the instance as unresponsive in a confusing, hard-to-diagnose way.',
      'Generating a request_id in middleware but failing to actually pass it through to EVERY subsequent log call and the exception handler, breaking the correlation the whole pattern exists to provide.',
      'Using a circuit breaker WITHOUT an accompanying per-call timeout (or vice versa) — the two patterns solve different but complementary problems and are most effective used together.',
    ],
    followUpQuestions: [
      'Why must the readiness check itself have per-dependency timeouts, given that each individual dependency call presumably already has its own client-level timeout configured?',
      'How would you extend this pattern to expose Prometheus-style metrics (request rate, error rate, duration histograms — the RED method from this module) alongside the existing logging?',
      'What would you change about this design if the `/products/{product_id}/price` endpoint needed to gracefully degrade to a cached/stale price instead of returning a 502 when the circuit is open?',
    ],
    relatedTopics: ['Health Checks', 'Circuit Breaker', 'Structured Logging', 'Error Handling', 'Lifespan', 'Production Readiness', 'Timeouts'],
  },
];

export const MOCK_PYTHON_MODULE23_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Python',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
