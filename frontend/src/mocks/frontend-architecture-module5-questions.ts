// Derived from frontend/src/document/Frontend_Architecture_Design_Patterns_Enterprise_Development_Master_Handbook.md (Module 5 — Folder Structure).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "arch-m5-1",
    number: "ARCH-M5-1",
    title: "Feature-first",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Feature-first: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Feature-first solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Folder Structure"],
  },
  {
    id: "arch-m5-2",
    number: "ARCH-M5-2",
    title: "Domain-first",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Domain-first: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Domain-first solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Folder Structure"],
  },
  {
    id: "arch-m5-3",
    number: "ARCH-M5-3",
    title: "Layer-first",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Layer-first: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Layer-first solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Folder Structure"],
  },
  {
    id: "arch-m5-4",
    number: "ARCH-M5-4",
    title: "Monorepo",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Monorepo: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Monorepo solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Folder Structure"],
  },
  {
    id: "arch-m5-5",
    number: "ARCH-M5-5",
    title: "Shared Packages",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Shared Packages: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Shared Packages solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Folder Structure"],
  },
  {
    id: "arch-m5-6",
    number: "ARCH-M5-6",
    title: "Internal Libraries",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Internal Libraries: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Internal Libraries solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Folder Structure"],
  },
  {
    id: "arch-m5-7",
    number: "ARCH-M5-7",
    title: "Enterprise Folder Structure",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Enterprise Folder Structure: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Enterprise Folder Structure solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Folder Structure"],
  },
  {
    id: "arch-m5-8",
    number: "ARCH-M5-8",
    title: "Migration Strategy",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "A good architecture defines explicit responsibilities and dependency boundaries so UI, domain logic, infrastructure, state, and platform concerns can evolve independently where justified.",
    deepExplanation: "Example:\n\n```ts\nexport interface Dependency<TInput, TOutput> {\n  execute(input: TInput): TOutput;\n}\n\nexport function createService<TInput, TOutput>(\n  dependency: Dependency<TInput, TOutput>,\n): Dependency<TInput, TOutput> {\n  return dependency;\n}\n```\n\nAdditional example:\n\n```ts\n// Migration Strategy: keep the architectural boundary small and explicit.\nexport function createService(dependency) {\n  if (!dependency) throw new TypeError(\"dependency is required\");\n\n  return {\n    execute(input) {\n      if (input == null) throw new TypeError(\"input is required\");\n      return dependency.execute(input);\n    },\n  };\n}\n```\n\nWhy it exists: Without boundaries, components become coupled to APIs, storage, analytics, authorization, and global state. This increases regression risk and makes migration difficult.\n\nInternal working: Dependencies should point toward stable contracts. Infrastructure details should be replaceable behind interfaces where that abstraction has a real benefit.",
    productionExample: "A feature can depend on an application service instead of directly calling `fetch`, allowing API details, retries, telemetry and test doubles to remain at the infrastructure boundary.",
    bestPractices: ["Start with the business problem.","Define constraints.","Choose the simplest architecture that satisfies them.","Make ownership explicit.","Document major decisions.","Measure rather than assume performance.","Plan migration before large rewrites."],
    tradeOffs: "Advantages: Clear ownership; Better testability; Lower accidental coupling; Easier migration; Better maintainability. Disadvantages: Additional abstraction; More files and concepts; Governance overhead; Risk of over-engineering.",
    commonMistakes: ["Choosing patterns because they are fashionable","Ignoring team topology","Ignoring deployment constraints","Designing for hypothetical scale","Creating abstractions before understanding change patterns"],
    followUpQuestions: ["What problem does Migration Strategy solve?","When would you use it?","When would you not use it?","What is the dependency direction?","How would you test it?","How does it affect bundle size?","How would you migrate to it?","What are its failure modes?","How would you observe it in production?","What trade-offs would you explain to leadership?"],
    relatedTopics: ["Folder Structure"],
  },
];

export const MOCK_FRONTEND_ARCHITECTURE_MODULE5_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Architecture',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
