// Derived from frontend/src/document/Frontend_Architecture_Design_Patterns_Enterprise_Development_Master_Handbook.md (Module 5 — Folder Structure).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "arch-m5-1",
    number: "ARCH-M5-1",
    title: "Feature-first",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "Organize around business features so UI, state, API, selectors, and tests that change together stay together. Shared code is extracted only after real cross-feature reuse.",
    deepExplanation: "Step 1 — Understand the folder-structure principle.\n\nStep 1 — Model each feature as a module.\n\nWITHOUT BUILT-IN / CORE:\n```ts\ninterface FeatureModule {\n  name: string;\n  publicApi: string[];\n}\n\nconst features: FeatureModule[] = [\n  { name: \"cart\", publicApi: [\"addItem\", \"removeItem\"] },\n  { name: \"checkout\", publicApi: [\"submitOrder\"] },\n];\n\nfunction findFeature(name: string): FeatureModule | undefined {\n  for (const feature of features) {\n    if (feature.name === name) return feature;\n  }\n  return undefined;\n}\n\n```\n\nWITH BUILT-IN:\n```ts\ninterface FeatureModule {\n  name: string;\n  publicApi: string[];\n}\n\nconst features: FeatureModule[] = [\n  { name: \"cart\", publicApi: [\"addItem\", \"removeItem\"] },\n  { name: \"checkout\", publicApi: [\"submitOrder\"] },\n];\n\nconst featureMap = new Map(\n  features.map((feature) => [feature.name, feature]),\n);\n\nconst cart = featureMap.get(\"cart\");\n```\n\nExample:\n```text\nsrc/\n  features/\n    cart/\n      components/\n      api/\n      state/\n      tests/\n    checkout/\n      components/\n      api/\n      state/\n```\n\nComplexity:\nMap lookup is O(1) average.\n\nStep 5 — Senior interview takeaway:\nExplain ownership, import direction, scalability, and why this structure is appropriate for the team and product.",
    productionExample: "Use feature-first for large products where teams own business capabilities independently.",
    bestPractices: ["Choose folders around stable ownership and change boundaries.", "Keep code close to the feature or domain that owns it.", "Make import/dependency direction explicit.", "Keep shared modules small and promote code only after real reuse.", "Use aliases and lint rules to enforce boundaries."],
    tradeOffs: "Feature-first improves discoverability but requires discipline around shared dependencies.",
    commonMistakes: ["Organizing only by file type as the application grows.", "Putting unrelated business logic into a giant shared or utils folder.", "Allowing circular imports between features and shared modules.", "Moving code into shared too early.", "Doing a large rewrite without an incremental migration path."],
    followUpQuestions: ["Why would you choose this structure?", "What are the dependency boundaries?", "When would you avoid it?", "How does it scale with team size?", "How do you prevent circular dependencies?", "How do you test the boundaries?", "How would you migrate an existing project?", "How does it work in a monorepo?", "What is the build/bundle impact?", "What trade-off would you explain in an interview?"],
    relatedTopics: ["Feature-first", "Feature Modules", "Code Ownership", "Modular Frontend"],
  },
  {
    id: "arch-m5-2",
    number: "ARCH-M5-2",
    title: "Domain-first",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "Organize around business/domain concepts when the domain model is reused across multiple workflows or interfaces.",
    deepExplanation: "Step 1 — Understand the folder-structure principle.\n\nStep 1 — Keep domain rules independent of UI.\n\nWITHOUT BUILT-IN / CORE:\n```ts\ninterface Product {\n  id: string;\n  price: number;\n}\n\nfunction calculateDiscount(\n  product: Product,\n  percentage: number,\n): number {\n  return product.price * (1 - percentage / 100);\n}\n\nconst discounted = calculateDiscount(\n  { id: \"p1\", price: 100 },\n  10,\n);\n\n```\n\nWITH BUILT-IN:\n```ts\ninterface Product {\n  id: string;\n  price: number;\n}\n\nfunction productLabel(product: Product): string {\n  return `${product.id}: ${product.price}`;\n}\n```\n\nExample:\n```text\nsrc/\n  domain/\n    product/\n    order/\n    customer/\n  application/\n  infrastructure/\n  presentation/\n```\n\nComplexity:\nThe sample domain calculation is O(1).\n\nStep 5 — Senior interview takeaway:\nExplain ownership, import direction, scalability, and why this structure is appropriate for the team and product.",
    productionExample: "Use domain-first when pricing, permissions, workflows, or other business rules are shared across many features.",
    bestPractices: ["Choose folders around stable ownership and change boundaries.", "Keep code close to the feature or domain that owns it.", "Make import/dependency direction explicit.", "Keep shared modules small and promote code only after real reuse.", "Use aliases and lint rules to enforce boundaries."],
    tradeOffs: "Domain-first creates a strong business layer, but simple CRUD applications may not need the extra separation.",
    commonMistakes: ["Organizing only by file type as the application grows.", "Putting unrelated business logic into a giant shared or utils folder.", "Allowing circular imports between features and shared modules.", "Moving code into shared too early.", "Doing a large rewrite without an incremental migration path."],
    followUpQuestions: ["Why would you choose this structure?", "What are the dependency boundaries?", "When would you avoid it?", "How does it scale with team size?", "How do you prevent circular dependencies?", "How do you test the boundaries?", "How would you migrate an existing project?", "How does it work in a monorepo?", "What is the build/bundle impact?", "What trade-off would you explain in an interview?"],
    relatedTopics: ["Domain-first", "Domain Model", "DDD", "Business Rules"],
  },
  {
    id: "arch-m5-3",
    number: "ARCH-M5-3",
    title: "Layer-first",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "Group by technical responsibility such as components, hooks, services, and utils. It is easy initially but can scatter one business feature across many directories.",
    deepExplanation: "Step 1 — Understand the folder-structure principle.\n\nStep 1 — Make technical layers explicit.\n\nWITHOUT BUILT-IN / CORE:\n```ts\ninterface LayerFile {\n  layer: \"components\" | \"hooks\" | \"services\" | \"utils\";\n  name: string;\n}\n\nconst files: LayerFile[] = [\n  { layer: \"components\", name: \"UserCard\" },\n  { layer: \"hooks\", name: \"useUser\" },\n  { layer: \"services\", name: \"userApi\" },\n];\n\nfunction filesInLayer(\n  layer: LayerFile[\"layer\"],\n): LayerFile[] {\n  return files.filter((file) => file.layer === layer);\n}\n\n```\n\nWITH BUILT-IN:\n```ts\ninterface LayerFile {\n  layer: \"components\" | \"hooks\" | \"services\" | \"utils\";\n  name: string;\n}\n\nconst files: LayerFile[] = [\n  { layer: \"components\", name: \"UserCard\" },\n  { layer: \"hooks\", name: \"useUser\" },\n  { layer: \"services\", name: \"userApi\" },\n];\n\nfunction filesInLayer(\n  layer: LayerFile[\"layer\"],\n): LayerFile[] {\n  return files.filter((file) => file.layer === layer);\n}\n\nconst hooks = filesInLayer(\"hooks\");\n```\n\nExample:\n```text\nsrc/\n  components/\n  hooks/\n  services/\n  state/\n  utils/\n```\n\nComplexity:\nFiltering is O(n).\n\nStep 5 — Senior interview takeaway:\nExplain ownership, import direction, scalability, and why this structure is appropriate for the team and product.",
    productionExample: "Use layer-first for smaller applications where the team and feature count are limited.",
    bestPractices: ["Choose folders around stable ownership and change boundaries.", "Keep code close to the feature or domain that owns it.", "Make import/dependency direction explicit.", "Keep shared modules small and promote code only after real reuse.", "Use aliases and lint rules to enforce boundaries."],
    tradeOffs: "Layer-first is simple to explain but feature ownership becomes less obvious as the codebase grows.",
    commonMistakes: ["Organizing only by file type as the application grows.", "Putting unrelated business logic into a giant shared or utils folder.", "Allowing circular imports between features and shared modules.", "Moving code into shared too early.", "Doing a large rewrite without an incremental migration path."],
    followUpQuestions: ["Why would you choose this structure?", "What are the dependency boundaries?", "When would you avoid it?", "How does it scale with team size?", "How do you prevent circular dependencies?", "How do you test the boundaries?", "How would you migrate an existing project?", "How does it work in a monorepo?", "What is the build/bundle impact?", "What trade-off would you explain in an interview?"],
    relatedTopics: ["Layer-first", "Technical Layers", "Feature-first", "Folder Conventions"],
  },
  {
    id: "arch-m5-4",
    number: "ARCH-M5-4",
    title: "Monorepo",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "A monorepo keeps multiple applications and packages in one repository. The folder structure should make workspace ownership and package dependencies explicit.",
    deepExplanation: "Step 1 — Understand the folder-structure principle.\n\nStep 1 — Represent package dependencies.\n\nWITHOUT BUILT-IN / CORE:\n```ts\ninterface Package {\n  name: string;\n  dependencies: string[];\n}\n\nconst packages: Package[] = [\n  { name: \"tokens\", dependencies: [] },\n  { name: \"ui\", dependencies: [\"tokens\"] },\n  { name: \"web\", dependencies: [\"ui\"] },\n];\n\nfunction packageByName(name: string): Package | undefined {\n  for (const pkg of packages) {\n    if (pkg.name === name) return pkg;\n  }\n  return undefined;\n}\n\n```\n\nWITH BUILT-IN:\n```ts\ninterface Package {\n  name: string;\n  dependencies: string[];\n}\n\nconst packages: Package[] = [\n  { name: \"tokens\", dependencies: [] },\n  { name: \"ui\", dependencies: [\"tokens\"] },\n  { name: \"web\", dependencies: [\"ui\"] },\n];\n\nconst packageMap = new Map(\n  packages.map((pkg) => [pkg.name, pkg]),\n);\n\nconst uiPackage = packageMap.get(\"ui\");\n```\n\nExample:\n```text\nrepo/\n  apps/\n    web/\n    admin/\n  packages/\n    tokens/\n    ui/\n  tooling/\n```\n\nComplexity:\nMap lookup is O(1) average.\n\nStep 5 — Senior interview takeaway:\nExplain ownership, import direction, scalability, and why this structure is appropriate for the team and product.",
    productionExample: "Use a monorepo when several apps genuinely share packages, tooling, or types.",
    bestPractices: ["Choose folders around stable ownership and change boundaries.", "Keep code close to the feature or domain that owns it.", "Make import/dependency direction explicit.", "Keep shared modules small and promote code only after real reuse.", "Use aliases and lint rules to enforce boundaries."],
    tradeOffs: "Monorepos simplify coordinated changes but require build, dependency, and ownership governance.",
    commonMistakes: ["Organizing only by file type as the application grows.", "Putting unrelated business logic into a giant shared or utils folder.", "Allowing circular imports between features and shared modules.", "Moving code into shared too early.", "Doing a large rewrite without an incremental migration path."],
    followUpQuestions: ["Why would you choose this structure?", "What are the dependency boundaries?", "When would you avoid it?", "How does it scale with team size?", "How do you prevent circular dependencies?", "How do you test the boundaries?", "How would you migrate an existing project?", "How does it work in a monorepo?", "What is the build/bundle impact?", "What trade-off would you explain in an interview?"],
    relatedTopics: ["Monorepo", "Workspaces", "Package Boundaries", "Dependency Graph"],
  },
  {
    id: "arch-m5-5",
    number: "ARCH-M5-5",
    title: "Shared Packages",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "Shared packages contain code intentionally reused by multiple consumers. They should expose a small public API and avoid importing app-specific modules.",
    deepExplanation: "Step 1 — Understand the folder-structure principle.\n\nStep 1 — Define a public package surface.\n\nWITHOUT BUILT-IN / CORE:\n```ts\ninterface PublicExport {\n  name: string;\n  path: string;\n}\n\nconst publicExports: PublicExport[] = [\n  { name: \"Button\", path: \"./Button\" },\n  { name: \"Dialog\", path: \"./Dialog\" },\n];\n\nfunction exportNames(\n  values: readonly PublicExport[],\n): string[] {\n  const result: string[] = [];\n\n  for (const item of values) {\n    result.push(item.name);\n  }\n\n  return result;\n}\n\n```\n\nWITH BUILT-IN:\n```ts\ninterface PublicExport {\n  name: string;\n  path: string;\n}\n\nconst publicExports: PublicExport[] = [\n  { name: \"Button\", path: \"./Button\" },\n  { name: \"Dialog\", path: \"./Dialog\" },\n];\n\nconst exportMap = new Map(\n  publicExports.map((item) => [item.name, item.path]),\n);\n\nconst buttonPath = exportMap.get(\"Button\");\n```\n\nExample:\n```text\npackages/\n  ui/\n    src/\n      Button.ts\n      Dialog.ts\n      index.ts\n```\n\nComplexity:\nBuilding the export list is O(n).\n\nStep 5 — Senior interview takeaway:\nExplain ownership, import direction, scalability, and why this structure is appropriate for the team and product.",
    productionExample: "Use shared packages for stable tokens, UI primitives, typed clients, configuration, or real cross-app utilities.",
    bestPractices: ["Choose folders around stable ownership and change boundaries.", "Keep code close to the feature or domain that owns it.", "Make import/dependency direction explicit.", "Keep shared modules small and promote code only after real reuse.", "Use aliases and lint rules to enforce boundaries."],
    tradeOffs: "Shared packages reduce duplication but every public export becomes a contract to maintain.",
    commonMistakes: ["Organizing only by file type as the application grows.", "Putting unrelated business logic into a giant shared or utils folder.", "Allowing circular imports between features and shared modules.", "Moving code into shared too early.", "Doing a large rewrite without an incremental migration path."],
    followUpQuestions: ["Why would you choose this structure?", "What are the dependency boundaries?", "When would you avoid it?", "How does it scale with team size?", "How do you prevent circular dependencies?", "How do you test the boundaries?", "How would you migrate an existing project?", "How does it work in a monorepo?", "What is the build/bundle impact?", "What trade-off would you explain in an interview?"],
    relatedTopics: ["Shared Packages", "Public API", "Package Boundaries", "Reuse"],
  },
  {
    id: "arch-m5-6",
    number: "ARCH-M5-6",
    title: "Internal Libraries",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "Internal libraries are private reusable modules for one organization or repository. Keep their contracts separate from application-specific implementation.",
    deepExplanation: "Step 1 — Understand the folder-structure principle.\n\nStep 1 — Track which apps consume an internal library.\n\nWITHOUT BUILT-IN / CORE:\n```ts\ninterface Library {\n  name: string;\n  consumers: string[];\n}\n\nconst libraries: Library[] = [\n  { name: \"auth-client\", consumers: [\"web\", \"admin\"] },\n  { name: \"analytics\", consumers: [\"web\", \"admin\"] },\n];\n\nfunction consumersOf(name: string): string[] {\n  for (const library of libraries) {\n    if (library.name === name) return [...library.consumers];\n  }\n  return [];\n}\n\n```\n\nWITH BUILT-IN:\n```ts\ninterface Library {\n  name: string;\n  consumers: string[];\n}\n\nconst libraries: Library[] = [\n  { name: \"auth-client\", consumers: [\"web\", \"admin\"] },\n  { name: \"analytics\", consumers: [\"web\", \"admin\"] },\n];\n\nfunction librariesForApp(app: string): string[] {\n  return libraries\n    .filter((library) => library.consumers.includes(app))\n    .map((library) => library.name);\n}\n```\n\nExample:\n```text\npackages/\n  internal/\n    auth-client/\n    analytics/\n    feature-flags/\n  apps/\n    web/\n    admin/\n```\n\nComplexity:\nThe filter version is O(n × consumers).\n\nStep 5 — Senior interview takeaway:\nExplain ownership, import direction, scalability, and why this structure is appropriate for the team and product.",
    productionExample: "Use internal libraries for organization-wide platform adapters such as auth, analytics, feature flags, or config.",
    bestPractices: ["Choose folders around stable ownership and change boundaries.", "Keep code close to the feature or domain that owns it.", "Make import/dependency direction explicit.", "Keep shared modules small and promote code only after real reuse.", "Use aliases and lint rules to enforce boundaries."],
    tradeOffs: "Internal libraries centralize shared behavior but too many small packages increase dependency management overhead.",
    commonMistakes: ["Organizing only by file type as the application grows.", "Putting unrelated business logic into a giant shared or utils folder.", "Allowing circular imports between features and shared modules.", "Moving code into shared too early.", "Doing a large rewrite without an incremental migration path."],
    followUpQuestions: ["Why would you choose this structure?", "What are the dependency boundaries?", "When would you avoid it?", "How does it scale with team size?", "How do you prevent circular dependencies?", "How do you test the boundaries?", "How would you migrate an existing project?", "How does it work in a monorepo?", "What is the build/bundle impact?", "What trade-off would you explain in an interview?"],
    relatedTopics: ["Internal Libraries", "Platform Packages", "Shared Services", "Package Ownership"],
  },
  {
    id: "arch-m5-7",
    number: "ARCH-M5-7",
    title: "Enterprise Folder Structure",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "An enterprise structure should reflect ownership and dependency direction, not just deeper nesting. A practical structure often separates apps/features, shared code, infrastructure, and tooling.",
    deepExplanation: "Step 1 — Understand the folder-structure principle.\n\nStep 1 — Model a folder tree.\n\nWITHOUT BUILT-IN / CORE:\n```ts\ninterface FolderNode {\n  name: string;\n  children: FolderNode[];\n}\n\nfunction countFolders(node: FolderNode): number {\n  let count = 1;\n\n  for (const child of node.children) {\n    count += countFolders(child);\n  }\n\n  return count;\n}\n\n```\n\nWITH BUILT-IN:\n```ts\ninterface FolderNode {\n  name: string;\n  children: FolderNode[];\n}\n\nfunction folderNames(node: FolderNode): string[] {\n  return [\n    node.name,\n    ...node.children.flatMap(folderNames),\n  ];\n}\n```\n\nExample:\n```text\nrepo/\n  apps/\n  packages/\n  src/\n    features/\n    shared/\n    infrastructure/\n  tooling/\n  docs/\n```\n\nComplexity:\nTree traversal is O(n).\n\nStep 5 — Senior interview takeaway:\nExplain ownership, import direction, scalability, and why this structure is appropriate for the team and product.",
    productionExample: "Use explicit top-level ownership boundaries and enforce import rules with linting or package constraints.",
    bestPractices: ["Choose folders around stable ownership and change boundaries.", "Keep code close to the feature or domain that owns it.", "Make import/dependency direction explicit.", "Keep shared modules small and promote code only after real reuse.", "Use aliases and lint rules to enforce boundaries."],
    tradeOffs: "Enterprise structure improves navigation and ownership, but excessive nesting slows discovery.",
    commonMistakes: ["Organizing only by file type as the application grows.", "Putting unrelated business logic into a giant shared or utils folder.", "Allowing circular imports between features and shared modules.", "Moving code into shared too early.", "Doing a large rewrite without an incremental migration path."],
    followUpQuestions: ["Why would you choose this structure?", "What are the dependency boundaries?", "When would you avoid it?", "How does it scale with team size?", "How do you prevent circular dependencies?", "How do you test the boundaries?", "How would you migrate an existing project?", "How does it work in a monorepo?", "What is the build/bundle impact?", "What trade-off would you explain in an interview?"],
    relatedTopics: ["Enterprise Architecture", "Folder Structure", "Team Ownership", "Dependency Rules"],
  },
  {
    id: "arch-m5-8",
    number: "ARCH-M5-8",
    title: "Migration Strategy",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Folder Structure",
    expectedAnswer: "Folder-structure migration should be incremental: define the target boundary, move one feature or package at a time, keep the app working, then enforce the new rules.",
    deepExplanation: "Step 1 — Understand the folder-structure principle.\n\nStep 1 — Represent migration progress.\n\nWITHOUT BUILT-IN / CORE:\n```ts\ntype MigrationStatus =\n  | \"legacy\"\n  | \"in-progress\"\n  | \"migrated\";\n\ninterface MigrationItem {\n  name: string;\n  status: MigrationStatus;\n}\n\nfunction nextMigration(\n  item: MigrationItem,\n): MigrationItem {\n  if (item.status === \"legacy\") {\n    return { ...item, status: \"in-progress\" };\n  }\n\n  if (item.status === \"in-progress\") {\n    return { ...item, status: \"migrated\" };\n  }\n\n  return item;\n}\n\n```\n\nWITH BUILT-IN:\n```ts\ntype MigrationStatus =\n  | \"legacy\"\n  | \"in-progress\"\n  | \"migrated\";\n\ninterface MigrationItem {\n  name: string;\n  status: MigrationStatus;\n}\n\nfunction countMigrated(\n  items: readonly MigrationItem[],\n): number {\n  return items.filter(\n    (item) => item.status === \"migrated\",\n  ).length;\n}\n```\n\nExample:\n```text\nBefore → src/components + src/services + src/state\nAfter  → src/features/cart/{components,api,state,tests}\n```\n\nComplexity:\nA state transition is O(1); the summary count is O(n).\n\nStep 5 — Senior interview takeaway:\nExplain ownership, import direction, scalability, and why this structure is appropriate for the team and product.",
    productionExample: "Use feature-by-feature migration, codemods, compatibility aliases, and lint rules instead of a long rewrite branch.",
    bestPractices: ["Choose folders around stable ownership and change boundaries.", "Keep code close to the feature or domain that owns it.", "Make import/dependency direction explicit.", "Keep shared modules small and promote code only after real reuse.", "Use aliases and lint rules to enforce boundaries."],
    tradeOffs: "Incremental migration takes longer but reduces release risk and keeps working software available throughout the change.",
    commonMistakes: ["Organizing only by file type as the application grows.", "Putting unrelated business logic into a giant shared or utils folder.", "Allowing circular imports between features and shared modules.", "Moving code into shared too early.", "Doing a large rewrite without an incremental migration path."],
    followUpQuestions: ["Why would you choose this structure?", "What are the dependency boundaries?", "When would you avoid it?", "How does it scale with team size?", "How do you prevent circular dependencies?", "How do you test the boundaries?", "How would you migrate an existing project?", "How does it work in a monorepo?", "What is the build/bundle impact?", "What trade-off would you explain in an interview?"],
    relatedTopics: ["Migration Strategy", "Refactoring", "Codemods", "Incremental Architecture"],
  },
];

export const MOCK_FRONTEND_ARCHITECTURE_MODULE5_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Architecture',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));