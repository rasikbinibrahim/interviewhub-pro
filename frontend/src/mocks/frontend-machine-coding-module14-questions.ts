// Derived from frontend/src/document/Part_13_Frontend_Machine_Coding_Master_Handbook.md (Module 14 — Dashboard Projects).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "machinecoding-m14-271",
    number: "MACHINECODING-M14-271",
    title: "Dashboard shell",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Dashboard shell** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-272",
    number: "MACHINECODING-M14-272",
    title: "Responsive dashboard layout",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Responsive dashboard layout** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-273",
    number: "MACHINECODING-M14-273",
    title: "Sidebar navigation",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Sidebar navigation** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-274",
    number: "MACHINECODING-M14-274",
    title: "KPI cards",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **KPI cards** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-275",
    number: "MACHINECODING-M14-275",
    title: "Charts integration",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Charts integration** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-276",
    number: "MACHINECODING-M14-276",
    title: "Filter toolbar",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Filter toolbar** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-277",
    number: "MACHINECODING-M14-277",
    title: "Date range filter",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Date range filter** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-278",
    number: "MACHINECODING-M14-278",
    title: "Dashboard URL state",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Dashboard URL state** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-279",
    number: "MACHINECODING-M14-279",
    title: "Widget configuration",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Widget configuration** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-280",
    number: "MACHINECODING-M14-280",
    title: "Widget drag and drop",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Widget drag and drop** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\ntype Item = { id: string; label: string };\n\nexport function SortableList({ items }: { items: Item[] }) {\n  return (\n    <ul aria-label=\"Sortable items\">\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" aria-label={`Move ${item.label}`}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-281",
    number: "MACHINECODING-M14-281",
    title: "Resizable widgets",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Resizable widgets** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-282",
    number: "MACHINECODING-M14-282",
    title: "Lazy widgets",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Lazy widgets** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-283",
    number: "MACHINECODING-M14-283",
    title: "Dashboard data fetching",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Dashboard data fetching** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-284",
    number: "MACHINECODING-M14-284",
    title: "Dashboard caching",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Dashboard caching** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-285",
    number: "MACHINECODING-M14-285",
    title: "Dashboard error boundaries",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Dashboard error boundaries** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-286",
    number: "MACHINECODING-M14-286",
    title: "Dashboard skeletons",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Dashboard skeletons** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-287",
    number: "MACHINECODING-M14-287",
    title: "Dashboard performance",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Dashboard performance** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\nexport function ExampleForm() {\n  const [name, setName] = useState(\"\");\n\n  function submit(event: React.FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    if (!name.trim()) return;\n    console.log({ name });\n  }\n\n  return (\n    <form onSubmit={submit} noValidate>\n      <label htmlFor=\"name\">Name</label>\n      <input\n        id=\"name\"\n        name=\"name\"\n        value={name}\n        onChange={(e) => setName(e.target.value)}\n        required\n      />\n      <button type=\"submit\">Save</button>\n    </form>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-288",
    number: "MACHINECODING-M14-288",
    title: "Dashboard accessibility",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Dashboard accessibility** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-289",
    number: "MACHINECODING-M14-289",
    title: "Dashboard testing",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Dashboard testing** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m14-290",
    number: "MACHINECODING-M14-290",
    title: "Enterprise analytics dashboard",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Dashboard Projects",
    expectedAnswer: "Build a production-quality **Enterprise analytics dashboard** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---","# Module 15 – Chat Applications","**Questions 291–310**"],
  },
];

export const MOCK_FRONTEND_MACHINE_CODING_MODULE14_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Machine Coding',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
