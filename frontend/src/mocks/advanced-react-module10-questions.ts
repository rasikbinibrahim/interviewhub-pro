// Derived from frontend/src/document/Part_9_Advanced_React_Master_Handbook.md (Module 10 — Advanced State Management).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "reactadv-m10-166",
    number: "REACTADV-M10-166",
    title: "State architecture",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "State architecture separates local UI state, shared client state, server state and durable/persisted state according to ownership and consistency requirements. A senior engineer chooses the smallest state scope that satisfies the feature instead of putting every value into one global store.",
    deepExplanation: "```tsx\nimport { useState } from \"react\";\n\nexport function SearchPanel() {\n  const [query, setQuery] = useState(\"\");\n  const [isOpen, setIsOpen] = useState(false);\n\n  return (\n    <section>\n      <button onClick={() => setIsOpen(value => !value)}>\n        Filters\n      </button>\n      {isOpen && (\n        <input\n          value={query}\n          onChange={event => setQuery(event.target.value)}\n          placeholder=\"Search products\"\n        />\n      )}\n    </section>\n  );\n}\n```\n\nThe input and panel visibility are local UI state. Server-owned products should normally remain server/query state rather than being copied into a global client store.",
    productionExample: "In a large commerce application, keep modal visibility and form drafts local, authentication/session information in an appropriate shared boundary, remote product data in a server-state/cache layer, and durable preferences in a persistence layer. This avoids a single store becoming a dumping ground.",
    bestPractices: ["Classify state by ownership and lifetime before choosing a library.", "Keep local state local unless multiple consumers genuinely need it.", "Separate server state from client UI state.", "Define one source of truth for each piece of data.", "Document persistence, synchronization and invalidation requirements.", "Measure rerenders and memory usage before optimizing."],
    tradeOffs: "A centralized store can simplify cross-feature coordination, but it increases coupling and invalidation scope. Local state is simpler but can require explicit coordination when many distant components need the same value.",
    commonMistakes: ["Putting every form field into global state.", "Duplicating server data in multiple stores without a synchronization strategy.", "Choosing a library before identifying state ownership.", "Using global state to solve a prop-drilling problem that only needs composition."],
    followUpQuestions: ["How would you classify state in a SaaS dashboard?", "When should state move from local to shared?", "How do you prevent duplicated server state?", "What metrics would you use to detect excessive rerenders?"],
    relatedTopics: ["Local state", "Global state", "Server state", "Derived state", "Persistence", "State ownership", "Caching"],
  },
  {
    id: "reactadv-m10-167",
    number: "REACTADV-M10-167",
    title: "Context versus Redux Toolkit",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "Context is primarily a dependency/value propagation mechanism, while Redux Toolkit is a structured state-management approach with centralized state, actions, reducers and middleware. Context does not automatically provide Redux-style event history, reducer conventions, middleware or selective subscription semantics. Choose based on state complexity, debugging needs, update frequency and team scale.",
    deepExplanation: "```tsx\nimport { createContext, useContext, useReducer } from \"react\";\n\ntype CartState = { count: number };\ntype Action = { type: \"add\" } | { type: \"remove\" };\n\nfunction reducer(state: CartState, action: Action): CartState {\n  if (action.type === \"add\") return { count: state.count + 1 };\n  return { count: Math.max(0, state.count - 1) };\n}\n\nconst CartContext = createContext<{\n  state: CartState;\n  dispatch: React.Dispatch<Action>;\n} | null>(null);\n\nexport function CartProvider({ children }: { children: React.ReactNode }) {\n  const [state, dispatch] = useReducer(reducer, { count: 0 });\n\n  return (\n    <CartContext.Provider value={{ state, dispatch }}>\n      {children}\n    </CartContext.Provider>\n  );\n}\n\nexport function useCart() {\n  const value = useContext(CartContext);\n  if (!value) throw new Error(\"CartProvider is missing\");\n  return value;\n}\n```\n\nFor a small subtree this can be enough. Redux Toolkit becomes more attractive when the application needs standardized actions, middleware, devtools, persistence patterns, normalized state or many independently evolving consumers.",
    productionExample: "A banking dashboard may use Context for a stable theme or authenticated-user metadata, while Redux Toolkit manages complex client workflows such as transaction filters, multi-step selections and audit-friendly state transitions. Remote account data should still have an appropriate server-data strategy.",
    bestPractices: ["Use Context for relatively stable dependencies or narrow shared state.", "Split contexts by update domain when necessary.", "Use Redux Toolkit when predictable event-driven state and tooling justify the dependency.", "Prefer selectors to avoid broad subscriptions where supported.", "Keep server state out of Redux unless there is a deliberate reason to copy it."],
    tradeOffs: "Context has low conceptual overhead but can cause broad consumer updates and lacks a full state-management ecosystem. Redux Toolkit adds conventions and tooling but introduces more architecture and dependency surface.",
    commonMistakes: ["Calling Context a full Redux replacement in every application.", "Putting high-frequency changing data into one large Context value.", "Using Redux for every local input.", "Assuming `useMemo` alone makes a Context architecture performant."],
    followUpQuestions: ["When is Context enough?", "Why can Context cause unnecessary rerenders?", "What does Redux Toolkit add beyond useReducer?", "How would you migrate a Context-heavy application to Redux Toolkit?"],
    relatedTopics: ["React Context", "Redux Toolkit", "useReducer", "Selectors", "Middleware", "Global state"],
  },
  {
    id: "reactadv-m10-168",
    number: "REACTADV-M10-168",
    title: "Zustand architecture",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "Zustand provides a small external-store model where components subscribe to selected slices of store state. Its architectural value comes from colocating shared state and actions without requiring a large reducer/action framework. The main senior concerns are store boundaries, selectors, state ownership, persistence, testing and avoiding a single unstructured global store.",
    deepExplanation: "```ts\nimport { create } from \"zustand\";\n\ntype CartStore = {\n  count: number;\n  add: () => void;\n  remove: () => void;\n};\n\nexport const useCartStore = create<CartStore>(set => ({\n  count: 0,\n  add: () => set(state => ({ count: state.count + 1 })),\n  remove: () => set(state => ({ count: Math.max(0, state.count - 1) })),\n}));\n```\n\n```tsx\nexport function CartButton() {\n  const count = useCartStore(state => state.count);\n  return <button>Cart ({count})</button>;\n}\n```\n\nSelecting a narrow slice is important because consumers should subscribe only to the state they need.",
    productionExample: "A design application can use separate Zustand stores for editor UI state and session-level client state. Large remote datasets should remain in a server-state/query system rather than being manually synchronized in the store.",
    bestPractices: ["Use focused stores or domains instead of one giant store.", "Select narrow slices.", "Keep actions close to the state they modify.", "Define persistence explicitly and version persisted schemas.", "Keep server cache responsibilities separate from client state when appropriate.", "Test store actions independently from React components."],
    tradeOffs: "Zustand has low boilerplate and flexible subscriptions, but that flexibility can produce inconsistent architecture if teams create ad-hoc global state. Large systems still need naming, ownership and persistence conventions.",
    commonMistakes: ["Putting every API response into Zustand.", "Subscribing to the entire store when only one field is needed.", "Creating hidden cross-store dependencies.", "Persisting sensitive state without a security review."],
    followUpQuestions: ["How do Zustand selectors affect rerenders?", "How would you structure stores by domain?", "When would you choose Redux Toolkit instead?", "How would you test a Zustand store?"],
    relatedTopics: ["Zustand", "External store", "Selectors", "useSyncExternalStore", "Persistence", "Client state"],
  },
  {
    id: "reactadv-m10-169",
    number: "REACTADV-M10-169",
    title: "Jotai architecture",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "Jotai models state as small atoms that components can read and update. Derived atoms can express relationships between pieces of state, which can provide fine-grained subscriptions. The architectural challenge is keeping the atom graph understandable and avoiding turning every transient value into an application-wide atom.",
    deepExplanation: "tsx\nimport { atom, useAtom } from \"jotai\";\n\nconst countAtom = atom(0);\nconst doubleAtom = atom(get => get(countAtom) * 2);\n\nexport function Counter() {\n  const [count, setCount] = useAtom(countAtom);\n\n  return (\n    <button onClick={() => setCount(value => value + 1)}>\n      {count} / {doubleAtom}\n    </button>\n  );\n}\n```\n\nA production implementation would read the derived atom with `useAtomValue(doubleAtom)` rather than rendering the atom object itself. The key architectural idea is the dependency graph: `doubleAtom` derives from `countAtom`.",
    productionExample: "A complex form builder can model independent fields and derived validation as atoms so unrelated controls do not need to subscribe to the whole form. Teams should still establish naming and ownership rules so the atom graph remains discoverable.",
    bestPractices: ["Use atoms around meaningful state domains.", "Keep derived state derived rather than duplicated.", "Avoid creating atoms for values that can stay local.", "Document cross-atom dependencies in complex domains.", "Test derived atoms and write atoms separately where practical."],
    tradeOffs: "Fine-grained subscriptions and composable derived state are strengths. The trade-off is that a large atom graph can become difficult to understand and debug without conventions.",
    commonMistakes: ["Treating atoms as a replacement for every local variable.", "Duplicating derived values in separate atoms.", "Creating circular or hard-to-trace dependencies.", "Mixing server cache ownership into ordinary UI atoms without a clear strategy."],
    followUpQuestions: ["How does Jotai differ from Zustand?", "What is a derived atom?", "How do you prevent atom-graph complexity?", "When would Context be simpler?"],
    relatedTopics: ["Jotai", "Atoms", "Derived state", "Selectors", "External state"],
  },
  {
    id: "reactadv-m10-170",
    number: "REACTADV-M10-170",
    title: "MobX architecture",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "MobX uses observable state and reactive derivations so observers update when the observable data they read changes. Its strength is direct modeling of mutable domain state and computed values. Senior design must control mutation boundaries, observability, serialization, debugging and team conventions.",
    deepExplanation: "ts\nimport { makeAutoObservable } from \"mobx\";\n\nclass CartStore {\n  items: number[] = [];\n\n  constructor() {\n    makeAutoObservable(this);\n  }\n\n  add(price: number) {\n    this.items.push(price);\n  }\n\n  get total() {\n    return this.items.reduce((sum, price) => sum + price, 0);\n  }\n}\n\nexport const cartStore = new CartStore();\n```\n\nA React observer component can consume `cartStore.total`. In a production system, mutations and domain rules should be kept explicit rather than allowing arbitrary components to mutate observables.",
    productionExample: "A planning application with rich domain objects can use MobX for editable project models and computed totals. The team defines ownership and mutation rules so the reactive model remains predictable across many screens.",
    bestPractices: ["Keep domain mutations in explicit actions/methods.", "Use computed values for derivations instead of duplicated state.", "Keep store boundaries aligned with domain ownership.", "Be deliberate about serialization and persistence.", "Use MobX DevTools or application telemetry when diagnosing reactive behavior."],
    tradeOffs: "MobX can model complex mutable domains naturally with less reducer boilerplate. The trade-off is implicit reactivity: developers need to understand what is observable and why an observer rerenders.",
    commonMistakes: ["Mutating observable state from arbitrary components.", "Using computed values for side effects.", "Persisting MobX objects without a serialization strategy.", "Assuming reactive tracking removes the need for architecture."],
    followUpQuestions: ["How does MobX track dependencies?", "MobX versus Redux Toolkit?", "How would you test observable domain logic?", "What problems occur with persistence?"],
    relatedTopics: ["MobX", "Observables", "Computed values", "Actions", "Reactive state"],
  },
  {
    id: "reactadv-m10-171",
    number: "REACTADV-M10-171",
    title: "Recoil concepts",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "Recoil introduced an atom/selector model for shared and derived React state. An atom is a unit of state, while selectors represent derived state or asynchronous dependencies. For a current enterprise architecture, the important interview point is not only the API but whether the library is actively suitable for the project's support and maintenance requirements.",
    deepExplanation: "tsx\nimport { atom, selector } from \"recoil\";\n\nexport const countState = atom({\n  key: \"countState\",\n  default: 0,\n});\n\nexport const doubledCount = selector({\n  key: \"doubledCount\",\n  get: ({ get }) => get(countState) * 2,\n});\n```\n\nComponents subscribe to the atom or selector through Recoil hooks. The architectural pattern is fine-grained shared state plus derived state; library selection should additionally consider project maintenance and ecosystem status.",
    productionExample: "For an existing application that already uses Recoil, isolate state domains, avoid unnecessary global atoms, and create a migration plan if the dependency no longer fits the organization's support requirements. Do not introduce it into a new system solely because its historical API is familiar.",
    bestPractices: ["Evaluate library maintenance before adopting it.", "Keep atom keys unique and domain-oriented.", "Use selectors for derived state.", "Avoid duplicating derived values.", "Plan migration boundaries if replacing Recoil in an existing codebase."],
    tradeOffs: "Atom/selector modeling can give fine-grained subscriptions and clear derivations. The major enterprise trade-off is ecosystem and maintenance risk if the dependency is not a good long-term fit.",
    commonMistakes: ["Treating Recoil as the default choice for new projects.", "Duplicating selector output into atoms.", "Ignoring dependency maintenance and upgrade risk.", "Using global atoms for purely local UI state."],
    followUpQuestions: ["What are atoms and selectors?", "How would you migrate from Recoil?", "What factors determine whether a state library is enterprise-ready?", "How do derived selectors affect performance?"],
    relatedTopics: ["Recoil", "Atoms", "Selectors", "Derived state", "State library evaluation"],
  },
  {
    id: "reactadv-m10-172",
    number: "REACTADV-M10-172",
    title: "TanStack Query",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "TanStack Query is primarily a server-state/data-fetching library. It manages asynchronous query lifecycles, caching, refetching, invalidation, retries and mutations. It should not be treated as a generic replacement for local UI state; its central abstraction is the query cache and the lifecycle of remote data.",
    deepExplanation: "tsx\nimport { useQuery } from \"@tanstack/react-query\";\n\nexport function UserProfile({ userId }: { userId: string }) {\n  const query = useQuery({\n    queryKey: [\"user\", userId],\n    queryFn: () => fetch(`/api/users/${userId}`).then(r => r.json()),\n  });\n\n  if (query.isPending) return <p>Loading…</p>;\n  if (query.isError) return <p role=\"alert\">Unable to load profile</p>;\n\n  return <h2>{query.data.name}</h2>;\n}\n```\n\nThe query key identifies the cached resource. In production, define stale-time, retry and invalidation behavior according to the API's freshness and failure characteristics.",
    productionExample: "A dashboard uses TanStack Query for customer records, orders and analytics. After a successful mutation, the relevant query keys are invalidated or updated so other screens do not continue showing stale server data.",
    bestPractices: ["Design stable query keys.", "Set stale/freshness policies deliberately.", "Invalidate or update affected queries after mutations.", "Handle cancellation, retries and error states.", "Keep UI-only state outside the query cache.", "Use devtools and telemetry to understand cache behavior."],
    tradeOffs: "It reduces custom server-data synchronization and provides mature cache/refetch behavior. The trade-offs are cache complexity, memory usage, stale-data decisions and another abstraction layer.",
    commonMistakes: ["Putting modal state into query data.", "Using unstable objects in query keys.", "Refetching everything after every mutation.", "Assuming cached data is always fresh."],
    followUpQuestions: ["What makes a good query key?", "How do staleTime and cache retention differ?", "How do you handle optimistic mutations?", "When would you use Redux in addition to TanStack Query?"],
    relatedTopics: ["TanStack Query", "Server state", "Query cache", "Mutations", "Invalidation", "Stale time"],
  },
  {
    id: "reactadv-m10-173",
    number: "REACTADV-M10-173",
    title: "Apollo Client",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "Apollo Client combines GraphQL querying, normalized client caching, mutations and reactive UI integration. Its cache can identify entities and merge results across queries when type policies and identifiers are configured correctly. Senior design focuses on cache normalization, mutation updates, pagination, error handling and whether GraphQL complexity is justified.",
    deepExplanation: "tsx\nimport { gql, useQuery } from \"@apollo/client\";\n\nconst GET_USER = gql`\n  query GetUser($id: ID!) {\n    user(id: $id) {\n      id\n      name\n    }\n  }\n`;\n\nexport function User({ id }: { id: string }) {\n  const { data, loading, error } = useQuery(GET_USER, {\n    variables: { id },\n  });\n\n  if (loading) return <p>Loading…</p>;\n  if (error) return <p role=\"alert\">Failed</p>;\n\n  return <h2>{data.user.name}</h2>;\n}\n```\n\nThe normalized cache can reuse the same user entity across different queries when the schema/client configuration supplies stable identity information.",
    productionExample: "A multi-team product platform uses GraphQL for customer, billing and project data. Apollo Client manages normalized cache updates while the backend owns authorization and business rules. Pagination and mutation policies are documented per domain.",
    bestPractices: ["Define stable entity identity and type policies.", "Plan pagination and cache merge behavior.", "Update or invalidate affected entities after mutations.", "Keep authorization on the server.", "Monitor query size, cache growth and GraphQL errors."],
    tradeOffs: "Apollo provides an integrated GraphQL client/cache model. The trade-off is GraphQL and cache-policy complexity, especially for pagination, mutations and highly dynamic schemas.",
    commonMistakes: ["Assuming a mutation automatically updates every relevant UI.", "Ignoring cache normalization rules.", "Caching unauthorized data in a shared client cache.", "Using Apollo when the application only needs simple REST server-state fetching."],
    followUpQuestions: ["How does normalized caching work?", "How do you handle pagination?", "Apollo versus TanStack Query?", "How do you update the cache after a mutation?"],
    relatedTopics: ["Apollo Client", "GraphQL", "Normalized cache", "Type policies", "Pagination", "Mutations"],
  },
  {
    id: "reactadv-m10-174",
    number: "REACTADV-M10-174",
    title: "SWR",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "SWR is a React data-fetching library built around stale-while-revalidate behavior: cached data can be shown immediately while a revalidation request refreshes it. It is useful for remote data that benefits from caching and automatic revalidation while leaving local UI state to React or another state layer.",
    deepExplanation: "tsx\nimport useSWR from \"swr\";\n\nconst fetcher = (url: string) =>\n  fetch(url).then(response => {\n    if (!response.ok) throw new Error(\"Request failed\");\n    return response.json();\n  });\n\nexport function Profile({ id }: { id: string }) {\n  const { data, error, isLoading } = useSWR(`/api/users/${id}`, fetcher);\n\n  if (isLoading) return <p>Loading…</p>;\n  if (error) return <p role=\"alert\">Failed</p>;\n\n  return <h2>{data.name}</h2>;\n}\n```\n\nSWR can revalidate according to events such as focus or configured intervals. The exact policy should match the freshness requirements of the resource.",
    productionExample: "A monitoring dashboard uses SWR for frequently viewed status endpoints. Users get cached data immediately and the client revalidates when appropriate. Critical mutations explicitly trigger `mutate` or another cache update so the UI reflects the new server state.",
    bestPractices: ["Choose stable cache keys.", "Configure revalidation frequency based on business freshness.", "Handle HTTP errors explicitly.", "Use mutation/update APIs deliberately after writes.", "Avoid using SWR as a container for unrelated client UI state."],
    tradeOffs: "SWR is lightweight and straightforward for stale-while-revalidate data. Compared with a larger server-state platform, teams may need to assemble more domain-specific mutation, normalization or workflow conventions themselves.",
    commonMistakes: ["Assuming stale data is equivalent to authoritative data.", "Refetching too frequently and increasing backend load.", "Using a changing object as a cache key without understanding identity.", "Mixing UI state into the remote-data cache."],
    followUpQuestions: ["What does stale-while-revalidate mean?", "SWR versus TanStack Query?", "How do you invalidate after a mutation?", "How would you control backend load?"],
    relatedTopics: ["SWR", "Server state", "Revalidation", "Caching", "mutate"],
  },
  {
    id: "reactadv-m10-175",
    number: "REACTADV-M10-175",
    title: "Server state versus client state",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "Server state is owned by a remote system and can become stale, require synchronization and be shared across users or sessions. Client state is owned by the browser/application, such as modal visibility, drafts or selected tabs. Treating them separately prevents duplicated caches and clarifies invalidation and persistence.",
    deepExplanation: "tsx\nfunction ProductPage() {\n  const [isCartOpen, setIsCartOpen] = useState(false); // client UI state\n\n  const { data: product } = useQuery({\n    queryKey: [\"product\", \"123\"], // server state\n    queryFn: () => fetch(\"/api/products/123\").then(r => r.json()),\n  });\n\n  return (\n    <>\n      <ProductDetails product={product} />\n      <button onClick={() => setIsCartOpen(true)}>Cart</button>\n      {isCartOpen && <Cart />}\n    </>\n  );\n}\n```\n\nThe product comes from a remote source; cart-panel visibility belongs to the browser UI.",
    productionExample: "In a CRM, customer records are server state, while the selected tab, open drawer, unsaved filter and temporary form draft are client state. The team avoids copying the customer record into Redux/Zustand unless a specific offline/editing requirement justifies it.",
    bestPractices: ["Identify the owner of each state value.", "Give server state a cache/freshness strategy.", "Keep transient UI state close to the component.", "Avoid duplicating remote data across client stores.", "Persist only data that has a clear product requirement."],
    tradeOffs: "Separating the categories simplifies consistency and reduces duplication. Some features, such as offline-first editing, intentionally blur the boundary and require an explicit synchronization model.",
    commonMistakes: ["Calling every application value 'global state'.", "Storing API responses permanently in localStorage without freshness rules.", "Using a client store as an undocumented second server cache.", "Ignoring conflict resolution for offline writes."],
    followUpQuestions: ["What is server state?", "When should server data enter a client store?", "How do you design offline editing?", "How do cache invalidation and UI state interact?"],
    relatedTopics: ["Server state", "Client state", "Cache", "Persistence", "Offline-first", "Synchronization"],
  },
  {
    id: "reactadv-m10-176",
    number: "REACTADV-M10-176",
    title: "Normalized state",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "Normalized state stores entities by identity and keeps relationships as IDs or references instead of duplicating the same object throughout nested structures. It is useful when many parts of the UI edit or consume the same entities because one update can change the canonical entity without synchronizing multiple copies.",
    deepExplanation: "ts\ntype User = { id: string; name: string };\ntype Project = { id: string; ownerId: string };\n\ntype State = {\n  usersById: Record<string, User>;\n  projectsById: Record<string, Project>;\n  projectIds: string[];\n};\n\nconst state: State = {\n  usersById: {\n    \"u1\": { id: \"u1\", name: \"Asha\" },\n  },\n  projectsById: {\n    \"p1\": { id: \"p1\", ownerId: \"u1\" },\n  },\n  projectIds: [\"p1\"],\n};\n```\n\nThe project stores `ownerId` rather than copying the entire user object. Selectors can join the entities for presentation.",
    productionExample: "An admin console displays the same users in projects, teams and audit tables. Normalization prevents three copies of a user's name from drifting when the user is renamed.",
    bestPractices: ["Use stable entity IDs.", "Keep relationships explicit.", "Centralize updates to canonical entities.", "Create selectors/view models for denormalized UI shapes.", "Do not normalize tiny, isolated data merely for theoretical purity."],
    tradeOffs: "Normalization improves consistency and update locality but adds selectors and indirection. For small or immutable datasets, nested data can be simpler and more readable.",
    commonMistakes: ["Normalizing every API response regardless of usage.", "Duplicating canonical fields after normalization.", "Using array indexes as entity identity.", "Creating selectors that rebuild huge object graphs on every render."],
    followUpQuestions: ["When is normalization worth the complexity?", "How do you normalize many-to-many relationships?", "How would you update an entity efficiently?", "How does normalization affect memoized selectors?"],
    relatedTopics: ["Entity identity", "Selectors", "Normalized cache", "Redux Toolkit", "Relational state"],
  },
  {
    id: "reactadv-m10-177",
    number: "REACTADV-M10-177",
    title: "Optimistic updates",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "An optimistic update changes the UI before the server confirms the mutation, assuming success. A production implementation needs a rollback or reconciliation path, request identity, error handling and a strategy for concurrent mutations. It is appropriate only when the UX benefit outweighs the consistency risk.",
    deepExplanation: "tsx\nconst mutation = useMutation({\n  mutationFn: updateTodo,\n  onMutate: async nextTodo => {\n    await queryClient.cancelQueries({ queryKey: [\"todos\"] });\n\n    const previous = queryClient.getQueryData<Todo[]>([\"todos\"]);\n\n    queryClient.setQueryData<Todo[]>([\"todos\"], todos =>\n      todos?.map(todo =>\n        todo.id === nextTodo.id ? { ...todo, ...nextTodo } : todo\n      )\n    );\n\n    return { previous };\n  },\n  onError: (_error, _nextTodo, context) => {\n    queryClient.setQueryData([\"todos\"], context?.previous);\n  },\n  onSettled: () => {\n    queryClient.invalidateQueries({ queryKey: [\"todos\"] });\n  },\n});\n```\n\nThe exact API varies by state library, but the pattern is: snapshot → optimistic change → rollback on failure → reconcile with authoritative server data.",
    productionExample: "A task-management app immediately marks a task complete while the request is in flight. If the server rejects the operation, the UI restores the previous state and shows an actionable error. For payment submission, optimistic success would be inappropriate because the cost of false confirmation is high.",
    bestPractices: ["Use optimistic updates only where failure is recoverable.", "Snapshot the previous state when rollback is needed.", "Handle concurrent mutations and out-of-order responses.", "Revalidate with authoritative server data after settlement when appropriate.", "Make errors visible without destroying the user's work."],
    tradeOffs: "Optimistic UI greatly improves perceived latency but increases consistency complexity. Pessimistic updates are slower visually but simpler and safer for high-risk operations.",
    commonMistakes: ["Optimistically updating irreversible financial operations.", "Ignoring rollback.", "Assuming requests always resolve in order.", "Updating only one cache while other views retain stale data."],
    followUpQuestions: ["How do you handle two optimistic mutations at once?", "What operations should never be optimistic?", "How do you reconcile server-generated IDs?", "How do you test rollback behavior?"],
    relatedTopics: ["Optimistic UI", "Mutations", "Rollback", "Cache invalidation", "Concurrency", "Consistency"],
  },
  {
    id: "reactadv-m10-178",
    number: "REACTADV-M10-178",
    title: "Undo and redo",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "Undo/redo can be implemented with history snapshots or, for complex domains, command/event representations. A simple history maintains past and future states; a new edit after undo clears the redo branch. Senior design considers memory, large object graphs, batching, persistence and whether every action is safely reversible.",
    deepExplanation: "ts\ntype History<T> = {\n  past: T[];\n  present: T;\n  future: T[];\n};\n\nfunction apply<T>(history: History<T>, next: T): History<T> {\n  return {\n    past: [...history.past, history.present],\n    present: next,\n    future: [],\n  };\n}\n\nfunction undo<T>(history: History<T>): History<T> {\n  const previous = history.past.at(-1);\n  if (previous === undefined) return history;\n\n  return {\n    past: history.past.slice(0, -1),\n    present: previous,\n    future: [history.present, ...history.future],\n  };\n}\n```\n\nFor large documents, storing full snapshots can be expensive; patches or commands may be preferable.",
    productionExample: "A drawing editor batches drag operations into one undoable command instead of recording hundreds of mousemove states. The history has a bounded size to control memory, and persistence is separated from the in-memory editing history.",
    bestPractices: ["Define what constitutes one user action.", "Batch high-frequency changes.", "Bound history size for large documents.", "Prefer patches/commands when snapshots are too expensive.", "Clear redo history after a new branch of edits.", "Test undo/redo around failed and asynchronous operations."],
    tradeOffs: "Snapshots are simple and reliable but consume memory. Commands/patches reduce storage in some workloads but increase implementation complexity and can be harder to replay safely.",
    commonMistakes: ["Recording every keystroke or mousemove as a separate expensive snapshot.", "Keeping unbounded history.", "Allowing redo after a new divergent edit.", "Mixing server persistence history with local undo semantics without a clear model."],
    followUpQuestions: ["Snapshot versus command history?", "How do you bound memory?", "How do you batch typing into undo steps?", "How would collaborative editing change the design?"],
    relatedTopics: ["History stack", "Command pattern", "Patches", "Time travel", "Collaborative editing"],
  },
  {
    id: "reactadv-m10-179",
    number: "REACTADV-M10-179",
    title: "State machines",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "A state machine models a finite set of explicit states and the events that can transition between them. It is valuable when boolean flags create impossible combinations or when a workflow has strict transitions. The machine should represent domain rules rather than merely wrapping ordinary React state.",
    deepExplanation: "ts\ntype State =\n  | { value: \"idle\" }\n  | { value: \"loading\" }\n  | { value: \"success\"; data: string }\n  | { value: \"error\"; message: string };\n\ntype Event =\n  | { type: \"FETCH\" }\n  | { type: \"RESOLVE\"; data: string }\n  | { type: \"REJECT\"; message: string };\n\nfunction reducer(state: State, event: Event): State {\n  if (state.value === \"idle\" && event.type === \"FETCH\") {\n    return { value: \"loading\" };\n  }\n\n  if (state.value === \"loading\" && event.type === \"RESOLVE\") {\n    return { value: \"success\", data: event.data };\n  }\n\n  if (state.value === \"loading\" && event.type === \"REJECT\") {\n    return { value: \"error\", message: event.message };\n  }\n\n  return state;\n}\n```\n\nThe discriminated union makes impossible states harder to represent than independent `isLoading`, `hasData` and `hasError` booleans.",
    productionExample: "A payment workflow can explicitly model `idle → submitting → requiresAction → succeeded/failed`. This prevents the UI from accidentally showing 'Payment succeeded' and 'Submitting' at the same time.",
    bestPractices: ["Use explicit states for meaningful workflow modes.", "Define valid events and transitions.", "Represent state-specific data with discriminated unions.", "Keep side effects outside pure transition logic.", "Use state machines when the workflow complexity justifies them."],
    tradeOffs: "State machines improve correctness and reasoning for complex workflows but can feel verbose for simple toggles or independent fields. The main trade-off is explicitness versus modeling overhead.",
    commonMistakes: ["Creating a state machine for a single boolean.", "Allowing transitions that violate domain rules.", "Putting network side effects directly inside a pure reducer.", "Using many boolean flags when the combinations are mutually exclusive."],
    followUpQuestions: ["When should you use a state machine?", "How do you model asynchronous states?", "How do guards work conceptually?", "State machine versus reducer?"],
    relatedTopics: ["Finite state machine", "Discriminated unions", "Reducers", "Workflow state", "XState"],
  },
  {
    id: "reactadv-m10-180",
    number: "REACTADV-M10-180",
    title: "Event-driven state",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "Event-driven state represents changes as domain events such as `USER_LOGGED_IN` or `ORDER_CANCELLED`, with one or more consumers reacting to them. It can decouple producers from consumers and provide an auditable flow, but event contracts, ordering, duplication and cleanup must be designed explicitly.",
    deepExplanation: "ts\ntype AppEvent =\n  | { type: \"USER_LOGGED_IN\"; userId: string }\n  | { type: \"CART_CLEARED\" };\n\ntype Listener = (event: AppEvent) => void;\n\nconst listeners = new Set<Listener>();\n\nexport function subscribe(listener: Listener) {\n  listeners.add(listener);\n  return () => listeners.delete(listener);\n}\n\nexport function publish(event: AppEvent) {\n  for (const listener of listeners) listener(event);\n}\n```\n\nIn a real application, events should have typed contracts and controlled ownership. React components should not silently subscribe forever; subscriptions must be cleaned up.",
    productionExample: "After checkout succeeds, the application emits a domain event that updates the confirmation UI, analytics pipeline and cart state. The event contract is versioned and listeners are observable so failures do not become silent.",
    bestPractices: ["Use domain-oriented event names.", "Keep event payloads small and stable.", "Define ownership and ordering expectations.", "Unsubscribe listeners to prevent leaks.", "Distinguish in-memory UI events from durable backend events.", "Make handlers idempotent where duplicate delivery is possible."],
    tradeOffs: "Events reduce direct coupling and can support auditability, but they make control flow less explicit and introduce ordering, duplication and debugging challenges.",
    commonMistakes: ["Using an event bus for simple parent-child state.", "Creating anonymous event names with unclear semantics.", "Ignoring listener cleanup.", "Assuming events are delivered exactly once."],
    followUpQuestions: ["Event-driven state versus Redux actions?", "How do you prevent duplicate events?", "How do you debug an event chain?", "When should an event become durable?"],
    relatedTopics: ["Event bus", "Domain events", "Pub/sub", "Reducers", "Idempotency"],
  },
  {
    id: "reactadv-m10-181",
    number: "REACTADV-M10-181",
    title: "Cross-tab synchronization",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "Cross-tab synchronization keeps browser contexts consistent when the same application is open in multiple tabs. Common mechanisms include BroadcastChannel and storage events. The design must define which state is synchronized, how conflicts are resolved, and whether sensitive data may be transmitted between tabs.",
    deepExplanation: "ts\nconst channel = new BroadcastChannel(\"app-state\");\n\nexport function publishTheme(theme: \"light\" | \"dark\") {\n  channel.postMessage({ type: \"THEME_CHANGED\", theme });\n}\n\nchannel.onmessage = event => {\n  if (event.data.type === \"THEME_CHANGED\") {\n    // Update local UI state.\n    console.log(\"Theme changed in another tab:\", event.data.theme);\n  }\n};\n\n// Cleanup when the application context is destroyed:\n// channel.close();\n```\n\nOnly synchronize data that genuinely needs cross-tab consistency.",
    productionExample: "When a user signs out in one tab, other tabs should react quickly and clear their authenticated UI. The server remains the authority for session validity; cross-tab messaging is a coordination mechanism, not the security boundary.",
    bestPractices: ["Synchronize only necessary state.", "Treat cross-tab messages as untrusted input.", "Handle duplicate/out-of-order messages when relevant.", "Close BroadcastChannel instances during cleanup.", "Keep authentication authorization server-side.", "Define conflict resolution for simultaneous edits."],
    tradeOffs: "Broadcasting improves consistency across tabs with low application complexity, but messages are transient and browser-scoped. Durable synchronization needs a server or persistence mechanism.",
    commonMistakes: ["Treating localStorage as a secure secret store.", "Assuming all tabs are permanently connected.", "Synchronizing huge application state snapshots.", "Using cross-tab events as proof of authorization."],
    followUpQuestions: ["BroadcastChannel versus storage events?", "How do you synchronize logout?", "How do you handle conflicting edits?", "What happens when a tab was offline?"],
    relatedTopics: ["BroadcastChannel", "Storage events", "Multi-tab state", "Session synchronization", "Conflict resolution"],
  },
  {
    id: "reactadv-m10-182",
    number: "REACTADV-M10-182",
    title: "Persistence",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "State persistence means storing selected client state across reloads or sessions, commonly in localStorage, IndexedDB or a backend. Persistence should be intentional: define the data schema, versioning, migration, expiration, privacy and recovery behavior rather than serializing the entire store.",
    deepExplanation: "ts\ntype Preferences = {\n  version: 2;\n  theme: \"light\" | \"dark\";\n  pageSize: number;\n};\n\nconst key = \"app.preferences\";\n\nexport function savePreferences(value: Preferences) {\n  localStorage.setItem(key, JSON.stringify(value));\n}\n\nexport function loadPreferences(): Preferences | null {\n  const raw = localStorage.getItem(key);\n  if (!raw) return null;\n\n  try {\n    const value = JSON.parse(raw) as Preferences;\n    return value.version === 2 ? value : null;\n  } catch {\n    return null;\n  }\n}\n```\n\nSensitive credentials and authorization decisions should not be treated as ordinary localStorage state.",
    productionExample: "An enterprise dashboard persists table density and filter preferences but not authorization tokens or sensitive customer records. The schema is versioned so an application upgrade can migrate or discard incompatible stored values.",
    bestPractices: ["Persist only necessary state.", "Version persisted schemas.", "Validate and migrate loaded data.", "Handle corrupted or unavailable storage gracefully.", "Define expiration and logout behavior.", "Avoid storing secrets in browser storage without a specific, reviewed security design."],
    tradeOffs: "Persistence improves continuity but introduces stale data, migration, privacy and storage-limit concerns. IndexedDB is more suitable for larger structured datasets than localStorage, but adds complexity.",
    commonMistakes: ["Persisting the complete Redux/Zustand store by default.", "Trusting persisted JSON without validation.", "Never clearing data after logout where policy requires it.", "Assuming browser storage is a secure credential vault."],
    followUpQuestions: ["localStorage versus IndexedDB?", "How do you migrate persisted state?", "What should never be persisted?", "How do you recover from corrupted persisted data?"],
    relatedTopics: ["localStorage", "IndexedDB", "Persistence", "Schema migration", "Storage security"],
  },
  {
    id: "reactadv-m10-183",
    number: "REACTADV-M10-183",
    title: "Offline state",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "Offline state allows an application to continue useful work when the network is unavailable. A robust design separates local durable data from server synchronization, queues mutations, tracks pending/failed operations and defines conflict resolution. An offline indicator alone is not an offline architecture.",
    deepExplanation: "ts\ntype PendingMutation = {\n  id: string;\n  type: \"UPDATE_NOTE\";\n  payload: { noteId: string; text: string };\n  createdAt: number;\n};\n\nconst queue: PendingMutation[] = [];\n\nexport function enqueueUpdate(noteId: string, text: string) {\n  queue.push({\n    id: crypto.randomUUID(),\n    type: \"UPDATE_NOTE\",\n    payload: { noteId, text },\n    createdAt: Date.now(),\n  });\n}\n```\n\nA production offline queue needs durable storage, retry/backoff, idempotency and conflict handling; an in-memory array is only a conceptual example.",
    productionExample: "A field-service mobile app lets technicians update job notes without connectivity. Notes are stored locally, mutations enter a durable queue, and synchronization resumes when connectivity returns. Conflicts are resolved using explicit business rules rather than blindly overwriting server data.",
    bestPractices: ["Persist offline data in durable storage.", "Use idempotency keys for retried mutations.", "Track pending, succeeded and failed operations.", "Use exponential backoff with limits.", "Define conflict-resolution rules before shipping.", "Provide users with clear sync status and recovery actions."],
    tradeOffs: "Offline-first UX improves resilience but substantially increases complexity around synchronization, storage, conflicts and data freshness. Online-only systems are simpler and safer where offline work has little business value.",
    commonMistakes: ["Assuming `navigator.onLine` proves the API is reachable.", "Retrying mutations without idempotency.", "Overwriting newer server data blindly.", "Keeping an unbounded offline queue."],
    followUpQuestions: ["How do you handle conflicts?", "How do you make retries safe?", "What storage would you use on mobile/web?", "How do you test offline synchronization?"],
    relatedTopics: ["Offline-first", "Mutation queue", "Synchronization", "Conflict resolution", "Idempotency", "IndexedDB"],
  },
  {
    id: "reactadv-m10-184",
    number: "REACTADV-M10-184",
    title: "State performance",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "State performance is primarily about controlling the amount and frequency of work caused by state changes. Measure component render time, commit time, subscription breadth, browser work and memory before optimizing. Common solutions include narrower subscriptions, component boundaries, memoization, virtualization and moving state closer to where it is used.",
    deepExplanation: "tsx\nimport { memo, useState } from \"react\";\n\nconst Row = memo(function Row({\n  label,\n}: {\n  label: string;\n}) {\n  return <li>{label}</li>;\n});\n\nexport function List() {\n  const [query, setQuery] = useState(\"\");\n\n  const items = [\"React\", \"TypeScript\", \"Node.js\"];\n\n  return (\n    <>\n      <input value={query} onChange={e => setQuery(e.target.value)} />\n      <ul>\n        {items\n          .filter(item => item.toLowerCase().includes(query.toLowerCase()))\n          .map(item => <Row key={item} label={item} />)}\n      </ul>\n    </>\n  );\n}\n```\n\nMemoization is useful only when it prevents meaningful repeated work; profiling should establish that before adding it.",
    productionExample: "A 20,000-row data grid should not rerender and paint all rows for every keystroke. The production solution may combine row virtualization, stable selectors, local filter state, debounced expensive operations and server-side filtering depending on requirements.",
    bestPractices: ["Profile before optimizing.", "Keep high-frequency state near its consumers.", "Use selective subscriptions.", "Virtualize large collections.", "Memoize expensive work only when measurement justifies it.", "Track Core Web Vitals and interaction latency in production."],
    tradeOffs: "Optimization can reduce CPU and rendering work but adds complexity and memory overhead. Over-memoization can make code harder to reason about without producing measurable benefit.",
    commonMistakes: ["Adding `useMemo` everywhere.", "Using array indexes as unstable keys.", "Optimizing before identifying the expensive component.", "Ignoring network or browser layout costs when blaming React."],
    followUpQuestions: ["How do you diagnose unnecessary rerenders?", "When is virtualization better than memoization?", "How would you optimize a 100k-row table?", "Which production metrics would you monitor?"],
    relatedTopics: ["React Profiler", "Memoization", "Selectors", "Virtualization", "Core Web Vitals", "Render performance"],
  },
  {
    id: "reactadv-m10-185",
    number: "REACTADV-M10-185",
    title: "Enterprise state strategy",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "Advanced State Management",
    expectedAnswer: "An enterprise state strategy should define state categories, ownership, persistence, synchronization, security, observability and library standards. A practical strategy usually uses local React state for local UI concerns, a server-state/cache layer for remote data, and a shared client-state solution only for genuinely cross-cutting client workflows.",
    deepExplanation: "tsx\ntype AppStatePolicy = {\n  localUI: string[];\n  serverState: string[];\n  sharedClientState: string[];\n  persistedState: string[];\n};\n\nconst policy: AppStatePolicy = {\n  localUI: [\"modalOpen\", \"inputDraft\"],\n  serverState: [\"orders\", \"customerProfile\"],\n  sharedClientState: [\"cartDraft\", \"editorSelection\"],\n  persistedState: [\"theme\", \"tablePreferences\"],\n};\n```\n\nThe important part is not the object itself; it is the organizational rule that each category has an owner, lifecycle and approved storage mechanism.",
    productionExample: "For a multi-team SaaS platform, the frontend platform team publishes a state decision guide: local state first, server-state library for remote resources, shared client store for cross-feature workflows, and persistence only for approved preferences/offline data. Architecture reviews enforce these boundaries.",
    bestPractices: ["Create a state decision matrix for teams.", "Standardize approved libraries and their use cases.", "Separate server data from client workflows.", "Define security and persistence rules.", "Provide profiling and observability guidance.", "Measure bundle size, render cost, cache hit behavior and memory.", "Migrate incrementally rather than rewriting every state domain."],
    tradeOffs: "Standardization reduces inconsistent patterns across teams, but overly rigid rules can block legitimate feature needs. Enterprise strategy should provide defaults and exceptions with clear reasoning rather than one universal state library.",
    commonMistakes: ["Mandating one global store for every application concern.", "Allowing each team to choose a different server-state strategy without standards.", "Ignoring library maintenance and licensing/security review.", "Measuring architecture by code style instead of reliability and performance outcomes."],
    followUpQuestions: ["How would you choose Redux Toolkit versus Zustand?", "Where does TanStack Query fit?", "How would you govern state libraries across 50 teams?", "What metrics prove the strategy works?"],
    relatedTopics: ["State architecture", "State ownership", "Server state", "Client state", "Governance", "Performance", "Security"],
  },

];

export const MOCK_ADVANCED_REACT_MODULE10_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Advanced React',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));