// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 17 — Production Security).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m17-271",
    number: "SEC-M17-271",
    title: "Production security",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Production security** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-272",
    number: "SEC-M17-272",
    title: "Security monitoring",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Security monitoring** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-273",
    number: "SEC-M17-273",
    title: "Incident response",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Incident response** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-274",
    number: "SEC-M17-274",
    title: "Incident timeline",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Incident timeline** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-275",
    number: "SEC-M17-275",
    title: "Root cause analysis",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Root cause analysis** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-276",
    number: "SEC-M17-276",
    title: "Containment",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Containment** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-277",
    number: "SEC-M17-277",
    title: "Eradication",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Eradication** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-278",
    number: "SEC-M17-278",
    title: "Recovery",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Recovery** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-279",
    number: "SEC-M17-279",
    title: "Security logging",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Security logging** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-280",
    number: "SEC-M17-280",
    title: "Audit trails",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Audit trails** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-281",
    number: "SEC-M17-281",
    title: "Alerting",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Alerting** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-282",
    number: "SEC-M17-282",
    title: "Credential compromise",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Credential compromise** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-283",
    number: "SEC-M17-283",
    title: "XSS incident",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**XSS incident** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// ❌ Vulnerable: untrusted HTML is interpreted by the browser.\n// element.innerHTML = userInput;\n\n// ✅ Secure default: render text as text.\nexport function SafeMessage({ message }: { message: string }) {\n  return <p>{message}</p>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-284",
    number: "SEC-M17-284",
    title: "CSRF incident",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**CSRF incident** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m17-285",
    number: "SEC-M17-285",
    title: "Supply-chain incident",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Production Security",
    expectedAnswer: "**Supply-chain incident** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE17_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
