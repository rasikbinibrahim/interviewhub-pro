// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 13 — Storybook).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m13-1",
    number: "TESTQA-M13-1",
    title: "Storybook Architecture",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Storybook Architecture** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Storybook Architecture** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-2",
    number: "TESTQA-M13-2",
    title: "Stories",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Stories** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Stories** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-3",
    number: "TESTQA-M13-3",
    title: "Args",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Args** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Args** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-4",
    number: "TESTQA-M13-4",
    title: "Controls",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Controls** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Controls** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-5",
    number: "TESTQA-M13-5",
    title: "Decorators",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Decorators** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Decorators** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-6",
    number: "TESTQA-M13-6",
    title: "Themes",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Themes** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Themes** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-7",
    number: "TESTQA-M13-7",
    title: "Interaction Tests",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Interaction Tests** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Interaction Tests** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-8",
    number: "TESTQA-M13-8",
    title: "Accessibility Addon",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Accessibility Addon** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Accessibility Addon** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-9",
    number: "TESTQA-M13-9",
    title: "Design Systems",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Design Systems** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Design Systems** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-10",
    number: "TESTQA-M13-10",
    title: "Mock Data",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Mock Data** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Mock Data** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-11",
    number: "TESTQA-M13-11",
    title: "API States",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**API States** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**API States** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-12",
    number: "TESTQA-M13-12",
    title: "Responsive Stories",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Responsive Stories** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Responsive Stories** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-13",
    number: "TESTQA-M13-13",
    title: "Enterprise Component Libraries",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Enterprise Component Libraries** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Enterprise Component Libraries** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-14",
    number: "TESTQA-M13-14",
    title: "Documentation",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Documentation** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Documentation** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-15",
    number: "TESTQA-M13-15",
    title: "Storybook CI and Visual QA",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Storybook CI and Visual QA** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Storybook CI and Visual QA** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
  {
    id: "testqa-m13-16",
    number: "TESTQA-M13-16",
    title: "Storybook — Module Review",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Storybook",
    expectedAnswer: "**Storybook — Module Review** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Storybook — Module Review** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Storybook"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE13_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));