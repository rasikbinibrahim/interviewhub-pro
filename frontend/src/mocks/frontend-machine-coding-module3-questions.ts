// Derived from frontend/src/document/Part_13_Frontend_Machine_Coding_Master_Handbook.md (Module 3 — Tabs).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "machinecoding-m3-41",
    number: "MACHINECODING-M3-41",
    title: "Basic tabs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Basic tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Basic tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Basic tabs logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Basic tabs logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-42",
    number: "MACHINECODING-M3-42",
    title: "Controlled tabs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Controlled tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Controlled tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Controlled tabs logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Controlled tabs logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-43",
    number: "MACHINECODING-M3-43",
    title: "Uncontrolled tabs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Uncontrolled tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Uncontrolled tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Uncontrolled tabs logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Uncontrolled tabs logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-44",
    number: "MACHINECODING-M3-44",
    title: "URL-synchronized tabs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement URL-synchronized tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement URL-synchronized tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the URL-synchronized tabs logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the URL-synchronized tabs logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-45",
    number: "MACHINECODING-M3-45",
    title: "Keyboard tab navigation",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Keyboard tab navigation as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Keyboard tab navigation as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Keyboard tab navigation logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Keyboard tab navigation logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-46",
    number: "MACHINECODING-M3-46",
    title: "ARIA tabs pattern",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement ARIA tabs pattern as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement ARIA tabs pattern as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the ARIA tabs pattern logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the ARIA tabs pattern logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-47",
    number: "MACHINECODING-M3-47",
    title: "Lazy tab panels",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Lazy tab panels as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Lazy tab panels as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Lazy tab panels logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Lazy tab panels logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-48",
    number: "MACHINECODING-M3-48",
    title: "Tabs with async data",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Tabs with async data as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Tabs with async data as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Tabs with async data logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Tabs with async data logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-49",
    number: "MACHINECODING-M3-49",
    title: "Tabs with unsaved changes",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Tabs with unsaved changes as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Tabs with unsaved changes as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Tabs with unsaved changes logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Tabs with unsaved changes logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-50",
    number: "MACHINECODING-M3-50",
    title: "Closable tabs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Closable tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Closable tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Closable tabs logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Closable tabs logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-51",
    number: "MACHINECODING-M3-51",
    title: "Reorderable tabs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Reorderable tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Reorderable tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Reorderable tabs logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Reorderable tabs logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-52",
    number: "MACHINECODING-M3-52",
    title: "Nested tabs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Nested tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Nested tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Nested tabs logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Nested tabs logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-53",
    number: "MACHINECODING-M3-53",
    title: "Scrollable tabs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Scrollable tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Scrollable tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Scrollable tabs logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Scrollable tabs logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-54",
    number: "MACHINECODING-M3-54",
    title: "Responsive tabs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Responsive tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Responsive tabs as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Responsive tabs logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Responsive tabs logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-55",
    number: "MACHINECODING-M3-55",
    title: "Tabs performance",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Tabs performance as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Tabs performance as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Tabs performance logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Tabs performance logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-56",
    number: "MACHINECODING-M3-56",
    title: "Tabs testing",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Tabs testing as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Tabs testing as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Tabs testing logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Tabs testing logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-57",
    number: "MACHINECODING-M3-57",
    title: "Tabs Storybook",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Tabs Storybook as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Tabs Storybook as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Tabs Storybook logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Tabs Storybook logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-58",
    number: "MACHINECODING-M3-58",
    title: "Tabs state persistence",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Tabs state persistence as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Tabs state persistence as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Tabs state persistence logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Tabs state persistence logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-59",
    number: "MACHINECODING-M3-59",
    title: "Tabs error handling",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Tabs error handling as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Tabs error handling as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Tabs error handling logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Tabs error handling logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m3-60",
    number: "MACHINECODING-M3-60",
    title: "Enterprise tab component",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Tabs",
    expectedAnswer: "Implement Enterprise tab component as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Enterprise tab component as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Tab {\n  id: string;\n  label: string;\n}\n\nfunction selectTab(\n  tabs: readonly Tab[],\n  id: string,\n): string | null {\n  for (let i = 0; i < tabs.length; i += 1) {\n    if (tabs[i].id === id) {\n      return tabs[i].id;\n    }\n  }\n\n  return null;\n}\n\nconst active = selectTab(\n  [{ id: \"home\", label: \"Home\" }],\n  \"home\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst tabs = [\n  { id: \"home\", label: \"Home\" },\n  { id: \"settings\", label: \"Settings\" },\n];\n\nconst active =\n  tabs.find((tab) => tab.id === \"settings\")?.id ?? null;\n```\n\nStep 5 — Production example:\nKeep the Enterprise tab component logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Enterprise tab component logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---","# Module 4 – Modal & Dialog","**Questions 61–80**"],
  },
];

export const MOCK_FRONTEND_MACHINE_CODING_MODULE3_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Machine Coding',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));