// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 4 — React Testing Library).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m4-1",
    number: "TESTQA-M4-1",
    title: "RTL Philosophy and render",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**RTL Philosophy and render** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**RTL Philosophy and render** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-2",
    number: "TESTQA-M4-2",
    title: "screen and Query Model",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**screen and Query Model** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**screen and Query Model** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-3",
    number: "TESTQA-M4-3",
    title: "getByRole",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**getByRole** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**getByRole** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-4",
    number: "TESTQA-M4-4",
    title: "getByLabelText and Form Queries",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**getByLabelText and Form Queries** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**getByLabelText and Form Queries** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-5",
    number: "TESTQA-M4-5",
    title: "getByText",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**getByText** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**getByText** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-6",
    number: "TESTQA-M4-6",
    title: "findBy Queries",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**findBy Queries** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**findBy Queries** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-7",
    number: "TESTQA-M4-7",
    title: "queryBy Queries",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**queryBy Queries** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**queryBy Queries** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-8",
    number: "TESTQA-M4-8",
    title: "userEvent",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**userEvent** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**userEvent** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-9",
    number: "TESTQA-M4-9",
    title: "fireEvent",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**fireEvent** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**fireEvent** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-10",
    number: "TESTQA-M4-10",
    title: "Testing Forms",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**Testing Forms** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Testing Forms** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-11",
    number: "TESTQA-M4-11",
    title: "Testing Async UI",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**Testing Async UI** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Testing Async UI** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-12",
    number: "TESTQA-M4-12",
    title: "Router Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**Router Testing** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Router Testing** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-13",
    number: "TESTQA-M4-13",
    title: "Context Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**Context Testing** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Context Testing** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-14",
    number: "TESTQA-M4-14",
    title: "Redux Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**Redux Testing** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Redux Testing** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-15",
    number: "TESTQA-M4-15",
    title: "API Testing and RTL Best Practices",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**API Testing and RTL Best Practices** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**API Testing and RTL Best Practices** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
  {
    id: "testqa-m4-16",
    number: "TESTQA-M4-16",
    title: "React Testing Library — Module Review",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "React Testing Library",
    expectedAnswer: "**React Testing Library — Module Review** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**React Testing Library — Module Review** should verify the UI as a user or assistive technology experiences it, preferring semantic queries and realistic interaction.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface UiState { label: string; disabled: boolean; }\n\nfunction accessibleName(state: UiState): string {\n  if (state.label.trim().length === 0) return \"Unnamed control\";\n  return state.label.trim();\n}\n\nconst name = accessibleName({\n  label: \"Save\",\n  disabled: false,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { label: \"Save\", disabled: false };\n\nconst name = state.label.trim() || \"Unnamed control\";\n```\n\nStep 5 — Production example:\nUse semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Use semantic queries and realistic interactions so tests protect accessible user behavior rather than React internals.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["React Testing Library"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE4_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));