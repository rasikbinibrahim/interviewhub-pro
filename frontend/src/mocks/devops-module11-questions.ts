// Derived from frontend/src/document/DevOps_Cloud_CICD_Frontend_Infrastructure_Master_Handbook.md (Module 11 — CDN & Edge Computing).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "devops-m11-1",
    number: "DEVOPS-M11-1",
    title: "Cloudflare",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "CDN & Edge Computing",
    expectedAnswer: "Cloudflare is an edge platform that provides CDN caching, DNS, TLS, WAF, DDoS protection, and edge-compute capabilities. For frontend applications it can serve static assets close to users and execute lightweight request logic at the edge.",
    deepExplanation: "```text\nBrowser\n   ↓\nCloudflare edge\n   ├─ cache hit → response\n   ├─ Worker/WAF logic\n   └─ cache miss → origin\n```\n\nA simple Worker can add a security header:\n\n```ts\nexport default {\n  async fetch(request: Request): Promise<Response> {\n    const response = await fetch(request);\n    const headers = new Headers(response.headers);\n    headers.set(\"X-Content-Type-Options\", \"nosniff\");\n\n    return new Response(response.body, {\n      status: response.status,\n      headers,\n    });\n  },\n};\n```\n\nUse fingerprinted assets and explicit cache rules so edge caching does not serve stale or personalized data incorrectly.",
    productionExample: "A React/Vite application uses Cloudflare for DNS, TLS, WAF and CDN delivery. Fingerprinted JS/CSS assets are cached for a long time, while HTML uses a shorter cache policy. A Worker performs lightweight header or routing logic.",
    bestPractices: ["Prefer content-hashed URLs for immutable frontend assets.","Keep cache keys minimal and safe.","Use narrow invalidation rather than broad purges.","Keep edge logic small and observable.","Never cache private responses publicly."],
    tradeOffs: "Cloudflare can consolidate edge, security and delivery features, but the platform introduces provider-specific configuration and distributed debugging complexity.",
    commonMistakes: ["Caching personalized responses publicly.","Using Workers for heavy stateful work.","Exposing origin directly.","No cache-versioning strategy."],
    followUpQuestions: ["CDN vs edge compute?","How do Cloudflare Workers execute?","How would you prevent stale SPA assets?"],
    relatedTopics: ["CDN & Edge Computing","Caching","HTTP","Performance","Security","Frontend Deployment","Image Optimization"],
  },
  {
    id: "devops-m11-2",
    number: "DEVOPS-M11-2",
    title: "CloudFront",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "CDN & Edge Computing",
    expectedAnswer: "Amazon CloudFront is AWS's CDN for delivering content from edge locations and can front S3, APIs, load balancers and other origins. It provides caching, TLS, routing and edge integration through CloudFront Functions or Lambda@Edge.",
    deepExplanation: "Architecture:\n\n```text\nBrowser\n  ↓\nCloudFront\n  ├─ cache hit\n  └─ origin\n      ├─ S3\n      └─ API / ALB\n```\n\nFor immutable assets:\n\n```http\nCache-Control: public, max-age=31536000, immutable\n```\n\nAWS deployment example:\n\n```bash\naws cloudfront create-invalidation   --distribution-id \"$DIST_ID\"   --paths \"/index.html\"\n```\n\nPrefer changing asset URLs through content hashing instead of invalidating every asset on every release.",
    productionExample: "A frontend stores static assets in private S3, uses CloudFront as the public edge, and updates only the HTML entry point during releases. The same distribution provides TLS and optional WAF protection.",
    bestPractices: ["Prefer content-hashed URLs for immutable frontend assets.","Keep cache keys minimal and safe.","Use narrow invalidation rather than broad purges.","Keep edge logic small and observable.","Never cache private responses publicly."],
    tradeOffs: "CloudFront improves global delivery and integrates deeply with AWS, but cache policies, invalidation, origin access and AWS-specific configuration add complexity.",
    commonMistakes: ["Invalidating `/*` for every deploy.","Long-caching mutable HTML.","Public S3 origins without need.","Caching sensitive API responses."],
    followUpQuestions: ["CloudFront vs S3?","How do cache policies work?","How would you deploy a SPA with CloudFront?"],
    relatedTopics: ["CDN & Edge Computing","Caching","HTTP","Performance","Security","Frontend Deployment","Image Optimization"],
  },
  {
    id: "devops-m11-3",
    number: "DEVOPS-M11-3",
    title: "Cache Keys",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "CDN & Edge Computing",
    expectedAnswer: "A cache key determines which requests are considered equivalent for a CDN cache. It usually derives from the URL and may include selected query parameters, headers, cookies or other request attributes. A good cache key maximizes safe reuse without mixing responses that should differ.",
    deepExplanation: "Conceptually:\n\n```text\ncache key =\n  scheme\n  + host\n  + path\n  + selected query parameters\n  + selected headers/cookies when required\n```\n\nExample:\n\n```text\n/css/app.css?v=abc\n```\n\ncan safely use the version query only if the CDN is configured consistently.\n\nFor personalized content:\n\n```text\nCookie: session=...\n```\n\nshould generally prevent shared public caching rather than being blindly added to a shared cache key.",
    productionExample: "A static frontend excludes unnecessary headers and cookies from asset cache keys while versioned filenames guarantee content identity. API responses use separate cache rules so authorization/session data cannot accidentally collide.",
    bestPractices: ["Prefer content-hashed URLs for immutable frontend assets.","Keep cache keys minimal and safe.","Use narrow invalidation rather than broad purges.","Keep edge logic small and observable.","Never cache private responses publicly."],
    tradeOffs: "More key dimensions improve correctness for varying responses but reduce cache-hit ratio and increase cache fragmentation.",
    commonMistakes: ["Ignoring a query/header that changes content.","Putting every cookie into the key.","Using user identity as a shared cache dimension.","No distinction between static and personalized content."],
    followUpQuestions: ["What makes a good CDN cache key?","How do query parameters affect caching?","When should `Vary` be used?"],
    relatedTopics: ["CDN & Edge Computing","Caching","HTTP","Performance","Security","Frontend Deployment","Image Optimization"],
  },
  {
    id: "devops-m11-4",
    number: "DEVOPS-M11-4",
    title: "Cache Invalidation",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "CDN & Edge Computing",
    expectedAnswer: "Cache invalidation removes or bypasses cached objects when their current representation is no longer valid. For frontend deployments, the safest strategy is usually immutable content-hashed assets plus short-lived HTML rather than frequent global purges.",
    deepExplanation: "Preferred frontend release pattern:\n\n```text\napp.8f31c.js  → max-age=31536000, immutable\napp.4a91d.css → max-age=31536000, immutable\nindex.html    → short TTL / selective purge\n```\n\nWhen an urgent HTML purge is needed:\n\n```bash\naws cloudfront create-invalidation   --distribution-id \"$DIST_ID\"   --paths \"/index.html\"\n```\n\nThe same principle applies to other CDNs: invalidate the smallest necessary scope.",
    productionExample: "A deployment uploads new fingerprinted assets and updates `index.html`. Because asset URLs changed, browsers and CDNs do not need a global purge; only the HTML entry point is refreshed.",
    bestPractices: ["Prefer content-hashed URLs for immutable frontend assets.","Keep cache keys minimal and safe.","Use narrow invalidation rather than broad purges.","Keep edge logic small and observable.","Never cache private responses publicly."],
    tradeOffs: "Purges provide immediate freshness but can be slow/costly and reduce cache efficiency. Versioned content avoids most purge work but requires disciplined asset naming.",
    commonMistakes: ["Purging every path on every deploy.","Ignoring browser cache after a CDN purge.","Long TTL for mutable HTML.","Deleting old assets before all clients stop requesting them."],
    followUpQuestions: ["Browser cache vs CDN cache?","How do hashed filenames eliminate invalidation?","How would you safely roll back a frontend release?"],
    relatedTopics: ["CDN & Edge Computing","Caching","HTTP","Performance","Security","Frontend Deployment","Image Optimization"],
  },
  {
    id: "devops-m11-5",
    number: "DEVOPS-M11-5",
    title: "Compression",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "CDN & Edge Computing",
    expectedAnswer: "Compression reduces transferred bytes between origin/edge and clients. For modern web delivery, text assets such as HTML, CSS, JS, SVG and JSON commonly use Brotli or gzip, while already-compressed formats such as JPEG/PNG/AVIF/WebP have different behavior.",
    deepExplanation: "Example response negotiation:\n\n```http\nAccept-Encoding: br, gzip\n```\n\nA successful compressed response can include:\n\n```http\nContent-Encoding: br\nVary: Accept-Encoding\n```\n\nBuild-time minification reduces source size first:\n\n```bash\nnpm run build\n```\n\nThen the CDN/web server can apply transport compression to the generated text assets.",
    productionExample: "A CDN serves Brotli-compressed JavaScript/CSS to browsers that support it and gzip as a fallback. HTML is compressed as well, while already-compressed images are optimized through format/quality selection rather than repeated gzip compression.",
    bestPractices: ["Prefer content-hashed URLs for immutable frontend assets.","Keep cache keys minimal and safe.","Use narrow invalidation rather than broad purges.","Keep edge logic small and observable.","Never cache private responses publicly."],
    tradeOffs: "Compression saves bandwidth and improves transfer latency but consumes CPU and can reduce benefits when payloads are already compressed.",
    commonMistakes: ["Compressing JPEG/WebP repeatedly.","Missing `Vary: Accept-Encoding`.","Measuring only raw bundle size.","Using expensive compression levels without performance benefit."],
    followUpQuestions: ["Brotli vs gzip?","What should a CDN compress?","How do compression and minification differ?"],
    relatedTopics: ["CDN & Edge Computing","Caching","HTTP","Performance","Security","Frontend Deployment","Image Optimization"],
  },
  {
    id: "devops-m11-6",
    number: "DEVOPS-M11-6",
    title: "HTTP/3",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "CDN & Edge Computing",
    expectedAnswer: "HTTP/3 is HTTP transported over QUIC, which runs over UDP. QUIC provides encrypted transport, stream multiplexing and connection migration properties that can improve behavior on lossy or changing networks compared with traditional TCP-based HTTP/2.",
    deepExplanation: "Typical delivery:\n\n```text\nBrowser\n  ↕\nHTTP/3 / QUIC\n  ↕\nCDN edge\n  ↕\nOrigin\n```\n\nA browser may advertise support through:\n\n```http\nAlt-Svc: h3=\":443\"\n```\n\nThe CDN and origin must support HTTP/3 for the client to negotiate it. Application code normally does not need to know whether the connection uses HTTP/2 or HTTP/3.",
    productionExample: "A global frontend enables HTTP/3 at the CDN edge. Mobile users switching between networks benefit from QUIC connection behavior, while the same static asset URLs and application protocol continue to work.",
    bestPractices: ["Prefer content-hashed URLs for immutable frontend assets.","Keep cache keys minimal and safe.","Use narrow invalidation rather than broad purges.","Keep edge logic small and observable.","Never cache private responses publicly."],
    tradeOffs: "HTTP/3 can improve transport behavior, but benefits vary by network and browser. Supporting multiple protocol versions adds operational testing requirements.",
    commonMistakes: ["Assuming HTTP/3 automatically makes every page faster.","Removing HTTP/2 fallback.","Confusing HTTP/3 with a new application API.","Ignoring CDN/browser support."],
    followUpQuestions: ["HTTP/2 vs HTTP/3?","What is QUIC?","How does connection migration help mobile clients?"],
    relatedTopics: ["CDN & Edge Computing","Caching","HTTP","Performance","Security","Frontend Deployment","Image Optimization"],
  },
  {
    id: "devops-m11-7",
    number: "DEVOPS-M11-7",
    title: "Edge Computing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "CDN & Edge Computing",
    expectedAnswer: "Edge computing executes supported logic closer to users at distributed edge locations. It is useful for lightweight request manipulation, personalization decisions, localization, authentication checks, redirects and cache-aware logic where moving computation closer reduces latency.",
    deepExplanation: "Conceptual flow:\n\n```text\nUser\n  ↓\nnearest edge\n  ├─ cached response\n  ├─ edge function\n  └─ origin request\n```\n\nExample edge middleware:\n\n```ts\nexport default async function middleware(request: Request) {\n  const url = new URL(request.url);\n\n  if (url.pathname === \"/old\") {\n    return Response.redirect(new URL(\"/new\", url), 308);\n  }\n\n  return fetch(request);\n}\n```\n\nThe exact runtime may restrict Node APIs, filesystem access, execution time and package size.",
    productionExample: "A global site executes locale routing at the edge while static assets are served from cache. Heavy database operations remain in regional services to avoid turning every edge request into a remote stateful call.",
    bestPractices: ["Prefer content-hashed URLs for immutable frontend assets.","Keep cache keys minimal and safe.","Use narrow invalidation rather than broad purges.","Keep edge logic small and observable.","Never cache private responses publicly."],
    tradeOffs: "Edge execution can reduce latency but increases distributed debugging, cache-consistency and runtime-management complexity.",
    commonMistakes: ["Treating edge runtime as full Node.js.","Performing expensive database queries at every edge.","Caching private responses.","Moving business logic to the edge without a measurable reason."],
    followUpQuestions: ["Edge compute vs CDN?","Which frontend logic belongs at the edge?","How do edge runtime constraints affect dependencies?"],
    relatedTopics: ["CDN & Edge Computing","Caching","HTTP","Performance","Security","Frontend Deployment","Image Optimization"],
  },
  {
    id: "devops-m11-8",
    number: "DEVOPS-M11-8",
    title: "Lambda@Edge",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "CDN & Edge Computing",
    expectedAnswer: "Lambda@Edge lets AWS Lambda functions run in association with CloudFront events, allowing request/response logic near CloudFront edge locations. It is useful for lightweight transformations, routing and authorization-related edge behavior that fits the Lambda@Edge execution model.",
    deepExplanation: "Example event-oriented handler shape:\n\n```ts\nexport const handler = async (event: any) => {\n  const request = event.Records[0].cf.request;\n\n  if (request.uri === \"/old\") {\n    request.uri = \"/new\";\n  }\n\n  return request;\n};\n```\n\nThe actual supported event phases, deployment model, runtime limits and regional requirements must be respected. Use CloudFront Functions when the simpler function model is sufficient.",
    productionExample: "A CloudFront distribution uses edge logic for a small URL rewrite while the main application remains a normal S3/API origin. This keeps the edge function small and reduces origin round trips.",
    bestPractices: ["Prefer content-hashed URLs for immutable frontend assets.","Keep cache keys minimal and safe.","Use narrow invalidation rather than broad purges.","Keep edge logic small and observable.","Never cache private responses publicly."],
    tradeOffs: "Lambda@Edge enables richer edge logic than simple CDN configuration but has a more complex deployment and runtime model than standard Lambda.",
    commonMistakes: ["Trying to run full application servers at the edge.","Ignoring Lambda@Edge event constraints.","Using edge code for work better handled by the origin.","No rollback/version strategy."],
    followUpQuestions: ["Lambda@Edge vs CloudFront Functions?","Which CloudFront event should a function handle?","What are the limitations of edge execution?"],
    relatedTopics: ["CDN & Edge Computing","Caching","HTTP","Performance","Security","Frontend Deployment","Image Optimization"],
  },
  {
    id: "devops-m11-9",
    number: "DEVOPS-M11-9",
    title: "Cloudflare Workers",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "CDN & Edge Computing",
    expectedAnswer: "Cloudflare Workers are JavaScript/TypeScript edge functions that execute on Cloudflare's network. They can intercept requests, modify responses, implement APIs, access platform bindings and coordinate with edge caching.",
    deepExplanation: "Example Worker:\n\n```ts\nexport default {\n  async fetch(request: Request): Promise<Response> {\n    const url = new URL(request.url);\n\n    if (url.pathname.startsWith(\"/api/\")) {\n      return Response.json({ edge: true });\n    }\n\n    return fetch(request);\n  },\n};\n```\n\nA Worker can also add a cache policy:\n\n```ts\nconst response = await fetch(request);\nconst headers = new Headers(response.headers);\nheaders.set(\"Cache-Control\", \"public, max-age=60\");\n\nreturn new Response(response.body, {\n  status: response.status,\n  headers,\n});\n```",
    productionExample: "A frontend uses Workers for lightweight routing and API aggregation while Cloudflare's CDN serves static assets. Secrets are stored in server-side bindings and never exposed through the browser bundle.",
    bestPractices: ["Prefer content-hashed URLs for immutable frontend assets.","Keep cache keys minimal and safe.","Use narrow invalidation rather than broad purges.","Keep edge logic small and observable.","Never cache private responses publicly."],
    tradeOffs: "Workers provide flexible edge programming with low operational overhead, but the runtime differs from a full Node environment and introduces platform-specific architecture.",
    commonMistakes: ["Using Node-only APIs blindly.","Putting secrets in client code.","Doing heavy stateful processing at the edge.","Caching responses without checking privacy."],
    followUpQuestions: ["Workers vs Lambda@Edge?","How do Workers access secrets?","How would you cache an API response safely?"],
    relatedTopics: ["CDN & Edge Computing","Caching","HTTP","Performance","Security","Frontend Deployment","Image Optimization"],
  },
  {
    id: "devops-m11-10",
    number: "DEVOPS-M11-10",
    title: "Vercel Edge",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "CDN & Edge Computing",
    expectedAnswer: "Vercel Edge refers to Vercel's edge execution and delivery model for supported framework routes and middleware. It enables lightweight request logic to run near users while integrating with Vercel's deployment and caching model.",
    deepExplanation: "Example middleware:\n\n```ts\nimport { NextResponse } from \"next/server\";\n\nexport function middleware(request: Request) {\n  const url = new URL(request.url);\n\n  if (url.pathname === \"/legacy\") {\n    return NextResponse.redirect(\n      new URL(\"/new\", url),\n      308,\n    );\n  }\n\n  return NextResponse.next();\n}\n```\n\nEdge-compatible code must stay within the APIs and runtime model supported by the selected Vercel deployment target.",
    productionExample: "A Next.js application uses middleware for lightweight locale routing and security headers at the edge while heavier data operations remain in server-side regional functions or services.",
    bestPractices: ["Prefer content-hashed URLs for immutable frontend assets.","Keep cache keys minimal and safe.","Use narrow invalidation rather than broad purges.","Keep edge logic small and observable.","Never cache private responses publicly."],
    tradeOffs: "Edge execution reduces latency for suitable logic but adds runtime restrictions and cache/deployment semantics that differ from a normal Node server.",
    commonMistakes: ["Assuming every npm package works at the edge.","Doing expensive database queries in middleware.","Caching user-specific responses incorrectly."],
    followUpQuestions: ["Vercel Edge vs Node runtime?","What belongs in middleware?","How do Vercel cache and edge execution interact?"],
    relatedTopics: ["CDN & Edge Computing","Caching","HTTP","Performance","Security","Frontend Deployment","Image Optimization"],
  },
  {
    id: "devops-m11-11",
    number: "DEVOPS-M11-11",
    title: "Image Optimization",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "CDN & Edge Computing",
    expectedAnswer: "Image optimization reduces image transfer size and rendering cost while preserving appropriate visual quality. It includes choosing efficient formats, responsive dimensions, compression, lazy loading, correct intrinsic dimensions and CDN/edge delivery.",
    deepExplanation: "Responsive image example:\n\n```html\n<img\n  src=\"/hero-800.webp\"\n  srcset=\"\n    /hero-400.webp 400w,\n    /hero-800.webp 800w,\n    /hero-1600.webp 1600w\n  \"\n  sizes=\"(max-width: 768px) 100vw, 50vw\"\n  width=\"1600\"\n  height=\"900\"\n  loading=\"lazy\"\n  decoding=\"async\"\n  alt=\"Product hero\"\n/>\n```\n\nUse AVIF/WebP where browser support and visual results justify them. Give images width/height or an aspect ratio so layout space is reserved before the image loads.",
    productionExample: "An ecommerce frontend stores responsive image variants at the CDN edge. Product cards request a width close to the rendered size, while above-the-fold hero media is prioritized and below-the-fold images are lazy-loaded.",
    bestPractices: ["Prefer content-hashed URLs for immutable frontend assets.","Keep cache keys minimal and safe.","Use narrow invalidation rather than broad purges.","Keep edge logic small and observable.","Never cache private responses publicly."],
    tradeOffs: "Smaller images improve network/render performance but image processing, variant storage and edge transformations add build or runtime complexity.",
    commonMistakes: ["Serving a 4K image to a small card.","Lazy-loading the LCP image.","No width/height or aspect ratio.","Using quality settings without measuring visual impact."],
    followUpQuestions: ["How does `srcset` choose an image?","What should be lazy-loaded?","How does image optimization affect LCP?"],
    relatedTopics: ["CDN & Edge Computing","Caching","HTTP","Performance","Security","Frontend Deployment","Image Optimization"],
  },

];

export const MOCK_DEVOPS_MODULE11_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'DevOps/Cloud',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));