// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 16 — Security Testing).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m16-256",
    number: "SEC-M16-256",
    title: "SAST",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**SAST** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**SAST** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Finding {\n  severity: \"low\" | \"medium\" | \"high\";\n  resolved: boolean;\n}\n\nfunction releaseAllowed(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (\n      findings[i].severity === \"high\" &&\n      !findings[i].resolved\n    ) {\n      return false;\n    }\n  }\n\n  return true;\n}\n\nconst allowed = releaseAllowed([\n  { severity: \"high\", resolved: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst findings = [\n  { severity: \"high\" as const, resolved: true },\n  { severity: \"medium\" as const, resolved: false },\n];\n\nconst allowed = findings.every(\n  (finding) =>\n    finding.severity !== \"high\" ||\n    finding.resolved,\n);\n```\n\nStep 5 — Production example:\nUse SAST as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use SAST as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-257",
    number: "SEC-M16-257",
    title: "DAST",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**DAST** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**DAST** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Finding {\n  severity: \"low\" | \"medium\" | \"high\";\n  resolved: boolean;\n}\n\nfunction releaseAllowed(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (\n      findings[i].severity === \"high\" &&\n      !findings[i].resolved\n    ) {\n      return false;\n    }\n  }\n\n  return true;\n}\n\nconst allowed = releaseAllowed([\n  { severity: \"high\", resolved: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst findings = [\n  { severity: \"high\" as const, resolved: true },\n  { severity: \"medium\" as const, resolved: false },\n];\n\nconst allowed = findings.every(\n  (finding) =>\n    finding.severity !== \"high\" ||\n    finding.resolved,\n);\n```\n\nStep 5 — Production example:\nUse DAST as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use DAST as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-258",
    number: "SEC-M16-258",
    title: "Dependency scanning",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**Dependency scanning** is a frontend supply-chain security concern. The goal is to reduce the chance that an untrusted or compromised dependency, script, package, or build artifact becomes executable code in the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Dependency scanning** is a frontend supply-chain security concern. The goal is to reduce the chance that an untrusted or compromised dependency, script, package, or build artifact becomes executable code in the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Dependency {\n  name: string;\n  version: string;\n  approved: boolean;\n}\n\nfunction approvedDependencies(\n  dependencies: readonly Dependency[],\n): Dependency[] {\n  const result: Dependency[] = [];\n\n  for (let i = 0; i < dependencies.length; i += 1) {\n    if (dependencies[i].approved) {\n      result.push(dependencies[i]);\n    }\n  }\n\n  return result;\n}\n\nconst approved = approvedDependencies([\n  { name: \"react\", version: \"19.0.0\", approved: true },\n  { name: \"demo-package\", version: \"1.0.0\", approved: false },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst dependencies = [\n  { name: \"react\", version: \"19.0.0\", approved: true },\n  { name: \"demo-package\", version: \"1.0.0\", approved: false },\n];\n\nconst approved = dependencies.filter(\n  (dependency) => dependency.approved,\n);\n```\n\nStep 5 — Production example:\nUse Dependency scanning in the dependency/build pipeline. Approve packages explicitly, preserve lockfile/provenance evidence, and block known high-risk artifacts before release.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Dependency scanning in the dependency/build pipeline. Approve packages explicitly, preserve lockfile/provenance evidence, and block known high-risk artifacts before release.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-259",
    number: "SEC-M16-259",
    title: "OWASP ZAP",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**OWASP ZAP** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**OWASP ZAP** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Finding {\n  severity: \"low\" | \"medium\" | \"high\";\n  resolved: boolean;\n}\n\nfunction releaseAllowed(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (\n      findings[i].severity === \"high\" &&\n      !findings[i].resolved\n    ) {\n      return false;\n    }\n  }\n\n  return true;\n}\n\nconst allowed = releaseAllowed([\n  { severity: \"high\", resolved: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst findings = [\n  { severity: \"high\" as const, resolved: true },\n  { severity: \"medium\" as const, resolved: false },\n];\n\nconst allowed = findings.every(\n  (finding) =>\n    finding.severity !== \"high\" ||\n    finding.resolved,\n);\n```\n\nStep 5 — Production example:\nUse OWASP ZAP as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use OWASP ZAP as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-260",
    number: "SEC-M16-260",
    title: "Burp Suite",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**Burp Suite** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Burp Suite** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Finding {\n  severity: \"low\" | \"medium\" | \"high\";\n  resolved: boolean;\n}\n\nfunction releaseAllowed(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (\n      findings[i].severity === \"high\" &&\n      !findings[i].resolved\n    ) {\n      return false;\n    }\n  }\n\n  return true;\n}\n\nconst allowed = releaseAllowed([\n  { severity: \"high\", resolved: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst findings = [\n  { severity: \"high\" as const, resolved: true },\n  { severity: \"medium\" as const, resolved: false },\n];\n\nconst allowed = findings.every(\n  (finding) =>\n    finding.severity !== \"high\" ||\n    finding.resolved,\n);\n```\n\nStep 5 — Production example:\nUse Burp Suite as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Burp Suite as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-261",
    number: "SEC-M16-261",
    title: "CSP Evaluator",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**CSP Evaluator** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CSP Evaluator** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Finding {\n  severity: \"low\" | \"medium\" | \"high\";\n  resolved: boolean;\n}\n\nfunction releaseAllowed(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (\n      findings[i].severity === \"high\" &&\n      !findings[i].resolved\n    ) {\n      return false;\n    }\n  }\n\n  return true;\n}\n\nconst allowed = releaseAllowed([\n  { severity: \"high\", resolved: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst findings = [\n  { severity: \"high\" as const, resolved: true },\n  { severity: \"medium\" as const, resolved: false },\n];\n\nconst allowed = findings.every(\n  (finding) =>\n    finding.severity !== \"high\" ||\n    finding.resolved,\n);\n```\n\nStep 5 — Production example:\nUse CSP Evaluator as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use CSP Evaluator as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-262",
    number: "SEC-M16-262",
    title: "Lighthouse security",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**Lighthouse security** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Lighthouse security** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Finding {\n  severity: \"low\" | \"medium\" | \"high\";\n  resolved: boolean;\n}\n\nfunction releaseAllowed(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (\n      findings[i].severity === \"high\" &&\n      !findings[i].resolved\n    ) {\n      return false;\n    }\n  }\n\n  return true;\n}\n\nconst allowed = releaseAllowed([\n  { severity: \"high\", resolved: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst findings = [\n  { severity: \"high\" as const, resolved: true },\n  { severity: \"medium\" as const, resolved: false },\n];\n\nconst allowed = findings.every(\n  (finding) =>\n    finding.severity !== \"high\" ||\n    finding.resolved,\n);\n```\n\nStep 5 — Production example:\nUse Lighthouse security as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Lighthouse security as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-263",
    number: "SEC-M16-263",
    title: "Browser DevTools",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**Browser DevTools** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Browser DevTools** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Finding {\n  severity: \"low\" | \"medium\" | \"high\";\n  resolved: boolean;\n}\n\nfunction releaseAllowed(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (\n      findings[i].severity === \"high\" &&\n      !findings[i].resolved\n    ) {\n      return false;\n    }\n  }\n\n  return true;\n}\n\nconst allowed = releaseAllowed([\n  { severity: \"high\", resolved: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst findings = [\n  { severity: \"high\" as const, resolved: true },\n  { severity: \"medium\" as const, resolved: false },\n];\n\nconst allowed = findings.every(\n  (finding) =>\n    finding.severity !== \"high\" ||\n    finding.resolved,\n);\n```\n\nStep 5 — Production example:\nUse Browser DevTools as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Browser DevTools as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-264",
    number: "SEC-M16-264",
    title: "Security unit tests",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**Security unit tests** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Security unit tests** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Finding {\n  severity: \"low\" | \"medium\" | \"high\";\n  resolved: boolean;\n}\n\nfunction releaseAllowed(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (\n      findings[i].severity === \"high\" &&\n      !findings[i].resolved\n    ) {\n      return false;\n    }\n  }\n\n  return true;\n}\n\nconst allowed = releaseAllowed([\n  { severity: \"high\", resolved: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst findings = [\n  { severity: \"high\" as const, resolved: true },\n  { severity: \"medium\" as const, resolved: false },\n];\n\nconst allowed = findings.every(\n  (finding) =>\n    finding.severity !== \"high\" ||\n    finding.resolved,\n);\n```\n\nStep 5 — Production example:\nUse Security unit tests as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Security unit tests as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-265",
    number: "SEC-M16-265",
    title: "Security integration tests",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**Security integration tests** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Security integration tests** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Finding {\n  severity: \"low\" | \"medium\" | \"high\";\n  resolved: boolean;\n}\n\nfunction releaseAllowed(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (\n      findings[i].severity === \"high\" &&\n      !findings[i].resolved\n    ) {\n      return false;\n    }\n  }\n\n  return true;\n}\n\nconst allowed = releaseAllowed([\n  { severity: \"high\", resolved: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst findings = [\n  { severity: \"high\" as const, resolved: true },\n  { severity: \"medium\" as const, resolved: false },\n];\n\nconst allowed = findings.every(\n  (finding) =>\n    finding.severity !== \"high\" ||\n    finding.resolved,\n);\n```\n\nStep 5 — Production example:\nUse Security integration tests as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Security integration tests as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-266",
    number: "SEC-M16-266",
    title: "E2E security tests",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**E2E security tests** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**E2E security tests** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Finding {\n  severity: \"low\" | \"medium\" | \"high\";\n  resolved: boolean;\n}\n\nfunction releaseAllowed(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (\n      findings[i].severity === \"high\" &&\n      !findings[i].resolved\n    ) {\n      return false;\n    }\n  }\n\n  return true;\n}\n\nconst allowed = releaseAllowed([\n  { severity: \"high\", resolved: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst findings = [\n  { severity: \"high\" as const, resolved: true },\n  { severity: \"medium\" as const, resolved: false },\n];\n\nconst allowed = findings.every(\n  (finding) =>\n    finding.severity !== \"high\" ||\n    finding.resolved,\n);\n```\n\nStep 5 — Production example:\nUse E2E security tests as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use E2E security tests as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-267",
    number: "SEC-M16-267",
    title: "API security tests",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**API security tests** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**API security tests** is a security-testing topic. Explain what class of weakness the technique detects, where in the pipeline it runs, its blind spots, and how findings become actionable engineering work.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Finding {\n  severity: \"low\" | \"medium\" | \"high\";\n  resolved: boolean;\n}\n\nfunction releaseAllowed(\n  findings: readonly Finding[],\n): boolean {\n  for (let i = 0; i < findings.length; i += 1) {\n    if (\n      findings[i].severity === \"high\" &&\n      !findings[i].resolved\n    ) {\n      return false;\n    }\n  }\n\n  return true;\n}\n\nconst allowed = releaseAllowed([\n  { severity: \"high\", resolved: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst findings = [\n  { severity: \"high\" as const, resolved: true },\n  { severity: \"medium\" as const, resolved: false },\n];\n\nconst allowed = findings.every(\n  (finding) =>\n    finding.severity !== \"high\" ||\n    finding.resolved,\n);\n```\n\nStep 5 — Production example:\nUse API security tests as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use API security tests as part of a layered security pipeline. Define what it catches, when it runs, its blind spots, and how findings are triaged.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-268",
    number: "SEC-M16-268",
    title: "Secret scanning",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**Secret scanning** is a frontend supply-chain security concern. The goal is to reduce the chance that an untrusted or compromised dependency, script, package, or build artifact becomes executable code in the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Secret scanning** is a frontend supply-chain security concern. The goal is to reduce the chance that an untrusted or compromised dependency, script, package, or build artifact becomes executable code in the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Dependency {\n  name: string;\n  version: string;\n  approved: boolean;\n}\n\nfunction approvedDependencies(\n  dependencies: readonly Dependency[],\n): Dependency[] {\n  const result: Dependency[] = [];\n\n  for (let i = 0; i < dependencies.length; i += 1) {\n    if (dependencies[i].approved) {\n      result.push(dependencies[i]);\n    }\n  }\n\n  return result;\n}\n\nconst approved = approvedDependencies([\n  { name: \"react\", version: \"19.0.0\", approved: true },\n  { name: \"demo-package\", version: \"1.0.0\", approved: false },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst dependencies = [\n  { name: \"react\", version: \"19.0.0\", approved: true },\n  { name: \"demo-package\", version: \"1.0.0\", approved: false },\n];\n\nconst approved = dependencies.filter(\n  (dependency) => dependency.approved,\n);\n```\n\nStep 5 — Production example:\nUse Secret scanning in the dependency/build pipeline. Approve packages explicitly, preserve lockfile/provenance evidence, and block known high-risk artifacts before release.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Secret scanning in the dependency/build pipeline. Approve packages explicitly, preserve lockfile/provenance evidence, and block known high-risk artifacts before release.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-269",
    number: "SEC-M16-269",
    title: "CodeQL",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**CodeQL** is a frontend supply-chain security concern. The goal is to reduce the chance that an untrusted or compromised dependency, script, package, or build artifact becomes executable code in the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CodeQL** is a frontend supply-chain security concern. The goal is to reduce the chance that an untrusted or compromised dependency, script, package, or build artifact becomes executable code in the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Dependency {\n  name: string;\n  version: string;\n  approved: boolean;\n}\n\nfunction approvedDependencies(\n  dependencies: readonly Dependency[],\n): Dependency[] {\n  const result: Dependency[] = [];\n\n  for (let i = 0; i < dependencies.length; i += 1) {\n    if (dependencies[i].approved) {\n      result.push(dependencies[i]);\n    }\n  }\n\n  return result;\n}\n\nconst approved = approvedDependencies([\n  { name: \"react\", version: \"19.0.0\", approved: true },\n  { name: \"demo-package\", version: \"1.0.0\", approved: false },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst dependencies = [\n  { name: \"react\", version: \"19.0.0\", approved: true },\n  { name: \"demo-package\", version: \"1.0.0\", approved: false },\n];\n\nconst approved = dependencies.filter(\n  (dependency) => dependency.approved,\n);\n```\n\nStep 5 — Production example:\nUse CodeQL in the dependency/build pipeline. Approve packages explicitly, preserve lockfile/provenance evidence, and block known high-risk artifacts before release.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use CodeQL in the dependency/build pipeline. Approve packages explicitly, preserve lockfile/provenance evidence, and block known high-risk artifacts before release.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m16-270",
    number: "SEC-M16-270",
    title: "SCA in CI",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Testing",
    expectedAnswer: "**SCA in CI** is a frontend supply-chain security concern. The goal is to reduce the chance that an untrusted or compromised dependency, script, package, or build artifact becomes executable code in the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**SCA in CI** is a frontend supply-chain security concern. The goal is to reduce the chance that an untrusted or compromised dependency, script, package, or build artifact becomes executable code in the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface Dependency {\n  name: string;\n  version: string;\n  approved: boolean;\n}\n\nfunction approvedDependencies(\n  dependencies: readonly Dependency[],\n): Dependency[] {\n  const result: Dependency[] = [];\n\n  for (let i = 0; i < dependencies.length; i += 1) {\n    if (dependencies[i].approved) {\n      result.push(dependencies[i]);\n    }\n  }\n\n  return result;\n}\n\nconst approved = approvedDependencies([\n  { name: \"react\", version: \"19.0.0\", approved: true },\n  { name: \"demo-package\", version: \"1.0.0\", approved: false },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst dependencies = [\n  { name: \"react\", version: \"19.0.0\", approved: true },\n  { name: \"demo-package\", version: \"1.0.0\", approved: false },\n];\n\nconst approved = dependencies.filter(\n  (dependency) => dependency.approved,\n);\n```\n\nStep 5 — Production example:\nUse SCA in CI in the dependency/build pipeline. Approve packages explicitly, preserve lockfile/provenance evidence, and block known high-risk artifacts before release.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use SCA in CI in the dependency/build pipeline. Approve packages explicitly, preserve lockfile/provenance evidence, and block known high-risk artifacts before release.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE16_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));