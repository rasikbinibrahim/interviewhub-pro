// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 3 — Authorization).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m3-41",
    number: "SEC-M3-41",
    title: "RBAC",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**RBAC** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-42",
    number: "SEC-M3-42",
    title: "ABAC",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**ABAC** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-43",
    number: "SEC-M3-43",
    title: "PBAC",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**PBAC** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-44",
    number: "SEC-M3-44",
    title: "Claims",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**Claims** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-45",
    number: "SEC-M3-45",
    title: "Scopes",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**Scopes** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-46",
    number: "SEC-M3-46",
    title: "Permissions",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**Permissions** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-47",
    number: "SEC-M3-47",
    title: "Role hierarchy",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**Role hierarchy** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-48",
    number: "SEC-M3-48",
    title: "Protected routes",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**Protected routes** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-49",
    number: "SEC-M3-49",
    title: "Feature authorization",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**Feature authorization** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-50",
    number: "SEC-M3-50",
    title: "Dynamic permissions",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**Dynamic permissions** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-51",
    number: "SEC-M3-51",
    title: "Server-side authorization",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**Server-side authorization** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-52",
    number: "SEC-M3-52",
    title: "Frontend authorization boundaries",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**Frontend authorization boundaries** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-53",
    number: "SEC-M3-53",
    title: "IDOR prevention",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**IDOR prevention** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-54",
    number: "SEC-M3-54",
    title: "Least privilege",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**Least privilege** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m3-55",
    number: "SEC-M3-55",
    title: "Authorization architecture",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authorization",
    expectedAnswer: "**Authorization architecture** is a frontend security concern that must be evaluated across the browser, application, identity layer, API boundary, dependencies and production operations.",
    deepExplanation: "Example:\n\n```ts\n// Secure-by-default API wrapper: never interpolate secrets into URLs.\nexport async function getJson<T>(url: string): Promise<T> {\n  const response = await fetch(url, {\n    credentials: \"same-origin\",\n    headers: { Accept: \"application/json\" },\n  });\n\n  if (!response.ok) throw new Error(`HTTP ${response.status}`);\n  return response.json() as Promise<T>;\n}\n```\n\nProduction pattern:\n\n```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```\n\nWhy it matters: Prevents account compromise, data exposure and unauthorized actions. Reduces exploitable browser and supply-chain attack surface. Makes security controls reviewable, testable and observable.",
    productionExample: "```ts\n// Validate security-sensitive state at the boundary.\nfunction isAllowedOrigin(origin: string, allowed: readonly string[]) {\n  return allowed.includes(origin);\n}\n\nconsole.log(isAllowedOrigin(\"https://app.example.com\", [\n  \"https://app.example.com\",\n]));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Reduced attack surface; Better separation of trust boundaries; Easier security testing; Stronger enterprise governance.",
    commonMistakes: ["Confusing CORS with authentication","Confusing encoding with authorization","Logging tokens or passwords","Assuming HTTPS prevents XSS","Treating JWT signing as permission enforcement"],
    followUpQuestions: ["What is the attacker capability?","What is the trust boundary?","What browser control can reduce the attack surface?","What must be enforced server-side?","How would you test the defense?","What telemetry would detect recurrence?","What is the migration strategy for legacy clients?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE3_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
