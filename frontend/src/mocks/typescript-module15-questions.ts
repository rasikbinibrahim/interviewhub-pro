// Updated TypeScript interview question bank.
// Source metadata preserved; repeated generic examples normalized into topic-specific coding examples.
import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "ts-m15-226",
    number: "TS-M15-226",
    title: "tsconfig.json",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Configuration",
    expectedAnswer: "tsconfig.json configures TypeScript project boundaries, compiler options, module resolution, emitted output, and included files.",
    deepExplanation: "Step 1 — Understand the topic.\ntsconfig.json configures TypeScript project boundaries, compiler options, module resolution, emitted output, and included files.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Config = {\n  strict: boolean;\n  noEmit: boolean;\n};\n\nconst config: Config = {\n  strict: true,\n  noEmit: true,\n};\n\nconsole.log(config);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\n{\n  \"compilerOptions\": {\n    \"strict\": true,\n    \"noEmit\": true,\n    \"target\": \"ES2021\",\n    \"module\": \"ESNext\"\n  }\n}\n```\n\nStep 5 — Example result:\n```text\n{ strict: true, noEmit: true }\n```\n\nStep 6 — Complexity / trade-off.\nKeep configuration explicit and checked into version control.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **tsconfig.json**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **tsconfig.json**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m15-227",
    number: "TS-M15-227",
    title: "strict",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Configuration",
    expectedAnswer: "strict enables a family of stricter type checks, improving null safety and reducing unsound assumptions.",
    deepExplanation: "Step 1 — Understand the topic.\nstrict enables a family of stricter type checks, improving null safety and reducing unsound assumptions.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction greet(\n  name: string | undefined,\n): string {\n  if (name === undefined) {\n    return \"Guest\";\n  }\n\n  return name;\n}\n\nconsole.log(greet(undefined));\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\n{\n  \"compilerOptions\": {\n    \"strict\": true\n  }\n}\n```\n\nStep 5 — Example result:\n```text\nGuest\n```\n\nStep 6 — Complexity / trade-off.\nUse strict mode for new production projects unless a migration plan requires staged adoption.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **strict**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **strict**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m15-228",
    number: "TS-M15-228",
    title: "noImplicitAny",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Configuration",
    expectedAnswer: "noImplicitAny should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nnoImplicitAny should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **noImplicitAny**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **noImplicitAny**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m15-229",
    number: "TS-M15-229",
    title: "strictNullChecks",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Configuration",
    expectedAnswer: "With `strictNullChecks`, `null` and `undefined` are distinct types, forcing code to prove a value is present before using it as a non-nullable value.",
    deepExplanation: "Step 1 — Understand the topic.\nstrictNullChecks should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **strictNullChecks**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **strictNullChecks**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m15-230",
    number: "TS-M15-230",
    title: "noUncheckedIndexedAccess",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Configuration",
    expectedAnswer: "noUncheckedIndexedAccess should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nnoUncheckedIndexedAccess should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **noUncheckedIndexedAccess**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **noUncheckedIndexedAccess**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m15-231",
    number: "TS-M15-231",
    title: "exactOptionalPropertyTypes",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Configuration",
    expectedAnswer: "exactOptionalPropertyTypes should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nexactOptionalPropertyTypes should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **exactOptionalPropertyTypes**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **exactOptionalPropertyTypes**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m15-232",
    number: "TS-M15-232",
    title: "paths",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Configuration",
    expectedAnswer: "paths and baseUrl-style settings can define compile-time import aliases, but runtime/bundler configuration must also understand the same aliases.",
    deepExplanation: "Step 1 — Understand the topic.\npaths and baseUrl-style settings can define compile-time import aliases, but runtime/bundler configuration must also understand the same aliases.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\n// compile-time alias concept\nimport {\n  UserService,\n} from \"@app/services/user\";\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\n{\n  \"compilerOptions\": {\n    \"paths\": {\n      \"@app/*\": [\"src/*\"]\n    }\n  }\n}\n```\n\nStep 5 — Example result:\n```text\n@app/services/user\n```\n\nStep 6 — Complexity / trade-off.\nConfigure aliases in both TypeScript and the build/runtime toolchain.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **paths**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **paths**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m15-233",
    number: "TS-M15-233",
    title: "baseUrl",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Configuration",
    expectedAnswer: "baseUrl should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nbaseUrl should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **baseUrl**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **baseUrl**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m15-234",
    number: "TS-M15-234",
    title: "moduleResolution",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Configuration",
    expectedAnswer: "moduleResolution should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.",
    deepExplanation: "Step 1 — Understand the topic.\nmoduleResolution should be understood in terms of the TypeScript/JavaScript mechanism, the compile-time contract, the runtime behavior, and the production trade-off.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype Value = {\n  value: string;\n};\n\nfunction run(value: Value): string {\n  return value.value;\n}\n\nconsole.log(\n  run({ value: \"example\" }),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\nconst value = \"example\";\n\nconsole.log(value);\n```\n\nStep 5 — Example result:\n```text\nexample\n```\n\nStep 6 — Complexity / trade-off.\nPrefer the simplest clear TypeScript design that preserves useful type safety.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **moduleResolution**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **moduleResolution**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos"],
  },
  {
    id: "ts-m15-235",
    number: "TS-M15-235",
    title: "Module and target configuration",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Configuration",
    expectedAnswer: "target controls the JavaScript language level emitted by TypeScript; it is separate from the type-checking model.",
    deepExplanation: "Step 1 — Understand the topic.\ntarget controls the JavaScript language level emitted by TypeScript; it is separate from the type-checking model.\n\nStep 2 — Easy method.\nStart with one small example, identify the compile-time contract, then trace the runtime JavaScript behavior.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nconst values = [1, 2, 3];\n\nconsole.log(\n  values.map(\n    value => value * 2,\n  ),\n);\n```\n\nStep 4 — WITH BUILT-IN / IDIOMATIC TypeScript:\n```ts\n{\n  \"compilerOptions\": {\n    \"target\": \"ES2021\"\n  }\n}\n```\n\nStep 5 — Example result:\n```text\n[2, 4, 6]\n```\n\nStep 6 — Complexity / trade-off.\nChoose target according to supported runtime environments and build pipeline.\n\nStep 7 — Edge cases.\nCheck null/undefined, invalid external data, inference loss, module/runtime boundaries, errors, stale async work, and API evolution where relevant.\n\nStep 8 — Senior interview takeaway.\nExplain what the compiler proves, what exists at runtime, why the idiomatic/built-in approach is useful, and the maintainability/performance trade-off.",
    productionExample: "Production example for **Module and target configuration**: keep the type/behavior contract close to the owning module or feature, then verify the runtime behavior with focused tests.",
    bestPractices: ["Enable strict compiler settings.", "Prefer `unknown` over `any` at trust boundaries.", "Model domain invariants explicitly.", "Keep shared types close to their owning domain.", "Separate transport DTOs from domain models when transformations are needed."],
    tradeOffs: "Advantages: Stronger contracts and safer refactoring.; Better editor tooling.; Excellent fit for large React codebases.. Disadvantages: Type complexity can slow the compiler.; Types do not validate runtime data.; Poorly designed abstractions can become difficult to understand..",
    commonMistakes: ["Assuming TypeScript validates JSON at runtime.", "Overusing `as` assertions.", "Using `any` to silence compiler errors.", "Creating excessively clever type-level abstractions.", "Interview trap: Confusing compile-time types with runtime values.", "Interview trap: Saying interfaces exist in emitted JavaScript.", "Interview trap: Assuming all generic information exists at runtime.", "Interview trap: Treating TypeScript as a replacement for runtime validation."],
    followUpQuestions: ["What does the compiler prove for **Module and target configuration**?", "What JavaScript is emitted?", "What happens at runtime?", "How would you model the same concept in a monorepo?", "How would you test the runtime boundary?", "What trade-off appears at scale?"],
    relatedTopics: ["Generics", "Interfaces", "Type Aliases", "Utility Types", "Conditional Types", "React + TypeScript", "tsconfig", "Runtime Validation", "Monorepos", "# Module 16 – Performance & Build", "**Questions 236–245**"],
  },
];

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};

export const MOCK_TYPESCRIPT_MODULE15_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'TypeScript',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
