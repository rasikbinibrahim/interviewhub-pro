// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 10 — Security Headers).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m10-161",
    number: "SEC-M10-161",
    title: "X-Content-Type-Options",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**X-Content-Type-Options** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**X-Content-Type-Options** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse X-Content-Type-Options as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use X-Content-Type-Options as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-162",
    number: "SEC-M10-162",
    title: "Strict-Transport-Security",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Strict-Transport-Security** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Strict-Transport-Security** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Strict-Transport-Security as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Strict-Transport-Security as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-163",
    number: "SEC-M10-163",
    title: "Referrer-Policy",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Referrer-Policy** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Referrer-Policy** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Referrer-Policy as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Referrer-Policy as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-164",
    number: "SEC-M10-164",
    title: "Permissions-Policy",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Permissions-Policy** is an authorization topic. The UI may hide or disable features, but the authoritative allow/deny decision must be enforced at the trusted API or service boundary.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Permissions-Policy** is an authorization topic. The UI may hide or disable features, but the authoritative allow/deny decision must be enforced at the trusted API or service boundary.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Principal {\n  roles: string[];\n}\n\nfunction can(\n  principal: Principal,\n  requiredRole: string,\n): boolean {\n  for (let i = 0; i < principal.roles.length; i += 1) {\n    if (principal.roles[i] === requiredRole) return true;\n  }\n\n  return false;\n}\n\nconst allowed = can(\n  { roles: [\"admin\", \"support\"] },\n  \"admin\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst principal = {\n  roles: new Set([\"admin\", \"support\"]),\n};\n\nconst allowed =\n  principal.roles.has(\"admin\");\n```\n\nStep 5 — Production example:\nUse Permissions-Policy to express a small, auditable policy contract. The client can improve UX, but every protected operation must be re-authorized by the trusted service.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Permissions-Policy to express a small, auditable policy contract. The client can improve UX, but every protected operation must be re-authorized by the trusted service.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-165",
    number: "SEC-M10-165",
    title: "X-Frame-Options",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**X-Frame-Options** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**X-Frame-Options** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse X-Frame-Options as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use X-Frame-Options as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-166",
    number: "SEC-M10-166",
    title: "Content-Security-Policy",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Content-Security-Policy** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Content-Security-Policy** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse Content-Security-Policy as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Content-Security-Policy as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-167",
    number: "SEC-M10-167",
    title: "Cross-Origin-Opener-Policy",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Cross-Origin-Opener-Policy** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Cross-Origin-Opener-Policy** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Cross-Origin-Opener-Policy as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Cross-Origin-Opener-Policy as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-168",
    number: "SEC-M10-168",
    title: "Cross-Origin-Embedder-Policy",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Cross-Origin-Embedder-Policy** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Cross-Origin-Embedder-Policy** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Cross-Origin-Embedder-Policy as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Cross-Origin-Embedder-Policy as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-169",
    number: "SEC-M10-169",
    title: "Cross-Origin-Resource-Policy",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Cross-Origin-Resource-Policy** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Cross-Origin-Resource-Policy** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Cross-Origin-Resource-Policy as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Cross-Origin-Resource-Policy as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-170",
    number: "SEC-M10-170",
    title: "Fetch Metadata headers",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Fetch Metadata headers** is a CSRF-defense topic. Explain how browser credentials are attached, how an attacker could cause a state change, and which same-site, token, origin, or request-metadata control prevents it.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Fetch Metadata headers** is a CSRF-defense topic. Explain how browser credentials are attached, how an attacker could cause a state change, and which same-site, token, origin, or request-metadata control prevents it.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface RequestState {\n  csrfToken: string;\n}\n\nfunction buildRequestToken(\n  state: RequestState,\n): string {\n  return state.csrfToken;\n}\n\nconst token = buildRequestToken({\n  csrfToken: \"server-issued-token\",\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst form = new FormData();\n\nform.set(\"csrfToken\", \"server-issued-token\");\n\nconst token = form.get(\"csrfToken\");\n```\n\nStep 5 — Production example:\nUse Fetch Metadata headers on state-changing requests when browser-managed credentials are involved. Verify the defense server-side and test both accepted and rejected requests.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Fetch Metadata headers on state-changing requests when browser-managed credentials are involved. Verify the defense server-side and test both accepted and rejected requests.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-171",
    number: "SEC-M10-171",
    title: "Security headers deployment",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Security headers deployment** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Security headers deployment** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Security headers deployment as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Security headers deployment as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-172",
    number: "SEC-M10-172",
    title: "Header debugging",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Header debugging** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Header debugging** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Header debugging as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Header debugging as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-173",
    number: "SEC-M10-173",
    title: "Header testing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Header testing** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Header testing** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Header testing as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Header testing as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-174",
    number: "SEC-M10-174",
    title: "Header interactions",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Header interactions** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Header interactions** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Header interactions as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Header interactions as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m10-175",
    number: "SEC-M10-175",
    title: "Enterprise header policy",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security Headers",
    expectedAnswer: "**Enterprise header policy** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Enterprise header policy** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Enterprise header policy as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Enterprise header policy as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE10_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));