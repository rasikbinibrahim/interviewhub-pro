// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 6 — CORS).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m6-91",
    number: "SEC-M6-91",
    title: "Same-Origin Policy",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Same-Origin Policy** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Same-Origin Policy** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isAllowedOrigin(\n  origin: string,\n  allowed: readonly string[],\n): boolean {\n  for (let i = 0; i < allowed.length; i += 1) {\n    if (allowed[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = isAllowedOrigin(\n  \"https://app.example.com\",\n  [\"https://app.example.com\"],\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst allowedOrigins = new Set([\n  \"https://app.example.com\",\n  \"https://admin.example.com\",\n]);\n\nconst allowed =\n  allowedOrigins.has(\"https://app.example.com\");\n```\n\nStep 5 — Production example:\nUse Same-Origin Policy to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Same-Origin Policy to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-92",
    number: "SEC-M6-92",
    title: "CORS fundamentals",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**CORS fundamentals** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CORS fundamentals** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isAllowedOrigin(\n  origin: string,\n  allowed: readonly string[],\n): boolean {\n  for (let i = 0; i < allowed.length; i += 1) {\n    if (allowed[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = isAllowedOrigin(\n  \"https://app.example.com\",\n  [\"https://app.example.com\"],\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst allowedOrigins = new Set([\n  \"https://app.example.com\",\n  \"https://admin.example.com\",\n]);\n\nconst allowed =\n  allowedOrigins.has(\"https://app.example.com\");\n```\n\nStep 5 — Production example:\nUse CORS fundamentals to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use CORS fundamentals to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-93",
    number: "SEC-M6-93",
    title: "Simple requests",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Simple requests** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Simple requests** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isAllowedOrigin(\n  origin: string,\n  allowed: readonly string[],\n): boolean {\n  for (let i = 0; i < allowed.length; i += 1) {\n    if (allowed[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = isAllowedOrigin(\n  \"https://app.example.com\",\n  [\"https://app.example.com\"],\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst allowedOrigins = new Set([\n  \"https://app.example.com\",\n  \"https://admin.example.com\",\n]);\n\nconst allowed =\n  allowedOrigins.has(\"https://app.example.com\");\n```\n\nStep 5 — Production example:\nUse Simple requests to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Simple requests to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-94",
    number: "SEC-M6-94",
    title: "Preflight",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Preflight** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Preflight** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isAllowedOrigin(\n  origin: string,\n  allowed: readonly string[],\n): boolean {\n  for (let i = 0; i < allowed.length; i += 1) {\n    if (allowed[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = isAllowedOrigin(\n  \"https://app.example.com\",\n  [\"https://app.example.com\"],\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst allowedOrigins = new Set([\n  \"https://app.example.com\",\n  \"https://admin.example.com\",\n]);\n\nconst allowed =\n  allowedOrigins.has(\"https://app.example.com\");\n```\n\nStep 5 — Production example:\nUse Preflight to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Preflight to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-95",
    number: "SEC-M6-95",
    title: "Access-Control-Allow-Origin",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Access-Control-Allow-Origin** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Access-Control-Allow-Origin** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isAllowedOrigin(\n  origin: string,\n  allowed: readonly string[],\n): boolean {\n  for (let i = 0; i < allowed.length; i += 1) {\n    if (allowed[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = isAllowedOrigin(\n  \"https://app.example.com\",\n  [\"https://app.example.com\"],\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst allowedOrigins = new Set([\n  \"https://app.example.com\",\n  \"https://admin.example.com\",\n]);\n\nconst allowed =\n  allowedOrigins.has(\"https://app.example.com\");\n```\n\nStep 5 — Production example:\nUse Access-Control-Allow-Origin to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Access-Control-Allow-Origin to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-96",
    number: "SEC-M6-96",
    title: "Access-Control-Allow-Credentials",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Access-Control-Allow-Credentials** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Access-Control-Allow-Credentials** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isAllowedOrigin(\n  origin: string,\n  allowed: readonly string[],\n): boolean {\n  for (let i = 0; i < allowed.length; i += 1) {\n    if (allowed[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = isAllowedOrigin(\n  \"https://app.example.com\",\n  [\"https://app.example.com\"],\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst allowedOrigins = new Set([\n  \"https://app.example.com\",\n  \"https://admin.example.com\",\n]);\n\nconst allowed =\n  allowedOrigins.has(\"https://app.example.com\");\n```\n\nStep 5 — Production example:\nUse Access-Control-Allow-Credentials to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Access-Control-Allow-Credentials to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-97",
    number: "SEC-M6-97",
    title: "Allowed methods",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Allowed methods** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Allowed methods** is a security architecture topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before choosing an implementation.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse Allowed methods as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Allowed methods as an architecture checklist: identify assets, threats, trust boundaries, controls, evidence, and operational ownership before implementation.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-98",
    number: "SEC-M6-98",
    title: "Allowed headers",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Allowed headers** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Allowed headers** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Allowed headers as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Allowed headers as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-99",
    number: "SEC-M6-99",
    title: "Expose headers",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Expose headers** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Expose headers** is a browser security-header topic. Explain what browser behavior the header changes, the safe policy shape, rollout/testing concerns, and how it interacts with other headers.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface HeadersMap {\n  values: Record<string, string>;\n}\n\nfunction headerValue(\n  headers: HeadersMap,\n  name: string,\n): string | null {\n  return headers.values[name.toLowerCase()] ?? null;\n}\n\nconst value = headerValue(\n  {\n    values: {\n      \"x-content-type-options\": \"nosniff\",\n    },\n  },\n  \"x-content-type-options\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst headers = new Headers();\n\nheaders.set(\n  \"X-Content-Type-Options\",\n  \"nosniff\",\n);\n\nconst value =\n  headers.get(\"X-Content-Type-Options\");\n```\n\nStep 5 — Production example:\nUse Expose headers as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Expose headers as part of a consistent response-header policy. Validate it in automated tests and browser tooling, and check interactions with CSP/CORS/embedding requirements.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-100",
    number: "SEC-M6-100",
    title: "CORS errors",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**CORS errors** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CORS errors** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isAllowedOrigin(\n  origin: string,\n  allowed: readonly string[],\n): boolean {\n  for (let i = 0; i < allowed.length; i += 1) {\n    if (allowed[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = isAllowedOrigin(\n  \"https://app.example.com\",\n  [\"https://app.example.com\"],\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst allowedOrigins = new Set([\n  \"https://app.example.com\",\n  \"https://admin.example.com\",\n]);\n\nconst allowed =\n  allowedOrigins.has(\"https://app.example.com\");\n```\n\nStep 5 — Production example:\nUse CORS errors to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use CORS errors to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-101",
    number: "SEC-M6-101",
    title: "Credentialed CORS",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Credentialed CORS** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Credentialed CORS** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isAllowedOrigin(\n  origin: string,\n  allowed: readonly string[],\n): boolean {\n  for (let i = 0; i < allowed.length; i += 1) {\n    if (allowed[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = isAllowedOrigin(\n  \"https://app.example.com\",\n  [\"https://app.example.com\"],\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst allowedOrigins = new Set([\n  \"https://app.example.com\",\n  \"https://admin.example.com\",\n]);\n\nconst allowed =\n  allowedOrigins.has(\"https://app.example.com\");\n```\n\nStep 5 — Production example:\nUse Credentialed CORS to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Credentialed CORS to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-102",
    number: "SEC-M6-102",
    title: "Dynamic origin validation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Dynamic origin validation** is a CSRF-defense topic. Explain how browser credentials are attached, how an attacker could cause a state change, and which same-site, token, origin, or request-metadata control prevents it.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Dynamic origin validation** is a CSRF-defense topic. Explain how browser credentials are attached, how an attacker could cause a state change, and which same-site, token, origin, or request-metadata control prevents it.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface RequestState {\n  csrfToken: string;\n}\n\nfunction buildRequestToken(\n  state: RequestState,\n): string {\n  return state.csrfToken;\n}\n\nconst token = buildRequestToken({\n  csrfToken: \"server-issued-token\",\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst form = new FormData();\n\nform.set(\"csrfToken\", \"server-issued-token\");\n\nconst token = form.get(\"csrfToken\");\n```\n\nStep 5 — Production example:\nUse Dynamic origin validation on state-changing requests when browser-managed credentials are involved. Verify the defense server-side and test both accepted and rejected requests.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Dynamic origin validation on state-changing requests when browser-managed credentials are involved. Verify the defense server-side and test both accepted and rejected requests.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-103",
    number: "SEC-M6-103",
    title: "CORS misconfiguration",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**CORS misconfiguration** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CORS misconfiguration** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isAllowedOrigin(\n  origin: string,\n  allowed: readonly string[],\n): boolean {\n  for (let i = 0; i < allowed.length; i += 1) {\n    if (allowed[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = isAllowedOrigin(\n  \"https://app.example.com\",\n  [\"https://app.example.com\"],\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst allowedOrigins = new Set([\n  \"https://app.example.com\",\n  \"https://admin.example.com\",\n]);\n\nconst allowed =\n  allowedOrigins.has(\"https://app.example.com\");\n```\n\nStep 5 — Production example:\nUse CORS misconfiguration to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use CORS misconfiguration to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-104",
    number: "SEC-M6-104",
    title: "Wildcard origins",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Wildcard origins** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Wildcard origins** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isAllowedOrigin(\n  origin: string,\n  allowed: readonly string[],\n): boolean {\n  for (let i = 0; i < allowed.length; i += 1) {\n    if (allowed[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = isAllowedOrigin(\n  \"https://app.example.com\",\n  [\"https://app.example.com\"],\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst allowedOrigins = new Set([\n  \"https://app.example.com\",\n  \"https://admin.example.com\",\n]);\n\nconst allowed =\n  allowedOrigins.has(\"https://app.example.com\");\n```\n\nStep 5 — Production example:\nUse Wildcard origins to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Wildcard origins to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m6-105",
    number: "SEC-M6-105",
    title: "Private network access",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "CORS",
    expectedAnswer: "**Private network access** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Private network access** is a browser cross-origin security topic. Explain what the browser permits or blocks, which response/request headers participate, and why CORS is not an authentication mechanism.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\nfunction isAllowedOrigin(\n  origin: string,\n  allowed: readonly string[],\n): boolean {\n  for (let i = 0; i < allowed.length; i += 1) {\n    if (allowed[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = isAllowedOrigin(\n  \"https://app.example.com\",\n  [\"https://app.example.com\"],\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst allowedOrigins = new Set([\n  \"https://app.example.com\",\n  \"https://admin.example.com\",\n]);\n\nconst allowed =\n  allowedOrigins.has(\"https://app.example.com\");\n```\n\nStep 5 — Production example:\nUse Private network access to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Private network access to explicitly control which origins may read cross-origin responses. Keep the allow-list narrow and never rely on CORS as authentication.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE6_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));