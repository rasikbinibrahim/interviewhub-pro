// Derived from frontend/src/document/Part_6_Browser_Internals_Web_APIs_Master_Handbook.md (Module 16 — Browser Caching).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question, Execution/Flow, Advanced Production Example, Performance/Security Notes,
// Best Practices, Advantages/Disadvantages, Common Mistakes/Interview Traps, Senior
// Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "jsbrowser-m16-236",
    number: "JSBROWSER-M16-236",
    title: "HTTP cache",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "An HTTP cache stores reusable HTTP responses according to HTTP caching semantics. Before making a network request, the browser can evaluate freshness and cache metadata and may reuse a response without contacting the origin. When a response is stale but revalidatable, the browser can send a conditional request and reuse the cached body if the server confirms it is unchanged.",
    deepExplanation: "http\nCache-Control: public, max-age=31536000, immutable\nETag: \"asset-v1042\"\n```\n\nFor a hashed static asset, the browser can reuse the response for a long time. For mutable API data, the server can use shorter freshness plus revalidation.",
    productionExample: "A production frontend serves hashed JS/CSS bundles with long-lived immutable caching, while personalized API responses use carefully scoped freshness/revalidation rules. DevTools is used to confirm cache hits, misses and revalidation behavior.",
    bestPractices: ["Define caching policy per resource class.","Use server/CDN headers as the source of HTTP-cache policy.","Measure cache hit, revalidation, staleness and bandwidth.","Keep private/personalized data separate from public caches.","Document invalidation, deployment and rollback behavior."],
    tradeOffs: "HTTP caching can dramatically reduce latency and bandwidth, but stale data and invalidation mistakes can be difficult to diagnose. Different resource types need different policies.",
    commonMistakes: ["Treating browser cache as application state.","Using one cache policy for every endpoint.","Assuming every request reaches the network.","Ignoring private/personalized response caching."],
    followUpQuestions: ["Browser HTTP cache vs Cache API?","What makes a response fresh?","When does revalidation happen?"],
    relatedTopics: ["HTTP cache","Cache-Control","ETag","Revalidation","Freshness"],
  },
  {
    id: "jsbrowser-m16-237",
    number: "JSBROWSER-M16-237",
    title: "Cache-Control",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "`Cache-Control` is the primary HTTP response-header mechanism for expressing caching policy. Directives such as `max-age`, `s-maxage`, `public`, `private`, `no-cache`, `no-store`, `must-revalidate` and `stale-while-revalidate` affect how caches may store, reuse or revalidate responses. Exact behavior depends on the directive combination and cache type.",
    deepExplanation: "http\nCache-Control: public, max-age=31536000, immutable\n```\n\nFor personalized responses, a safer policy may be:\n\n```http\nCache-Control: private, no-cache\n```",
    productionExample: "A CDN serves versioned static assets with long cache lifetimes, while account APIs use private or revalidation-oriented policies. The team reviews cache headers as part of every API/resource contract.",
    bestPractices: ["Define caching policy per resource class.","Use server/CDN headers as the source of HTTP-cache policy.","Measure cache hit, revalidation, staleness and bandwidth.","Keep private/personalized data separate from public caches.","Document invalidation, deployment and rollback behavior."],
    tradeOffs: "Long freshness maximizes cache efficiency but increases staleness risk. `no-store` avoids storage/reuse but sacrifices performance. The correct policy is a product/data-consistency decision.",
    commonMistakes: ["Saying `no-cache` means 'never store'.","Using `no-store` everywhere.","Using long max-age on mutable personalized data.","Confusing browser cache and shared CDN semantics."],
    followUpQuestions: ["no-cache vs no-store?","max-age vs s-maxage?","What does immutable mean for versioned assets?"],
    relatedTopics: ["Cache-Control","Freshness","CDN","Browser cache","HTTP caching"],
  },
  {
    id: "jsbrowser-m16-238",
    number: "JSBROWSER-M16-238",
    title: "ETag",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "An ETag is a server-generated validator associated with a representation. When a cached response is stale or requires validation, the browser can send `If-None-Match: <etag>`. If the representation has not changed, the server can return `304 Not Modified`, allowing the browser to reuse the cached body without retransmitting it.",
    deepExplanation: "http\nETag: \"orders-v1042\"\n\nGET /api/orders\nIf-None-Match: \"orders-v1042\"\n\nHTTP/1.1 304 Not Modified\n```\n\nThe response body is omitted on 304 because the cached representation remains usable.",
    productionExample: "A production API uses stable ETags for relatively large read responses. The frontend benefits from smaller revalidation responses while the server remains authoritative for freshness.",
    bestPractices: ["Define caching policy per resource class.","Use server/CDN headers as the source of HTTP-cache policy.","Measure cache hit, revalidation, staleness and bandwidth.","Keep private/personalized data separate from public caches.","Document invalidation, deployment and rollback behavior."],
    tradeOffs: "ETag reduces response transfer on revalidation, but generating validators and handling distributed consistency adds server complexity. It is less useful for resources that are always freshly generated or tiny.",
    commonMistakes: ["Thinking ETag is a client-side JavaScript API.","Returning 304 with an unrelated body.","Using changing/random ETags that prevent useful revalidation.","Assuming every cached response uses ETag."],
    followUpQuestions: ["ETag vs Last-Modified?","What does 304 mean?","Strong vs weak ETags?"],
    relatedTopics: ["ETag","If-None-Match","304","Conditional requests"],
  },
  {
    id: "jsbrowser-m16-239",
    number: "JSBROWSER-M16-239",
    title: "Last-Modified",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "`Last-Modified` communicates the representation's modification time. A browser can later send `If-Modified-Since` to ask whether the resource changed since that time. If it has not changed according to the server's comparison rules, the server may return `304 Not Modified`.",
    deepExplanation: "http\nLast-Modified: Wed, 20 Aug 2026 08:30:00 GMT\n```\n\nLater:\n\n```http\nIf-Modified-Since: Wed, 20 Aug 2026 08:30:00 GMT\n```\n",
    productionExample: "A static-content server exposes Last-Modified for resources where modification timestamps are meaningful. For assets with content hashing and strong versioning, ETag or URL versioning is typically more precise.",
    bestPractices: ["Define caching policy per resource class.","Use server/CDN headers as the source of HTTP-cache policy.","Measure cache hit, revalidation, staleness and bandwidth.","Keep private/personalized data separate from public caches.","Document invalidation, deployment and rollback behavior."],
    tradeOffs: "Timestamp validators are simple and widely understood, but modification time alone may be less precise than a representation-specific validator such as ETag.",
    commonMistakes: ["Thinking Last-Modified forces revalidation on every request.","Using client clock time as authoritative.","Assuming second-level timestamps always distinguish changes."],
    followUpQuestions: ["Last-Modified vs ETag?","How does If-Modified-Since work?","When is URL versioning better?"],
    relatedTopics: ["Last-Modified","If-Modified-Since","304","Revalidation"],
  },
  {
    id: "jsbrowser-m16-240",
    number: "JSBROWSER-M16-240",
    title: "Conditional requests",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Conditional requests let the client ask the server whether a cached representation is still valid instead of downloading the full response again. Common validators are `If-None-Match` with ETag and `If-Modified-Since` with Last-Modified. A successful validation commonly produces `304 Not Modified`.",
    deepExplanation: "text\nCached response\n   ↓\nValidator becomes stale\n   ↓\nConditional GET\n   ├─ unchanged → 304 → reuse cached body\n   └─ changed   → 200 + new body\n```\n\nThe browser handles this according to HTTP caching rules; application code typically does not manually implement this for ordinary HTTP cache usage.",
    productionExample: "A product API uses ETag-based validation for large read models. The browser revalidates when necessary, reducing response bytes while preserving freshness.",
    bestPractices: ["Define caching policy per resource class.","Use server/CDN headers as the source of HTTP-cache policy.","Measure cache hit, revalidation, staleness and bandwidth.","Keep private/personalized data separate from public caches.","Document invalidation, deployment and rollback behavior."],
    tradeOffs: "Conditional requests still require network round trips, so they are slower than a fresh cache hit, but much cheaper than transferring a complete unchanged representation.",
    commonMistakes: ["Calling 304 a cache miss.","Manually implementing browser revalidation with fetch without a reason.","Forgetting that a conditional request still incurs network latency."],
    followUpQuestions: ["Fresh cache vs revalidation?","Can POST use conditional requests?","How do ETag and Last-Modified interact?"],
    relatedTopics: ["Conditional requests","ETag","304","Last-Modified","HTTP cache"],
  },
  {
    id: "jsbrowser-m16-241",
    number: "JSBROWSER-M16-241",
    title: "Freshness and revalidation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Freshness determines whether a cached response can be reused immediately based on HTTP caching metadata and age. A fresh response can usually be reused without a network request. A stale response may be revalidated if validators exist, or refetched if it cannot be safely reused. Revalidation and freshness are distinct from application-level cache invalidation.",
    deepExplanation: "text\nResponse stored\n   ↓\nFresh?\n ├─ yes → reuse\n └─ no → revalidate/refetch\n            ↓\n       304 or new 200\n```\n\nThe browser evaluates cache metadata rather than simply assuming every stored response is current.",
    productionExample: "A news site gives article JSON a short freshness window and ETags. The UI gets cache-speed responses during the fresh period and uses low-cost revalidation afterward.",
    bestPractices: ["Define caching policy per resource class.","Use server/CDN headers as the source of HTTP-cache policy.","Measure cache hit, revalidation, staleness and bandwidth.","Keep private/personalized data separate from public caches.","Document invalidation, deployment and rollback behavior."],
    tradeOffs: "Longer freshness improves latency and reduces traffic but increases possible staleness. Very short freshness increases network requests and server load.",
    commonMistakes: ["Equating stale with unusable.","Assuming every stale response requires full download.","Treating application cache invalidation as identical to HTTP freshness."],
    followUpQuestions: ["What determines freshness?","Fresh response vs 304?","How do SWR and HTTP revalidation differ?"],
    relatedTopics: ["Freshness","Revalidation","Cache-Control","ETag","HTTP cache"],
  },
  {
    id: "jsbrowser-m16-242",
    number: "JSBROWSER-M16-242",
    title: "Cache-Control no-cache vs no-store",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "`no-cache` means a stored response must be revalidated with the origin/cache rules before reuse; it does not necessarily mean the response cannot be stored. `no-store` tells compliant caches not to store the response. Therefore `no-cache` is mainly about mandatory validation, while `no-store` is about preventing storage.",
    deepExplanation: "http\nCache-Control: no-cache\n```\n\nmay permit storage but requires validation before reuse.\n\n```http\nCache-Control: no-store\n```\n\ninstructs caches not to store the response.",
    productionExample: "A sensitive account response that should not be retained uses a deliberate `no-store` policy when appropriate. A frequently changing but cacheable resource may use `no-cache` plus a validator so the browser can keep a copy for efficient revalidation.",
    bestPractices: ["Define caching policy per resource class.","Use server/CDN headers as the source of HTTP-cache policy.","Measure cache hit, revalidation, staleness and bandwidth.","Keep private/personalized data separate from public caches.","Document invalidation, deployment and rollback behavior."],
    tradeOffs: "The distinction is subtle but important: `no-cache` can retain data and reduce bytes through revalidation, while `no-store` sacrifices cache reuse for stronger retention control.",
    commonMistakes: ["Saying no-cache means no storage.","Using no-store for every asset and destroying performance.","Assuming no-cache means no network round trip."],
    followUpQuestions: ["What does private mean?","Can no-cache use ETag?","Which directives are suitable for sensitive responses?"],
    relatedTopics: ["no-cache","no-store","private","ETag","HTTP cache"],
  },
  {
    id: "jsbrowser-m16-243",
    number: "JSBROWSER-M16-243",
    title: "Cache busting",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Cache busting is a deployment strategy that changes a resource's URL when its content changes so old cached versions and new versions can coexist safely. Content hashing in filenames, such as `app.8f31c2.js`, is the preferred modern pattern for immutable static assets.",
    deepExplanation: "html\n<script type=\"module\"\n        src=\"/assets/app.8f31c2.js\">\n</script>\n```\n\nWhen the bundle changes, the build emits a new hash and therefore a new URL. Old URLs can remain cached safely.",
    productionExample: "A production CI/CD pipeline fingerprints JS/CSS/image assets and publishes an HTML entry point that references the new hashes. Long-lived asset cache headers are safe because content changes create new URLs.",
    bestPractices: ["Define caching policy per resource class.","Use server/CDN headers as the source of HTTP-cache policy.","Measure cache hit, revalidation, staleness and bandwidth.","Keep private/personalized data separate from public caches.","Document invalidation, deployment and rollback behavior."],
    tradeOffs: "Cache busting improves deployment reliability but increases the number of distinct cached objects and requires build/deploy coordination.",
    commonMistakes: ["Using a timestamp query parameter on every request.","Changing asset URLs without updating HTML consistently.","Long-caching unversioned HTML and expecting instant deployment changes."],
    followUpQuestions: ["Content hash vs query-string busting?","How do you roll back a deployment?","Why can HTML have a different cache policy from JS assets?"],
    relatedTopics: ["Cache busting","Content hashing","CDN","Deployments","Immutable assets"],
  },
  {
    id: "jsbrowser-m16-244",
    number: "JSBROWSER-M16-244",
    title: "Service Worker cache",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Service Worker cache is an application-managed cache using the Cache API from a Service Worker. Unlike the browser's normal HTTP cache, the Service Worker explicitly decides when to intercept requests and which cached response to return. It is useful for offline strategies and controlled application caching.",
    deepExplanation: "js\nconst CACHE = \"shell-v3\";\n\nself.addEventListener(\"install\", event => {\n  event.waitUntil(\n    caches.open(CACHE).then(cache =>\n      cache.addAll([\"/\", \"/app.js\", \"/app.css\"])\n    )\n  );\n});\n```\n\nA fetch handler can then choose cache-first, network-first or stale-while-revalidate behavior.",
    productionExample: "A PWA precaches its shell and uses runtime caching for selected read-only content. Personalized API responses are excluded unless a privacy/freshness design explicitly allows caching them.",
    bestPractices: ["Define caching policy per resource class.","Use server/CDN headers as the source of HTTP-cache policy.","Measure cache hit, revalidation, staleness and bandwidth.","Keep private/personalized data separate from public caches.","Document invalidation, deployment and rollback behavior."],
    tradeOffs: "Service Worker caching offers strong offline control, but invalidation, lifecycle updates and debugging are more complex than relying solely on the HTTP cache.",
    commonMistakes: ["Thinking Service Worker cache is the HTTP cache.","Caching every API response.","Serving stale authenticated data.","Never deleting old cache versions."],
    followUpQuestions: ["Cache API vs HTTP cache?","How do cache-first and network-first differ?","How do Service Worker updates affect caches?"],
    relatedTopics: ["Service Worker","Cache API","Offline","Caching strategies"],
  },
  {
    id: "jsbrowser-m16-245",
    number: "JSBROWSER-M16-245",
    title: "Caching strategy design",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "A caching strategy maps resource classes to appropriate freshness, validation and storage policies. A strong architecture usually treats immutable assets, HTML, public API data, private API data and offline resources differently. The strategy should consider correctness, latency, bandwidth, privacy, deployment and recovery.",
    deepExplanation: "text\nResource class\n  ├─ Hashed assets → long-lived immutable\n  ├─ HTML shell   → short/controlled freshness\n  ├─ Public API   → cache + revalidate\n  ├─ Private API  → private/no-store or carefully scoped\n  └─ Offline data → IndexedDB/Cache API + sync strategy\n```\n\nThe browser HTTP cache, CDN and Service Worker cache should have clear responsibilities rather than overlapping unpredictably.",
    productionExample: "A production SaaS app uses immutable hashed assets at the CDN, short-lived HTML, ETag-backed API responses and IndexedDB for explicitly offline business data. Cache policy is part of the API/resource contract.",
    bestPractices: ["Define caching policy per resource class.","Use server/CDN headers as the source of HTTP-cache policy.","Measure cache hit, revalidation, staleness and bandwidth.","Keep private/personalized data separate from public caches.","Document invalidation, deployment and rollback behavior."],
    tradeOffs: "A layered cache improves performance but creates more invalidation paths. Simpler products should avoid adding Service Worker or custom storage caching unless the business need justifies it.",
    commonMistakes: ["Using one cache strategy for everything.","Having the CDN, browser and Service Worker disagree about freshness.","Caching private data broadly.","No rollback or stale-data recovery plan."],
    followUpQuestions: ["How do you design cache hierarchy?","When would you avoid a Service Worker cache?","How do you debug stale content across browser/CDN/SW layers?"],
    relatedTopics: ["Caching strategy","HTTP cache","CDN","Service Worker","IndexedDB","Invalidation"],
  },

];

export const MOCK_BROWSER_INTERNALS_MODULE16_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Browser/Web APIs',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));