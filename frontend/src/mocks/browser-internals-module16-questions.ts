// Derived from frontend/src/document/Part_6_Browser_Internals_Web_APIs_Master_Handbook.md (Module 16 — Browser Caching).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question, Execution/Flow, Advanced Production Example, Performance/Security Notes,
// Best Practices, Advantages/Disadvantages, Common Mistakes/Interview Traps, Senior
// Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "jsbrowser-m16-236",
    number: "JSBROWSER-M16-236",
    title: "HTTP cache",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Explain **HTTP cache** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **HTTP cache**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for HTTP cache: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for HTTP cache: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **HTTP cache** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **HTTP cache**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m16-237",
    number: "JSBROWSER-M16-237",
    title: "Cache-Control",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Explain **Cache-Control** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Cache-Control**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Cache-Control: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Cache-Control: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Cache-Control** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Cache-Control**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m16-238",
    number: "JSBROWSER-M16-238",
    title: "ETag",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Explain **ETag** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **ETag**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for ETag: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for ETag: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **ETag** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **ETag**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m16-239",
    number: "JSBROWSER-M16-239",
    title: "Last-Modified",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Explain **Last-Modified** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Last-Modified**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Last-Modified: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Last-Modified: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Last-Modified** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Last-Modified**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m16-240",
    number: "JSBROWSER-M16-240",
    title: "Conditional requests",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Explain **Conditional requests** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Conditional requests**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Conditional requests: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Conditional requests: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Conditional requests** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Conditional requests**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m16-241",
    number: "JSBROWSER-M16-241",
    title: "Freshness and revalidation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Explain **Freshness and revalidation** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Freshness and revalidation**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Freshness and revalidation: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Freshness and revalidation: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Freshness and revalidation** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Freshness and revalidation**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m16-242",
    number: "JSBROWSER-M16-242",
    title: "Cache-Control no-cache vs no-store",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Explain **Cache-Control no-cache vs no-store** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Cache-Control no-cache vs no-store**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Cache-Control no-cache vs no-store: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Cache-Control no-cache vs no-store: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Cache-Control no-cache vs no-store** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Cache-Control no-cache vs no-store**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m16-243",
    number: "JSBROWSER-M16-243",
    title: "Cache busting",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Explain **Cache busting** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Cache busting**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Cache busting: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Cache busting: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Cache busting** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Cache busting**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m16-244",
    number: "JSBROWSER-M16-244",
    title: "Service Worker cache",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Explain **Service Worker cache** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Service Worker cache**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Service Worker cache: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\nWorker output is asynchronous and depends on the worker message handler.\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Service Worker cache: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Service Worker cache** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Service Worker cache**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m16-245",
    number: "JSBROWSER-M16-245",
    title: "Caching strategy design",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    category: "Browser Caching",
    expectedAnswer: "Explain **Caching strategy design** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Caching strategy design**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Caching strategy design: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Caching strategy design: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Caching strategy design** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Caching strategy design**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---","# Module 17 – Browser Debugging","**Questions 246–255**"],
  },
];

export const MOCK_BROWSER_INTERNALS_MODULE16_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Browser/Web APIs',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
