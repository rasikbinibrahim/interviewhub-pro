// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 10 — Accessibility Testing).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m10-1",
    number: "TESTQA-M10-1",
    title: "WCAG Fundamentals",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**WCAG Fundamentals** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**WCAG Fundamentals** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-2",
    number: "TESTQA-M10-2",
    title: "Semantic HTML",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Semantic HTML** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Semantic HTML** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-3",
    number: "TESTQA-M10-3",
    title: "ARIA",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**ARIA** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**ARIA** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-4",
    number: "TESTQA-M10-4",
    title: "Keyboard Navigation",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Keyboard Navigation** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Keyboard Navigation** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-5",
    number: "TESTQA-M10-5",
    title: "Screen Readers",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Screen Readers** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Screen Readers** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-6",
    number: "TESTQA-M10-6",
    title: "Focus Management",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Focus Management** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Focus Management** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-7",
    number: "TESTQA-M10-7",
    title: "Accessible Forms",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Accessible Forms** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Accessible Forms** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-8",
    number: "TESTQA-M10-8",
    title: "Dialogs and Modals",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Dialogs and Modals** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Dialogs and Modals** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-9",
    number: "TESTQA-M10-9",
    title: "Tables and Data Grids",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Tables and Data Grids** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Tables and Data Grids** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-10",
    number: "TESTQA-M10-10",
    title: "axe-core",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**axe-core** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**axe-core** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-11",
    number: "TESTQA-M10-11",
    title: "jest-axe",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**jest-axe** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**jest-axe** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-12",
    number: "TESTQA-M10-12",
    title: "Lighthouse Accessibility",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Lighthouse Accessibility** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Lighthouse Accessibility** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-13",
    number: "TESTQA-M10-13",
    title: "Accessibility Reports",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Accessibility Reports** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Accessibility Reports** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-14",
    number: "TESTQA-M10-14",
    title: "Automated vs Manual Accessibility Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Automated vs Manual Accessibility Testing** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Automated vs Manual Accessibility Testing** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-15",
    number: "TESTQA-M10-15",
    title: "Enterprise Accessibility Strategy",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Enterprise Accessibility Strategy** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Enterprise Accessibility Strategy** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
  {
    id: "testqa-m10-16",
    number: "TESTQA-M10-16",
    title: "Accessibility Testing — Module Review",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Accessibility Testing",
    expectedAnswer: "**Accessibility Testing — Module Review** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Accessibility Testing — Module Review** should verify semantic structure, keyboard interaction, accessible names/states, focus behavior, and automated rules, supplemented by manual review.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface A11yCheck { name: string; passed: boolean; }\n\nfunction allPassed(checks: readonly A11yCheck[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst checks = [\n  { name: \"name\", passed: true },\n  { name: \"keyboard\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nCombine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Combine automated accessibility rules with keyboard, semantics, focus, and manual assistive-technology review.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Accessibility Testing"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE10_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));