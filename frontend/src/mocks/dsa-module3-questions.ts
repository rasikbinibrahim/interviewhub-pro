// Derived from frontend/src/document/DSA_Master_Handbook_Senior_Frontend_Engineers.md (Module 3 — Strings).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Definition, JavaScript/TypeScript implementation, Time/Space Complexity,
// Trade-offs, Common Mistakes, Interview Tricks, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Apple",
  "Netflix",
  "Uber",
  "Airbnb",
  "Atlassian",
  "Adobe",
  "LinkedIn",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "dsa-m3-21",
    number: "DSA-M3-21",
    title: "String traversal",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Strings",
    expectedAnswer: "String traversal means scanning characters in order to inspect, count, transform, or validate a string. The simplest approach is a single pass with O(n) time; in JavaScript, be aware that string indexing works on UTF-16 code units, so Unicode-aware problems may require code-point handling.",
    deepExplanation: "Step 1 — Understand:\nFor input `hello`, inspect each character from left to right.\n\nStep 2 — WITHOUT BUILT-IN / CORE:\n```ts\nfunction countVowels(value: string): number {\n  let count = 0;\n\n  for (let i = 0; i < value.length; i++) {\n    const ch = value[i]!;\n    if (\n      ch === \"a\" ||\n      ch === \"e\" ||\n      ch === \"i\" ||\n      ch === \"o\" ||\n      ch === \"u\"\n    ) {\n      count++;\n    }\n  }\n\n  return count;\n}\n```\n\nStep 3 — WITH BUILT-IN:\n```ts\nfunction countVowelsUsingBuiltIns(value: string): number {\n  return value\n    .toLowerCase()\n    .split(\"\")\n    .filter((ch) => \"aeiou\".includes(ch))\n    .length;\n}\n```\n\nStep 4 — Example:\n`\"interview\"` → vowels are `i,e,i,e` → answer `4`.\n\nCore idea: one pass, clear invariant, no unnecessary string rebuilding inside the loop.",
    productionExample: "Frontend example: scanning a search term once to normalize or validate input is usually cheaper than repeatedly creating intermediate strings.",
    bestPractices: ["Use a single pass when each character needs one decision.", "Be explicit about case sensitivity.", "Know whether the problem is ASCII-only or Unicode-aware.", "Avoid repeatedly concatenating large strings in hot loops.", "State O(n) time and the extra space used by any transformed copy."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| Index loop | Clear and low overhead | More verbose |\n| for...of | Readable character iteration | Still depends on string semantics |\n| split/map/filter | Concise | Extra arrays/allocations |",
    commonMistakes: ["Off-by-one indexes.", "Forgetting case normalization.", "Assuming UTF-16 code units are always complete characters.", "Building unnecessary intermediate arrays.", "Using quadratic string concatenation in a large loop."],
    followUpQuestions: ["How do you reverse a string?", "How do you count words?", "How do you handle Unicode code points?", "How do you find the first unique character?", "What is the difference between code unit and code point?", "Can you traverse without split()?", "How would you process a very large string?", "How would you benchmark string traversal?", "What is the extra space of split()?", "How do you handle case-insensitive input?"],
    relatedTopics: ["Strings", "Unicode", "Two Pointers", "Frequency Counting"],
  },
  {
    id: "dsa-m3-22",
    number: "DSA-M3-22",
    title: "Frequency counting",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Strings",
    expectedAnswer: "Frequency counting records how many times each character or token appears. It is a common string technique for anagrams, duplicate detection, first unique character, and histogram-style problems.",
    deepExplanation: "Step 1 — Build the frequency table.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction charFrequency(value: string): Record<string, number> {\n  const count: Record<string, number> = Object.create(null);\n\n  for (let i = 0; i < value.length; i++) {\n    const ch = value[i]!;\n    count[ch] = (count[ch] ?? 0) + 1;\n  }\n\n  return count;\n}\n```\n\nWITH BUILT-IN HELPERS:\n```ts\nfunction charFrequencyUsingMap(value: string): Map<string, number> {\n  const count = new Map<string, number>();\n\n  for (const ch of value) {\n    count.set(ch, (count.get(ch) ?? 0) + 1);\n  }\n\n  return count;\n}\n```\n\nExample:\n`\"aabcc\"` → `{ a: 2, b: 1, c: 2 }`.\n\nTime O(n). Auxiliary space O(k), where k is the number of distinct characters.",
    productionExample: "Frontend example: autocomplete or analytics code often needs fast frequency/occurrence counts for tokens, events, or search terms.",
    bestPractices: ["State whether counts are case-sensitive.", "Use Map when arbitrary keys or clearer key semantics are needed.", "Use a fixed array when the alphabet is tightly constrained.", "Do not claim O(1) memory when the number of distinct characters grows with n.", "Use counting instead of nested scans for repeated membership work."],
    tradeOffs: "| Method | Benefit | Cost |\n|---|---|---|\n| Object | Simple for string keys | Key semantics / prototype concerns |\n| Map | Explicit key-value structure | More verbose |\n| Fixed array | Very fast for bounded alphabets | Only works for known character ranges |",
    commonMistakes: ["Forgetting case normalization.", "Assuming ASCII when input may contain Unicode.", "Using nested loops for frequency.", "Ignoring duplicate counts.", "Claiming O(1) space for an unbounded character set."],
    followUpQuestions: ["How do you find the first non-repeating character?", "How do you compare two frequency maps?", "When is a fixed array better than Map?", "How do you count words instead of characters?", "How do you handle Unicode?", "Can you solve anagram checking with one map?", "How do you keep only the top-k frequent characters?", "What is the time complexity?", "What is the space complexity?", "How would you stream the counts?"],
    relatedTopics: ["Hashing", "Strings", "Anagram", "Top K Frequency"],
  },
  {
    id: "dsa-m3-23",
    number: "DSA-M3-23",
    title: "Palindrome",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Strings",
    expectedAnswer: "A palindrome reads the same from both ends after applying the problem's normalization rules. The standard interview solution uses two pointers and compares symmetric characters.",
    deepExplanation: "Step 1 — Normalize only if the problem requires ignoring case or punctuation.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction isPalindrome(value: string): boolean {\n  let left = 0;\n  let right = value.length - 1;\n\n  while (left < right) {\n    if (value[left] !== value[right]) return false;\n    left++;\n    right--;\n  }\n\n  return true;\n}\n```\n\nWITH BUILT-IN HELPERS:\n```ts\nfunction isPalindromeUsingBuiltIns(value: string): boolean {\n  const normalized = value.toLowerCase();\n  return normalized === normalized.split(\"\").reverse().join(\"\");\n}\n```\n\nExample:\n`\"racecar\"` → r=r, a=a, c=c → true.\n`\"hello\"` → h!=o → false.\n\nCore method: O(n) time and O(1) auxiliary space.",
    productionExample: "Frontend example: validating symmetric tokens, formatted IDs, or user-entered text can use two-pointer comparison when normalization rules are simple.",
    bestPractices: ["Clarify whether spaces and punctuation count.", "Use two pointers for O(1) extra space.", "Normalize once, not repeatedly inside the loop.", "Handle empty and single-character strings explicitly."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| Two pointers | O(1) extra space | Requires careful normalization |\n| Reverse + compare | Very simple | O(n) extra string/array space |",
    commonMistakes: ["Forgetting normalization requirements.", "Comparing only half incorrectly.", "Using reverse() on Unicode text without considering code points.", "Returning false for empty strings when the problem defines them as palindromes."],
    followUpQuestions: ["How do you ignore spaces?", "How do you ignore punctuation?", "How do you handle Unicode?", "Can it be solved recursively?", "How do you find the longest palindromic substring?", "What is the extra space?", "How do you validate a number palindrome?", "What is the two-pointer invariant?", "How would you test empty input?", "Can you do it with one pass?"],
    relatedTopics: ["Two Pointers", "Strings", "Substrings", "Recursion"],
  },
  {
    id: "dsa-m3-24",
    number: "DSA-M3-24",
    title: "Anagram",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Strings",
    expectedAnswer: "Two strings are anagrams when they contain the same characters with the same frequencies. The safest linear-time interview approach is to compare character counts.",
    deepExplanation: "Step 1 — Reject different lengths.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction isAnagram(a: string, b: string): boolean {\n  if (a.length !== b.length) return false;\n\n  const count: Record<string, number> = Object.create(null);\n\n  for (let i = 0; i < a.length; i++) {\n    const ch = a[i]!;\n    count[ch] = (count[ch] ?? 0) + 1;\n  }\n\n  for (let i = 0; i < b.length; i++) {\n    const ch = b[i]!;\n    if (count[ch] === undefined) return false;\n\n    count[ch]--;\n\n    if (count[ch] < 0) return false;\n  }\n\n  return true;\n}\n```\n\nWITH BUILT-IN:\n```ts\nfunction isAnagramUsingBuiltIns(a: string, b: string): boolean {\n  if (a.length !== b.length) return false;\n\n  return a.split(\"\").sort().join(\"\") ===\n    b.split(\"\").sort().join(\"\");\n}\n```\n\nExample:\n`\"listen\"` + `\"silent\"` → true.\n`\"rat\"` + `\"car\"` → false.\n\nCore solution: O(n) average time and O(k) extra space.",
    productionExample: "Frontend example: comparing normalized tags, search tokens, or user-entered rearrangements is a typical frequency-map use case.",
    bestPractices: ["Check lengths first.", "Use frequency counts for linear time.", "Clarify whether case and spaces should be ignored.", "Use sorting only when O(n log n) is acceptable."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| Frequency map | O(n) average | Extra memory |\n| Sort and compare | Simple | O(n log n), extra allocations |",
    commonMistakes: ["Sorting but claiming O(n).", "Ignoring case/space rules.", "Comparing sets instead of frequencies.", "Forgetting length mismatch."],
    followUpQuestions: ["How do you handle Unicode?", "How do you group anagrams?", "Can you do it with a fixed array?", "How do you ignore spaces?", "How would you solve in O(1) extra space?", "Why does length comparison help?", "What is the complexity of sort()?", "How do you group thousands of strings?", "How do you stream anagram checks?", "What if the alphabet is fixed?"],
    relatedTopics: ["Frequency Counting", "Hashing", "Strings", "Sorting"],
  },
  {
    id: "dsa-m3-25",
    number: "DSA-M3-25",
    title: "Longest common prefix",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Strings",
    expectedAnswer: "The longest common prefix is the longest starting substring shared by every string. A simple approach starts with the first string as the candidate prefix and shrinks it until every string begins with it.",
    deepExplanation: "Step 1 — Start with the first string.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction longestCommonPrefix(strs: readonly string[]): string {\n  if (strs.length === 0) return \"\";\n\n  let prefix = strs[0]!;\n\n  for (let i = 1; i < strs.length; i++) {\n    const value = strs[i]!;\n    let commonLength = 0;\n    const limit = Math.min(prefix.length, value.length);\n\n    while (\n      commonLength < limit &&\n      prefix[commonLength] === value[commonLength]\n    ) {\n      commonLength++;\n    }\n\n    prefix = prefix.slice(0, commonLength);\n\n    if (prefix.length === 0) return \"\";\n  }\n\n  return prefix;\n}\n```\n\nWITH BUILT-IN:\n```ts\nfunction longestCommonPrefixUsingBuiltIns(\n  strs: readonly string[],\n): string {\n  if (strs.length === 0) return \"\";\n\n  return strs.reduce((prefix, value) => {\n    let end = 0;\n    const limit = Math.min(prefix.length, value.length);\n\n    while (end < limit && prefix[end] === value[end]) {\n      end++;\n    }\n\n    return prefix.slice(0, end);\n  });\n}\n```\n\nExample:\n[\"flower\",\"flow\",\"flight\"] → \"fl\".\n\nTime is O(total characters inspected).",
    productionExample: "Frontend example: deriving a shared route prefix or common namespace from a list of strings can use the same shrinking-prefix technique.",
    bestPractices: ["Handle empty input.", "Use the shortest current prefix to limit comparison.", "Return early when the prefix becomes empty.", "Avoid sorting when a direct scan is sufficient."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| Prefix shrinking | Simple and direct | Can inspect several characters repeatedly |\n| Vertical scan | Similar complexity | Needs careful bounds |\n| Sort + compare first/last | Can simplify reasoning | Sorting cost |",
    commonMistakes: ["Comparing only first two strings.", "Using substring instead of prefix semantics.", "Forgetting empty strings.", "Wrong min-length boundary."],
    followUpQuestions: ["How would sorting help?", "What is the best-case complexity?", "What if strings are streamed?", "Can you solve using a Trie?", "How do you find longest common suffix?", "What if input has Unicode?", "How do you avoid repeated slicing?", "How do you test one string?", "What if the shortest string is first?", "Can you compare vertically instead of horizontally?"],
    relatedTopics: ["Strings", "Trie", "Prefix", "Sorting"],
  },
  {
    id: "dsa-m3-26",
    number: "DSA-M3-26",
    title: "String compression",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Strings",
    expectedAnswer: "Run-length string compression replaces consecutive repeated characters with a character plus its run length, such as `aabbbcc → a2b3c2`. The key is to count one run at a time.",
    deepExplanation: "Step 1 — Track the current run.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction compress(value: string): string {\n  if (value.length === 0) return \"\";\n\n  let result = \"\";\n  let runStart = 0;\n\n  for (let i = 1; i <= value.length; i++) {\n    if (i === value.length || value[i] !== value[runStart]) {\n      result += value[runStart] + String(i - runStart);\n      runStart = i;\n    }\n  }\n\n  return result;\n}\n```\n\nWITH BUILT-IN:\n```ts\nfunction compressUsingBuiltIns(value: string): string {\n  if (value.length === 0) return \"\";\n\n  const parts: string[] = [];\n  let start = 0;\n\n  value.split(\"\").forEach((_, index, chars) => {\n    if (index === chars.length - 1 || chars[index + 1] !== chars[start]) {\n      parts.push(chars[start]! + String(index - start + 1));\n      start = index + 1;\n    }\n  });\n\n  return parts.join(\"\");\n}\n```\n\nExample:\n`aabbbcc` → `a2b3c2`.\n\nTime O(n). Extra output storage depends on the compressed result.",
    productionExample: "Production example: this pattern is useful for teaching run-length encoding, though real compression systems use more advanced algorithms and binary formats.",
    bestPractices: ["Use a run boundary condition.", "Handle empty strings.", "Do not confuse run-length compression with general-purpose compression.", "Consider output size before assuming compression always makes data smaller."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| Run-length | Simple, one pass | Poor for alternating characters |\n| General compression | Better ratio | More CPU and implementation complexity |",
    commonMistakes: ["Counting global frequency instead of consecutive runs.", "Forgetting the final run.", "Returning wrong multi-digit counts.", "Assuming compressed output is always shorter."],
    followUpQuestions: ["How do you decompress the string?", "What if counts exceed 9?", "When does RLE increase size?", "How do you compress in place?", "What is the complexity?", "How would you stream the encoding?", "How is RLE different from Huffman coding?", "Can you omit count 1?", "How would you validate malformed input?", "How would Unicode change this?"],
    relatedTopics: ["Strings", "Run Length Encoding", "Two Pointers"],
  },
  {
    id: "dsa-m3-27",
    number: "DSA-M3-27",
    title: "Sliding-window strings",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Strings",
    expectedAnswer: "Sliding window is useful for contiguous substring problems where the window can be expanded and shrunk while maintaining an invariant. A classic string example is the longest substring without repeating characters.",
    deepExplanation: "Step 1 — Keep a window [left, right].\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction lengthOfLongestSubstring(value: string): number {\n  const lastSeen: Record<string, number> = Object.create(null);\n\n  let left = 0;\n  let best = 0;\n\n  for (let right = 0; right < value.length; right++) {\n    const ch = value[right]!;\n    const previous = lastSeen[ch];\n\n    if (previous !== undefined && previous >= left) {\n      left = previous + 1;\n    }\n\n    lastSeen[ch] = right;\n\n    const length = right - left + 1;\n    if (length > best) best = length;\n  }\n\n  return best;\n}\n```\n\nWITH BUILT-IN:\n```ts\nfunction lengthOfLongestSubstringUsingBuiltIns(value: string): number {\n  const lastSeen = new Map<string, number>();\n  let left = 0;\n  let best = 0;\n\n  for (let right = 0; right < value.length; right++) {\n    const ch = value[right]!;\n    const previous = lastSeen.get(ch);\n\n    if (previous !== undefined && previous >= left) {\n      left = previous + 1;\n    }\n\n    lastSeen.set(ch, right);\n    best = Math.max(best, right - left + 1);\n  }\n\n  return best;\n}\n```\n\nExample:\n`abcabcbb` → longest substring without repeats is `abc` → answer `3`.\n\nTime O(n) average.",
    productionExample: "Frontend example: scanning a stream of recent tokens/events while maintaining a constraint is a common sliding-window pattern.",
    bestPractices: ["Define the window invariant before coding.", "Move left only forward.", "Store the last index or counts depending on the constraint.", "Do not reset the entire window when one character violates the rule."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| Sliding window + Map | O(n) average | Extra state |\n| Brute force substrings | Easy to derive | O(n²) or worse |",
    commonMistakes: ["Moving left backwards.", "Forgetting to check previous index against left.", "Confusing substring with subsequence.", "Using window logic when the condition is not monotonic."],
    followUpQuestions: ["Longest substring with at most k distinct characters?", "Minimum window substring?", "Longest repeating character replacement?", "How does count-based window differ from last-index window?", "When does sliding window fail?", "How do you prove O(n)?", "What is the space complexity?", "Can you solve with a fixed array?", "How would you handle Unicode?", "How would you test repeated characters?"],
    relatedTopics: ["Sliding Window", "Hashing", "Strings", "Two Pointers"],
  },
  {
    id: "dsa-m3-28",
    number: "DSA-M3-28",
    title: "String parsing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Strings",
    expectedAnswer: "String parsing means reading a string according to delimiters, syntax, or grammar rules and producing structured output. Good interview solutions define tokens, boundaries, malformed-input behavior, and complexity before coding.",
    deepExplanation: "Step 1 — Split only when the delimiter rule is simple.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction parseCsvLine(value: string): string[] {\n  const result: string[] = [];\n  let current = \"\";\n\n  for (let i = 0; i < value.length; i++) {\n    const ch = value[i]!;\n\n    if (ch === \",\") {\n      result.push(current);\n      current = \"\";\n    } else {\n      current += ch;\n    }\n  }\n\n  result.push(current);\n  return result;\n}\n```\n\nWITH BUILT-IN:\n```ts\nfunction parseCsvLineUsingBuiltIns(value: string): string[] {\n  return value.split(\",\");\n}\n```\n\nExample:\n`\"Alice,25,India\"` → `[\"Alice\",\"25\",\"India\"]`.\n\nImportant: this is a simple comma parser, not a full RFC-compliant CSV parser with quoted commas and escaped quotes.",
    productionExample: "Frontend example: parsing query parameters, route segments, or simple configuration strings often uses delimiter-based parsing; use a real parser for complex formats rather than inventing a partial one.",
    bestPractices: ["Define the grammar first.", "Separate tokenization from validation.", "Specify malformed-input behavior.", "Do not claim a simple split() is a full CSV/parser implementation.", "Avoid regular expressions when a small state machine is clearer."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| split() | Simple and readable | Limited grammar support |\n| Manual parser | Full control | More code |\n| RegExp | Compact for regular patterns | Harder to maintain for complex grammars |",
    commonMistakes: ["Ignoring escaped delimiters.", "Assuming CSV is just split(',').", "Mixing parsing and business logic.", "Not defining malformed input behavior."],
    followUpQuestions: ["How would you parse quoted CSV?", "When should you use a state machine?", "Regex vs parser?", "How do you validate tokens?", "How do you parse query strings?", "How do you avoid quadratic parsing?", "How would you handle Unicode?", "How do you stream a large text file?", "How do you test malformed input?", "What makes a grammar ambiguous?"],
    relatedTopics: ["Strings", "Parsing", "Regular Expressions", "State Machines"],
  },
  {
    id: "dsa-m3-29",
    number: "DSA-M3-29",
    title: "Unicode and JavaScript strings",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Strings",
    expectedAnswer: "JavaScript strings are sequences of UTF-16 code units. `length` and indexing operate on code units, so some Unicode characters use two code units. For user-visible characters, `for...of`, code-point iteration, or `Intl.Segmenter` may be more appropriate depending on the requirement.",
    deepExplanation: "Step 1 — Understand the representation:\n```ts\nfunction codeUnitLength(value: string): number {\n  return value.length;\n}\n```\n\nFor `\"😀\"`, `value.length` is 2 because the emoji uses a surrogate pair.\n\nStep 2 — WITHOUT BUILT-IN / CORE-style code-point iteration:\n```ts\nfunction codePointCount(value: string): number {\n  let count = 0;\n\n  for (let i = 0; i < value.length; i++) {\n    const code = value.charCodeAt(i);\n\n    if (\n      code >= 0xd800 &&\n      code <= 0xdbff &&\n      i + 1 < value.length\n    ) {\n      const next = value.charCodeAt(i + 1);\n\n      if (next >= 0xdc00 && next <= 0xdfff) {\n        i++;\n      }\n    }\n\n    count++;\n  }\n\n  return count;\n}\n```\n\nStep 3 — WITH BUILT-IN:\n```ts\nfunction codePointCountUsingBuiltIns(value: string): number {\n  return [...value].length;\n}\n```\n\nStep 4 — Interview takeaway:\nUse code units when the problem explicitly talks about JavaScript string indexing. Use code points or grapheme-aware tooling when the requirement is about user-visible characters.",
    productionExample: "Frontend example: character counters, cursor movement, truncation, and text editors can break if code units are treated as user-visible characters. Requirements must decide whether “character” means code unit, code point, or grapheme cluster.",
    bestPractices: ["Clarify what 'character' means.", "Use for...of or spread for code-point iteration when appropriate.", "Use Intl.Segmenter for grapheme-cluster requirements when available.", "Do not reverse arbitrary Unicode text with naive split('').reverse().join('') if user-visible characters matter."],
    tradeOffs: "| Representation | Benefit | Limitation |\n|---|---|---|\n| UTF-16 code units | Native JS indexing | Surrogate pairs can count as 2 |\n| Code points | Better logical Unicode characters | Combining marks can still span multiple code points |\n| Grapheme clusters | Closest to user-visible characters | More complex / segmentation cost |",
    commonMistakes: ["Assuming length equals visible character count.", "Using charAt/indexing for all Unicode cases.", "Breaking surrogate pairs.", "Ignoring combining marks and emoji sequences."],
    followUpQuestions: ["What is a surrogate pair?", "What does for...of iterate?", "What is a grapheme cluster?", "Why can emoji length be greater than one?", "How does Intl.Segmenter help?", "How would you safely reverse user-visible text?", "What is codePointAt()?", "Why can normalization matter?", "What is NFC/NFD normalization?", "How do you test Unicode edge cases?"],
    relatedTopics: ["Strings", "Unicode", "Intl.Segmenter", "UTF-16"],
  },
  {
    id: "dsa-m3-30",
    number: "DSA-M3-30",
    title: "Production text-processing patterns",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Strings",
    expectedAnswer: "Production string processing should separate normalization, parsing, validation, transformation, and presentation. The right algorithm depends on input size, Unicode requirements, security constraints, and whether the operation is on a hot path.",
    deepExplanation: "Step 1 — Normalize explicitly.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction normalizeSearchText(value: string): string {\n  let result = \"\";\n\n  for (let i = 0; i < value.length; i++) {\n    const ch = value[i]!;\n\n    if (ch === \" \" || ch === \"\\t\" || ch === \"\\n\") {\n      if (result.length > 0 && result[result.length - 1] !== \" \") {\n        result += \" \";\n      }\n    } else {\n      result += ch.toLowerCase();\n    }\n  }\n\n  return result.trim();\n}\n```\n\nWITH BUILT-IN:\n```ts\nfunction normalizeSearchTextUsingBuiltIns(value: string): string {\n  return value\n    .trim()\n    .replace(/\\s+/g, \" \")\n    .toLowerCase();\n}\n```\n\nExample:\n`\"  Hello   WORLD  \"` → `\"hello world\"`.\n\nStep 2 — Production caution:\nNormalization for display/search is not the same as security validation. URLs, HTML, user names, identifiers, and user-visible text may require different rules and potentially dedicated libraries/APIs.",
    productionExample: "Frontend example: search boxes often normalize whitespace and case before filtering, while HTML escaping, URL parsing, or security-sensitive validation should use dedicated platform APIs and well-defined rules.",
    bestPractices: ["Separate normalization from validation.", "Document whether matching is case-sensitive.", "Define Unicode behavior.", "Avoid regex-only parsing for complex grammars.", "Prefer platform APIs for URLs, dates, and structured formats.", "Do not treat display normalization as security sanitization."],
    tradeOffs: "| Strategy | Benefit | Cost |\n|---|---|---|\n| Built-in string methods | Concise and readable | Can allocate intermediate strings |\n| Manual scanner | Fine-grained control | More code |\n| Specialized parser/API | Correct for structured data | API/library complexity |",
    commonMistakes: ["Using ad-hoc parsing for URLs or dates.", "Calling string normalization security sanitization.", "Ignoring Unicode normalization.", "Doing expensive full-string transforms on every keystroke without considering debounce/memoization."],
    followUpQuestions: ["How do you normalize search input?", "What is the difference between normalization and sanitization?", "How do you parse URLs safely?", "How do you handle Unicode normalization?", "How do you process huge text files?", "How do you optimize string work in React?", "When should you debounce text processing?", "How do you prevent regex backtracking problems?", "How do you test malformed Unicode?", "How do you benchmark string transformations?"],
    relatedTopics: ["Strings", "Parsing", "Unicode", "Performance", "Security"],
  },
];

export const MOCK_DSA_MODULE3_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'DSA',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));