// Derived from frontend/src/document/Part_6_Browser_Internals_Web_APIs_Master_Handbook.md (Module 18 — Browser Compatibility).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question, Execution/Flow, Advanced Production Example, Performance/Security Notes,
// Best Practices, Advantages/Disadvantages, Common Mistakes/Interview Traps, Senior
// Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "jsbrowser-m18-256",
    number: "JSBROWSER-M18-256",
    title: "Feature detection",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    category: "Browser Compatibility",
    expectedAnswer: "Explain **Feature detection** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Feature detection**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Feature detection: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Feature detection: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Feature detection** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Feature detection**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m18-257",
    number: "JSBROWSER-M18-257",
    title: "Can I use style support concepts",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    category: "Browser Compatibility",
    expectedAnswer: "Explain **Can I use style support concepts** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Can I use style support concepts**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Can I use style support concepts: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Can I use style support concepts: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Can I use style support concepts** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Can I use style support concepts**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m18-258",
    number: "JSBROWSER-M18-258",
    title: "Polyfills",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    category: "Browser Compatibility",
    expectedAnswer: "Explain **Polyfills** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Polyfills**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Polyfills: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Polyfills: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Polyfills** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Polyfills**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m18-259",
    number: "JSBROWSER-M18-259",
    title: "Transpilation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    category: "Browser Compatibility",
    expectedAnswer: "Explain **Transpilation** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Transpilation**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Transpilation: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Transpilation: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Transpilation** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Transpilation**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m18-260",
    number: "JSBROWSER-M18-260",
    title: "Babel",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    category: "Browser Compatibility",
    expectedAnswer: "Explain **Babel** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Babel**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Babel: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Babel: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Babel** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Babel**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m18-261",
    number: "JSBROWSER-M18-261",
    title: "Progressive enhancement",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    category: "Browser Compatibility",
    expectedAnswer: "Explain **Progressive enhancement** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Progressive enhancement**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Progressive enhancement: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Progressive enhancement: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Progressive enhancement** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Progressive enhancement**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m18-262",
    number: "JSBROWSER-M18-262",
    title: "Graceful degradation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    category: "Browser Compatibility",
    expectedAnswer: "Explain **Graceful degradation** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Graceful degradation**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Graceful degradation: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Graceful degradation: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Graceful degradation** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Graceful degradation**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m18-263",
    number: "JSBROWSER-M18-263",
    title: "Vendor prefixes",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    category: "Browser Compatibility",
    expectedAnswer: "Explain **Vendor prefixes** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Vendor prefixes**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Vendor prefixes: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Vendor prefixes: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Vendor prefixes** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Vendor prefixes**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m18-264",
    number: "JSBROWSER-M18-264",
    title: "Cross-browser testing",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    category: "Browser Compatibility",
    expectedAnswer: "Explain **Cross-browser testing** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Cross-browser testing**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Cross-browser testing: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Cross-browser testing: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Cross-browser testing** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Cross-browser testing**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---"],
  },
  {
    id: "jsbrowser-m18-265",
    number: "JSBROWSER-M18-265",
    title: "Compatibility strategy",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    category: "Browser Compatibility",
    expectedAnswer: "Explain **Compatibility strategy** from a browser-engine and production frontend perspective. What happens internally, why does it matter, and when should a senior engineer choose or avoid it?",
    deepExplanation: "Execution flow: The application invokes or triggers **Compatibility strategy**. → The browser validates the relevant document, origin, permission or lifecycle state. → The responsible browser subsystem performs the operation. → The result is delivered synchronously or through an asynchronous callback/event. → Any DOM, network, storage, rendering or memory consequences become observable.\n\nProduction pattern:\n\n```js\n// Production rule for Compatibility strategy: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```\n\nExpected output:\n\n```text\n42\n```\n\nPerformance: Avoid unnecessary main-thread work. Measure before optimizing with Performance, Network and Memory tooling. Batch DOM reads/writes when layout work is involved. Use caching, streaming, lazy loading and resource hints when they match the workload. Consider memory retention and cleanup for listeners, observers, workers and long-lived resources.\n\nSecurity: Respect the Same-Origin Policy, CORS, CSP, secure contexts, permissions, cookie attributes and iframe isolation. Never treat client-side checks as a replacement for server authorization.",
    productionExample: "```js\n// Production rule for Compatibility strategy: isolate the capability behind a small boundary.\nexport function useBrowserCapability() {\n  if (typeof window === \"undefined\") return false;\n  return true;\n}\n```",
    bestPractices: ["Encapsulate **Compatibility strategy** behind a small application boundary.","Feature-detect where appropriate.","Handle failure and unsupported environments.","Instrument production-critical behavior.","Clean up listeners, observers, workers and resources."],
    tradeOffs: "Advantages: Standardized browser capability when broadly supported.; Can improve user experience, performance or reliability when used appropriately.. Disadvantages: Browser support and policy restrictions vary.; Some APIs require secure contexts, permissions or user activation.; Poor usage can increase memory, CPU, network or rendering cost..",
    commonMistakes: ["Assuming all browsers implement an API identically.","Ignoring security policies.","Performing expensive work on the main thread.","Forgetting lifecycle cleanup.","Optimizing without measurement.","Interview trap: Confusing browser APIs with ECMAScript language features.","Interview trap: Saying that every callback is a macrotask without considering the specific scheduling mechanism.","Interview trap: Treating the DOM, render tree and CSSOM as the same structure.","Interview trap: Claiming that `localStorage` is suitable for sensitive authentication secrets."],
    followUpQuestions: ["What is the browser's internal cost of **Compatibility strategy**?","Which work happens on the main thread?","Which lifecycle or security restrictions apply?","How would Chrome DevTools expose the behavior?","What changes across Chrome, Firefox, Safari and Edge?","How would you design a production abstraction around it?"],
    relatedTopics: ["DOM","CSSOM","Rendering Pipeline","Event Loop","Network","Storage","Security","Performance","DevTools","Browser Compatibility","---","# Module 19 – Senior Browser Internals","**Questions 266–285**"],
  },
];

export const MOCK_BROWSER_INTERNALS_MODULE18_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Browser/Web APIs',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
