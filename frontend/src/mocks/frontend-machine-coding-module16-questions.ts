// Derived from frontend/src/document/Frontend_Architecture_Design_Patterns_Enterprise_Development_Master_Handbook.md (Module 16 — Enterprise Security Architecture).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "arch-m16-1",
    number: "ARCH-M16-1",
    title: "JWT",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise Security Architecture",
    expectedAnswer: "JWT is a signed token format carrying claims. Servers must validate signature, issuer, audience, expiry, and other required claims before trusting it.",
    deepExplanation: "Step 1 — Understand JWT.\n\nJWT is a signed token format carrying claims. Servers must validate signature, issuer, audience, expiry, and other required claims before trusting it.\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest contract, implement the simplest path, then add the required operational safeguards.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface Claims {\n  sub: string;\n  exp: number;\n}\n\nfunction validClaims(\n  claims: Claims,\n  now: number,\n): boolean {\n  return claims.sub.length > 0 && claims.exp > now;\n}\n\nconst valid = validClaims({\n  sub: \"u1\",\n  exp: 2000,\n}, 1000);\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst token = \"header.payload.signature\";\nconst parts = token.split(\".\");\nconst shapeIsValid = parts.length === 3;\n```\n\nStep 3 — Production example:\nUse JWT as a security boundary with explicit validation and least-privilege behavior.\n\nStep 4 — Edge cases:\nConsider invalid input, lifecycle cleanup, failure handling, compatibility, and observability at the boundary.\n\nStep 5 — Senior interview takeaway:\nExplain what problem the approach solves, what it does not solve, the simplest alternative, and the main production trade-off.",
    productionExample: "Use JWT as a security boundary with explicit validation and least-privilege behavior.",
    bestPractices: ["Define the responsibility and public contract before choosing an implementation.", "Keep data or behavior ownership explicit and avoid hidden global coupling.", "Handle invalid input, failure, cleanup, and lifecycle boundaries.", "Prefer the smallest abstraction that solves the real problem.", "Add automated tests and operational checks for important behavior."],
    tradeOffs: "Security controls reduce attack surface, but they require careful configuration, testing, and ongoing maintenance.",
    commonMistakes: ["Using the concept because it sounds enterprise-grade instead of solving a concrete problem.", "Mixing responsibilities so business rules and infrastructure become inseparable.", "Ignoring edge cases, cleanup, compatibility, or security implications.", "Creating an abstraction before the change pattern is understood.", "Treating a sample implementation as production-complete without operational safeguards."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the source of truth or owner?", "How would you test it?", "What are the main failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["Enterprise Security Architecture"],
  },
  {
    id: "arch-m16-2",
    number: "ARCH-M16-2",
    title: "OAuth",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise Security Architecture",
    expectedAnswer: "OAuth is an authorization framework for delegated access. Client flows and token handling should follow the identity provider's supported secure flow.",
    deepExplanation: "Step 1 — Understand OAuth.\n\nOAuth is an authorization framework for delegated access. Client flows and token handling should follow the identity provider's supported secure flow.\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest contract, implement the simplest path, then add the required operational safeguards.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface AuditRecord {\n  actorId: string;\n  action: string;\n  resourceId: string;\n}\n\nfunction createAuditRecord(\n  actorId: string,\n  action: string,\n  resourceId: string,\n): AuditRecord {\n  return { actorId, action, resourceId };\n}\n\nconst record = createAuditRecord(\n  \"u1\",\n  \"read\",\n  \"doc-1\",\n);\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst record = {\n  actorId: \"u1\",\n  action: \"read\",\n  resourceId: \"doc-1\",\n};\n\nconst json = JSON.stringify(record);\n```\n\nStep 3 — Production example:\nUse OAuth as a security boundary with explicit validation and least-privilege behavior.\n\nStep 4 — Edge cases:\nConsider invalid input, lifecycle cleanup, failure handling, compatibility, and observability at the boundary.\n\nStep 5 — Senior interview takeaway:\nExplain what problem the approach solves, what it does not solve, the simplest alternative, and the main production trade-off.",
    productionExample: "Use OAuth as a security boundary with explicit validation and least-privilege behavior.",
    bestPractices: ["Define the responsibility and public contract before choosing an implementation.", "Keep data or behavior ownership explicit and avoid hidden global coupling.", "Handle invalid input, failure, cleanup, and lifecycle boundaries.", "Prefer the smallest abstraction that solves the real problem.", "Add automated tests and operational checks for important behavior."],
    tradeOffs: "Security controls reduce attack surface, but they require careful configuration, testing, and ongoing maintenance.",
    commonMistakes: ["Using the concept because it sounds enterprise-grade instead of solving a concrete problem.", "Mixing responsibilities so business rules and infrastructure become inseparable.", "Ignoring edge cases, cleanup, compatibility, or security implications.", "Creating an abstraction before the change pattern is understood.", "Treating a sample implementation as production-complete without operational safeguards."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the source of truth or owner?", "How would you test it?", "What are the main failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["Enterprise Security Architecture"],
  },
  {
    id: "arch-m16-3",
    number: "ARCH-M16-3",
    title: "Secure Storage",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise Security Architecture",
    expectedAnswer: "Secure storage means minimizing sensitive data in browser-accessible storage and preferring server-managed or protected mechanisms where possible.",
    deepExplanation: "Step 1 — Understand Secure Storage.\n\nSecure storage means minimizing sensitive data in browser-accessible storage and preferring server-managed or protected mechanisms where possible.\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest contract, implement the simplest path, then add the required operational safeguards.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface AuditRecord {\n  actorId: string;\n  action: string;\n  resourceId: string;\n}\n\nfunction createAuditRecord(\n  actorId: string,\n  action: string,\n  resourceId: string,\n): AuditRecord {\n  return { actorId, action, resourceId };\n}\n\nconst record = createAuditRecord(\n  \"u1\",\n  \"read\",\n  \"doc-1\",\n);\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst record = {\n  actorId: \"u1\",\n  action: \"read\",\n  resourceId: \"doc-1\",\n};\n\nconst json = JSON.stringify(record);\n```\n\nStep 3 — Production example:\nUse Secure Storage as a security boundary with explicit validation and least-privilege behavior.\n\nStep 4 — Edge cases:\nConsider invalid input, lifecycle cleanup, failure handling, compatibility, and observability at the boundary.\n\nStep 5 — Senior interview takeaway:\nExplain what problem the approach solves, what it does not solve, the simplest alternative, and the main production trade-off.",
    productionExample: "Use Secure Storage as a security boundary with explicit validation and least-privilege behavior.",
    bestPractices: ["Define the responsibility and public contract before choosing an implementation.", "Keep data or behavior ownership explicit and avoid hidden global coupling.", "Handle invalid input, failure, cleanup, and lifecycle boundaries.", "Prefer the smallest abstraction that solves the real problem.", "Add automated tests and operational checks for important behavior."],
    tradeOffs: "Security controls reduce attack surface, but they require careful configuration, testing, and ongoing maintenance.",
    commonMistakes: ["Using the concept because it sounds enterprise-grade instead of solving a concrete problem.", "Mixing responsibilities so business rules and infrastructure become inseparable.", "Ignoring edge cases, cleanup, compatibility, or security implications.", "Creating an abstraction before the change pattern is understood.", "Treating a sample implementation as production-complete without operational safeguards."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the source of truth or owner?", "How would you test it?", "What are the main failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["Enterprise Security Architecture"],
  },
  {
    id: "arch-m16-4",
    number: "ARCH-M16-4",
    title: "CSP",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise Security Architecture",
    expectedAnswer: "Content Security Policy restricts which sources can execute or load content, reducing the impact of injection vulnerabilities.",
    deepExplanation: "Step 1 — Understand CSP.\n\nContent Security Policy restricts which sources can execute or load content, reducing the impact of injection vulnerabilities.\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest contract, implement the simplest path, then add the required operational safeguards.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface Policy {\n  scriptSources: string[];\n}\n\nfunction allowsScript(\n  policy: Policy,\n  source: string,\n): boolean {\n  for (let i = 0; i < policy.scriptSources.length; i += 1) {\n    if (policy.scriptSources[i] === source) return true;\n  }\n  return false;\n}\n\nconst allowed = allowsScript({\n  scriptSources: [\"'self'\"],\n}, \"'self'\");\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst scriptSources = new Set([\"'self'\"]);\nconst allowed = scriptSources.has(\"'self'\");\n```\n\nStep 3 — Production example:\nUse CSP as a security boundary with explicit validation and least-privilege behavior.\n\nStep 4 — Edge cases:\nConsider invalid input, lifecycle cleanup, failure handling, compatibility, and observability at the boundary.\n\nStep 5 — Senior interview takeaway:\nExplain what problem the approach solves, what it does not solve, the simplest alternative, and the main production trade-off.",
    productionExample: "Use CSP as a security boundary with explicit validation and least-privilege behavior.",
    bestPractices: ["Define the responsibility and public contract before choosing an implementation.", "Keep data or behavior ownership explicit and avoid hidden global coupling.", "Handle invalid input, failure, cleanup, and lifecycle boundaries.", "Prefer the smallest abstraction that solves the real problem.", "Add automated tests and operational checks for important behavior."],
    tradeOffs: "Security controls reduce attack surface, but they require careful configuration, testing, and ongoing maintenance.",
    commonMistakes: ["Using the concept because it sounds enterprise-grade instead of solving a concrete problem.", "Mixing responsibilities so business rules and infrastructure become inseparable.", "Ignoring edge cases, cleanup, compatibility, or security implications.", "Creating an abstraction before the change pattern is understood.", "Treating a sample implementation as production-complete without operational safeguards."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the source of truth or owner?", "How would you test it?", "What are the main failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["Enterprise Security Architecture"],
  },
  {
    id: "arch-m16-5",
    number: "ARCH-M16-5",
    title: "CORS",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise Security Architecture",
    expectedAnswer: "CORS controls whether browsers allow one origin to read responses from another origin. It is a browser policy, not an authentication mechanism.",
    deepExplanation: "Step 1 — Understand CORS.\n\nCORS controls whether browsers allow one origin to read responses from another origin. It is a browser policy, not an authentication mechanism.\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest contract, implement the simplest path, then add the required operational safeguards.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface HeaderMap {\n  values: Record<string, string>;\n}\n\nfunction readHeader(\n  headers: HeaderMap,\n  name: string,\n): string | null {\n  return headers.values[name] ?? null;\n}\n\nconst value = readHeader({\n  values: {\n    \"access-control-allow-origin\": \"https://app.example.com\",\n  },\n}, \"access-control-allow-origin\");\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst headers = new Headers();\nheaders.set(\n  \"Access-Control-Allow-Origin\",\n  \"https://app.example.com\",\n);\n\nconst value = headers.get(\n  \"Access-Control-Allow-Origin\",\n);\n```\n\nStep 3 — Production example:\nUse CORS as a security boundary with explicit validation and least-privilege behavior.\n\nStep 4 — Edge cases:\nConsider invalid input, lifecycle cleanup, failure handling, compatibility, and observability at the boundary.\n\nStep 5 — Senior interview takeaway:\nExplain what problem the approach solves, what it does not solve, the simplest alternative, and the main production trade-off.",
    productionExample: "Use CORS as a security boundary with explicit validation and least-privilege behavior.",
    bestPractices: ["Define the responsibility and public contract before choosing an implementation.", "Keep data or behavior ownership explicit and avoid hidden global coupling.", "Handle invalid input, failure, cleanup, and lifecycle boundaries.", "Prefer the smallest abstraction that solves the real problem.", "Add automated tests and operational checks for important behavior."],
    tradeOffs: "Security controls reduce attack surface, but they require careful configuration, testing, and ongoing maintenance.",
    commonMistakes: ["Using the concept because it sounds enterprise-grade instead of solving a concrete problem.", "Mixing responsibilities so business rules and infrastructure become inseparable.", "Ignoring edge cases, cleanup, compatibility, or security implications.", "Creating an abstraction before the change pattern is understood.", "Treating a sample implementation as production-complete without operational safeguards."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the source of truth or owner?", "How would you test it?", "What are the main failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["Enterprise Security Architecture"],
  },
  {
    id: "arch-m16-6",
    number: "ARCH-M16-6",
    title: "XSS",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise Security Architecture",
    expectedAnswer: "XSS occurs when attacker-controlled content is interpreted as executable markup or script. Prevention is based on contextual output encoding, safe DOM APIs, and content controls.",
    deepExplanation: "Step 1 — Understand XSS.\n\nXSS occurs when attacker-controlled content is interpreted as executable markup or script. Prevention is based on contextual output encoding, safe DOM APIs, and content controls.\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest contract, implement the simplest path, then add the required operational safeguards.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface AuditRecord {\n  actorId: string;\n  action: string;\n  resourceId: string;\n}\n\nfunction createAuditRecord(\n  actorId: string,\n  action: string,\n  resourceId: string,\n): AuditRecord {\n  return { actorId, action, resourceId };\n}\n\nconst record = createAuditRecord(\n  \"u1\",\n  \"read\",\n  \"doc-1\",\n);\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst record = {\n  actorId: \"u1\",\n  action: \"read\",\n  resourceId: \"doc-1\",\n};\n\nconst json = JSON.stringify(record);\n```\n\nStep 3 — Production example:\nUse XSS as a security boundary with explicit validation and least-privilege behavior.\n\nStep 4 — Edge cases:\nConsider invalid input, lifecycle cleanup, failure handling, compatibility, and observability at the boundary.\n\nStep 5 — Senior interview takeaway:\nExplain what problem the approach solves, what it does not solve, the simplest alternative, and the main production trade-off.",
    productionExample: "Use XSS as a security boundary with explicit validation and least-privilege behavior.",
    bestPractices: ["Define the responsibility and public contract before choosing an implementation.", "Keep data or behavior ownership explicit and avoid hidden global coupling.", "Handle invalid input, failure, cleanup, and lifecycle boundaries.", "Prefer the smallest abstraction that solves the real problem.", "Add automated tests and operational checks for important behavior."],
    tradeOffs: "Security controls reduce attack surface, but they require careful configuration, testing, and ongoing maintenance.",
    commonMistakes: ["Using the concept because it sounds enterprise-grade instead of solving a concrete problem.", "Mixing responsibilities so business rules and infrastructure become inseparable.", "Ignoring edge cases, cleanup, compatibility, or security implications.", "Creating an abstraction before the change pattern is understood.", "Treating a sample implementation as production-complete without operational safeguards."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the source of truth or owner?", "How would you test it?", "What are the main failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["Enterprise Security Architecture"],
  },
  {
    id: "arch-m16-7",
    number: "ARCH-M16-7",
    title: "CSRF",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise Security Architecture",
    expectedAnswer: "CSRF abuses ambient credentials to trigger unwanted state-changing requests. Defenses include same-site cookies, CSRF tokens, and request/origin checks where appropriate.",
    deepExplanation: "Step 1 — Understand CSRF.\n\nCSRF abuses ambient credentials to trigger unwanted state-changing requests. Defenses include same-site cookies, CSRF tokens, and request/origin checks where appropriate.\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest contract, implement the simplest path, then add the required operational safeguards.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface AuditRecord {\n  actorId: string;\n  action: string;\n  resourceId: string;\n}\n\nfunction createAuditRecord(\n  actorId: string,\n  action: string,\n  resourceId: string,\n): AuditRecord {\n  return { actorId, action, resourceId };\n}\n\nconst record = createAuditRecord(\n  \"u1\",\n  \"read\",\n  \"doc-1\",\n);\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst record = {\n  actorId: \"u1\",\n  action: \"read\",\n  resourceId: \"doc-1\",\n};\n\nconst json = JSON.stringify(record);\n```\n\nStep 3 — Production example:\nUse CSRF as a security boundary with explicit validation and least-privilege behavior.\n\nStep 4 — Edge cases:\nConsider invalid input, lifecycle cleanup, failure handling, compatibility, and observability at the boundary.\n\nStep 5 — Senior interview takeaway:\nExplain what problem the approach solves, what it does not solve, the simplest alternative, and the main production trade-off.",
    productionExample: "Use CSRF as a security boundary with explicit validation and least-privilege behavior.",
    bestPractices: ["Define the responsibility and public contract before choosing an implementation.", "Keep data or behavior ownership explicit and avoid hidden global coupling.", "Handle invalid input, failure, cleanup, and lifecycle boundaries.", "Prefer the smallest abstraction that solves the real problem.", "Add automated tests and operational checks for important behavior."],
    tradeOffs: "Security controls reduce attack surface, but they require careful configuration, testing, and ongoing maintenance.",
    commonMistakes: ["Using the concept because it sounds enterprise-grade instead of solving a concrete problem.", "Mixing responsibilities so business rules and infrastructure become inseparable.", "Ignoring edge cases, cleanup, compatibility, or security implications.", "Creating an abstraction before the change pattern is understood.", "Treating a sample implementation as production-complete without operational safeguards."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the source of truth or owner?", "How would you test it?", "What are the main failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["Enterprise Security Architecture"],
  },
  {
    id: "arch-m16-8",
    number: "ARCH-M16-8",
    title: "Security Headers",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise Security Architecture",
    expectedAnswer: "Security headers harden browser behavior for framing, content types, transport, referrer behavior, and script execution.",
    deepExplanation: "Step 1 — Understand Security Headers.\n\nSecurity headers harden browser behavior for framing, content types, transport, referrer behavior, and script execution.\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest contract, implement the simplest path, then add the required operational safeguards.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface HeaderMap {\n  values: Record<string, string>;\n}\n\nfunction readHeader(\n  headers: HeaderMap,\n  name: string,\n): string | null {\n  return headers.values[name] ?? null;\n}\n\nconst value = readHeader({\n  values: {\n    \"access-control-allow-origin\": \"https://app.example.com\",\n  },\n}, \"access-control-allow-origin\");\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst headers = new Headers();\nheaders.set(\n  \"Access-Control-Allow-Origin\",\n  \"https://app.example.com\",\n);\n\nconst value = headers.get(\n  \"Access-Control-Allow-Origin\",\n);\n```\n\nStep 3 — Production example:\nUse Security Headers as a security boundary with explicit validation and least-privilege behavior.\n\nStep 4 — Edge cases:\nConsider invalid input, lifecycle cleanup, failure handling, compatibility, and observability at the boundary.\n\nStep 5 — Senior interview takeaway:\nExplain what problem the approach solves, what it does not solve, the simplest alternative, and the main production trade-off.",
    productionExample: "Use Security Headers as a security boundary with explicit validation and least-privilege behavior.",
    bestPractices: ["Define the responsibility and public contract before choosing an implementation.", "Keep data or behavior ownership explicit and avoid hidden global coupling.", "Handle invalid input, failure, cleanup, and lifecycle boundaries.", "Prefer the smallest abstraction that solves the real problem.", "Add automated tests and operational checks for important behavior."],
    tradeOffs: "Security controls reduce attack surface, but they require careful configuration, testing, and ongoing maintenance.",
    commonMistakes: ["Using the concept because it sounds enterprise-grade instead of solving a concrete problem.", "Mixing responsibilities so business rules and infrastructure become inseparable.", "Ignoring edge cases, cleanup, compatibility, or security implications.", "Creating an abstraction before the change pattern is understood.", "Treating a sample implementation as production-complete without operational safeguards."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the source of truth or owner?", "How would you test it?", "What are the main failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["Enterprise Security Architecture"],
  },
  {
    id: "arch-m16-9",
    number: "ARCH-M16-9",
    title: "Threat Modeling",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise Security Architecture",
    expectedAnswer: "Threat modeling identifies assets, trust boundaries, threats, mitigations, and residual risk before implementation.",
    deepExplanation: "Step 1 — Understand Threat Modeling.\n\nThreat modeling identifies assets, trust boundaries, threats, mitigations, and residual risk before implementation.\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest contract, implement the simplest path, then add the required operational safeguards.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface Threat {\n  asset: string;\n  likelihood: number;\n  impact: number;\n}\n\nfunction riskScore(threat: Threat): number {\n  return threat.likelihood * threat.impact;\n}\n\nconst score = riskScore({\n  asset: \"session\",\n  likelihood: 3,\n  impact: 5,\n});\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst threat = {\n  likelihood: 3,\n  impact: 5,\n};\n\nconst score = threat.likelihood * threat.impact;\n```\n\nStep 3 — Production example:\nUse Threat Modeling as a security boundary with explicit validation and least-privilege behavior.\n\nStep 4 — Edge cases:\nConsider invalid input, lifecycle cleanup, failure handling, compatibility, and observability at the boundary.\n\nStep 5 — Senior interview takeaway:\nExplain what problem the approach solves, what it does not solve, the simplest alternative, and the main production trade-off.",
    productionExample: "Use Threat Modeling as a security boundary with explicit validation and least-privilege behavior.",
    bestPractices: ["Define the responsibility and public contract before choosing an implementation.", "Keep data or behavior ownership explicit and avoid hidden global coupling.", "Handle invalid input, failure, cleanup, and lifecycle boundaries.", "Prefer the smallest abstraction that solves the real problem.", "Add automated tests and operational checks for important behavior."],
    tradeOffs: "Security controls reduce attack surface, but they require careful configuration, testing, and ongoing maintenance.",
    commonMistakes: ["Using the concept because it sounds enterprise-grade instead of solving a concrete problem.", "Mixing responsibilities so business rules and infrastructure become inseparable.", "Ignoring edge cases, cleanup, compatibility, or security implications.", "Creating an abstraction before the change pattern is understood.", "Treating a sample implementation as production-complete without operational safeguards."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the source of truth or owner?", "How would you test it?", "What are the main failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["Enterprise Security Architecture"],
  },
  {
    id: "arch-m16-10",
    number: "ARCH-M16-10",
    title: "Audit Logs",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise Security Architecture",
    expectedAnswer: "Audit logs record security- or business-significant actions with actor, resource, time, and outcome information.",
    deepExplanation: "Step 1 — Understand Audit Logs.\n\nAudit logs record security- or business-significant actions with actor, resource, time, and outcome information.\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest contract, implement the simplest path, then add the required operational safeguards.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface AuditRecord {\n  actorId: string;\n  action: string;\n  resourceId: string;\n}\n\nfunction createAuditRecord(\n  actorId: string,\n  action: string,\n  resourceId: string,\n): AuditRecord {\n  return { actorId, action, resourceId };\n}\n\nconst record = createAuditRecord(\n  \"u1\",\n  \"read\",\n  \"doc-1\",\n);\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst record = {\n  actorId: \"u1\",\n  action: \"read\",\n  resourceId: \"doc-1\",\n};\n\nconst json = JSON.stringify(record);\n```\n\nStep 3 — Production example:\nUse Audit Logs as a security boundary with explicit validation and least-privilege behavior.\n\nStep 4 — Edge cases:\nConsider invalid input, lifecycle cleanup, failure handling, compatibility, and observability at the boundary.\n\nStep 5 — Senior interview takeaway:\nExplain what problem the approach solves, what it does not solve, the simplest alternative, and the main production trade-off.",
    productionExample: "Use Audit Logs as a security boundary with explicit validation and least-privilege behavior.",
    bestPractices: ["Define the responsibility and public contract before choosing an implementation.", "Keep data or behavior ownership explicit and avoid hidden global coupling.", "Handle invalid input, failure, cleanup, and lifecycle boundaries.", "Prefer the smallest abstraction that solves the real problem.", "Add automated tests and operational checks for important behavior."],
    tradeOffs: "Security controls reduce attack surface, but they require careful configuration, testing, and ongoing maintenance.",
    commonMistakes: ["Using the concept because it sounds enterprise-grade instead of solving a concrete problem.", "Mixing responsibilities so business rules and infrastructure become inseparable.", "Ignoring edge cases, cleanup, compatibility, or security implications.", "Creating an abstraction before the change pattern is understood.", "Treating a sample implementation as production-complete without operational safeguards."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the source of truth or owner?", "How would you test it?", "What are the main failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["Enterprise Security Architecture"],
  },
  {
    id: "arch-m16-11",
    number: "ARCH-M16-11",
    title: "Compliance",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Enterprise Security Architecture",
    expectedAnswer: "Compliance architecture maps technical controls, evidence, retention, and access processes to required organizational or regulatory obligations.",
    deepExplanation: "Step 1 — Understand Compliance.\n\nCompliance architecture maps technical controls, evidence, retention, and access processes to required organizational or regulatory obligations.\n\nStep 2 — Easy method:\nIdentify the owner, define the smallest contract, implement the simplest path, then add the required operational safeguards.\n\nWITHOUT BUILT-IN / CORE TypeScript:\n```ts\ninterface AuditRecord {\n  actorId: string;\n  action: string;\n  resourceId: string;\n}\n\nfunction createAuditRecord(\n  actorId: string,\n  action: string,\n  resourceId: string,\n): AuditRecord {\n  return { actorId, action, resourceId };\n}\n\nconst record = createAuditRecord(\n  \"u1\",\n  \"read\",\n  \"doc-1\",\n);\n```\n\nWITH BUILT-IN / practical TypeScript:\n```ts\nconst record = {\n  actorId: \"u1\",\n  action: \"read\",\n  resourceId: \"doc-1\",\n};\n\nconst json = JSON.stringify(record);\n```\n\nStep 3 — Production example:\nUse Compliance as a security boundary with explicit validation and least-privilege behavior.\n\nStep 4 — Edge cases:\nConsider invalid input, lifecycle cleanup, failure handling, compatibility, and observability at the boundary.\n\nStep 5 — Senior interview takeaway:\nExplain what problem the approach solves, what it does not solve, the simplest alternative, and the main production trade-off.",
    productionExample: "Use Compliance as a security boundary with explicit validation and least-privilege behavior.",
    bestPractices: ["Define the responsibility and public contract before choosing an implementation.", "Keep data or behavior ownership explicit and avoid hidden global coupling.", "Handle invalid input, failure, cleanup, and lifecycle boundaries.", "Prefer the smallest abstraction that solves the real problem.", "Add automated tests and operational checks for important behavior."],
    tradeOffs: "Security controls reduce attack surface, but they require careful configuration, testing, and ongoing maintenance.",
    commonMistakes: ["Using the concept because it sounds enterprise-grade instead of solving a concrete problem.", "Mixing responsibilities so business rules and infrastructure become inseparable.", "Ignoring edge cases, cleanup, compatibility, or security implications.", "Creating an abstraction before the change pattern is understood.", "Treating a sample implementation as production-complete without operational safeguards."],
    followUpQuestions: ["What problem does this concept solve?", "When would you use it?", "When would you avoid it?", "What is the source of truth or owner?", "How would you test it?", "What are the main failure modes?", "What is the performance impact?", "How would you migrate an existing system?", "How would you monitor it?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["Enterprise Security Architecture"],
  },
];

export const MOCK_FRONTEND_MACHINE_CODING_MODULE16_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Architecture',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));