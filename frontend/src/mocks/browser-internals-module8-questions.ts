// Derived from frontend/src/document/Part_6_Browser_Internals_Web_APIs_Master_Handbook.md (Module 8 — Fetch & Networking).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question, Execution/Flow, Advanced Production Example, Performance/Security Notes,
// Best Practices, Advantages/Disadvantages, Common Mistakes/Interview Traps, Senior
// Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "jsbrowser-m8-126",
    number: "JSBROWSER-M8-126",
    title: "HTTP request lifecycle",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "An HTTP request lifecycle in the browser goes from URL/request construction through cache lookup, DNS/connection reuse or setup, request transmission, response headers, response body streaming, decoding and application consumption. Browser policy such as CORS, credentials, cache rules and redirects can affect the lifecycle. The frontend should distinguish transport failure, HTTP status failure and application-level validation failure.",
    deepExplanation: "```text\nApp code\n  ↓\nRequest construction\n  ↓\nHTTP cache / connection reuse\n  ↓\nDNS + connection/TLS/HTTP setup when needed\n  ↓\nRequest sent\n  ↓\nResponse headers\n  ↓\nResponse body stream\n  ↓\nDecode / parse\n  ↓\nApplication state\n```\n\nWith `fetch`, the promise resolves when response headers are available; the body can then be consumed or streamed separately.",
    productionExample: "A production dashboard has slow first data. DevTools shows a 250ms cache miss, 180ms connection setup and a 900ms server response. The team fixes backend latency and avoids duplicate requests instead of blaming React rendering.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Detailed networking visibility improves diagnosis but adds instrumentation/observability work. Caching can reduce latency but introduces freshness and invalidation decisions.",
    commonMistakes: ["Treating the whole lifecycle as one blocking step.","Assuming fetch resolves only after the body is fully downloaded.","Confusing DNS/connection delay with server processing time.","Ignoring cache hits and redirects."],
    followUpQuestions: ["What happens before DNS?","When does fetch resolve?","How do redirects/caches affect the lifecycle?"],
    relatedTopics: ["HTTP","Fetch","DNS","TLS","Caching","Network timing"],
  },
  {
    id: "jsbrowser-m8-127",
    number: "JSBROWSER-M8-127",
    title: "DNS and connection setup",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "Before a request can reach an origin, the browser may need DNS resolution and a usable network connection. Depending on existing connection state, it can reuse an established connection or establish one. Modern transports may use TCP+TLS or QUIC/HTTP/3; the exact path depends on browser, server and protocol negotiation.",
    deepExplanation: "```text\nURL\n ↓\nDNS resolution (if needed)\n ↓\nConnection selection/reuse\n ↓\nTLS/secure transport setup if needed\n ↓\nHTTP request\n```\n\nA warm connection can remove most setup work; repeated origins and connection reuse therefore matter to perceived latency.",
    productionExample: "A global app has many small APIs on one origin. Network traces show repeated connection setup because the architecture uses too many origins. Consolidating critical traffic behind a suitable endpoint and enabling protocol reuse reduces setup overhead.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Connection reuse reduces latency, but keeping connections alive consumes resources. HTTP/2/3 multiplexing reduces the need for many parallel connections, while protocol/network conditions vary by browser and deployment.",
    commonMistakes: ["Assuming every request performs fresh DNS/TLS.","Confusing DNS latency with backend latency.","Opening many unnecessary origins.","Ignoring HTTP/2/3 multiplexing."],
    followUpQuestions: ["What gets reused between requests?","How does HTTP/2 differ from HTTP/1.1 connection behavior?","Why can HTTP/3 change the setup path?"],
    relatedTopics: ["DNS","TCP","TLS","QUIC","HTTP/2","HTTP/3","Connection reuse"],
  },
  {
    id: "jsbrowser-m8-128",
    number: "JSBROWSER-M8-128",
    title: "Request headers",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "Request headers carry metadata and client hints for the HTTP request, such as content type, accepted formats, conditional-cache validators, tracing information and credentials handled by the browser. Some headers are controlled or restricted by the browser and cannot be freely set by page JavaScript.",
    deepExplanation: "```js\nconst response = await fetch(\"/api/report\", {\n  headers: {\n    \"Accept\": \"application/json\",\n    \"X-Request-ID\": requestId,\n  },\n});\n```\n\nApplication code can set many non-forbidden headers, while browser-controlled headers such as `Host`, some security headers and connection metadata are managed by the user agent.",
    productionExample: "A frontend sends a correlation ID and explicit Accept header through a shared API client. Authentication remains cookie/session based, and the server validates all client-provided metadata.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Custom headers improve observability and content negotiation but can trigger CORS preflights for cross-origin requests, adding latency and server complexity.",
    commonMistakes: ["Assuming every header can be set from JS.","Using custom headers for secrets that should not be exposed to the client.","Ignoring CORS preflight caused by non-simple headers.","Duplicating browser-managed headers."],
    followUpQuestions: ["Which request headers are forbidden to script?","How can custom headers trigger CORS preflight?","Which headers help HTTP caching?"],
    relatedTopics: ["Request headers","CORS","Preflight","Caching","Content negotiation"],
  },
  {
    id: "jsbrowser-m8-129",
    number: "JSBROWSER-M8-129",
    title: "Response headers",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "Response headers describe how a returned resource should be interpreted, cached, exposed to script and handled by the browser. Important examples include Content-Type, Cache-Control, ETag, Last-Modified, Set-Cookie and security headers. Script visibility of headers can also be constrained by CORS.",
    deepExplanation: "js\nconst response = await fetch(\"/api/orders\");\n\nconsole.log(response.headers.get(\"content-type\"));\nconsole.log(response.headers.get(\"cache-control\"));\n```\n\nThe browser may use some response headers internally while only a CORS-exposed subset is readable from cross-origin JavaScript.",
    productionExample: "A production API uses Cache-Control/ETag for revalidation and security headers at the edge. The frontend reads only the headers that are legitimately exposed to the application.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Rich response metadata enables caching and security policies, but misconfigured cache or exposure headers can cause stale data or information disclosure.",
    commonMistakes: ["Assuming JS can read every cross-origin response header.","Ignoring Cache-Control when diagnosing stale data.","Treating response headers as trusted business data without validation."],
    followUpQuestions: ["Which response headers does JS see cross-origin?","ETag vs Last-Modified?","Which headers control browser caching?"],
    relatedTopics: ["Response headers","Cache-Control","ETag","CORS","Set-Cookie","Security headers"],
  },
  {
    id: "jsbrowser-m8-130",
    number: "JSBROWSER-M8-130",
    title: "HTTP status codes",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "HTTP status codes communicate the outcome of a request at the protocol level. 2xx generally indicate success, 3xx redirects, 4xx client/request problems and 5xx server-side failures. Frontend code should map status classes to appropriate UX/retry behavior, but status alone does not define the business outcome.",
    deepExplanation: "```js\nconst response = await fetch(\"/api/orders\");\n\nswitch (true) {\n  case response.ok:\n    return response.json();\n  case response.status === 401:\n    throw new Error(\"SESSION_EXPIRED\");\n  case response.status === 403:\n    throw new Error(\"FORBIDDEN\");\n  case response.status === 429:\n    throw new Error(\"RATE_LIMITED\");\n  default:\n    throw new Error(`HTTP_${response.status}`);\n}\n```",
    productionExample: "A SaaS API treats 401 as session-expired UX, 403 as authorization failure, 404 as resource absence and 429/5xx as retry candidates when the operation is safe and idempotent.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Status-aware handling improves recovery but can couple the client to API conventions. A domain error contract may be needed in addition to HTTP semantics.",
    commonMistakes: ["Assuming fetch rejects on 4xx/5xx.","Retrying every 4xx/5xx automatically.","Treating 204 like a JSON response.","Ignoring 429 Retry-After semantics."],
    followUpQuestions: ["Which statuses are safe to retry?","How should 401 and 403 differ in UX?","How do you handle 429?"],
    relatedTopics: ["HTTP status","Error handling","Retry","429","401","403"],
  },
  {
    id: "jsbrowser-m8-131",
    number: "JSBROWSER-M8-131",
    title: "Streaming responses",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "Streaming responses let the browser process body bytes incrementally instead of waiting for the entire payload. With fetch, the response body is a ReadableStream in supporting environments, enabling progressive parsing, rendering or processing.",
    deepExplanation: "js\nconst response = await fetch(\"/api/stream\");\n\nconst reader = response.body?.getReader();\nif (!reader) throw new Error(\"Streaming unsupported\");\n\nwhile (true) {\n  const { value, done } = await reader.read();\n  if (done) break;\n\n  // Decode/process the chunk incrementally.\n}\n```",
    productionExample: "An AI/chat interface receives incremental text and updates the UI as chunks arrive. The app limits buffering and aborts the stream when the user navigates away.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Streaming improves time-to-first-useful-content and memory behavior for large data, but parsing partial boundaries and cancellation/reconnect logic add complexity.",
    commonMistakes: ["Assuming each chunk is a complete message.","Buffering the whole stream anyway.","Ignoring stream cancellation/backpressure.","Parsing UTF-8 chunks without handling split characters."],
    followUpQuestions: ["How does ReadableStream backpressure work?","How do you safely decode streamed UTF-8?","What happens when the network disconnects mid-stream?"],
    relatedTopics: ["Fetch","ReadableStream","Streaming","Backpressure","TextDecoder"],
  },
  {
    id: "jsbrowser-m8-132",
    number: "JSBROWSER-M8-132",
    title: "ReadableStream",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "ReadableStream is a streaming abstraction with readable data chunks and a controlled flow mechanism. Fetch response bodies use streams, and application code can read through a reader or pipe streams through transforms. Backpressure helps avoid producing/consuming data faster than the downstream can handle.",
    deepExplanation: "js\nconst response = await fetch(\"/api/export\");\nconst stream = response.body;\n\nif (!stream) throw new Error(\"No body\");\n\nconst reader = stream.getReader();\n\nfor (;;) {\n  const { value, done } = await reader.read();\n  if (done) break;\n  processChunk(value);\n}\n```",
    productionExample: "A large export is processed chunk-by-chunk so the browser does not allocate one giant JSON string. If the user cancels, the owning AbortController stops the request and stream consumption.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Streams reduce peak memory and support progressive processing, but they require more complex control flow and careful error/cancellation handling.",
    commonMistakes: ["Reading the entire body before processing.","Ignoring backpressure.","Forgetting to release/cancel readers when appropriate."],
    followUpQuestions: ["Reader vs async iteration?","What is backpressure?","How do you cancel a stream?"],
    relatedTopics: ["ReadableStream","Fetch","Backpressure","TransformStream","AbortController"],
  },
  {
    id: "jsbrowser-m8-133",
    number: "JSBROWSER-M8-133",
    title: "AbortController",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "AbortController provides an AbortSignal that can notify compatible asynchronous operations to stop. Fetch integrates directly with AbortSignal, making it useful for route changes, search cancellation, request replacement and component cleanup.",
    deepExplanation: "js\nconst controller = new AbortController();\n\nconst promise = fetch(\"/api/search?q=react\", {\n  signal: controller.signal,\n});\n\n// Later:\ncontroller.abort();\n```",
    productionExample: "A search box starts a request for each settled query. When a new query starts, the previous controller is aborted so stale work does not compete with the latest result.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Cancellation reduces wasted network/CPU work, but callers must distinguish intentional aborts from real failures and ensure every owned operation receives the signal.",
    commonMistakes: ["Ignoring AbortError.","Creating a controller but never passing its signal.","Reusing one controller for unrelated requests.","Updating UI from a canceled stale request."],
    followUpQuestions: ["How does AbortSignal propagate?","Can one signal cancel multiple operations?","How do you handle cancellation in React effects?"],
    relatedTopics: ["AbortController","AbortSignal","Fetch","Request cancellation","React effects"],
  },
  {
    id: "jsbrowser-m8-134",
    number: "JSBROWSER-M8-134",
    title: "Request cancellation",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "Request cancellation means actively stopping work that is no longer valuable, usually via AbortController for fetch. Cancellation is especially important for route changes, autocomplete, live search and replaced requests where stale responses should not update state.",
    deepExplanation: "tsx\nuseEffect(() => {\n  const controller = new AbortController();\n\n  fetch(`/api/search?q=${encodeURIComponent(query)}`, {\n    signal: controller.signal,\n  }).then(/*...*/);\n\n  return () => controller.abort();\n}, [query]);\n```",
    productionExample: "A product search cancels the previous request when the query changes and on route unmount, reducing wasted bandwidth and eliminating stale-result races.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Cancellation improves correctness and resource use, but backend work may continue after the client disconnects, so cancellation is not the same as transactional rollback.",
    commonMistakes: ["Thinking browser abort guarantees server-side rollback.","Ignoring race conditions after cancellation.","Sharing one controller across independent feature lifecycles."],
    followUpQuestions: ["Does abort stop server execution?","How does cancellation prevent stale UI?","How do you combine cancellation with retries?"],
    relatedTopics: ["Request cancellation","AbortController","Race conditions","Fetch"],
  },
  {
    id: "jsbrowser-m8-135",
    number: "JSBROWSER-M8-135",
    title: "Timeout strategies",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "The browser fetch API does not provide a universal timeout option directly; production code commonly uses AbortController plus a timer or `AbortSignal.timeout()` where supported. A timeout should define what user-facing recovery means rather than simply turning every slow request into failure.",
    deepExplanation: "js\nconst signal = AbortSignal.timeout\n  ? AbortSignal.timeout(5000)\n  : (() => {\n      const controller = new AbortController();\n      setTimeout(() => controller.abort(), 5000);\n      return controller.signal;\n    })();\n\nconst response = await fetch(\"/api/report\", { signal });\n```",
    productionExample: "A dashboard gives critical summary requests a 5-second deadline, then shows a retry/fallback state. Background refreshes use different timing because they are lower priority.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Short timeouts improve perceived responsiveness but can abort valid slow requests. Timeout values should be based on user journey and backend SLOs.",
    commonMistakes: ["Using one timeout for every endpoint.","Calling timeout a server cancellation guarantee.","Retrying immediately after timeout without backoff or idempotency."],
    followUpQuestions: ["How should timeout values be chosen?","AbortSignal.timeout vs setTimeout+AbortController?","How do timeouts interact with retries?"],
    relatedTopics: ["Timeout","AbortController","SLO","Retry","Resilience"],
  },
  {
    id: "jsbrowser-m8-136",
    number: "JSBROWSER-M8-136",
    title: "Retry strategies",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "Retries repeat a failed operation after a failure that is likely transient, using bounded attempts and backoff. Safe retries depend on idempotency: reads are often retryable, while non-idempotent writes need an idempotency key or explicit server semantics.",
    deepExplanation: "js\nasync function retry<T>(\n  task: () => Promise<T>,\n  attempts = 3,\n) {\n  for (let i = 0; i < attempts; i++) {\n    try {\n      return await task();\n    } catch (error) {\n      if (i === attempts - 1) throw error;\n      await new Promise(r =>\n        setTimeout(r, 2 ** i * 250),\n      );\n    }\n  }\n  throw new Error(\"unreachable\");\n}\n```",
    productionExample: "A GET report endpoint retries transient 503/connection failures with exponential backoff and jitter. A payment POST is not blindly retried; it uses an idempotency key.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Retries improve resilience to transient failures but can amplify outages and increase latency/load. Backoff, jitter and retry budgets are essential.",
    commonMistakes: ["Retrying every error.","Retrying non-idempotent writes without idempotency.","No maximum attempt count.","No backoff/jitter."],
    followUpQuestions: ["Which errors are retryable?","How do you prevent retry storms?","How do idempotency keys change retry policy?"],
    relatedTopics: ["Retries","Exponential backoff","Jitter","Idempotency","Resilience"],
  },
  {
    id: "jsbrowser-m8-137",
    number: "JSBROWSER-M8-137",
    title: "HTTP caching",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "HTTP caching lets the browser reuse or revalidate stored responses according to server cache directives. Cache-Control controls freshness/reuse, while validators such as ETag or Last-Modified support conditional requests. Correct caching requires an explicit freshness/privacy strategy.",
    deepExplanation: "http\nCache-Control: max-age=60, stale-while-revalidate=300\nETag: \"orders-v1042\"\n```\n\nA subsequent request may be served from cache, or sent conditionally so the server can return `304 Not Modified` when the representation is unchanged.",
    productionExample: "A static JavaScript bundle uses long-lived immutable caching with content hashes, while API responses use short freshness plus revalidation to avoid serving stale account data for too long.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Caching reduces latency and bandwidth but introduces staleness, invalidation and privacy concerns. Aggressive caching is ideal for immutable assets but dangerous for rapidly changing/private data.",
    commonMistakes: ["Setting long max-age on mutable API data.","Assuming cache is always bypassed in dev.","Ignoring private/personalized response caching semantics.","Using one policy for every resource."],
    followUpQuestions: ["Cache-Control vs ETag?","When does a 304 happen?","How do you cache immutable static assets safely?"],
    relatedTopics: ["HTTP cache","Cache-Control","ETag","304","Revalidation"],
  },
  {
    id: "jsbrowser-m8-138",
    number: "JSBROWSER-M8-138",
    title: "Connection reuse",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "Connection reuse lets the browser send multiple requests over an existing suitable connection instead of repeating DNS/handshake setup. HTTP/2 and HTTP/3 can multiplex many requests over a connection, reducing the motivation for opening many parallel connections.",
    deepExplanation: "text\nFirst request:\nDNS → connection/TLS/QUIC setup → request\n\nLater requests:\nreuse existing suitable connection → request\n```\n\nThe exact reuse decision is browser- and protocol-dependent and may be affected by origin, network changes and connection state.",
    productionExample: "A production app avoids unnecessary cross-origin fragmentation so critical API and asset traffic can benefit from connection reuse and multiplexing.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "Reuse lowers setup latency but long-lived connections consume resources. Network changes, server limits or origin differences can break reuse.",
    commonMistakes: ["Assuming one connection is reused forever.","Creating many API origins without reason.","Ignoring protocol multiplexing."],
    followUpQuestions: ["How does HTTP/2 multiplexing affect connection strategy?","What changes with HTTP/3?","When can a connection be unavailable for reuse?"],
    relatedTopics: ["Connection reuse","HTTP/2","HTTP/3","DNS","TLS","QUIC"],
  },
  {
    id: "jsbrowser-m8-139",
    number: "JSBROWSER-M8-139",
    title: "Network optimization",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "Network optimization means reducing avoidable latency, bytes, connection setup, duplicate requests and blocking dependencies. Techniques include caching, compression, code/data splitting, request parallelization, preconnect/preload when justified, streaming, HTTP/2/3 and backend optimization. The right choice comes from measured waterfalls and user-centric metrics.",
    deepExplanation: "text\nMeasure:\n  request waterfall\n  TTFB\n  response size\n  cache hit/miss\n  connection setup\n  long tasks\n        ↓\nChoose bottleneck-specific fix\n```\n\nAvoid adding every resource hint automatically; hints compete for bandwidth and can make priority worse.",
    productionExample: "A slow home page had six sequential API calls. The team moved independent requests to parallel fetching, cached immutable configuration, removed a duplicate call and reduced the initial JavaScript payload. LCP/INP improved in RUM.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "More parallelism can shorten latency but increase server load and congestion. More caching lowers network cost but increases invalidation complexity.",
    commonMistakes: ["Optimizing before reading the waterfall.","Making every request parallel even when dependencies exist.","Adding preload/prefetch without verifying priority.","Ignoring backend TTFB."],
    followUpQuestions: ["How do you prioritize network optimizations?","Preconnect vs preload vs prefetch?","How do you balance parallelism and server load?"],
    relatedTopics: ["Network performance","Waterfall","Caching","Preload","Prefetch","RUM"],
  },
  {
    id: "jsbrowser-m8-140",
    number: "JSBROWSER-M8-140",
    title: "Fetch production scenario",
    difficulty: "Hard",
    experienceLevel: "2–5 Years",
    category: "Fetch & Networking",
    expectedAnswer: "For a senior Fetch production scenario, separate transport concerns from server-state and UI concerns. Define cancellation, timeout, retry/idempotency, caching, validation, error mapping and observability. The frontend must handle stale responses and preserve a clear user-facing state.",
    deepExplanation: "tsx\nasync function getOrders(signal: AbortSignal) {\n  const response = await fetch(\"/api/orders\", {\n    credentials: \"include\",\n    signal,\n  });\n\n  if (!response.ok) {\n    throw new Error(`Orders failed: ${response.status}`);\n  }\n\n  return response.json() as Promise<Order[]>;\n}\n```",
    productionExample: "A production orders page uses a shared transport layer plus a query/cache layer. Route changes abort obsolete requests, 401 triggers session recovery, 429 follows server retry guidance, and request IDs connect browser telemetry to backend traces.",
    bestPractices: ["Measure the network waterfall before optimizing.","Keep transport, caching and domain logic separated.","Use cancellation, explicit timeouts and bounded retries.","Respect HTTP/CORS/security semantics.","Instrument critical requests with request IDs and useful error context."],
    tradeOffs: "A strong abstraction improves consistency, but too much hidden behavior makes debugging difficult. Keep retry/cache/error policies explicit at the right layer.",
    commonMistakes: ["One giant API wrapper that hides every detail.","Retrying payments automatically.","No cancellation on route changes.","Mixing transport errors with domain validation."],
    followUpQuestions: ["How would you design the API client?","What belongs in fetch wrapper vs query layer?","How do you trace a frontend request across backend services?"],
    relatedTopics: ["Fetch","API client","Retries","Caching","AbortController","Observability"],
  },

];

export const MOCK_BROWSER_INTERNALS_MODULE8_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Browser/Web APIs',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));