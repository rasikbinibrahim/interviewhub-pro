// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 6 — Hook Testing).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m6-1",
    number: "TESTQA-M6-1",
    title: "useState",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**useState** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**useState** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-2",
    number: "TESTQA-M6-2",
    title: "useEffect",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**useEffect** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**useEffect** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-3",
    number: "TESTQA-M6-3",
    title: "useReducer",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**useReducer** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**useReducer** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-4",
    number: "TESTQA-M6-4",
    title: "useRef",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**useRef** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**useRef** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-5",
    number: "TESTQA-M6-5",
    title: "useMemo",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**useMemo** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**useMemo** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-6",
    number: "TESTQA-M6-6",
    title: "useCallback",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**useCallback** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**useCallback** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-7",
    number: "TESTQA-M6-7",
    title: "useContext",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**useContext** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**useContext** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-8",
    number: "TESTQA-M6-8",
    title: "Custom Hooks",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**Custom Hooks** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Custom Hooks** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-9",
    number: "TESTQA-M6-9",
    title: "Async Hooks",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**Async Hooks** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Async Hooks** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-10",
    number: "TESTQA-M6-10",
    title: "Optimistic Hooks",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**Optimistic Hooks** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Optimistic Hooks** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-11",
    number: "TESTQA-M6-11",
    title: "API Hooks",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**API Hooks** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**API Hooks** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-12",
    number: "TESTQA-M6-12",
    title: "Timer Hooks",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**Timer Hooks** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Timer Hooks** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-13",
    number: "TESTQA-M6-13",
    title: "Cleanup Functions",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**Cleanup Functions** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Cleanup Functions** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-14",
    number: "TESTQA-M6-14",
    title: "Retry Logic",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**Retry Logic** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Retry Logic** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-15",
    number: "TESTQA-M6-15",
    title: "Hook Testing Architecture",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**Hook Testing Architecture** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Hook Testing Architecture** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
  {
    id: "testqa-m6-16",
    number: "TESTQA-M6-16",
    title: "Hook Testing — Module Review",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Hook Testing",
    expectedAnswer: "**Hook Testing — Module Review** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Hook Testing — Module Review** should be tested through its consuming behavior, with focused checks for state transitions, effects, async paths, and cleanup.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface EffectState { active: boolean; cleaned: boolean; }\n\nfunction cleanup(state: EffectState): EffectState {\n  return { active: false, cleaned: true };\n}\n\nconst next = cleanup({ active: true, cleaned: false });\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst state = { active: true, cleaned: false };\n\nconst next = {\n  ...state,\n  active: false,\n  cleaned: true,\n};\n```\n\nStep 5 — Production example:\nTest hooks through real consumers where possible and isolate pure state/effect transitions when useful.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Test hooks through real consumers where possible and isolate pure state/effect transitions when useful.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Hook Testing"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE6_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));