// Derived from frontend/src/document/Part_6_Browser_Internals_Web_APIs_Master_Handbook.md (Module 3 — DOM).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question, Execution/Flow, Advanced Production Example, Performance/Security Notes,
// Best Practices, Advantages/Disadvantages, Common Mistakes/Interview Traps, Senior
// Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "jsbrowser-m3-36",
    number: "JSBROWSER-M3-36",
    title: "DOM definition",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "DOM (Document Object Model) is the browser's in-memory object representation of a document tree. It exposes nodes, elements, text, attributes and relationships to JavaScript. The DOM is a structural model; it is not the CSSOM, layout tree or painted output.",
    deepExplanation: "```html\n<body>\n  <main id=\"app\">\n    <h1>Dashboard</h1>\n  </main>\n</body>\n```\n\nConceptually:\nDocument\n└── html\n    └── body\n        └── main#app\n            └── h1\n                └── text node\n\nJavaScript can traverse and mutate this object graph through Web APIs.",
    productionExample: "A production application keeps the DOM intentionally small for large lists and updates only the regions that change. Frameworks such as React calculate UI changes, but the browser ultimately receives DOM mutations and performs its own rendering work.",
    bestPractices: ["Use semantic HTML.","Keep DOM size reasonable.","Prefer framework-managed updates when a framework owns the UI.","Avoid unnecessary direct DOM mutation inside React components.","Measure DOM/render cost on realistic devices."],
    tradeOffs: "A DOM provides a flexible standard document model, but a large tree consumes memory and can increase style, layout and paint work. Direct DOM APIs provide control but can fight a framework's rendering model.",
    commonMistakes: ["Calling the DOM the render tree.","Assuming every DOM mutation triggers full-page layout.","Mixing imperative DOM updates with React-owned nodes without a clear boundary.","Treating DOM nodes as the same thing as pixels."],
    followUpQuestions: ["What is the difference between DOM, CSSOM and render tree?","What happens after a DOM mutation?","How is DOM identity preserved?"],
    relatedTopics: ["DOM","Document","Element","CSSOM","Render tree","Layout"],
  },
  {
    id: "jsbrowser-m3-37",
    number: "JSBROWSER-M3-37",
    title: "DOM tree",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "The DOM tree is the hierarchical node structure represented by the Document object. It contains element nodes, text nodes, comments and other node types. Parent/child/sibling relationships are fundamental to traversal, events and mutation.",
    deepExplanation: "```js\nconst root = document.querySelector(\"#app\");\n\nconsole.log(root?.parentElement?.tagName);\nconsole.log(root?.children.length);\n```\n\nThe `children` collection contains element children; `childNodes` also includes text/comment nodes. This distinction matters when whitespace text nodes exist in markup.",
    productionExample: "A production accessibility utility traverses actual element relationships to find labels or landmarks, while performance-sensitive code avoids repeated whole-tree traversal when a direct reference can be retained safely.",
    bestPractices: ["Know `children` versus `childNodes`.","Use stable element references where appropriate.","Avoid repeated global queries in hot paths.","Keep tree structure semantically meaningful."],
    tradeOffs: "The tree model is easy to reason about but large traversals can cost CPU. Cached references reduce lookup work but must be invalidated when nodes are removed or replaced.",
    commonMistakes: ["Assuming whitespace does not create text nodes.","Using `childNodes[0]` when an element is expected.","Keeping references to removed nodes indefinitely."],
    followUpQuestions: ["How does DOM tree identity change during replacement?","What is a Node versus Element?","How do shadow roots affect the tree?"],
    relatedTopics: ["Node","Element","childNodes","children","Shadow DOM"],
  },
  {
    id: "jsbrowser-m3-38",
    number: "JSBROWSER-M3-38",
    title: "Document object",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "The `Document` object represents the loaded document and is the root of the DOM tree. It provides access to document-level APIs such as element creation, selectors, events, lifecycle state and the active document metadata. It is distinct from `window`, which represents the browsing context's global object.",
    deepExplanation: "```js\nconsole.log(document.readyState);\n\nconst title = document.createElement(\"title\");\ntitle.textContent = \"Dashboard\";\ndocument.head.replaceChildren(title);\n```\n\nThe document exposes the current page DOM and document-level state; browser APIs on `window` cover a broader browsing-context scope.",
    productionExample: "A multi-route application uses `document.visibilityState`, `document.activeElement` and document-level event handling only at platform integration boundaries, while component-level behavior remains encapsulated in the UI framework.",
    bestPractices: ["Avoid global document mutation unless the feature owns that responsibility.","Use document-level listeners sparingly.","Check lifecycle/readiness state when needed.","Clean up document-level listeners."],
    tradeOffs: "Document-level APIs are powerful and universal, but they are global and easy to misuse. Excessive use can make code difficult to compose with frameworks.",
    commonMistakes: ["Confusing document with window.","Querying global document for every component update.","Attaching permanent listeners without cleanup."],
    followUpQuestions: ["Document versus Window?","What is `document.readyState`?","When is document-level event delegation appropriate?"],
    relatedTopics: ["Document","Window","readyState","activeElement","DOM events"],
  },
  {
    id: "jsbrowser-m3-39",
    number: "JSBROWSER-M3-39",
    title: "Element nodes",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "An Element node represents an HTML or XML element in the DOM and provides element-specific APIs such as attributes, class lists, dimensions, selectors, children and event targets. Elements are a subset of the broader Node hierarchy.",
    deepExplanation: "```js\nconst button = document.createElement(\"button\");\nbutton.textContent = \"Save\";\nbutton.setAttribute(\"type\", \"button\");\nbutton.classList.add(\"primary\");\ndocument.body.append(button);\n```\n\nThe resulting node participates in the DOM and can later be styled, measured and interacted with.",
    productionExample: "A design-system integration uses a small imperative DOM adapter to insert an external widget into a dedicated container element. The rest of the application stays declarative and React-owned.",
    bestPractices: ["Prefer semantic element types.","Use element-specific APIs when needed.","Keep imperative integrations behind explicit boundaries.","Avoid unnecessary layout measurement."],
    tradeOffs: "Direct element APIs are precise and standard, but they can bypass framework state management and can trigger rendering work if used heavily.",
    commonMistakes: ["Treating every Node as an Element.","Using non-semantic divs for native controls.","Mutating an element owned by another rendering system."],
    followUpQuestions: ["Node versus Element?","What APIs are Element-specific?","How does changing an attribute affect rendering?"],
    relatedTopics: ["Element","Node","Attributes","classList","Events"],
  },
  {
    id: "jsbrowser-m3-40",
    number: "JSBROWSER-M3-40",
    title: "Text nodes",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "Text nodes represent textual content in the DOM. They are distinct Node objects and can be observed/traversed independently of element nodes. Changing text content can affect layout and paint if the rendered text changes dimensions or wrapping.",
    deepExplanation: "```js\nconst textNode = document.createTextNode(\"Hello\");\nconst heading = document.createElement(\"h1\");\n\nheading.append(textNode);\ndocument.body.append(heading);\n\ntextNode.data = \"Hello world\";\n```\n\nThe browser updates the document text node and then performs any required style/layout/paint work.",
    productionExample: "A localization system changes text content across a page. The production implementation uses framework rendering/i18n rather than directly mutating individual text nodes so React and accessibility state remain consistent.",
    bestPractices: ["Prefer semantic/framework rendering for application UI.","Avoid repeated text-node mutation in hot paths.","Remember text changes can affect layout.","Use proper escaping/serialization for untrusted text."],
    tradeOffs: "Direct text-node updates are cheap compared with rebuilding entire structures, but changing rendered text can still cause layout/paint. Framework abstraction usually provides safer ownership.",
    commonMistakes: ["Confusing text nodes with text content strings.","Assuming text changes never cause layout.","Using `innerHTML` for plain text."],
    followUpQuestions: ["Text node versus `textContent`?","When does changing text trigger layout?","How is text escaped safely?"],
    relatedTopics: ["Text node","textContent","Layout","Paint","XSS"],
  },
  {
    id: "jsbrowser-m3-41",
    number: "JSBROWSER-M3-41",
    title: "Node traversal",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "DOM traversal means moving through the node graph using parent/child/sibling relationships, selectors, TreeWalker or NodeIterator. The best approach depends on whether the target is known by a stable selector, structural relationship or broad traversal requirement.",
    deepExplanation: "```js\nconst walker = document.createTreeWalker(\n  document.body,\n  NodeFilter.SHOW_TEXT,\n);\n\nlet node;\nwhile ((node = walker.nextNode())) {\n  console.log(node.textContent);\n}\n```\n\nFor normal UI lookups, `querySelector` or a saved element reference is usually clearer than traversing every node manually.",
    productionExample: "A document-processing feature traverses a large article tree to build a table of contents. It runs once after content load rather than during every user interaction, and results are cached.",
    bestPractices: ["Prefer the narrowest traversal.","Avoid full-tree walks in hot paths.","Use TreeWalker/NodeIterator for specialized traversal.","Cache stable results when safe."],
    tradeOffs: "General traversal is flexible but can be O(n) over large subtrees. Selector engines can be more expressive, while cached references reduce repeated work.",
    commonMistakes: ["Walking the entire document on every event.","Using brittle parent/child indexes.","Ignoring detached-node references."],
    followUpQuestions: ["TreeWalker versus querySelectorAll?","How do you traverse a shadow tree?","How would you optimize a large DOM traversal?"],
    relatedTopics: ["TreeWalker","NodeIterator","Selectors","DOM traversal"],
  },
  {
    id: "jsbrowser-m3-42",
    number: "JSBROWSER-M3-42",
    title: "querySelector",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "`querySelector()` returns the first element matching a CSS selector within a document or element subtree, or `null` if none matches. The selector is parsed and matched by the browser's selector engine.",
    deepExplanation: "```js\nconst saveButton = document.querySelector(\n  '[data-testid=\"save\"]',\n);\n\nsaveButton?.addEventListener(\"click\", save);\n```\n\nFor application behavior tests, accessibility queries are often preferable to test IDs, but the DOM API itself uses CSS selector semantics.",
    productionExample: "A browser integration layer finds a dedicated host element for a third-party widget once during initialization and stores the reference rather than querying the whole document on every update.",
    bestPractices: ["Keep selectors precise.","Scope queries to the smallest container.","Cache references when the node is stable.","Handle `null` results.","Do not use selector strings as your only application state model."],
    tradeOffs: "Selectors are expressive and fast enough for ordinary UI work, but repeated broad queries in hot paths can become unnecessary CPU work.",
    commonMistakes: ["Assuming `querySelector` returns a collection.","Using invalid CSS selectors without handling exceptions.","Calling it repeatedly inside animation/input loops."],
    followUpQuestions: ["querySelector versus getElementById?","What happens with invalid selectors?","How does selector complexity affect performance?"],
    relatedTopics: ["Selectors","CSS selector engine","querySelectorAll","getElementById"],
  },
  {
    id: "jsbrowser-m3-43",
    number: "JSBROWSER-M3-43",
    title: "querySelectorAll",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "`querySelectorAll()` returns a static `NodeList` containing all elements matching a CSS selector at the time of the call. It does not live-update as the DOM changes, unlike some older DOM collections.",
    deepExplanation: "```js\nconst buttons = document.querySelectorAll(\n  \"button[data-action]\",\n);\n\nbuttons.forEach(button => {\n  button.addEventListener(\"click\", handleAction);\n});\n```\n\nIf buttons are added later, the returned NodeList does not automatically contain them; event delegation or a new query may be needed.",
    productionExample: "A large table originally re-ran `querySelectorAll` on every state update. The implementation was changed to event delegation and stable references, reducing repeated traversal.",
    bestPractices: ["Know that the returned NodeList is static.","Scope the selector.","Prefer event delegation for dynamic repeated controls when appropriate.","Avoid unnecessary repeated queries."],
    tradeOffs: "Returning all matches can allocate and iterate more data than needed. A one-time query is often fine, but dynamic UIs may benefit from delegation or framework-level event handling.",
    commonMistakes: ["Assuming the NodeList is live.","Querying the entire document repeatedly.","Attaching hundreds of listeners when delegation is simpler."],
    followUpQuestions: ["Static NodeList versus live HTMLCollection?","When is event delegation better?","How would you process thousands of matches efficiently?"],
    relatedTopics: ["querySelectorAll","NodeList","HTMLCollection","Event delegation"],
  },
  {
    id: "jsbrowser-m3-44",
    number: "JSBROWSER-M3-44",
    title: "DOM mutation",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "DOM mutation changes the document tree or node state, such as appending/removing elements, changing attributes, text or classes. The mutation itself is separate from subsequent style calculation, layout, paint and compositing work that the browser may need to perform.",
    deepExplanation: "```js\nconst list = document.querySelector(\"ul\");\n\nconst item = document.createElement(\"li\");\nitem.textContent = \"New item\";\n\nlist?.append(item);\n```\n\nAfter the mutation, the browser may recalculate affected style/layout and update the visual output.",
    productionExample: "A production drag-and-drop surface batches DOM changes and avoids alternating reads/writes that force synchronous layout. A framework-owned UI uses state updates instead of direct mutation wherever possible.",
    bestPractices: ["Batch related mutations.","Avoid layout-thrashing read/write interleaving.","Measure DOM mutation cost.","Keep one clear owner of each DOM subtree."],
    tradeOffs: "Direct mutations provide low-level control but can create synchronization problems with declarative frameworks. Batching reduces browser work but may delay immediate visual changes.",
    commonMistakes: ["Mutating a React-owned DOM node directly.","Reading layout after every write.","Assuming one mutation means a full-page reflow."],
    followUpQuestions: ["What is layout thrashing?","Mutation versus style recalculation?","How does batching help?"],
    relatedTopics: ["DOM mutation","Style calculation","Layout thrashing","Rendering pipeline"],
  },
  {
    id: "jsbrowser-m3-45",
    number: "JSBROWSER-M3-45",
    title: "createElement",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "`document.createElement()` creates an Element node in a document but does not insert it into the document tree. The new element becomes visible or participates in rendering only after it is attached to the DOM.",
    deepExplanation: "```js\nconst card = document.createElement(\"article\");\ncard.className = \"card\";\n\nconst title = document.createElement(\"h2\");\ntitle.textContent = \"Invoice\";\n\ncard.append(title);\n\n// Still detached here:\nconsole.log(card.isConnected); // false\n\ndocument.body.append(card);\n```\n\nBuilding a subtree while detached lets code assemble it before a single insertion.",
    productionExample: "A legacy widget builds a complete detached subtree, sets all attributes/content, then inserts it once into a host container to reduce repeated live-DOM updates.",
    bestPractices: ["Build detached subtrees when many nodes must be created.","Prefer framework rendering for framework-owned UI.","Use semantic elements.","Avoid building huge trees unnecessarily."],
    tradeOffs: "Detached construction can reduce intermediate DOM/render work, but manual DOM construction is verbose and easy to desynchronize from application state.",
    commonMistakes: ["Assuming createElement inserts automatically.","Appending each small node directly to a live container in a tight loop.","Creating markup with innerHTML when structured DOM APIs are safer."],
    followUpQuestions: ["createElement versus innerHTML?","Why can detached construction help?","When is a framework better than manual DOM creation?"],
    relatedTopics: ["createElement","append","DocumentFragment","DOM construction"],
  },
  {
    id: "jsbrowser-m3-46",
    number: "JSBROWSER-M3-46",
    title: "append and remove",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "`append()` inserts nodes or strings at the end of an Element or DocumentFragment, while `remove()` removes a node from its parent. These are convenient DOM mutation primitives and can participate in rendering work once nodes are connected.",
    deepExplanation: "```js\nconst container = document.querySelector(\"#messages\");\nconst message = document.createElement(\"p\");\n\nmessage.textContent = \"Saved\";\ncontainer?.append(message);\n\n// Later:\nmessage.remove();\n```\n\nIf the node has listeners or references elsewhere, removing it from the DOM does not automatically remove every JavaScript reference to it.",
    productionExample: "A notification system appends temporary messages and removes them after a timeout. The code also clears timers/listeners so detached nodes are not retained unnecessarily.",
    bestPractices: ["Own lifecycle of inserted nodes.","Clean up related listeners/timers.","Prefer `DocumentFragment` for large batches.","Do not retain detached nodes unnecessarily."],
    tradeOffs: "These APIs are simple, but high-frequency insertion/removal can create DOM and garbage-collection pressure. Batching can improve performance.",
    commonMistakes: ["Thinking remove() destroys all references.","Adding many nodes one by one during heavy rendering.","Forgetting timeout cleanup."],
    followUpQuestions: ["append versus appendChild?","What happens to event listeners after remove?","How can detached DOM nodes cause memory leaks?"],
    relatedTopics: ["append","remove","DocumentFragment","Memory leaks","GC"],
  },
  {
    id: "jsbrowser-m3-47",
    number: "JSBROWSER-M3-47",
    title: "attributes",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "HTML attributes are name/value metadata associated with elements and represented through DOM attribute APIs. Some attributes reflect to corresponding DOM properties and can affect behavior, accessibility, resource loading or styling. Attribute and property are related but not always identical.",
    deepExplanation: "```js\nconst input = document.querySelector(\"input\");\n\ninput?.setAttribute(\"aria-invalid\", \"true\");\ninput?.setAttribute(\"data-user-id\", \"42\");\n\nconsole.log(input?.getAttribute(\"data-user-id\"));\n```\n\nBoolean attributes such as `disabled` also have special HTML semantics and corresponding DOM properties.",
    productionExample: "A form component updates `aria-invalid`, `aria-describedby` and `disabled` consistently with its React state. The team avoids using string attributes as the application source of truth.",
    bestPractices: ["Use semantic/native attributes when available.","Understand property versus attribute reflection.","Avoid arbitrary custom attributes when data-* or standard semantics fit better.","Validate values used in accessibility attributes."],
    tradeOffs: "Attributes are declarative metadata and integrate deeply with browser semantics, but property/attribute reflection rules can be surprising.",
    commonMistakes: ["Treating every attribute as a string-only property.","Setting `disabled=\"false\"` and expecting it to mean enabled.","Using non-standard attributes for standard behavior."],
    followUpQuestions: ["Attribute versus property?","Boolean attributes?","How do ARIA attributes affect accessibility?"],
    relatedTopics: ["Attributes","Properties","ARIA","data-*","Boolean attributes"],
  },
  {
    id: "jsbrowser-m3-48",
    number: "JSBROWSER-M3-48",
    title: "classList",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "`classList` exposes a token-based `DOMTokenList` for adding, removing, toggling and checking CSS classes. It is preferable to manual string concatenation when code intentionally manipulates classes on a DOM element.",
    deepExplanation: "```js\nconst panel = document.querySelector(\"#panel\");\n\npanel?.classList.add(\"is-open\");\npanel?.classList.toggle(\"is-selected\", true);\npanel?.classList.remove(\"is-loading\");\n```\n\nThe class change can trigger style recalculation and possibly layout/paint depending on the styles affected.",
    productionExample: "A legacy integration toggles a state class on an external widget. The application measures the effect and avoids rapid class changes that would cause repeated layout work.",
    bestPractices: ["Use classList methods instead of string concatenation.","Keep class ownership clear.","Avoid toggling classes from many unrelated systems.","Measure style/layout cost for animation-heavy updates."],
    tradeOffs: "Class manipulation is readable and interoperable, but class changes can trigger style recalculation and visual work. Framework styling systems may be safer when the framework owns the DOM.",
    commonMistakes: ["Using `className +=` and creating duplicate classes.","Toggling state from multiple sources.","Assuming class changes are free."],
    followUpQuestions: ["classList versus className?","Can class changes trigger layout?","How does classList work with SVG?"],
    relatedTopics: ["classList","className","CSS","Style recalculation"],
  },
  {
    id: "jsbrowser-m3-49",
    number: "JSBROWSER-M3-49",
    title: "dataset",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "`dataset` exposes `data-*` HTML attributes through a convenient `DOMStringMap`. It is useful for small declarative metadata and integration hooks, but it should not become a substitute for application state or a place for sensitive information.",
    deepExplanation: "html\n<button\n  data-action=\"delete\"\n  data-record-id=\"42\"\n>\n  Delete\n</button>\n```\n\n```js\nconst button = document.querySelector(\"button\");\n\nconsole.log(button?.dataset.action);\nconsole.log(button?.dataset.recordId);\n```\n\n`data-record-id` maps to `dataset.recordId`.",
    productionExample: "A legacy analytics layer reads `data-track` attributes for event metadata. The data is non-sensitive and intentionally limited to UI/integration metadata; authorization and business state remain elsewhere.",
    bestPractices: ["Use data-* for lightweight metadata.","Keep values non-sensitive.","Use standard attributes/ARIA when semantics exist.","Do not store application secrets or authorization state in dataset."],
    tradeOffs: "dataset is convenient and declarative, but every value is a string and remains visible in the DOM. It can become hard to manage if used as a general state store.",
    commonMistakes: ["Storing tokens in data attributes.","Expecting numbers/booleans to remain typed.","Using data-* when a native attribute already exists."],
    followUpQuestions: ["dataset versus getAttribute?","How is camelCase mapping formed?","Why is dataset inappropriate for secrets?"],
    relatedTopics: ["dataset","data-*","Attributes","Security","DOM metadata"],
  },
  {
    id: "jsbrowser-m3-50",
    number: "JSBROWSER-M3-50",
    title: "textContent vs innerHTML",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "`textContent` treats assigned data as text, while `innerHTML` parses a string as HTML markup. `textContent` is appropriate for untrusted/plain text; `innerHTML` is useful when intentionally creating markup but requires strict sanitization for untrusted content because unsafe HTML can cause XSS.",
    deepExplanation: "js\nconst message = document.querySelector(\"#message\");\n\nmessage.textContent = userInput; // safe as text\n\n// Only use innerHTML with trusted or correctly sanitized markup:\nmessage.innerHTML = \"<strong>Saved</strong>\";\n```\n\nFor untrusted HTML, sanitize with a trusted sanitizer and a strong CSP rather than inserting it directly.",
    productionExample: "A rich-text CMS uses a vetted sanitization pipeline before rendering stored HTML. Normal names, labels and user-entered strings use `textContent` or framework text rendering instead.",
    bestPractices: ["Prefer textContent for plain/untrusted text.","Sanitize HTML before inserting untrusted markup.","Use CSP as defense in depth.","Avoid string HTML construction in ordinary UI code.","Test XSS payloads at security boundaries."],
    tradeOffs: "innerHTML is flexible and can be efficient for bulk markup creation, but parsing and security risks are higher. textContent is simpler and safer for text but cannot express markup.",
    commonMistakes: ["Using innerHTML with user input.","Saying textContent escapes HTML by magic across every API without understanding context.","Replacing framework rendering with innerHTML on managed nodes."],
    followUpQuestions: ["innerHTML versus insertAdjacentHTML?","How does DOM XSS happen?","When would you choose textContent?"],
    relatedTopics: ["innerHTML","textContent","XSS","Sanitization","CSP"],
  },
  {
    id: "jsbrowser-m3-51",
    number: "JSBROWSER-M3-51",
    title: "DOM events integration",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "DOM events propagate through capture, target and bubble phases. Event delegation allows a parent to handle events from many descendants by inspecting `event.target`/`closest`, while cleanup is required for manually registered listeners. Frameworks may provide their own event abstraction, but they ultimately integrate with browser events.",
    deepExplanation: "js\ndocument\n  .querySelector(\"#list\")\n  ?.addEventListener(\"click\", event => {\n    const button = event.target instanceof Element\n      ? event.target.closest(\"[data-action]\")\n      : null;\n\n    if (button) {\n      handleAction(button.getAttribute(\"data-action\"));\n    }\n  });\n```\n\nOne parent listener can handle dynamically added buttons inside the list.",
    productionExample: "A large table uses event delegation for row actions instead of attaching thousands of independent listeners. The feature is isolated from React-owned DOM when used alongside a framework.",
    bestPractices: ["Understand capture and bubble phases.","Prefer delegation for large dynamic repeated controls when appropriate.","Use AbortSignal or explicit cleanup for listeners.","Preserve accessible native interaction."],
    tradeOffs: "Delegation reduces listener count, but hit testing and target matching add logic. Direct listeners are simpler for small stable component trees.",
    commonMistakes: ["Stopping propagation without understanding other handlers.","Attaching listeners repeatedly on rerenders.","Ignoring cleanup.","Using event delegation when a framework already owns the event lifecycle."],
    followUpQuestions: ["Capture versus bubble?","How does event delegation work with dynamic children?","How would you clean up an event listener safely?"],
    relatedTopics: ["DOM events","Bubbling","Capturing","Event delegation","AbortSignal"],
  },
  {
    id: "jsbrowser-m3-52",
    number: "JSBROWSER-M3-52",
    title: "MutationObserver",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "`MutationObserver` asynchronously reports DOM changes to an observing callback. It batches mutations into records and delivers them after the current synchronous mutation work, making it suitable for reacting to external DOM changes without polling.",
    deepExplanation: "js\nconst observer = new MutationObserver(records => {\n  for (const record of records) {\n    console.log(record.type, record.target);\n  }\n});\n\nobserver.observe(document.body, {\n  childList: true,\n  subtree: true,\n});\n\nobserver.disconnect();\n```\n\nThe callback is asynchronous; it should not be confused with a synchronous DOM mutation hook.",
    productionExample: "A third-party widget injects DOM into a controlled host element. The integration observes only that subtree, translates meaningful changes into application state and disconnects the observer when the host is removed.",
    bestPractices: ["Observe the smallest necessary subtree.","Filter mutation records.","Disconnect observers during cleanup.","Avoid mutating observed nodes in a feedback loop.","Prefer direct application state/events when you control the source of the change."],
    tradeOffs: "MutationObserver is useful for uncontrolled DOM integration but can generate substantial work when observing large subtrees or high-frequency mutations.",
    commonMistakes: ["Observing `document.body` for everything.","Creating observer→mutation→observer feedback loops.","Forgetting `disconnect()`.","Using MutationObserver when the application already knows exactly when it changed."],
    followUpQuestions: ["When does MutationObserver callback run?","How do mutation records batch?","How do you avoid feedback loops?"],
    relatedTopics: ["MutationObserver","Microtasks","MutationRecord","DOM integration"],
  },
  {
    id: "jsbrowser-m3-53",
    number: "JSBROWSER-M3-53",
    title: "DOM memory",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "DOM memory is consumed by nodes, associated browser state, layout/style data and JavaScript references that keep nodes reachable. Memory problems often arise from oversized DOM trees, detached nodes retained by closures/listeners, or caches storing DOM references.",
    deepExplanation: "js\nlet cachedNode = document.querySelector(\"#panel\");\n\nfunction clearCache() {\n  cachedNode = null;\n}\n```\n\nIf a removed node remains referenced by `cachedNode`, garbage collection cannot reclaim the JavaScript reference even though the node is no longer connected to the document.",
    productionExample: "A long-lived dashboard leaks memory after repeated route changes because a service retained detached DOM nodes in a Map. Heap snapshots show the retaining path; the service is changed to store IDs/data instead of DOM references.",
    bestPractices: ["Avoid retaining detached nodes.","Disconnect observers/listeners.","Keep caches bounded.","Use heap snapshots and allocation timelines.","Prefer data references over DOM references in long-lived services."],
    tradeOffs: "Large DOMs can consume substantial memory, but keeping data outside the DOM does not automatically make it cheap. Reducing DOM size and clearing references usually requires architectural changes.",
    commonMistakes: ["Assuming remove() guarantees immediate memory release.","Keeping DOM nodes in global stores.","Ignoring closures and event listeners as retaining paths."],
    followUpQuestions: ["How do you identify detached DOM nodes?","What keeps an object alive for GC?","How would you reproduce a DOM leak?"],
    relatedTopics: ["Memory","Garbage collection","Detached DOM","Heap snapshots","Leaks"],
  },
  {
    id: "jsbrowser-m3-54",
    number: "JSBROWSER-M3-54",
    title: "DOM performance",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "DOM performance is influenced by tree size, selector/traversal cost, mutation frequency, style recalculation, layout, paint, event handling and memory. A good optimization targets the dominant stage rather than assuming every DOM API has the same cost.",
    deepExplanation: "js\nconst fragment = document.createDocumentFragment();\n\nfor (const item of items) {\n  const li = document.createElement(\"li\");\n  li.textContent = item;\n  fragment.append(li);\n}\n\ndocument.querySelector(\"#list\")?.append(fragment);\n```\n\nBuilding off-DOM and inserting once can reduce intermediate live-tree updates for large batches.",
    productionExample: "A legacy report with 20,000 rows is slow because both DOM size and layout work are high. The final solution uses server pagination plus virtualization rather than trying to optimize individual DOM calls.",
    bestPractices: ["Measure before optimizing.","Batch large DOM updates.","Avoid forced synchronous layout.","Virtualize large collections.","Keep selectors/traversals scoped.","Test on low-end devices."],
    tradeOffs: "Manual DOM optimizations can help hot paths, but reducing the amount of DOM and work is often more valuable than micro-optimizing individual methods.",
    commonMistakes: ["Micro-benchmarking isolated DOM methods without user context.","Using DocumentFragment as a universal performance fix.","Ignoring layout/paint after mutation."],
    followUpQuestions: ["How do you diagnose layout thrashing?","What is the biggest DOM performance win for huge lists?","How do style recalculation and layout relate to DOM changes?"],
    relatedTopics: ["DOM performance","Layout","Paint","Virtualization","DocumentFragment","Performance"],
  },
  {
    id: "jsbrowser-m3-55",
    number: "JSBROWSER-M3-55",
    title: "DOM architecture scenario",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "DOM",
    expectedAnswer: "A senior DOM architecture scenario should identify who owns the DOM, how external widgets integrate, what the accessibility contract is, and where performance/memory boundaries exist. Choose declarative framework rendering for application UI, and use imperative DOM APIs at well-defined integration edges.",
    deepExplanation: "text\nApplication UI\n   ↓\nReact/component tree\n   ↓\nDOM owned by application\n        │\n        └── integration boundary\n              ↓\n        third-party widget host\n\nRules:\n- One clear owner per subtree\n- No unmanaged cross-boundary mutation\n- Cleanup listeners/observers\n- Test keyboard/accessibility behavior\n- Measure DOM/layout cost\n```\n\nThis is more important architecturally than memorizing individual DOM methods.",
    productionExample: "A production app integrates a third-party editor inside a dedicated `<div>` host. The adapter owns initialization/destruction, translates events into application state, and never lets the editor mutate unrelated React-owned nodes.",
    bestPractices: ["Define DOM ownership.","Isolate imperative integrations.","Use cleanup APIs.","Keep security/sanitization boundaries explicit.","Test lifecycle, accessibility and performance.","Document integration contracts."],
    tradeOffs: "Imperative DOM boundaries provide compatibility with legacy/third-party widgets but increase lifecycle and synchronization complexity. A clean ownership boundary prevents most integration bugs.",
    commonMistakes: ["Letting two systems control the same subtree.","Using global selectors for feature-local integration.","Forgetting teardown on route change.","Ignoring keyboard/focus behavior at the boundary."],
    followUpQuestions: ["How would you integrate a chart/editor widget with React?","How do you prevent memory leaks?","How would you test the boundary?"],
    relatedTopics: ["DOM architecture","React integration","Third-party widgets","Imperative boundaries","Lifecycle","Accessibility"],
  },

];

export const MOCK_BROWSER_INTERNALS_MODULE3_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Browser/Web APIs',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));