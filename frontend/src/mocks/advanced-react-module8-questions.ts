// Derived from frontend/src/document/Part_9_Advanced_React_Master_Handbook.md (Module 8 — React 19 Features).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "reactadv-m8-136",
    number: "REACTADV-M8-136",
    title: "React 19 overview",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "React 19 is a stable React release that adds Actions, form Actions, useActionState, useOptimistic, useFormStatus, the use API, native document metadata, stylesheet and async script support, resource preloading APIs, ref-as-a-prop, ref cleanup, improved hydration/error reporting, and custom-element support. A senior engineer should explain not only the APIs but also when they reduce application complexity and how they fit with the framework/server architecture.",
    deepExplanation: "```tsx\nimport { useState } from \"react\";\n\nexport function Profile() {\n  const [name, setName] = useState(\"\");\n\n  return (\n    <label>\n      Name\n      <input value={name} onChange={e => setName(e.target.value)} />\n    </label>\n  );\n}\n```\n\nReact 19 is not a rewrite of the component model. The important production change is that several common async/form/resource patterns can now be expressed with first-class React/React DOM APIs.",
    productionExample: "In a SaaS application, use Actions for mutations, useActionState for action result/pending state, useOptimistic for immediate UI feedback, useFormStatus inside reusable form controls, native metadata for route-level document information, and React DOM resource APIs for critical resource discovery. Keep framework-specific server integration separate from React primitives.",
    bestPractices: ["Adopt React 19 APIs where they remove duplicated state-management code.", "Keep server-side validation and authorization authoritative.", "Use accessible pending/error feedback for forms.", "Profile resource loading before adding preloads or preinit.", "Test hydration, SSR and client-only paths.", "Avoid depending on React internals or framework-specific APIs as if they were React core APIs."],
    tradeOffs: "Advantages: less boilerplate for mutations/forms; better integration between rendering and resource loading; improved SSR/hydration behavior; stronger primitives for optimistic UI. Trade-offs: some features depend on a framework/server architecture; careless preloading can waste bandwidth; Actions do not replace server validation, caching or application-level error handling.",
    commonMistakes: ["Calling React 19 a completely new rendering engine.", "Treating Server Actions as identical to ordinary client callbacks.", "Assuming Actions automatically make server mutations secure.", "Preloading every asset without measuring network cost.", "Using useOptimistic without defining failure/reversion behavior.", "Confusing React 19 core features with framework-specific Server Components tooling."],
    followUpQuestions: ["Which React 19 feature gives the biggest benefit in a large form-heavy application?", "Which features require React DOM rather than React?", "What is the difference between Actions and Server Actions?", "How would you migrate a React 18 application incrementally?", "Which React 19 features affect SSR/hydration most?"],
    relatedTopics: ["Actions", "useActionState", "useOptimistic", "useFormStatus", "use", "Document Metadata", "Stylesheets", "Async Scripts", "Resource Preloading", "Hydration", "React Compiler"],
  },
  {
    id: "reactadv-m8-137",
    number: "REACTADV-M8-137",
    title: "Actions",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "In React 19, an Action is conventionally an async function used with an Action-aware API such as an async transition or a form Action. React can track pending state and coordinate state updates around the mutation. Actions are designed to reduce manual handling of pending state, errors, optimistic updates and form submission; they do not replace backend authorization or validation.",
    deepExplanation: "```tsx\nimport { useTransition, useState } from \"react\";\n\nasync function updateName(name: string) {\n  const response = await fetch(\"/api/profile\", {\n    method: \"POST\",\n    headers: { \"content-type\": \"application/json\" },\n    body: JSON.stringify({ name }),\n  });\n\n  if (!response.ok) throw new Error(\"Update failed\");\n}\n\nexport function ChangeName() {\n  const [name, setName] = useState(\"\");\n  const [isPending, startTransition] = useTransition();\n\n  function submit() {\n    startTransition(async () => {\n      await updateName(name);\n    });\n  }\n\n  return (\n    <>\n      <input value={name} onChange={e => setName(e.target.value)} />\n      <button onClick={submit} disabled={isPending}>\n        {isPending ? \"Saving…\" : \"Save\"}\n      </button>\n    </>\n  );\n}\n```",
    productionExample: "A profile editor submits a mutation through an Action so the UI immediately exposes pending state while the server request is running. The server still validates the authenticated user and input before writing to the database.",
    bestPractices: ["Keep mutation authorization on the server.", "Return structured action results when the UI needs success or validation information.", "Use pending state to prevent accidental duplicate submissions when appropriate.", "Handle expected validation failures separately from unexpected exceptions.", "Use optimistic UI only when rollback/reconciliation is well defined."],
    tradeOffs: "Advantages: fewer manual pending-state transitions; Actions compose with useActionState, useOptimistic and form Actions. Trade-offs: async Actions require careful error semantics; they do not automatically provide idempotency, authorization or request cancellation; framework integrations can add additional constraints.",
    commonMistakes: ["Thinking an Action itself is a server function.", "Assuming pending means the network request is automatically cancelled on unmount.", "Putting authorization only in the client.", "Ignoring duplicate-submission/idempotency concerns."],
    followUpQuestions: ["Action vs ordinary async event handler?", "How do Actions work with useOptimistic?", "How do you handle validation errors?", "How do you prevent duplicate mutations?"],
    relatedTopics: ["startTransition", "useTransition", "useActionState", "useOptimistic", "Form Actions", "Server Actions"],
  },
  {
    id: "reactadv-m8-138",
    number: "REACTADV-M8-138",
    title: "useActionState",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "useActionState is a React hook for actions that need their last returned result and pending state. It returns [state, action, isPending]. The action receives the previous state followed by the submitted arguments. With a form Action, the submitted FormData becomes the action argument. It is useful for server validation results, success messages and other action state without manually coordinating several useState values.",
    deepExplanation: "```tsx\nimport { useActionState } from \"react\";\n\ntype State = { message: string; error?: string };\n\nasync function saveProfile(\n  previous: State,\n  formData: FormData\n): Promise<State> {\n  const name = String(formData.get(\"name\") ?? \"\").trim();\n\n  if (!name) {\n    return { ...previous, error: \"Name is required\" };\n  }\n\n  const response = await fetch(\"/api/profile\", {\n    method: \"POST\",\n    body: JSON.stringify({ name }),\n    headers: { \"content-type\": \"application/json\" },\n  });\n\n  if (!response.ok) {\n    return { ...previous, error: \"Unable to save profile\" };\n  }\n\n  return { message: \"Profile saved\" };\n}\n\nexport function ProfileForm() {\n  const [state, action, isPending] = useActionState(saveProfile, {\n    message: \"\",\n  });\n\n  return (\n    <form action={action}>\n      <label>\n        Name\n        <input name=\"name\" />\n      </label>\n      <button disabled={isPending}>\n        {isPending ? \"Saving…\" : \"Save\"}\n      </button>\n      <p role=\"status\">{state.message}</p>\n      {state.error && <p role=\"alert\">{state.error}</p>}\n    </form>\n  );\n}\n```",
    productionExample: "A checkout form uses useActionState to return field-independent submission results such as “Payment method rejected” or “Order created”. Field-level validation can be returned as structured data and rendered next to the appropriate control.",
    bestPractices: ["Return serializable, predictable state shapes when the action crosses a server boundary.", "Keep validation authoritative on the server.", "Use the pending value to provide accessible submission feedback.", "Do not store unrelated application state in the action state.", "Model success and expected failure explicitly."],
    tradeOffs: "Advantages: combines action result state and pending state; reduces boilerplate; works naturally with form Actions. Trade-offs: it is action-state management, not a replacement for a general server-state cache; complex forms may still need structured validation and field mapping.",
    commonMistakes: ["Forgetting that the action receives previous state before its normal arguments.", "Treating isPending as a global network loading indicator.", "Returning inconsistent result shapes.", "Using it as a substitute for server-side validation."],
    followUpQuestions: ["useActionState vs useReducer?", "How do you return field-level validation errors?", "How does it behave with a form Action?", "How would you test pending and failure states?"],
    relatedTopics: ["Actions", "Form Actions", "useFormStatus", "useOptimistic", "FormData", "Server validation"],
  },
  {
    id: "reactadv-m8-139",
    number: "REACTADV-M8-139",
    title: "useOptimistic",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "useOptimistic lets a component show an optimistic value while an Action or async mutation is in progress. The optimistic state is temporary: React uses the current source value as the baseline and returns to the current value when the mutation finishes or fails. It is appropriate when the expected result is predictable and immediate feedback improves UX.",
    deepExplanation: "```tsx\nimport { useOptimistic } from \"react\";\n\ntype Props = {\n  currentName: string;\n  updateName: (name: string) => Promise<string>;\n};\n\nexport function NameForm({ currentName, updateName }: Props) {\n  const [optimisticName, setOptimisticName] =\n    useOptimistic(currentName);\n\n  async function submit(formData: FormData) {\n    const nextName = String(formData.get(\"name\") ?? \"\").trim();\n    if (!nextName) return;\n\n    setOptimisticName(nextName);\n    const savedName = await updateName(nextName);\n\n    // Parent/source state should be updated with savedName.\n    console.log(savedName);\n  }\n\n  return (\n    <form action={submit}>\n      <p>Your name: {optimisticName}</p>\n      <input name=\"name\" defaultValue={currentName} />\n      <button type=\"submit\">Change</button>\n    </form>\n  );\n}\n```",
    productionExample: "A comment system immediately inserts a submitted comment into the visible conversation while the server mutation is pending. If the server rejects it, the optimistic item disappears and the UI displays the failure; if successful, the authoritative server result becomes the source of truth.",
    bestPractices: ["Use optimistic updates only when rollback is understandable.", "Keep authoritative state separate from the optimistic presentation.", "Handle server conflicts and validation failures.", "Prevent users from triggering contradictory mutations where necessary.", "Prefer server responses as the final source of truth."],
    tradeOffs: "Advantages: much faster perceived feedback and simpler optimistic UI than manually duplicating temporary state. Trade-offs: temporary UI can be wrong; concurrent mutations can conflict; rollback/error UX must be explicit; it does not solve server consistency.",
    commonMistakes: ["Treating optimistic state as authoritative database state.", "Forgetting to update the real source state after success.", "Ignoring failed requests.", "Using optimistic UI for operations with unpredictable irreversible outcomes."],
    followUpQuestions: ["How does optimistic state revert?", "How would you handle two rapid optimistic mutations?", "When should you avoid optimistic UI?", "How would you reconcile a server conflict?"],
    relatedTopics: ["Actions", "useActionState", "Transitions", "Server state", "Rollback", "Concurrency"],
  },
  {
    id: "reactadv-m8-140",
    number: "REACTADV-M8-140",
    title: "Form Actions",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "React 19 allows functions to be passed to the action prop of form elements and formAction on submit controls. React treats these functions as Actions, enabling pending handling, submission with FormData and automatic reset behavior for uncontrolled form fields after a successful form Action. This reduces manual onSubmit plumbing.",
    deepExplanation: "```tsx\nimport { useActionState } from \"react\";\n\nasync function createUser(_previous: string, formData: FormData) {\n  const email = String(formData.get(\"email\") ?? \"\");\n\n  if (!email.includes(\"@\")) {\n    return \"Enter a valid email\";\n  }\n\n  await fetch(\"/api/users\", {\n    method: \"POST\",\n    body: JSON.stringify({ email }),\n    headers: { \"content-type\": \"application/json\" },\n  });\n\n  return \"User created\";\n}\n\nexport function CreateUserForm() {\n  const [message, action, pending] =\n    useActionState(createUser, \"\");\n\n  return (\n    <form action={action}>\n      <label>\n        Email\n        <input name=\"email\" type=\"email\" />\n      </label>\n      <button disabled={pending}>\n        {pending ? \"Creating…\" : \"Create user\"}\n      </button>\n      <p role=\"status\">{message}</p>\n    </form>\n  );\n}\n```",
    productionExample: "A reusable admin form can pass an action function directly to `<form action={...}>`, while the same design-system submit button reads pending state through useFormStatus instead of receiving a loading prop through several component layers.",
    bestPractices: ["Use native form semantics and labels.", "Validate again on the server.", "Keep the Action focused on one mutation.", "Use useFormStatus in nested form controls.", "Use controlled fields only when their immediate state is required."],
    tradeOffs: "Advantages: less event-handler boilerplate; integrates with pending/action state; natural FormData flow. Trade-offs: framework/server integration may be required for server-side Actions; controlled and uncontrolled inputs have different reset behavior; complex client-only workflows may still be clearer with explicit handlers.",
    commonMistakes: ["Putting the submit button outside the form while expecting useFormStatus to track it.", "Assuming action functions are automatically secure server endpoints.", "Forgetting that FormData contains strings/files rather than already-typed domain objects."],
    followUpQuestions: ["Form action vs onSubmit?", "How does form reset work?", "How do you handle field-level errors?", "When should a form remain client-controlled?"],
    relatedTopics: ["Actions", "useActionState", "useFormStatus", "FormData", "Progressive enhancement", "Validation"],
  },
  {
    id: "reactadv-m8-141",
    number: "REACTADV-M8-141",
    title: "Server Actions concepts",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "Server Actions are async functions executed on the server and callable from client components through a framework/runtime that supports React Server Components. The `\"use server\"` directive marks a Server Action; it does not mark a Server Component. React 19 provides the underlying Action model, while the framework is responsible for the transport and server execution environment.",
    deepExplanation: "```tsx\n// Server-side module in a framework that supports Server Actions\n\"use server\";\n\nexport async function updateProfile(formData: FormData) {\n  const name = String(formData.get(\"name\") ?? \"\").trim();\n\n  // Authenticate and authorize on the server.\n  // Validate input.\n  // Update the database.\n  return { ok: true, name };\n}\n```\n\n```tsx\n// Client component\n\"use client\";\n\nimport { updateProfile } from \"./actions\";\n\nexport function ProfileForm() {\n  return (\n    <form action={updateProfile}>\n      <input name=\"name\" required />\n      <button type=\"submit\">Save</button>\n    </form>\n  );\n}\n```",
    productionExample: "A Next.js-style application uses a Server Action to perform an authenticated database mutation. The browser submits the form to the framework, the server validates the session and input, performs the write, and returns the action result. The exact transport and caching/revalidation behavior belongs to the framework integration.",
    bestPractices: ["Authenticate and authorize every sensitive Server Action.", "Validate all input on the server.", "Treat action arguments as untrusted network input.", "Use idempotency where repeated submission can create duplicate side effects.", "Understand framework-specific caching/revalidation semantics.", "Do not expose secrets to client bundles."],
    tradeOffs: "Advantages: colocated mutation logic; natural form integration; less custom API plumbing for supported full-stack frameworks. Trade-offs: framework/runtime coupling; network boundaries remain; security and observability are still application responsibilities; not every React host supports Server Actions.",
    commonMistakes: ["Saying `\"use server\"` creates a Server Component.", "Treating Server Actions as private functions that cannot be attacked.", "Assuming React itself defines the HTTP protocol.", "Skipping authorization because the action is not directly called from a visible button."],
    followUpQuestions: ["Server Action vs REST endpoint?", "Who owns caching/revalidation?", "How do you authorize a Server Action?", "How do you test it without a browser?"],
    relatedTopics: ["Server Components", "Actions", "use server", "Forms", "Authentication", "Authorization", "Framework integration"],
  },
  {
    id: "reactadv-m8-142",
    number: "REACTADV-M8-142",
    title: "useFormStatus",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "useFormStatus is a React DOM hook that reads the status of the nearest parent form. Its primary value is that reusable child components, such as design-system submit buttons, can access the form's pending state without receiving it through props. It must be called from a component rendered inside the form.",
    deepExplanation: "```tsx\nimport { useFormStatus } from \"react-dom\";\n\nfunction SubmitButton() {\n  const { pending } = useFormStatus();\n\n  return (\n    <button type=\"submit\" disabled={pending} aria-disabled={pending}>\n      {pending ? \"Saving…\" : \"Save\"}\n    </button>\n  );\n}\n\nexport function Form() {\n  return (\n    <form action={async () => {\n      await new Promise(resolve => setTimeout(resolve, 500));\n    }}>\n      <input name=\"email\" type=\"email\" required />\n      <SubmitButton />\n    </form>\n  );\n}\n```",
    productionExample: "A shared design system exposes one SubmitButton component used by dozens of forms. Each button automatically reflects the pending state of its parent form without every product team wiring a loading prop through intermediate components.",
    bestPractices: ["Call useFormStatus from a descendant of the form.", "Use pending for button state and accessible status feedback.", "Keep the button's disabled behavior consistent with the mutation semantics.", "Use form-native controls and labels.", "Do not use it as a global request-status hook."],
    tradeOffs: "Advantages: removes prop drilling; excellent for reusable form controls; closely matches form submission state. Trade-offs: scoped to a parent form; cannot replace general async state; component placement matters.",
    commonMistakes: ["Calling useFormStatus in the component that renders the `<form>` itself and expecting it to read that same form.", "Using it for unrelated fetch requests.", "Assuming pending represents every mutation in the page."],
    followUpQuestions: ["Why must the hook be below the form?", "useFormStatus vs useTransition?", "How would you expose validation errors in a design system?", "Can one page have multiple independent form statuses?"],
    relatedTopics: ["react-dom", "Form Actions", "useActionState", "Actions", "Design systems"],
  },
  {
    id: "reactadv-m8-143",
    number: "REACTADV-M8-143",
    title: "Metadata support",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "React 19 supports rendering document metadata elements such as `<title>`, `<meta>`, and `<link>` directly inside components. React recognizes these tags and hoists them into the document head, including in supported client, streaming SSR and Server Component rendering environments. This removes simple metadata cases from manual DOM effects.",
    deepExplanation: "```tsx\nexport function ProductPage({ product }: {\n  product: { name: string; description: string };\n}) {\n  return (\n    <>\n      <title>{product.name}</title>\n      <meta name=\"description\" content={product.description} />\n      <link rel=\"canonical\" href={`https://example.com/products/${product.name}`} />\n\n      <main>\n        <h1>{product.name}</h1>\n      </main>\n    </>\n  );\n}\n```",
    productionExample: "A product-detail component owns the product title and description. React can place those metadata tags in the document head without a useEffect that manually calls document.title or appends meta elements.",
    bestPractices: ["Keep metadata derived from trusted route/data state.", "Define canonical/SEO rules centrally when the application needs complex overrides.", "Avoid duplicate conflicting metadata.", "Escape/validate externally sourced metadata as appropriate.", "Use framework metadata systems when they provide richer routing and SEO behavior."],
    tradeOffs: "Advantages: colocated metadata; works naturally with React rendering; fewer manual DOM effects. Trade-offs: advanced SEO requirements may still benefit from framework metadata APIs; duplicate metadata can become difficult to reason about in deeply nested trees.",
    commonMistakes: ["Using useEffect for every title when direct metadata elements are sufficient.", "Assuming React 19 replaces all framework SEO features.", "Creating multiple conflicting canonical/title tags."],
    followUpQuestions: ["How does React hoist metadata?", "How does metadata behave during streaming SSR?", "When would you use a framework metadata API?"],
    relatedTopics: ["title", "meta", "link", "SSR", "Streaming", "Server Components", "SEO"],
  },
  {
    id: "reactadv-m8-144",
    number: "REACTADV-M8-144",
    title: "Stylesheet loading",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "React 19 supports native stylesheet resources through `<link rel=\"stylesheet\" href=\"...\">` and `<style>` elements rendered as part of the React tree. React can coordinate stylesheet discovery with rendering, including waiting for newly rendered stylesheets to load before committing client renders and coordinating late stylesheets with Suspense during streaming. Duplicate stylesheet links rendered from multiple places are deduplicated.",
    deepExplanation: "```tsx\nexport function Checkout() {\n  return (\n    <>\n      <link\n        rel=\"stylesheet\"\n        href=\"/styles/checkout.css\"\n      />\n      <section className=\"checkout\">\n        <h1>Checkout</h1>\n      </section>\n    </>\n  );\n}\n```",
    productionExample: "A checkout feature can declare its stylesheet near the component that needs it. With supported rendering, React can coordinate the resource rather than forcing the application to manually inject a link in an effect or global bootstrap file.",
    bestPractices: ["Load only styles required by the rendered feature.", "Use stable URLs for stylesheet resources.", "Measure CSS transfer, blocking and render timing.", "Avoid shipping the entire application stylesheet when route-level styles are practical.", "Verify SSR/streaming behavior in the framework being used."],
    tradeOffs: "Advantages: component-local resource declaration; React-aware loading and deduplication; better coordination with Suspense/streaming. Trade-offs: CSS can still block rendering; excessive route-level splitting can increase requests; bundler/framework behavior must be understood.",
    commonMistakes: ["Thinking the feature replaces CSS bundlers.", "Loading every stylesheet from every component.", "Ignoring CSS size and render-blocking cost.", "Assuming a stylesheet is always loaded asynchronously."],
    followUpQuestions: ["How does React deduplicate stylesheets?", "What happens if a stylesheet is discovered during streaming?", "How does this differ from preload()?"],
    relatedTopics: ["link", "style", "Suspense", "Streaming SSR", "Resource preloading", "CSS performance"],
  },
  {
    id: "reactadv-m8-145",
    number: "REACTADV-M8-145",
    title: "Script loading",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "React 19 adds better support for async scripts rendered directly in the component tree. React can deduplicate async script instances across components and, during SSR, place them in the document head with appropriate prioritization behind more critical blocking resources such as stylesheets, fonts and image preloads.",
    deepExplanation: "```tsx\nexport function AnalyticsWidget() {\n  return (\n    <section>\n      <script\n        async\n        src=\"https://cdn.example.com/widget.js\"\n      />\n      <div id=\"widget-root\" />\n    </section>\n  );\n}\n```",
    productionExample: "A third-party analytics or payment widget can declare its async script next to the feature that depends on it. If several instances render the same script, React can avoid duplicate loading/execution.",
    bestPractices: ["Use async scripts only for scripts that can load independently.", "Prefer self-hosting or trusted vendors for sensitive applications.", "Use CSP and integrity/security controls where appropriate.", "Avoid scripts that mutate DOM nodes React owns.", "Measure third-party script CPU and network cost."],
    tradeOffs: "Advantages: colocated dependency declaration and deduplication; improved SSR handling. Trade-offs: third-party JavaScript can still hurt performance; async execution order is not guaranteed; security/privacy and CSP remain application responsibilities.",
    commonMistakes: ["Assuming async scripts execute in source order.", "Rendering the same third-party script with different URLs and expecting deduplication.", "Allowing third-party code to mutate React-managed DOM arbitrarily.", "Ignoring script security."],
    followUpQuestions: ["Async vs defer?", "How does React deduplicate scripts?", "How would you isolate a third-party widget?", "What CSP policy would you use?"],
    relatedTopics: ["script", "async", "CSP", "Third-party scripts", "SSR", "Performance"],
  },
  {
    id: "reactadv-m8-146",
    number: "REACTADV-M8-146",
    title: "Resource preloading",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "React DOM 19 provides resource APIs such as preload, preinit, preconnect and prefetchDNS. They let React applications tell the browser about resources that are likely to be needed, potentially moving discovery earlier. preload fetches a resource for later use, preinit can load and initialize resources such as scripts, preconnect establishes an early connection, and prefetchDNS performs DNS lookup preparation.",
    deepExplanation: "```tsx\nimport {\n  preconnect,\n  preload,\n  prefetchDNS,\n} from \"react-dom\";\n\nexport function ProductPage() {\n  preconnect(\"https://cdn.example.com\");\n  prefetchDNS(\"https://images.example.com\");\n\n  preload(\"https://cdn.example.com/product-font.woff2\", {\n    as: \"font\",\n    crossOrigin: \"anonymous\",\n  });\n\n  return <h1>Product</h1>;\n}\n```",
    productionExample: "An e-commerce route knows a critical product font and image CDN will be required. It preconnects to the CDN and preloads only the critical resource, while less certain resources remain lazy-loaded.",
    bestPractices: ["Preload only resources with high confidence and meaningful critical-path value.", "Match preload attributes such as `as` and cross-origin behavior correctly.", "Use preconnect for origins you know you will contact soon.", "Measure LCP, request waterfalls and bandwidth impact.", "Do not preload large resources that users may never need."],
    tradeOffs: "Advantages: earlier resource discovery and potentially lower critical-path latency. Trade-offs: wrong hints compete for bandwidth and cache; excessive preloads can make performance worse; browser behavior still determines actual scheduling.",
    commonMistakes: ["Preloading everything.", "Confusing preload with prefetch.", "Using the wrong `as` value.", "Assuming preload guarantees immediate download completion.", "Ignoring mobile bandwidth."],
    followUpQuestions: ["preload vs preinit?", "preconnect vs prefetchDNS?", "How do you prove a preload improved LCP?", "What happens when the preloaded resource is never used?"],
    relatedTopics: ["preload", "preinit", "preconnect", "prefetchDNS", "LCP", "Resource hints"],
  },
  {
    id: "reactadv-m8-147",
    number: "REACTADV-M8-147",
    title: "Hydration improvements",
    difficulty: "Hard",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "React 19 improves hydration diagnostics and compatibility. Hydration errors now provide a clearer single message with a diff between server and client output. React also improves handling around third-party scripts and browser extensions by skipping unexpected tags in the head/body where appropriate. The underlying rule remains: server-rendered markup and the initial client render should represent the same UI state.",
    deepExplanation: "```tsx\nexport function Greeting({ name }: { name: string }) {\n  // Keep the first client render deterministic relative to SSR.\n  return <p>Hello, {name}</p>;\n}\n```\n\nAvoid this during the initial render when the server cannot produce the same value:\n\n```tsx\n// Risky for hydration\nconst value = Math.random();\nreturn <p>{value}</p>;\n```",
    productionExample: "A server-rendered dashboard previously reported vague hydration warnings. After upgrading to React 19, the team can use the mismatch diff to identify a client-only date/randomness branch and move browser-dependent work into an appropriate client-side phase.",
    bestPractices: ["Keep SSR and first client render deterministic.", "Avoid Date.now(), Math.random() and browser-only branches in markup unless the architecture explicitly handles them.", "Use framework-supported hydration boundaries.", "Treat hydration warnings as correctness bugs, not cosmetic logs.", "Test with production-like SSR and browser extensions/third-party scripts where relevant."],
    tradeOffs: "Advantages: clearer debugging and more resilient handling of unexpected DOM additions. Trade-offs: React cannot make genuinely nondeterministic application output correct; fixing the source of the mismatch is still required.",
    commonMistakes: ["Silencing hydration warnings instead of fixing the mismatch.", "Reading window/localStorage during SSR markup generation.", "Using different locale/time-zone formatting on server and client without a strategy.", "Assuming React 19 eliminates all hydration errors."],
    followUpQuestions: ["What causes hydration mismatch?", "How do you debug a mismatch in production?", "What belongs in useEffect versus the initial render?", "How do browser extensions affect hydration?"],
    relatedTopics: ["SSR", "hydrateRoot", "Streaming", "Hydration mismatch", "onRecoverableError", "Server Components"],
  },
  {
    id: "reactadv-m8-148",
    number: "REACTADV-M8-148",
    title: "React Compiler overview",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "React Compiler is a build-time compiler that can automatically optimize React code by applying memoization-style transformations when its analysis can prove them safe. The goal is to reduce the amount of manual performance memoization developers need. It does not mean every component becomes magically faster, and teams should still write correct, pure React code and verify performance with measurement.",
    deepExplanation: "```tsx\nfunction ProductList({\n  products,\n  query,\n}: {\n  products: { id: string; name: string }[];\n  query: string;\n}) {\n  const filtered = products.filter(product =>\n    product.name.toLowerCase().includes(query.toLowerCase())\n  );\n\n  return (\n    <ul>\n      {filtered.map(product => (\n        <li key={product.id}>{product.name}</li>\n      ))}\n    </ul>\n  );\n}\n```\n\nThe important compiler-oriented principle is to write straightforward, pure React code rather than manually adding memoization everywhere. Compiler adoption and directives/configuration should follow the version and framework documentation used by the project.",
    productionExample: "A large application with many hand-written useMemo/useCallback/memo optimizations evaluates React Compiler incrementally. The team compares bundle behavior, render performance and correctness before removing manual optimizations.",
    bestPractices: ["Keep components pure and follow the Rules of React.", "Measure before and after compiler adoption.", "Adopt the compiler according to the supported React/framework toolchain.", "Review compiler diagnostics and unsupported patterns.", "Do not remove all memoization blindly without profiling and compatibility testing."],
    tradeOffs: "Advantages: can automate repetitive memoization work and simplify component code. Trade-offs: compiler analysis adds a build step and compatibility constraints; not every optimization is beneficial; debugging generated optimization behavior requires good tooling and profiling.",
    commonMistakes: ["Calling the compiler a runtime scheduler.", "Assuming useMemo/useCallback are always unnecessary immediately after enabling it.", "Writing impure render code because the compiler is enabled.", "Skipping compiler diagnostics."],
    followUpQuestions: ["Compiler vs React.memo?", "When can manual memoization still be useful?", "How would you roll it out to a large monorepo?", "What metrics prove the compiler helped?"],
    relatedTopics: ["Memoization", "React.memo", "useMemo", "useCallback", "Rules of React", "Build tooling"],
  },
  {
    id: "reactadv-m8-149",
    number: "REACTADV-M8-149",
    title: "Migration strategy",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "A React 19 migration should be incremental: inventory React/React DOM versions and framework compatibility, read the official upgrade guide, upgrade dependencies and codemods where appropriate, run tests and type checks, fix behavioral changes, then adopt new APIs feature by feature. Do not mix a version upgrade with a large architectural rewrite unless there is a separate reason.",
    deepExplanation: "```tsx\n// Before: manual submit state\nconst [pending, setPending] = useState(false);\n\nasync function handleSubmit() {\n  setPending(true);\n  try {\n    await save();\n  } finally {\n    setPending(false);\n  }\n}\n```\n\n```tsx\n// React 19 direction: move suitable mutation flows to Actions\nconst [state, action, pending] = useActionState(saveAction, initialState);\n```\n\nThe exact migration depends on whether the application is CSR, SSR, streaming, or a framework with Server Components/Server Actions.",
    productionExample: "For a large enterprise SPA, upgrade React and React DOM first behind normal CI gates, then migrate one high-value form to Actions. Monitor error rates, hydration warnings and interaction performance before expanding adoption.",
    bestPractices: ["Pin and verify compatible React/React DOM/framework versions.", "Run unit, integration, E2E and SSR/hydration tests.", "Use codemods only where the migration guide supports them.", "Introduce React 19 APIs by feature rather than rewriting everything.", "Monitor production errors and performance after each rollout.", "Keep rollback/version-control strategy explicit."],
    tradeOffs: "Advantages: lower migration risk, easier diagnosis and measurable adoption. Trade-offs: temporary mixed patterns; framework dependencies can constrain timing; migration requires test and observability investment.",
    commonMistakes: ["Upgrading React without checking the framework and renderer versions.", "Replacing every form with Actions immediately.", "Ignoring hydration/SSR tests.", "Treating compiler adoption as mandatory for a React 19 upgrade.", "Changing architecture and React version in one unmeasured release."],
    followUpQuestions: ["What would you upgrade first?", "How would you migrate 500 forms?", "What regression metrics would you monitor?", "How would you roll back?", "Which React 19 features would you postpone?"],
    relatedTopics: ["React 19 Upgrade Guide", "Codemods", "SSR", "Hydration", "Actions", "React Compiler", "Testing"],
  },
  {
    id: "reactadv-m8-150",
    number: "REACTADV-M8-150",
    title: "React 19 production architecture",
    difficulty: "Easy",
    experienceLevel: "5–8 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Stripe", "Atlassian", "Uber", "Airbnb"],
    category: "React 19 Features",
    expectedAnswer: "A production React 19 architecture should use each feature for the problem it solves: Actions and form Actions for mutation workflows, useActionState for action results, useOptimistic for predictable temporary UI, useFormStatus for reusable form controls, native metadata/resource APIs for document and loading concerns, use for supported resource/context consumption, and framework-specific Server Components/Server Actions where the full-stack framework supports them. The architecture still needs authentication, authorization, validation, caching, observability, accessibility, error boundaries and performance budgets.",
    deepExplanation: "```tsx\nimport { Suspense } from \"react\";\nimport { useActionState } from \"react\";\n\nasync function saveSettings(\n  _previous: { ok: boolean; message: string },\n  formData: FormData\n) {\n  const theme = String(formData.get(\"theme\") ?? \"\");\n\n  if (![\"light\", \"dark\"].includes(theme)) {\n    return { ok: false, message: \"Invalid theme\" };\n  }\n\n  // In a real application this mutation must be authenticated\n  // and authorized on the server.\n  return { ok: true, message: \"Settings saved\" };\n}\n\nexport function SettingsPage() {\n  const [state, action, pending] = useActionState(saveSettings, {\n    ok: false,\n    message: \"\",\n  });\n\n  return (\n    <Suspense fallback={<p role=\"status\">Loading settings…</p>}>\n      <>\n        <title>Settings</title>\n        <form action={action}>\n          <select name=\"theme\" defaultValue=\"light\">\n            <option value=\"light\">Light</option>\n            <option value=\"dark\">Dark</option>\n          </select>\n          <button disabled={pending}>\n            {pending ? \"Saving…\" : \"Save\"}\n          </button>\n        </form>\n        <p role=\"status\">{state.message}</p>\n      </>\n    </Suspense>\n  );\n}\n```",
    productionExample: "For a large SaaS platform, establish a feature boundary: local UI state stays local; server-owned data uses the framework/server-state layer; mutations use Actions where they simplify the flow; reusable form controls consume useFormStatus; optimistic UI is restricted to safe operations; resource hints are performance-reviewed; Server Components/Actions remain framework-specific; observability captures caught, uncaught and recoverable errors plus Core Web Vitals.",
    bestPractices: ["Define clear client/server ownership of state.", "Authenticate and authorize every server mutation.", "Centralize validation schemas where appropriate.", "Use error boundaries and structured action errors.", "Set performance budgets for JavaScript, CSS and third-party resources.", "Test SSR, hydration, forms, optimistic rollback and failure paths.", "Document which React 19 APIs are approved for the codebase."],
    tradeOffs: "Advantages: fewer bespoke patterns and stronger integration between rendering, forms and resources. Trade-offs: more framework/runtime coupling; more sophisticated SSR and server-state architecture; resource hints can waste bandwidth; optimistic and async workflows require careful failure handling.",
    commonMistakes: ["Using every React 19 feature simply because it exists.", "Treating React core as the owner of authentication/caching.", "Making all state server-side or all state client-side.", "Ignoring accessibility for pending/optimistic UI.", "Using resource hints without performance measurements.", "Coupling application code to private React internals."],
    followUpQuestions: ["Design React 19 architecture for a 500-page SaaS application.", "Which state belongs on the client vs server?", "How would you secure Server Actions?", "How would you standardize form patterns?", "How would you measure React 19 migration success?", "How would you handle optimistic conflicts?"],
    relatedTopics: ["Actions", "useActionState", "useOptimistic", "useFormStatus", "Server Components", "Server Actions", "SSR", "Hydration", "Resource Loading", "Error Boundaries", "Observability"],
  },

];

export const MOCK_ADVANCED_REACT_MODULE8_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Advanced React',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));