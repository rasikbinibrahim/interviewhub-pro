// Derived from frontend/src/document/Part_13_Frontend_Machine_Coding_Master_Handbook.md (Module 6 — File Explorer).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "machinecoding-m6-106",
    number: "MACHINECODING-M6-106",
    title: "Basic file explorer",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Basic file explorer as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Basic file explorer as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Basic file explorer logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Basic file explorer logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-107",
    number: "MACHINECODING-M6-107",
    title: "Nested folders",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Nested folders as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Nested folders as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Nested folders logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Nested folders logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-108",
    number: "MACHINECODING-M6-108",
    title: "Expand/collapse state",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Expand/collapse state as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Expand/collapse state as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Expand/collapse state logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Expand/collapse state logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-109",
    number: "MACHINECODING-M6-109",
    title: "Tree keyboard navigation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Tree keyboard navigation as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Tree keyboard navigation as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Tree keyboard navigation logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Tree keyboard navigation logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-110",
    number: "MACHINECODING-M6-110",
    title: "ARIA tree pattern",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement ARIA tree pattern as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement ARIA tree pattern as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the ARIA tree pattern logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the ARIA tree pattern logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-111",
    number: "MACHINECODING-M6-111",
    title: "Lazy folder loading",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Lazy folder loading as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Lazy folder loading as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Lazy folder loading logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Lazy folder loading logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-112",
    number: "MACHINECODING-M6-112",
    title: "Context menu",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Context menu as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Context menu as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Context menu logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Context menu logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-113",
    number: "MACHINECODING-M6-113",
    title: "Rename node",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Rename node as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Rename node as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Rename node logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Rename node logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-114",
    number: "MACHINECODING-M6-114",
    title: "Delete node",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Delete node as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Delete node as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Delete node logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Delete node logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-115",
    number: "MACHINECODING-M6-115",
    title: "Create folder",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Create folder as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Create folder as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Create folder logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Create folder logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-116",
    number: "MACHINECODING-M6-116",
    title: "Drag and drop files",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Drag and drop files as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Drag and drop files as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Drag and drop files logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Drag and drop files logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-117",
    number: "MACHINECODING-M6-117",
    title: "Search files",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Search files as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Search files as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface Item {\n  name: string;\n  role: string;\n}\n\nfunction searchItems(\n  items: readonly Item[],\n  term: string,\n): Item[] {\n  const query = term.trim().toLowerCase();\n  const result: Item[] = [];\n\n  for (let i = 0; i < items.length; i += 1) {\n    const item = items[i];\n\n    if (\n      item.name.toLowerCase().includes(query) ||\n      item.role.toLowerCase().includes(query)\n    ) {\n      result.push(item);\n    }\n  }\n\n  return result;\n}\n\nconst result = searchItems(\n  [\n    { name: \"Alice\", role: \"Developer\" },\n    { name: \"Bob\", role: \"Designer\" },\n  ],\n  \"dev\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst items = [\n  { name: \"Alice\", role: \"Developer\" },\n  { name: \"Bob\", role: \"Designer\" },\n];\n\nconst query = \"dev\";\n\nconst result = items.filter((item) =>\n  `${item.name} ${item.role}`\n    .toLowerCase()\n    .includes(query.toLowerCase()),\n);\n```\n\nStep 5 — Production example:\nKeep the Search files logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Search files logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-118",
    number: "MACHINECODING-M6-118",
    title: "File selection",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement File selection as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement File selection as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the File selection logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the File selection logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-119",
    number: "MACHINECODING-M6-119",
    title: "Multi-select files",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Multi-select files as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Multi-select files as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Multi-select files logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Multi-select files logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-120",
    number: "MACHINECODING-M6-120",
    title: "Breadcrumb navigation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Breadcrumb navigation as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Breadcrumb navigation as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Breadcrumb navigation logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Breadcrumb navigation logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-121",
    number: "MACHINECODING-M6-121",
    title: "File explorer virtualization",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement File explorer virtualization as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement File explorer virtualization as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the File explorer virtualization logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the File explorer virtualization logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-122",
    number: "MACHINECODING-M6-122",
    title: "File explorer testing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement File explorer testing as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement File explorer testing as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the File explorer testing logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the File explorer testing logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-123",
    number: "MACHINECODING-M6-123",
    title: "File explorer performance",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement File explorer performance as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement File explorer performance as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the File explorer performance logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the File explorer performance logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-124",
    number: "MACHINECODING-M6-124",
    title: "File explorer Storybook",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement File explorer Storybook as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement File explorer Storybook as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the File explorer Storybook logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the File explorer Storybook logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m6-125",
    number: "MACHINECODING-M6-125",
    title: "Enterprise file explorer",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "File Explorer",
    expectedAnswer: "Implement Enterprise file explorer as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Enterprise file explorer as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nfunction findNode(\n  node: TreeNode,\n  id: string,\n): TreeNode | null {\n  if (node.id === id) return node;\n\n  const children = node.children ?? [];\n\n  for (let i = 0; i < children.length; i += 1) {\n    const found = findNode(children[i], id);\n\n    if (found !== null) return found;\n  }\n\n  return null;\n}\n\nconst found = findNode(\n  {\n    id: \"root\",\n    label: \"Root\",\n    children: [\n      { id: \"child\", label: \"Child\" },\n    ],\n  },\n  \"child\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\ninterface TreeNode {\n  id: string;\n  label: string;\n  children?: TreeNode[];\n}\n\nconst root: TreeNode = {\n  id: \"root\",\n  label: \"Root\",\n  children: [\n    { id: \"child\", label: \"Child\" },\n  ],\n};\n\nconst flat: TreeNode[] = [\n  root,\n  ...(root.children ?? []),\n];\n\nconst found =\n  flat.find((node) => node.id === \"child\") ?? null;\n```\n\nStep 5 — Production example:\nKeep the Enterprise file explorer logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Enterprise file explorer logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---","# Module 7 – Tree View","**Questions 126–145**"],
  },
];

export const MOCK_FRONTEND_MACHINE_CODING_MODULE6_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Machine Coding',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));