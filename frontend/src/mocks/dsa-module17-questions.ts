// Derived from frontend/src/document/DSA_Master_Handbook_Senior_Frontend_Engineers.md (Module 17 — Bit Manipulation).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Definition, JavaScript/TypeScript implementation, Time/Space Complexity,
// Trade-offs, Common Mistakes, Interview Tricks, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Apple",
  "Netflix",
  "Uber",
  "Airbnb",
  "Atlassian",
  "Adobe",
  "LinkedIn",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "dsa-m17-161",
    number: "DSA-M17-161",
    title: "Binary representation",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Bit Manipulation",
    expectedAnswer: "Binary representation writes an integer as a sequence of bits. In JavaScript/TypeScript, bitwise operators work on signed 32-bit integer representations, so binary algorithms should explicitly account for that behavior.",
    deepExplanation: "Step 1 — Convert a non-negative integer to binary manually.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction toBinaryString(value: number): string {\n  if (!Number.isInteger(value) || value < 0) {\n    throw new Error(\"value must be a non-negative integer\");\n  }\n\n  if (value === 0) return \"0\";\n\n  let current = value;\n  let result = \"\";\n\n  while (current > 0) {\n    const bit = current % 2;\n    result = String(bit) + result;\n    current = Math.floor(current / 2);\n  }\n\n  return result;\n}\n```\n\nWITH BUILT-IN:\n```ts\nfunction toBinaryUsingBuiltIn(value: number): string {\n  if (!Number.isInteger(value) || value < 0) {\n    throw new Error(\"value must be a non-negative integer\");\n  }\n\n  return value.toString(2);\n}\n```\n\nStep 2 — Example:\n13 → divide by 2 repeatedly:\n13 → 6 remainder 1\n6 → 3 remainder 0\n3 → 1 remainder 1\n1 → 0 remainder 1\nbinary = `1101`.\n\nStep 3 — Bitwise inspection of the lower 32 bits:\n```ts\nfunction to32BitBinary(value: number): string {\n  return (value >>> 0).toString(2).padStart(32, \"0\");\n}\n```\n\nStep 4 — Complexity:\nManual conversion O(log n) arithmetic steps.\nBuilt-in conversion is also proportional to output size.\n\nImportant JavaScript rule:\nBitwise operators operate on signed 32-bit integers; `>>> 0` converts the resulting 32-bit pattern to an unsigned Number representation.",
    productionExample: "Binary representations are useful for compact flags, protocol fields, permissions, feature toggles, checksums, and low-level data formats.",
    bestPractices: ["State the bit invariant before coding.", "Remember JavaScript bitwise operators coerce Numbers to signed 32-bit integers.", "Use unsigned `>>> 0` when you need a non-negative 32-bit result.", "Use `BigInt` when the problem requires integers outside JavaScript's 32-bit bitwise range.", "Test zero, one, negative values when the problem specification allows them."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| Arithmetic conversion | Easy to reason about | More steps |\n| `toString(2)` | Simple and readable | Uses built-in conversion |\n| Bitwise operators | Fast bit-level operations | 32-bit coercion semantics |",
    commonMistakes: ["Confusing logical operators (`&&`, `||`) with bitwise operators (`&`, `|`, `^`).", "Forgetting that JavaScript bitwise operations use signed 32-bit integer coercion.", "Using `1 << 31` and expecting a positive 32-bit value.", "Using `>>` when unsigned right shift `>>>` is required.", "Ignoring operator precedence and missing parentheses."],
    followUpQuestions: ["What does the operator do to each bit?", "What is the time complexity?", "What is the space complexity?", "What happens for negative numbers?", "Why use `>>> 0`?", "When should you use BigInt?", "How do you test edge cases?", "How does operator precedence affect the expression?", "Can the solution be written without bitwise operators?", "Where is the technique useful in production?"],
    relatedTopics: ["Bit Manipulation", "Binary Numbers", "32-bit Integers", "Number Representation"],
  },
  {
    id: "dsa-m17-162",
    number: "DSA-M17-162",
    title: "AND OR XOR",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Bit Manipulation",
    expectedAnswer: "`&` performs bitwise AND, `|` performs bitwise OR, and `^` performs bitwise XOR. They operate bit-by-bit and are fundamental for masks, toggles, permissions, and parity-style algorithms.",
    deepExplanation: "Step 1 — Core truth table for one bit:\n- AND: 1 only when both bits are 1.\n- OR: 1 when either bit is 1.\n- XOR: 1 when the bits differ.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction bitwiseExample(a: number, b: number) {\n  return {\n    and: a & b,\n    or: a | b,\n    xor: a ^ b,\n  };\n}\n```\n\nStep 2 — Example:\na = `6` → `0110`\nb = `3` → `0011`\n\nAND → `0010` = 2\nOR  → `0111` = 7\nXOR → `0101` = 5\n\nStep 3 — Built-in-free XOR swapping example:\n```ts\nfunction xorSwap(a: number, b: number): [number, number] {\n  a = a ^ b;\n  b = a ^ b;\n  a = a ^ b;\n  return [a, b];\n}\n```\n\nStep 4 — WITH BUILT-IN / ordinary arithmetic alternative:\n```ts\nfunction arithmeticSwap(a: number, b: number): [number, number] {\n  const temp = a;\n  a = b;\n  b = temp;\n  return [a, b];\n}\n```\n\nComplexity:\nAll operations above are O(1).\n\nPractical note:\nUse normal variable swapping in application code; XOR swap is mainly an interview/bit-manipulation exercise.",
    productionExample: "AND/OR/XOR are useful for permission flags, feature flags, packed status values, checksums, compact state, and protocol parsing.",
    bestPractices: ["State the bit invariant before coding.", "Remember JavaScript bitwise operators coerce Numbers to signed 32-bit integers.", "Use unsigned `>>> 0` when you need a non-negative 32-bit result.", "Use `BigInt` when the problem requires integers outside JavaScript's 32-bit bitwise range.", "Test zero, one, negative values when the problem specification allows them.", "Use parentheses when combining several bitwise operations with comparisons or arithmetic."],
    tradeOffs: "| Operator | Main use | Example |\n|---|---|---|\n| `&` | Keep common set bits / test a mask | `(flags & READ) !== 0` |\n| `|` | Set bits | `flags | WRITE` |\n| `^` | Toggle/difference | `flags ^ OPTION` |",
    commonMistakes: ["Confusing logical operators (`&&`, `||`) with bitwise operators (`&`, `|`, `^`).", "Forgetting that JavaScript bitwise operations use signed 32-bit integer coercion.", "Using `1 << 31` and expecting a positive 32-bit value.", "Using `>>` when unsigned right shift `>>>` is required.", "Ignoring operator precedence and missing parentheses."],
    followUpQuestions: ["What does the operator do to each bit?", "What is the time complexity?", "What is the space complexity?", "What happens for negative numbers?", "Why use `>>> 0`?", "When should you use BigInt?", "How do you test edge cases?", "How does operator precedence affect the expression?", "Can the solution be written without bitwise operators?", "Where is the technique useful in production?", "What identity does XOR have?", "Why does `x ^ x` equal 0?", "Why does `x ^ 0` equal x?"],
    relatedTopics: ["Bitwise AND", "Bitwise OR", "Bitwise XOR", "Masks", "Permissions"],
  },
  {
    id: "dsa-m17-163",
    number: "DSA-M17-163",
    title: "Bit shifts",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Bit Manipulation",
    expectedAnswer: "Bit shifts move bits left or right. `<<` shifts left, `>>` shifts right with sign extension, and `>>>` shifts right with zero fill. In JavaScript, shifts operate on 32-bit integer representations.",
    deepExplanation: "Step 1 — Left shift:\n```ts\nfunction doubleByShift(value: number): number {\n  return value << 1;\n}\n```\n\nFor ordinary small non-negative integers, `value << 1` corresponds to multiplying by 2 within the 32-bit signed range.\n\nStep 2 — WITHOUT BUILT-IN / CORE right shifts:\n```ts\nfunction halveByShift(value: number): number {\n  return value >> 1;\n}\n\nfunction unsignedHalf(value: number): number {\n  return value >>> 1;\n}\n```\n\nStep 3 — Example:\n5 = `0101`\n5 << 1 → `1010` = 10\n\n20 >> 2 → 5\n\nStep 4 — WITH BUILT-IN arithmetic alternative:\n```ts\nfunction halveByMath(value: number): number {\n  return Math.floor(value / 2);\n}\n```\n\nImportant:\n`>>` preserves the sign bit.\n`>>>` fills with zeros and returns a non-negative 32-bit Number.\nBitwise arithmetic can overflow/truncate outside the signed 32-bit model.",
    productionExample: "Shifts are useful in packed binary formats, bitmap operations, protocol fields, bit masks, and performance-sensitive integer transformations where 32-bit semantics are intentional.",
    bestPractices: ["State the bit invariant before coding.", "Remember JavaScript bitwise operators coerce Numbers to signed 32-bit integers.", "Use unsigned `>>> 0` when you need a non-negative 32-bit result.", "Use `BigInt` when the problem requires integers outside JavaScript's 32-bit bitwise range.", "Test zero, one, negative values when the problem specification allows them.", "Use arithmetic operations when their semantics are clearer; use shifts when the bit representation itself is part of the problem."],
    tradeOffs: "| Operation | Benefit | Limitation |\n|---|---|---|\n| `<<` / `>>` / `>>>` | Direct bit-level manipulation | 32-bit coercion |\n| Arithmetic `* 2`, `/ 2` | Clear numeric intent | Not a bit-level operation |\n| BigInt shifts | Arbitrary-size integer bits | Different type and syntax |",
    commonMistakes: ["Confusing logical operators (`&&`, `||`) with bitwise operators (`&`, `|`, `^`).", "Forgetting that JavaScript bitwise operations use signed 32-bit integer coercion.", "Using `1 << 31` and expecting a positive 32-bit value.", "Using `>>` when unsigned right shift `>>>` is required.", "Ignoring operator precedence and missing parentheses.", "Assuming `>>` and `>>>` are interchangeable."],
    followUpQuestions: ["What does the operator do to each bit?", "What is the time complexity?", "What is the space complexity?", "What happens for negative numbers?", "Why use `>>> 0`?", "When should you use BigInt?", "How do you test edge cases?", "How does operator precedence affect the expression?", "Can the solution be written without bitwise operators?", "Where is the technique useful in production?", "What happens at bit 31?", "Why does `1 << 31` become negative?", "How do BigInt shifts differ?"],
    relatedTopics: ["Bit Shifts", "Signed Integers", "Unsigned Integers", "BigInt"],
  },
  {
    id: "dsa-m17-164",
    number: "DSA-M17-164",
    title: "Set and clear bits",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Bit Manipulation",
    expectedAnswer: "To set bit k, OR with `1 << k`. To clear bit k, AND with the inverse mask `~(1 << k)`. To toggle bit k, XOR with `1 << k`.",
    deepExplanation: "Step 1 — Define the mask.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction setBit(value: number, bit: number): number {\n  return value | (1 << bit);\n}\n\nfunction clearBit(value: number, bit: number): number {\n  return value & ~(1 << bit);\n}\n\nfunction toggleBit(value: number, bit: number): number {\n  return value ^ (1 << bit);\n}\n\nfunction isBitSet(value: number, bit: number): boolean {\n  return (value & (1 << bit)) !== 0;\n}\n```\n\nStep 2 — Example:\nvalue = 8 → binary `1000`\nSet bit 1:\n1000 | 0010 = 1010 → 10\n\nClear bit 3:\n1010 & 0111 = 0010 → 2\n\nToggle bit 1:\n0010 ^ 0010 = 0000 → 0\n\nStep 3 — WITH BUILT-IN helper for formatting:\n```ts\nfunction showBits(value: number): string {\n  return (value >>> 0).toString(2).padStart(32, \"0\");\n}\n```\n\nStep 4 — Complexity:\nEvery operation is O(1) time and O(1) space.\n\nNote:\nFor bit positions 0–31, JavaScript uses 32-bit signed coercion for these operators.",
    productionExample: "These operations map directly to feature flags, permission masks, compact UI state, protocol option fields, and binary configuration values.",
    bestPractices: ["State the bit invariant before coding.", "Remember JavaScript bitwise operators coerce Numbers to signed 32-bit integers.", "Use unsigned `>>> 0` when you need a non-negative 32-bit result.", "Use `BigInt` when the problem requires integers outside JavaScript's 32-bit bitwise range.", "Test zero, one, negative values when the problem specification allows them.", "Keep bit constants named (`READ`, `WRITE`, etc.) instead of scattering magic shifts through production code."],
    tradeOffs: "| Representation | Benefit | Cost |\n|---|---|---|\n| Bit mask | Very compact | Less readable without constants |\n| Boolean object fields | Very readable | More memory |\n| Enum/string flags | Explicit semantics | More storage/comparison overhead |",
    commonMistakes: ["Confusing logical operators (`&&`, `||`) with bitwise operators (`&`, `|`, `^`).", "Forgetting that JavaScript bitwise operations use signed 32-bit integer coercion.", "Using `1 << 31` and expecting a positive 32-bit value.", "Using `>>` when unsigned right shift `>>>` is required.", "Ignoring operator precedence and missing parentheses.", "Using `1 << bit` for a bit position outside the intended 32-bit range."],
    followUpQuestions: ["What does the operator do to each bit?", "What is the time complexity?", "What is the space complexity?", "What happens for negative numbers?", "Why use `>>> 0`?", "When should you use BigInt?", "How do you test edge cases?", "How does operator precedence affect the expression?", "Can the solution be written without bitwise operators?", "Where is the technique useful in production?", "How do you set multiple bits?", "How do you clear several flags at once?", "How do you test whether all flags in a mask are set?"],
    relatedTopics: ["Bit Masks", "Permission Flags", "AND", "OR", "XOR"],
  },
  {
    id: "dsa-m17-165",
    number: "DSA-M17-165",
    title: "Power of two",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Bit Manipulation",
    expectedAnswer: "A positive integer is a power of two exactly when it has one set bit. For 32-bit integer values, `n > 0 && (n & (n - 1)) === 0` checks this by removing the lowest set bit and testing whether nothing remains.",
    deepExplanation: "Step 1 — Core trick:\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction isPowerOfTwo(value: number): boolean {\n  return value > 0 && (value & (value - 1)) === 0;\n}\n```\n\nStep 2 — Why it works:\n8 = `1000`\n7 = `0111`\n8 & 7 = `0000`\n\n12 = `1100`\n11 = `1011`\n12 & 11 = `1000` ≠ 0\n\nStep 3 — WITH BUILT-IN arithmetic alternative:\n```ts\nfunction isPowerOfTwoUsingMath(value: number): boolean {\n  if (!Number.isInteger(value) || value <= 0) return false;\n\n  const log = Math.log2(value);\n  return Number.isInteger(log);\n}\n```\n\nStep 4 — Complexity:\nBitwise version O(1).\nMath version O(1), but floating-point/log semantics make the bit trick preferable for integer bit problems.\n\nJavaScript caveat:\nThe bitwise trick is for 32-bit integer values. For larger exact integers, use a BigInt version.",
    productionExample: "Power-of-two checks are common for buffer sizes, hash-table capacities, memory blocks, alignment rules, and binary-tree/heap indexing concepts.",
    bestPractices: ["State the bit invariant before coding.", "Remember JavaScript bitwise operators coerce Numbers to signed 32-bit integers.", "Use unsigned `>>> 0` when you need a non-negative 32-bit result.", "Use `BigInt` when the problem requires integers outside JavaScript's 32-bit bitwise range.", "Test zero, one, negative values when the problem specification allows them."],
    tradeOffs: "| Approach | Benefit | Cost |\n|---|---|---|\n| `n & (n-1)` | Simple integer bit invariant | 32-bit bitwise semantics |\n| `Math.log2` | General numeric expression | Floating-point semantics |\n| BigInt | Arbitrary integer size | Different type |",
    commonMistakes: ["Confusing logical operators (`&&`, `||`) with bitwise operators (`&`, `|`, `^`).", "Forgetting that JavaScript bitwise operations use signed 32-bit integer coercion.", "Using `1 << 31` and expecting a positive 32-bit value.", "Using `>>` when unsigned right shift `>>>` is required.", "Ignoring operator precedence and missing parentheses.", "Forgetting `value > 0`; zero also satisfies `0 & -1 === 0`."],
    followUpQuestions: ["What does the operator do to each bit?", "What is the time complexity?", "What is the space complexity?", "What happens for negative numbers?", "Why use `>>> 0`?", "When should you use BigInt?", "How do you test edge cases?", "How does operator precedence affect the expression?", "Can the solution be written without bitwise operators?", "Where is the technique useful in production?", "How do you test power of four?", "How do you implement it for BigInt?"],
    relatedTopics: ["Bit Manipulation", "Binary Numbers", "Bit Tricks", "Powers of Two"],
  },
  {
    id: "dsa-m17-166",
    number: "DSA-M17-166",
    title: "Single number",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Bit Manipulation",
    expectedAnswer: "When every element appears exactly twice except one value that appears once, XOR returns the single value because `x ^ x = 0` and `x ^ 0 = x`. XOR is also associative and commutative.",
    deepExplanation: "Step 1 — XOR every value.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction singleNumber(nums: readonly number[]): number {\n  let result = 0;\n\n  for (const value of nums) {\n    result ^= value;\n  }\n\n  return result;\n}\n```\n\nStep 2 — Example:\n[4,1,2,1,2]\n4 ^ 1 ^ 2 ^ 1 ^ 2\n\nPairs cancel:\n1^1 = 0\n2^2 = 0\nresult = 4\n\nStep 3 — WITH BUILT-IN:\n```ts\nfunction singleNumberUsingReduce(nums: readonly number[]): number {\n  return nums.reduce((result, value) => result ^ value, 0);\n}\n```\n\nStep 4 — Complexity:\nO(n) time, O(1) auxiliary space.\n\nImportant:\nThis exact solution depends on the \"all others appear exactly twice\" condition.",
    productionExample: "The cancellation property is useful for parity-style detection and compact identity checks, though production code should prefer explicit data structures when the input contract is more complicated.",
    bestPractices: ["State the bit invariant before coding.", "Remember JavaScript bitwise operators coerce Numbers to signed 32-bit integers.", "Use unsigned `>>> 0` when you need a non-negative 32-bit result.", "Use `BigInt` when the problem requires integers outside JavaScript's 32-bit bitwise range.", "Test zero, one, negative values when the problem specification allows them.", "State the duplicate-count contract explicitly before using XOR."],
    tradeOffs: "| Method | Benefit | Cost |\n|---|---|---|\n| XOR | O(1) extra memory | Limited to specific occurrence patterns |\n| Set | General duplicate tracking | O(n) memory |\n| Map frequency | Handles arbitrary counts | O(n) memory |",
    commonMistakes: ["Confusing logical operators (`&&`, `||`) with bitwise operators (`&`, `|`, `^`).", "Forgetting that JavaScript bitwise operations use signed 32-bit integer coercion.", "Using `1 << 31` and expecting a positive 32-bit value.", "Using `>>` when unsigned right shift `>>>` is required.", "Ignoring operator precedence and missing parentheses.", "Applying XOR when values can appear three or more times without a matching problem-specific property."],
    followUpQuestions: ["What does the operator do to each bit?", "What is the time complexity?", "What is the space complexity?", "What happens for negative numbers?", "Why use `>>> 0`?", "When should you use BigInt?", "How do you test edge cases?", "How does operator precedence affect the expression?", "Can the solution be written without bitwise operators?", "Where is the technique useful in production?", "How would you solve the variant where every other value appears three times?", "What if there are two single values?"],
    relatedTopics: ["XOR", "Parity", "Bit Tricks", "Hash Set"],
  },
  {
    id: "dsa-m17-167",
    number: "DSA-M17-167",
    title: "Counting bits",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Bit Manipulation",
    expectedAnswer: "Counting bits means finding how many 1-bits are set in an integer. A useful DP identity is `bits[i] = bits[i >> 1] + (i & 1)`, which removes the lowest binary digit and reuses the previous result.",
    deepExplanation: "Step 1 — Count set bits for one value.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction countSetBits(value: number): number {\n  let count = 0;\n  let current = value >>> 0;\n\n  while (current !== 0) {\n    current &= current - 1;\n    count++;\n  }\n\n  return count;\n}\n```\n\nStep 2 — Count bits for every number from 0 to n using DP:\n```ts\nfunction countBitsUpTo(n: number): number[] {\n  if (n < 0) return [];\n\n  const result = new Array<number>(n + 1).fill(0);\n\n  for (let value = 1; value <= n; value++) {\n    result[value] = result[value >> 1]! + (value & 1);\n  }\n\n  return result;\n}\n```\n\nStep 3 — WITH BUILT-IN:\n```ts\nfunction countSetBitsUsingString(value: number): number {\n  return (value >>> 0).toString(2).split(\"1\").length - 1;\n}\n```\n\nStep 4 — Example:\n5 = `101` → 2 set bits.\nFor n=5:\n[0,1,1,2,1,2]\n\nComplexity:\nBrian Kernighan loop: O(number of set bits), at most 32 for 32-bit values.\nDP up to n: O(n) time and O(n) output space.",
    productionExample: "Bit counts are useful for capability masks, feature combinations, compact state representations, population-count problems, and hardware-oriented algorithms.",
    bestPractices: ["State the bit invariant before coding.", "Remember JavaScript bitwise operators coerce Numbers to signed 32-bit integers.", "Use unsigned `>>> 0` when you need a non-negative 32-bit result.", "Use `BigInt` when the problem requires integers outside JavaScript's 32-bit bitwise range.", "Test zero, one, negative values when the problem specification allows them.", "Use unsigned coercion when interpreting a Number as an unsigned 32-bit bit pattern."],
    tradeOffs: "| Method | Benefit | Cost |\n|---|---|---|\n| `n & (n-1)` loop | Efficient per set bit | 32-bit semantics |\n| Shift every bit | Simple | More iterations |\n| String conversion | Readable demo | Allocations and slower path |",
    commonMistakes: ["Confusing logical operators (`&&`, `||`) with bitwise operators (`&`, `|`, `^`).", "Forgetting that JavaScript bitwise operations use signed 32-bit integer coercion.", "Using `1 << 31` and expecting a positive 32-bit value.", "Using `>>` when unsigned right shift `>>>` is required.", "Ignoring operator precedence and missing parentheses.", "Using `value >> 1` and assuming it behaves like unsigned division for negative values."],
    followUpQuestions: ["What does the operator do to each bit?", "What is the time complexity?", "What is the space complexity?", "What happens for negative numbers?", "Why use `>>> 0`?", "When should you use BigInt?", "How do you test edge cases?", "How does operator precedence affect the expression?", "Can the solution be written without bitwise operators?", "Where is the technique useful in production?", "What is Brian Kernighan's trick?", "How do you count bits in BigInt?", "How do you find parity from the bit count?"],
    relatedTopics: ["Bit Counting", "XOR", "Brian Kernighan", "Dynamic Programming"],
  },
  {
    id: "dsa-m17-168",
    number: "DSA-M17-168",
    title: "Bit masks",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Bit Manipulation",
    expectedAnswer: "A bit mask uses individual bits as compact boolean flags. A mask can test, set, clear, toggle, combine, and enumerate subsets of options using AND/OR/XOR and shifts.",
    deepExplanation: "Step 1 — Define named flags.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nconst READ = 1 << 0;\nconst WRITE = 1 << 1;\nconst EXECUTE = 1 << 2;\n\nfunction hasFlag(flags: number, mask: number): boolean {\n  return (flags & mask) === mask;\n}\n\nfunction addFlag(flags: number, mask: number): number {\n  return flags | mask;\n}\n\nfunction removeFlag(flags: number, mask: number): number {\n  return flags & ~mask;\n}\n\nfunction toggleFlag(flags: number, mask: number): number {\n  return flags ^ mask;\n}\n```\n\nStep 2 — Example:\nflags = READ | WRITE\n→ binary `011`\n\n`hasFlag(flags, WRITE)` → true\n`removeFlag(flags, WRITE)` → READ only.\n\nStep 3 — WITH BUILT-IN Set alternative:\n```ts\nfunction updateFlagsUsingSet(\n  flags: Set<string>,\n  flag: string,\n  enabled: boolean,\n): Set<string> {\n  const next = new Set(flags);\n\n  if (enabled) next.add(flag);\n  else next.delete(flag);\n\n  return next;\n}\n```\n\nStep 4 — Complexity:\nBit-mask operations O(1).\nSet operations are O(1) average but use more memory.",
    productionExample: "Bit masks work well for compact permissions, feature flags, keyboard modifiers, protocol options, and packed status state.",
    bestPractices: ["State the bit invariant before coding.", "Remember JavaScript bitwise operators coerce Numbers to signed 32-bit integers.", "Use unsigned `>>> 0` when you need a non-negative 32-bit result.", "Use `BigInt` when the problem requires integers outside JavaScript's 32-bit bitwise range.", "Test zero, one, negative values when the problem specification allows them.", "Use named constants so bit positions are self-documenting.", "Use immutable Set/object representations when readability is more important than compactness."],
    tradeOffs: "| Representation | Benefit | Cost |\n|---|---|---|\n| Bit mask | Very compact, O(1) operations | Harder to inspect/read |\n| Set<string> | Readable and dynamic | More memory |\n| Boolean object | Very explicit | Verbose/more fields |",
    commonMistakes: ["Confusing logical operators (`&&`, `||`) with bitwise operators (`&`, `|`, `^`).", "Forgetting that JavaScript bitwise operations use signed 32-bit integer coercion.", "Using `1 << 31` and expecting a positive 32-bit value.", "Using `>>` when unsigned right shift `>>>` is required.", "Ignoring operator precedence and missing parentheses.", "Writing `flags === mask` when the actual question is whether a mask is contained in flags."],
    followUpQuestions: ["What does the operator do to each bit?", "What is the time complexity?", "What is the space complexity?", "What happens for negative numbers?", "Why use `>>> 0`?", "When should you use BigInt?", "How do you test edge cases?", "How does operator precedence affect the expression?", "Can the solution be written without bitwise operators?", "Where is the technique useful in production?", "How do you test any flag versus all flags?", "How do you enumerate set flags?"],
    relatedTopics: ["Bit Manipulation", "Permission Flags", "AND", "OR", "XOR"],
  },
  {
    id: "dsa-m17-169",
    number: "DSA-M17-169",
    title: "Permission flags",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Bit Manipulation",
    expectedAnswer: "Permission flags use individual bits to represent independent capabilities such as read, write, and admin. Combining permissions with OR and testing them with AND gives compact constant-time checks.",
    deepExplanation: "Step 1 — Define the permission mask.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nconst PERMISSION_READ = 1 << 0;\nconst PERMISSION_WRITE = 1 << 1;\nconst PERMISSION_DELETE = 1 << 2;\nconst PERMISSION_ADMIN = 1 << 3;\n\nfunction hasPermission(\n  permissions: number,\n  required: number,\n): boolean {\n  return (permissions & required) === required;\n}\n\nfunction grantPermission(\n  permissions: number,\n  permission: number,\n): number {\n  return permissions | permission;\n}\n\nfunction revokePermission(\n  permissions: number,\n  permission: number,\n): number {\n  return permissions & ~permission;\n}\n```\n\nStep 2 — Example:\npermissions = READ | WRITE\nCheck READ → true\nGrant DELETE → READ | WRITE | DELETE\nRevoke WRITE → READ | DELETE\n\nStep 3 — WITH BUILT-IN readable alternative:\n```ts\ntype Permission = \"read\" | \"write\" | \"delete\" | \"admin\";\n\nfunction hasPermissionUsingSet(\n  permissions: ReadonlySet<Permission>,\n  required: Permission,\n): boolean {\n  return permissions.has(required);\n}\n```\n\nStep 4 — Complexity:\nBit-mask operations O(1) time and O(1) extra space.\nSet membership is O(1) average with higher memory usage.\n\nSecurity note:\nBit masks are only an encoding; authorization still needs server-side enforcement when the permission protects backend data or actions.",
    productionExample: "Frontend uses include capability flags, feature gating, keyboard modifiers, UI permissions, and compact state passed between layers. Authorization decisions should still be enforced at the backend.",
    bestPractices: ["State the bit invariant before coding.", "Remember JavaScript bitwise operators coerce Numbers to signed 32-bit integers.", "Use unsigned `>>> 0` when you need a non-negative 32-bit result.", "Use `BigInt` when the problem requires integers outside JavaScript's 32-bit bitwise range.", "Test zero, one, negative values when the problem specification allows them.", "Use named constants and document the bit assignments.", "Separate permission encoding from the actual authorization policy."],
    tradeOffs: "| Choice | Benefit | Cost |\n|---|---|---|\n| Bit mask | Compact and fast | 32-bit Number limitation |\n| Set/string roles | Readable | More memory |\n| Object booleans | Clear | Larger representation |",
    commonMistakes: ["Confusing logical operators (`&&`, `||`) with bitwise operators (`&`, `|`, `^`).", "Forgetting that JavaScript bitwise operations use signed 32-bit integer coercion.", "Using `1 << 31` and expecting a positive 32-bit value.", "Using `>>` when unsigned right shift `>>>` is required.", "Ignoring operator precedence and missing parentheses.", "Treating client-side permission flags as a security boundary."],
    followUpQuestions: ["What does the operator do to each bit?", "What is the time complexity?", "What is the space complexity?", "What happens for negative numbers?", "Why use `>>> 0`?", "When should you use BigInt?", "How do you test edge cases?", "How does operator precedence affect the expression?", "Can the solution be written without bitwise operators?", "Where is the technique useful in production?", "How do you test multiple required permissions?", "How should permissions be enforced on the server?"],
    relatedTopics: ["Bit Masks", "Authorization", "Feature Flags", "AND", "OR"],
  },
  {
    id: "dsa-m17-170",
    number: "DSA-M17-170",
    title: "Bitwise performance and pitfalls",
    difficulty: "Hard",
    experienceLevel: "5–8 Years / 8+ Years",
    category: "Bit Manipulation",
    expectedAnswer: "Bitwise operations are constant-time primitive operations, but in JavaScript they have important semantics: Numbers are coerced to signed 32-bit integers for bitwise operators, which can truncate large values and change sign. BigInt provides a separate arbitrary-size integer bitwise model.",
    deepExplanation: "Step 1 — Observe 32-bit coercion.\n\nWITHOUT BUILT-IN / CORE:\n```ts\nfunction asUnsigned32(value: number): number {\n  return value >>> 0;\n}\n\nfunction highestBitMask(): number {\n  return 1 << 31;\n}\n```\n\n`highestBitMask()` is negative because bit 31 is the sign bit in a signed 32-bit representation.\n\nStep 2 — WITH BUILT-IN / BigInt for wider exact bit manipulation:\n```ts\nfunction setLargeBit(value: bigint, bit: bigint): bigint {\n  return value | (1n << bit);\n}\n\nfunction hasLargeBit(value: bigint, bit: bigint): boolean {\n  return (value & (1n << bit)) !== 0n;\n}\n```\n\nStep 3 — Example:\n```ts\nconst value = 0xFFFFFFFF;\nconst unsigned = value >>> 0; // 4294967295\nconst signed = value | 0;     // -1\n```\n\nStep 4 — Practical performance rules:\n- Prefer ordinary arithmetic for ordinary numeric code.\n- Use bitwise operations when the bit representation is meaningful.\n- Avoid bit tricks that reduce readability without a measured benefit.\n- Do not mix Number and BigInt in the same arithmetic expression.\n\nComplexity:\nIndividual bitwise operations are O(1). Real performance depends on surrounding allocations, loops, conversions, and data representation.",
    productionExample: "Bitwise operations are valuable in compact protocol/state encodings, binary parsers, masks, graphics/bitmap work, and low-level data processing. They are less appropriate as a readability-first abstraction for ordinary business logic.",
    bestPractices: ["State the bit invariant before coding.", "Remember JavaScript bitwise operators coerce Numbers to signed 32-bit integers.", "Use unsigned `>>> 0` when you need a non-negative 32-bit result.", "Use `BigInt` when the problem requires integers outside JavaScript's 32-bit bitwise range.", "Test zero, one, negative values when the problem specification allows them.", "Document the signed/unsigned representation at API boundaries.", "Benchmark before claiming a bitwise rewrite is faster in application code."],
    tradeOffs: "| Choice | Benefit | Cost |\n|---|---|---|\n| Number + bitwise | Familiar and fast for 32-bit patterns | Signed 32-bit coercion |\n| BigInt | Arbitrary-size exact integer bits | Cannot mix directly with Number |\n| Arithmetic/boolean fields | Clearer business semantics | More storage/work in some encodings |",
    commonMistakes: ["Confusing logical operators (`&&`, `||`) with bitwise operators (`&`, `|`, `^`).", "Forgetting that JavaScript bitwise operations use signed 32-bit integer coercion.", "Using `1 << 31` and expecting a positive 32-bit value.", "Using `>>` when unsigned right shift `>>>` is required.", "Ignoring operator precedence and missing parentheses.", "Mixing Number and BigInt, which throws in many arithmetic operations.", "Assuming bitwise operators preserve arbitrary-precision Number values."],
    followUpQuestions: ["What does the operator do to each bit?", "What is the time complexity?", "What is the space complexity?", "What happens for negative numbers?", "Why use `>>> 0`?", "When should you use BigInt?", "How do you test edge cases?", "How does operator precedence affect the expression?", "Can the solution be written without bitwise operators?", "Where is the technique useful in production?", "When should you use BigInt?", "Why does `0xFFFFFFFF | 0` become -1?", "What does `>>> 0` accomplish?"],
    relatedTopics: ["Bit Manipulation", "JavaScript Numbers", "32-bit Integers", "BigInt", "Performance"],
  },
];

export const MOCK_DSA_MODULE17_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'DSA',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));