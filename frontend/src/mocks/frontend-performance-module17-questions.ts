// Derived from frontend/src/document/Part_11_Frontend_Performance_Optimization_Master_Handbook.md (Module 17 — Security & Performance).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "perf-m17-281",
    number: "PERF-M17-281",
    title: "XSS and performance",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security & Performance",
    expectedAnswer: "**XSS and performance** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m17-282",
    number: "PERF-M17-282",
    title: "CSP",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security & Performance",
    expectedAnswer: "**CSP** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m17-283",
    number: "PERF-M17-283",
    title: "Third-party scripts",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security & Performance",
    expectedAnswer: "**Third-party scripts** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m17-284",
    number: "PERF-M17-284",
    title: "Secure caching",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security & Performance",
    expectedAnswer: "**Secure caching** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m17-285",
    number: "PERF-M17-285",
    title: "Security headers",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security & Performance",
    expectedAnswer: "**Security headers** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m17-286",
    number: "PERF-M17-286",
    title: "Subresource Integrity",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security & Performance",
    expectedAnswer: "**Subresource Integrity** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m17-287",
    number: "PERF-M17-287",
    title: "iframe security",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security & Performance",
    expectedAnswer: "**iframe security** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m17-288",
    number: "PERF-M17-288",
    title: "Trusted Types",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security & Performance",
    expectedAnswer: "**Trusted Types** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m17-289",
    number: "PERF-M17-289",
    title: "Dependency security",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security & Performance",
    expectedAnswer: "**Dependency security** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m17-290",
    number: "PERF-M17-290",
    title: "Security-performance trade-offs",
    difficulty: "Medium",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Security & Performance",
    expectedAnswer: "**Security-performance trade-offs** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---","# Module 18 – Enterprise Optimization","**Questions 291–310**"],
  },
];

export const MOCK_FRONTEND_PERFORMANCE_MODULE17_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Performance',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
