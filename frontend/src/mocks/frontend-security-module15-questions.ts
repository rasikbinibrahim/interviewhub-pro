// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 15 — Secure React Applications).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m15-236",
    number: "SEC-M15-236",
    title: "React XSS protection",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**React XSS protection** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**React XSS protection** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse React XSS protection inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use React XSS protection inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-237",
    number: "SEC-M15-237",
    title: "dangerouslySetInnerHTML",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**dangerouslySetInnerHTML** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**dangerouslySetInnerHTML** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse dangerouslySetInnerHTML inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use dangerouslySetInnerHTML inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-238",
    number: "SEC-M15-238",
    title: "DOMPurify",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**DOMPurify** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**DOMPurify** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse DOMPurify inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use DOMPurify inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-239",
    number: "SEC-M15-239",
    title: "Trusted Types",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Trusted Types** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Trusted Types** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Trusted Types inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Trusted Types inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-240",
    number: "SEC-M15-240",
    title: "Secure routing",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Secure routing** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Secure routing** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Secure routing inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Secure routing inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-241",
    number: "SEC-M15-241",
    title: "Protected routes",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Protected routes** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Protected routes** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Protected routes inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Protected routes inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-242",
    number: "SEC-M15-242",
    title: "Token handling",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Token handling** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Token handling** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Token handling inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Token handling inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-243",
    number: "SEC-M15-243",
    title: "Secure forms",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Secure forms** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Secure forms** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Secure forms inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Secure forms inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-244",
    number: "SEC-M15-244",
    title: "Secure state",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Secure state** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Secure state** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Secure state inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Secure state inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-245",
    number: "SEC-M15-245",
    title: "Error boundaries",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Error boundaries** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Error boundaries** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Error boundaries inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Error boundaries inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-246",
    number: "SEC-M15-246",
    title: "Security logging",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Security logging** is a frontend security topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before implementing it.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Security logging** is a frontend security topic. Define the threat, trust boundary, secure default, enforcement point, and verification strategy before implementing it.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SecurityCheck {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(\n  checks: readonly SecurityCheck[],\n): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst secure = allPassed([\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst checks = [\n  { name: \"https\", passed: true },\n  { name: \"auth\", passed: true },\n];\n\nconst secure = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse Security logging as part of a layered security model with clear ownership and an auditable enforcement point.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Security logging as part of a layered security model with clear ownership and an auditable enforcement point.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-247",
    number: "SEC-M15-247",
    title: "Environment variables",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Environment variables** is a secure-coding topic. Treat inputs as untrusted, use safe APIs, validate at boundaries, avoid dangerous dynamic execution, and make the secure behavior the easiest behavior.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Environment variables** is a secure-coding topic. Treat inputs as untrusted, use safe APIs, validate at boundaries, avoid dangerous dynamic execution, and make the secure behavior the easiest behavior.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface UserInput {\n  value: string;\n}\n\nfunction normalize(\n  input: UserInput,\n): string {\n  let result = \"\";\n\n  for (let i = 0; i < input.value.length; i += 1) {\n    const code = input.value.charCodeAt(i);\n\n    if (code !== 0) {\n      result += input.value[i];\n    }\n  }\n\n  return result.trim();\n}\n\nconst value = normalize({\n  value: \" safe input \",\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \" safe input \";\n\nconst value = input.trim();\n```\n\nStep 5 — Production example:\nUse Environment variables at the data or API boundary. Prefer safe platform APIs, validate input, minimize data exposure, and make unsafe behavior difficult to express.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Environment variables at the data or API boundary. Prefer safe platform APIs, validate input, minimize data exposure, and make unsafe behavior difficult to express.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-248",
    number: "SEC-M15-248",
    title: "Safe links",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Safe links** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Safe links** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Safe links inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Safe links inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-249",
    number: "SEC-M15-249",
    title: "URL validation",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**URL validation** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**URL validation** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse URL validation inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use URL validation inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-250",
    number: "SEC-M15-250",
    title: "Third-party components",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Third-party components** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Third-party components** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Third-party components inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Third-party components inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-251",
    number: "SEC-M15-251",
    title: "React security testing",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**React security testing** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**React security testing** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse React security testing inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use React security testing inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-252",
    number: "SEC-M15-252",
    title: "CSP in React",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**CSP in React** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CSP in React** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse CSP in React inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use CSP in React inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-253",
    number: "SEC-M15-253",
    title: "Secure data fetching",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Secure data fetching** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Secure data fetching** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Secure data fetching inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Secure data fetching inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-254",
    number: "SEC-M15-254",
    title: "React security architecture",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**React security architecture** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**React security architecture** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse React security architecture inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use React security architecture inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m15-255",
    number: "SEC-M15-255",
    title: "Production React security checklist",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Secure React Applications",
    expectedAnswer: "**Production React security checklist** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Production React security checklist** is a React security topic. Keep untrusted data out of unsafe sinks, make security state explicit, and keep final authorization and secret handling outside the component tree.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface SafeRenderState {\n  text: string;\n}\n\nfunction toSafeText(\n  input: string,\n): SafeRenderState {\n  return { text: input };\n}\n\nconst state = toSafeText(\n  \"<img src=x onerror=alert(1)>\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst input = \"<img src=x onerror=alert(1)>\";\n\nconst element = document.createElement(\"span\");\nelement.textContent = input;\n\nconst safeText = element.textContent ?? \"\";\n```\n\nStep 5 — Production example:\nUse Production React security checklist inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Production React security checklist inside React without turning the component tree into a trust boundary. Keep dangerous sinks isolated and keep authorization/secret decisions on trusted services.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE15_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));