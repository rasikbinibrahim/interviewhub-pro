// Derived from frontend/src/document/Part_13_Frontend_Machine_Coding_Master_Handbook.md (Module 2 — Accordion Components).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "machinecoding-m2-21",
    number: "MACHINECODING-M2-21",
    title: "Basic accordion",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Basic accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Basic accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Basic accordion logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Basic accordion logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-22",
    number: "MACHINECODING-M2-22",
    title: "Single-open accordion",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Single-open accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Single-open accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Single-open accordion logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Single-open accordion logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-23",
    number: "MACHINECODING-M2-23",
    title: "Multi-open accordion",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Multi-open accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Multi-open accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Multi-open accordion logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Multi-open accordion logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-24",
    number: "MACHINECODING-M2-24",
    title: "Controlled accordion",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Controlled accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Controlled accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Controlled accordion logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Controlled accordion logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-25",
    number: "MACHINECODING-M2-25",
    title: "Uncontrolled accordion",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Uncontrolled accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Uncontrolled accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Uncontrolled accordion logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Uncontrolled accordion logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-26",
    number: "MACHINECODING-M2-26",
    title: "Keyboard navigation",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Keyboard navigation as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Keyboard navigation as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Keyboard navigation logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Keyboard navigation logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-27",
    number: "MACHINECODING-M2-27",
    title: "ARIA accordion pattern",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement ARIA accordion pattern as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement ARIA accordion pattern as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the ARIA accordion pattern logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the ARIA accordion pattern logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-28",
    number: "MACHINECODING-M2-28",
    title: "Animated accordion",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Animated accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Animated accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Animated accordion logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Animated accordion logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-29",
    number: "MACHINECODING-M2-29",
    title: "Lazy-rendered panels",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Lazy-rendered panels as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Lazy-rendered panels as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Lazy-rendered panels logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Lazy-rendered panels logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-30",
    number: "MACHINECODING-M2-30",
    title: "Nested accordion",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Nested accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Nested accordion as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Nested accordion logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Nested accordion logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-31",
    number: "MACHINECODING-M2-31",
    title: "Accordion with async content",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Accordion with async content as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Accordion with async content as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Accordion with async content logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Accordion with async content logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-32",
    number: "MACHINECODING-M2-32",
    title: "Accordion with URL state",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Accordion with URL state as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Accordion with URL state as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Accordion with URL state logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Accordion with URL state logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-33",
    number: "MACHINECODING-M2-33",
    title: "Accordion with search",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Accordion with search as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Accordion with search as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Accordion with search logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Accordion with search logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-34",
    number: "MACHINECODING-M2-34",
    title: "Accordion virtualization",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Accordion virtualization as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Accordion virtualization as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Accordion virtualization logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Accordion virtualization logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-35",
    number: "MACHINECODING-M2-35",
    title: "Accordion performance",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Accordion performance as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Accordion performance as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Accordion performance logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Accordion performance logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-36",
    number: "MACHINECODING-M2-36",
    title: "Accordion testing",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Accordion testing as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Accordion testing as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Accordion testing logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Accordion testing logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-37",
    number: "MACHINECODING-M2-37",
    title: "Accordion Storybook",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Accordion Storybook as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Accordion Storybook as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Accordion Storybook logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Accordion Storybook logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-38",
    number: "MACHINECODING-M2-38",
    title: "Accordion design tokens",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Accordion design tokens as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Accordion design tokens as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Accordion design tokens logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Accordion design tokens logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-39",
    number: "MACHINECODING-M2-39",
    title: "Accordion error handling",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Accordion error handling as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Accordion error handling as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Accordion error handling logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Accordion error handling logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m2-40",
    number: "MACHINECODING-M2-40",
    title: "Enterprise accordion API",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Accordion Components",
    expectedAnswer: "Implement Enterprise accordion API as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.",
    deepExplanation: "Step 1 — Understand the requirement.\n\nImplement Enterprise accordion API as a small, typed responsibility with explicit inputs, outputs, state ownership, and edge-case handling.\n\nStep 2 — Easy method:\nFirst define the state/data model, then implement one small pure operation, then connect it to the UI or side effect. Keep the source of truth in one place and derive everything else from it.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nWrite the loops, conditionals, immutable updates, or small helper logic explicitly so the algorithm is easy to explain in an interview.\n\n```ts\ntype Panel = {\n  id: string;\n  open: boolean;\n};\n\nfunction togglePanel(\n  panels: readonly Panel[],\n  id: string,\n): Panel[] {\n  const next: Panel[] = [];\n\n  for (let i = 0; i < panels.length; i += 1) {\n    const panel = panels[i];\n\n    next.push(\n      panel.id === id\n        ? { ...panel, open: !panel.open }\n        : panel,\n    );\n  }\n\n  return next;\n}\n\nconst result = togglePanel(\n  [{ id: \"one\", open: false }],\n  \"one\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native array, collection, date, or browser helper when it makes the implementation clearer without hiding an important algorithmic idea.\n\n```ts\nconst panels = [\n  { id: \"one\", open: false },\n  { id: \"two\", open: true },\n];\n\nconst result = panels.map((panel) =>\n  panel.id === \"one\"\n    ? { ...panel, open: !panel.open }\n    : panel,\n);\n```\n\nStep 5 — Production example:\nKeep the Enterprise accordion API logic isolated from rendering and external side effects so it can be unit-tested independently.\n\nStep 6 — Complexity:\nThe exact complexity depends on the collection size or interaction frequency. The manual examples deliberately expose the loop or recursion so the time/space cost can be explained; the built-in form expresses the same intent more concisely.\n\nStep 7 — Edge cases:\nHandle empty data, duplicate IDs, rapid interaction, slow/failed network, keyboard-only input, small screens, and large datasets when relevant to this problem.\n\nStep 8 — Senior interview takeaway:\nExplain why the state is owned here, what can be reused, what should remain controlled by the parent, and what you would optimize only after measuring.",
    productionExample: "Keep the Enterprise accordion API logic isolated from rendering and external side effects so it can be unit-tested independently.",
    bestPractices: ["Clarify the required behavior before writing code.", "Keep state ownership explicit and keep derived state out of the store when it can be calculated.", "Use stable keys/identifiers and typed models.", "Handle loading, empty, error, keyboard, responsive, and large-data cases explicitly.", "Keep side effects isolated so the core logic remains easy to test."],
    tradeOffs: "The manual version is more verbose but ideal for explaining the algorithm step by step; the built-in version is shorter and more maintainable for production when the underlying behavior is well understood.",
    commonMistakes: ["Implementing only the happy path.", "Using array index as a stable identifier.", "Duplicating derived state.", "Ignoring keyboard and screen-reader behavior.", "Adding optimization without measuring the real bottleneck."],
    followUpQuestions: ["What is the simplest implementation?", "What state is local versus controlled?", "How would you make it reusable?", "How would you handle 10,000+ items?", "How would you test it?", "What are the accessibility requirements?", "How would you handle API failure or slow network?", "How would you optimize it after measuring?", "How would you evolve the API without breaking consumers?", "What trade-offs would you explain in an interview?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---","# Module 3 – Tabs","**Questions 41–60**"],
  },
];

export const MOCK_FRONTEND_MACHINE_CODING_MODULE2_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Machine Coding',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));