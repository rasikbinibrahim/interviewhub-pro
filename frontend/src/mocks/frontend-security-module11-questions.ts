// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 11 — Clickjacking & UI Security).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m11-176",
    number: "SEC-M11-176",
    title: "Clickjacking",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Clickjacking & UI Security",
    expectedAnswer: "**Clickjacking** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Clickjacking** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface FramePolicy {\n  allowedParents: readonly string[];\n}\n\nfunction canEmbed(\n  policy: FramePolicy,\n  origin: string,\n): boolean {\n  for (let i = 0; i < policy.allowedParents.length; i += 1) {\n    if (policy.allowedParents[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = canEmbed(\n  { allowedParents: [\"https://portal.example.com\"] },\n  \"https://portal.example.com\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst allowedParents = new Set([\n  \"https://portal.example.com\",\n]);\n\nconst allowed =\n  allowedParents.has(\"https://portal.example.com\");\n```\n\nStep 5 — Production example:\nUse Clickjacking at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Clickjacking at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m11-177",
    number: "SEC-M11-177",
    title: "Frame-ancestors",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Clickjacking & UI Security",
    expectedAnswer: "**Frame-ancestors** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Frame-ancestors** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface FramePolicy {\n  allowedParents: readonly string[];\n}\n\nfunction canEmbed(\n  policy: FramePolicy,\n  origin: string,\n): boolean {\n  for (let i = 0; i < policy.allowedParents.length; i += 1) {\n    if (policy.allowedParents[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = canEmbed(\n  { allowedParents: [\"https://portal.example.com\"] },\n  \"https://portal.example.com\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst allowedParents = new Set([\n  \"https://portal.example.com\",\n]);\n\nconst allowed =\n  allowedParents.has(\"https://portal.example.com\");\n```\n\nStep 5 — Production example:\nUse Frame-ancestors at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Frame-ancestors at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m11-178",
    number: "SEC-M11-178",
    title: "X-Frame-Options",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Clickjacking & UI Security",
    expectedAnswer: "**X-Frame-Options** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**X-Frame-Options** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface FramePolicy {\n  allowedParents: readonly string[];\n}\n\nfunction canEmbed(\n  policy: FramePolicy,\n  origin: string,\n): boolean {\n  for (let i = 0; i < policy.allowedParents.length; i += 1) {\n    if (policy.allowedParents[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = canEmbed(\n  { allowedParents: [\"https://portal.example.com\"] },\n  \"https://portal.example.com\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst allowedParents = new Set([\n  \"https://portal.example.com\",\n]);\n\nconst allowed =\n  allowedParents.has(\"https://portal.example.com\");\n```\n\nStep 5 — Production example:\nUse X-Frame-Options at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use X-Frame-Options at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m11-179",
    number: "SEC-M11-179",
    title: "Iframe sandbox",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Clickjacking & UI Security",
    expectedAnswer: "**Iframe sandbox** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Iframe sandbox** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface FramePolicy {\n  allowedParents: readonly string[];\n}\n\nfunction canEmbed(\n  policy: FramePolicy,\n  origin: string,\n): boolean {\n  for (let i = 0; i < policy.allowedParents.length; i += 1) {\n    if (policy.allowedParents[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = canEmbed(\n  { allowedParents: [\"https://portal.example.com\"] },\n  \"https://portal.example.com\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst allowedParents = new Set([\n  \"https://portal.example.com\",\n]);\n\nconst allowed =\n  allowedParents.has(\"https://portal.example.com\");\n```\n\nStep 5 — Production example:\nUse Iframe sandbox at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Iframe sandbox at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m11-180",
    number: "SEC-M11-180",
    title: "UI redressing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Clickjacking & UI Security",
    expectedAnswer: "**UI redressing** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**UI redressing** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface FramePolicy {\n  allowedParents: readonly string[];\n}\n\nfunction canEmbed(\n  policy: FramePolicy,\n  origin: string,\n): boolean {\n  for (let i = 0; i < policy.allowedParents.length; i += 1) {\n    if (policy.allowedParents[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = canEmbed(\n  { allowedParents: [\"https://portal.example.com\"] },\n  \"https://portal.example.com\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst allowedParents = new Set([\n  \"https://portal.example.com\",\n]);\n\nconst allowed =\n  allowedParents.has(\"https://portal.example.com\");\n```\n\nStep 5 — Production example:\nUse UI redressing at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use UI redressing at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m11-181",
    number: "SEC-M11-181",
    title: "Overlay attacks",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Clickjacking & UI Security",
    expectedAnswer: "**Overlay attacks** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Overlay attacks** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface FramePolicy {\n  allowedParents: readonly string[];\n}\n\nfunction canEmbed(\n  policy: FramePolicy,\n  origin: string,\n): boolean {\n  for (let i = 0; i < policy.allowedParents.length; i += 1) {\n    if (policy.allowedParents[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = canEmbed(\n  { allowedParents: [\"https://portal.example.com\"] },\n  \"https://portal.example.com\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst allowedParents = new Set([\n  \"https://portal.example.com\",\n]);\n\nconst allowed =\n  allowedParents.has(\"https://portal.example.com\");\n```\n\nStep 5 — Production example:\nUse Overlay attacks at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Overlay attacks at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m11-182",
    number: "SEC-M11-182",
    title: "Pointer interception",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Clickjacking & UI Security",
    expectedAnswer: "**Pointer interception** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Pointer interception** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface FramePolicy {\n  allowedParents: readonly string[];\n}\n\nfunction canEmbed(\n  policy: FramePolicy,\n  origin: string,\n): boolean {\n  for (let i = 0; i < policy.allowedParents.length; i += 1) {\n    if (policy.allowedParents[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = canEmbed(\n  { allowedParents: [\"https://portal.example.com\"] },\n  \"https://portal.example.com\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst allowedParents = new Set([\n  \"https://portal.example.com\",\n]);\n\nconst allowed =\n  allowedParents.has(\"https://portal.example.com\");\n```\n\nStep 5 — Production example:\nUse Pointer interception at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Pointer interception at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m11-183",
    number: "SEC-M11-183",
    title: "Window.opener",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Clickjacking & UI Security",
    expectedAnswer: "**Window.opener** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Window.opener** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface FramePolicy {\n  allowedParents: readonly string[];\n}\n\nfunction canEmbed(\n  policy: FramePolicy,\n  origin: string,\n): boolean {\n  for (let i = 0; i < policy.allowedParents.length; i += 1) {\n    if (policy.allowedParents[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = canEmbed(\n  { allowedParents: [\"https://portal.example.com\"] },\n  \"https://portal.example.com\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst allowedParents = new Set([\n  \"https://portal.example.com\",\n]);\n\nconst allowed =\n  allowedParents.has(\"https://portal.example.com\");\n```\n\nStep 5 — Production example:\nUse Window.opener at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use Window.opener at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m11-184",
    number: "SEC-M11-184",
    title: "noopener",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Clickjacking & UI Security",
    expectedAnswer: "**noopener** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**noopener** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface FramePolicy {\n  allowedParents: readonly string[];\n}\n\nfunction canEmbed(\n  policy: FramePolicy,\n  origin: string,\n): boolean {\n  for (let i = 0; i < policy.allowedParents.length; i += 1) {\n    if (policy.allowedParents[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = canEmbed(\n  { allowedParents: [\"https://portal.example.com\"] },\n  \"https://portal.example.com\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst allowedParents = new Set([\n  \"https://portal.example.com\",\n]);\n\nconst allowed =\n  allowedParents.has(\"https://portal.example.com\");\n```\n\nStep 5 — Production example:\nUse noopener at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use noopener at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m11-185",
    number: "SEC-M11-185",
    title: "noreferrer",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Clickjacking & UI Security",
    expectedAnswer: "**noreferrer** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**noreferrer** is a browser/UI security boundary problem. The key is controlling framing, opener relationships, user interaction, and which origins are allowed to embed or navigate the application.\n\nStep 2 — Easy method:\nDefine the protected asset, attacker capability, trust boundary, secure default, enforcement point, and recovery path before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core policy or state check so you can explain the security reasoning line by line during the interview.\n\n```ts\ninterface FramePolicy {\n  allowedParents: readonly string[];\n}\n\nfunction canEmbed(\n  policy: FramePolicy,\n  origin: string,\n): boolean {\n  for (let i = 0; i < policy.allowedParents.length; i += 1) {\n    if (policy.allowedParents[i] === origin) return true;\n  }\n\n  return false;\n}\n\nconst allowed = canEmbed(\n  { allowedParents: [\"https://portal.example.com\"] },\n  \"https://portal.example.com\",\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same control clearer without hiding the security boundary.\n\n```ts\nconst allowedParents = new Set([\n  \"https://portal.example.com\",\n]);\n\nconst allowed =\n  allowedParents.has(\"https://portal.example.com\");\n```\n\nStep 5 — Production example:\nUse noreferrer at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.\n\nStep 6 — Validation:\nTest the intended path, denied path, malformed input, missing/expired state, misconfiguration, and recovery/rollback behavior where applicable.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where final enforcement occurs, why the default is safe, and how monitoring or testing prevents recurrence.",
    productionExample: "Use noreferrer at the browser framing/navigation boundary. Keep embedding permissions narrow and verify response headers and window relationships in browser tests.",
    bestPractices: ["Define the asset, attacker capability, and trust boundary first.", "Enforce security-sensitive decisions at a trusted boundary.", "Prefer least privilege, explicit allow-lists, and secure defaults.", "Do not log passwords, tokens, secrets, or unnecessary personal data.", "Test both successful and denied/invalid paths."],
    tradeOffs: "The manual/core version is intentionally explicit for interview explanation. The built-in/browser version is shorter for production. Prefer the approach that preserves a clear security boundary, least privilege, and maintainability.",
    commonMistakes: ["Treating frontend checks as the final security control.", "Confusing CORS, CSRF, authentication, and authorization.", "Logging sensitive credentials or personal information.", "Using broad allow-lists or permissive defaults.", "Assuming one control such as CSP or HTTPS solves the whole risk."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What is the secure default?", "How would you test the allowed and denied paths?", "What happens when the control is misconfigured?", "What telemetry would detect recurrence?", "How would you migrate a legacy implementation?", "What is the main trade-off?", "How would you explain it in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE11_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));