// Derived from frontend/src/document/Part_12_Frontend_Security_Master_Handbook.md (Module 2 — Authentication).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "sec-m2-21",
    number: "SEC-M2-21",
    title: "Session authentication",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Session authentication** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Session authentication** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Session authentication in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Session authentication in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-22",
    number: "SEC-M2-22",
    title: "JWT",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**JWT** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**JWT** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse JWT in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use JWT in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-23",
    number: "SEC-M2-23",
    title: "Access tokens",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Access tokens** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Access tokens** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Access tokens in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Access tokens in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-24",
    number: "SEC-M2-24",
    title: "Refresh tokens",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Refresh tokens** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Refresh tokens** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Refresh tokens in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Refresh tokens in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-25",
    number: "SEC-M2-25",
    title: "OAuth 2.1",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**OAuth 2.1** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**OAuth 2.1** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse OAuth 2.1 in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use OAuth 2.1 in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-26",
    number: "SEC-M2-26",
    title: "OpenID Connect",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**OpenID Connect** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**OpenID Connect** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse OpenID Connect in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use OpenID Connect in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-27",
    number: "SEC-M2-27",
    title: "PKCE",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**PKCE** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**PKCE** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse PKCE in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use PKCE in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-28",
    number: "SEC-M2-28",
    title: "MFA",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**MFA** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**MFA** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse MFA in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use MFA in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-29",
    number: "SEC-M2-29",
    title: "SSO",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**SSO** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**SSO** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse SSO in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use SSO in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-30",
    number: "SEC-M2-30",
    title: "Passwordless authentication",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Passwordless authentication** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Passwordless authentication** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Passwordless authentication in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Passwordless authentication in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-31",
    number: "SEC-M2-31",
    title: "Passkeys",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Passkeys** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Passkeys** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Passkeys in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Passkeys in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-32",
    number: "SEC-M2-32",
    title: "WebAuthn",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**WebAuthn** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**WebAuthn** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse WebAuthn in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use WebAuthn in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-33",
    number: "SEC-M2-33",
    title: "FIDO2",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**FIDO2** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**FIDO2** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse FIDO2 in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use FIDO2 in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-34",
    number: "SEC-M2-34",
    title: "Session management",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Session management** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Session management** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Session management in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Session management in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-35",
    number: "SEC-M2-35",
    title: "Token rotation",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Token rotation** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Token rotation** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Token rotation in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Token rotation in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-36",
    number: "SEC-M2-36",
    title: "Silent refresh",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Silent refresh** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Silent refresh** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Silent refresh in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Silent refresh in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-37",
    number: "SEC-M2-37",
    title: "Secure logout",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Secure logout** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Secure logout** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Secure logout in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Secure logout in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-38",
    number: "SEC-M2-38",
    title: "Authentication state",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Authentication state** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Authentication state** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Authentication state in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Authentication state in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-39",
    number: "SEC-M2-39",
    title: "Identity providers",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Identity providers** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Identity providers** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Identity providers in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Identity providers in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
  {
    id: "sec-m2-40",
    number: "SEC-M2-40",
    title: "Authentication architecture",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Authentication",
    expectedAnswer: "**Authentication architecture** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Authentication architecture** is an authentication/identity topic. Explain how identity is established, where credentials/tokens live, how expiry/rotation/logout work, and which trust decisions must remain server-side.\n\nStep 2 — Easy method:\nDefine the asset, attacker capability, trust boundary, secure default, enforcement point, and failure response before writing code.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example exposes the core decision or policy check so you can explain the security reasoning line by line.\n\n```ts\ninterface Session {\n  userId: string;\n  expiresAt: number;\n}\n\nfunction sessionIsValid(\n  session: Session,\n  now: number,\n): boolean {\n  return (\n    session.userId.length > 0 &&\n    session.expiresAt > now\n  );\n}\n\nconst valid = sessionIsValid(\n  { userId: \"u1\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collections or browser APIs when they make the same security decision clearer without hiding the important control.\n\n```ts\nconst session = {\n  userId: \"u1\",\n  expiresAt: 2_000,\n};\n\nconst valid =\n  Boolean(session.userId) &&\n  session.expiresAt > 1_000;\n```\n\nStep 5 — Production example:\nUse Authentication architecture in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.\n\nStep 6 — Validation:\nTest the allowed path, denied path, malformed input, missing credential, expired state, and misconfiguration that could broaden access.\n\nStep 7 — Senior interview takeaway:\nExplain what is trusted, what is untrusted, where the final enforcement happens, what the secure default is, and how the control is monitored and migrated.",
    productionExample: "Use Authentication architecture in a dedicated identity flow with explicit expiry, rotation, logout, and failure handling. Keep final identity and permission checks on the trusted backend.",
    bestPractices: ["Enforce security-sensitive decisions at a trusted boundary.", "Use least privilege and explicit allow-lists where practical.", "Do not log secrets, tokens, passwords, or sensitive payloads.", "Prefer secure-by-default browser and server controls.", "Test both the intended path and the failure/denial path."],
    tradeOffs: "The manual/core example is intentionally explicit for interview reasoning. The built-in/browser version is shorter for production. Security controls should favor correctness and least privilege over convenience.",
    commonMistakes: ["Treating frontend checks as the final authorization control.", "Confusing CORS with authentication or CSRF protection.", "Logging credentials or sensitive tokens.", "Using wildcard origins or overly broad permissions without a clear need.", "Assuming a security header alone solves an application vulnerability."],
    followUpQuestions: ["What is the attacker capability?", "What is the trust boundary?", "What must be enforced server-side?", "What browser control reduces the attack surface?", "How would you test the defense?", "What happens when the control is misconfigured?", "What is the secure default?", "How would you monitor failures?", "How would you migrate legacy clients?", "What trade-off would you explain in a senior interview?"],
    relatedTopics: ["OWASP","OAuth 2.1","OIDC","PKCE","CSP","Trusted Types","SameSite","CORS","CSRF","XSS","Secure React","Supply Chain","SAST","DAST","---"],
  },
];

export const MOCK_FRONTEND_SECURITY_MODULE2_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Security',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));