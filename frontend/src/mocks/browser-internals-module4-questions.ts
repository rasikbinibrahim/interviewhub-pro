// Derived from frontend/src/document/Part_6_Browser_Internals_Web_APIs_Master_Handbook.md (Module 4 — CSSOM & Render Tree).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question, Execution/Flow, Advanced Production Example, Performance/Security Notes,
// Best Practices, Advantages/Disadvantages, Common Mistakes/Interview Traps, Senior
// Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "jsbrowser-m4-56",
    number: "JSBROWSER-M4-56",
    title: "CSSOM definition",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "The CSS Object Model (CSSOM) is the browser's structured representation of CSS rules and style information. It includes stylesheet/rule structures and computed style access, and it participates with the DOM in determining how elements are styled. CSSOM is distinct from the DOM and from the final layout/paint output.",
    deepExplanation: "js\nconst sheet = document.styleSheets[0];\nconst rules = sheet?.cssRules;\n\nconsole.log(rules?.length);\n```\n\nFor element-level resolved values, `getComputedStyle(element)` exposes the browser's computed style result. The CSSOM is the bridge between parsed stylesheet rules and style resolution.",
    productionExample: "A design system uses CSS classes/custom properties for most styling and reserves CSSOM APIs for infrastructure such as theme tooling, diagnostics and third-party integrations. The application avoids repeatedly reading computed styles during animation loops.",
    bestPractices: ["Keep normal styling declarative.","Use CSS custom properties for runtime theme values where appropriate.","Avoid broad CSSOM traversal in hot paths.","Respect same-origin restrictions on stylesheet rule access.","Measure style/recalculation cost."],
    tradeOffs: "CSSOM gives scriptable access to CSS structures, but direct manipulation is more complex than classes/styles and some stylesheet rule access is restricted by origin/security policy.",
    commonMistakes: ["Calling CSSOM the same thing as computed style.","Assuming all stylesheet cssRules are readable cross-origin.","Using CSSOM APIs for ordinary component state changes."],
    followUpQuestions: ["CSSOM versus CSSStyleDeclaration?","Why can cross-origin cssRules throw?","How does CSSOM feed style calculation?"],
    relatedTopics: ["CSSOM","CSSStyleSheet","CSSRule","Computed style","DOM"],
  },
  {
    id: "jsbrowser-m4-57",
    number: "JSBROWSER-M4-57",
    title: "CSS parsing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "CSS parsing converts stylesheet text into CSS rules and declarations that the browser can use during style resolution. Invalid declarations are generally ignored or recovered according to the CSS syntax rules, allowing the rest of a stylesheet to remain useful.",
    deepExplanation: "css\n.card {\n  color: blue;\n  padding: 1rem;\n}\n\n@media (min-width: 768px) {\n  .card {\n    padding: 2rem;\n  }\n}\n```\n\nThe browser parses selectors, declarations, at-rules and values before applying them during style calculation.",
    productionExample: "A production app reduces CSS startup cost by code-splitting non-critical styles, removing unused CSS and avoiding huge generated stylesheets. Performance traces verify whether CSS download/parse/style work affects first render.",
    bestPractices: ["Keep critical CSS reasonably small.","Remove dead CSS where tooling permits.","Avoid generating enormous selector/declaration sets.","Use modern CSS features deliberately and test browser support.","Measure CSS transfer and style calculation."],
    tradeOffs: "Smaller CSS can improve parsing and style work, but aggressive removal tooling can break dynamically generated class usage. Splitting CSS also adds loading coordination.",
    commonMistakes: ["Treating CSS parsing as JavaScript execution.","Assuming unused CSS is always removed safely.","Ignoring dynamically generated classes in purge tooling."],
    followUpQuestions: ["What happens after CSS parsing?","How does a parser recover from invalid CSS?","How do CSS size and selector complexity affect startup?"],
    relatedTopics: ["CSS parser","Stylesheets","Selectors","At-rules","Style calculation"],
  },
  {
    id: "jsbrowser-m4-58",
    number: "JSBROWSER-M4-58",
    title: "CSSOM construction",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "CSSOM construction represents parsed stylesheet rules in a structure the browser can use for cascading and style resolution. As stylesheets arrive or change, the browser updates the relevant CSS state and may invalidate styles for affected elements.",
    deepExplanation: "js\nconst style = document.createElement(\"style\");\nstyle.textContent = `\n  .notice { color: red; }\n`;\ndocument.head.append(style);\n```\n\nAdding a stylesheet changes the CSSOM and can trigger style invalidation/recalculation for matching elements.",
    productionExample: "A theme engine injects a small scoped stylesheet once when a tenant theme changes, instead of repeatedly editing hundreds of inline styles. The change is measured for style/recalculation cost.",
    bestPractices: ["Prefer class/custom-property changes over rebuilding stylesheets repeatedly.","Scope dynamic styles when possible.","Keep injected stylesheets bounded and removable.","Clean up temporary style elements."],
    tradeOffs: "Dynamic CSSOM updates are flexible but can invalidate many nodes and make style ownership difficult. Static class/token changes are usually easier to reason about.",
    commonMistakes: ["Creating a new `<style>` tag on every render.","Leaving abandoned style sheets in the document.","Injecting broad selectors for a small component change."],
    followUpQuestions: ["How does inserting a style element affect style recalculation?","CSSOM mutation versus class toggle?","How would you clean up dynamic styles?"],
    relatedTopics: ["CSSStyleSheet","Style recalculation","CSSOM mutation","Themes"],
  },
  {
    id: "jsbrowser-m4-59",
    number: "JSBROWSER-M4-59",
    title: "Render tree",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "The render tree is the browser's representation of the nodes and styles that participate in visual rendering. Conceptually it combines DOM structure with resolved style information, excluding content that does not generate rendered boxes. Modern engines have more specialized internal trees, so 'render tree' is a useful conceptual model rather than a single standardized object.",
    deepExplanation: "text\nDOM + CSSOM\n    ↓\nstyle resolution\n    ↓\nrender/layout representation\n    ↓\nlayout\n    ↓\npaint\n    ↓\ncompositing\n```\n\nElements such as `display:none` do not contribute a rendered box, while `visibility:hidden` still affects layout but is not painted visibly.",
    productionExample: "A performance investigation compares DOM size with the amount of content that actually participates in layout. The team removes hidden but unnecessary subtrees and virtualizes long lists instead of optimizing arbitrary DOM calls.",
    bestPractices: ["Use the render-tree model as a conceptual diagnostic tool.","Understand that display/visibility affect rendering differently.","Do not assume one internal tree exists in every engine.","Measure layout/paint rather than relying only on DOM counts."],
    tradeOffs: "The conceptual model is useful for interviews and debugging, but browser internals evolve and differ between engines. Exact internal structures should not be treated as Web APIs.",
    commonMistakes: ["Claiming the render tree is a public browser object.","Saying hidden elements always have zero cost.","Confusing render participation with DOM existence."],
    followUpQuestions: ["What is excluded from rendering?","DOM versus render tree?","How does display:none affect layout?"],
    relatedTopics: ["Render tree","Layout tree","DOM","CSSOM","Compositing"],
  },
  {
    id: "jsbrowser-m4-60",
    number: "JSBROWSER-M4-60",
    title: "DOM vs CSSOM",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "The DOM describes document structure and content; the CSSOM describes parsed stylesheet rules and style information. The browser combines information from both during style calculation before layout and paint. Neither structure alone is the final visual output.",
    deepExplanation: "text\nHTML → DOM\nCSS  → CSSOM\n        ↓\n   style calculation\n        ↓\n      layout\n        ↓\n       paint\n```\n\nJavaScript can inspect and mutate both, but the rendering pipeline determines when those changes become visible.",
    productionExample: "A frontend engineer debugging a styling bug checks the DOM class/attributes first and then the computed style/CSS rules that win in the cascade, rather than treating the two structures as interchangeable.",
    bestPractices: ["Keep DOM semantics and CSS responsibilities separate.","Use computed styles only for diagnostics/true layout needs.","Prefer classes/custom properties over direct style churn.","Understand cascade and inheritance."],
    tradeOffs: "Separating structure from style improves maintainability, but the browser must reconcile them during rendering. Frequent mutations to either side can cause style work.",
    commonMistakes: ["Calling CSSOM the visual tree.","Using inline style for every state when classes/tokens are better.","Ignoring cascade specificity when debugging."],
    followUpQuestions: ["How does CSSOM influence the DOM?","What is computed style?","How does inheritance participate?"],
    relatedTopics: ["DOM","CSSOM","Cascade","Computed style","Layout"],
  },
  {
    id: "jsbrowser-m4-61",
    number: "JSBROWSER-M4-61",
    title: "Style calculation",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "Style calculation determines the resolved style for elements by applying matching selectors, cascade, inheritance, media/container conditions and CSS values. It produces the style information used by later layout/paint stages.",
    deepExplanation: "css\n.card {\n  color: black;\n}\n\n.dark .card {\n  color: white;\n}\n```\n\nWhen the ancestor changes from `.light` to `.dark`, the browser may invalidate matching descendants and recalculate their resolved styles.",
    productionExample: "A theme switch is optimized by toggling one root class/custom-property set rather than changing hundreds of individual element styles, reducing the breadth of style invalidation.",
    bestPractices: ["Keep selector structure understandable.","Scope dynamic state changes to small regions when possible.","Use CSS custom properties for theme values.","Avoid unnecessary class churn.","Measure style recalculation in DevTools."],
    tradeOffs: "Broad style changes can invalidate large subtrees, but highly fragmented CSS can also make selector maintenance difficult. Optimization must balance style complexity and maintainability.",
    commonMistakes: ["Assuming only changed elements are recalculated.","Using extremely broad selectors for dynamic state.","Adding specificity hacks instead of fixing the cascade."],
    followUpQuestions: ["What triggers style invalidation?","How does inheritance affect recalculation?","How do custom properties interact with style calculation?"],
    relatedTopics: ["Style calculation","Cascade","Inheritance","Invalidation","Custom properties"],
  },
  {
    id: "jsbrowser-m4-62",
    number: "JSBROWSER-M4-62",
    title: "Computed styles",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "Computed style is the browser's resolved value for CSS properties after considering the cascade, inheritance and applicable CSS rules. `getComputedStyle()` exposes a read-only view of those resolved values for an element.",
    deepExplanation: "js\nconst card = document.querySelector(\".card\");\n\nif (card) {\n  const styles = getComputedStyle(card);\n  console.log(styles.display);\n  console.log(styles.color);\n}\n```\n\nReading computed style can require the browser to ensure style information is current, and some related layout reads can force synchronous work.",
    productionExample: "A drag-and-drop integration reads only the dimensions it truly needs and avoids repeatedly calling `getComputedStyle()` inside every pointer-move event. The final implementation uses cached configuration where possible.",
    bestPractices: ["Avoid repeated computed-style reads in hot loops.","Batch style reads before writes.","Use CSS variables or application state when the value is already known.","Profile forced synchronous style/layout work."],
    tradeOffs: "Computed styles are useful for integration and diagnostics, but repeated synchronous reads can become expensive because the browser may need up-to-date style/layout information.",
    commonMistakes: ["Calling getComputedStyle on thousands of nodes every frame.","Assuming the returned object is a free cache.","Mixing style reads and writes in the same tight loop."],
    followUpQuestions: ["getComputedStyle versus CSSStyleDeclaration?","Can getComputedStyle force layout?","How do you avoid layout thrashing?"],
    relatedTopics: ["getComputedStyle","Style calculation","Forced synchronous layout","Performance"],
  },
  {
    id: "jsbrowser-m4-63",
    number: "JSBROWSER-M4-63",
    title: "CSS selector matching",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "Selector matching determines which CSS rules apply to which elements. The browser uses its selector engine and optimized invalidation structures to avoid naively testing every selector against every node on every update. Complex selector patterns and broad invalidation can still increase style work.",
    deepExplanation: "css\n.card .title {\n  font-weight: 600;\n}\n\n[data-state=\"open\"] .panel {\n  display: block;\n}\n```\n\nThe browser evaluates selector relationships against the DOM when calculating styles for affected elements.",
    productionExample: "A large enterprise app simplifies frequently toggled selectors and scopes state classes near the affected component. Profiling shows less style recalculation during interactive filtering and panel expansion.",
    bestPractices: ["Keep selectors readable and reasonably scoped.","Avoid unnecessary deeply chained selectors in hot UI.","Use classes/attributes for component state.","Measure before rewriting selectors for performance."],
    tradeOffs: "Selector optimization is usually a secondary concern compared with reducing DOM/style invalidation. Aggressively simplifying CSS can harm maintainability or create specificity problems.",
    commonMistakes: ["Claiming the rightmost selector is always the only performance factor.","Over-optimizing selectors without measuring.","Creating high-specificity selectors that are hard to override."],
    followUpQuestions: ["How are selector changes invalidated?","Why are classes useful for UI state?","Does selector complexity always dominate CSS performance?"],
    relatedTopics: ["Selectors","Style calculation","Specificity","Invalidation"],
  },
  {
    id: "jsbrowser-m4-64",
    number: "JSBROWSER-M4-64",
    title: "Style invalidation",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "Style invalidation marks elements whose styles may have changed after DOM, class, attribute, stylesheet or environmental changes. The browser then recalculates style for the affected region. Efficient invalidation is essential for responsive UI performance.",
    deepExplanation: "js\npanel.classList.toggle(\"open\", isOpen);\n```\n\nThis may invalidate the panel and relevant descendants/related selectors. The amount of work depends on the selector rules, inheritance and scope of the change.",
    productionExample: "A dashboard toggles one state class on a small component subtree. The team avoids changing a root class unnecessarily when it would invalidate styles for tens of thousands of nodes.",
    bestPractices: ["Keep state classes scoped.","Avoid unnecessary root-wide style changes.","Use CSS containment where appropriate.","Profile style recalculation/invalidation before changing architecture."],
    tradeOffs: "Scoped invalidation reduces work, but too many isolated styling boundaries can complicate design-system theming. CSS containment can improve isolation but changes layout/size semantics.",
    commonMistakes: ["Treating all class changes as full-page style recalculation.","Toggling many classes instead of one state class.","Using containment without understanding its layout effects."],
    followUpQuestions: ["What can invalidate style?","How does inheritance widen invalidation?","How can containment help?"],
    relatedTopics: ["Style invalidation","CSS containment","Class changes","Inheritance"],
  },
  {
    id: "jsbrowser-m4-65",
    number: "JSBROWSER-M4-65",
    title: "Render tree exclusions",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "Some DOM elements do not generate normal rendered boxes, depending on their CSS and state. `display:none` removes an element/subtree from layout/render participation, while other mechanisms such as `visibility:hidden` retain layout space but suppress painting. The exact effect depends on the property and rendering context.",
    deepExplanation: "css\n.hidden {\n  display: none;\n}\n\n.invisible {\n  visibility: hidden;\n}\n```\n\nThe first removes the element from the normal layout tree; the second keeps its geometry while making it visually invisible.",
    productionExample: "A large application keeps expensive inactive panels unmounted or `display:none` when appropriate, but uses visibility/opacity strategies when layout stability or transitions require retained geometry.",
    bestPractices: ["Choose hiding semantics from UX/layout requirements.","Remember accessibility implications of hidden content.","Unmount or remove large inactive DOM when retention is unnecessary.","Test animations and focus behavior when toggling visibility."],
    tradeOffs: "Removing content from layout reduces rendering work but also destroys layout space and may affect focus/state if unmounted. Keeping it hidden preserves state but consumes memory/DOM cost.",
    commonMistakes: ["Saying `visibility:hidden` removes layout.","Using `display:none` when keyboard focus should remain available.","Assuming opacity:0 is equivalent to display:none."],
    followUpQuestions: ["display:none versus visibility:hidden versus opacity:0?","How does hidden content affect accessibility?","When should you unmount instead?"],
    relatedTopics: ["Render tree","display:none","visibility:hidden","opacity","Accessibility"],
  },
  {
    id: "jsbrowser-m4-66",
    number: "JSBROWSER-M4-66",
    title: "display:none vs visibility:hidden",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "The two properties have different rendering semantics. `display:none` removes the element and its descendants from normal layout/render participation, so it occupies no space. `visibility:hidden` keeps the element's layout box but makes it not visibly painted; it can still affect layout. Neither is equivalent to `opacity:0`, which keeps the element rendered and can remain interactive unless separately controlled.",
    deepExplanation: "css\n.hidden {\n  display: none;\n}\n\n.invisible {\n  visibility: hidden;\n}\n\n.transparent {\n  opacity: 0;\n}\n```\n\nA modal system chooses among these based on whether the component should occupy layout space, participate in transitions, retain state and remain accessible/focusable. `opacity:0` alone is not a substitute for hiding content from interaction.",
    productionExample: "A production tab panel keeps inactive content mounted only when preserving complex state is valuable; otherwise it unmounts or uses an appropriate hidden strategy. A dropdown uses visibility/opacity for animation while managing pointer/focus behavior explicitly.",
    bestPractices: ["Choose the property based on layout, interaction, animation and accessibility requirements.","Do not use opacity as a security/visibility control.","Test keyboard focus when changing visibility.","Unmount large inactive subtrees when retention is unnecessary.","Verify screen-reader behavior for hidden content."],
    tradeOffs: "`display:none` can remove layout work but may discard layout state and interrupt transitions. `visibility:hidden` retains geometry but still consumes DOM/layout resources. `opacity:0` can preserve rendering/compositing cost and interaction unless disabled separately.",
    commonMistakes: ["Saying visibility:hidden removes layout.","Using opacity:0 and assuming the element is inaccessible/uninteractive.","Focusing an element after hiding it.","Changing display during an animation without planning the transition."],
    followUpQuestions: ["display:none vs visibility:hidden vs opacity:0?","How do hidden elements affect accessibility?","When should you unmount instead of hide?"],
    relatedTopics: ["display","visibility","opacity","Accessibility","Layout","Transitions"],
  },
  {
    id: "jsbrowser-m4-67",
    number: "JSBROWSER-M4-67",
    title: "CSS containment",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "CSS containment lets an element declare that parts of its layout/style/paint/size effects are isolated from the outside document. It can limit the scope of recalculation and improve predictable performance, but containment changes layout semantics and must be applied deliberately.",
    deepExplanation: "css\n.cardList {\n  contain: content;\n}\n```\n\nThe exact containment mode matters: `layout`, `paint`, `size` and composite values have different guarantees and trade-offs.",
    productionExample: "A dashboard contains independently updating widgets with appropriate containment so changes inside one widget do not cause unnecessary work across the entire page.",
    bestPractices: ["Choose the smallest containment mode that solves the problem.","Understand size containment before applying it.","Test sticky/positioning/overflow behavior.","Measure actual recalculation improvements."],
    tradeOffs: "Containment can improve rendering isolation but can change sizing, positioning and paint behavior. Incorrect use can break layouts while delivering little performance benefit.",
    commonMistakes: ["Applying contain globally.","Ignoring size/position side effects.","Assuming containment always improves performance."],
    followUpQuestions: ["What is `contain: layout`?","What does `contain: paint` do?","How does size containment affect intrinsic sizing?"],
    relatedTopics: ["CSS containment","Style invalidation","Layout","Performance"],
  },
  {
    id: "jsbrowser-m4-68",
    number: "JSBROWSER-M4-68",
    title: "CSS custom properties",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "CSS custom properties are author-defined properties such as `--brand-color` that participate in the CSS cascade and can be referenced with `var()`. They are useful for themes, tokens and runtime customization because changing a small set of variables can update many consumers without rewriting every rule.",
    deepExplanation: "css\n:root {\n  --brand-color: #2563eb;\n  --space-md: 1rem;\n}\n\n.button {\n  color: white;\n  background: var(--brand-color);\n  padding: var(--space-md);\n}\n```\n\nJavaScript can update a scoped token:\n\n```js\ndocument.documentElement.style\n  .setProperty(\"--brand-color\", \"#7c3aed\");\n```",
    productionExample: "A multi-tenant design system sets semantic custom properties on a root or tenant container. Components consume the variables consistently, while the application keeps business state separate from CSS token state.",
    bestPractices: ["Prefer semantic tokens over raw color names.","Scope variables when tenant/component isolation is useful.","Use fallbacks where appropriate.","Keep theme updates centralized.","Test contrast and fallback behavior."],
    tradeOffs: "Custom properties improve theming and reduce duplicated style definitions, but broad changes can still trigger style recalculation and poorly designed token hierarchies can become difficult to maintain.",
    commonMistakes: ["Using custom properties as arbitrary application state.","Putting secrets in CSS variables.","Creating hundreds of undocumented tokens.","Assuming variable changes have zero rendering cost."],
    followUpQuestions: ["Custom properties versus Sass variables?","How do inherited custom properties work?","How would you design a multi-brand token system?"],
    relatedTopics: ["CSS variables","Design tokens","Theming","Cascade","Inheritance"],
  },
  {
    id: "jsbrowser-m4-69",
    number: "JSBROWSER-M4-69",
    title: "CSSOM performance",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "CSSOM performance depends on stylesheet size, selector complexity, invalidation breadth, style recalculation, computed-style/layout reads and dynamic stylesheet mutations. The most effective optimizations usually reduce the amount or frequency of affected style work rather than micro-optimize CSSOM method calls.",
    deepExplanation: "js\nconst panel = document.querySelector(\".panel\");\n\nconst color = panel\n  ? getComputedStyle(panel).color\n  : \"\";\n\nconsole.log(color);\n```\n\nThis read should not be placed in a high-frequency animation loop unless profiling proves it necessary.",
    productionExample: "A production drag UI separates read phases from write phases and uses known state values instead of querying computed styles every frame, eliminating forced synchronous style/layout work.",
    bestPractices: ["Batch reads and writes.","Avoid repeated stylesheet rule mutations.","Use classes/custom properties for state.","Keep selectors maintainable.","Profile forced style/layout work."],
    tradeOffs: "CSSOM control can solve integration problems but increases imperative complexity. Performance gains often come from reducing style invalidation rather than from using a different CSSOM API.",
    commonMistakes: ["Micro-optimizing API syntax instead of pipeline work.","Reading computed styles repeatedly inside loops.","Injecting CSS repeatedly on every state update."],
    followUpQuestions: ["What is forced style recalculation?","How do style reads become expensive?","How would you profile CSSOM performance?"],
    relatedTopics: ["CSSOM","Style recalculation","Layout thrashing","getComputedStyle","Performance"],
  },
  {
    id: "jsbrowser-m4-70",
    number: "JSBROWSER-M4-70",
    title: "Render tree interview scenario",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "CSSOM & Render Tree",
    expectedAnswer: "For a senior render-tree scenario, trace the path from DOM/CSSOM through style calculation to layout, paint and compositing. Identify which elements participate in visual rendering, which property change invalidates which stage, and what evidence DevTools provides. Prefer measured diagnosis over generic statements such as 'the DOM is slow.'",
    deepExplanation: "text\nDOM + CSSOM\n   ↓\nStyle calculation\n   ↓\nLayout\n   ↓\nPaint\n   ↓\nRaster/composite\n   ↓\nScreen\n\nScenario:\n- A modal opens.\n- `display:none` changes to `block`.\n- Style/layout are recalculated.\n- The modal is painted.\n- A transform animation can later run largely in the compositor path.\n```\n\nA strong interview answer distinguishes the initial layout/paint from subsequent compositor-friendly animation.",
    productionExample: "A production modal is optimized so opening it performs one layout/paint update, while the entrance animation uses transform/opacity. The team verifies the trace on low-end mobile hardware and checks focus/accessibility separately.",
    bestPractices: ["Name each pipeline stage precisely.","Distinguish layout-triggering changes from compositor-friendly animation.","Use DevTools traces.","Discuss accessibility and focus alongside visual performance.","Validate on representative devices."],
    tradeOffs: "Pipeline optimization can trade layout simplicity, GPU memory and implementation complexity. A visually smooth animation can still have poor accessibility or excessive memory usage.",
    commonMistakes: ["Saying every animation runs on the GPU.","Skipping the initial style/layout work.","Confusing style recalculation with painting.","Ignoring the browser differences hidden by simplified diagrams."],
    followUpQuestions: ["What causes layout after a style change?","Which properties are commonly compositor-friendly?","How would you diagnose paint versus layout cost?","How do accessibility requirements affect the architecture?"],
    relatedTopics: ["Render tree","Style calculation","Layout","Paint","Compositor","Accessibility"],
  },

];

export const MOCK_BROWSER_INTERNALS_MODULE4_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Browser/Web APIs',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));