// Derived from frontend/src/document/Part_11_Frontend_Performance_Optimization_Master_Handbook.md (Module 6 — Network Optimization).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "perf-m6-101",
    number: "PERF-M6-101",
    title: "HTTP/1.1",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**HTTP/1.1** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**HTTP/1.1** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse HTTP/1.1 at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use HTTP/1.1 at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-102",
    number: "PERF-M6-102",
    title: "HTTP/2",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**HTTP/2** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**HTTP/2** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse HTTP/2 at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use HTTP/2 at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-103",
    number: "PERF-M6-103",
    title: "HTTP/3",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**HTTP/3** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**HTTP/3** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse HTTP/3 at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use HTTP/3 at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-104",
    number: "PERF-M6-104",
    title: "QUIC",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**QUIC** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**QUIC** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse QUIC at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use QUIC at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-105",
    number: "PERF-M6-105",
    title: "Compression",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Compression** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Compression** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Compression at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Compression at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-106",
    number: "PERF-M6-106",
    title: "Brotli",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Brotli** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Brotli** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Brotli at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Brotli at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-107",
    number: "PERF-M6-107",
    title: "Gzip",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Gzip** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Gzip** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Gzip at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Gzip at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-108",
    number: "PERF-M6-108",
    title: "CDN",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**CDN** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CDN** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse CDN at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use CDN at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-109",
    number: "PERF-M6-109",
    title: "Caching",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Caching** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Caching** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Caching at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Caching at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-110",
    number: "PERF-M6-110",
    title: "TLS",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**TLS** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**TLS** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse TLS at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use TLS at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-111",
    number: "PERF-M6-111",
    title: "DNS",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**DNS** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**DNS** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse DNS at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use DNS at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-112",
    number: "PERF-M6-112",
    title: "Preconnect",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Preconnect** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Preconnect** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Preconnect at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Preconnect at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-113",
    number: "PERF-M6-113",
    title: "Preload",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Preload** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Preload** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Preload at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Preload at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-114",
    number: "PERF-M6-114",
    title: "Prefetch",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Prefetch** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Prefetch** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Prefetch at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Prefetch at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-115",
    number: "PERF-M6-115",
    title: "Priority Hints",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Priority Hints** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Priority Hints** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Priority Hints at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Priority Hints at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-116",
    number: "PERF-M6-116",
    title: "103 Early Hints",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**103 Early Hints** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**103 Early Hints** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse 103 Early Hints at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use 103 Early Hints at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-117",
    number: "PERF-M6-117",
    title: "Connection reuse",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Connection reuse** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Connection reuse** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Connection reuse at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Connection reuse at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-118",
    number: "PERF-M6-118",
    title: "Request waterfalls",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Request waterfalls** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Request waterfalls** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Request waterfalls at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Request waterfalls at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-119",
    number: "PERF-M6-119",
    title: "Network contention",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Network contention** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Network contention** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Network contention at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Network contention at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-120",
    number: "PERF-M6-120",
    title: "Network performance architecture",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Network performance architecture** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Network performance architecture** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Network performance architecture at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Network performance architecture at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---","# Module 7 – Image Optimization","**Questions 121–135**"],
  },
];

export const MOCK_FRONTEND_PERFORMANCE_MODULE6_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Performance',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));