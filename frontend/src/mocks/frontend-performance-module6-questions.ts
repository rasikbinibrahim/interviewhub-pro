// Derived from frontend/src/document/Part_11_Frontend_Performance_Optimization_Master_Handbook.md (Module 6 — Network Optimization).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "perf-m6-101",
    number: "PERF-M6-101",
    title: "HTTP/1.1",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**HTTP/1.1** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-102",
    number: "PERF-M6-102",
    title: "HTTP/2",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**HTTP/2** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-103",
    number: "PERF-M6-103",
    title: "HTTP/3",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**HTTP/3** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-104",
    number: "PERF-M6-104",
    title: "QUIC",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**QUIC** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-105",
    number: "PERF-M6-105",
    title: "Compression",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Compression** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-106",
    number: "PERF-M6-106",
    title: "Brotli",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Brotli** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-107",
    number: "PERF-M6-107",
    title: "Gzip",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Gzip** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-108",
    number: "PERF-M6-108",
    title: "CDN",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**CDN** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-109",
    number: "PERF-M6-109",
    title: "Caching",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Caching** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-110",
    number: "PERF-M6-110",
    title: "TLS",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**TLS** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-111",
    number: "PERF-M6-111",
    title: "DNS",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**DNS** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-112",
    number: "PERF-M6-112",
    title: "Preconnect",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Preconnect** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-113",
    number: "PERF-M6-113",
    title: "Preload",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Preload** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-114",
    number: "PERF-M6-114",
    title: "Prefetch",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Prefetch** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-115",
    number: "PERF-M6-115",
    title: "Priority Hints",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Priority Hints** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-116",
    number: "PERF-M6-116",
    title: "103 Early Hints",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**103 Early Hints** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-117",
    number: "PERF-M6-117",
    title: "Connection reuse",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Connection reuse** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-118",
    number: "PERF-M6-118",
    title: "Request waterfalls",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Request waterfalls** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-119",
    number: "PERF-M6-119",
    title: "Network contention",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Network contention** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m6-120",
    number: "PERF-M6-120",
    title: "Network performance architecture",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Network Optimization",
    expectedAnswer: "**Network performance architecture** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---","# Module 7 – Image Optimization","**Questions 121–135**"],
  },
];

export const MOCK_FRONTEND_PERFORMANCE_MODULE6_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
