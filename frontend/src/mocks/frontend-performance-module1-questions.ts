// Derived from frontend/src/document/Part_11_Frontend_Performance_Optimization_Master_Handbook.md (Module 1 — Performance Fundamentals).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "perf-m1-1",
    number: "PERF-M1-1",
    title: "What is frontend performance?",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**What is frontend performance?** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**What is frontend performance?** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse What is frontend performance? at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use What is frontend performance? at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-2",
    number: "PERF-M1-2",
    title: "Performance budgets",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Performance budgets** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Performance budgets** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Performance budgets at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Performance budgets at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-3",
    number: "PERF-M1-3",
    title: "User-perceived performance",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**User-perceived performance** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**User-perceived performance** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse User-perceived performance at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use User-perceived performance at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-4",
    number: "PERF-M1-4",
    title: "Lab vs field data",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Lab vs field data** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Lab vs field data** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Lab vs field data at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Lab vs field data at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-5",
    number: "PERF-M1-5",
    title: "Latency",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Latency** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Latency** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Latency at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Latency at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-6",
    number: "PERF-M1-6",
    title: "Throughput",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Throughput** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Throughput** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Throughput at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Throughput at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-7",
    number: "PERF-M1-7",
    title: "CPU cost",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**CPU cost** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**CPU cost** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse CPU cost at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use CPU cost at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-8",
    number: "PERF-M1-8",
    title: "Memory cost",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Memory cost** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Memory cost** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Memory cost at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Memory cost at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-9",
    number: "PERF-M1-9",
    title: "Network cost",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Network cost** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Network cost** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Network cost at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Network cost at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-10",
    number: "PERF-M1-10",
    title: "Rendering cost",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Rendering cost** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Rendering cost** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Rendering cost at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Rendering cost at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-11",
    number: "PERF-M1-11",
    title: "Performance trade-offs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Performance trade-offs** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Performance trade-offs** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Performance trade-offs at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Performance trade-offs at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-12",
    number: "PERF-M1-12",
    title: "Progressive enhancement",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Progressive enhancement** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Progressive enhancement** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Progressive enhancement at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Progressive enhancement at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-13",
    number: "PERF-M1-13",
    title: "Graceful degradation",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Graceful degradation** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Graceful degradation** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Graceful degradation at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Graceful degradation at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-14",
    number: "PERF-M1-14",
    title: "Performance culture",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Performance culture** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Performance culture** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Performance culture at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Performance culture at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-15",
    number: "PERF-M1-15",
    title: "Performance KPIs",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Performance KPIs** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Performance KPIs** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Performance KPIs at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Performance KPIs at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-16",
    number: "PERF-M1-16",
    title: "Performance regression",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Performance regression** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Performance regression** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Performance regression at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Performance regression at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-17",
    number: "PERF-M1-17",
    title: "Performance budgets in CI/CD",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Performance budgets in CI/CD** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Performance budgets in CI/CD** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Performance budgets in CI/CD at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Performance budgets in CI/CD at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-18",
    number: "PERF-M1-18",
    title: "RUM",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**RUM** is a Core Web Vitals / field-performance topic. The correct approach is to identify what the metric represents, understand its main contributors, measure it in real environments, and optimize the largest contributor first.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**RUM** is a Core Web Vitals / field-performance topic. The correct approach is to identify what the metric represents, understand its main contributors, measure it in real environments, and optimize the largest contributor first.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface MetricSample {\n  value: number;\n  good: number;\n}\n\nfunction isWithinTarget(\n  sample: MetricSample,\n): boolean {\n  return sample.value <= sample.good;\n}\n\nconst ok = isWithinTarget({\n  value: 2200,\n  good: 2500,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst sample = {\n  value: 2200,\n  good: 2500,\n};\n\nconst ok = sample.value <= sample.good;\n```\n\nStep 5 — Production example:\nMeasure RUM in production and connect it to the responsible page/template, device/network segment, and largest contributing resource or interaction before optimizing.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Measure RUM in production and connect it to the responsible page/template, device/network segment, and largest contributing resource or interaction before optimizing.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-19",
    number: "PERF-M1-19",
    title: "Synthetic monitoring",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Synthetic monitoring** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Synthetic monitoring** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Synthetic monitoring at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Synthetic monitoring at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m1-20",
    number: "PERF-M1-20",
    title: "Performance architecture",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Performance Fundamentals",
    expectedAnswer: "**Performance architecture** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Performance architecture** is a network-performance topic. Explain the protocol or browser mechanism, where latency/bytes are introduced, how caching or connection behavior changes the result, and how to validate the improvement.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\ninterface CacheEntry<T> {\n  value: T;\n  expiresAt: number;\n}\n\nfunction readCache<T>(\n  entry: CacheEntry<T> | undefined,\n  now: number,\n): T | undefined {\n  if (!entry || entry.expiresAt <= now) {\n    return undefined;\n  }\n\n  return entry.value;\n}\n\nconst value = readCache(\n  { value: \"data\", expiresAt: 2_000 },\n  1_000,\n);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst cache = new Map<string, string>();\n\ncache.set(\"users\", \"data\");\n\nconst value = cache.get(\"users\");\n```\n\nStep 5 — Production example:\nUse Performance architecture at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Performance architecture at the browser/server delivery boundary so bytes and latency are reduced without creating stale or difficult-to-debug behavior.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---","# Module 2 – Core Web Vitals","**Questions 21–40**"],
  },
];

export const MOCK_FRONTEND_PERFORMANCE_MODULE1_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Performance',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));