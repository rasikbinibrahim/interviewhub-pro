// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 11 — Visual Regression Testing).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m11-1",
    number: "TESTQA-M11-1",
    title: "Visual Testing Fundamentals",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Visual Testing Fundamentals** should verify visual appearance at a stable boundary: representative state, viewport/browser, fonts/assets, and deterministic data. The goal is to detect unintended visual change while minimizing noise.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Visual Testing Fundamentals** should verify visual appearance at a stable boundary: representative state, viewport/browser, fonts/assets, and deterministic data. The goal is to detect unintended visual change while minimizing noise.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nUse deterministic component states, fixed viewport/browser settings, stable fonts/assets, and reviewed baselines.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use deterministic component states, fixed viewport/browser settings, stable fonts/assets, and reviewed baselines.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-2",
    number: "TESTQA-M11-2",
    title: "Storybook Visual Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Storybook Visual Testing** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Storybook Visual Testing** should represent a deterministic component state or interaction scenario that can be reused for development, documentation, accessibility, and visual checks.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep stories as small, reproducible component scenarios that can be reused for visual, interaction, and accessibility validation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-3",
    number: "TESTQA-M11-3",
    title: "Chromatic",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Chromatic** should compare Storybook-rendered UI against an approved visual baseline and make review ownership explicit for meaningful changes.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Chromatic** should compare Storybook-rendered UI against an approved visual baseline and make review ownership explicit for meaningful changes.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nRun visual checks on CI with reviewable baseline changes and explicit ownership of updates.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Run visual checks on CI with reviewable baseline changes and explicit ownership of updates.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-4",
    number: "TESTQA-M11-4",
    title: "Percy",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Percy** should capture stable rendered states and compare them across approved environments while controlling fonts, timing, and dynamic data.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Percy** should capture stable rendered states and compare them across approved environments while controlling fonts, timing, and dynamic data.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nCapture stable states and compare screenshots only after controlling dynamic data and animation.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Capture stable states and compare screenshots only after controlling dynamic data and animation.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-5",
    number: "TESTQA-M11-5",
    title: "Screenshot Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Screenshot Testing** should capture a deterministic UI state and compare its pixels/regions with a reviewed baseline.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Screenshot Testing** should capture a deterministic UI state and compare its pixels/regions with a reviewed baseline.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nUse screenshot comparison for important visual contracts such as layout, responsive breakpoints, and critical components.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use screenshot comparison for important visual contracts such as layout, responsive breakpoints, and critical components.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-6",
    number: "TESTQA-M11-6",
    title: "Responsive Visual Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Responsive Visual Testing** should verify important breakpoints, layout transitions, content wrapping, overflow, and touch targets using representative viewport sizes.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Responsive Visual Testing** should verify important breakpoints, layout transitions, content wrapping, overflow, and touch targets using representative viewport sizes.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nTest representative breakpoints and content extremes rather than every possible pixel width.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Test representative breakpoints and content extremes rather than every possible pixel width.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-7",
    number: "TESTQA-M11-7",
    title: "Dark Mode",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Dark Mode** should verify the same component behavior across theme tokens, contrast states, and assets while keeping snapshots stable.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Dark Mode** should verify the same component behavior across theme tokens, contrast states, and assets while keeping snapshots stable.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nVerify light/dark or brand themes using the same component states and a consistent token source.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Verify light/dark or brand themes using the same component states and a consistent token source.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-8",
    number: "TESTQA-M11-8",
    title: "Theme Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Theme Testing** should verify the same component behavior across theme tokens, contrast states, and assets while keeping snapshots stable.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Theme Testing** should verify the same component behavior across theme tokens, contrast states, and assets while keeping snapshots stable.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nVerify light/dark or brand themes using the same component states and a consistent token source.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Verify light/dark or brand themes using the same component states and a consistent token source.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-9",
    number: "TESTQA-M11-9",
    title: "Animation Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Animation Testing** should make motion deterministic for visual tests by freezing or controlling animations, then separately verify the intended motion behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Animation Testing** should make motion deterministic for visual tests by freezing or controlling animations, then separately verify the intended motion behavior.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nFreeze motion for pixel comparison and test actual animation behavior separately.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Freeze motion for pixel comparison and test actual animation behavior separately.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-10",
    number: "TESTQA-M11-10",
    title: "Cross-browser Visual Testing",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Cross-browser Visual Testing** should keep the same behavioral contract across supported engines while allowing expected rendering differences to be explicitly controlled.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Cross-browser Visual Testing** should keep the same behavioral contract across supported engines while allowing expected rendering differences to be explicitly controlled.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nRun a focused compatibility matrix on supported engines and investigate real browser-specific differences.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Run a focused compatibility matrix on supported engines and investigate real browser-specific differences.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-11",
    number: "TESTQA-M11-11",
    title: "Snapshot Management",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Snapshot Management** should treat snapshots as reviewed change artifacts, not automatic truth. Keep them focused and remove obsolete snapshots.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Snapshot Management** should treat snapshots as reviewed change artifacts, not automatic truth. Keep them focused and remove obsolete snapshots.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nKeep snapshots small and meaningful; review each update and delete obsolete baselines.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Keep snapshots small and meaningful; review each update and delete obsolete baselines.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-12",
    number: "TESTQA-M11-12",
    title: "Visual Baselines",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Visual Baselines** should define who owns the approved visual state and how baseline updates are reviewed, versioned, and reproduced.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Visual Baselines** should define who owns the approved visual state and how baseline updates are reviewed, versioned, and reproduced.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nStore baselines with the same source/version/context needed to reproduce them.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Store baselines with the same source/version/context needed to reproduce them.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-13",
    number: "TESTQA-M11-13",
    title: "False Positives",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**False Positives** should reduce noise by controlling fonts, data, animations, viewport, timing, and platform-specific rendering differences.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**False Positives** should reduce noise by controlling fonts, data, animations, viewport, timing, and platform-specific rendering differences.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface VisualCase {\n  name: string;\n  stable: boolean;\n}\n\nfunction approvedCases(\n  cases: readonly VisualCase[],\n): string[] {\n  const result: string[] = [];\n\n  for (let i = 0; i < cases.length; i += 1) {\n    if (cases[i].stable) result.push(cases[i].name);\n  }\n\n  return result;\n}\n\nconst cases = approvedCases([\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst cases = [\n  { name: \"desktop\", stable: true },\n  { name: \"mobile\", stable: true },\n];\n\nconst approved = cases\n  .filter((item) => item.stable)\n  .map((item) => item.name);\n```\n\nStep 5 — Production example:\nControl fonts, animations, data, time, viewport, and platform variability before weakening visual thresholds.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Control fonts, animations, data, time, viewport, and platform variability before weakening visual thresholds.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-14",
    number: "TESTQA-M11-14",
    title: "Visual Test Performance",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Visual Test Performance** should balance screenshot coverage with execution time and parallelization. Visual tests should not become the only validation layer.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Visual Test Performance** should balance screenshot coverage with execution time and parallelization. Visual tests should not become the only validation layer.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Check {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(checks: readonly Check[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst checks = [\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nParallelize visual checks where useful and avoid broad screenshot coverage of low-risk states.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Parallelize visual checks where useful and avoid broad screenshot coverage of low-risk states.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-15",
    number: "TESTQA-M11-15",
    title: "Enterprise Visual QA",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Enterprise Visual QA** should define ownership, supported environments, baseline governance, CI integration, retention, and failure triage.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Enterprise Visual QA** should define ownership, supported environments, baseline governance, CI integration, retention, and failure triage.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Check {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(checks: readonly Check[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst checks = [\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse the smallest deterministic test that provides meaningful confidence.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use the smallest deterministic test that provides meaningful confidence.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
  {
    id: "testqa-m11-16",
    number: "TESTQA-M11-16",
    title: "Visual Regression Testing — Module Review",
    difficulty: "Medium",
    experienceLevel: "2–5 Years",
    category: "Visual Regression Testing",
    expectedAnswer: "**Visual Regression Testing — Module Review** is a module-level revision topic. Be ready to explain the purpose, setup, deterministic strategy, failure diagnosis, and trade-offs.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Visual Regression Testing — Module Review** is a module-level revision topic. Be ready to explain the purpose, setup, deterministic strategy, failure diagnosis, and trade-offs.\n\nStep 2 — Easy method:\nDefine the observable quality signal, inputs/context, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the core logic visible and easy to explain line by line.\n\n```ts\ninterface Check {\n  name: string;\n  passed: boolean;\n}\n\nfunction allPassed(checks: readonly Check[]): boolean {\n  for (let i = 0; i < checks.length; i += 1) {\n    if (!checks[i].passed) return false;\n  }\n\n  return true;\n}\n\nconst passed = allPassed([\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n]);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native collection/platform helpers when they communicate the same behavior more clearly.\n\n```ts\nconst checks = [\n  { name: \"contract\", passed: true },\n  { name: \"behavior\", passed: true },\n];\n\nconst passed = checks.every(\n  (check) => check.passed,\n);\n```\n\nStep 5 — Production example:\nUse the smallest deterministic test that provides meaningful confidence.\n\nStep 6 — Test quality:\nKeep the check deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why the check belongs at this level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider QA strategy.",
    productionExample: "Use the smallest deterministic test that provides meaningful confidence.",
    bestPractices: ["Test observable behavior and the intended quality signal.", "Keep tests deterministic, isolated, and easy to diagnose.", "Use realistic data and the lowest-cost test level that gives enough confidence.", "Cover normal, boundary, failure, accessibility, and recovery behavior.", "Keep fixtures, cleanup, environments, and test ownership explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical version is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Using a generic test example that does not exercise the actual topic.", "Testing implementation details instead of public behavior.", "Using arbitrary sleeps or unstable timing.", "Over-mocking until the test no longer resembles production.", "Allowing flaky or obsolete checks to remain without ownership."],
    followUpQuestions: ["What exact behavior or quality attribute should this test protect?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make it deterministic?", "How do you test failure and recovery?", "How would you run it in CI?", "How would you debug a flaky failure?", "How would you scale the suite?", "What is the main cost or trade-off?", "How would you explain this as a senior engineer?"],
    relatedTopics: ["Visual Regression Testing"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE11_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));