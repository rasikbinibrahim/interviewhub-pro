// Derived from frontend/src/document/Frontend_Testing_QA_Master_Handbook.md (Module 3 — Jest).
// Auto-generated by a one-off parser script from the handbook's per-topic template
// (Introduction/Definition, code examples, Advantages/Disadvantages, Best
// Practices, Common Mistakes/Anti-patterns, Interview Questions).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "testqa-m3-1",
    number: "TESTQA-M3-1",
    title: "Jest Installation and Project Setup",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Jest Installation and Project Setup** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Jest Installation and Project Setup** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-2",
    number: "TESTQA-M3-2",
    title: "Jest Configuration",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Jest Configuration** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Jest Configuration** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-3",
    number: "TESTQA-M3-3",
    title: "Test Suites and Test Cases",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Test Suites and Test Cases** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Test Suites and Test Cases** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-4",
    number: "TESTQA-M3-4",
    title: "Assertions and Matchers",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Assertions and Matchers** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Assertions and Matchers** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-5",
    number: "TESTQA-M3-5",
    title: "Mock Functions",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Mock Functions** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Mock Functions** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-6",
    number: "TESTQA-M3-6",
    title: "Mock Modules",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Mock Modules** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Mock Modules** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-7",
    number: "TESTQA-M3-7",
    title: "Timers and Fake Timers",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Timers and Fake Timers** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Timers and Fake Timers** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-8",
    number: "TESTQA-M3-8",
    title: "Async Tests",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Async Tests** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Async Tests** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-9",
    number: "TESTQA-M3-9",
    title: "Snapshot Testing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Snapshot Testing** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Snapshot Testing** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-10",
    number: "TESTQA-M3-10",
    title: "beforeEach and afterEach",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**beforeEach and afterEach** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**beforeEach and afterEach** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-11",
    number: "TESTQA-M3-11",
    title: "beforeAll and afterAll",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**beforeAll and afterAll** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**beforeAll and afterAll** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-12",
    number: "TESTQA-M3-12",
    title: "Coverage Reports",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Coverage Reports** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Coverage Reports** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-13",
    number: "TESTQA-M3-13",
    title: "Jest CLI and Watch Mode",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Jest CLI and Watch Mode** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Jest CLI and Watch Mode** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-14",
    number: "TESTQA-M3-14",
    title: "Debugging and Performance",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Debugging and Performance** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Debugging and Performance** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-15",
    number: "TESTQA-M3-15",
    title: "Enterprise Jest Configuration",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Enterprise Jest Configuration** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Enterprise Jest Configuration** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
  {
    id: "testqa-m3-16",
    number: "TESTQA-M3-16",
    title: "Jest — Module Review",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "Jest",
    expectedAnswer: "**Jest — Module Review** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.",
    deepExplanation: "Step 1 — Understand the topic.\n\n**Jest — Module Review** should be explained through Jest's lifecycle, configuration, assertions, mocks, timers, async execution, coverage, and CLI behavior.\n\nStep 2 — Easy method:\nDefine the observable behavior, inputs, expected result, failure conditions, and cleanup. Use the smallest test level that provides enough confidence.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual version exposes the core logic so it is easy to explain line by line in an interview.\n\n```ts\ninterface JestConfig { roots: string[]; collectCoverage: boolean; }\n\nfunction validConfig(config: JestConfig): boolean {\n  return config.roots.length > 0 && config.collectCoverage;\n}\n\nconst valid = validConfig({\n  roots: [\"src\"],\n  collectCoverage: true,\n});\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse native array/collection helpers when they make the same behavior clearer without hiding the important test idea.\n\n```ts\nconst config = { roots: [\"src\"], collectCoverage: true };\n\nconst valid =\n  config.roots.length > 0 &&\n  config.collectCoverage;\n```\n\nStep 5 — Production example:\nKeep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.\n\nStep 6 — Test quality:\nKeep the test deterministic, isolated, diagnostic, and representative. Cover success, boundary, failure, accessibility, and cleanup where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain why this belongs at this test level, what it deliberately does not cover, how flakiness is prevented, and how it fits the wider suite.",
    productionExample: "Keep Jest configuration centralized and align lifecycle, timers, mocks, coverage, and CLI behavior with repository needs.",
    bestPractices: ["Test observable behavior rather than private implementation details.", "Keep tests isolated, deterministic, and easy to diagnose.", "Use the lowest-cost test level that gives enough confidence.", "Cover happy path, boundary, failure, accessibility, and recovery behavior.", "Keep setup, fixtures, cleanup, and mocks explicit."],
    tradeOffs: "The manual/core example is intentionally explicit for interview explanation. The built-in/practical form is shorter for production. Choose the lowest-cost test that gives adequate confidence and keep broader tests focused on high-value boundaries.",
    commonMistakes: ["Testing implementation details instead of user-visible behavior.", "Using arbitrary sleeps instead of waiting on a real condition.", "Sharing mutable fixtures across tests.", "Mocking everything until the test no longer resembles production.", "Treating coverage percentage as proof of quality."],
    followUpQuestions: ["What behavior should be protected?", "Which test level is appropriate and why?", "What should be mocked and what should stay real?", "How do you make the test deterministic?", "How do you test failure and recovery?", "How do you test accessibility?", "How do you diagnose CI-only failures?", "How do you scale the suite?", "What would you change if test runtime doubled?", "How would you prevent flaky tests?"],
    relatedTopics: ["Jest"],
  },
];

export const MOCK_FRONTEND_TESTING_QA_MODULE3_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Testing',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));