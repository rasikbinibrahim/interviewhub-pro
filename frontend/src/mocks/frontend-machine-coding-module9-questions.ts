// Derived from frontend/src/document/Part_13_Frontend_Machine_Coding_Master_Handbook.md (Module 9 — Data Grid).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "machinecoding-m9-166",
    number: "MACHINECODING-M9-166",
    title: "Basic data grid",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Basic data grid** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-167",
    number: "MACHINECODING-M9-167",
    title: "Column definitions",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Column definitions** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-168",
    number: "MACHINECODING-M9-168",
    title: "Sortable columns",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Sortable columns** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-169",
    number: "MACHINECODING-M9-169",
    title: "Filterable columns",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Filterable columns** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-170",
    number: "MACHINECODING-M9-170",
    title: "Pagination",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Pagination** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-171",
    number: "MACHINECODING-M9-171",
    title: "Server-side pagination",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Server-side pagination** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-172",
    number: "MACHINECODING-M9-172",
    title: "Column resizing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Column resizing** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-173",
    number: "MACHINECODING-M9-173",
    title: "Column reordering",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Column reordering** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-174",
    number: "MACHINECODING-M9-174",
    title: "Column visibility",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Column visibility** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-175",
    number: "MACHINECODING-M9-175",
    title: "Row selection",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Row selection** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-176",
    number: "MACHINECODING-M9-176",
    title: "Multi-row selection",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Multi-row selection** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-177",
    number: "MACHINECODING-M9-177",
    title: "Inline editing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Inline editing** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-178",
    number: "MACHINECODING-M9-178",
    title: "Cell validation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Cell validation** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\nexport function ExampleForm() {\n  const [name, setName] = useState(\"\");\n\n  function submit(event: React.FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    if (!name.trim()) return;\n    console.log({ name });\n  }\n\n  return (\n    <form onSubmit={submit} noValidate>\n      <label htmlFor=\"name\">Name</label>\n      <input\n        id=\"name\"\n        name=\"name\"\n        value={name}\n        onChange={(e) => setName(e.target.value)}\n        required\n      />\n      <button type=\"submit\">Save</button>\n    </form>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-179",
    number: "MACHINECODING-M9-179",
    title: "Keyboard navigation",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Keyboard navigation** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-180",
    number: "MACHINECODING-M9-180",
    title: "ARIA grid",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **ARIA grid** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-181",
    number: "MACHINECODING-M9-181",
    title: "Virtualized rows",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Virtualized rows** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-182",
    number: "MACHINECODING-M9-182",
    title: "Virtualized columns",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Virtualized columns** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-183",
    number: "MACHINECODING-M9-183",
    title: "Sticky headers",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Sticky headers** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-184",
    number: "MACHINECODING-M9-184",
    title: "Sticky columns",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Sticky columns** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-185",
    number: "MACHINECODING-M9-185",
    title: "Grouped rows",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Grouped rows** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-186",
    number: "MACHINECODING-M9-186",
    title: "Expandable rows",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Expandable rows** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-187",
    number: "MACHINECODING-M9-187",
    title: "Custom cell renderers",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Custom cell renderers** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-188",
    number: "MACHINECODING-M9-188",
    title: "Server-side filtering",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Server-side filtering** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-189",
    number: "MACHINECODING-M9-189",
    title: "Data grid testing",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Data grid testing** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---"],
  },
  {
    id: "machinecoding-m9-190",
    number: "MACHINECODING-M9-190",
    title: "Enterprise data grid",
    difficulty: "Medium",
    experienceLevel: "5–8 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Data Grid",
    expectedAnswer: "Build a production-quality **Enterprise data grid** component suitable for a machine-coding interview. The implementation must be reusable, accessible, responsive, typed, testable and explainable.",
    deepExplanation: "Example:\n\n```ts\nimport { useState } from \"react\";\n\ntype Props = { items: string[] };\n\nexport function Component({ items }: Props) {\n  const [selected, setSelected] = useState<string | null>(null);\n\n  return (\n    <section aria-label=\"Interactive component\">\n      {items.map((item) => (\n        <button\n          key={item}\n          type=\"button\"\n          aria-pressed={selected === item}\n          onClick={() => setSelected(item)}\n        >\n          {item}\n        </button>\n      ))}\n    </section>\n  );\n}\n```\n\nProduction pattern:\n\n```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    productionExample: "```ts\n// Production pattern: isolate state, keep rendering pure,\n// and expose a small typed API to consumers.\nexport type Item = {\n  id: string;\n  label: string;\n};\n\nexport function ItemList({\n  items,\n  onSelect,\n}: {\n  items: readonly Item[];\n  onSelect: (item: Item) => void;\n}) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <li key={item.id}>\n          <button type=\"button\" onClick={() => onSelect(item)}>\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```",
    bestPractices: ["Semantic HTML","Keyboard complete","Focus visible","Accessible names","Error messages associated with controls","Dynamic updates announced where required","React DevTools Profiler reviewed","No unnecessary global state","Large collections virtualized","Network requests debounced/cancelled where needed","Lazy loading applied to appropriate boundaries","Bundle impact measured","Untrusted HTML is never rendered without a deliberate sanitization policy","External URLs are validated","Sensitive data is not placed in logs","API authorization is enforced server-side","Third-party dependencies are reviewed"],
    tradeOffs: "Add observability for failures and latency, avoid logging sensitive data, verify responsive behavior, test slow networks and ensure accessibility regressions are caught before deployment.",
    commonMistakes: ["Empty input/data","Duplicate items","Slow network","Failed requests","Rapid repeated interaction","Keyboard-only use","Small viewport","Very large data sets"],
    followUpQuestions: ["How would you make this component reusable across products?","How would you test keyboard and screen-reader behavior?","What happens with 100,000 records?","How would you handle slow or failed APIs?","Which state belongs locally versus globally?","Where would you use memoization, and how would you prove it helps?","How would you evolve the API without breaking consumers?","How would you instrument this in production?"],
    relatedTopics: ["React 19","TypeScript","Vite","React Router","Redux Toolkit","TanStack Query","Zustand","React Hook Form","Zod","Storybook","Vitest","Playwright","React Testing Library","---","# Module 10 – Calendar","**Questions 191–210**"],
  },
];

export const MOCK_FRONTEND_MACHINE_CODING_MODULE9_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
