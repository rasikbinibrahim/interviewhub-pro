// Updated React question bank — cleaned TypeScript and topic-specific coding practice.
import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "reactfund-m3-36",
    number: "REACTFUND-M3-36",
    title: "Functional components",
    difficulty: "Hard",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Functional components**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-37",
    number: "REACTFUND-M3-37",
    title: "Class components legacy",
    difficulty: "Easy",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Class components legacy**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-38",
    number: "REACTFUND-M3-38",
    title: "Component composition",
    difficulty: "Medium",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Component composition**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-39",
    number: "REACTFUND-M3-39",
    title: "Reusable components",
    difficulty: "Hard",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Reusable components**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-40",
    number: "REACTFUND-M3-40",
    title: "Container vs presentational components",
    difficulty: "Easy",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Container vs presentational components**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-41",
    number: "REACTFUND-M3-41",
    title: "Component communication",
    difficulty: "Medium",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Component communication**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-42",
    number: "REACTFUND-M3-42",
    title: "Parent to child communication",
    difficulty: "Hard",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "Parent to child communication is a React concept that should be understood at three levels: the public API, the rendering behavior it triggers, and the production trade-offs involved in accessibility, performance, testing and maintainability.",
    deepExplanation: "Step 1 — Understand the topic.\nUnderstand the core mechanism first, then use the idiomatic React/TypeScript form.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction example(value: string): string {\n  return value.trim();\n}\n\nconsole.log(\n  example(\" React \")\n);\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nconsole.log(\n  \" React \".trim()\n);\n```\n\nStep 5 — Example result:\n```text\nReact\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Parent to child communication**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-43",
    number: "REACTFUND-M3-43",
    title: "Child to parent communication",
    difficulty: "Easy",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "Child to parent communication is a React concept that should be understood at three levels: the public API, the rendering behavior it triggers, and the production trade-offs involved in accessibility, performance, testing and maintainability.",
    deepExplanation: "Step 1 — Understand the topic.\nUnderstand the core mechanism first, then use the idiomatic React/TypeScript form.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction example(value: string): string {\n  return value.trim();\n}\n\nconsole.log(\n  example(\" React \")\n);\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nconsole.log(\n  \" React \".trim()\n);\n```\n\nStep 5 — Example result:\n```text\nReact\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Child to parent communication**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-44",
    number: "REACTFUND-M3-44",
    title: "Sibling communication",
    difficulty: "Medium",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "Sibling communication is a React concept that should be understood at three levels: the public API, the rendering behavior it triggers, and the production trade-offs involved in accessibility, performance, testing and maintainability.",
    deepExplanation: "Step 1 — Understand the topic.\nUnderstand the core mechanism first, then use the idiomatic React/TypeScript form.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction example(value: string): string {\n  return value.trim();\n}\n\nconsole.log(\n  example(\" React \")\n);\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nconsole.log(\n  \" React \".trim()\n);\n```\n\nStep 5 — Example result:\n```text\nReact\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Sibling communication**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-45",
    number: "REACTFUND-M3-45",
    title: "Composition patterns",
    difficulty: "Hard",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "Composition patterns is a React concept that should be understood at three levels: the public API, the rendering behavior it triggers, and the production trade-offs involved in accessibility, performance, testing and maintainability.",
    deepExplanation: "Step 1 — Understand the topic.\nUnderstand the core mechanism first, then use the idiomatic React/TypeScript form.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\nfunction example(value: string): string {\n  return value.trim();\n}\n\nconsole.log(\n  example(\" React \")\n);\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nconsole.log(\n  \" React \".trim()\n);\n```\n\nStep 5 — Example result:\n```text\nReact\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Composition patterns**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-46",
    number: "REACTFUND-M3-46",
    title: "Component props API design",
    difficulty: "Easy",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nProps are read-only inputs supplied by the parent; use composition/callbacks instead of mutating them.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype ButtonProps = {\n  label: string;\n  disabled?: boolean;\n};\n\nfunction Button(props: ButtonProps): string {\n  return props.disabled\n    ? \"disabled\"\n    : props.label;\n}\n\nconsole.log(\n  Button({ label: \"Save\" })\n);\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction Button({\n  label,\n  disabled = false,\n}: ButtonProps) {\n  return (\n    <button disabled={disabled}>\n      {label}\n    </button>\n  );\n}\n```\n\nStep 5 — Example result:\n```text\nSave\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Component props API design**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-47",
    number: "REACTFUND-M3-47",
    title: "Component boundaries",
    difficulty: "Medium",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Component boundaries**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-48",
    number: "REACTFUND-M3-48",
    title: "Smart and dumb components",
    difficulty: "Hard",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Smart and dumb components**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-49",
    number: "REACTFUND-M3-49",
    title: "Headless component concept",
    difficulty: "Easy",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Headless component concept**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-50",
    number: "REACTFUND-M3-50",
    title: "Compound components",
    difficulty: "Medium",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Compound components**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-51",
    number: "REACTFUND-M3-51",
    title: "Controlled components",
    difficulty: "Hard",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Controlled components**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-52",
    number: "REACTFUND-M3-52",
    title: "Uncontrolled components",
    difficulty: "Easy",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Uncontrolled components**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-53",
    number: "REACTFUND-M3-53",
    title: "Component testing",
    difficulty: "Medium",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Component testing**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-54",
    number: "REACTFUND-M3-54",
    title: "Component naming",
    difficulty: "Hard",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Component naming**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---"],
  },
  {
    id: "reactfund-m3-55",
    number: "REACTFUND-M3-55",
    title: "Enterprise component architecture",
    difficulty: "Easy",
    experienceLevel: "0\u20132 Years",
    companies: ["Google", "Meta", "Amazon", "Microsoft", "Netflix", "Adobe", "Atlassian", "Stripe", "Uber", "Flipkart", "Zoho"],
    category: "Components",
    expectedAnswer: "A React component describes UI and behavior as a reusable unit. Good component boundaries expose a small, stable API and keep ownership of state close to where it is needed.",
    deepExplanation: "Step 1 — Understand the topic.\nA good component exposes a small API, keeps render pure, and owns state at the narrowest useful boundary.\n\nStep 2 — Easy method.\nTrace one small example from input/props/state to rendered output.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\n```ts\ntype User = { name: string };\n\nfunction UserCard({ name }: User): string {\n  return `User: ${name}`;\n}\n\nconsole.log(UserCard({ name: \"Ada\" }));\n```\n\nStep 4 — WITH BUILT-IN / PRACTICAL React/TypeScript:\n```tsx\nfunction UserCard({ name }: User) {\n  return <article>User: {name}</article>;\n}\n```\n\nStep 5 — Example result:\n```text\nUser: Ada\n```\n\nStep 6 — Edge cases:\nCheck undefined props, conditional rendering, stale closures, unnecessary renders, cleanup, accessibility, and list keys where relevant.\n\nStep 7 — Senior interview takeaway:\nExplain the rendering rule, ownership boundary, complexity/performance impact, testing strategy, and production trade-off.",
    productionExample: "Production example for **Enterprise component architecture**: validate the behavior with a focused component test, an accessibility check where applicable, and a performance check for large/interactive UIs.",
    bestPractices: ["Keep render logic pure.", "Co-locate state with the components that own it.", "Prefer composition over inheritance.", "Use stable list keys.", "Build accessible HTML first.", "Measure performance rather than guessing."],
    tradeOffs: "Advantages: Declarative UI model; Component composition; Predictable one-way data flow; Strong ecosystem and tooling. Disadvantages: Abstractions can become over-engineered.; Poor state ownership can create unnecessary re-renders.; Client bundles can grow without code-splitting discipline..",
    commonMistakes: ["Mutating state directly.", "Using array indexes as keys for dynamic lists.", "Putting every value into Context.", "Using effects to derive values that can be calculated during render.", "Premature use of `useMemo` / `useCallback`.", "Interview trap: Saying React simply 'updates the DOM' without explaining reconciliation and commit.", "Interview trap: Treating the Virtual DOM as a literal browser DOM copy.", "Interview trap: Confusing render phase with commit phase.", "Interview trap: Assuming every state update immediately changes the DOM synchronously."],
    followUpQuestions: ["What causes a render?", "What work happens during render versus commit?", "How would you measure the performance impact?", "How does accessibility change your implementation?", "How would you test the behavior?", "How does this design scale across an enterprise codebase?"],
    relatedTopics: ["JSX", "Components", "Props", "State", "Hooks", "Context", "Keys", "Reconciliation", "Fiber", "Suspense", "React Router", "Testing Library", "Accessibility", "Performance", "---", "# Module 4 – Props", "**Questions 56–70**"],
  },
];

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};

export const MOCK_REACT_FUNDAMENTALS_MODULE3_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'React Fundamentals',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
