// Derived from frontend/src/document/Part_6_Browser_Internals_Web_APIs_Master_Handbook.md (Module 2 — HTML Parsing).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question, Execution/Flow, Advanced Production Example, Performance/Security Notes,
// Best Practices, Advantages/Disadvantages, Common Mistakes/Interview Traps, Senior
// Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

const COMPANIES = [
  "Google",
  "Meta",
  "Amazon",
  "Microsoft",
  "Netflix",
  "Adobe",
  "Atlassian",
  "Stripe",
  "Uber",
  "Flipkart",
  "Zoho"
];

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "jsbrowser-m2-21",
    number: "JSBROWSER-M2-21",
    title: "HTML parser",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "HTML parsing turns the byte/character stream of an HTML document into tokens and then a DOM tree using the HTML parsing algorithm. Parsing is incremental and can be affected by parser-inserted scripts, malformed markup recovery, document.write and resource discovery. It is mainly a browser-engine operation, not a React feature.",
    deepExplanation: "text\nHTML bytes\n   ↓\ncharacter decoding\n   ↓\nHTML tokenization\n   ↓\ntree construction\n   ↓\nDOM\n   ↓\nstyle/layout/paint\n```\n\nThe parser may pause when it encounters parser-blocking classic scripts, while the browser can continue discovering resources through speculative/preload scanning.",
    productionExample: "A production SSR page is optimized by reducing parser-blocking resources, keeping HTML reasonably compact, prioritizing critical CSS/content and ensuring scripts use appropriate loading semantics. DevTools Performance and Network traces are used to verify whether parsing is actually on the critical path.",
    bestPractices: ["Keep HTML valid and semantically structured.","Minimize parser-blocking scripts.","Use appropriate script loading semantics.","Avoid unnecessary `document.write`.","Measure parsing and critical rendering path cost."],
    tradeOffs: "Faster parsing can improve first render, but aggressively shrinking/streaming markup must be balanced against readability, caching and server complexity.",
    commonMistakes: ["Thinking HTML is parsed only after the entire response arrives.","Treating tokenization and DOM construction as the same step.","Ignoring parser-blocking scripts.","Assuming React components are what the browser parser consumes."],
    followUpQuestions: ["How does the HTML tokenizer differ from tree construction?","What pauses the parser?","How does speculative scanning improve loading?"],
    relatedTopics: ["HTML tokenizer","Tree construction","DOM","Parser-blocking scripts","Preload scanner","Critical rendering path"],
  },
  {
    id: "jsbrowser-m2-22",
    number: "JSBROWSER-M2-22",
    title: "Tokenization",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "HTML tokenization converts characters from the input stream into tokens such as start tags, end tags, comments, doctype tokens and character tokens. The tokenizer has state-dependent behavior, including data, tag-open, attribute and script-data states. The resulting tokens are consumed by the tree-construction stage.",
    deepExplanation: "text\n\"<div class='card'>Hello</div>\"\n        ↓\nStartTag(div)\n  Attribute(class=\"card\")\nCharacter(\"Hello\")\nEndTag(div)\n```\n\nTokenization is not the DOM; it is the lexical stage that feeds the tree builder.",
    productionExample: "A malformed or unusual HTML payload can cause different tokenization states than developers expect. Browser compatibility work therefore relies on the standard parsing algorithm rather than assuming XML-style parsing.",
    bestPractices: ["Understand tokenizer state concepts.","Do not parse HTML with regular expressions.","Use standards-based HTML serialization/parsing APIs when programmatically manipulating markup.","Test malformed input when building HTML tooling."],
    tradeOffs: "Precise parsing guarantees broad interoperability, but the HTML parsing algorithm is complex because it preserves web compatibility with historical markup.",
    commonMistakes: ["Saying HTML parsing is XML parsing.","Confusing tokens with DOM nodes.","Assuming every `<` starts a normal tag state.","Using regex to parse arbitrary HTML."],
    followUpQuestions: ["What happens to attributes during tokenization?","Why does script data have special states?","How does tokenization feed tree construction?"],
    relatedTopics: ["HTML tokenizer","Tree builder","Script data state","Attributes","DOM"],
  },
  {
    id: "jsbrowser-m2-23",
    number: "JSBROWSER-M2-23",
    title: "Tree construction",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "Tree construction consumes HTML tokens and builds the DOM using insertion modes and parser state. It handles implied elements, foster parenting for table markup, template content and error recovery. Therefore the resulting DOM is not always a literal one-to-one representation of malformed source text.",
    deepExplanation: "html\n<table>\n  <tr><td>A</td></tr>\n</table>\n```\n\nThe tree builder uses insertion modes to place nodes in the correct DOM structure. For malformed markup, it can create or close elements implicitly according to the HTML parsing algorithm.",
    productionExample: "A browser compatibility bug caused by malformed table markup is diagnosed by inspecting the actual DOM rather than the original HTML string. The fix is to produce valid semantic markup and verify the resulting tree in DevTools.",
    bestPractices: ["Think in parser states/insertion modes.","Inspect the resulting DOM when debugging malformed HTML.","Prefer valid semantic HTML.","Test templates that generate tables/forms dynamically."],
    tradeOffs: "The algorithm's error recovery maximizes web compatibility, but it is complex and can surprise developers who expect source markup to map directly to the DOM.",
    commonMistakes: ["Assuming source indentation equals DOM hierarchy.","Ignoring implied elements.","Confusing tree construction with CSS render-tree construction.","Expecting malformed markup to fail like XML."],
    followUpQuestions: ["What is an insertion mode?","How are table parsing errors handled?","What is foster parenting?"],
    relatedTopics: ["DOM construction","Insertion modes","Table parsing","HTML error recovery"],
  },
  {
    id: "jsbrowser-m2-24",
    number: "JSBROWSER-M2-24",
    title: "DOM construction",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "DOM construction is the creation/update of the document tree from parsed HTML tokens. The DOM represents document structure and relationships; it is not the same as the CSSOM, render tree or painted output. JavaScript can later mutate the DOM, causing style/layout/paint work as necessary.",
    deepExplanation: "js\nconst root = document.createElement(\"main\");\nconst heading = document.createElement(\"h1\");\n\nheading.textContent = \"Dashboard\";\nroot.appendChild(heading);\n\ndocument.body.appendChild(root);\n```\n\nThis creates DOM nodes directly. The browser may then perform style and layout work because the document changed.",
    productionExample: "A production application avoids constructing thousands of DOM nodes at once when virtualization can reduce the visible set. The team profiles DOM mutation and layout cost rather than treating every DOM operation as equally expensive.",
    bestPractices: ["Keep DOM size reasonable.","Use semantic elements.","Batch large mutations where practical.","Avoid unnecessary DOM reads/writes that force synchronous layout.","Virtualize large collections when appropriate."],
    tradeOffs: "DOM provides a powerful document model, but a large mutable tree consumes memory and can increase style/layout/paint cost.",
    commonMistakes: ["Calling the DOM the render tree.","Assuming any DOM change always triggers full layout.","Keeping huge hidden DOM subtrees permanently mounted.","Mixing read/write patterns that cause layout thrashing."],
    followUpQuestions: ["DOM versus CSSOM?","What happens after a DOM mutation?","How does DOM size affect performance?"],
    relatedTopics: ["DOM","CSSOM","Style calculation","Layout","Paint","Virtualization"],
  },
  {
    id: "jsbrowser-m2-25",
    number: "JSBROWSER-M2-25",
    title: "DOCTYPE processing",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "The HTML doctype declaration (`<!doctype html>`) tells the browser to use standards mode for HTML documents. Modern HTML uses the simple HTML5 doctype. The browser's standards/quirks decision affects how CSS/layout behavior is interpreted.",
    deepExplanation: "html\n<!doctype html>\n<html lang=\"en\">\n  <head>...</head>\n  <body>...</body>\n</html>\n```\n\nThe modern doctype is intentionally simple and prevents accidental quirks-mode behavior in normal documents.",
    productionExample: "A legacy application rendering unexpectedly different box-model/layout results is found to be missing a standards doctype. Adding `<!doctype html>` and validating the document removes quirks-mode behavior.",
    bestPractices: ["Always use the HTML5 doctype.","Validate generated document shells.","Understand standards versus quirks mode when maintaining legacy pages.","Do not use obsolete doctype strings for modern applications."],
    tradeOffs: "Standards mode provides predictable modern behavior. Supporting legacy quirks-mode pages can be necessary but increases CSS/layout compatibility complexity.",
    commonMistakes: ["Saying the doctype is an HTML element.","Assuming the doctype itself creates the DOM tree.","Using a legacy XHTML doctype without understanding its mode implications."],
    followUpQuestions: ["What is quirks mode?","How can doctype affect CSS behavior?","How do you detect document compatibility mode?"],
    relatedTopics: ["DOCTYPE","Standards mode","Quirks mode","HTML document mode"],
  },
  {
    id: "jsbrowser-m2-26",
    number: "JSBROWSER-M2-26",
    title: "Parser blocking scripts",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "A classic parser-inserted script without `async` or `defer` can block HTML parsing while the browser fetches and executes it. This exists because such scripts can call `document.write()` and inspect/mutate the parser's current document state. Blocking scripts therefore have direct critical-path performance impact.",
    deepExplanation: "html\n<head>\n  <script src=\"/app.js\"></script>\n</head>\n```\n\nThe parser can pause around this script until the classic script is fetched and executed. By contrast, `defer` downloads in parallel and executes after parsing, while `async` downloads in parallel and executes as soon as ready.",
    productionExample: "A production app moved non-critical third-party scripts away from the parser-blocking path and converted eligible application scripts to `defer`. This reduced document parsing delay and improved first-contentful rendering on slower networks.",
    bestPractices: ["Avoid unnecessary parser-blocking scripts.","Use `defer` for ordered, DOM-dependent scripts that can wait for parsing.","Use `async` for independent scripts.","Measure script discovery/execution in Network and Performance panels.","Keep critical inline script very small."],
    tradeOffs: "Blocking scripts preserve historical document.write/script semantics but can delay parsing and rendering. Async scripts reduce blocking but can execute in unpredictable order.",
    commonMistakes: ["Saying `async` preserves script order.","Saying `defer` executes before parsing finishes.","Ignoring parser-blocking third-party scripts.","Loading analytics synchronously in the critical head."],
    followUpQuestions: ["async versus defer?","Why does `document.write` complicate parsing?","When is an inline script justified?"],
    relatedTopics: ["Script loading","async","defer","document.write","Critical rendering path"],
  },
  {
    id: "jsbrowser-m2-27",
    number: "JSBROWSER-M2-27",
    title: "async scripts",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "External classic scripts with `async` are fetched in parallel with HTML parsing and execute as soon as they are ready, potentially before the document has finished parsing. Async scripts do not preserve execution order with one another and should be used for independent scripts.",
    deepExplanation: "html\n<script async src=\"/analytics.js\"></script>\n<script async src=\"/experiment.js\"></script>\n```\n\nThe two scripts may complete and execute in a different order from their source order.",
    productionExample: "A site loads independent analytics and experimentation scripts with `async` so they do not block document parsing. The scripts do not depend on one another or on a fully built DOM.",
    bestPractices: ["Use async only for independent scripts.","Do not rely on execution order among async scripts.","Handle failure gracefully.","Keep third-party script count small.","Measure third-party impact on CPU and network."],
    tradeOffs: "Async reduces parser blocking but introduces ordering nondeterminism. It is inappropriate when scripts depend on each other or on DOM completion.",
    commonMistakes: ["Using async for dependent libraries.","Assuming async means after DOMContentLoaded.","Adding many async third-party scripts without measuring their aggregate cost."],
    followUpQuestions: ["async versus defer?","When does an async script execute relative to DOM parsing?","What if two async scripts depend on each other?"],
    relatedTopics: ["async","defer","Script execution","Third-party scripts"],
  },
  {
    id: "jsbrowser-m2-28",
    number: "JSBROWSER-M2-28",
    title: "defer scripts",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "Classic external scripts with `defer` download in parallel with HTML parsing, preserve document order relative to other deferred scripts, and execute after the document has been parsed but before DOMContentLoaded. They are a strong default for scripts that need the DOM but do not need to block parsing.",
    deepExplanation: "html\n<script defer src=\"/vendor.js\"></script>\n<script defer src=\"/app.js\"></script>\n```\n\n`vendor.js` executes before `app.js`, and both can be fetched while parsing continues.",
    productionExample: "A production application uses deferred bundles for client behavior that depends on parsed markup. This keeps HTML parsing progressing while preserving predictable execution order.",
    bestPractices: ["Prefer defer for ordered DOM-dependent classic scripts.","Avoid mixing unnecessary parser-blocking scripts with deferred scripts.","Understand module scripts separately: module scripts are deferred by default.","Measure DOMContentLoaded and script evaluation cost."],
    tradeOffs: "`defer` improves the critical path while retaining ordered execution, but the scripts still consume CPU before DOMContentLoaded and can delay interactivity if very large.",
    commonMistakes: ["Saying defer executes immediately when downloaded.","Assuming defer preserves order with async scripts.","Ignoring execution cost after parsing completes."],
    followUpQuestions: ["defer versus module scripts?","When does DOMContentLoaded fire?","How do deferred scripts affect interactivity?"],
    relatedTopics: ["defer","DOMContentLoaded","Module scripts","Script scheduling"],
  },
  {
    id: "jsbrowser-m2-29",
    number: "JSBROWSER-M2-29",
    title: "Preload scanner",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "The preload scanner is an optimization that scans ahead in HTML while the main HTML parser may be blocked, discovering subresources such as stylesheets, images and scripts so their network requests can begin earlier. It is an implementation optimization, not an application API.",
    deepExplanation: "html\n<link rel=\"stylesheet\" href=\"/app.css\">\n<img src=\"/hero.webp\" alt=\"Hero\">\n<script src=\"/app.js\"></script>\n```\n\nThe scanner can discover these URLs before the primary parser reaches all of them, reducing network-discovery delay.",
    productionExample: "A performance trace reveals the hero image was discovered late because it was only referenced from deeply generated markup. The team uses an appropriate preload/priority strategy only after confirming discovery latency is the bottleneck.",
    bestPractices: ["Keep resource references discoverable in the HTML when possible.","Use preload carefully for truly critical resources.","Avoid preloading resources that may not be used.","Use correct `as`, type and crossorigin attributes when applicable.","Validate resource priority with DevTools."],
    tradeOffs: "Speculative discovery can improve network parallelism, but excessive preloading competes with critical resources and can worsen performance.",
    commonMistakes: ["Calling preload scanner a JavaScript API.","Preloading everything important-looking.","Using incorrect preload metadata.","Assuming preload always improves LCP."],
    followUpQuestions: ["Preload versus prefetch?","How does speculative scanning relate to parser blocking?","How do you verify resource priority?"],
    relatedTopics: ["Preload scanner","Speculative parsing","Preload","Resource priority","Critical rendering path"],
  },
  {
    id: "jsbrowser-m2-30",
    number: "JSBROWSER-M2-30",
    title: "Speculative parsing",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "Speculative parsing refers to browser work that predicts or discovers future resource loads while the main parser is blocked or before they are immediately needed. In practice, speculative scanners can discover scripts, styles and images early without constructing the authoritative DOM ahead of the parser.",
    deepExplanation: "text\nMain parser:  ----blocked by script----\nSpeculative scanner:  discovers next CSS/image/script\n                                 ↓\n                           network request starts\n```\n\nThe browser can start fetching a discovered resource before the main parser resumes.",
    productionExample: "A page with a parser-blocking script still downloads later CSS/images early because the speculative scanner discovers them. DevTools confirms earlier resource start times even though script execution remains on the critical path.",
    bestPractices: ["Let browser resource discovery work naturally before adding manual hints.","Use preload only for proven critical resources.","Avoid markup patterns that hide critical URLs from discovery.","Measure discovery-to-download timing."],
    tradeOffs: "Speculation can shorten resource discovery time but cannot eliminate the CPU cost of parser-blocking script execution.",
    commonMistakes: ["Saying speculative parsing builds the final DOM.","Confusing speculative scanning with `<link rel=prefetch>`.","Adding resource hints without measuring."],
    followUpQuestions: ["Preload scanner versus speculative parser?","What cannot speculative scanning safely execute?","How does it affect waterfalls?"],
    relatedTopics: ["Speculative parsing","Preload scanner","Resource discovery","Network waterfalls"],
  },
  {
    id: "jsbrowser-m2-31",
    number: "JSBROWSER-M2-31",
    title: "Malformed HTML recovery",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "Browsers implement the HTML standard's error-recovery rules so malformed markup still produces a deterministic DOM. This includes implied end tags, insertion modes, adoption-agency behavior for misnested formatting elements and special table parsing rules. Different source text can therefore result in a valid-looking DOM.",
    deepExplanation: "html\n<p>One<div>Two</div>Three\n```\n\nThe parser does not simply preserve the textual nesting; it applies HTML parsing rules that can implicitly close or reposition elements.",
    productionExample: "A production bug appears only in one page template because invalid nested elements generate a surprising DOM. The team inspects the browser's parsed DOM, fixes the server-side template and adds HTML validation to CI.",
    bestPractices: ["Generate valid HTML.","Inspect actual DOM for malformed legacy markup.","Use HTML validators/linting in templates.","Be especially careful with table/form nesting.","Avoid relying on browser recovery quirks."],
    tradeOffs: "Recovery rules preserve web compatibility but make malformed source harder to reason about. Fixing the source is preferable to depending on recovery behavior.",
    commonMistakes: ["Expecting browsers to reject malformed HTML.","Treating parser recovery as identical to XML recovery.","Debugging only the HTML string instead of the DOM."],
    followUpQuestions: ["What is the adoption agency algorithm?","Why is table parsing special?","How can malformed HTML affect accessibility?"],
    relatedTopics: ["HTML parsing","Tree construction","Error recovery","DOM","Tables"],
  },
  {
    id: "jsbrowser-m2-32",
    number: "JSBROWSER-M2-32",
    title: "Parser-inserted scripts",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "A parser-inserted script is a script element inserted as part of HTML parsing rather than dynamically inserted by script. This matters because parser-inserted classic scripts participate in the parser's blocking rules, while dynamically inserted scripts have different fetching/execution behavior unless configured otherwise.",
    deepExplanation: "html\n<script src=\"/app.js\"></script>\n```\n\nThis is parser-inserted by the HTML parser. Compare with:\n\n```js\nconst script = document.createElement(\"script\");\nscript.src = \"/analytics.js\";\ndocument.head.appendChild(script);\n```\n\nThe dynamic script follows dynamic insertion semantics rather than classic parser-blocking semantics.",
    productionExample: "A legacy integration depended on parser-inserted script order, so the team migrated it carefully instead of simply adding `async` or converting it to dynamic insertion without checking initialization dependencies.",
    bestPractices: ["Understand script insertion mode before changing loading attributes.","Avoid hidden dependency on parser state/document.write.","Prefer explicit dependency/loading strategies.","Test execution order when migrating legacy scripts."],
    tradeOffs: "Parser-inserted scripts can support legacy bootstrapping semantics but often create critical-path coupling. Dynamic insertion can improve flexibility but introduces explicit loading/error handling.",
    commonMistakes: ["Assuming all script elements behave the same.","Changing parser-inserted scripts to async without checking dependencies.","Forgetting dynamic script loading can change execution timing."],
    followUpQuestions: ["Parser-inserted versus dynamically inserted script?","How does `async` interact with parser insertion?","Why does document.write matter?"],
    relatedTopics: ["Parser-inserted scripts","Dynamic scripts","async","defer","document.write"],
  },
  {
    id: "jsbrowser-m2-33",
    number: "JSBROWSER-M2-33",
    title: "Document.write",
    difficulty: "Easy",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "`document.write()` writes markup into the document stream while it is being parsed. When used during parsing it can alter the input stream and block progress; when called after the document is loaded it may implicitly replace the document in surprising ways. It is therefore generally avoided in modern application code.",
    deepExplanation: "html\n<script>\n  document.write(\"<p>Legacy content</p>\");\n</script>\n```\n\nThis can inject content into the parser's current stream. The behavior is tightly coupled to document parsing and makes resource scheduling and DOM reasoning harder.",
    productionExample: "A legacy third-party widget using document.write causes slow mobile rendering and parser blocking. The team replaces it with an asynchronous loader that creates DOM nodes explicitly and reports load failure.",
    bestPractices: ["Avoid document.write in modern application code.","Replace legacy integrations incrementally.","Use DOM APIs or framework rendering instead.","Audit third-party scripts for parser-blocking behavior.","Test on slow networks and mobile devices."],
    tradeOffs: "document.write can support old bootstrap patterns but harms composability and performance. Replacing it often requires vendor/integration changes.",
    commonMistakes: ["Using document.write in React components.","Assuming it just appends to the end of the DOM.","Ignoring its interaction with parser blocking."],
    followUpQuestions: ["Why can document.write block parsing?","What happens when it runs after load?","How would you replace a legacy widget that requires it?"],
    relatedTopics: ["document.write","HTML parser","Parser blocking","Legacy scripts"],
  },
  {
    id: "jsbrowser-m2-34",
    number: "JSBROWSER-M2-34",
    title: "HTML parsing performance",
    difficulty: "Medium",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "HTML parsing performance depends on document size, tokenization/tree-construction work, parser-blocking scripts, resource discovery, DOM size and server delivery. The critical-path question is not simply 'is parsing slow?' but whether parsing delays useful content or resource discovery.",
    deepExplanation: "text\nResponse bytes\n   ↓\nHTML parse + DOM construction\n   ↘ resource discovery\n   ↓\nCSS/JS/layout/rendering\n```\n\nA large document with synchronous scripts can make parsing itself and parser stalls visible in the critical path.",
    productionExample: "A production page reduced initial HTML and removed synchronous third-party scripts. The resulting trace showed earlier CSS/image discovery and less main-thread blocking on low-end mobile devices.",
    bestPractices: ["Measure document size and parse time.","Reduce parser-blocking JavaScript.","Keep critical HTML focused.","Stream where the stack supports it.","Measure resource discovery and LCP together."],
    tradeOffs: "Reducing HTML can lower parsing/memory cost but may require more client requests. Streaming can improve progressive delivery while increasing server/rendering complexity.",
    commonMistakes: ["Optimizing gzip size only.","Ignoring parser-blocking scripts.","Assuming HTML parse is the only cause of slow first paint."],
    followUpQuestions: ["How would you measure HTML parse cost?","What is parse-blocking versus network-blocking?","How do server streaming and parsing interact?"],
    relatedTopics: ["HTML parser","DOM","Preload scanner","LCP","Streaming","Performance"],
  },
  {
    id: "jsbrowser-m2-35",
    number: "JSBROWSER-M2-35",
    title: "HTML parsing interview scenario",
    difficulty: "Hard",
    experienceLevel: "0–2 Years",
    category: "HTML Parsing",
    expectedAnswer: "For a senior HTML-parsing scenario, reason from the critical rendering path: response delivery → character decoding → tokenization/tree construction → resource discovery → script/CSS dependencies → DOM/CSSOM → style/layout/paint. Explain which resource is blocking and what evidence you would collect before changing code.",
    deepExplanation: "text\nScenario:\n- Mobile LCP is 4.5s.\n- HTML arrives quickly.\n- A 220 KB synchronous script is in <head>.\n- Hero image starts late.\n- DOM parsing stalls during script evaluation.\n\nDiagnosis:\n1. Move eligible script to defer.\n2. Verify hero discovery/priority.\n3. Re-run a mobile trace.\n4. Check CPU as well as network timing.\n```\n\nThe senior answer connects parser behavior to measurable user impact.",
    productionExample: "A production incident shows good TTFB but poor LCP. DevTools reveals head-script evaluation and late hero discovery. The fix targets the parser critical path instead of tuning unrelated React renders.",
    bestPractices: ["Start with traces.","Differentiate server, network and parser stalls.","Check script loading semantics.","Verify resource discovery and priority.","Test on representative mobile hardware/network."],
    tradeOffs: "Parser optimization can improve first-render metrics, but changing script order may break initialization dependencies. Every loading change should be verified with functional and performance tests.",
    commonMistakes: ["Jumping straight to `async`.","Ignoring script dependencies.","Fixing only desktop measurements.","Assuming good TTFB means the page is fast."],
    followUpQuestions: ["How would you distinguish TTFB from parser blocking?","Would you use async or defer here?","How would you prove the hero is now discovered earlier?"],
    relatedTopics: ["HTML parsing","Script loading","LCP","Network waterfall","Critical rendering path"],
  },

];

export const MOCK_BROWSER_INTERNALS_MODULE2_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: COMPANIES,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Browser/Web APIs',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));