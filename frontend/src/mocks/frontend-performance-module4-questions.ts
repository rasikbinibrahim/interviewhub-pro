// Derived from frontend/src/document/Part_11_Frontend_Performance_Optimization_Master_Handbook.md (Module 4 — JavaScript Performance).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "perf-m4-61",
    number: "PERF-M4-61",
    title: "Event Loop",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Event Loop** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-62",
    number: "PERF-M4-62",
    title: "Long Tasks",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Long Tasks** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-63",
    number: "PERF-M4-63",
    title: "Garbage Collection",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Garbage Collection** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-64",
    number: "PERF-M4-64",
    title: "Memory leaks",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Memory leaks** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nimport { memo, useMemo } from \"react\";\n\ntype Props = { items: readonly string[] };\n\nexport const ItemList = memo(function ItemList({ items }: Props) {\n  const visible = useMemo(() => items.slice(0, 50), [items]);\n  return (\n    <ul>\n      {visible.map((item) => <li key={item}>{item}</li>)}\n    </ul>\n  );\n});\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-65",
    number: "PERF-M4-65",
    title: "Debounce",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Debounce** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nexport function debounce<T extends (...args: any[]) => void>(\n  fn: T,\n  delayMs: number,\n) {\n  let timer: ReturnType<typeof setTimeout> | undefined;\n  return (...args: Parameters<T>) => {\n    if (timer) clearTimeout(timer);\n    timer = setTimeout(() => fn(...args), delayMs);\n  };\n}\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-66",
    number: "PERF-M4-66",
    title: "Throttle",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Throttle** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-67",
    number: "PERF-M4-67",
    title: "requestAnimationFrame",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**requestAnimationFrame** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-68",
    number: "PERF-M4-68",
    title: "requestIdleCallback",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**requestIdleCallback** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-69",
    number: "PERF-M4-69",
    title: "Web Workers",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Web Workers** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-70",
    number: "PERF-M4-70",
    title: "Memoization",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Memoization** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nimport { memo, useMemo } from \"react\";\n\ntype Props = { items: readonly string[] };\n\nexport const ItemList = memo(function ItemList({ items }: Props) {\n  const visible = useMemo(() => items.slice(0, 50), [items]);\n  return (\n    <ul>\n      {visible.map((item) => <li key={item}>{item}</li>)}\n    </ul>\n  );\n});\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-71",
    number: "PERF-M4-71",
    title: "Efficient loops",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Efficient loops** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-72",
    number: "PERF-M4-72",
    title: "Async scheduling",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Async scheduling** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-73",
    number: "PERF-M4-73",
    title: "Scheduler.postTask",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Scheduler.postTask** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-74",
    number: "PERF-M4-74",
    title: "Task prioritization",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Task prioritization** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-75",
    number: "PERF-M4-75",
    title: "Main-thread blocking",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Main-thread blocking** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-76",
    number: "PERF-M4-76",
    title: "Code execution cost",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Code execution cost** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-77",
    number: "PERF-M4-77",
    title: "Algorithmic complexity",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Algorithmic complexity** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-78",
    number: "PERF-M4-78",
    title: "JIT considerations",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**JIT considerations** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-79",
    number: "PERF-M4-79",
    title: "Performance profiling",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Performance profiling** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-80",
    number: "PERF-M4-80",
    title: "JavaScript optimization",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**JavaScript optimization** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---","# Module 5 – React Performance","**Questions 81–100**"],
  },
];

export const MOCK_FRONTEND_PERFORMANCE_MODULE4_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Performance',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
