// Derived from frontend/src/document/Part_11_Frontend_Performance_Optimization_Master_Handbook.md (Module 4 — JavaScript Performance).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "perf-m4-61",
    number: "PERF-M4-61",
    title: "Event Loop",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Event Loop** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Event Loop** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Event Loop when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Event Loop when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-62",
    number: "PERF-M4-62",
    title: "Long Tasks",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Long Tasks** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Long Tasks** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Long Tasks when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Long Tasks when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-63",
    number: "PERF-M4-63",
    title: "Garbage Collection",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Garbage Collection** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Garbage Collection** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Garbage Collection when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Garbage Collection when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-64",
    number: "PERF-M4-64",
    title: "Memory leaks",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Memory leaks** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Memory leaks** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Memory leaks when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Memory leaks when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-65",
    number: "PERF-M4-65",
    title: "Debounce",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Debounce** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Debounce** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Debounce when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Debounce when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-66",
    number: "PERF-M4-66",
    title: "Throttle",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Throttle** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Throttle** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Throttle when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Throttle when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-67",
    number: "PERF-M4-67",
    title: "requestAnimationFrame",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**requestAnimationFrame** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**requestAnimationFrame** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse requestAnimationFrame when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use requestAnimationFrame when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-68",
    number: "PERF-M4-68",
    title: "requestIdleCallback",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**requestIdleCallback** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**requestIdleCallback** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse requestIdleCallback when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use requestIdleCallback when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-69",
    number: "PERF-M4-69",
    title: "Web Workers",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Web Workers** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Web Workers** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Web Workers when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Web Workers when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-70",
    number: "PERF-M4-70",
    title: "Memoization",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Memoization** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Memoization** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Memoization when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Memoization when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-71",
    number: "PERF-M4-71",
    title: "Efficient loops",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Efficient loops** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Efficient loops** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Efficient loops when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Efficient loops when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-72",
    number: "PERF-M4-72",
    title: "Async scheduling",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Async scheduling** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Async scheduling** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Async scheduling when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Async scheduling when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-73",
    number: "PERF-M4-73",
    title: "Scheduler.postTask",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Scheduler.postTask** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Scheduler.postTask** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Scheduler.postTask when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Scheduler.postTask when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-74",
    number: "PERF-M4-74",
    title: "Task prioritization",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Task prioritization** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Task prioritization** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Task prioritization when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Task prioritization when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-75",
    number: "PERF-M4-75",
    title: "Main-thread blocking",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Main-thread blocking** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Main-thread blocking** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Main-thread blocking when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Main-thread blocking when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-76",
    number: "PERF-M4-76",
    title: "Code execution cost",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Code execution cost** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Code execution cost** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Code execution cost when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Code execution cost when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-77",
    number: "PERF-M4-77",
    title: "Algorithmic complexity",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Algorithmic complexity** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Algorithmic complexity** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Algorithmic complexity when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Algorithmic complexity when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-78",
    number: "PERF-M4-78",
    title: "JIT considerations",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**JIT considerations** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**JIT considerations** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse JIT considerations when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use JIT considerations when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-79",
    number: "PERF-M4-79",
    title: "Performance profiling",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**Performance profiling** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**Performance profiling** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse Performance profiling when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use Performance profiling when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m4-80",
    number: "PERF-M4-80",
    title: "JavaScript optimization",
    difficulty: "Easy",
    experienceLevel: "2–5 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "JavaScript Performance",
    expectedAnswer: "**JavaScript optimization** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.",
    deepExplanation: "Step 1 — Understand the concept.\n\n**JavaScript optimization** concerns JavaScript execution cost on the main thread or background work. Explain the scheduling/execution model, the cost of the operation, and the safest way to reduce blocking or unnecessary work.\n\nStep 2 — Easy method:\nIdentify the user-visible symptom, locate the browser/network/build bottleneck, change one variable, measure before and after, and keep the optimization only if it improves the target outcome.\n\nStep 3 — WITHOUT BUILT-IN / CORE TypeScript:\nThe manual example makes the important logic explicit so you can explain the data flow and complexity during an interview.\n\n```ts\nfunction sumRange(\n  count: number,\n): number {\n  let total = 0;\n\n  for (let i = 0; i < count; i += 1) {\n    total += i;\n  }\n\n  return total;\n}\n\nconst result = sumRange(10_000);\n```\n\nStep 4 — WITH BUILT-IN / practical TypeScript:\nUse the native/browser helper when it communicates the same intent more clearly and does not hide the key performance trade-off.\n\n```ts\nconst result = Array\n  .from({ length: 10_000 }, (_, index) => index)\n  .reduce((sum, value) => sum + value, 0);\n```\n\nStep 5 — Production example:\nUse JavaScript optimization when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.\n\nStep 6 — Complexity and measurement:\nState the dominant time/space or network cost, then validate it with profiling, field data, network traces, build analysis, or browser timing tools as appropriate.\n\nStep 7 — Edge cases:\nTest slow networks, low-end devices, large datasets, cache misses, repeated interaction, cold loads, and regression after new dependencies or features.\n\nStep 8 — Senior interview takeaway:\nExplain the bottleneck, the measurement, the chosen optimization, the cost/trade-off, and the guardrail that prevents the regression from returning.",
    productionExample: "Use JavaScript optimization when JavaScript execution is a measured bottleneck; isolate the work, keep it off the critical path when possible, and verify the main-thread impact.",
    bestPractices: ["Measure the user-visible bottleneck before optimizing it.", "Prefer a clear implementation with explicit ownership and measurable budgets.", "Optimize for real devices, networks, and field behavior—not only local development.", "Keep caching, loading, rendering, and memory behavior observable.", "Prevent regressions with automated checks and production monitoring."],
    tradeOffs: "The manual/core approach is more verbose but useful for interview explanation; the built-in/native approach is shorter for production. Any optimization should be retained only when measured improvement justifies its complexity, maintenance, or memory cost.",
    commonMistakes: ["Optimizing a metric that is not the actual user problem.", "Applying an optimization without measuring the before/after impact.", "Trading memory or complexity for speed without documenting the trade-off.", "Testing only a high-end desktop on a fast network.", "Ignoring regressions after new dependencies or features are added."],
    followUpQuestions: ["What user-visible problem does this solve?", "How would you measure the improvement?", "What is the cost before and after?", "What happens on a low-end device or slow network?", "What is the memory trade-off?", "How would you test it?", "How would you prevent regression?", "When would you avoid this optimization?", "What is the simplest alternative?", "How would you explain the trade-off in a senior interview?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---","# Module 5 – React Performance","**Questions 81–100**"],
  },
];

export const MOCK_FRONTEND_PERFORMANCE_MODULE4_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Performance',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));