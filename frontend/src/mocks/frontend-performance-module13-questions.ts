// Derived from frontend/src/document/Part_11_Frontend_Performance_Optimization_Master_Handbook.md (Module 13 — Service Workers & PWA Performance).
// Auto-generated by a one-off parser script from the handbook's per-question template
// (Question/Definition, code examples, Best Practices, Advantages/Disadvantages,
// Common Mistakes/Interview Traps, Senior Follow-up Questions, Related Topics).
// Mirrors the MockTechnicalQuestion shape defined in @/mocks/questions.

import type { MockTechnicalQuestion } from '@/mocks/questions';

interface QuestionSeed {
  id: string;
  number: string;
  title: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  experienceLevel: string;
  companies: string[];
  category: string;
  expectedAnswer: string;
  deepExplanation: string;
  productionExample: string;
  bestPractices: string[];
  tradeOffs: string;
  commonMistakes: string[];
  followUpQuestions: string[];
  relatedTopics: string[];
}

const FREQUENCY_BY_DIFFICULTY: Record<QuestionSeed['difficulty'], number> = {
  Easy: 5,
  Medium: 4,
  Hard: 3,
};


const QUESTION_SEEDS: QuestionSeed[] = [
  {
    id: "perf-m13-221",
    number: "PERF-M13-221",
    title: "Service worker lifecycle",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Service worker lifecycle** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-222",
    number: "PERF-M13-222",
    title: "Install",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Install** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-223",
    number: "PERF-M13-223",
    title: "Activate",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Activate** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-224",
    number: "PERF-M13-224",
    title: "Fetch",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Fetch** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-225",
    number: "PERF-M13-225",
    title: "Cache API",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Cache API** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\n// Immutable, versioned assets can use long-lived caching.\n// HTML generally needs a shorter revalidation policy.\nconst cachePolicy = {\n  html: \"no-cache\",\n  hashedAsset: \"public, max-age=31536000, immutable\",\n};\nconsole.log(cachePolicy);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-226",
    number: "PERF-M13-226",
    title: "Precache",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Precache** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\n// Immutable, versioned assets can use long-lived caching.\n// HTML generally needs a shorter revalidation policy.\nconst cachePolicy = {\n  html: \"no-cache\",\n  hashedAsset: \"public, max-age=31536000, immutable\",\n};\nconsole.log(cachePolicy);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-227",
    number: "PERF-M13-227",
    title: "Runtime caching",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Runtime caching** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-228",
    number: "PERF-M13-228",
    title: "Offline fallback",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Offline fallback** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-229",
    number: "PERF-M13-229",
    title: "Background sync",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Background sync** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-230",
    number: "PERF-M13-230",
    title: "Push",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Push** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-231",
    number: "PERF-M13-231",
    title: "PWA performance",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**PWA performance** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-232",
    number: "PERF-M13-232",
    title: "App shell",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**App shell** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-233",
    number: "PERF-M13-233",
    title: "Navigation preload",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Navigation preload** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-234",
    number: "PERF-M13-234",
    title: "Workbox overview",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Workbox overview** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---"],
  },
  {
    id: "perf-m13-235",
    number: "PERF-M13-235",
    title: "Service worker debugging",
    difficulty: "Hard",
    experienceLevel: "8–12 Years",
    companies: ["Google","Meta","Amazon","Microsoft","Netflix","Adobe","Atlassian","Stripe","Uber","Airbnb"],
    category: "Service Workers & PWA Performance",
    expectedAnswer: "**Service worker debugging** is a frontend performance concern whose value must be evaluated through user impact, browser cost, network cost, memory behavior and measurable production outcomes.",
    deepExplanation: "Example:\n\n```ts\nconst start = performance.now();\n\n// Keep expensive work bounded and measurable.\nconst result = Array.from({ length: 10_000 }, (_, i) => i * 2);\n\nconsole.log(result.length);\nconsole.log(`elapsed ≈ ${(performance.now() - start).toFixed(2)}ms`);\n```\n\nProduction pattern:\n\n```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```\n\nWhy it matters: Directly affects user experience and conversion-sensitive flows. Performance regressions accumulate through dependencies, features and third-party code. Enterprise teams need budgets, ownership and observability—not one-time optimization.",
    productionExample: "```ts\n// Measure before and after an optimization instead of assuming it helps.\nperformance.mark(\"work-start\");\n// ...operation...\nperformance.mark(\"work-end\");\nperformance.measure(\"work\", \"work-start\", \"work-end\");\n\nconsole.table(performance.getEntriesByName(\"work\").map(({ duration }) => ({\n  durationMs: Number(duration.toFixed(2)),\n})));\n```",
    bestPractices: ["Prefer explicit, readable code."],
    tradeOffs: "Advantages: Better responsiveness; Lower bandwidth and CPU usage; Better perceived quality; More resilient experience on constrained devices. Disadvantages: Optimization adds engineering complexity; Aggressive caching can complicate releases; Extra abstraction can increase maintenance cost; Some optimizations trade memory for speed.",
    commonMistakes: ["Measuring only one device/network","Optimizing averages instead of tail percentiles","Fixing bundle size while the real issue is server latency","Removing caching to simplify deployment without measuring the cost"],
    followUpQuestions: ["What metric proves the optimization worked?","What is the user-visible symptom?","What happens on a low-end mobile device?","What is the memory trade-off?","Does the optimization change caching behavior?","How would you prevent regression in CI/CD?","What field-data segment would you monitor?"],
    relatedTopics: ["Core Web Vitals","Browser Rendering","React Performance","Network Optimization","Caching","Memory","Build Tools","RUM","Lighthouse","DevTools","---","# Module 14 – Memory Optimization","**Questions 236–250**"],
  },
];

export const MOCK_FRONTEND_PERFORMANCE_MODULE13_TECHNICAL_QUESTIONS: MockTechnicalQuestion[] = QUESTION_SEEDS.map((seed) => ({
  detail: {
    id: seed.id,
    questionNumber: seed.number,
    title: seed.title,
    difficulty: seed.difficulty,
    companies: seed.companies,
    frequency: FREQUENCY_BY_DIFFICULTY[seed.difficulty],
    category: seed.category,
    part: 'Performance',
    concepts: seed.relatedTopics,
    solved: false,
    attempted: false,
    bookmarked: false,
    questionType: 'technical',
    experienceLevel: seed.experienceLevel,
    question: seed.title,
  },
  answer: {
    expectedAnswer: seed.expectedAnswer,
    deepExplanation: seed.deepExplanation,
    productionExample: seed.productionExample,
    bestPractices: seed.bestPractices,
    tradeOffs: seed.tradeOffs,
    commonMistakes: seed.commonMistakes,
    followUpQuestions: seed.followUpQuestions,
    relatedTopics: seed.relatedTopics,
  },
}));
